
Gemini
Cuộc trò chuyện mới
Tìm kiếm trong các cuộc trò chuyện
Video
Thư viện
Sổ ghi chú mới
2025 Hospital Infection Control and Isolation Compliance Report
Preoperative Chlorhexidine Bathing Protocol
Hỗ trợ Dược Lâm Sàng Bệnh Viện
Power Apps Quality Dashboard Guide
Đánh giá hư hại ống nội soi 30 độ
Cây Bóc Tách Màng Xương Prichard
Nhận dạng dụng cụ phẫu thuật thẩm mỹ
Anios'Clean Excel D: Hóa Chất Y Tế
Thống kê tỷ lệ ĐẠT theo tháng
Thống kê tỷ lệ % theo tháng
KKR: Quỹ Đầu Tư Toàn Cầu Tại Việt Nam
Bộ giải pháp khử khuẩn Tristel
Quản lý giao nhận dụng cụ y tế
Đánh số trang tài liệu quy trình
Thông tin về Phạm Thanh Dũng
Xác định PCI quy trình vận hành máy
Lọc Ngày Hôm Nay Trong Microsoft Forms
Phương Châu Hợp Tác Với TPC
Lưu Trữ Thông Tin Cá Nhân Và Hỗ Trợ
Chia sẻ tài khoản ChatGPT an toàn
Giải thích Siêu âm Thai Giai đoạn Sớm
Phương Châu: CVC thoái vốn, thu hút nhà đầu tư
Chỉnh sửa tên hội đồng quản lý
Chỉnh nền xanh lá thành xanh dương
Nâng cấp tính năng giao nhận dụng cụ
Gemini: Khả năng và Hỗ trợ Công việc
Kiểm tra tình trạng hoạt động Claude
Chỉnh sửa tiêu đề thư mời họp
Giao diện CSSD Hiện đại Hóa
Chưa có tiêu đề
Chuyển đổi trang tính sang hệ thống số
Tích hợp Power Apps, SharePoint, Teams, GitHub
Tìm kiếm "Steve Jobs Việt Nam"
Chỉnh sửa văn bản trên TV
Giải thích kết quả xét nghiệm Beta-hCG
Trò chuyện với Gemini
Hiện chỗ IN BIÊN BẢN GIAO NHẬN đang bị lỗi. Ở phần này bạn mặc định in theo máy in bình thường là được nè và chỗ "Ký xác nhận vào tủ khoa" hiện tại thao tác thì tôi thấy chỉ cần nhấn oki nhận là được chứ hiện tại ko có ký điện tử

Chào anh Hùng,

Nguồn gốc hai lỗi trong hình ảnh:

Lỗi In Biên Bản Giao Nhận: CSS @media print ở file style.css trước đó đang ép cứng toàn bộ các lệnh in về kích thước tem Bixolon (80mm x 50mm) và ép #print-zone có chiều rộng 80mm. Khi in Biên bản Giao nhận (trên máy in HP LaserJet / văn phòng khổ A4), bảng bị bó hẹp lại thành một dải dọc nhỏ giữa trang A4.

Nút "Ký xác nhận vào tủ khoa": Loại bỏ các hộp thoại nhắc/ký tên phức tạp, đổi thành nút bấm "XÁC NHẬN NHẬN ĐỒ" nhanh. Khi bấm chỉ cần xác nhận OK là hệ thống tự động lưu thời gian và cập nhật trạng thái nhận khay vào tủ khoa.

1. Cập nhật style.css (Tách biệt cấu hình in A4 và in Tem Bixolon)
Anh thay toàn bộ file style.css bằng mã bên dưới. Phần @media print đã được điều chỉnh để hỗ trợ cả In Biên Bản khổ A4/A5 và In Tem Bixolon 80x50mm mà không bị đè CSS lên nhau:

CSS
/* =========================================================================
   1. CẤU HÌNH CƠ BẢN & GIAO DIỆN CHUNG (CSSD PHUONG NAM HOSPITAL)
   ========================================================================= */
html, body { 
  max-width: 100%; 
  overflow-x: hidden; 
  height: 100%; 
  background-color: #f1f5f9; 
  color: #1e293b; 
  font-family: system-ui, -apple-system, sans-serif; 
}

/* Thanh cuộn ẩn */
.scrollbar-none::-webkit-scrollbar { 
  display: none; 
}

/* Sidebar & Menu Active */
.sidebar-item-active { 
  background-color: #e0f2fe !important; 
  color: #0284c7 !important; 
  border-left: 4px solid #0284c7; 
  font-weight: 800; 
}

/* Form Controls & Focus */
input, select, textarea { 
  background-color: #ffffff; 
  border: 1px solid #cbd5e1; 
  color: #0f172a; 
  border-radius: 0.375rem; 
  font-size: 13px; 
}

input:focus, select:focus, textarea:focus { 
  outline: none; 
  border-color: #0284c7; 
  box-shadow: 0 0 0 3px rgba(2, 132, 199, 0.15); 
}

/* Glass Panel Style */
.glass-panel { 
  background: #ffffff; 
  border: 1px solid #e2e8f0; 
  border-radius: 0.75rem; 
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05); 
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

/* Primary Button */
.btn-primary { 
  background: linear-gradient(to right, #0284c7, #0369a1); 
  color: white; 
}

.btn-primary:hover { 
  background: linear-gradient(to right, #0369a1, #075985); 
}

/* Checkbox Trạm Hấp & Nghiệm Thu */
.hap-checkbox, .nghiemthu-checkbox { 
  width: 1.25rem; 
  height: 1.25rem; 
  accent-color: #0284c7; 
  cursor: pointer; 
}

/* Popups & Overlays */
.popup-overlay { 
  background: rgba(15, 23, 42, 0.7); 
  backdrop-filter: blur(2px); 
}

/* Table Headers */
th { 
  font-size: 11px; 
  text-transform: uppercase; 
  letter-spacing: 0.05em; 
  color: #64748b; 
  background-color: #f8fafc; 
  font-weight: 700; 
}

/* Subtabs Admin */
.admin-subtab-active { 
  background-color: #0284c7; 
  color: white !important; 
}

/* Phân quyền Chế độ Guest */
body.guest-mode .action-btn, 
body.guest-mode .action-col { 
  display: none !important; 
  pointer-events: none; 
  opacity: 0.5; 
}

/* Khung Camera Quét Mã QR / Barcode */
#reader { 
  width: 100%; 
  border-radius: 8px; 
  overflow: hidden; 
  border: 2px solid #0284c7; 
  background: #000; 
}

#reader video { 
  object-fit: cover; 
  width: 100%; 
  height: 100%; 
}

/* Utility Classes */
.flex-wrap { flex-wrap: wrap; }
.gap-1 { gap: 0.25rem; }
.transition-colors { 
  transition-property: color, background-color, border-color, text-decoration-color, fill, stroke; 
  transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1); 
  transition-duration: 150ms; 
}

/* =========================================================================
   2. TỐI ƯU CẢM ỨNG TABLET & THAO TÁC NGÓN TAY (TOUCH-TARGET)
   ========================================================================= */
.touch-btn {
  min-height: 48px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

button:active, 
.action-btn:active {
  transform: scale(0.97);
  opacity: 0.9;
}

.overflow-auto::-webkit-scrollbar,
.overflow-y-auto::-webkit-scrollbar {
  width: 5px;
  height: 5px;
}

.overflow-auto::-webkit-scrollbar-track,
.overflow-y-auto::-webkit-scrollbar-track {
  background: #f1f5f9;
}

.overflow-auto::-webkit-scrollbar-thumb,
.overflow-y-auto::-webkit-scrollbar-thumb {
  background-color: #cbd5e1;
  border-radius: 4px;
}

/* =========================================================================
   3. CẤU HÌNH IN TRANG IN (PHÂN BIỆT THƯỜNG VÀ IN TEM BIXOLON)
   ========================================================================= */
@media print {
  body * {
    visibility: hidden !important;
  }
  
  #print-zone, #print-zone * {
    visibility: visible !important;
  }
  
  #print-zone {
    position: absolute !important;
    left: 0 !important;
    top: 0 !important;
    width: 100% !important;
    margin: 0 !important;
    padding: 0 !important;
  }

  /* Trường hợp 1: In Tem Bixolon 80mm x 50mm */
  body.print-mode-bixolon #print-zone {
    width: 80mm !important;
  }
  body.print-mode-bixolon @page {
    size: 80mm 50mm;
    margin: 0mm;
  }
  
  .bixolon-label {
    width: 80mm !important;
    height: 50mm !important;
    padding: 3mm 4mm !important;
    box-sizing: border-box !important;
    page-break-after: always;
    display: flex !important;
    flex-direction: column !important;
    justify-content: space-between !important;
    font-family: Arial, sans-serif !important;
    background: #fff !important;
    color: #000 !important;
  }

  /* Trường hợp 2: In Biên Bản Giao Nhận Thường (A4 Standard) */
  body.print-mode-doc @page {
    size: A4 portrait;
    margin: 15mm;
  }
}
2. Cập nhật app.js (Sửa hàm In Biên bản & Xác nhận nhận đồ)
Anh cập nhật lại file app.js với hai hàm đã được chỉnh sửa:

inHoaDonGiaoNhan(): Đã được tối ưu lại bảng, font chữ chuẩn A4 và áp dụng class print-mode-doc để máy in HP LaserJet in trọn khổ giấy.

khoaKyNhanDoSachDienTu(): Rút gọn tối đa, khi click chỉ cần bấm nút xác nhận OK là đồ tự động chuyển trạng thái hoàn tất vào tủ khoa.

Mã nguồn cập nhật app.js:

JavaScript
// =========================================================================
// HÀM IN BIÊN BẢN GIAO NHẬN (KHỔ GIẤY THƯỜNG A4 MÁY IN VĂN PHÒNG)
// =========================================================================
function inHoaDonGiaoNhan() {
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 
    if (!k) return showToast("Chọn Khoa trước!", "error");

    let htmlBang = `
        <table style="width:100%; border-collapse:collapse; margin-top:20px; font-size:13px; text-align:left; font-family:Arial, sans-serif;">
            <thead>
                <tr style="background-color:#f1f5f9;">
                    <th style="padding:10px; border:1px solid #334155; width:35%; text-align:left; color:#0f172a;">PHÂN LOẠI MÂM / LOẠI DỤNG CỤ</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ TRẢ BẨN</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ NHẬN SẠCH</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">NHẬN VÔ KHUẨN</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:20%; color:#0f172a;">CSSD NỢ KHOA</th>
                </tr>
            </thead>
            <tbody>`;

    const rows = document.querySelectorAll('#bangDonGiaoNhan tr');
    if (rows.length === 0 || (rows.length === 1 && rows[0].cells.length === 1)) { 
        htmlBang += `<tr><td colspan="5" style="padding:15px; border:1px solid #334155; text-align:center; font-style:italic;">Không có dữ liệu công nợ.</td></tr>`; 
    } else {
        rows.forEach(row => {
            const cells = row.cells;
            if (cells.length >= 5) { 
                htmlBang += `
                    <tr>
                        <td style="padding:10px; border:1px solid #334155; font-weight:bold; color:#0f172a;">${cells[0].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#e11d48;">${cells[1].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#0284c7;">${cells[2].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#7c3aed;">${cells[3].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#d97706;">${cells[4].innerText}</td>
                    </tr>`; 
            }
        });
    }
    htmlBang += `</tbody></table>`;

    const pZone = document.getElementById("print-zone");
    pZone.innerHTML = `
        <div style="width:100%; font-family:Arial, sans-serif; padding:10px; color:#000;">
            <div style="text-align:center; margin-bottom:25px;">
                <h2 style="margin:0; font-size:18px; font-weight:900; text-transform:uppercase; letter-spacing:0.5px;">BIÊN BẢN GIAO NHẬN DỤNG CỤ CSSD</h2>
                <p style="margin:8px 0 0 0; font-size:13px; font-weight:bold;">Khoa/Phòng: <span style="text-transform:uppercase; color:#0284c7;">${k}</span> - Ngày xuất: ${new Date().toLocaleDateString('vi-VN')}</p>
            </div>
            ${htmlBang}
            <div style="margin-top:50px; display:flex; justify-content:space-between; font-size:12px; font-weight:bold; padding:0 30px;">
                <div style="text-align:center; width:40%;">
                    <p style="margin-bottom:70px; text-transform:uppercase;">ĐIỀU DƯỠNG LÂM SÀNG</p>
                    <p style="font-weight:normal; font-style:italic; color:#64748b;">(Ký và ghi rõ họ tên)</p>
                </div>
                <div style="text-align:center; width:40%;">
                    <p style="margin-bottom:70px; text-transform:uppercase;">CHUYÊN VIÊN CSSD</p>
                    <p style="font-weight:normal; font-style:italic; color:#64748b;">(Ký và ghi rõ họ tên)</p>
                </div>
            </div>
        </div>`;

    document.body.classList.remove("print-mode-bixolon");
    document.body.classList.add("print-mode-doc");
    pZone.classList.remove("hidden");
    
    setTimeout(() => { 
        window.print(); 
        pZone.classList.add("hidden"); 
        document.body.classList.remove("print-mode-doc");
    }, 200);
}

// Cập nhật hàm in tem Bixolon để bật đúng class `print-mode-bixolon`
function inTemBixolonChuan(itemData) {
    const pZone = document.getElementById("print-zone");
    if (!pZone) return;

    const tenBo = itemData.bo ? itemData.bo.split(" [ID:")[0] : "DỤNG CỤ VÔ KHUẨN";
    const maID = itemData.maMacDinh || itemData.idKhay || "000000";
    const ngayNSX = itemData.ngayHapRealtime || itemData.ngayTao || getTodayDateStr();
    const ngayHSD = itemData.hsd ? new Date(itemData.hsd).toLocaleDateString('vi-VN') : "Theo quy định";
    const maBatch = itemData.batchCode || itemData.batchId || "N/A";
    const tenKhoa = itemData.khoa || "TẬP TRUNG";

    pZone.innerHTML = `
        <div class="bixolon-label" style="font-family: Arial, sans-serif;">
            <div style="text-align: center; border-bottom: 1.5px solid #000; padding-bottom: 2px;">
                <div style="font-size: 10px; font-weight: bold; text-transform: uppercase;">PHUONG NAM HOSPITAL - CSSD</div>
                <div style="font-size: 12px; font-weight: 900; margin-top: 1px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${tenBo}</div>
            </div>
            <div style="text-align: center; margin: 3px 0; display: flex; justify-content: center;">
                <svg id="bixolon-single-barcode"></svg>
            </div>
            <div style="font-size: 9px; border-top: 1px dashed #000; padding-top: 2px;">
                <div style="display: flex; justify-content: space-between; font-weight: bold;">
                    <span>LÔ: ${maBatch}</span>
                    <span>KHOA: ${tenKhoa}</span>
                </div>
                <div style="display: flex; justify-content: space-between; margin-top: 1px;">
                    <span>NSX: ${ngayNSX}</span>
                    <span style="font-size: 10px; font-weight: 900;">HSD: ${ngayHSD}</span>
                </div>
            </div>
        </div>`;

    JsBarcode("#bixolon-single-barcode", maID, {
        format: "CODE128",
        width: 1.6,
        height: 36,
        displayValue: true,
        fontSize: 10,
        fontOptions: "bold",
        margin: 0
    });

    document.body.classList.remove("print-mode-doc");
    document.body.classList.add("print-mode-bixolon");
    pZone.classList.remove("hidden");
    
    setTimeout(() => {
        window.print();
        pZone.classList.add("hidden");
        document.body.classList.remove("print-mode-bixolon");
    }, 250);
}

// =========================================================================
// XÁC NHẬN NHẬN ĐỒ SẠCH VÀO TỦ KHOA (ĐÃ BỎ KÝ ĐIỆN TỬ PHỨC TẠP)
// =========================================================================
function khoaKyNhanDoSachDienTu() {
    const khoaDangNhap = currentRole === "KHOA" ? loginUserCode : (document.getElementById("khoa_selKhoa") ? document.getElementById("khoa_selKhoa").value : "");
    const nameNguoiDung = loginUserCode || "Điều dưỡng khoa";
    const checkboxes = document.querySelectorAll("#bangChoNhanTaiKhoa input[type='checkbox']:checked");
    const dsIdDuocChon = []; 
    checkboxes.forEach(chk => dsIdDuocChon.push(chk.value));

    if (dsIdDuocChon.length === 0) { 
        playSound('error'); 
        return showToast("Vui lòng tích chọn ít nhất 1 khay để xác nhận nhận!", "error"); 
    }

    if (confirm(`Xác nhận nhận ${dsIdDuocChon.length} mâm dụng cụ vô khuẩn vào tủ của khoa ${khoaDangNhap}?`)) {
        let p = [];
        dsIdDuocChon.forEach(idDoc => {
            p.push(
                db.collection("phieuGiaoNhan").doc(idDoc).update({
                    status: "HOAN_TAT", 
                    nguoiKyNhanKhoa: nameNguoiDung, 
                    timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'), 
                    ngayKhoaNhanThucTe: getTodayDateStr()
                })
            );
        });
        Promise.all(p).then(() => { 
            playSound('success'); 
            showToast(`Đã tiếp nhận ${dsIdDuocChon.length} mâm vào tủ khoa thành công!`, "success"); 
            callRender(); 
        });
    }
}
3. Cập nhật index.html (Đổi tên nút bấm cho khớp)
Ở phần Trạm 1 (Cổng Lâm Sàng) trong index.html, anh sửa lại nút nhận đồ từ tên cũ sang tên mới đơn giản hơn:

HTML
<!-- Nút xác nhận nhận đồ đơn giản -->
<button onclick="khoaKyNhanDoSachDienTu()" class="bg-emerald-600 hover:bg-emerald-700 active:scale-95 text-white font-black px-4 py-2.5 rounded-lg text-xs shadow transition-all flex items-center gap-2">
    <i class="fa-solid fa-square-check text-sm"></i>
    <span>XÁC NHẬN NHẬN ĐỒ VÀO TỦ KHOA</span>
</button>
à bạn đang hiểu sai ý tôi. Chỗ "Ký xác nhận vào kho" tôi muốn bạn hiện chỗ để ai nhận có thể thao tác ký bằng chữ ký điện tử

Cảm ơn anh Hùng đã làm rõ ý. Như vậy đối với quy trình giao nhận dụng cụ:

In Biên Bản Giao Nhận: Mặc định in khổ A4 tiêu chuẩn (dùng máy in văn phòng thông thường như HP LaserJet).

Tiếp Nhận Đồ Vào Tủ Khoa: Cần một khung vẽ chữ ký điện tử trực tiếp (Touch/Mouse Canvas) để Điều dưỡng hoặc người nhận thao tác ký tên xác nhận, sau đó lưu chữ ký vào hệ thống.

Dưới đây là mã nguồn đã được cập nhật đầy đủ cả 3 file:

1. Cập nhật index.html (Thêm Popup Canvas Ký Điện Tử)
Anh thêm đoạn mã Modal Canvas này vào cuối file index.html (ngay trước thẻ đóng </body>):

HTML
<!-- POPUP KÝ ĐIỆN TỬ TIẾP NHẬN DỤNG CỤ -->
<div id="popupKyDienTu" class="fixed inset-0 z-[100] hidden popup-overlay flex items-center justify-center p-4">
    <div class="bg-white w-full max-w-md rounded-2xl shadow-2xl border border-slate-200 overflow-hidden animate-in fade-in zoom-in duration-200">
        <!-- Header Popup -->
        <div class="bg-slate-900 text-white p-4 flex justify-between items-center">
            <div class="flex items-center gap-2">
                <i class="fa-solid fa-signature text-amber-400 text-lg"></i>
                <h3 class="font-black text-sm uppercase tracking-wide">KÝ XÁC NHẬN NHẬN ĐỒ VÔ KHUẨN</h3>
            </div>
            <button onclick="dongPopupKyDienTu()" class="text-slate-400 hover:text-white transition-colors">
                <i class="fa-solid fa-xmark text-lg"></i>
            </button>
        </div>

        <!-- Body Popup -->
        <div class="p-5 space-y-4">
            <div>
                <label class="block text-xs font-bold text-slate-700 mb-1">Họ tên Điều dưỡng / Người nhận:</label>
                <input type="text" id="ky_tenNguoiNhan" class="w-full p-2.5 font-bold text-slate-800 border rounded-lg" placeholder="Nhập tên người ký nhận...">
            </div>

            <div>
                <div class="flex justify-between items-center mb-1">
                    <label class="text-xs font-bold text-slate-700">Vẽ chữ ký trực tiếp lên màn hình:</label>

                </div>
                <!-- Khung Canvas Vẽ Chữ Ký -->
                <div class="border-2 border-dashed border-sky-300 rounded-xl bg-slate-50 relative overflow-hidden touch-none">
                    <canvas id="canvasKyDienTu" class="w-full h-40 cursor-crosshair block"></canvas>
                    <span class="absolute bottom-2 right-3 text-[10px] text-slate-400 italic pointer-events-none">Ký tên vào ô này</span>
                </div>
            </div>
        </div>

        <!-- Footer Popup -->
        <div class="bg-slate-50 p-4 border-t border-slate-200 flex justify-between items-center gap-2">
            <button type="button" onclick="xoaChuKyCanvas()" class="px-3 py-2 bg-slate-200 hover:bg-slate-300 text-slate-700 font-bold rounded-lg text-xs transition-colors">
                <i class="fa-solid fa-eraser mr-1"></i>Ký lại
            </button>
            <div class="flex items-center gap-2">
                <button type="button" onclick="dongPopupKyDienTu()" class="px-4 py-2 bg-slate-200 hover:bg-slate-300 text-slate-700 font-bold rounded-lg text-xs transition-colors">Hủy</button>
                <button type="button" onclick="luuXacNhanKyNhan()" class="px-5 py-2 bg-emerald-600 hover:bg-emerald-700 text-white font-black rounded-lg text-xs shadow-md transition-all flex items-center gap-1.5">
                    <i class="fa-solid fa-check-double"></i>Lưu & Tiếp Nhận
                </button>
            </div>
        </div>
    </div>
</div>
2. Cập nhật app.js
Thêm các biến và hàm xử lý Canvas ký điện tử, lưu ảnh chữ ký Base64 lên Firestore và chuẩn hóa lại hàm In Biên bản Giao nhận A4:

JavaScript
// =========================================================================
// XỬ LÝ CHỮ KÝ ĐIỆN TỬ TIẾP NHẬN DỤNG CỤ VÀO TỦ KHOA
// =========================================================================
let canvasSignature = null;
let ctxSignature = null;
let isDrawingSignature = false;
let dsIdKhayChoKyNhan = [];

function khoiTaoCanvasKyDienTu() {
    canvasSignature = document.getElementById("canvasKyDienTu");
    if (!canvasSignature) return;
    ctxSignature = canvasSignature.getContext("2d");

    // Đặt kích thước thực tế cho canvas theo khung hiển thị
    const rect = canvasSignature.getBoundingClientRect();
    canvasSignature.width = rect.width || 380;
    canvasSignature.height = rect.height || 160;

    ctxSignature.lineWidth = 2.5;
    ctxSignature.lineCap = "round";
    ctxSignature.strokeStyle = "#0284c7";

    // Sự kiện Chuột
    canvasSignature.onmousedown = (e) => {
        isDrawingSignature = true;
        ctxSignature.beginPath();
        const pos = getCanvasPos(e);
        ctxSignature.moveTo(pos.x, pos.y);
    };
    canvasSignature.onmousemove = (e) => {
        if (!isDrawingSignature) return;
        const pos = getCanvasPos(e);
        ctxSignature.lineTo(pos.x, pos.y);
        ctxSignature.stroke();
    };
    canvasSignature.onmouseup = () => { isDrawingSignature = false; };

    // Sự kiện Cảm ứng Tablet / Smartphone
    canvasSignature.ontouchstart = (e) => {
        isDrawingSignature = true;
        ctxSignature.beginPath();
        const pos = getCanvasPos(e.touches[0]);
        ctxSignature.moveTo(pos.x, pos.y);
    };
    canvasSignature.ontouchmove = (e) => {
        if (!isDrawingSignature) return;
        e.preventDefault();
        const pos = getCanvasPos(e.touches[0]);
        ctxSignature.lineTo(pos.x, pos.y);
        ctxSignature.stroke();
    };
    canvasSignature.ontouchend = () => { isDrawingSignature = false; };
}

function getCanvasPos(evt) {
    const rect = canvasSignature.getBoundingClientRect();
    return {
        x: evt.clientX - rect.left,
        y: evt.clientY - rect.top
    };
}

function xoaChuKyCanvas() {
    if (ctxSignature && canvasSignature) {
        ctxSignature.clearRect(0, 0, canvasSignature.width, canvasSignature.height);
    }
}

function moPopupKyDienTu() {
    const checkboxes = document.querySelectorAll("#bangChoNhanTaiKhoa input[type='checkbox']:checked");
    dsIdKhayChoKyNhan = [];
    checkboxes.forEach(chk => dsIdKhayChoKyNhan.push(chk.value));

    if (dsIdKhayChoKyNhan.length === 0) {
        playSound('error');
        return showToast("Vui lòng tích chọn ít nhất 1 khay để ký nhận!", "error");
    }

    const inpName = document.getElementById("ky_tenNguoiNhan");
    if (inpName) inpName.value = loginUserCode || "";

    document.getElementById("popupKyDienTu").classList.remove("hidden");
    setTimeout(() => {
        khoiTaoCanvasKyDienTu();
        xoaChuKyCanvas();
    }, 100);
}

function dongPopupKyDienTu() {
    document.getElementById("popupKyDienTu").classList.add("hidden");
    dsIdKhayChoKyNhan = [];
}

function luuXacNhanKyNhan() {
    const tenNguoiNhan = document.getElementById("ky_tenNguoiNhan").value.trim();
    if (!tenNguoiNhan) {
        playSound('error');
        return showToast("Vui lòng nhập tên người nhận!", "error");
    }

    // Lấy chuỗi ảnh chữ ký Base64 từ Canvas
    const chuoiAnhChuKy = canvasSignature ? canvasSignature.toDataURL("image/png") : "";

    let p = [];
    dsIdKhayChoKyNhan.forEach(idDoc => {
        p.push(
            db.collection("phieuGiaoNhan").doc(idDoc).update({
                status: "HOAN_TAT",
                nguoiKyNhanKhoa: tenNguoiNhan,
                anhChuKyNhanBase64: chuoiAnhChuKy,
                timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'),
                ngayKhoaNhanThucTe: getTodayDateStr()
            })
        );
    });

    Promise.all(p).then(() => {
        playSound('success');
        showToast(`Đã ký nhận thành công ${dsIdKhayChoKyNhan.length} mâm vào tủ khoa!`, "success");
        dongPopupKyDienTu();
        callRender();
    });
}

// =========================================================================
// HÀM IN BIÊN BẢN GIAO NHẬN (MẶC ĐỊNH MÁY IN VĂN PHÒNG A4)
// =========================================================================
function inHoaDonGiaoNhan() {
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;
    if (!k) return showToast("Chọn Khoa trước!", "error");

    let htmlBang = `
        <table style="width:100%; border-collapse:collapse; margin-top:20px; font-size:13px; text-align:left; font-family:Arial, sans-serif;">
            <thead>
                <tr style="background-color:#f1f5f9;">
                    <th style="padding:10px; border:1px solid #334155; width:35%; text-align:left; color:#0f172a;">PHÂN LOẠI MÂM / LOẠI DỤNG CỤ</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ TRẢ BẨN</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ NHẬN SẠCH</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">NHẬN VÔ KHUẨN</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:20%; color:#0f172a;">CSSD NỢ KHOA</th>
                </tr>
            </thead>
            <tbody>`;

    const rows = document.querySelectorAll('#bangDonGiaoNhan tr');
    if (rows.length === 0 || (rows.length === 1 && rows[0].cells.length === 1)) {
        htmlBang += `<tr><td colspan="5" style="padding:15px; border:1px solid #334155; text-align:center; font-style:italic;">Không có dữ liệu công nợ.</td></tr>`;
    } else {
        rows.forEach(row => {
            const cells = row.cells;
            if (cells.length >= 5) {
                htmlBang += `
                    <tr>
                        <td style="padding:10px; border:1px solid #334155; font-weight:bold; color:#0f172a;">${cells[0].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#e11d48;">${cells[1].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#0284c7;">${cells[2].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#7c3aed;">${cells[3].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#d97706;">${cells[4].innerText}</td>
                    </tr>`;
            }
        });
    }
    htmlBang += `</tbody></table>`;

    const pZone = document.getElementById("print-zone");
    pZone.innerHTML = `
        <div style="width:100%; font-family:Arial, sans-serif; padding:10px; color:#000;">
            <div style="text-align:center; margin-bottom:25px;">
                <h2 style="margin:0; font-size:18px; font-weight:900; text-transform:uppercase;">BIÊN BẢN GIAO NHẬN DỤNG CỤ CSSD</h2>
                <p style="margin:8px 0 0 0; font-size:13px; font-weight:bold;">Khoa/Phòng: <span style="text-transform:uppercase; color:#0284c7;">${k}</span> - Ngày xuất: ${new Date().toLocaleDateString('vi-VN')}</p>
            </div>
            ${htmlBang}
            <div style="margin-top:50px; display:flex; justify-content:space-between; font-size:12px; font-weight:bold; padding:0 30px;">
                <div style="text-align:center; width:40%;">
                    <p style="margin-bottom:70px; text-transform:uppercase;">ĐIỀU DƯỠNG LÂM SÀNG</p>
                    <p style="font-weight:normal; font-style:italic; color:#64748b;">(Ký và ghi rõ họ tên)</p>
                </div>
                <div style="text-align:center; width:40%;">
                    <p style="margin-bottom:70px; text-transform:uppercase;">CHUYÊN VIÊN CSSD</p>
                    <p style="font-weight:normal; font-style:italic; color:#64748b;">(Ký và ghi rõ họ tên)</p>
                </div>
            </div>
        </div>`;

    document.body.classList.remove("print-mode-bixolon");
    document.body.classList.add("print-mode-doc");
    pZone.classList.remove("hidden");

    setTimeout(() => {
        window.print();
        pZone.classList.add("hidden");
        document.body.classList.remove("print-mode-doc");
    }, 200);
}
3. Cập nhật style.css
File style.css giúp phân biệt giữa in máy in thường (A4) và in tem Bixolon 80x50mm:

CSS
/* =========================================================================
   CẤU HÌNH IN (TÁCH BIỆT IN A4 VĂN PHÒNG VÀ IN TEM BIXOLON)
   ========================================================================= */
@media print {
  body * {
    visibility: hidden !important;
  }
  
  #print-zone, #print-zone * {
    visibility: visible !important;
  }
  
  #print-zone {
    position: absolute !important;
    left: 0 !important;
    top: 0 !important;
    width: 100% !important;
    margin: 0 !important;
    padding: 0 !important;
  }

  /* Mặc định In Biên Bản A4 */
  body.print-mode-doc @page {
    size: A4 portrait;
    margin: 15mm;
  }

  /* In Tem Bixolon 80mm x 50mm */
  body.print-mode-bixolon #print-zone {
    width: 80mm !important;
  }
  body.print-mode-bixolon @page {
    size: 80mm 50mm;
    margin: 0mm;
  }
  
  .bixolon-label {
    width: 80mm !important;
    height: 50mm !important;
    padding: 3mm 4mm !important;
    box-sizing: border-box !important;
    page-break-after: always;
    display: flex !important;
    flex-direction: column !important;
    justify-content: space-between !important;
    font-family: Arial, sans-serif !important;
    background: #fff !important;
    color: #000 !important;
  }
}
tôi sẽ gửi từng code để bạn thêm dùm tôi nha

Dạ được chứ anh Hùng. Anh cứ gửi từng file mã nguồn qua (HTML, CSS hoặc JS), em sẽ giúp anh tích hợp chính xác và hoàn chỉnh từng phần nhé!

// =========================================================================

// 1. KHỞI TẠO HỆ THỐNG & CẤU HÌNH BIẾN TOÀN CỤC (PN HOSPITAL)

// =========================================================================

const firebaseConfig = { 

    apiKey: "AIzaSyCxjdCTKHQlpm7SYbWCEws1HhcOaFp0LBA", 

    authDomain: "cssd-system-2878c.firebaseapp.com", 

    projectId: "cssd-system-2878c", 

    storageBucket: "cssd-system-2878c.firebasestorage.app", 

    messagingSenderId: "662377321937", 

    appId: "1:662377321937:web:001c092e10319547623cf0" 

};

firebase.initializeApp(firebaseConfig); 

const db = firebase.firestore();



let thongTinMatKhauAdmin = { adminPIN: "admin2026", cssdPIN: "cssd2026", guestPIN: "guest2026" };

let cauHinhGiaoDien = {

    "ADMIN": ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'],

    "CSSD": ['thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','dashboard_tv'], 

    "KHOA": ['khoaphong','quanlykho','lichsuluanchuyen'], 

    "GUEST": ['quanlykho','danhmuc','lichsuluanchuyen','performance','dashboard_tv']

};



let currentRole = "", loginUserCode = "";

let danhSachKhoa = [], listGiaoDich = [], gioHangTam = [], danhSachKtvCssd = [], databaseExcel = [];

let html5QrCode = null; let targetInputIdForScan = ""; let idDangDongGoi = null;

let activeTab = 'thugom'; let renderTimeout = null;

let duLieuAnhBiTamBase64 = ""; let maLoTruyVetToanCuc = ""; 

let gioHangXuatKho = []; let gioKhaySuDungTam = []; 

let currentKiemDemData = null;



// Cấu hình máy hấp & máy rửa (Belimed WD250)

const cauHinhMayHap = { "Hấp hơi nước": ["A1", "A2", "A3", "A4"], "Hấp H2O2 (Plasma)": ["P1", "P2"], "Khử khuẩn EO": ["EO1", "EO2"] };

const cauHinhMayRua = {

    "Máy rửa khử khuẩn tự động": ["Belimed WD250"],

    "Máy rửa sóng siêu âm": ["US1"],

    "Bồn rửa thủ công": ["MAN1"]

};

const danhMucHoaChatRua = [

    { ten: "Cidezyme (Enzym phân hủy protein)", lieuLuong: "8 ml/L" },

    { ten: "Sekusept Active (Khử khuẩn mức độ cao)", lieuLuong: "10 ml/L" },

    { ten: "Smeg (Nước rửa trung tính chuyên dụng)", lieuLuong: "5 ml/L" },

    { ten: "Aniosyme DD1 (Làm sạch & khử trùng sơ bộ)", lieuLuong: "5 ml/L" }

];



function getTodayDateStr() { 

    const d = new Date(); 

    return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`; 

}



function playSound(type) {

    try {

        const AudioContext = window.AudioContext || window.webkitAudioContext;

        if (!AudioContext) return;

        const ctx = new AudioContext();

        const osc = ctx.createOscillator();

        const gain = ctx.createGain();

        osc.connect(gain); gain.connect(ctx.destination);

        if (type === 'success') {

            osc.type = 'sine'; osc.frequency.setValueAtTime(800, ctx.currentTime);

            gain.gain.setValueAtTime(0.3, ctx.currentTime);

            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.15);

            osc.start(); osc.stop(ctx.currentTime + 0.15);

        } else {

            osc.type = 'sawtooth'; osc.frequency.setValueAtTime(250, ctx.currentTime);

            gain.gain.setValueAtTime(0.3, ctx.currentTime);

            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.4);

            osc.start(); osc.stop(ctx.currentTime + 0.4);

        }

    } catch(e) {}

}



// =========================================================================

// 2. REALTIME LISTENER (FIRESTORE SYNC)

// =========================================================================

db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").onSnapshot(doc => { 

    try {

        if (doc.exists) { 

            let res = doc.data(); 

            danhSachKhoa = res.danhSachKhoa || []; 

            danhSachKtvCssd = res.danhSachKtvCssd || []; 

            databaseExcel = res.databaseExcel || []; 

            if(res.thongTinMatKhauAdmin) thongTinMatKhauAdmin = Object.assign(thongTinMatKhauAdmin, res.thongTinMatKhauAdmin); 

            if(res.cauHinhGiaoDien) cauHinhGiaoDien = Object.assign(cauHinhGiaoDien, res.cauHinhGiaoDien);

        } 

        initSelects();

        if(activeTab === 'khoaphong') loadBoDungCuTheoKhoa(); 

        renderAdminInterface(); 

        if(currentRole) apDungPhanQuyenGiaoDien(currentRole);

        taiDanhMucLinhKienChuan();

        callRender();

    } catch(err) { console.error("Lỗi đồng bộ danh mục:", err); }

});



db.collection("phieuGiaoNhan").orderBy("id", "desc").limit(1000).onSnapshot(snap => { 

    listGiaoDich = []; 

    snap.forEach(doc => { let d = doc.data(); d.firestoreId = doc.id; listGiaoDich.push(d); }); 

    callRender();

});



function callRender() { clearTimeout(renderTimeout); renderTimeout = setTimeout(() => { renderTheoTabHienTai(); }, 100); }



// =========================================================================

// 3. XỬ LÝ ĐĂNG NHẬP & PHÂN QUYỀN HỆ THỐNG

// =========================================================================

function toggleLoginFields() {

    const r = document.getElementById("login_role").value;

    document.getElementById("field_khoa").classList.toggle("hidden", r !== "KHOA");

    document.getElementById("field_nhanvien_cssd").classList.toggle("hidden", r !== "CSSD");

}



function checkLogin() {

    const role = document.getElementById("login_role").value; 

    const pass = document.getElementById("login_pass").value; 

    let pA = thongTinMatKhauAdmin.adminPIN || "admin2026"; 

    let pC = thongTinMatKhauAdmin.cssdPIN || "cssd2026"; 

    let pG = thongTinMatKhauAdmin.guestPIN || "guest2026"; 

     

    if (role === "ADMIN" && pass === pA) { 

        currentRole = "ADMIN"; loginUserCode = "ADMIN"; document.getElementById("nav_user_info").innerText = "ADMINISTRATOR";

        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');

        apDungPhanQuyenGiaoDien("ADMIN");

        switchTab('lichsuluanchuyen');

    } else if (role === "KHOA") { 

        const khoaSelect = document.getElementById("login_khoa").value; 

        let found = danhSachKhoa.find(x => x.ten === khoaSelect); 

        if (pass === (found ? found.pin : "123")) { 

            currentRole = "KHOA"; loginUserCode = khoaSelect; document.getElementById("nav_user_info").innerText = khoaSelect; 

            if (document.getElementById("khoa_selKhoa")) {

                document.getElementById("khoa_selKhoa").value = khoaSelect;

                document.getElementById("khoa_selKhoa").disabled = true;

            }

            document.body.classList.remove('guest-mode');

            apDungPhanQuyenGiaoDien("KHOA");

            switchTab('khoaphong');

        } else { return showToast("Sai mã PIN Khoa!", "error"); } 

    } else if (role === "CSSD") {

        const nvCode = document.getElementById("login_nv_cssd").value;

        if(!nvCode && pass === pC) { currentRole = "CSSD"; loginUserCode = "CSSD_CHUNG"; document.getElementById("nav_user_info").innerText = "CSSD GENERAL"; } 

        else if (nvCode) {

            let nv = danhSachKtvCssd.find(x => x.code === nvCode);

            if (nv && pass === nv.pin) { currentRole = "CSSD"; loginUserCode = nv.ten; document.getElementById("nav_user_info").innerText = `${nvCode} - ${nv.ten}`; }

            else { return showToast("Sai mã PIN của nhân viên này!", "error"); }

        } else { return showToast("Vui lòng chọn Nhân viên hoặc nhập PIN Backup!", "error"); }

        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');

        apDungPhanQuyenGiaoDien("CSSD");

        let tabsDuocPhep = cauHinhGiaoDien["CSSD"] || [];

        if(tabsDuocPhep.includes('thugom')) switchTab('thugom');

        else if (tabsDuocPhep.length > 0) switchTab(tabsDuocPhep[0]);

    } else if (role === "GUEST") {

        if(pass === pG) {

            currentRole = "GUEST"; loginUserCode = "GUEST"; document.getElementById("nav_user_info").innerText = "KHÁCH THAM QUAN";

            document.body.classList.add('guest-mode'); 

            apDungPhanQuyenGiaoDien("GUEST");

            switchTab('quanlykho');

        } else { return showToast("Sai mã PIN Khách tham quan!", "error"); }

    } else { return showToast("Sai thông tin đăng nhập!", "error"); }

    document.getElementById("login-screen").classList.add("hidden"); document.getElementById("main-app").classList.remove("hidden");

}



function anTatCaHeadersVaMenus() {

    document.getElementById('header-lamsang')?.classList.add('hidden');

    document.getElementById('header-vanhanh')?.classList.add('hidden');

    document.getElementById('header-dulieu')?.classList.add('hidden');

    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => {

        document.getElementById('menu-' + x)?.classList.add('hidden');

    });

}



function apDungPhanQuyenGiaoDien(role) {

    anTatCaHeadersVaMenus();

    if (role === "ADMIN") {

        ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(tab => {

            document.getElementById('menu-' + tab)?.classList.remove('hidden');

        });

        document.getElementById('header-lamsang')?.classList.remove('hidden');

        document.getElementById('header-vanhanh')?.classList.remove('hidden');

        document.getElementById('header-dulieu')?.classList.remove('hidden');

        return;

    }

    let tabsDuocPhep = cauHinhGiaoDien[role] || [];

    tabsDuocPhep.forEach(tab => { document.getElementById('menu-' + tab)?.classList.remove('hidden'); });

    if (tabsDuocPhep.includes('khoaphong')) document.getElementById('header-lamsang')?.classList.remove('hidden');

    if (['thugom','mayrua','donggoi','mayhap','khovokhuan'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-vanhanh')?.classList.remove('hidden');

    if (['quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-dulieu')?.classList.remove('hidden');

}



function switchTab(t) { 

    if (currentRole !== "ADMIN") {

        let tabsDuocPhep = cauHinhGiaoDien[currentRole] || [];

        if(!tabsDuocPhep.includes(t)) { return showToast("Tài khoản không có quyền truy cập!", "error"); }

    }

    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => { 

        document.getElementById('tab-'+x)?.classList.add('hidden'); 

        document.getElementById('menu-'+x)?.classList.remove('sidebar-item-active'); 

    }); 

    document.getElementById('tab-'+t)?.classList.remove('hidden'); 

    document.getElementById('menu-'+t)?.classList.add('sidebar-item-active'); 

    activeTab = t; 

    if (t === 'mayrua') { setTimeout(() => { capNhatDanhSachMaMayRua(); }, 50); }

    if (t === 'mayhap') { setTimeout(() => { tuDongTaoMaLoMeHap(); }, 50); }

    callRender(); 

}



// =========================================================================

// 4. TRẠM 1: CỔNG LÂM SÀNG & HÀNH ĐỘNG ĐỘC LẬP (TRẢ ĐỒ BẨN / YÊU CẦU ĐỒ SẠCH)

// =========================================================================

function loadBoDungCuTheoKhoa() { 

    let k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 

    let list = document.getElementById("listBoDungCu"); 

    if(!list) return; list.innerHTML = ""; 

    let f = danhSachKhoa.find(x => (x.ten || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase()); 

    if (f && f.danhSachBo) {

        let htmlOptions = "";

        f.danhSachBo.forEach(x => {

            if (x) {

                let chuoiX = String(x);

                let tenBoThuanTuy = chuoiX.includes(" [ID:") ? chuoiX.split(" [ID:")[0] : chuoiX;

                htmlOptions += `<option value="${chuoiX}"><option value="${tenBoThuanTuy}">`;

            }

        });

        list.innerHTML = htmlOptions; 

    }

}



function themVaoGio() { 

    let val = document.getElementById("khoa_inpMaBo").value.trim().toUpperCase(); if(!val) return; 

    if (gioHangTam.some(x => x.maMacDinh === val)) { document.getElementById("khoa_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách chờ!", "error"); } 

    let tenGoc = val.includes("[ID:") ? val.split(" [ID:")[0] : val; 

    gioHangTam.push({bo: tenGoc, maMacDinh: val, slYeuCau: 1}); 

    document.getElementById("khoa_inpMaBo").value = ""; 

    renderGioHang(); 

}



function renderGioHang() { 

    let khuVuc = document.getElementById("khuVucGioHang"); if(khuVuc) khuVuc.classList.toggle("hidden", gioHangTam.length===0); 

    let bHang = document.getElementById("bangGioHang");

    if(bHang) {

        bHang.innerHTML = gioHangTam.map(i => `<tr><td class="p-2.5 font-bold text-sky-700 text-[11px]">${i.bo}</td></tr>`).join(''); 

    }

    let badge = document.getElementById("badgeGioHang");

    if(badge) badge.innerText = `${gioHangTam.length} món`;

}



function khoaGuiPhieuTraBatches() { 

    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 

    if(!k) return showToast("Vui lòng chọn Khoa trước!"); 

    if(gioHangTam.length === 0) return showToast("Vui lòng quét hoặc chọn mâm bẩn muốn trả trước!", "error"); 

     

    let p=[]; 

    gioHangTam.forEach((i, idx) => {

        p.push(

            db.collection("phieuGiaoNhan").add({ 

                id: Date.now() + idx, 

                ngayTao: getTodayDateStr(), 

                time: new Date().toLocaleTimeString('vi-VN'), 

                khoa: k, 

                bo: i.bo, 

                maMacDinh: i.maMacDinh, 

                slYeuCau: 1, 

                slThucTe: 1, 

                status: "CHO_THU" 

            })

        );

    }); 

     

    Promise.all(p).then(() => { 

        clearGioHang(); 

        playSound('success');

        showToast("Đã gửi lệnh báo trả đồ bẩn về CSSD!", "success"); 

        callRender(); 

    }); 

}



function khoaGuiYeuCauCapPhat() {

    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;

    if(!k) return showToast("Vui lòng chọn Khoa/Phòng gửi yêu cầu!", "error");

    

    const tenBoChon = document.getElementById("khoa_inpMaBo")?.value.trim().toUpperCase();

    if(!tenBoChon) return showToast("Vui lòng chọn hoặc nhập tên mâm cần cấp mới!", "error");

    

    let tenMâmSạch = tenBoChon.split(" [ID:")[0];

    

    db.collection("phieuGiaoNhan").add({

        id: Date.now(),

        ngayTao: getTodayDateStr(),

        time: new Date().toLocaleTimeString('vi-VN'),

        khoa: k,

        bo: tenMâmSạch,

        maMacDinh: "CHO_CAP_PHAT", 

        slYeuCau: 1,

        status: "CHO_CAP_PHAT", 

        nvYeuCau: loginUserCode || "Điều dưỡng khoa"

    }).then(() => {

        playSound('success');

        showToast(`Đã gửi yêu cầu cấp phát mâm ${tenMâmSạch} thành công!`, "success");

        if(document.getElementById("khoa_inpMaBo")) document.getElementById("khoa_inpMaBo").value = "";

        callRender();

    });

}



// =========================================================================

// 5. TRẠM 2: XE THU GOM & KIỂM ĐẾM CHI TIẾT LINH KIỆN

// =========================================================================

function moPopupKiemDem(id) { 

    let item = listGiaoDich.find(x => x.firestoreId === id); 

    if(!item) return showToast("Không tìm thấy thông tin dòng chỉ định!", "error");



    currentKiemDemData = {

        id: id,

        tenBoDungCu: item.bo ? String(item.bo).split(" [ID:")[0] : "Chưa rõ mâm",

        khoaYeuCau: item.khoa || "Chưa rõ khoa",

        ghiChuLamSang: item.ghiChu || ""

    }; 



    document.getElementById('popBo').innerText = currentKiemDemData.tenBoDungCu;

    document.getElementById('popKhoa').innerText = currentKiemDemData.khoaYeuCau;

    document.getElementById('popGhiChu').value = currentKiemDemData.ghiChuLamSang;

    document.getElementById('popGhiChu').classList.remove('border-rose-500', 'ring-2', 'ring-rose-200');



    let itemsInBo = databaseExcel.filter(x => { 

        let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; 

        return String(boName).trim().toUpperCase() === String(currentKiemDemData.tenBoDungCu).trim().toUpperCase(); 

    });



    let checklistSoBo = [];

    if(itemsInBo.length > 0) {

        checklistSoBo = itemsInBo.map(ct => {

            let tenDc = ct['Tên TS chuẩn'] || ct['Tên Dụng Cụ Chi Tiết'] || ct['Tên dụng cụ chi tiết'] || ct['Tên dụng cụ'] || ct['Chi tiết'] || ct['Dụng cụ'] || ct['Tên Chi Tiết'] || ct['NAME'] || "Dụng cụ";

             

            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {

                tenDc = "Dụng cụ chuẩn mâm";

            }

             

            let sl = parseInt(ct['Số lượng'] || ct['SL'] || ct['Số Lượng'] || 1) || 0;

            return { ten: tenDc, slChuan: sl, slThuc: sl, tinhTrang: "ĐỦ" };

        }).filter(line => line.slChuan > 0); 

    } else {

        checklistSoBo = [{ ten: "Dụng cụ nguyên bộ (Chưa phân rã cấu hình)", slChuan: 1, slThuc: 1, tinhTrang: "ĐỦ" }];

    }



    currentKiemDemData.linhKienKiemDem = checklistSoBo;

    renderChecklistLinhKien();

    document.getElementById('popupKiemDem').classList.remove('hidden'); 

}



function renderChecklistLinhKien() {

    const container = document.getElementById('popKiemDemChecklist');

    if(!container) return; container.innerHTML = ""; 



    currentKiemDemData.linhKienKiemDem.forEach((item, index) => {

        let bgBadge = "bg-emerald-50 text-emerald-700 border-emerald-200";

        if (item.tinhTrang === "THIẾU") bgBadge = "bg-rose-50 text-rose-700 border-rose-200 animate-pulse";

        if (item.tinhTrang === "HỎNG") bgBadge = "bg-amber-50 text-amber-700 border-amber-200";



        const row = document.createElement('div');

        row.className = "flex items-center justify-between py-3 px-2 transition-all hover:bg-slate-50";

        row.innerHTML = `

            <div class="flex-1 pr-2">

                <p class="text-xs font-bold text-slate-700">${item.ten}</p>

                <p class="text-[10px] text-slate-400 font-medium mt-0.5">Cơ số chuẩn: <span class="font-bold text-slate-600">${item.slChuan}</span></p>

            </div>

            <div class="flex items-center gap-3 mr-4">

                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, -1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">-</button>

                <span class="w-6 text-center text-xs font-black text-slate-800">${item.slThuc}</span>

                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, 1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">+</button>

            </div>

            <div>

                <select onchange="thayDoiTinhTrangLinhKien(${index}, this.value)" class="text-[11px] font-black border rounded-lg px-2 py-1.5 shadow-sm transition-all ${bgBadge}">

                    <option value="ĐỦ" ${item.tinhTrang === 'ĐỦ' ? 'selected' : ''}>🟢 Đủ đồ</option>

                    <option value="THIẾU" ${item.tinhTrang === 'THIẾU' ? 'selected' : ''}>🔴 Thiếu đồ</option>

                    <option value="HỎNG" ${item.tinhTrang === 'HỎNG' ? 'selected' : ''}>🟡 Hỏng đồ</option>

                </select>

            </div>

        `;

        container.appendChild(row);

    });

}



function thayDoiSoLuongLinhKien(index, thayDoi) {

    const item = currentKiemDemData.linhKienKiemDem[index];

    const targetSl = item.slThuc + thayDoi;

    if (targetSl >= 0 && targetSl <= item.slChuan * 2) {

        item.slThuc = targetSl;

        if (item.slThuc < item.slChuan) { item.tinhTrang = "THIẾU"; } 

        else if (item.slThuc === item.slChuan) { item.tinhTrang = "ĐỦ"; }

        renderChecklistLinhKien();

    }

}



function thayDoiTinhTrangLinhKien(index, value) {

    currentKiemDemData.linhKienKiemDem[index].tinhTrang = value;

    if (value === "THIẾU" && currentKiemDemData.linhKienKiemDem[index].slThuc === currentKiemDemData.linhKienKiemDem[index].slChuan) {

        currentKiemDemData.linhKienKiemDem[index].slThuc = Math.max(0, currentKiemDemData.linhKienKiemDem[index].slChuan - 1);

    } else if (value === "ĐỦ") {

        currentKiemDemData.linhKienKiemDem[index].slThuc = currentKiemDemData.linhKienKiemDem[index].slChuan;

    }

    renderChecklistLinhKien();

}



function closePopupKiemDem() { document.getElementById("popupKiemDem").classList.add("hidden"); currentKiemDemData = null; }



async function saveKiemDem() { 

    if(!currentKiemDemData) return; 

    const ghiChuValue = document.getElementById("popGhiChu").value.trim();

    let coSuCoChenhLech = false;

    currentKiemDemData.linhKienKiemDem.forEach(item => {

        let thuc = Number(item.slThuc) || 0;

        let chuan = Number(item.slChuan) || 0;

        if (item.tinhTrang === "THIẾU" || item.tinhTrang === "HỎNG" || thuc < chuan) { coSuCoChenhLech = true; }

    });



    if (coSuCoChenhLech && ghiChuValue === "") {

        playSound('error');

        alert("⚠️ PHÁT HIỆN SỰ CỐ CHÊNH LỆCH LINH KIỆN!\nAnh/Chị bắt buộc phải nhập lý do chênh lệch hoặc số biên bản sự cố trước khi 'Chốt và Chuyển Rửa'.");

        document.getElementById('popGhiChu').focus();

        document.getElementById('popGhiChu').classList.add('border-rose-500', 'ring-2', 'ring-rose-200');

        return;

    }



    try {

        await db.collection("phieuGiaoNhan").doc(currentKiemDemData.id).update({ 

            status: "DANG_RUA", ghiChu: ghiChuValue,

            ktvThuGom: loginUserCode || "CSSD_CHUNG",

            thoiGianDoiSoat: new Date().toLocaleTimeString('vi-VN'),

            coSuCoChenhLech: coSuCoChenhLech,

            chiTietChecklistLinhKien: currentKiemDemData.linhKienKiemDem

        });

        showToast("Đã đối soát xong! Mâm đồ đã chuyển sang Trạm Rửa.", "success"); 

        closePopupKiemDem(); callRender(); 

    } catch(err) { showToast("Lỗi kết nối Firebase nội bộ!", "error"); }

}



function clearGioHang() { gioHangTam = []; renderGioHang(); }



// =========================================================================

// 6. MODULE: QUẢN LÝ MẺ RỬA KHỬ KHUẨN (BELIMED WD250)

// =========================================================================

function toggleSelectAllRua() { let checked = document.getElementById('selectAllRua').checked; document.querySelectorAll('.rua-checkbox').forEach(cb => cb.checked = checked); }

function toggleSelectAllNghiemThuRua() { let checked = document.getElementById('selectAllNghiemThuRua').checked; document.querySelectorAll('.nghiemthurua-checkbox').forEach(cb => cb.checked = checked); }



function capNhatDanhSachMaMayRua() {

    const loaiRua = document.getElementById("rua_loaiRua")?.value;

    const selectMay = document.getElementById("rua_maySo");

    if (!selectMay || !loaiRua) return;

    selectMay.innerHTML = "";

    if (cauHinhMayRua[loaiRua]) {

        cauHinhMayRua[loaiRua].forEach(may => {

            selectMay.innerHTML += `<option value="${may}">${may}</option>`;

        });

    }

    tuDongTaoMaLoMeRua();

}



function tuDongTaoMaLoMeRua() {

    const maMay = document.getElementById("rua_maySo")?.value || "Belimed WD250";

    const maMayVietTat = (maMay === "Belimed WD250") ? "BL" : maMay;

    const now = new Date();

    const yy = String(now.getFullYear()).slice(-2);

    const mm = String(now.getMonth() + 1).padStart(2, '0');

    const dd = String(now.getDate()).padStart(2, '0');

    const ngayChuoi = `${yy}${mm}${dd}`;



    let cacMeTrongNgay = listGiaoDich.filter(x => x.rua_batchCode && x.rua_batchCode.startsWith(maMayVietTat + ngayChuoi));

    let soMeMax = 0;

    cacMeTrongNgay.forEach(x => {

        let phanDuoi = x.rua_batchCode.split("_")[1];

        if (phanDuoi) {

            let num = parseInt(phanDuoi);

            if (num > soMeMax) soMeMax = num;

        }

    });

    let meTiepTheo = soMeMax + 1;

    const chuoiMe = String(meTiepTheo).padStart(2, '0');

    const batchIdHoanChinh = `${maMayVietTat}${ngayChuoi}_${chuoiMe}`;



    if (document.getElementById("rua_meSo")) document.getElementById("rua_meSo").value = chuoiMe;

    if (document.getElementById("rua_batchId")) document.getElementById("rua_batchId").value = batchIdHoanChinh;

}



function xacNhanMeRua() {

    let checkboxes = document.querySelectorAll('.rua-checkbox:checked');

    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm dụng cụ bẩn để xếp vào máy!", "error");

    

    let loaiRua = document.getElementById("rua_loaiRua").value;

    let maMay = document.getElementById("rua_maySo").value;

    let batchCode = document.getElementById("rua_batchId").value;

    let chuKy = document.getElementById("rua_chuKy").value;

    let hoaChat = document.getElementById("rua_hoaChat").value;

    let lieuLuong = document.getElementById("rua_lieuLuong").value || "Theo chuẩn";

    const ngayHomNay = getTodayDateStr();



    let p = [];

    checkboxes.forEach(cb => {

        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({

                status: "TRONG_BUONG_RUA",

                rua_batchCode: batchCode,

                rua_ngayRua: ngayHomNay,

                rua_mayRua: maMay,

                rua_chuKy: chuKy,

                rua_hoaChat: hoaChat,

                rua_lieuLuong: lieuLuong,

                rua_timeBatDau: new Date().toLocaleTimeString('vi-VN')

            })

        );

    });

    Promise.all(p).then(() => {

        playSound('success');

        showToast(`Bắt đầu chạy mẻ máy rửa: ${batchCode}`, "success");

        tuDongTaoMaLoMeRua();

        callRender();

    });

}



function duyetSachMeRuaHangLoat() {

    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');

    if(checkboxes.length === 0) return showToast("Chọn mâm đã rửa sạch để nghiệm thu!", "error");

    

    let ketQuaTest = document.getElementById("rua_testDoSach").value;

    let p = [];

    checkboxes.forEach(cb => {

        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({

                status: "CHO_DONG_GOI",

                rua_ketQuaTestSach: ketQuaTest,

                rua_timeKetThuc: new Date().toLocaleTimeString('vi-VN')

            })

        );

    });

    Promise.all(p).then(() => {

        playSound('success');

        showToast("Xác nhận nghiệm thu đạt chất lượng làm sạch!", "success");

        callRender();

    });

}



function tuChoiMeRuaHangLoat() {

    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');

    if(checkboxes.length === 0) return showToast("Chọn mâm không đạt!", "error");

    

    let p = [];

    checkboxes.forEach(cb => {

        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({

                status: "DANG_RUA",

                rua_batchCode: firebase.firestore.FieldValue.delete(),

                ghiChu: "Làm sạch chưa đạt, yêu cầu rửa lại."

            })

        );

    });

    Promise.all(p).then(() => {

        playSound('error');

        showToast("Đã trả các mâm không đạt về hàng đợi chờ rửa lại!", "success");

        callRender();

    });

}



// =========================================================================

// 7. TRẠM 4: LÀM SẠCH VÀ ĐÓNG GÓI

// =========================================================================

function moPopupDongGoi(id) { idDangDongGoi = id; let item = listGiaoDich.find(x => x.firestoreId === id); if(item) { document.getElementById("popDG_Bo").innerText = item.bo; tinhHanSuDung(); document.getElementById("popupDongGoi").classList.remove("hidden"); } }

function closePopupDongGoi() { document.getElementById("popupDongGoi").classList.add("hidden"); }

function tinhHanSuDung() { let val = document.getElementById("popDG_Loai").value.split("|"); let days = parseInt(val[1]); let dateHSD = new Date(); dateHSD.setDate(dateHSD.getDate() + days); let p = document.getElementById("popDG_Han"); p.innerText = dateHSD.toLocaleDateString('vi-VN'); p.dataset.dateDB = dateHSD.toISOString().split('T')[0]; }

function chotDongGoi() { if(!idDangDongGoi) return; let chatLieuTen = document.getElementById("popDG_Loai").value.split("|")[0]; db.collection("phieuGiaoNhan").doc(idDangDongGoi).update({ status: "CHO_HAP", chatLieu: chatLieuTen, hsd: document.getElementById("popDG_Han").dataset.dateDB }).then(() => { showToast("Đã đóng gói, chuyển chờ hấp!", "success"); closePopupDongGoi(); callRender(); }); }



// =========================================================================

// 8. TRẠM 5: VẬN HÀNH MẺ HẤP & DUYỆT NHẬP KHO VÔ KHUẨN

// =========================================================================

function toggleSelectAllHap() { let checked = document.getElementById('selectAllHap').checked; document.querySelectorAll('.hap-checkbox').forEach(cb => cb.checked = checked); }

function capNhatDanhSachMaMay() { const loaiHap = document.getElementById("hap_loaiHap")?.value; const selectMay = document.getElementById("hap_maySo"); if (!selectMay || !loaiHap) return; selectMay.innerHTML = ""; if (cauHinhMayHap[loaiHap]) { cauHinhMayHap[loaiHap].forEach(may => { selectMay.innerHTML += `<option value="${may}">${may}</option>`; }); } tuDongTaoMaLoMeHap(); }



function tuDongTaoMaLoMeHap() {

    const maMay = document.getElementById("hap_maySo")?.value || "A1"; const now = new Date(); const yy = String(now.getFullYear()).slice(-2); const mm = String(now.getMonth() + 1).padStart(2, '0'); const dd = String(now.getDate()).padStart(2, '0'); const ngayChuoi = `${yy}${mm}${dd}`;

    let cacMeTrongNgay = listGiaoDich.filter(x => x.batchCode && x.batchCode.startsWith(maMay + ngayChuoi)); let soMeMax = 0;

    cacMeTrongNgay.forEach(x => { let phanDuoi = x.batchCode.split("_")[1]; if (phanDuoi) { let num = parseInt(phanDuoi); if (num > soMeMax) soMeMax = num; } });

    let meTiepTheo = soMeMax + 1; if (meTiepTheo > 99) meTiepTheo = 1; const chuoiMe = String(meTiepTheo).padStart(2, '0'); const soLanDone = `${ngayChuoi}_${chuoiMe}`; const batchIdHoanChinh = `${maMay}${soLanDone}`;

    if (document.getElementById("hap_meSo")) document.getElementById("hap_meSo").value = chuoiMe;

    if (document.getElementById("hap_batchId")) document.getElementById("hap_batchId").value = batchIdHoanChinh;

}



function xacNhanMeHap() { 

    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); 

    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 

    

    let loaiHap = document.getElementById("hap_loaiHap").value; 

    let maMay = document.getElementById("hap_maySo").value; 

    let batchCode = document.getElementById("hap_batchId").value; 

    let chuKyNhiet = document.getElementById("hap_nhietDo").value; 

    let apSuat = document.getElementById("hap_apSuat").value || "N/A"; 

    let coKemBI = document.getElementById("hap_hasBI") ? document.getElementById("hap_hasBI").checked : false;

    const ngayHomNay = getTodayDateStr(); 

    let bayGio = new Date(); 

    let thoiGianDuKienXong = new Date(bayGio.getTime() + 45 * 60 * 1000);



    let p = []; 

    checkboxes.forEach(cb => { 

        if(cb.id === 'selectAllHap') return; 

        

        let thongTinLo = { 

            loaiHap: loaiHap, 

            maMay: maMay, 

            chuKyNhiet: chuKyNhiet, 

            apSuat: apSuat, 

            thoiGianBatDau: bayGio.toLocaleTimeString('vi-VN'), 

            giamSatChatLuong: { 

                chiThiHoaHoc: "ĐẠT", 

                laMeTestSinhHocGoc: coKemBI, 

                ketQuaSinhHoc: coKemBI ? "ĐANG CHỜ MÁY Ủ ĐỌC BI (20 PHÚT)" : "KẾ THỪA ĐẦU NGÀY" 

            } 

        };

        

        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({ 

                status: "DANG_HAP", 

                batchCode: batchCode, 

                ngayHapRealtime: ngayHomNay, 

                hasBI: coKemBI, 

                thoiGianKetThucChuTrinh: thoiGianDuKienXong.toISOString(), 

                thongTinLoHap: thongTinLo 

            })

        ); 

    }); 



    Promise.all(p).then(() => { 

        playSound('success');

        showToast(`Kích hoạt lò thành công! Lô: ${batchCode}`, "success"); 

        if(document.getElementById("hap_hasBI")) document.getElementById("hap_hasBI").checked = false; 

        duLieuAnhBiTamBase64 = ""; 

        const fileInp = document.getElementById("input_anhBI"); 

        if(fileInp) fileInp.value = ""; 

        tuDongTaoMaLoMeHap(); 

        callRender(); 

    }).catch(err => {

        showToast("Lỗi khi cập nhật mẻ hấp: " + err.message, "error");

    }); 

}



function toggleSelectAllNghiemThu() { let checked = document.getElementById('selectAllNghiemThu').checked; document.querySelectorAll('.nghiemthu-checkbox').forEach(cb => cb.checked = checked); }



function kiemTraQuyenDuyetMeHap() {

    const nutDuyetDat = document.querySelector("button[onclick='nhapKhoHangLoat()']"); if (!nutDuyetDat) return;

    let checkboxesNghiemThu = document.querySelectorAll('.nghiemthu-checkbox:checked'); let phaiChoKetQuaBI = false;

    checkboxesNghiemThu.forEach(cb => { if (cb.getAttribute('data-hasbi') === 'true') phaiChoKetQuaBI = true; });



    if (phaiChoKetQuaBI && !duLieuAnhBiTamBase64) {

        nutDuyetDat.disabled = true;

        nutDuyetDat.className = "w-full bg-slate-300 text-slate-500 font-black py-2.5 rounded text-xs shadow-md cursor-not-allowed flex items-center justify-center gap-2";

        nutDuyetDat.innerHTML = `<i class="fa-solid fa-lock text-slate-400"></i> CHỜ MINH CHỨNG BI (MÁY Ủ ĐỌC 20 PHÚT)`;

    } else {

        nutDuyetDat.disabled = false;

        nutDuyetDat.className = "w-full bg-emerald-600 hover:bg-emerald-700 text-white font-black py-2.5 rounded text-xs shadow-md flex items-center justify-center gap-2 transition-all cursor-pointer";

        nutDuyetDat.innerHTML = `<i class="fa-solid fa-check-double mr-1"></i> DUYỆT ĐẠT & CHO VÀO KHO LƯU TRỮ CHUNG`;

    }

}



function nhapKhoHangLoat() { 

    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!"); 

    const bayGio = new Date(); let p = []; 

     

    checkboxes.forEach(cb => { 

        if(cb.id === 'selectAllNghiemThu') return; 

        let itemData = listGiaoDich.find(x => x.firestoreId === cb.value); if(!itemData) return;

        let coKemBI = itemData.hasBI === true;

        let thoiGianHapXong = itemData.thoiGianKetThucChuTrinh ? new Date(itemData.thoiGianKetThucChuTrinh) : new Date(bayGio.getTime() - 45 * 60 * 1000);

        let soPhutThucTe = Math.floor((bayGio - thoiGianHapXong) / (1000 * 60)); 

        let soPhutTinhKPI = coKemBI ? Math.max(0, soPhutThucTe - 20) : soPhutThucTe;

        let trangThaiKPI = (soPhutTinhKPI > 30) ? "KHÔNG ĐẠT" : "ĐẠT";



        let bgGiamSat = itemData.thongTinLoHap?.giamSatChatLuong || {};

        let updateGiamSat = {

            ...bgGiamSat,

            ketQuaSinhHoc: coKemBI ? "ÂM TÍNH (ĐẠT)" : "KẾ THỪA ĐẦU NGÀY",

            minhChungAnhBase64: coKemBI ? duLieuAnhBiTamBase64 : (bgGiamSat.minhChungAnhBase64 || "")

        };



        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({

                status: "CHO_XUAT", thoiGianKetLuan: bayGio.toISOString(), thoiGianDocTestBiPhut: soPhutThucTe, 

                thoiGianTinhKpiPhut: soPhutTinhKPI, ketQuaGiamSatKpi: trangThaiKPI, "thongTinLoHap.giamSatChatLuong": updateGiamSat

            })

        ); 

    }); 

     

    Promise.all(p).then(() => { 

        playSound('success');

        showToast("Đã duyệt mâm đạt chuẩn Vô Khuẩn!", "success"); 

        duLieuAnhBiTamBase64 = ""; const fileInp = document.getElementById("input_anhBI"); if(fileInp) fileInp.value = ""; 

        callRender(); 

    }); 

}



function tuChoiHapHangLoat() { 

    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 

    let p = []; 

    checkboxes.forEach(cb => { if(cb.id !== 'selectAllNghiemThu') p.push(db.collection("phieuGiaoNhan").doc(cb.value).update({status: "DANG_RUA", ghiChu: "Không đạt hấp, trả về rửa lại"})); }); 

    Promise.all(p).then(() => { playSound('error'); showToast("Đã trả các mâm không đạt về Trạm làm sạch!", "success"); callRender(); }); 

}



// =========================================================================

// 9. TRẠM 6: KHO VÔ KHUẨN & PHỐI HỢP CẤP PHÁT ĐIỀU PHỐI XOAY VÒNG (FIFO)

// =========================================================================

function xuatKhoXoayVong(maTrucTiep) { 

    const k = document.getElementById("xuat_selKhoa") ? document.getElementById("xuat_selKhoa").value : ""; 

    let ma = maTrucTiep || (document.getElementById("xuat_inpMaBo") ? document.getElementById("xuat_inpMaBo").value.trim().toUpperCase() : ""); 

    

    if(!k) { playSound('error'); return showToast("Vui lòng Chọn Khoa nhận trước khi xuất!", "error"); } 

    if(!ma) return;

    if(gioHangXuatKho.some(x => x.maMacDinh === ma)) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách xuất!", "error"); }

    

    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && x.status === "CHO_XUAT"); 

    if(!khayThucTe) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast(`Mã ID ${ma} không sẵn sàng ở Kho Vô Khuẩn.`, "error"); } 

    

    gioHangXuatKho.push(khayThucTe); playSound('success'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; renderGioHangXuat();

}



function renderGioHangXuat() {

    let listContainer = document.getElementById("danhSachXuatTam");

    if (!listContainer) {

        let bangKho = document.getElementById("bangKhoVoKhuan");

        if (bangKho && bangKho.closest('table')) {

            let wrapper = document.createElement("div"); wrapper.id = "danhSachXuatTam";

            wrapper.className = "mb-4 bg-sky-50 border border-sky-200 rounded p-3 hidden shadow-inner";

            bangKho.closest('table').parentNode.insertBefore(wrapper, bangKho.closest('table')); listContainer = wrapper;

        }

    }

    if(listContainer) {

        if(gioHangXuatKho.length === 0) { listContainer.classList.add("hidden"); } 

        else {

            listContainer.classList.remove("hidden");

            let html = `<div class="flex justify-between items-center mb-2 pb-2 border-b border-sky-200"><h3 class="font-bold text-sky-800 text-[13px]"><i class="fa-solid fa-cart-flatbed mr-2"></i>Đang quét xuất kho: <span class="bg-rose-500 text-white px-2 py-0.5 rounded ml-1">${gioHangXuatKho.length} mâm</span></h3><button onclick="xacNhanXuatKhoHangLoat()" class="bg-emerald-600 text-white px-4 py-1.5 rounded shadow font-black text-xs hover:bg-emerald-700 transition-colors uppercase"><i class="fa-solid fa-check-double mr-1"></i>Chốt Bàn Giao</button></div><div class="flex flex-wrap gap-2">`;

            gioHangXuatKho.forEach((item, index) => { 

                let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "Dụng cụ";

                html += `<div class="bg-white border border-sky-300 px-2 py-1 rounded flex items-center gap-2 shadow-sm animate-pulse"><span class="text-[11px] font-bold text-slate-700">${tenBoGoc}</span><span class="text-[10px] font-mono text-sky-700 bg-sky-100 px-1 rounded font-bold">${item.maMacDinh}</span><i class="fa-solid fa-xmark text-rose-500 cursor-pointer ml-1 text-xs hover:text-rose-700" onclick="xoaKhoiGioXuat(${index})"></i></div>`; 

            });

            html += `</div>`; listContainer.innerHTML = html;

        }

    }

}



function xoaKhoiGioXuat(index) { gioHangXuatKho.splice(index, 1); renderGioHangXuat(); }



function xacNhanXuatKhoHangLoat() {

    const k = document.getElementById("xuat_selKhoa").value; if(!k || gioHangXuatKho.length === 0) return;

    let p = []; 

    gioHangXuatKho.forEach(khay => { 

        p.push(

            db.collection("phieuGiaoNhan").doc(khay.firestoreId).update({ 

                status: "ĐANG_VAN_CHUYEN", khoa: k, ngayHoanTat: getTodayDateStr(), timeHoanTat: new Date().toLocaleTimeString('vi-VN'), nvXuatKho: loginUserCode || "CSSD_CHUNG" 

            })

        ); 

    });

    Promise.all(p).then(() => { 

        playSound('success'); showToast(`Đã xuất kho bàn giao về Khoa ${k}!`, "success"); 

        gioHangXuatKho = []; if (document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; 

        renderGioHangXuat(); callRender(); 

    });

}



function duyetCapPhatTậpTrung(yeuCauId, tenBo, khoaNhan) {

    let khaySẵnCo = listGiaoDich.find(x => x.status === "CHO_XUAT" && x.bo.split(" [ID:")[0].trim().toUpperCase() === tenBo.trim().toUpperCase());

    

    if (!khaySẵnCo) {

        playSound('error');

        return alert(`⚠️ KHÔNG CÓ KHAY SẴN SÀNG TRONG KHO!\nHiện tại trong Kho vô khuẩn CSSD không có khay "${tenBo}" nào sẵn sàng để cấp phát.`);

    }



    if (confirm(`Xác nhận cấp phát khay ${khaySẵnCo.maMacDinh} để đáp ứng yêu cầu của khoa ${khoaNhan}?`)) {

        let bayGio = new Date();

        let batch = db.batch();

        

        let refKhay = db.collection("phieuGiaoNhan").doc(khaySẵnCo.firestoreId);

        batch.update(refKhay, {

            status: "ĐANG_VAN_CHUYEN",

            khoa: khoaNhan,

            ngayHoanTat: getTodayDateStr(),

            timeHoanTat: bayGio.toLocaleTimeString('vi-VN'),

            nvXuatKho: loginUserCode || "CSSD_CHUNG"

        });



        let refYeuCau = db.collection("phieuGiaoNhan").doc(yeuCauId);

        batch.delete(refYeuCau);



        batch.commit().then(() => {

            playSound('success');

            showToast(`Đã cấp phát thành công khay ${khaySẵnCo.maMacDinh} cho khoa ${khoaNhan}!`, "success");

            callRender();

        });

    }

}



function khoaKyNhanDoSachDienTu() {

    const khoaDangNhap = currentRole === "KHOA" ? loginUserCode : (document.getElementById("khoa_selKhoa") ? document.getElementById("khoa_selKhoa").value : "");

    const nameNguoiDung = loginUserCode || "Điều dưỡng khoa";

    const checkboxes = document.querySelectorAll("#bangChoNhanTaiKhoa input[type='checkbox']:checked");

    const dsIdDuocChon = []; checkboxes.forEach(chk => dsIdDuocChon.push(chk.value));



    if (dsIdDuocChon.length === 0) { playSound('error'); return showToast("Vui lòng tích chọn khay để nhận!", "error"); }



    if (confirm(`Xác nhận ký điện tử nhận ${dsIdDuocChon.length} mâm về khoa ${khoaDangNhap}?`)) {

        let p = [];

        dsIdDuocChon.forEach(idDoc => {

            p.push(

                db.collection("phieuGiaoNhan").doc(idDoc).update({

                    status: "HOAN_TAT", nguoiKyNhanKhoa: nameNguoiDung, timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'), ngayKhoaNhanThucTe: getTodayDateStr()

                })

            );

        });

        Promise.all(p).then(() => { playSound('success'); showToast(`Đã nhận đồ vô khuẩn vào tủ khoa!`, "success"); callRender(); });

    }

}



// =========================================================================

// 10. GHI NHẬN SỬ DỤNG BỘ DỤNG CỤ TRÊN BỆNH NHÂN

// =========================================================================

function moPopupSuDungBoDungCu() {

    gioKhaySuDungTam = []; document.getElementById("sd_bangKhayChon").innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng quét hoặc nhập mã khay...</td></tr>`;

    document.getElementById("sd_ngaySuDung").value = getTodayDateStr(); document.getElementById("sd_nhanChung").value = loginUserCode || "ADMIN";

    if(document.getElementById("sd_khoaBenhNhan")) document.getElementById("sd_khoaBenhNhan").value = (currentRole === "KHOA") ? loginUserCode : "";

    if(document.getElementById("sd_yTaPhongMo")) document.getElementById("sd_yTaPhongMo").value = "";

    if(document.getElementById("sd_yTaVongNgoai")) document.getElementById("sd_yTaVongNgoai").value = "";

    if(document.getElementById("sd_searchBN")) document.getElementById("sd_searchBN").value = "";

    if(document.getElementById("sd_ghiChu")) document.getElementById("sd_ghiChu").value = "";

    if(document.getElementById("sd_maKhayInp")) document.getElementById("sd_maKhayInp").value = "";

    document.getElementById("popupSuDungBoDungCu").classList.remove("hidden");

}



function closePopupSuDung() { document.getElementById("popupSuDungBoDungCu").classList.add("hidden"); }



function scanKhayVaoSuDung() {

    let ma = document.getElementById("sd_maKhayInp").value.trim().toUpperCase(); if(!ma) return;

    document.getElementById("sd_maKhayInp").value = "";

    if(gioKhaySuDungTam.some(x => x.maMacDinh === ma)) { playSound('error'); return showToast("Mã khay này đã được quét!", "error"); }



    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT"));

    if(!khayThucTe) { playSound('error'); return showToast(`Mã ID ${ma} không hợp lệ hoặc chưa vô khuẩn!`, "error"); }



    gioKhaySuDungTam.push(khayThucTe); playSound('success'); renderBangKhaySuDung();

}



function renderBangKhaySuDung() {

    const tbody = document.getElementById("sd_bangKhayChon"); if(!tbody) return;

    if(gioKhaySuDungTam.length === 0) { tbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Chưa chọn khay...</td></tr>`; return; }

    tbody.innerHTML = gioKhaySuDungTam.map((item, index) => {

        let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A";

        return `<tr class="border-b font-medium hover:bg-slate-50 text-xs">

            <td class="p-2 text-center"><i class="fa-solid fa-square-check text-teal-600 text-sm"></i></td>

            <td class="p-2 font-mono font-bold text-sky-700">${item.maMacDinh}</td>

            <td class="p-2 font-bold text-slate-800">${tenBoGoc}</td>

            <td class="p-2 text-center font-mono font-bold text-rose-600 bg-rose-50/30">${item.batchCode || 'N/A'}</td>

            <td class="p-2 text-center"><span class="px-2 py-0.5 rounded text-[10px] font-bold bg-teal-100 text-teal-800">${item.status}</span></td>

            <td class="p-2 text-center"><i class="fa-solid fa-trash-can text-rose-500 cursor-pointer hover:text-rose-700" onclick="xoaKhayKhoiListSuDung(${index})"></i></td>

        </tr>`;

    }).join('');

}



function xoaKhayKhoiListSuDung(index) {

    gioKhaySuDungTam.splice(index, 1);

    renderBangKhaySuDung();

}



// =========================================================================

// 11. THIẾT LẬP MÁY IN BIXOLON SLP-TX403 (80mm x 50mm) & BIÊN BẢN HÀNG LOẠT

// =========================================================================



// Hàm in tem chuẩn Bixolon SLP-TX403 khổ 80mm x 50mm cho 1 item

function inTemBixolonChuan(itemData) {

    const pZone = document.getElementById("print-zone");

    if (!pZone) return;



    const tenBo = itemData.bo ? itemData.bo.split(" [ID:")[0] : "DỤNG CỤ VÔ KHUẨN";

    const maID = itemData.maMacDinh || itemData.idKhay || "000000";

    const ngayNSX = itemData.ngayHapRealtime || itemData.ngayTao || getTodayDateStr();

    const ngayHSD = itemData.hsd ? new Date(itemData.hsd).toLocaleDateString('vi-VN') : "Theo quy định";

    const maBatch = itemData.batchCode || itemData.batchId || "N/A";

    const tenKhoa = itemData.khoa || "TẬP TRUNG";



    pZone.innerHTML = `

        <div class="bixolon-label" style="font-family: Arial, sans-serif;">

            <div style="text-align: center; border-bottom: 1.5px solid #000; padding-bottom: 2px;">

                <div style="font-size: 10px; font-weight: bold; text-transform: uppercase;">PHUONG NAM HOSPITAL - CSSD</div>

                <div style="font-size: 12px; font-weight: 900; margin-top: 1px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${tenBo}</div>

            </div>



            <div style="text-align: center; margin: 3px 0; display: flex; justify-content: center;">

                <svg id="bixolon-single-barcode"></svg>

            </div>



            <div style="font-size: 9px; border-top: 1px dashed #000; padding-top: 2px;">

                <div style="display: flex; justify-content: space-between; font-weight: bold;">

                    <span>LÔ: ${maBatch}</span>

                    <span>KHOA: ${tenKhoa}</span>

                </div>

                <div style="display: flex; justify-content: space-between; margin-top: 1px;">

                    <span>NSX: ${ngayNSX}</span>

                    <span style="font-size: 10px; font-weight: 900;">HSD: ${ngayHSD}</span>

                </div>

            </div>

        </div>

    `;



    JsBarcode("#bixolon-single-barcode", maID, {

        format: "CODE128",

        width: 1.6,

        height: 36,

        displayValue: true,

        fontSize: 10,

        fontOptions: "bold",

        margin: 0

    });



    pZone.classList.remove("hidden");

    setTimeout(() => {

        window.print();

        pZone.classList.add("hidden");

    }, 250);

}



function inTemTongHangLoat() { 

    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 

    let batchCode = document.getElementById("hap_batchId")?.value || "A1000000_01"; 

    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 

    let stylePrint = document.createElement('style'); 

    stylePrint.innerHTML = `@media print { body * { visibility: hidden; } #print-zone, #print-zone * { visibility: visible; } #print-zone { position: absolute; left: 0; top: 0; width: 80mm; margin: 0; padding: 0; background: white; } @page { size: 80mm 50mm; margin: 0; } .single-tem { width: 80mm !important; height: 50mm !important; page-break-inside: avoid; break-inside: avoid; page-break-after: always; margin: 0 !important; padding: 3mm 4mm !important; box-sizing: border-box !important; display: flex !important; flex-direction: column !important; justify-content: space-between !important; } }`; 

    container.appendChild(stylePrint); 



    checkboxes.forEach((cb) => { 

        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 

        if(item) { 

            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 

            container.innerHTML += `<div class="single-tem" style="font-family: Arial; font-size: 11px; color: #000; background: #fff; border: 1px solid #000;"><div style="text-align: center; font-weight: bold; font-size: 12px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-lo-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span></div><div style="display: flex; justify-content: space-between; border-top: 1px dashed #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 9px; font-family: monospace; font-weight: bold; margin-top: 1px;">Lô: ${batchCode}</div></div>`; 

        } 

    }); 

    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); pZone.classList.remove("hidden"); 

    checkboxes.forEach(cb => { 

        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 

        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-lo-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 

    }); 

    setTimeout(() => { window.print(); pZone.classList.add("hidden"); }, 300); 

}



function inTemNghiemThuHangLoat() { 

    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 

    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 

    let stylePrint = document.createElement('style'); 

    stylePrint.innerHTML = `@media print { body * { visibility: hidden; } #print-zone, #print-zone * { visibility: visible; } #print-zone { position: absolute; left: 0; top: 0; width: 80mm; margin: 0; padding: 0; background: white; } @page { size: 80mm 50mm; margin: 0; } .single-tem { width: 80mm !important; height: 50mm !important; page-break-inside: avoid; break-inside: avoid; page-break-after: always; margin: 0 !important; padding: 3mm 4mm !important; box-sizing: border-box !important; display: flex !important; flex-direction: column !important; justify-content: space-between !important; } }`; 

    container.appendChild(stylePrint); 



    checkboxes.forEach((cb) => { 

        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 

        if(item) { 

            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 

            container.innerHTML += `<div class="single-tem" style="font-family: Arial; font-size: 11px; color: #000; background: #fff; border: 1px solid #000;"><div style="text-align: center; font-size: 9px; font-weight: bold; letter-spacing: 0.5px;">PN HOSPITAL - CSSD</div><div style="text-align: center; font-weight: bold; font-size: 12px; margin: 1px 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-nt-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span><span style="color: green;">ĐẠT VÔ KHUẨN</span></div><div style="display: flex; justify-content: space-between; border-top: 1px solid #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 8px; font-weight: bold; font-family: monospace; margin-top: 1px;">BATCH: ${item.batchCode || 'N/A'}</div></div>`; 

        } 

    }); 

    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); pZone.classList.remove("hidden"); 

    checkboxes.forEach(cb => { 

        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 

        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-nt-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 

    }); 

    setTimeout(() => { window.print(); pZone.classList.add("hidden"); }, 300); 

}



function inHoaDonGiaoNhan() {

    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; if (!k) return showToast("Chọn Khoa trước!", "error");

    let htmlBang = `<table style="width:100%; border-collapse:collapse; margin-top:15px; font-size:12px; text-align:left; font-family:Arial, sans-serif;"><thead><tr style="background-color:#f8fafc; border:1px solid #000;"><th style="padding:8px; border:1px solid #000; width:40%;">PHÂN LOẠI MÂM / LOẠI DỤNG CỤ</th><th style="padding:8px; border:1px solid #000; text-align:center; width:15%;">ĐÃ TRẢ BẨN</th><th style="padding:8px; border:1px solid #000; text-align:center; width:15%;">ĐÃ NHẬN SẠCH</th><th style="padding:8px; border:1px solid #000; text-align:center; width:15%;">NHẬN VÔ KHUẨN</th><th style="padding:8px; border:1px solid #000; text-align:center; width:15%;">CSSD NỢ KHOA</th></tr></thead><tbody>`;

    const rows = document.querySelectorAll('#bangDonGiaoNhan tr');

    if (rows.length === 0 || (rows.length === 1 && rows[0].cells.length === 1)) { htmlBang += `<tr><td colspan="5" style="padding:12px; border:1px solid #000; text-align:center; font-style:italic;">Không có dữ liệu công nợ.</td></tr>`; } 

    else {

        rows.forEach(row => {

            const cells = row.cells;

            if (cells.length >= 4) { htmlBang += `<tr style="border:1px solid #000;"><td style="padding:8px; border:1px solid #000; font-weight:bold;">${cells[0].innerText}</td><td style="padding:8px; border:1px solid #000; text-align:center; color:#e11d48;">${cells[1].innerText}</td><td style="padding:8px; border:1px solid #000; text-align:center; color:#0284c7;">${cells[2].innerText}</td><td style="padding:8px; border:1px solid #000; text-align:center; color:#7c3aed;">${cells[3].innerText}</td><td style="padding:8px; border:1px solid #000; text-align:center; font-weight:bold; color:#d97706;">${cells[4].innerText}</td></tr>`; }

        });

    }

    htmlBang += `</tbody></table>`;

    const pZone = document.getElementById("print-zone");

    pZone.innerHTML = `<div style="width:100%; font-family:Arial, sans-serif; padding:10px; color:#000;"><div style="text-align:center; margin-bottom:20px;"><h2 style="margin:0; font-size:16px; font-weight:900; text-transform:uppercase;">BIÊN BẢN GIAO NHẬN DỤNG CỤ CSSD</h2><p style="margin:5px 0 0 0; font-size:12px; font-weight:bold;">Khoa/Phòng: <span style="text-transform:uppercase;">${k}</span> - Ngày xuất: ${new Date().toLocaleDateString('vi-VN')}</p></div>${htmlBang}<div style="margin-top:40px; display:flex; justify-content:space-between; font-size:11px; font-weight:bold; padding:0 20px;"><div style="text-align:center; width:45%;"><p style="margin-bottom:60px; text-transform:uppercase;">ĐIỀU DƯỠNG LÂM SÀNG</p><p style="font-weight:normal; font-style:italic; color:#64748b;">(Ký và ghi rõ họ tên)</p></div><div style="text-align:center; width:45%;"><p style="margin-bottom:60px; text-transform:uppercase;">CHUYÊN VIÊN CSSD</p><p style="font-weight:normal; font-style:italic; color:#64748b;">(Ký và ghi rõ họ tên)</p></div></div></div>`;

    pZone.classList.remove("hidden"); window.print(); pZone.classList.add("hidden");

}



// =========================================================================

// 12. HÀM PHỤ TRỢ: CHỨC NĂNG QUẢN TRỊ ADMIN & TIỆN ÍCH

// =========================================================================

function taiDanhMucLinhKienChuan() {

    const tbody = document.getElementById("bangDanhMucLinhKien"); if (!tbody) return; tbody.innerHTML = "";

    if (!databaseExcel || databaseExcel.length === 0) { tbody.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-slate-400 italic">Chưa có dữ liệu. Vui lòng nạp file Excel.</td></tr>`; return; }

    let gopBoExcel = {};

    databaseExcel.forEach(x => {

        let tenBo = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; if (!tenBo) return;

        tenBo = String(tenBo).trim().toUpperCase(); if (!gopBoExcel[tenBo]) gopBoExcel[tenBo] = []; gopBoExcel[tenBo].push(x);

    });

    for (let tenBo in gopBoExcel) {

        let linhKienHtml = `<div class="flex flex-wrap gap-1">`; let tongSl = 0;

        gopBoExcel[tenBo].forEach(item => {

            let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || item['Dụng cụ'] || item['Tên Chi Tiết'] || "Dụng cụ Chi Tiết";

            

            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {

                tenDc = "Dụng cụ chuẩn mâm";

            }

            

            let sl = parseInt(item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1) || 0; let hanHap = item['Tuổi thọ mẻ hấp'] || item['Tuổi thọ'] || 100;

            tongSl += sl; linhKienHtml += `<span class="bg-slate-100 text-slate-700 px-1.5 py-0.5 rounded text-[10px] font-medium border border-slate-200">${tenDc} (${sl}) <span class="text-amber-600 font-bold">[Max: ${hanHap}]</span></span>`;

        });

        linhKienHtml += `</div>`; const tr = document.createElement("tr"); tr.className = "hover:bg-slate-50 transition-colors border-b";

        tr.innerHTML = `<td class="p-3 font-bold text-slate-800 text-xs">${tenBo}</td><td class="p-3">${linhKienHtml}</td><td class="p-3 text-center font-black text-sky-700 bg-sky-50/30">${tongSl}</td>`;

        tbody.appendChild(tr);

    }

}



function updateGiaoDienMatrix(role, tabId, checkboxElement) {

    if(!cauHinhGiaoDien[role]) cauHinhGiaoDien[role] = [];

    if(checkboxElement.checked) { if(!cauHinhGiaoDien[role].includes(tabId)) cauHinhGiaoDien[role].push(tabId); } 

    else { cauHinhGiaoDien[role] = cauHinhGiaoDien[role].filter(x => x !== tabId); }

    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ cauHinhGiaoDien: cauHinhGiaoDien }).then(() => showToast("Đã cập nhật quyền truy cập!", "success"));

}



function handleViewAnhBiMoi(firestoreId) { hanhDongXemAnhBiMoi(firestoreId); }

function hanhDongXemAnhBiMoi(firestoreId) {

    let item = listGiaoDich.find(x => x.firestoreId === firestoreId); if (!item) return showToast("Không tìm thấy thông tin!", "error");

    let loGoc = listGiaoDich.find(m => m.batchCode === item.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 

    let chuoiAnhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || item.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;

    if (!chuoiAnhBase64) return showToast("Mẻ này chưa tải ảnh minh chứng hoặc kế thừa.", "error");

    let w = window.open(); w.document.write(`<html><head><title>MINH CHỨNG BI - LÔ ${item.batchCode}</title></head><body style='margin:0; background:#000; display:flex; justify-content:center; align-items:center;'><img src='${chuoiAnhBase64}' style='max-width:100%; max-height:100vh; object-fit:contain;'/></body></html>`);

}



function switchAdminSubtab(sub) { 

    document.getElementById('subtab-database')?.classList.add('hidden'); document.getElementById('subtab-security')?.classList.add('hidden'); 

    document.getElementById('subbtn-database')?.classList.replace('admin-subtab-active', 'text-slate-600'); document.getElementById('subbtn-security')?.classList.replace('admin-subtab-active', 'text-slate-600'); 

    document.getElementById('subtab-' + sub)?.classList.remove('hidden'); document.getElementById('subbtn-' + sub)?.classList.add('admin-subtab-active'); 

}



function saveAdminPIN(type) { let newVal = document.getElementById(`cfg_pin${type.charAt(0).toUpperCase() + type.slice(1).toLowerCase()}`).value.trim(); if(type === 'ADMIN') thongTinMatKhauAdmin.adminPIN = newVal; if(type === 'CSSD') thongTinMatKhauAdmin.cssdPIN = newVal; if(type === 'GUEST') thongTinMatKhauAdmin.guestPIN = newVal; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ thongTinMatKhauAdmin: thongTinMatKhauAdmin }).then(() => showToast("Đã lưu PIN!", "success")); }

function themKtvCssd() { let code = prompt("Mã NV:"); let ten = prompt("Tên:"); let pin = prompt("PIN:"); if(code && ten && pin) { danhSachKtvCssd.push({ code: code.toUpperCase(), ten: ten, pin: pin }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }

function xaoKtvCssd(code) { if(confirm("Xóa?")) { danhSachKtvCssd = danhSachKtvCssd.filter(x => x.code !== code); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }

function themKhoaThuCong() { let t = prompt("Nhập Tên Khoa/Phòng:"); if(t) { danhSachKhoa.push({ ten: t.toUpperCase(), pin: "123", danhSachBo: [] }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Thêm Khoa thành công!", "success")); } }



function khaiSinhKhayVangLai() {

    if(danhSachKhoa.length === 0) return showToast("Hệ thống chưa có Khoa nào!", "error");

    let tenKhay = prompt("Nhập Tên Mâm/Khay dụng cụ:"); if(!tenKhay) return;

    let maId = prompt("Nhập Mã ID khay:"); if(!maId) return; maId = maId.toUpperCase().trim();

    let dsTenKhoa = danhSachKhoa.map((k, idx) => `${idx + 1}. ${k.ten}`).join("\n");

    let luaChon = prompt(`Chọn số thứ tự Khoa sở hữu:\n${dsTenKhoa}`); if(!luaChon) return;

    let idxKhoa = parseInt(luaChon) - 1; if(idxKhoa < 0 || idxKhoa >= danhSachKhoa.length) return showToast("Lựa chọn không hợp lệ!", "error");

    let khoaChon = danhSachKhoa[idxKhoa]; let chuoiKhayDinhDang = `${tenKhay.toUpperCase().trim()} [ID:${maId}]`;

    if(!khoaChon.danhSachBo) khoaChon.danhSachBo = []; if(khoaChon.danhSachBo.includes(chuoiKhayDinhDang)) return showToast("Khay đã tồn tại!", "error");

    khoaChon.danhSachBo.push(chuoiKhayDinhDang);

    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => { showToast(`Khai sinh khay ${maId} thành công!`, "success"); callRender(); });

}



function updatePINTrựcTiep(idx, tenKhoa) { let p = document.getElementById(`pin-khoa-${idx}`).value.trim(); if(p) { danhSachKhoa.find(x => x.ten === tenKhoa).pin = p; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Đổi PIN thành công", "success")); } }



function initSelects() { 

    let opts = '<option value="">-- Chọn Khoa --</option>' + danhSachKhoa.map(k=>`<option value="${k.ten}">${k.ten}</option>`).join('');

    if(document.getElementById("login_khoa")) document.getElementById("login_khoa").innerHTML = opts; 

    if(document.getElementById("khoa_selKhoa")) document.getElementById("khoa_selKhoa").innerHTML = opts; 

    if(document.getElementById("login_nv_cssd")) document.getElementById("login_nv_cssd").innerHTML = '<option value="">-- Chọn KTV CSSD --</option>' + danhSachKtvCssd.map(k=>`<option value="${k.code}">${k.code} - ${k.ten}</option>`).join(''); 

     

    // Khởi tạo các danh mục hóa chất & máy rửa mẻ

    if (document.getElementById("rua_hoaChat")) {

        document.getElementById("rua_hoaChat").innerHTML = danhMucHoaChatRua.map(hc => `<option value="${hc.ten}">${hc.ten}</option>`).join('');

        document.getElementById("rua_lieuLuong").value = danhMucHoaChatRua[0].lieuLuong;

        document.getElementById("rua_hoaChat").addEventListener("change", (e) => {

            let selectedHc = danhMucHoaChatRua.find(hc => hc.ten === e.target.value);

            if(selectedHc) document.getElementById("rua_lieuLuong").value = selectedHc.lieuLuong;

        });

    }

    if (document.getElementById("rua_loaiRua")) { capNhatDanhSachMaMayRua(); }

    if (document.getElementById("hap_loaiHap")) { capNhatDanhSachMaMay(); }

     

    const selKhoaTraba = document.getElementById("khoa_selKhoa"); if(selKhoaTraba) { selKhoaTraba.removeEventListener("change", loadBoDungCuTheoKhoa); selKhoaTraba.addEventListener("change", loadBoDungCuTheoKhoa); }

}



function showToast(msg, type="error") { const t = document.createElement('div'); t.className = `fixed top-6 right-6 ${type==="error"?"bg-rose-600":"bg-emerald-600"} text-white px-5 py-3.5 rounded-lg shadow-2xl z-[100] font-bold text-sm`; t.innerHTML = msg; document.body.appendChild(t); setTimeout(() => t.remove(), 2500); }

function toggleMobileMenu() { document.getElementById("sidebar_menu").classList.toggle("-translate-x-full"); document.getElementById("mobile-overlay").classList.toggle("hidden"); }



function moCamera(inputId) { targetInputIdForScan = inputId; document.getElementById("popupScanner").classList.remove("hidden"); html5QrCode = new Html5Qrcode("reader"); Html5Qrcode.getCameras().then(devices => { let cid = devices.length > 1 ? devices[devices.length - 1].id : devices[0].id; html5QrCode.start(cid, { fps: 15, qrbox: { width: 260, height: 180 } }, (txt) => { document.getElementById(targetInputIdForScan).value = txt.trim().toUpperCase(); if(targetInputIdForScan==='khoa_inpMaBo') themVaoGio(); if(targetInputIdForScan==='xuat_inpMaBo') xuatKhoXoayVong(); if(targetInputIdForScan==='sd_maKhayInp') scanKhayVaoSuDung(); }).catch(e=>{}) }); }

function dongCamera() { if(html5QrCode) html5QrCode.stop().then(() => html5QrCode.clear()); document.getElementById("popupScanner").classList.add("hidden"); }

function xoaSachDuLieuGiaoDichRealtime() { if(prompt("Nhập PIN ADMIN:") === (thongTinMatKhauAdmin.adminPIN||"admin2026")) { db.collection("phieuGiaoNhan").get().then(snap => { let b = db.batch(); snap.forEach(d => b.delete(d.ref)); b.commit().then(() => location.reload()); }); } }



function docAnhBiUpTaiCho(inputElement) { 

    const file = inputElement.files[0]; if (!file) return; 

    const reader = new FileReader(); reader.onloadend = function() { duLieuAnhBiTamBase64 = reader.result; showToast("Tải ảnh minh chứng BI thành công!", "success"); kiemTraQuyenDuyetMeHap(); }; 

    reader.readAsDataURL(file); 

}



// =========================================================================

// 13. HÀM CORE RENDER DỮ LIỆU ĐA TAB LUỒNG HỆ THỐNG

// =========================================================================

function renderTheoTabHienTai() {

    if(activeTab === 'khoaphong') {

        const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;

        let tatCaDonCuaKhoa = listGiaoDich.filter(x => (x.khoa || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase());

        

        // 1. TÍNH TOÁN CÔNG NỢ LUÂN CHUYỂN THỰC TẾ

        let gopCongNo = {};

        tatCaDonCuaKhoa.forEach(x => {

            if (x.status === "CHO_CAP_PHAT") return;



            let tenLoaiMâm = x.bo ? String(x.bo).split(" [ID:")[0] : "Chưa rõ mâm";

            if (!gopCongNo[tenLoaiMâm]) gopCongNo[tenLoaiMâm] = { daTraBan: 0, nhanSach: 0, nhanVoKhuan: 0, cssdNo: 0 };

            

            if (x.status !== "CHO_THU") gopCongNo[tenLoaiMâm].daTraBan += 1; 

            if (["DANG_RUA", "TRONG_BUONG_RUA", "CHO_DONG_GOI", "CHO_HAP", "DANG_HAP", "CHO_XUAT", "ĐANG_VAN_CHUYEN", "HOAN_TAT", "DA_SU_DUNG"].includes(x.status)) gopCongNo[tenLoaiMâm].nhanSach += 1;

            if (x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG") gopCongNo[tenLoaiMâm].nhanVoKhuan += 1; 

            gopCongNo[tenLoaiMâm].cssdNo = gopCongNo[tenLoaiMâm].daTraBan - gopCongNo[tenLoaiMâm].nhanVoKhuan;

        });

        

        let arrHtml = [];

        for (let key in gopCongNo) {

            let item = gopCongNo[key];

            arrHtml.push(`<tr class="border-b hover:bg-slate-50 transition-colors font-medium text-[11px]"><td class="p-3 font-bold text-slate-800 border-r">${key}</td><td class="p-3 text-center text-rose-600 font-bold bg-rose-50/20 border-r">${item.daTraBan}</td><td class="p-3 text-center text-sky-600 font-bold bg-sky-50/20 border-r">${item.nhanSach}</td><td class="p-3 text-center text-purple-600 font-bold bg-purple-50/20 border-r">${item.nhanVoKhuan}</td><td class="p-3 text-center text-amber-600 font-black bg-amber-50/20">${item.cssdNo > 0 ? 'Nợ ' + item.cssdNo : '-'}</td></tr>`);

        }

        if(document.getElementById("bangDonGiaoNhan")) {

            document.getElementById("bangDonGiaoNhan").innerHTML = arrHtml.length ? arrHtml.join('') : `<tr><td colspan="5" class="p-8 text-center text-slate-400 italic">Khoa chưa phát sinh công nợ luân chuyển đồ.</td></tr>`;

        }

        

        // 2. DANH SÁCH CHỜ NHẬN ĐỒ SẠCH (Từ xe trung chuyển CSSD bàn giao lên)

        const tbodyChoNhan = document.getElementById("bangChoNhanTaiKhoa"); const badgeChoNhan = document.getElementById("badgeChoNhanKhoa"); const txtNguoiXacNhan = document.getElementById("txtNguoiDungNhanHienTai");

        if(txtNguoiXacNhan) txtNguoiXacNhan.innerText = `PM-ĐD: ${loginUserCode || "Chưa Đăng Nhập"}`;

        

        let dsDangVanChuyen = tatCaDonCuaKhoa.filter(x => x.status === "ĐANG_VAN_CHUYEN"); if(badgeChoNhan) badgeChoNhan.innerText = `${dsDangVanChuyen.length} khay`;

        if(tbodyChoNhan) {

            if(dsDangVanChuyen.length === 0) { tbodyChoNhan.innerHTML = `<tr><td colspan="5" class="p-3 text-center text-slate-400 italic">Hiện không có dụng cụ nào đang chuyển về khoa.</td></tr>`; } 

            else { tbodyChoNhan.innerHTML = dsDangVanChuyen.map(khay => `<tr class="hover:bg-slate-50 transition-colors"><td class="p-2 text-center"><input type="checkbox" value="${khay.firestoreId}" class="w-3.5 h-3.5 text-sky-600 rounded border-slate-300 focus:ring-sky-500 cursor-pointer"></td><td class="p-2 font-mono font-bold text-slate-700">${khay.maMacDinh || 'N/A'}</td><td class="p-2 font-semibold text-slate-800">${khay.bo ? String(khay.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-2 text-slate-500 font-medium">${khay.nvXuatKho || '--'}</td><td class="p-2 text-center"><span class="bg-purple-50 text-purple-700 font-mono text-[10px] px-1.5 py-0.5 rounded font-bold">${khay.batchCode || 'N/A'}</span></td></tr>`).join(''); }

        }



        // 3. THEO DÕI YÊU CẦU ĐANG CHỜ CSSD CẤP PHÁT

        let dsChoCapPhat = tatCaDonCuaKhoa.filter(x => x.status === "CHO_CAP_PHAT");

        const tbodyChoCapPhat = document.getElementById("bangChoCapPhatTaiKhoa");

        if (tbodyChoCapPhat) {

            if (dsChoCapPhat.length === 0) {

                tbodyChoCapPhat.innerHTML = `<tr><td colspan="4" class="p-3 text-center text-slate-400 italic">Không có yêu cầu mượn khay mới nào đang chờ duyệt.</td></tr>`;

            } else {

                tbodyChoCapPhat.innerHTML = dsChoCapPhat.map(yc => `

                    <tr class="border-b text-xs hover:bg-amber-50/10 font-medium">

                        <td class="p-2.5 font-bold text-amber-800">${yc.bo}</td>

                        <td class="p-2.5 text-center"><span class="px-2 py-0.5 rounded-full text-[10px] font-bold bg-amber-100 text-amber-800 border border-amber-200 animate-pulse">Chờ CSSD cấp khay</span></td>

                        <td class="p-2.5 text-center font-mono text-slate-400">${yc.time}</td>

                        <td class="p-2.5 text-center text-slate-500">${yc.nvYeuCau || 'ĐD'}</td>

                    </tr>

                `).join('');

            }

        }

    }

    else if(activeTab === 'thugom') {

        let fK = document.getElementById("filterKhoaThuGom")?.value || ""; 

        let lsTG = listGiaoDich.filter(x => x.status === "CHO_THU"); 

        

        if (document.getElementById("filterKhoaThuGom")) {

            let danhSachKhoaCoDon = [...new Set(lsTG.map(x => x.khoa))].filter(Boolean); 

            let htmlOpts = '<option value="">-- Lọc theo Khoa --</option>';

            danhSachKhoaCoDon.forEach(k => { 

                htmlOpts += `<option value="${k}" ${(k === fK) ? "selected" : ""}>${k}</option>`; 

            });

            document.getElementById("filterKhoaThuGom").innerHTML = htmlOpts; 

            fK = document.getElementById("filterKhoaThuGom").value || "";

        }

        

        if (fK) lsTG = lsTG.filter(x => (x.khoa || "").toString().trim().toUpperCase() === fK.toString().trim().toUpperCase());

        if (document.getElementById("badgeSoCho")) document.getElementById("badgeSoCho").innerText = `${lsTG.length} Lệnh`;

        

        let bodyThuGom = document.getElementById("bangChoThuGom");

        if(bodyThuGom) {

            bodyThuGom.innerHTML = lsTG.map(i => {

                let tenBo = i.bo ? String(i.bo).split(" [ID:")[0].trim() : ""; 

                

                let itemsInBo = databaseExcel.filter(x => {

                    let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || '';

                    return String(boName).trim().toUpperCase() === String(tenBo).toUpperCase();

                });

                

                let checklistHtml = "";

                if (itemsInBo.length > 0) {

                    checklistHtml = `<div class="max-h-32 overflow-y-auto pr-1 border border-slate-200/60 rounded bg-slate-50/50 p-1.5 mt-1 flex flex-col gap-1">`;

                    itemsInBo.forEach(item => {

                        let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || "Dụng cụ chi tiết";

                        let sl = item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1;

                        

                        if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {

                            tenDc = "Dụng cụ chuẩn mâm";

                        }



                        checklistHtml += `

                            <div class="flex justify-between items-center border-b border-dashed border-slate-200 pb-0.5 last:border-none text-[11px]">

                                <span class="text-slate-700 font-semibold"><i class="fa-solid fa-circle text-[4px] text-sky-400 mr-1.5 align-middle"></i>${tenDc}</span>

                                <span class="font-bold text-sky-700 bg-sky-50 px-1.5 py-0.5 rounded border border-sky-100 text-[10px]">x${sl}</span>

                            </div>`;

                    });

                    checklistHtml += `</div>`;

                } else {

                    checklistHtml = `<div class="text-[10px] text-emerald-700 bg-emerald-50 border border-emerald-100 p-1.5 rounded font-medium mt-1"><i class="fa-solid fa-circle-check mr-1"></i>Dụng cụ lẻ (Cơ số: 1 chiếc)</div>`;

                }

                

                return `

                    <tr class="border-b border-slate-100 hover:bg-slate-50/40 transition-colors">

                        <td class="p-3 align-top">

                            <div class="font-black text-slate-700 text-[11px] uppercase tracking-wide">${i.khoa}</div>

                            ${i.ghiChu ? `<div class="text-[10px] text-rose-600 font-bold bg-rose-50 border border-rose-100 px-1.5 py-0.5 rounded mt-1.5 inline-block"><i class="fa-solid fa-triangle-exclamation mr-1"></i>${i.ghiChu}</div>` : ''}

                        </td>

                        <td class="p-3 align-top">

                            <div class="font-black text-sky-800 text-[13px] uppercase tracking-tight">${tenBo}</div>

                            <div class="text-[10px] font-mono text-slate-400 font-bold mt-0.5">MÃ SỐ ID: ${i.maMacDinh}</div>

                            ${checklistHtml}

                        </td>

                        <td class="p-3 text-center align-top font-mono font-bold text-slate-500 text-[11px]">${i.time}</td>

                        <td class="p-3 text-center align-top action-col">

                            <button onclick="moPopupKiemDem('${i.firestoreId}')" class="bg-sky-600 hover:bg-sky-700 text-white font-black px-3.5 py-2 rounded-lg shadow-sm text-[11px] uppercase tracking-wider transition-all"><i class="fa-solid fa-clipboard-check mr-1"></i>Kiểm Đếm</button>

                        </td>

                    </tr>`;

            }).join('');

        }

    }

    else if(activeTab === 'mayrua') {

        let lsCR = listGiaoDich.filter(x => x.status === "DANG_RUA");

        if(document.getElementById("badgeChoRua")) document.getElementById("badgeChoRua").innerText = `${lsCR.length} Mục`;

        

        let bodyChoRua = document.getElementById("bangChoRua");

        if(bodyChoRua) {

            bodyChoRua.innerHTML = lsCR.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="rua-checkbox"></td><td class="p-3 font-bold">${i.bo}</td><td class="p-3 text-right font-mono">${i.maMacDinh}</td></tr>`).join('');

        }

        

        let lsTR = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA");

        let bodyChoNghiemThuRua = document.getElementById("bangChoNiemThuRua");

        if(bodyChoNghiemThuRua) {

            bodyChoNghiemThuRua.innerHTML = lsTR.map(i => `<tr class="border-b"><td class="p-2.5 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="nghiemthurua-checkbox"></td><td class="p-2 text-xs font-bold">${i.bo} <span class="text-slate-400 font-mono">(${i.rua_batchCode || 'Mẻ Rửa'})</span></td></tr>`).join('');

        }

        

        let bodyLichSuRua = document.getElementById("bangLichSuRua");

        if(bodyLichSuRua) {

            const homNayMoiStr = getTodayDateStr();

            let tatCaMucCoLoRua = listGiaoDich.filter(x => x.rua_batchCode && (x.rua_ngayRua === homNayMoiStr || x.ngayTao === homNayMoiStr));

            let cacMeRuaGop = {};

            tatCaMucCoLoRua.forEach(x => {

                if(!cacMeRuaGop[x.rua_batchCode]) {

                    cacMeRuaGop[x.rua_batchCode] = {

                        batchCode: x.rua_batchCode,

                        mayRua: x.rua_mayRua || "Belimed WD250",

                        chuKy: x.rua_chuKy || "Chu trình tiêu chuẩn",

                        hoaChat: x.rua_hoaChat || "Smeg",

                        timeBatDau: x.rua_timeBatDau || "--:--",

                        ngay: x.rua_ngayRua || "",

                        soLuong: 0,

                        trangThaiMe: x.status === "TRONG_BUONG_RUA" ? "Đang rửa" : "Hoàn tất sạch"

                    };

                }

                cacMeRuaGop[x.rua_batchCode].soLuong += 1;

            });

            let danhSachMeRuaSapXep = Object.values(cacMeRuaGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));

            if(danhSachMeRuaSapXep.length === 0) {

                bodyLichSuRua.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa chạy mẻ rửa nào</td></tr>`;

            } else {

                bodyLichSuRua.innerHTML = danhSachMeRuaSapXep.map(me => `

                    <tr class="hover:bg-slate-50 transition-colors">

                        <td class="p-3">

                            <div class="flex items-center gap-2">

                                <span class="font-mono font-black text-sky-700 text-sm tracking-wider">${me.batchCode}</span>

                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "Đang rửa" ? "text-amber-600 bg-amber-50 border-amber-200" : "text-emerald-600 bg-emerald-50 border-emerald-200"}">${me.trangThaiMe}</span>

                            </div>

                            <div class="text-[11px] text-slate-500 font-medium mt-1">${me.mayRua} | ${me.chuKy}</div>

                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Hoá chất: ${me.hoaChat} | Bắt đầu: ${me.timeBatDau} (${me.ngay})</div>

                        </td>

                        <td class="p-3 text-right pr-4">

                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuong} khay</span>

                        </td>

                    </tr>`).join('');

            }

        }

    }

    else if(activeTab === 'donggoi') {

        let lsDG = listGiaoDich.filter(x => x.status === "CHO_DONG_GOI"); 

        if(document.getElementById("badgeDongGoi")) document.getElementById("badgeDongGoi").innerText = lsDG.length;

        let gDongGoi = document.getElementById("gridDongGoi");

        if(gDongGoi) {

            gDongGoi.innerHTML = lsDG.map(i => `<div class="bg-white p-3 rounded border border-slate-200 mb-2 flex justify-between items-center"><div class="flex-1"><div class="font-bold text-sky-700 text-[13px]">${i.bo}</div><div class="text-[10px] text-slate-500">Từ khoa: ${i.khoa}</div></div><button onclick="moPopupDongGoi('${i.firestoreId}')" class="bg-sky-50 text-sky-700 border border-sky-300 px-3 py-1.5 rounded text-[11px] font-black">ĐÓNG GÓI</button></div>`).join('');

        }

    }

    else if(activeTab === 'mayhap') {

        // CỘT 1: HÀNG ĐỢI CHỜ HẤP

        let lsCH = listGiaoDich.filter(x => x.status === "CHO_HAP"); 

        if(document.getElementById("badgeChoHap")) document.getElementById("badgeChoHap").innerText = `${lsCH.length} Mục`;

        let bodyChoHap = document.getElementById("bangChoHap");

        if(bodyChoHap) { 

            bodyChoHap.innerHTML = lsCH.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="hap-checkbox"></td><td class="p-3 font-bold text-xs">${i.bo}</td><td class="p-3 text-right font-mono text-xs font-bold text-sky-700">${i.maMacDinh}</td></tr>`).join(''); 

        }



        // CỘT 2: PHÊ DUYỆT - CHẤP NHẬN VÔ KHO

        let lsNT = listGiaoDich.filter(x => x.status === "DANG_HAP"); 

        let bodyChoNghiemThu = document.getElementById("bangChoNghiệmThu") || document.getElementById("bangChoNghiemThu");

        

        if(bodyChoNghiemThu) { 

            if (lsNT.length === 0) {

                bodyChoNghiemThu.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Không có mâm/mẻ nào đang trong lò chờ duyệt.</td></tr>`;

            } else {

                bodyChoNghiemThu.innerHTML = lsNT.map(i => {

                    let tenBoClean = i.bo ? String(i.bo).split(" [ID:")[0] : "Chưa rõ mâm";

                    return `

                        <tr class="border-b hover:bg-amber-50/40 transition-colors">

                            <td class="p-2.5 text-center action-col">

                                <input type="checkbox" value="${i.firestoreId}" class="nghiemthu-checkbox w-4 h-4 text-amber-600 rounded cursor-pointer" data-hasbi="${i.hasBI ? 'true' : 'false'}" onchange="kiemTraQuyenDuyetMeHap()">

                            </td>

                            <td class="p-2 text-xs">

                                <div class="font-black text-slate-800">${tenBoClean} <span class="text-[10px] font-mono text-sky-700">(${i.maMacDinh || 'N/A'})</span></div>

                                <div class="flex items-center gap-2 mt-0.5">

                                    <span class="text-[10px] font-mono font-bold text-rose-700 bg-rose-50 px-1 rounded border border-rose-100">Lô: ${i.batchCode || 'N/A'}</span>

                                    ${i.hasBI ? '<span class="px-1.5 py-0.5 bg-amber-100 text-amber-800 rounded text-[9px] font-black border border-amber-300 flex items-center gap-1"><i class="fa-solid fa-flask"></i> CHỜ BI</span>' : ''}

                                </div>

                            </td>

                        </tr>`;

                }).join(''); 

            }

        }

        kiemTraQuyenDuyetMeHap();



        // CỘT 3: NHẬT KÝ VẬN HÀNH LÒ

        if(document.getElementById("bangLichSuHap")) {

            const homNayMoiStr = getTodayDateStr(); 

            let tatCaMucCoLo = listGiaoDich.filter(x => x.batchCode && (x.ngayHapRealtime === homNayMoiStr || x.ngayTao === homNayMoiStr)); 

            let cacMeHapGop = {};

            tatCaMucCoLo.forEach(x => {

                if(!cacMeHapGop[x.batchCode]) { 

                    cacMeHapGop[x.batchCode] = { 

                        batchCode: x.batchCode, 

                        loaiHap: x.thongTinLoHap?.loaiHap || "Hấp hơi nước", 

                        chuKyNhiet: x.thongTinLoHap?.chuKyNhiet || "134°C - 4 phút", 

                        apSuat: x.thongTinLoHap?.apSuat || "N/A", 

                        thoiGian: x.thongTinLoHap?.thoiGianBatDau || x.time || "--:--", 

                        ngay: x.ngayHapRealtime || x.ngayTao || "", 

                        soLuongKhay: 0, 

                        trangThaiMe: x.status === "DANG_HAP" ? "ĐANG CHẠY LÒ" : "HOÀN THÀNH" 

                    }; 

                }

                cacMeHapGop[x.batchCode].soLuongKhay += 1;

            });

            let danhSachMeHapSapXep = Object.values(cacMeHapGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));

            if(danhSachMeHapSapXep.length === 0) { 

                document.getElementById("bangLichSuHap").innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa có mẻ hấp nào</td></tr>`; 

            } else { 

                document.getElementById("bangLichSuHap").innerHTML = danhSachMeHapSapXep.map(me => `

                    <tr class="hover:bg-slate-50 transition-colors border-b border-slate-100">

                        <td class="p-3">

                            <div class="flex items-center gap-2">

                                <span class="font-mono font-black text-rose-700 text-sm tracking-wider">${me.batchCode}</span>

                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "ĐANG CHẠY LÒ" ? "text-purple-700 bg-purple-100 border border-purple-200 animate-pulse" : "text-emerald-700 bg-emerald-100 border border-emerald-200"}">${me.trangThaiMe}</span>

                            </div>

                            <div class="text-[11px] text-slate-600 font-medium mt-1">${me.loaiHap} | ${me.chuKyNhiet} | Áp suất: ${me.apSuat} Bar</div>

                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Bắt đầu: ${me.thoiGian} (${me.ngay})</div>

                        </td>

                        <td class="p-3 text-right pr-4">

                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuongKhay} khay</span>

                        </td>

                    </tr>`).join(''); 

            }

        }

    }

    else if(activeTab === 'khovokhuan') {

        let uniqueKhoaSanCo = [...new Set(listGiaoDich.map(x => x.khoa))].filter(Boolean); const selKhoaXuat = document.getElementById("xuat_selKhoa");

        if(selKhoaXuat) {

            let currentSelected = selKhoaXuat.value; let htmlOpts = '<option value="">-- Chọn Khoa Muốn Trả Đồ --</option>';

            uniqueKhoaSanCo.forEach(k => { htmlOpts += `<option value="${k}" ${k === currentSelected ? 'selected' : ''}>${k}</option>`; });

            selKhoaXuat.innerHTML = htmlOpts;

        }



        let lsXK = listGiaoDich.filter(x => x.status === "CHO_XUAT").sort((a, b) => a.id - b.id);

        renderKhoVoKhuan(lsXK);



        let dsYeuCau = listGiaoDich.filter(x => x.status === "CHO_CAP_PHAT");

        let vungYeuCau = document.getElementById("vungYeuCauCapPhat");

        if (!vungYeuCau) {

            let parentContainer = document.getElementById("tab-khovokhuan");

            if (parentContainer) {

                let divYc = document.createElement("div");

                divYc.id = "vungYeuCauCapPhat";

                divYc.className = "mb-6 bg-amber-50/70 border border-amber-200 rounded-xl p-4 shadow-sm";

                parentContainer.insertBefore(divYc, parentContainer.firstChild);

                vungYeuCau = divYc;

            }

        }

        

        if (vungYeuCau) {

            if (dsYeuCau.length === 0) {

                vungYeuCau.innerHTML = `<h3 class="font-black text-amber-950 text-xs flex items-center gap-1.5"><i class="fa-solid fa-bell-slash"></i> HIỆN KHÔNG CÓ YÊU CẦU CẤP PHÁT ĐANG CHỜ</h3>`;

            } else {

                vungYeuCau.innerHTML = `

                    <h3 class="font-black text-amber-900 text-xs mb-2.5 flex items-center gap-1.5 animate-pulse">

                        <i class="fa-solid fa-bell text-rose-500 animate-bounce"></i> CÓ ${dsYeuCau.length} YÊU CẦU CẤP PHÁT TỪ CÁC KHOA LÂM SÀNG

                    </h3>

                    <div class="flex flex-col gap-2 max-h-48 overflow-y-auto">

                        ${dsYeuCau.map(yc => `

                            <div class="bg-white p-3 rounded-lg border border-amber-200 shadow-sm flex justify-between items-center text-xs">

                                <div>

                                    <span class="font-black text-amber-800 uppercase">[KHOA: ${yc.khoa}]</span> 

                                    Yêu cầu mâm: <span class="font-bold text-slate-800">${yc.bo}</span>

                                    <span class="text-[10px] text-slate-400 block mt-0.5">Thời gian: ${yc.time} - Người yêu cầu: ${yc.nvYeuCau || 'ĐD'}</span>

                                </div>

                                <div class="flex items-center gap-2">

                                    <button onclick="duyetCapPhatTậpTrung('${yc.firestoreId}', '${yc.bo}', '${yc.khoa}')" class="bg-amber-600 hover:bg-amber-700 text-white font-black px-3 py-2 rounded-lg text-[10px] uppercase shadow-sm flex items-center gap-1.5 transition-all">

                                        <i class="fa-solid fa-truck-ramp-box"></i>Cấp phát khay

                                    </button>

                                </div>

                            </div>

                        `).join('')}

                    </div>`;

            }

        }

        renderGioHangXuat();

    }

    else if(activeTab === 'quanlykho') {

        let fK = document.getElementById("inv_filterKhoa") ? document.getElementById("inv_filterKhoa").value : ""; 

        let uniqueKhoa = [...new Set(listGiaoDich.map(x=>x.khoa))].filter(Boolean);

        if(document.getElementById("inv_filterKhoa")) { document.getElementById("inv_filterKhoa").innerHTML = '<option value="">-- Tất cả --</option>' + uniqueKhoa.map(k=>`<option value="${k}" ${k===fK?'selected':''}>${k}</option>`).join(''); }

        let uniqueIDs = [...new Set(listGiaoDich.map(x=>x.maMacDinh))]; let arrHtml = []; const ngayHomNay = new Date(); ngayHomNay.setHours(0,0,0,0); 

        uniqueIDs.forEach(ma => {

            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a,b) => b.id - a.id)[0]; if(!currentTrans || !ma) return;

            let viTriCode = currentTrans.status; let khoaGiữ = currentTrans.khoa; if (fK && (khoaGiữ || "").toString().trim().toUpperCase() !== fK.toString().trim().toUpperCase()) return;

            let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";

            if(viTriCode === "HOAN_TAT") { viTriText = `Sẵn sàng tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 

            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã sử dụng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }

            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang vận chuyển`; viTriColor = "bg-purple-100 text-purple-800 animate-pulse"; }

            else if(viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }

            let hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-slate-100 text-slate-500">An toàn</span>`; let dongColorClass = ""; 

            if (currentTrans.hsd && (viTriCode === "CHO_XUAT" || viTriCode === "HOAN_TAT")) {

                const ngayHsd = new Date(currentTrans.hsd); ngayHsd.setHours(0,0,0,0); const soNgayConLai = Math.ceil((ngayHsd.getTime() - ngayHomNay.getTime()) / (1000 * 60 * 60 * 24));

                if (soNgayConLai < 0) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-rose-600 text-white">QUÁ HẠN</span>`; dongColorClass = "bg-rose-50/70 border-l-4 border-l-rose-500 font-bold"; } 

                else if (soNgayConLai <= 3) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-amber-500 text-white">HẠN < ${soNgayConLai} NGÀY</span>`; dongColorClass = "bg-amber-50/40 border-l-4 border-l-amber-500"; } 

                else { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-emerald-100 text-emerald-800">Hạn ${soNgayConLai} ngày</span>`; }

            }

            arrHtml.push(`<tr class="border-b border-slate-100 ${dongColorClass}"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-slate-500 text-[11px]">${khoaGiữ}</td><td class="p-3 text-center"><span class="px-2.5 py-1 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-mono font-bold text-slate-400">${currentTrans.batchCode || 'N/A'}</td><td class="p-3 text-center">${hsdBadget}</td></tr>`);

        });

        if(document.getElementById("bangTonKhoTe")) document.getElementById("bangTonKhoTe").innerHTML = arrHtml.join('');

    }

    else if(activeTab === 'danhmuc') {

        const tbody = document.getElementById("bangDanhMucTong"); if (!tbody) return;

        let uniqueIDs = [...new Set(listGiaoDich.map(x => x.maMacDinh))].filter(Boolean); let arrHtml = [];

        uniqueIDs.forEach(ma => {

            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a, b) => b.id - a.id)[0]; if (!currentTrans) return;

            let viTriCode = currentTrans.status; let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";

            if (viTriCode === "HOAN_TAT") { viTriText = `Tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 

            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã dùng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }

            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang đi đường`; viTriColor = "bg-purple-100 text-purple-800"; }

            else if (viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }

            let chuKyLo = listGiaoDich.filter(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG" || x.status === "ĐANG_VAN_CHUYEN")).length;

            arrHtml.push(`<tr class="border-b border-slate-100 font-medium"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-center"><span class="px-2.5 py-0.5 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-black text-amber-700 bg-amber-50/50">${chuKyLo} lần</td></tr>`);

        });

        tbody.innerHTML = arrHtml.join('');

    }

    else if(activeTab === 'lichsuluanchuyen') {

        const tbody = document.getElementById("bangLichSuHanhTrinhGoc");

        if(tbody) {

            tbody.innerHTML = listGiaoDich.map(x => {

                let statusBadge = `<span class="px-2 py-0.5 bg-slate-100 text-slate-700 font-bold rounded text-[10px]">${x.status}</span>`;

                if(x.status === "HOAN_TAT") statusBadge = `<span class="px-2 py-0.5 bg-emerald-100 text-emerald-800 font-black rounded text-[10px]">ĐÃ BÀN GIAO</span>`;

                else if(x.status === "CHO_XUAT") statusBadge = `<span class="px-2 py-0.5 bg-teal-100 text-teal-800 font-bold rounded text-[10px]">KHO VÔ KHUẨN</span>`;

                else if(x.status === "DANG_HAP") statusBadge = `<span class="px-2 py-0.5 bg-purple-100 text-purple-800 font-bold rounded text-[10px]">ĐANG HẤP LÒ</span>`;

                else if(x.status === "DA_SU_DUNG") statusBadge = `<span class="px-2 py-0.5 bg-rose-600 text-white font-black rounded text-[10px]">SỬ DỤNG BN</span>`;

                else if(x.status === "ĐANG_VAN_CHUYEN") statusBadge = `<span class="px-2 py-0.5 bg-purple-500 text-white font-bold rounded text-[10px] animate-pulse">ĐANG VẬN CHUYỂN</span>`;

                else if(x.status === "DANG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-blue-100 text-blue-700 font-bold rounded text-[10px]">HÀNG ĐỢI RỬA</span>`;

                else if(x.status === "TRONG_BUONG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-cyan-600 text-white font-bold rounded text-[10px]">ĐANG RỬA MÁY</span>`;

                else if(x.status === "CHO_CAP_PHAT") statusBadge = `<span class="px-2 py-0.5 bg-amber-100 text-amber-800 font-bold rounded text-[10px] border border-amber-200">CHỜ CẤP PHÁT</span>`;

                

                let fontColor = ""; let loGoc = listGiaoDich.find(m => m.batchCode === x.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 

                let anhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || x.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;

                if (anhBase64) fontColor = `<br><span onclick="handleViewAnhBiMoi('${x.firestoreId}')" class="text-[10px] text-emerald-600 bg-emerald-50 border border-emerald-300 rounded px-1 cursor-pointer font-bold mt-1 inline-block"><i class="fa-solid fa-image mr-1"></i>Xem ảnh BI</span>`;

                

                let kpiHtml = x.ketQuaGiamSatKpi ? `<br><span class="text-[9px] px-1 rounded font-black border mt-1 inline-block ${x.ketQuaGiamSatKpi === "ĐẠT" ? "text-emerald-700 bg-emerald-50 border-emerald-200" : "text-rose-700 bg-rose-50 border-rose-200"}">KPI: ${x.ketQuaGiamSatKpi} (${x.thoiGianTinhKpiPhut}m)</span>` : "";

                let thongTinKemTheo = x.nvXuatKho || '<span class="text-slate-300 font-normal">Chưa xuất</span>';

                if (x.nguoiKyNhanKhoa) thongTinKemTheo += ` ➔ <span class="text-emerald-700 font-semibold">[ĐD: ${x.nguoiKyNhanKhoa}]</span>`;

                if (x.status === "DA_SU_DUNG" && x.thongTinBenhNhan) thongTinKemTheo = `<span class="text-teal-700 font-black">BN: ${x.thongTinBenhNhan.thongTinTimKiemBN}</span>`;



                return `<tr class="border-b text-xs hover:bg-slate-50 transition-colors"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3 text-center">${statusBadge}${kpiHtml}</td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/30">${x.batchCode || 'N/A'}${fontColor}</td><td class="p-3 text-center font-bold text-sky-800 bg-sky-50/40">${thongTinKemTheo}</td><td class="p-3 text-center text-slate-400 font-mono text-[11px]">${x.ngayTao || ''} ${x.time || ''}</td></tr>`;

            }).join('');

        }

    }

    else if(activeTab === 'tracuu') {

        let safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");

        if (!safeTbody) {

            let parentContainer = document.getElementById("tab-tracuu") || document.querySelector('.bg-white.p-6.rounded-xl.shadow-md');

            if(parentContainer) {

                let existingTableZone = document.getElementById("vung-ket-qua-tu-dong");

                if(!existingTableZone) {

                    let tableWrapper = document.createElement("div"); tableWrapper.id = "vung-ket-qua-tu-dong"; tableWrapper.className = "mt-6 bg-white border border-slate-200 rounded-xl p-4 shadow-sm";

                    tableWrapper.innerHTML = `<h3 class="font-black text-slate-800 text-sm mb-3 text-sky-800"><i class="fa-solid fa-list-check mr-2"></i>DANH SÁCH MÂM DỤNG CỤ TRONG MẺ TRUY VẾT</h3><div class="overflow-x-auto"><table class="w-full text-left border-collapse"><thead><tr class="bg-slate-100 text-[11px] font-bold text-slate-600 uppercase border-b"><th class="p-3">Mã ID Khay</th><th class="p-3">Tên Bộ Dụng Cụ</th><th class="p-3">Khoa Sử Dụng</th><th class="p-3">Trạng thái</th><th class="p-3 text-center">Mã Lô Hấp</th><th class="p-3 text-center">Thời Gian Kích Hoạt</th></tr></thead><tbody id="bangLichSuTruyXuatAdmin"></tbody></table></div>`;

                    parentContainer.appendChild(tableWrapper); safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");

                }

            }

        }

        let searchInp = maLoTruyVetToanCuc || ""; if (!searchInp && document.getElementById("inp_searchBatch")) { searchInp = document.getElementById("inp_searchBatch").value.trim(); }

        if(safeTbody) {

            if (!searchInp) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng nhập mã mẻ hấp và nhấn "TRA CỨU KHẨN CẤP"...</td></tr>`; return; }

            let dataFiltered = listGiaoDich.filter(x => x.batchCode && String(x.batchCode).toUpperCase().includes(searchInp.toUpperCase()));

            if(dataFiltered.length === 0) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-rose-500 italic font-bold">Không tìm thấy dữ liệu: ${searchInp}</td></tr>`; } 

            else { safeTbody.innerHTML = dataFiltered.map(x => `<tr class="border-b text-xs hover:bg-slate-50 font-medium"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3"><span class="px-2 py-0.5 rounded text-[10px] ${x.status === "HOAN_TAT" ? "bg-emerald-100 text-emerald-800 font-bold" : "bg-teal-100 text-teal-800"}">${x.status}</span></td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/40">${x.batchCode || 'N/A'}</td><td class="p-3 text-center text-slate-400 font-mono">${x.ngayTao || ''} ${x.time || ''}</td></tr>`).join(''); }

        }

    }

    else if(activeTab === 'performance') { renderKpiPerformanceGoc(); }

    else if(activeTab === 'dashboard_tv') { renderDashboardTiviRealtime(); }

}



// Cập nhật hàm render bảng Kho Vô Khuẩn hỗ trợ cảm ứng Tablet

function renderKhoVoKhuan(listData) {

    const tbody = document.getElementById("bangKhoVoKhuan");

    if (!tbody) return;



    if (!listData || listData.length === 0) {

        tbody.innerHTML = `<tr><td colspan="6" class="p-6 text-center text-slate-400 italic">Kho vô khuẩn hiện tại trống.</td></tr>`;

        return;

    }



    tbody.innerHTML = listData.map(item => `

        <tr class="hover:bg-slate-50 transition-colors">

            <td class="p-3.5 font-bold text-slate-800 text-xs">${item.bo ? String(item.bo).split(" [ID:")[0] : 'N/A'}</td>

            <td class="p-3.5 font-mono font-black text-sky-700 text-xs">${item.maMacDinh || '--'}</td>

            <td class="p-3.5 text-center font-semibold text-slate-600 text-xs">${item.khoa || 'Tập trung'}</td>

            <td class="p-3.5 text-center"><span class="bg-emerald-100 text-emerald-800 font-bold px-2.5 py-1 rounded-full text-[11px]">${item.viTriKe || 'Kệ A1'}</span></td>

            <td class="p-3.5 text-center font-mono font-bold text-xs text-rose-600">${item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN') : 'An toàn'}</td>

            <td class="p-2 text-center">

                <button onclick="xuatKhoXoayVong('${item.maMacDinh}')" 

                        class="min-h-[44px] px-4 bg-emerald-600 active:bg-emerald-700 text-white font-bold rounded-lg text-xs shadow-sm inline-flex items-center gap-1.5 transition-transform active:scale-95">

                    <i class="fa-solid fa-cart-flatbed"></i>

                    <span>Xuất Khay</span>

                </button>

            </td>

        </tr>

    `).join('');

}



// =========================================================================

// VÁ LỖI CÁC HÀM TRUY VẾT & ĐỒNG BỘ HIỆU SUẤT TV MÀN HÌNH

// =========================================================================

function truyVetTheoMaBatch() {

    const inp = document.getElementById("inp_searchBatch");

    if (inp) {

        maLoTruyVetToanCuc = inp.value.trim();

        playSound('success');

        callRender();

    }

}



function clearTruyVetBatch() {

    const inp = document.getElementById("inp_searchBatch");

    if (inp) inp.value = "";

    maLoTruyVetToanCuc = "";

    callRender();

}



function resetDuLieuKet() {

    if (confirm("Anh Hùng có chắc chắn muốn giải phóng các mâm đang xử lý dở dang (chưa Hoàn tất / Chưa dùng) về trạng thái Chờ thu gom?")) {

        let p = [];

        listGiaoDich.forEach(x => {

            if (x.status !== "HOAN_TAT" && x.status !== "DA_SU_DUNG") {

                p.push(db.collection("phieuGiaoNhan").doc(x.firestoreId).update({ status: "CHO_THU" }));

            }

        });

        Promise.all(p).then(() => {

            playSound('success');

            showToast("Đã giải phóng dữ liệu mâm kẹt thành công!", "success");

            callRender();

        });

    }

}



function renderKpiPerformanceGoc() {

    const tbody = document.getElementById("bangHieuSuatKTV");

    if (!tbody) return;

    let kpiGop = {};

    listGiaoDich.forEach(x => {

        let ktv = x.ktvThuGom || x.nvXuatKho || "CSSD_CHUNG";

        if (!kpiGop[ktv]) kpiGop[ktv] = { code: ktv, ten: ktv, dat: 0, tong: 0 };

        if (x.ketQuaGiamSatKpi === "ĐẠT") kpiGop[ktv].dat += 1;

        if (x.ketQuaGiamSatKpi) kpiGop[ktv].tong += 1;

    });

    let sortedKpi = Object.values(kpiGop).sort((a,b) => b.dat - a.dat);

    if(sortedKpi.length === 0) {

        tbody.innerHTML = `<tr><td colspan="4" class="p-4 text-center italic text-slate-400">Hôm nay chưa có dữ liệu mẻ KPI nào.</td></tr>`;

    } else {

        tbody.innerHTML = sortedKpi.map((item, idx) => `

            <tr class="border-b text-xs hover:bg-slate-50 font-medium">

                <td class="p-3 text-center font-bold">${idx + 1}</td>

                <td class="p-3 font-mono font-bold text-sky-700">${item.code}</td>

                <td class="p-3 font-bold text-slate-800">${item.ten}</td>

                <td class="p-3 text-center"><span class="bg-emerald-100 text-emerald-800 px-2.5 py-1 rounded font-black">${item.dat}/${item.tong} Mẻ Đạt KPI</span></td>

            </tr>

        `).join('');

    }

}



function renderDashboardTiviRealtime() {

    let meRuaInDay = listGiaoDich.filter(x => x.rua_batchCode).length;

    let meHapInDay = listGiaoDich.filter(x => x.batchCode).length;

    let dangRua = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA").length;

    let dangHap = listGiaoDich.filter(x => x.status === "DANG_HAP").length;

    let sanSangXuat = listGiaoDich.filter(x => x.status === "CHO_XUAT").length;

    

    const today = new Date(); today.setHours(0,0,0,0);

    let quaHan = 0;

    listGiaoDich.forEach(x => {

        if (x.hsd && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT")) {

            if (new Date(x.hsd) < today) quaHan++;

        }

    });



    if (document.getElementById('tv_meRua')) document.getElementById('tv_meRua').innerText = meRuaInDay;

    if (document.getElementById('tv_meHap')) document.getElementById('tv_meHap').innerText = meHapInDay;

    if (document.getElementById('tv_meHap2')) document.getElementById('tv_meHap2').innerText = meHapInDay;

    if (document.getElementById('tv_dangRua')) document.getElementById('tv_dangRua').innerText = dangRua;

    if (document.getElementById('tv_dangHap')) document.getElementById('tv_dangHap').innerText = dangHap;

    if (document.getElementById('tv_khoVoKhuan')) document.getElementById('tv_khoVoKhuan').innerText = sanSangXuat;

    if (document.getElementById('tv_canhBaoHsd')) document.getElementById('tv_canhBaoHsd').innerText = quaHan;

}



function renderAdminInterface() {

    if (document.getElementById("cfg_pinAdmin")) document.getElementById("cfg_pinAdmin").value = thongTinMatKhauAdmin.adminPIN || "";

    if (document.getElementById("cfg_pinCSSD")) document.getElementById("cfg_pinCSSD").value = thongTinMatKhauAdmin.cssdPIN || "";

    if (document.getElementById("cfg_pinGuest")) document.getElementById("cfg_pinGuest").value = thongTinMatKhauAdmin.guestPIN || "";

    const MatrixRoles = ["CSSD", "KHOA", "GUEST"];

    const MatrixTabs = [

        { id: "khoaphong", name: "1. Cổng Báo Trả Đồ" }, { id: "thugom", name: "2. Xe Thu Gom" }, { id: "mayrua", name: "3. Quản Lý Mẻ Rửa" }, { id: "donggoi", name: "4. Làm Sạch & Gói" }, { id: "mayhap", name: "5. Quản Lý Mẻ Hấp" }, { id: "khovokhuan", name: "6. Kho Vô Khuẩn" }, { id: "quanlykho", name: "7. Tồn Kho Toàn Viện" }, { id: "danhmuc", name: "8. Giám Sát Tuổi Thọ" }, { id: "lichsuluanchuyen", name: "9. Nhật Ký Luân Chuyển" }, { id: "tracuu", name: "10. Cấu Hình Hệ Thống" }, { id: "performance", name: "11. Hiệu Suất KPI" }, { id: "dashboard_tv", name: "12. Màn Hình Tivi" }

    ];

    let htmlMatrix = "";

    MatrixTabs.forEach(tab => {

        htmlMatrix += `<tr class="border-b font-medium text-xs hover:bg-slate-50"><td class="p-3 font-bold text-slate-700">${tab.name}</td>`;

        MatrixRoles.forEach(role => {

            let isChecked = (cauHinhGiaoDien[role] && cauHinhGiaoDien[role].includes(tab.id)) ? "checked" : "";

            htmlMatrix += `<td class="p-3 text-center"><input type="checkbox" ${isChecked} onchange="updateGiaoDienMatrix('${role}', '${tab.id}', this)" class="w-4 h-4 rounded text-sky-600 focus:ring-sky-500"></td>`;

        });

        htmlMatrix += `</tr>`;

    });

    const tbodyMatrix = document.getElementById("bodyMaTranGiaoDien"); if (tbodyMatrix) tbodyMatrix.innerHTML = htmlMatrix;

    const trKhoa = document.getElementById("bangPhanQuyenKhoa");

    if(trKhoa) {

        if(danhSachKhoa.length === 0) { trKhoa.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-rose-500 font-bold italic">Danh sách Khoa trống.</td></tr>`; } 

        else { trKhoa.innerHTML = danhSachKhoa.map((k, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-slate-700 text-[11px]">${k.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-rose-600 text-sm bg-rose-50/50">${k.pin || '123'}</td><td class="p-3 text-center"><div class="flex items-center justify-center gap-2"><input type="text" id="pin-khoa-${index}" placeholder="PIN" class="w-24 p-1.5 text-center border border-slate-300 rounded text-xs font-bold"><button onclick="updatePINTrựcTiep(${index}, '${k.ten}')" class="bg-sky-600 text-white font-bold py-1.5 px-3 rounded shadow text-[10px]">ĐỔI PIN</button></div></td></tr>`).join(''); }

    }

    const tbKtv = document.getElementById("bangNhanVienCssd");

    if(tbKtv) {

        if(danhSachKtvCssd.length === 0) { tbKtv.innerHTML = `<tr><td colspan="4" class="p-4 text-center text-slate-400 italic">Chưa có Nhân viên.</td></tr>`; }

        else { tbKtv.innerHTML = danhSachKtvCssd.map((nv, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-sky-700 text-xs">${nv.code}</td><td class="p-3 font-bold text-slate-700 text-[11px]">${nv.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-sky-600 text-sm bg-sky-50/50">${nv.pin}</td><td class="p-3 text-center"><button onclick="xaoKtvCssd('${nv.code}')" class="text-rose-600 font-bold text-[10px]"><i class="fa-solid fa-trash-can mr-1"></i>XÓA</button></td></tr>`).join(''); }

    }

}



// =========================================================================

// 14. XỬ LÝ ĐỌC FILE EXCEL ĐA SHEET

// =========================================================================

document.addEventListener("DOMContentLoaded", () => {

    const fileInput = document.getElementById("excelFileInput");

    if (fileInput) {

        fileInput.addEventListener("change", (e) => {

            const file = e.target.files[0];

            if (!file) return;



            const reader = new FileReader();

            reader.onload = function(e) {

                try {

                    const data = new Uint8Array(e.target.result);

                    const workbook = XLSX.read(data, { type: 'array' });

                    

                    let updateData = {};

                    let logs = [];



                    workbook.SheetNames.forEach(sheetName => {

                        const worksheet = workbook.Sheets[sheetName];

                        const jsonData = XLSX.utils.sheet_to_json(worksheet);

                        if (jsonData.length === 0) return;



                        const firstRow = jsonData[0];

                        const keys = Object.keys(firstRow).map(k => k.trim().toUpperCase());



                        const coCotMaDC = keys.includes("MÃ DC");

                        if (coCotMaDC) {

                            let mapKhoa = {};

                            danhSachKhoa.forEach(k => {

                                mapKhoa[k.ten.trim().toUpperCase()] = {

                                    ten: k.ten.trim().toUpperCase(),

                                    pin: k.pin || "123",

                                    danhSachBo: []

                                };

                            });



                            jsonData.forEach(row => {

                                let rawKeyKhoa = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS (I)");

                                let rawKeyBo = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS CHUẨN");

                                let rawKeyMa = Object.keys(row).find(k => k.trim().toUpperCase() === "MÃ DC");



                                let tenKhoa = rawKeyKhoa ? row[rawKeyKhoa] : null;

                                let tenBo = rawKeyBo ? row[rawKeyBo] : null;

                                let maKhay = rawKeyMa ? row[rawKeyMa] : null;



                                if (tenKhoa && tenBo) {

                                    let keyKhoa = String(tenKhoa).trim().toUpperCase();

                                    let cleanBo = String(tenBo).trim().toUpperCase();

                                    let cleanMa = maKhay ? String(maKhay).trim().toUpperCase() : "CHUA_CO_ID";

                                    let chuoiDinhDangKhay = `${cleanBo} [ID:${cleanMa}]`;



                                    if (!mapKhoa[keyKhoa]) {

                                        mapKhoa[keyKhoa] = { ten: keyKhoa, pin: "123", danhSachBo: [] };

                                    }

                                    if (!mapKhoa[keyKhoa].danhSachBo.includes(chuoiDinhDangKhay)) {

                                        mapKhoa[keyKhoa].danhSachBo.push(chuoiDinhDangKhay);

                                    }

                                }

                            });



                            updateData.danhSachKhoa = Object.values(mapKhoa);

                            logs.push(`Cập nhật định biên cơ số cho ${updateData.danhSachKhoa.length} Khoa/Phòng`);

                        }



                        const coCotMaAesculap = keys.includes("AB 120/12");

                        if (coCotMaAesculap) {

                            updateData.databaseExcel = jsonData;

                            logs.push(`Nạp thành công ${jsonData.length} linh kiện chi tiết mâm Aesculap`);

                        }

                    });



                    if (Object.keys(updateData).length === 0) {

                        return showToast("Không tìm thấy cấu trúc dữ liệu phù hợp (Cột 'MÃ DC' hoặc 'AB 120/12') trong file Excel up lên!", "error");

                    }



                    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update(updateData)

                    .then(() => {

                        playSound('success');

                        showToast(`Đồng bộ thành công: ${logs.join(" | ")}!`, "success");

                        fileInput.value = ""; 

                        callRender();

                    }).catch(err => {

                        console.error("Lỗi cập nhật Firestore:", err);

                        showToast("Thất bại khi đẩy dữ liệu lên Cloud Firestore!", "error");

                    });



                } catch (err) {

                    console.error("Lỗi FileReader:", err);

                    showToast("Định dạng file không được hỗ trợ hoặc bị lỗi!", "error");

                }

            };

            reader.readAsArrayBuffer(file);

        });

    }

});

Tôi gửi bạn code app.js nha

Dưới đây là file app.js hoàn chỉnh đã được tích hợp đầy đủ tính năng:

Ký chữ ký điện tử trực tiếp (Canvas Draw): Mở popup ký tên bằng ngón tay hoặc chuột, lưu hình ảnh dạng Base64 và cập nhật trạng thái HOAN_TAT lên Firestore.

Khôi phục in Biên bản Giao nhận A4: Tách biệt hoàn toàn cơ chế in khổ A4 (cho máy in văn phòng tiêu chuẩn HP LaserJet) và in Tem Bixolon (80mm x 50mm) bằng class CSS print-mode-doc.

Anh chỉ cần sao chép toàn bộ mã nguồn bên dưới và đè (replace) vào file app.js:

JavaScript
// =========================================================================
// 1. KHỞI TẠO HỆ THỐNG & CẤU HÌNH BIẾN TOÀN CỤC (PN HOSPITAL)
// =========================================================================
const firebaseConfig = { 
    apiKey: "AIzaSyCxjdCTKHQlpm7SYbWCEws1HhcOaFp0LBA", 
    authDomain: "cssd-system-2878c.firebaseapp.com", 
    projectId: "cssd-system-2878c", 
    storageBucket: "cssd-system-2878c.firebasestorage.app", 
    messagingSenderId: "662377321937", 
    appId: "1:662377321937:web:001c092e10319547623cf0" 
};
firebase.initializeApp(firebaseConfig); 
const db = firebase.firestore();

let thongTinMatKhauAdmin = { adminPIN: "admin2026", cssdPIN: "cssd2026", guestPIN: "guest2026" };
let cauHinhGiaoDien = {
    "ADMIN": ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'],
    "CSSD": ['thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','dashboard_tv'], 
    "KHOA": ['khoaphong','quanlykho','lichsuluanchuyen'], 
    "GUEST": ['quanlykho','danhmuc','lichsuluanchuyen','performance','dashboard_tv']
};

let currentRole = "", loginUserCode = "";
let danhSachKhoa = [], listGiaoDich = [], gioHangTam = [], danhSachKtvCssd = [], databaseExcel = [];
let html5QrCode = null; let targetInputIdForScan = ""; let idDangDongGoi = null;
let activeTab = 'thugom'; let renderTimeout = null;
let duLieuAnhBiTamBase64 = ""; let maLoTruyVetToanCuc = ""; 
let gioHangXuatKho = []; let gioKhaySuDungTam = []; 
let currentKiemDemData = null;

// Biến hỗ trợ vẽ chữ ký điện tử
let canvasSignature = null;
let ctxSignature = null;
let isDrawingSignature = false;
let dsIdKhayChoKyNhan = [];

// Cấu hình máy hấp & máy rửa (Belimed WD250)
const cauHinhMayHap = { "Hấp hơi nước": ["A1", "A2", "A3", "A4"], "Hấp H2O2 (Plasma)": ["P1", "P2"], "Khử khuẩn EO": ["EO1", "EO2"] };
const cauHinhMayRua = {
    "Máy rửa khử khuẩn tự động": ["Belimed WD250"],
    "Máy rửa sóng siêu âm": ["US1"],
    "Bồn rửa thủ công": ["MAN1"]
};
const danhMucHoaChatRua = [
    { ten: "Cidezyme (Enzym phân hủy protein)", lieuLuong: "8 ml/L" },
    { ten: "Sekusept Active (Khử khuẩn mức độ cao)", lieuLuong: "10 ml/L" },
    { ten: "Smeg (Nước rửa trung tính chuyên dụng)", lieuLuong: "5 ml/L" },
    { ten: "Aniosyme DD1 (Làm sạch & khử trùng sơ bộ)", lieuLuong: "5 ml/L" }
];

function getTodayDateStr() { 
    const d = new Date(); 
    return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`; 
}

function playSound(type) {
    try {
        const AudioContext = window.AudioContext || window.webkitAudioContext;
        if (!AudioContext) return;
        const ctx = new AudioContext();
        const osc = ctx.createOscillator();
        const gain = ctx.createGain();
        osc.connect(gain); gain.connect(ctx.destination);
        if (type === 'success') {
            osc.type = 'sine'; osc.frequency.setValueAtTime(800, ctx.currentTime);
            gain.gain.setValueAtTime(0.3, ctx.currentTime);
            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.15);
            osc.start(); osc.stop(ctx.currentTime + 0.15);
        } else {
            osc.type = 'sawtooth'; osc.frequency.setValueAtTime(250, ctx.currentTime);
            gain.gain.setValueAtTime(0.3, ctx.currentTime);
            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.4);
            osc.start(); osc.stop(ctx.currentTime + 0.4);
        }
    } catch(e) {}
}

// =========================================================================
// 2. REALTIME LISTENER (FIRESTORE SYNC)
// =========================================================================
db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").onSnapshot(doc => { 
    try {
        if (doc.exists) { 
            let res = doc.data(); 
            danhSachKhoa = res.danhSachKhoa || []; 
            danhSachKtvCssd = res.danhSachKtvCssd || []; 
            databaseExcel = res.databaseExcel || []; 
            if(res.thongTinMatKhauAdmin) thongTinMatKhauAdmin = Object.assign(thongTinMatKhauAdmin, res.thongTinMatKhauAdmin); 
            if(res.cauHinhGiaoDien) cauHinhGiaoDien = Object.assign(cauHinhGiaoDien, res.cauHinhGiaoDien);
        } 
        initSelects();
        if(activeTab === 'khoaphong') loadBoDungCuTheoKhoa(); 
        renderAdminInterface(); 
        if(currentRole) apDungPhanQuyenGiaoDien(currentRole);
        taiDanhMucLinhKienChuan();
        callRender();
    } catch(err) { console.error("Lỗi đồng bộ danh mục:", err); }
});

db.collection("phieuGiaoNhan").orderBy("id", "desc").limit(1000).onSnapshot(snap => { 
    listGiaoDich = []; 
    snap.forEach(doc => { let d = doc.data(); d.firestoreId = doc.id; listGiaoDich.push(d); }); 
    callRender();
});

function callRender() { clearTimeout(renderTimeout); renderTimeout = setTimeout(() => { renderTheoTabHienTai(); }, 100); }

// =========================================================================
// 3. XỬ LÝ ĐĂNG NHẬP & PHÂN QUYỀN HỆ THỐNG
// =========================================================================
function toggleLoginFields() {
    const r = document.getElementById("login_role").value;
    document.getElementById("field_khoa").classList.toggle("hidden", r !== "KHOA");
    document.getElementById("field_nhanvien_cssd").classList.toggle("hidden", r !== "CSSD");
}

function checkLogin() {
    const role = document.getElementById("login_role").value; 
    const pass = document.getElementById("login_pass").value; 
    let pA = thongTinMatKhauAdmin.adminPIN || "admin2026"; 
    let pC = thongTinMatKhauAdmin.cssdPIN || "cssd2026"; 
    let pG = thongTinMatKhauAdmin.guestPIN || "guest2026"; 
     
    if (role === "ADMIN" && pass === pA) { 
        currentRole = "ADMIN"; loginUserCode = "ADMIN"; document.getElementById("nav_user_info").innerText = "ADMINISTRATOR";
        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');
        apDungPhanQuyenGiaoDien("ADMIN");
        switchTab('lichsuluanchuyen');
    } else if (role === "KHOA") { 
        const khoaSelect = document.getElementById("login_khoa").value; 
        let found = danhSachKhoa.find(x => x.ten === khoaSelect); 
        if (pass === (found ? found.pin : "123")) { 
            currentRole = "KHOA"; loginUserCode = khoaSelect; document.getElementById("nav_user_info").innerText = khoaSelect; 
            if (document.getElementById("khoa_selKhoa")) {
                document.getElementById("khoa_selKhoa").value = khoaSelect;
                document.getElementById("khoa_selKhoa").disabled = true;
            }
            document.body.classList.remove('guest-mode');
            apDungPhanQuyenGiaoDien("KHOA");
            switchTab('khoaphong');
        } else { return showToast("Sai mã PIN Khoa!", "error"); } 
    } else if (role === "CSSD") {
        const nvCode = document.getElementById("login_nv_cssd").value;
        if(!nvCode && pass === pC) { currentRole = "CSSD"; loginUserCode = "CSSD_CHUNG"; document.getElementById("nav_user_info").innerText = "CSSD GENERAL"; } 
        else if (nvCode) {
            let nv = danhSachKtvCssd.find(x => x.code === nvCode);
            if (nv && pass === nv.pin) { currentRole = "CSSD"; loginUserCode = nv.ten; document.getElementById("nav_user_info").innerText = `${nvCode} - ${nv.ten}`; }
            else { return showToast("Sai mã PIN của nhân viên này!", "error"); }
        } else { return showToast("Vui lòng chọn Nhân viên hoặc nhập PIN Backup!", "error"); }
        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');
        apDungPhanQuyenGiaoDien("CSSD");
        let tabsDuocPhep = cauHinhGiaoDien["CSSD"] || [];
        if(tabsDuocPhep.includes('thugom')) switchTab('thugom');
        else if (tabsDuocPhep.length > 0) switchTab(tabsDuocPhep[0]);
    } else if (role === "GUEST") {
        if(pass === pG) {
            currentRole = "GUEST"; loginUserCode = "GUEST"; document.getElementById("nav_user_info").innerText = "KHÁCH THAM QUAN";
            document.body.classList.add('guest-mode'); 
            apDungPhanQuyenGiaoDien("GUEST");
            switchTab('quanlykho');
        } else { return showToast("Sai mã PIN Khách tham quan!", "error"); }
    } else { return showToast("Sai thông tin đăng nhập!", "error"); }
    document.getElementById("login-screen").classList.add("hidden"); document.getElementById("main-app").classList.remove("hidden");
}

function anTatCaHeadersVaMenus() {
    document.getElementById('header-lamsang')?.classList.add('hidden');
    document.getElementById('header-vanhanh')?.classList.add('hidden');
    document.getElementById('header-dulieu')?.classList.add('hidden');
    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => {
        document.getElementById('menu-' + x)?.classList.add('hidden');
    });
}

function apDungPhanQuyenGiaoDien(role) {
    anTatCaHeadersVaMenus();
    if (role === "ADMIN") {
        ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(tab => {
            document.getElementById('menu-' + tab)?.classList.remove('hidden');
        });
        document.getElementById('header-lamsang')?.classList.remove('hidden');
        document.getElementById('header-vanhanh')?.classList.remove('hidden');
        document.getElementById('header-dulieu')?.classList.remove('hidden');
        return;
    }
    let tabsDuocPhep = cauHinhGiaoDien[role] || [];
    tabsDuocPhep.forEach(tab => { document.getElementById('menu-' + tab)?.classList.remove('hidden'); });
    if (tabsDuocPhep.includes('khoaphong')) document.getElementById('header-lamsang')?.classList.remove('hidden');
    if (['thugom','mayrua','donggoi','mayhap','khovokhuan'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-vanhanh')?.classList.remove('hidden');
    if (['quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-dulieu')?.classList.remove('hidden');
}

function switchTab(t) { 
    if (currentRole !== "ADMIN") {
        let tabsDuocPhep = cauHinhGiaoDien[currentRole] || [];
        if(!tabsDuocPhep.includes(t)) { return showToast("Tài khoản không có quyền truy cập!", "error"); }
    }
    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => { 
        document.getElementById('tab-'+x)?.classList.add('hidden'); 
        document.getElementById('menu-'+x)?.classList.remove('sidebar-item-active'); 
    }); 
    document.getElementById('tab-'+t)?.classList.remove('hidden'); 
    document.getElementById('menu-'+t)?.classList.add('sidebar-item-active'); 
    activeTab = t; 
    if (t === 'mayrua') { setTimeout(() => { capNhatDanhSachMaMayRua(); }, 50); }
    if (t === 'mayhap') { setTimeout(() => { tuDongTaoMaLoMeHap(); }, 50); }
    callRender(); 
}

// =========================================================================
// 4. TRẠM 1: CỔNG LÂM SÀNG & HÀNH ĐỘNG ĐỘC LẬP (TRẢ ĐỒ BẨN / YÊU CẦU ĐỒ SẠCH)
// =========================================================================
function loadBoDungCuTheoKhoa() { 
    let k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 
    let list = document.getElementById("listBoDungCu"); 
    if(!list) return; list.innerHTML = ""; 
    let f = danhSachKhoa.find(x => (x.ten || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase()); 
    if (f && f.danhSachBo) {
        let htmlOptions = "";
        f.danhSachBo.forEach(x => {
            if (x) {
                let chuoiX = String(x);
                let tenBoThuanTuy = chuoiX.includes(" [ID:") ? chuoiX.split(" [ID:")[0] : chuoiX;
                htmlOptions += `<option value="${chuoiX}"><option value="${tenBoThuanTuy}">`;
            }
        });
        list.innerHTML = htmlOptions; 
    }
}

function themVaoGio() { 
    let val = document.getElementById("khoa_inpMaBo").value.trim().toUpperCase(); if(!val) return; 
    if (gioHangTam.some(x => x.maMacDinh === val)) { document.getElementById("khoa_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách chờ!", "error"); } 
    let tenGoc = val.includes("[ID:") ? val.split(" [ID:")[0] : val; 
    gioHangTam.push({bo: tenGoc, maMacDinh: val, slYeuCau: 1}); 
    document.getElementById("khoa_inpMaBo").value = ""; 
    renderGioHang(); 
}

function renderGioHang() { 
    let khuVuc = document.getElementById("khuVucGioHang"); if(khuVuc) khuVuc.classList.toggle("hidden", gioHangTam.length===0); 
    let bHang = document.getElementById("bangGioHang");
    if(bHang) {
        bHang.innerHTML = gioHangTam.map(i => `<tr><td class="p-2.5 font-bold text-sky-700 text-[11px]">${i.bo}</td></tr>`).join(''); 
    }
    let badge = document.getElementById("badgeGioHang");
    if(badge) badge.innerText = `${gioHangTam.length} món`;
}

function khoaGuiPhieuTraBatches() { 
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 
    if(!k) return showToast("Vui lòng chọn Khoa trước!"); 
    if(gioHangTam.length === 0) return showToast("Vui lòng quét hoặc chọn mâm bẩn muốn trả trước!", "error"); 
     
    let p=[]; 
    gioHangTam.forEach((i, idx) => {
        p.push(
            db.collection("phieuGiaoNhan").add({ 
                id: Date.now() + idx, 
                ngayTao: getTodayDateStr(), 
                time: new Date().toLocaleTimeString('vi-VN'), 
                khoa: k, 
                bo: i.bo, 
                maMacDinh: i.maMacDinh, 
                slYeuCau: 1, 
                slThucTe: 1, 
                status: "CHO_THU" 
            })
        );
    }); 
     
    Promise.all(p).then(() => { 
        clearGioHang(); 
        playSound('success');
        showToast("Đã gửi lệnh báo trả đồ bẩn về CSSD!", "success"); 
        callRender(); 
    }); 
}

function khoaGuiYeuCauCapPhat() {
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;
    if(!k) return showToast("Vui lòng chọn Khoa/Phòng gửi yêu cầu!", "error");
    
    const tenBoChon = document.getElementById("khoa_inpMaBo")?.value.trim().toUpperCase();
    if(!tenBoChon) return showToast("Vui lòng chọn hoặc nhập tên mâm cần cấp mới!", "error");
    
    let tenMâmSạch = tenBoChon.split(" [ID:")[0];
    
    db.collection("phieuGiaoNhan").add({
        id: Date.now(),
        ngayTao: getTodayDateStr(),
        time: new Date().toLocaleTimeString('vi-VN'),
        khoa: k,
        bo: tenMâmSạch,
        maMacDinh: "CHO_CAP_PHAT", 
        slYeuCau: 1,
        status: "CHO_CAP_PHAT", 
        nvYeuCau: loginUserCode || "Điều dưỡng khoa"
    }).then(() => {
        playSound('success');
        showToast(`Đã gửi yêu cầu cấp phát mâm ${tenMâmSạch} thành công!`, "success");
        if(document.getElementById("khoa_inpMaBo")) document.getElementById("khoa_inpMaBo").value = "";
        callRender();
    });
}

// =========================================================================
// 5. TRẠM 2: XE THU GOM & KIỂM ĐẾM CHI TIẾT LINH KIỆN
// =========================================================================
function moPopupKiemDem(id) { 
    let item = listGiaoDich.find(x => x.firestoreId === id); 
    if(!item) return showToast("Không tìm thấy thông tin dòng chỉ định!", "error");

    currentKiemDemData = {
        id: id,
        tenBoDungCu: item.bo ? String(item.bo).split(" [ID:")[0] : "Chưa rõ mâm",
        khoaYeuCau: item.khoa || "Chưa rõ khoa",
        ghiChuLamSang: item.ghiChu || ""
    }; 

    document.getElementById('popBo').innerText = currentKiemDemData.tenBoDungCu;
    document.getElementById('popKhoa').innerText = currentKiemDemData.khoaYeuCau;
    document.getElementById('popGhiChu').value = currentKiemDemData.ghiChuLamSang;
    document.getElementById('popGhiChu').classList.remove('border-rose-500', 'ring-2', 'ring-rose-200');

    let itemsInBo = databaseExcel.filter(x => { 
        let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; 
        return String(boName).trim().toUpperCase() === String(currentKiemDemData.tenBoDungCu).trim().toUpperCase(); 
    });

    let checklistSoBo = [];
    if(itemsInBo.length > 0) {
        checklistSoBo = itemsInBo.map(ct => {
            let tenDc = ct['Tên TS chuẩn'] || ct['Tên Dụng Cụ Chi Tiết'] || ct['Tên dụng cụ chi tiết'] || ct['Tên dụng cụ'] || ct['Chi tiết'] || ct['Dụng cụ'] || ct['Tên Chi Tiết'] || ct['NAME'] || "Dụng cụ";
             
            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {
                tenDc = "Dụng cụ chuẩn mâm";
            }
             
            let sl = parseInt(ct['Số lượng'] || ct['SL'] || ct['Số Lượng'] || 1) || 0;
            return { ten: tenDc, slChuan: sl, slThuc: sl, tinhTrang: "ĐỦ" };
        }).filter(line => line.slChuan > 0); 
    } else {
        checklistSoBo = [{ ten: "Dụng cụ nguyên bộ (Chưa phân rã cấu hình)", slChuan: 1, slThuc: 1, tinhTrang: "ĐỦ" }];
    }

    currentKiemDemData.linhKienKiemDem = checklistSoBo;
    renderChecklistLinhKien();
    document.getElementById('popupKiemDem').classList.remove('hidden'); 
}

function renderChecklistLinhKien() {
    const container = document.getElementById('popKiemDemChecklist');
    if(!container) return; container.innerHTML = ""; 

    currentKiemDemData.linhKienKiemDem.forEach((item, index) => {
        let bgBadge = "bg-emerald-50 text-emerald-700 border-emerald-200";
        if (item.tinhTrang === "THIẾU") bgBadge = "bg-rose-50 text-rose-700 border-rose-200 animate-pulse";
        if (item.tinhTrang === "HỎNG") bgBadge = "bg-amber-50 text-amber-700 border-amber-200";

        const row = document.createElement('div');
        row.className = "flex items-center justify-between py-3 px-2 transition-all hover:bg-slate-50";
        row.innerHTML = `
            <div class="flex-1 pr-2">
                <p class="text-xs font-bold text-slate-700">${item.ten}</p>
                <p class="text-[10px] text-slate-400 font-medium mt-0.5">Cơ số chuẩn: <span class="font-bold text-slate-600">${item.slChuan}</span></p>
            </div>
            <div class="flex items-center gap-3 mr-4">
                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, -1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">-</button>
                <span class="w-6 text-center text-xs font-black text-slate-800">${item.slThuc}</span>
                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, 1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">+</button>
            </div>
            <div>
                <select onchange="thayDoiTinhTrangLinhKien(${index}, this.value)" class="text-[11px] font-black border rounded-lg px-2 py-1.5 shadow-sm transition-all ${bgBadge}">
                    <option value="ĐỦ" ${item.tinhTrang === 'ĐỦ' ? 'selected' : ''}>🟢 Đủ đồ</option>
                    <option value="THIẾU" ${item.tinhTrang === 'THIẾU' ? 'selected' : ''}>🔴 Thiếu đồ</option>
                    <option value="HỎNG" ${item.tinhTrang === 'HỎNG' ? 'selected' : ''}>🟡 Hỏng đồ</option>
                </select>
            </div>
        `;
        container.appendChild(row);
    });
}

function thayDoiSoLuongLinhKien(index, thayDoi) {
    const item = currentKiemDemData.linhKienKiemDem[index];
    const targetSl = item.slThuc + thayDoi;
    if (targetSl >= 0 && targetSl <= item.slChuan * 2) {
        item.slThuc = targetSl;
        if (item.slThuc < item.slChuan) { item.tinhTrang = "THIẾU"; } 
        else if (item.slThuc === item.slChuan) { item.tinhTrang = "ĐỦ"; }
        renderChecklistLinhKien();
    }
}

function thayDoiTinhTrangLinhKien(index, value) {
    currentKiemDemData.linhKienKiemDem[index].tinhTrang = value;
    if (value === "THIẾU" && currentKiemDemData.linhKienKiemDem[index].slThuc === currentKiemDemData.linhKienKiemDem[index].slChuan) {
        currentKiemDemData.linhKienKiemDem[index].slThuc = Math.max(0, currentKiemDemData.linhKienKiemDem[index].slChuan - 1);
    } else if (value === "ĐỦ") {
        currentKiemDemData.linhKienKiemDem[index].slThuc = currentKiemDemData.linhKienKiemDem[index].slChuan;
    }
    renderChecklistLinhKien();
}

function closePopupKiemDem() { document.getElementById("popupKiemDem").classList.add("hidden"); currentKiemDemData = null; }

async function saveKiemDem() { 
    if(!currentKiemDemData) return; 
    const ghiChuValue = document.getElementById("popGhiChu").value.trim();
    let coSuCoChenhLech = false;
    currentKiemDemData.linhKienKiemDem.forEach(item => {
        let thuc = Number(item.slThuc) || 0;
        let chuan = Number(item.slChuan) || 0;
        if (item.tinhTrang === "THIẾU" || item.tinhTrang === "HỎNG" || thuc < chuan) { coSuCoChenhLech = true; }
    });

    if (coSuCoChenhLech && ghiChuValue === "") {
        playSound('error');
        alert("⚠️ PHÁT HIỆN SỰ CỐ CHÊNH LỆCH LINH KIỆN!\nAnh/Chị bắt buộc phải nhập lý do chênh lệch hoặc số biên bản sự cố trước khi 'Chốt và Chuyển Rửa'.");
        document.getElementById('popGhiChu').focus();
        document.getElementById('popGhiChu').classList.add('border-rose-500', 'ring-2', 'ring-rose-200');
        return;
    }

    try {
        await db.collection("phieuGiaoNhan").doc(currentKiemDemData.id).update({ 
            status: "DANG_RUA", ghiChu: ghiChuValue,
            ktvThuGom: loginUserCode || "CSSD_CHUNG",
            thoiGianDoiSoat: new Date().toLocaleTimeString('vi-VN'),
            coSuCoChenhLech: coSuCoChenhLech,
            chiTietChecklistLinhKien: currentKiemDemData.linhKienKiemDem
        });
        showToast("Đã đối soát xong! Mâm đồ đã chuyển sang Trạm Rửa.", "success"); 
        closePopupKiemDem(); callRender(); 
    } catch(err) { showToast("Lỗi kết nối Firebase nội bộ!", "error"); }
}

function clearGioHang() { gioHangTam = []; renderGioHang(); }

// =========================================================================
// 6. MODULE: QUẢN LÝ MẺ RỬA KHỬ KHUẨN (BELIMED WD250)
// =========================================================================
function toggleSelectAllRua() { let checked = document.getElementById('selectAllRua').checked; document.querySelectorAll('.rua-checkbox').forEach(cb => cb.checked = checked); }
function toggleSelectAllNghiemThuRua() { let checked = document.getElementById('selectAllNghiemThuRua').checked; document.querySelectorAll('.nghiemthurua-checkbox').forEach(cb => cb.checked = checked); }

function capNhatDanhSachMaMayRua() {
    const loaiRua = document.getElementById("rua_loaiRua")?.value;
    const selectMay = document.getElementById("rua_maySo");
    if (!selectMay || !loaiRua) return;
    selectMay.innerHTML = "";
    if (cauHinhMayRua[loaiRua]) {
        cauHinhMayRua[loaiRua].forEach(may => {
            selectMay.innerHTML += `<option value="${may}">${may}</option>`;
        });
    }
    tuDongTaoMaLoMeRua();
}

function tuDongTaoMaLoMeRua() {
    const maMay = document.getElementById("rua_maySo")?.value || "Belimed WD250";
    const maMayVietTat = (maMay === "Belimed WD250") ? "BL" : maMay;
    const now = new Date();
    const yy = String(now.getFullYear()).slice(-2);
    const mm = String(now.getMonth() + 1).padStart(2, '0');
    const dd = String(now.getDate()).padStart(2, '0');
    const ngayChuoi = `${yy}${mm}${dd}`;

    let cacMeTrongNgay = listGiaoDich.filter(x => x.rua_batchCode && x.rua_batchCode.startsWith(maMayVietTat + ngayChuoi));
    let soMeMax = 0;
    cacMeTrongNgay.forEach(x => {
        let phanDuoi = x.rua_batchCode.split("_")[1];
        if (phanDuoi) {
            let num = parseInt(phanDuoi);
            if (num > soMeMax) soMeMax = num;
        }
    });
    let meTiepTheo = soMeMax + 1;
    const chuoiMe = String(meTiepTheo).padStart(2, '0');
    const batchIdHoanChinh = `${maMayVietTat}${ngayChuoi}_${chuoiMe}`;

    if (document.getElementById("rua_meSo")) document.getElementById("rua_meSo").value = chuoiMe;
    if (document.getElementById("rua_batchId")) document.getElementById("rua_batchId").value = batchIdHoanChinh;
}

function xacNhanMeRua() {
    let checkboxes = document.querySelectorAll('.rua-checkbox:checked');
    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm dụng cụ bẩn để xếp vào máy!", "error");
    
    let loaiRua = document.getElementById("rua_loaiRua").value;
    let maMay = document.getElementById("rua_maySo").value;
    let batchCode = document.getElementById("rua_batchId").value;
    let chuKy = document.getElementById("rua_chuKy").value;
    let hoaChat = document.getElementById("rua_hoaChat").value;
    let lieuLuong = document.getElementById("rua_lieuLuong").value || "Theo chuẩn";
    const ngayHomNay = getTodayDateStr();

    let p = [];
    checkboxes.forEach(cb => {
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "TRONG_BUONG_RUA",
                rua_batchCode: batchCode,
                rua_ngayRua: ngayHomNay,
                rua_mayRua: maMay,
                rua_chuKy: chuKy,
                rua_hoaChat: hoaChat,
                rua_lieuLuong: lieuLuong,
                rua_timeBatDau: new Date().toLocaleTimeString('vi-VN')
            })
        );
    });
    Promise.all(p).then(() => {
        playSound('success');
        showToast(`Bắt đầu chạy mẻ máy rửa: ${batchCode}`, "success");
        tuDongTaoMaLoMeRua();
        callRender();
    });
}

function duyetSachMeRuaHangLoat() {
    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');
    if(checkboxes.length === 0) return showToast("Chọn mâm đã rửa sạch để nghiệm thu!", "error");
    
    let ketQuaTest = document.getElementById("rua_testDoSach").value;
    let p = [];
    checkboxes.forEach(cb => {
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "CHO_DONG_GOI",
                rua_ketQuaTestSach: ketQuaTest,
                rua_timeKetThuc: new Date().toLocaleTimeString('vi-VN')
            })
        );
    });
    Promise.all(p).then(() => {
        playSound('success');
        showToast("Xác nhận nghiệm thu đạt chất lượng làm sạch!", "success");
        callRender();
    });
}

function tuChoiMeRuaHangLoat() {
    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');
    if(checkboxes.length === 0) return showToast("Chọn mâm không đạt!", "error");
    
    let p = [];
    checkboxes.forEach(cb => {
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "DANG_RUA",
                rua_batchCode: firebase.firestore.FieldValue.delete(),
                ghiChu: "Làm sạch chưa đạt, yêu cầu rửa lại."
            })
        );
    });
    Promise.all(p).then(() => {
        playSound('error');
        showToast("Đã trả các mâm không đạt về hàng đợi chờ rửa lại!", "success");
        callRender();
    });
}

// =========================================================================
// 7. TRẠM 4: LÀM SẠCH VÀ ĐÓNG GÓI
// =========================================================================
function moPopupDongGoi(id) { idDangDongGoi = id; let item = listGiaoDich.find(x => x.firestoreId === id); if(item) { document.getElementById("popDG_Bo").innerText = item.bo; tinhHanSuDung(); document.getElementById("popupDongGoi").classList.remove("hidden"); } }
function closePopupDongGoi() { document.getElementById("popupDongGoi").classList.add("hidden"); }
function tinhHanSuDung() { let val = document.getElementById("popDG_Loai").value.split("|"); let days = parseInt(val[1]); let dateHSD = new Date(); dateHSD.setDate(dateHSD.getDate() + days); let p = document.getElementById("popDG_Han"); p.innerText = dateHSD.toLocaleDateString('vi-VN'); p.dataset.dateDB = dateHSD.toISOString().split('T')[0]; }
function chotDongGoi() { if(!idDangDongGoi) return; let chatLieuTen = document.getElementById("popDG_Loai").value.split("|")[0]; db.collection("phieuGiaoNhan").doc(idDangDongGoi).update({ status: "CHO_HAP", chatLieu: chatLieuTen, hsd: document.getElementById("popDG_Han").dataset.dateDB }).then(() => { showToast("Đã đóng gói, chuyển chờ hấp!", "success"); closePopupDongGoi(); callRender(); }); }

// =========================================================================
// 8. TRẠM 5: VẬN HÀNH MẺ HẤP & DUYỆT NHẬP KHO VÔ KHUẨN
// =========================================================================
function toggleSelectAllHap() { let checked = document.getElementById('selectAllHap').checked; document.querySelectorAll('.hap-checkbox').forEach(cb => cb.checked = checked); }
function capNhatDanhSachMaMay() { const loaiHap = document.getElementById("hap_loaiHap")?.value; const selectMay = document.getElementById("hap_maySo"); if (!selectMay || !loaiHap) return; selectMay.innerHTML = ""; if (cauHinhMayHap[loaiHap]) { cauHinhMayHap[loaiHap].forEach(may => { selectMay.innerHTML += `<option value="${may}">${may}</option>`; }); } tuDongTaoMaLoMeHap(); }

function tuDongTaoMaLoMeHap() {
    const maMay = document.getElementById("hap_maySo")?.value || "A1"; const now = new Date(); const yy = String(now.getFullYear()).slice(-2); const mm = String(now.getMonth() + 1).padStart(2, '0'); const dd = String(now.getDate()).padStart(2, '0'); const ngayChuoi = `${yy}${mm}${dd}`;
    let cacMeTrongNgay = listGiaoDich.filter(x => x.batchCode && x.batchCode.startsWith(maMay + ngayChuoi)); let soMeMax = 0;
    cacMeTrongNgay.forEach(x => { let phanDuoi = x.batchCode.split("_")[1]; if (phanDuoi) { let num = parseInt(phanDuoi); if (num > soMeMax) soMeMax = num; } });
    let meTiepTheo = soMeMax + 1; if (meTiepTheo > 99) meTiepTheo = 1; const chuoiMe = String(meTiepTheo).padStart(2, '0'); const soLanDone = `${ngayChuoi}_${chuoiMe}`; const batchIdHoanChinh = `${maMay}${soLanDone}`;
    if (document.getElementById("hap_meSo")) document.getElementById("hap_meSo").value = chuoiMe;
    if (document.getElementById("hap_batchId")) document.getElementById("hap_batchId").value = batchIdHoanChinh;
}

function xacNhanMeHap() { 
    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); 
    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 
    
    let loaiHap = document.getElementById("hap_loaiHap").value; 
    let maMay = document.getElementById("hap_maySo").value; 
    let batchCode = document.getElementById("hap_batchId").value; 
    let chuKyNhiet = document.getElementById("hap_nhietDo").value; 
    let apSuat = document.getElementById("hap_apSuat").value || "N/A"; 
    let coKemBI = document.getElementById("hap_hasBI") ? document.getElementById("hap_hasBI").checked : false;
    const ngayHomNay = getTodayDateStr(); 
    let bayGio = new Date(); 
    let thoiGianDuKienXong = new Date(bayGio.getTime() + 45 * 60 * 1000);

    let p = []; 
    checkboxes.forEach(cb => { 
        if(cb.id === 'selectAllHap') return; 
        
        let thongTinLo = { 
            loaiHap: loaiHap, 
            maMay: maMay, 
            chuKyNhiet: chuKyNhiet, 
            apSuat: apSuat, 
            thoiGianBatDau: bayGio.toLocaleTimeString('vi-VN'), 
            giamSatChatLuong: { 
                chiThiHoaHoc: "ĐẠT", 
                laMeTestSinhHocGoc: coKemBI, 
                ketQuaSinhHoc: coKemBI ? "ĐANG CHỜ MÁY Ủ ĐỌC BI (20 PHÚT)" : "KẾ THỪA ĐẦU NGÀY" 
            } 
        };
        
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({ 
                status: "DANG_HAP", 
                batchCode: batchCode, 
                ngayHapRealtime: ngayHomNay, 
                hasBI: coKemBI, 
                thoiGianKetThucChuTrinh: thoiGianDuKienXong.toISOString(), 
                thongTinLoHap: thongTinLo 
            })
        ); 
    }); 

    Promise.all(p).then(() => { 
        playSound('success');
        showToast(`Kích hoạt lò thành công! Lô: ${batchCode}`, "success"); 
        if(document.getElementById("hap_hasBI")) document.getElementById("hap_hasBI").checked = false; 
        duLieuAnhBiTamBase64 = ""; 
        const fileInp = document.getElementById("input_anhBI"); 
        if(fileInp) fileInp.value = ""; 
        tuDongTaoMaLoMeHap(); 
        callRender(); 
    }).catch(err => {
        showToast("Lỗi khi cập nhật mẻ hấp: " + err.message, "error");
    }); 
}

function toggleSelectAllNghiemThu() { let checked = document.getElementById('selectAllNghiemThu').checked; document.querySelectorAll('.nghiemthu-checkbox').forEach(cb => cb.checked = checked); }

function kiemTraQuyenDuyetMeHap() {
    const nutDuyetDat = document.querySelector("button[onclick='nhapKhoHangLoat()']"); if (!nutDuyetDat) return;
    let checkboxesNghiemThu = document.querySelectorAll('.nghiemthu-checkbox:checked'); let phaiChoKetQuaBI = false;
    checkboxesNghiemThu.forEach(cb => { if (cb.getAttribute('data-hasbi') === 'true') phaiChoKetQuaBI = true; });

    if (phaiChoKetQuaBI && !duLieuAnhBiTamBase64) {
        nutDuyetDat.disabled = true;
        nutDuyetDat.className = "w-full bg-slate-300 text-slate-500 font-black py-2.5 rounded text-xs shadow-md cursor-not-allowed flex items-center justify-center gap-2";
        nutDuyetDat.innerHTML = `<i class="fa-solid fa-lock text-slate-400"></i> CHỜ MINH CHỨNG BI (MÁY Ủ ĐỌC 20 PHÚT)`;
    } else {
        nutDuyetDat.disabled = false;
        nutDuyetDat.className = "w-full bg-emerald-600 hover:bg-emerald-700 text-white font-black py-2.5 rounded text-xs shadow-md flex items-center justify-center gap-2 transition-all cursor-pointer";
        nutDuyetDat.innerHTML = `<i class="fa-solid fa-check-double mr-1"></i> DUYỆT ĐẠT & CHO VÀO KHO LƯU TRỮ CHUNG`;
    }
}

function nhapKhoHangLoat() { 
    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!"); 
    const bayGio = new Date(); let p = []; 
     
    checkboxes.forEach(cb => { 
        if(cb.id === 'selectAllNghiemThu') return; 
        let itemData = listGiaoDich.find(x => x.firestoreId === cb.value); if(!itemData) return;
        let coKemBI = itemData.hasBI === true;
        let thoiGianHapXong = itemData.thoiGianKetThucChuTrinh ? new Date(itemData.thoiGianKetThucChuTrinh) : new Date(bayGio.getTime() - 45 * 60 * 1000);
        let soPhutThucTe = Math.floor((bayGio - thoiGianHapXong) / (1000 * 60)); 
        let soPhutTinhKPI = coKemBI ? Math.max(0, soPhutThucTe - 20) : soPhutThucTe;
        let trangThaiKPI = (soPhutTinhKPI > 30) ? "KHÔNG ĐẠT" : "ĐẠT";

        let bgGiamSat = itemData.thongTinLoHap?.giamSatChatLuong || {};
        let updateGiamSat = {
            ...bgGiamSat,
            ketQuaSinhHoc: coKemBI ? "ÂM TÍNH (ĐẠT)" : "KẾ THỪA ĐẦU NGÀY",
            minhChungAnhBase64: coKemBI ? duLieuAnhBiTamBase64 : (bgGiamSat.minhChungAnhBase64 || "")
        };

        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "CHO_XUAT", thoiGianKetLuan: bayGio.toISOString(), thoiGianDocTestBiPhut: soPhutThucTe, 
                thoiGianTinhKpiPhut: soPhutTinhKPI, ketQuaGiamSatKpi: trangThaiKPI, "thongTinLoHap.giamSatChatLuong": updateGiamSat
            })
        ); 
    }); 
     
    Promise.all(p).then(() => { 
        playSound('success');
        showToast("Đã duyệt mâm đạt chuẩn Vô Khuẩn!", "success"); 
        duLieuAnhBiTamBase64 = ""; const fileInp = document.getElementById("input_anhBI"); if(fileInp) fileInp.value = ""; 
        callRender(); 
    }); 
}

function tuChoiHapHangLoat() { 
    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 
    let p = []; 
    checkboxes.forEach(cb => { if(cb.id !== 'selectAllNghiemThu') p.push(db.collection("phieuGiaoNhan").doc(cb.value).update({status: "DANG_RUA", ghiChu: "Không đạt hấp, trả về rửa lại"})); }); 
    Promise.all(p).then(() => { playSound('error'); showToast("Đã trả các mâm không đạt về Trạm làm sạch!", "success"); callRender(); }); 
}

// =========================================================================
// 9. TRẠM 6: KHO VÔ KHUẨN & PHỐI HỢP CẤP PHÁT ĐIỀU PHỐI XOAY VÒNG (FIFO)
// =========================================================================
function xuatKhoXoayVong(maTrucTiep) { 
    const k = document.getElementById("xuat_selKhoa") ? document.getElementById("xuat_selKhoa").value : ""; 
    let ma = maTrucTiep || (document.getElementById("xuat_inpMaBo") ? document.getElementById("xuat_inpMaBo").value.trim().toUpperCase() : ""); 
    
    if(!k) { playSound('error'); return showToast("Vui lòng Chọn Khoa nhận trước khi xuất!", "error"); } 
    if(!ma) return;
    if(gioHangXuatKho.some(x => x.maMacDinh === ma)) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách xuất!", "error"); }
    
    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && x.status === "CHO_XUAT"); 
    if(!khayThucTe) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast(`Mã ID ${ma} không sẵn sàng ở Kho Vô Khuẩn.`, "error"); } 
    
    gioHangXuatKho.push(khayThucTe); playSound('success'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; renderGioHangXuat();
}

function renderGioHangXuat() {
    let listContainer = document.getElementById("danhSachXuatTam");
    if (!listContainer) {
        let bangKho = document.getElementById("bangKhoVoKhuan");
        if (bangKho && bangKho.closest('table')) {
            let wrapper = document.createElement("div"); wrapper.id = "danhSachXuatTam";
            wrapper.className = "mb-4 bg-sky-50 border border-sky-200 rounded p-3 hidden shadow-inner";
            bangKho.closest('table').parentNode.insertBefore(wrapper, bangKho.closest('table')); listContainer = wrapper;
        }
    }
    if(listContainer) {
        if(gioHangXuatKho.length === 0) { listContainer.classList.add("hidden"); } 
        else {
            listContainer.classList.remove("hidden");
            let html = `<div class="flex justify-between items-center mb-2 pb-2 border-b border-sky-200"><h3 class="font-bold text-sky-800 text-[13px]"><i class="fa-solid fa-cart-flatbed mr-2"></i>Đang quét xuất kho: <span class="bg-rose-500 text-white px-2 py-0.5 rounded ml-1">${gioHangXuatKho.length} mâm</span></h3><button onclick="xacNhanXuatKhoHangLoat()" class="bg-emerald-600 text-white px-4 py-1.5 rounded shadow font-black text-xs hover:bg-emerald-700 transition-colors uppercase"><i class="fa-solid fa-check-double mr-1"></i>Chốt Bàn Giao</button></div><div class="flex flex-wrap gap-2">`;
            gioHangXuatKho.forEach((item, index) => { 
                let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "Dụng cụ";
                html += `<div class="bg-white border border-sky-300 px-2 py-1 rounded flex items-center gap-2 shadow-sm animate-pulse"><span class="text-[11px] font-bold text-slate-700">${tenBoGoc}</span><span class="text-[10px] font-mono text-sky-700 bg-sky-100 px-1 rounded font-bold">${item.maMacDinh}</span><i class="fa-solid fa-xmark text-rose-500 cursor-pointer ml-1 text-xs hover:text-rose-700" onclick="xoaKhoiGioXuat(${index})"></i></div>`; 
            });
            html += `</div>`; listContainer.innerHTML = html;
        }
    }
}

function xoaKhoiGioXuat(index) { gioHangXuatKho.splice(index, 1); renderGioHangXuat(); }

function xacNhanXuatKhoHangLoat() {
    const k = document.getElementById("xuat_selKhoa").value; if(!k || gioHangXuatKho.length === 0) return;
    let p = []; 
    gioHangXuatKho.forEach(khay => { 
        p.push(
            db.collection("phieuGiaoNhan").doc(khay.firestoreId).update({ 
                status: "ĐANG_VAN_CHUYEN", khoa: k, ngayHoanTat: getTodayDateStr(), timeHoanTat: new Date().toLocaleTimeString('vi-VN'), nvXuatKho: loginUserCode || "CSSD_CHUNG" 
            })
        ); 
    });
    Promise.all(p).then(() => { 
        playSound('success'); showToast(`Đã xuất kho bàn giao về Khoa ${k}!`, "success"); 
        gioHangXuatKho = []; if (document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; 
        renderGioHangXuat(); callRender(); 
    });
}

function duyetCapPhatTậpTrung(yeuCauId, tenBo, khoaNhan) {
    let khaySẵnCo = listGiaoDich.find(x => x.status === "CHO_XUAT" && x.bo.split(" [ID:")[0].trim().toUpperCase() === tenBo.trim().toUpperCase());
    
    if (!khaySẵnCo) {
        playSound('error');
        return alert(`⚠️ KHÔNG CÓ KHAY SẴN SÀNG TRONG KHO!\nHiện tại trong Kho vô khuẩn CSSD không có khay "${tenBo}" nào sẵn sàng để cấp phát.`);
    }

    if (confirm(`Xác nhận cấp phát khay ${khaySẵnCo.maMacDinh} để đáp ứng yêu cầu của khoa ${khoaNhan}?`)) {
        let bayGio = new Date();
        let batch = db.batch();
        
        let refKhay = db.collection("phieuGiaoNhan").doc(khaySẵnCo.firestoreId);
        batch.update(refKhay, {
            status: "ĐANG_VAN_CHUYEN",
            khoa: khoaNhan,
            ngayHoanTat: getTodayDateStr(),
            timeHoanTat: bayGio.toLocaleTimeString('vi-VN'),
            nvXuatKho: loginUserCode || "CSSD_CHUNG"
        });

        let refYeuCau = db.collection("phieuGiaoNhan").doc(yeuCauId);
        batch.delete(refYeuCau);

        batch.commit().then(() => {
            playSound('success');
            showToast(`Đã cấp phát thành công khay ${khaySẵnCo.maMacDinh} cho khoa ${khoaNhan}!`, "success");
            callRender();
        });
    }
}

// =========================================================================
// XỬ LÝ KÝ CHỮ KÝ ĐIỆN TỬ TRỰC TIẾP TRÊN CANVAS
// =========================================================================
function khoiTaoCanvasKyDienTu() {
    canvasSignature = document.getElementById("canvasKyDienTu");
    if (!canvasSignature) return;
    ctxSignature = canvasSignature.getContext("2d");

    const rect = canvasSignature.getBoundingClientRect();
    canvasSignature.width = rect.width || 380;
    canvasSignature.height = rect.height || 160;

    ctxSignature.lineWidth = 2.5;
    ctxSignature.lineCap = "round";
    ctxSignature.strokeStyle = "#0284c7";

    // Chuột
    canvasSignature.onmousedown = (e) => {
        isDrawingSignature = true;
        ctxSignature.beginPath();
        const pos = getCanvasPos(e);
        ctxSignature.moveTo(pos.x, pos.y);
    };
    canvasSignature.onmousemove = (e) => {
        if (!isDrawingSignature) return;
        const pos = getCanvasPos(e);
        ctxSignature.lineTo(pos.x, pos.y);
        ctxSignature.stroke();
    };
    canvasSignature.onmouseup = () => { isDrawingSignature = false; };

    // Cảm ứng Tablet/Phone
    canvasSignature.ontouchstart = (e) => {
        isDrawingSignature = true;
        ctxSignature.beginPath();
        const pos = getCanvasPos(e.touches[0]);
        ctxSignature.moveTo(pos.x, pos.y);
    };
    canvasSignature.ontouchmove = (e) => {
        if (!isDrawingSignature) return;
        e.preventDefault();
        const pos = getCanvasPos(e.touches[0]);
        ctxSignature.lineTo(pos.x, pos.y);
        ctxSignature.stroke();
    };
    canvasSignature.ontouchend = () => { isDrawingSignature = false; };
}

function getCanvasPos(evt) {
    const rect = canvasSignature.getBoundingClientRect();
    return {
        x: evt.clientX - rect.left,
        y: evt.clientY - rect.top
    };
}

function xoaChuKyCanvas() {
    if (ctxSignature && canvasSignature) {
        ctxSignature.clearRect(0, 0, canvasSignature.width, canvasSignature.height);
    }
}

function khoaKyNhanDoSachDienTu() {
    const checkboxes = document.querySelectorAll("#bangChoNhanTaiKhoa input[type='checkbox']:checked");
    dsIdKhayChoKyNhan = [];
    checkboxes.forEach(chk => dsIdKhayChoKyNhan.push(chk.value));

    if (dsIdKhayChoKyNhan.length === 0) {
        playSound('error');
        return showToast("Vui lòng tích chọn ít nhất 1 khay để ký nhận!", "error");
    }

    const inpName = document.getElementById("ky_tenNguoiNhan");
    if (inpName) inpName.value = loginUserCode || "";

    const pop = document.getElementById("popupKyDienTu");
    if (pop) {
        pop.classList.remove("hidden");
        setTimeout(() => {
            khoiTaoCanvasKyDienTu();
            xoaChuKyCanvas();
        }, 100);
    } else {
        // Fallback trường hợp chưa thêm popup vào HTML
        const khoaDangNhap = currentRole === "KHOA" ? loginUserCode : (document.getElementById("khoa_selKhoa") ? document.getElementById("khoa_selKhoa").value : "");
        if (confirm(`Xác nhận nhận ${dsIdKhayChoKyNhan.length} mâm về khoa ${khoaDangNhap}?`)) {
            let p = [];
            dsIdKhayChoKyNhan.forEach(idDoc => {
                p.push(
                    db.collection("phieuGiaoNhan").doc(idDoc).update({
                        status: "HOAN_TAT", nguoiKyNhanKhoa: loginUserCode || "Điều dưỡng khoa", timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'), ngayKhoaNhanThucTe: getTodayDateStr()
                    })
                );
            });
            Promise.all(p).then(() => { playSound('success'); showToast(`Đã tiếp nhận vào tủ khoa thành công!`, "success"); callRender(); });
        }
    }
}

function dongPopupKyDienTu() {
    document.getElementById("popupKyDienTu")?.classList.add("hidden");
    dsIdKhayChoKyNhan = [];
}

function luuXacNhanKyNhan() {
    const tenNguoiNhan = document.getElementById("ky_tenNguoiNhan")?.value.trim();
    if (!tenNguoiNhan) {
        playSound('error');
        return showToast("Vui lòng nhập tên người nhận!", "error");
    }

    const chuoiAnhChuKy = canvasSignature ? canvasSignature.toDataURL("image/png") : "";

    let p = [];
    dsIdKhayChoKyNhan.forEach(idDoc => {
        p.push(
            db.collection("phieuGiaoNhan").doc(idDoc).update({
                status: "HOAN_TAT",
                nguoiKyNhanKhoa: tenNguoiNhan,
                anhChuKyNhanBase64: chuoiAnhChuKy,
                timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'),
                ngayKhoaNhanThucTe: getTodayDateStr()
            })
        );
    });

    Promise.all(p).then(() => {
        playSound('success');
        showToast(`Đã ký nhận thành công ${dsIdKhayChoKyNhan.length} mâm vào tủ khoa!`, "success");
        dongPopupKyDienTu();
        callRender();
    });
}

// =========================================================================
// 10. GHI NHẬN SỬ DỤNG BỘ DỤNG CỤ TRÊN BỆNH NHÂN
// =========================================================================
function moPopupSuDungBoDungCu() {
    gioKhaySuDungTam = []; document.getElementById("sd_bangKhayChon").innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng quét hoặc nhập mã khay...</td></tr>`;
    document.getElementById("sd_ngaySuDung").value = getTodayDateStr(); document.getElementById("sd_nhanChung").value = loginUserCode || "ADMIN";
    if(document.getElementById("sd_khoaBenhNhan")) document.getElementById("sd_khoaBenhNhan").value = (currentRole === "KHOA") ? loginUserCode : "";
    if(document.getElementById("sd_yTaPhongMo")) document.getElementById("sd_yTaPhongMo").value = "";
    if(document.getElementById("sd_yTaVongNgoai")) document.getElementById("sd_yTaVongNgoai").value = "";
    if(document.getElementById("sd_searchBN")) document.getElementById("sd_searchBN").value = "";
    if(document.getElementById("sd_ghiChu")) document.getElementById("sd_ghiChu").value = "";
    if(document.getElementById("sd_maKhayInp")) document.getElementById("sd_maKhayInp").value = "";
    document.getElementById("popupSuDungBoDungCu").classList.remove("hidden");
}

function closePopupSuDung() { document.getElementById("popupSuDungBoDungCu").classList.add("hidden"); }

function scanKhayVaoSuDung() {
    let ma = document.getElementById("sd_maKhayInp").value.trim().toUpperCase(); if(!ma) return;
    document.getElementById("sd_maKhayInp").value = "";
    if(gioKhaySuDungTam.some(x => x.maMacDinh === ma)) { playSound('error'); return showToast("Mã khay này đã được quét!", "error"); }

    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT"));
    if(!khayThucTe) { playSound('error'); return showToast(`Mã ID ${ma} không hợp lệ hoặc chưa vô khuẩn!`, "error"); }

    gioKhaySuDungTam.push(khayThucTe); playSound('success'); renderBangKhaySuDung();
}

function renderBangKhaySuDung() {
    const tbody = document.getElementById("sd_bangKhayChon"); if(!tbody) return;
    if(gioKhaySuDungTam.length === 0) { tbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Chưa chọn khay...</td></tr>`; return; }
    tbody.innerHTML = gioKhaySuDungTam.map((item, index) => {
        let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A";
        return `<tr class="border-b font-medium hover:bg-slate-50 text-xs">
            <td class="p-2 text-center"><i class="fa-solid fa-square-check text-teal-600 text-sm"></i></td>
            <td class="p-2 font-mono font-bold text-sky-700">${item.maMacDinh}</td>
            <td class="p-2 font-bold text-slate-800">${tenBoGoc}</td>
            <td class="p-2 text-center font-mono font-bold text-rose-600 bg-rose-50/30">${item.batchCode || 'N/A'}</td>
            <td class="p-2 text-center"><span class="px-2 py-0.5 rounded text-[10px] font-bold bg-teal-100 text-teal-800">${item.status}</span></td>
            <td class="p-2 text-center"><i class="fa-solid fa-trash-can text-rose-500 cursor-pointer hover:text-rose-700" onclick="xoaKhayKhoiListSuDung(${index})"></i></td>
        </tr>`;
    }).join('');
}

function xoaKhayKhoiListSuDung(index) {
    gioKhaySuDungTam.splice(index, 1);
    renderBangKhaySuDung();
}

// =========================================================================
// 11. THIẾT LẬP MÁY IN BIXOLON SLP-TX403 (80mm x 50mm) & BIÊN BẢN HÀNG LOẠT
// =========================================================================

// In tem đơn Bixolon 80mm x 50mm
function inTemBixolonChuan(itemData) {
    const pZone = document.getElementById("print-zone");
    if (!pZone) return;

    const tenBo = itemData.bo ? itemData.bo.split(" [ID:")[0] : "DỤNG CỤ VÔ KHUẨN";
    const maID = itemData.maMacDinh || itemData.idKhay || "000000";
    const ngayNSX = itemData.ngayHapRealtime || itemData.ngayTao || getTodayDateStr();
    const ngayHSD = itemData.hsd ? new Date(itemData.hsd).toLocaleDateString('vi-VN') : "Theo quy định";
    const maBatch = itemData.batchCode || itemData.batchId || "N/A";
    const tenKhoa = itemData.khoa || "TẬP TRUNG";

    pZone.innerHTML = `
        <div class="bixolon-label" style="font-family: Arial, sans-serif;">
            <div style="text-align: center; border-bottom: 1.5px solid #000; padding-bottom: 2px;">
                <div style="font-size: 10px; font-weight: bold; text-transform: uppercase;">PHUONG NAM HOSPITAL - CSSD</div>
                <div style="font-size: 12px; font-weight: 900; margin-top: 1px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${tenBo}</div>
            </div>

            <div style="text-align: center; margin: 3px 0; display: flex; justify-content: center;">
                <svg id="bixolon-single-barcode"></svg>
            </div>

            <div style="font-size: 9px; border-top: 1px dashed #000; padding-top: 2px;">
                <div style="display: flex; justify-content: space-between; font-weight: bold;">
                    <span>LÔ: ${maBatch}</span>
                    <span>KHOA: ${tenKhoa}</span>
                </div>
                <div style="display: flex; justify-content: space-between; margin-top: 1px;">
                    <span>NSX: ${ngayNSX}</span>
                    <span style="font-size: 10px; font-weight: 900;">HSD: ${ngayHSD}</span>
                </div>
            </div>
        </div>
    `;

    JsBarcode("#bixolon-single-barcode", maID, {
        format: "CODE128",
        width: 1.6,
        height: 36,
        displayValue: true,
        fontSize: 10,
        fontOptions: "bold",
        margin: 0
    });

    document.body.classList.remove("print-mode-doc");
    document.body.classList.add("print-mode-bixolon");
    pZone.classList.remove("hidden");

    setTimeout(() => {
        window.print();
        pZone.classList.add("hidden");
        document.body.classList.remove("print-mode-bixolon");
    }, 250);
}

function inTemTongHangLoat() { 
    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 
    let batchCode = document.getElementById("hap_batchId")?.value || "A1000000_01"; 
    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 

    checkboxes.forEach((cb) => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { 
            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 
            container.innerHTML += `<div class="bixolon-label" style="font-family: Arial; font-size: 11px; color: #000; background: #fff;"><div style="text-align: center; font-weight: bold; font-size: 12px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-lo-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span></div><div style="display: flex; justify-content: space-between; border-top: 1px dashed #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 9px; font-family: monospace; font-weight: bold; margin-top: 1px;">Lô: ${batchCode}</div></div>`; 
        } 
    }); 

    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); 
    document.body.classList.remove("print-mode-doc");
    document.body.classList.add("print-mode-bixolon");
    pZone.classList.remove("hidden"); 

    checkboxes.forEach(cb => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-lo-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 
    }); 

    setTimeout(() => { 
        window.print(); 
        pZone.classList.add("hidden"); 
        document.body.classList.remove("print-mode-bixolon");
    }, 300); 
}

function inTemNghiemThuHangLoat() { 
    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 
    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 

    checkboxes.forEach((cb) => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { 
            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 
            container.innerHTML += `<div class="bixolon-label" style="font-family: Arial; font-size: 11px; color: #000; background: #fff;"><div style="text-align: center; font-size: 9px; font-weight: bold; letter-spacing: 0.5px;">PN HOSPITAL - CSSD</div><div style="text-align: center; font-weight: bold; font-size: 12px; margin: 1px 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-nt-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span><span style="color: green;">ĐẠT VÔ KHUẨN</span></div><div style="display: flex; justify-content: space-between; border-top: 1px solid #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 8px; font-weight: bold; font-family: monospace; margin-top: 1px;">BATCH: ${item.batchCode || 'N/A'}</div></div>`; 
        } 
    }); 

    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); 
    document.body.classList.remove("print-mode-doc");
    document.body.classList.add("print-mode-bixolon");
    pZone.classList.remove("hidden"); 

    checkboxes.forEach(cb => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-nt-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 
    }); 

    setTimeout(() => { 
        window.print(); 
        pZone.classList.add("hidden"); 
        document.body.classList.remove("print-mode-bixolon");
    }, 300); 
}

// In Biên bản giao nhận khổ A4 máy in văn phòng
function inHoaDonGiaoNhan() {
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; if (!k) return showToast("Chọn Khoa trước!", "error");
    
    let htmlBang = `
        <table style="width:100%; border-collapse:collapse; margin-top:20px; font-size:13px; text-align:left; font-family:Arial, sans-serif;">
            <thead>
                <tr style="background-color:#f1f5f9;">
                    <th style="padding:10px; border:1px solid #334155; width:35%; text-align:left; color:#0f172a;">PHÂN LOẠI MÂM / LOẠI DỤNG CỤ</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ TRẢ BẨN</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ NHẬN SẠCH</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">NHẬN VÔ KHUẨN</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:20%; color:#0f172a;">CSSD NỢ KHOA</th>
                </tr>
            </thead>
            <tbody>`;

    const rows = document.querySelectorAll('#bangDonGiaoNhan tr');
    if (rows.length === 0 || (rows.length === 1 && rows[0].cells.length === 1)) { 
        htmlBang += `<tr><td colspan="5" style="padding:15px; border:1px solid #334155; text-align:center; font-style:italic;">Không có dữ liệu công nợ.</td></tr>`; 
    } else {
        rows.forEach(row => {
            const cells = row.cells;
            if (cells.length >= 5) { 
                htmlBang += `
                    <tr>
                        <td style="padding:10px; border:1px solid #334155; font-weight:bold; color:#0f172a;">${cells[0].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#e11d48;">${cells[1].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#0284c7;">${cells[2].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#7c3aed;">${cells[3].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#d97706;">${cells[4].innerText}</td>
                    </tr>`; 
            }
        });
    }
    htmlBang += `</tbody></table>`;

    const pZone = document.getElementById("print-zone");
    pZone.innerHTML = `
        <div style="width:100%; font-family:Arial, sans-serif; padding:10px; color:#000;">
            <div style="text-align:center; margin-bottom:25px;">
                <h2 style="margin:0; font-size:18px; font-weight:900; text-transform:uppercase;">BIÊN BẢN GIAO NHẬN DỤNG CỤ CSSD</h2>
                <p style="margin:8px 0 0 0; font-size:13px; font-weight:bold;">Khoa/Phòng: <span style="text-transform:uppercase; color:#0284c7;">${k}</span> - Ngày xuất: ${new Date().toLocaleDateString('vi-VN')}</p>
            </div>
            ${htmlBang}
            <div style="margin-top:50px; display:flex; justify-content:space-between; font-size:12px; font-weight:bold; padding:0 30px;">
                <div style="text-align:center; width:40%;">
                    <p style="margin-bottom:70px; text-transform:uppercase;">ĐIỀU DƯỠNG LÂM SÀNG</p>
                    <p style="font-weight:normal; font-style:italic; color:#64748b;">(Ký và ghi rõ họ tên)</p>
                </div>
                <div style="text-align:center; width:40%;">
                    <p style="margin-bottom:70px; text-transform:uppercase;">CHUYÊN VIÊN CSSD</p>
                    <p style="font-weight:normal; font-style:italic; color:#64748b;">(Ký và ghi rõ họ tên)</p>
                </div>
            </div>
        </div>`;

    document.body.classList.remove("print-mode-bixolon");
    document.body.classList.add("print-mode-doc");
    pZone.classList.remove("hidden");

    setTimeout(() => { 
        window.print(); 
        pZone.classList.add("hidden"); 
        document.body.classList.remove("print-mode-doc");
    }, 200);
}

// =========================================================================
// 12. HÀM PHỤ TRỢ: CHỨC NĂNG QUẢN TRỊ ADMIN & TIỆN ÍCH
// =========================================================================
function taiDanhMucLinhKienChuan() {
    const tbody = document.getElementById("bangDanhMucLinhKien"); if (!tbody) return; tbody.innerHTML = "";
    if (!databaseExcel || databaseExcel.length === 0) { tbody.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-slate-400 italic">Chưa có dữ liệu. Vui lòng nạp file Excel.</td></tr>`; return; }
    let gopBoExcel = {};
    databaseExcel.forEach(x => {
        let tenBo = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; if (!tenBo) return;
        tenBo = String(tenBo).trim().toUpperCase(); if (!gopBoExcel[tenBo]) gopBoExcel[tenBo] = []; gopBoExcel[tenBo].push(x);
    });
    for (let tenBo in gopBoExcel) {
        let linhKienHtml = `<div class="flex flex-wrap gap-1">`; let tongSl = 0;
        gopBoExcel[tenBo].forEach(item => {
            let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || item['Dụng cụ'] || item['Tên Chi Tiết'] || "Dụng cụ Chi Tiết";
            
            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {
                tenDc = "Dụng cụ chuẩn mâm";
            }
            
            let sl = parseInt(item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1) || 0; let hanHap = item['Tuổi thọ mẻ hấp'] || item['Tuổi thọ'] || 100;
            tongSl += sl; linhKienHtml += `<span class="bg-slate-100 text-slate-700 px-1.5 py-0.5 rounded text-[10px] font-medium border border-slate-200">${tenDc} (${sl}) <span class="text-amber-600 font-bold">[Max: ${hanHap}]</span></span>`;
        });
        linhKienHtml += `</div>`; const tr = document.createElement("tr"); tr.className = "hover:bg-slate-50 transition-colors border-b";
        tr.innerHTML = `<td class="p-3 font-bold text-slate-800 text-xs">${tenBo}</td><td class="p-3">${linhKienHtml}</td><td class="p-3 text-center font-black text-sky-700 bg-sky-50/30">${tongSl}</td>`;
        tbody.appendChild(tr);
    }
}

function updateGiaoDienMatrix(role, tabId, checkboxElement) {
    if(!cauHinhGiaoDien[role]) cauHinhGiaoDien[role] = [];
    if(checkboxElement.checked) { if(!cauHinhGiaoDien[role].includes(tabId)) cauHinhGiaoDien[role].push(tabId); } 
    else { cauHinhGiaoDien[role] = cauHinhGiaoDien[role].filter(x => x !== tabId); }
    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ cauHinhGiaoDien: cauHinhGiaoDien }).then(() => showToast("Đã cập nhật quyền truy cập!", "success"));
}

function handleViewAnhBiMoi(firestoreId) { hanhDongXemAnhBiMoi(firestoreId); }
function hanhDongXemAnhBiMoi(firestoreId) {
    let item = listGiaoDich.find(x => x.firestoreId === firestoreId); if (!item) return showToast("Không tìm thấy thông tin!", "error");
    let loGoc = listGiaoDich.find(m => m.batchCode === item.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 
    let chuoiAnhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || item.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;
    if (!chuoiAnhBase64) return showToast("Mẻ này chưa tải ảnh minh chứng hoặc kế thừa.", "error");
    let w = window.open(); w.document.write(`<html><head><title>MINH CHỨNG BI - LÔ ${item.batchCode}</title></head><body style='margin:0; background:#000; display:flex; justify-content:center; align-items:center;'><img src='${chuoiAnhBase64}' style='max-width:100%; max-height:100vh; object-fit:contain;'/></body></html>`);
}

function switchAdminSubtab(sub) { 
    document.getElementById('subtab-database')?.classList.add('hidden'); document.getElementById('subtab-security')?.classList.add('hidden'); 
    document.getElementById('subbtn-database')?.classList.replace('admin-subtab-active', 'text-slate-600'); document.getElementById('subbtn-security')?.classList.replace('admin-subtab-active', 'text-slate-600'); 
    document.getElementById('subtab-' + sub)?.classList.remove('hidden'); document.getElementById('subbtn-' + sub)?.classList.add('admin-subtab-active'); 
}

function saveAdminPIN(type) { let newVal = document.getElementById(`cfg_pin${type.charAt(0).toUpperCase() + type.slice(1).toLowerCase()}`).value.trim(); if(type === 'ADMIN') thongTinMatKhauAdmin.adminPIN = newVal; if(type === 'CSSD') thongTinMatKhauAdmin.cssdPIN = newVal; if(type === 'GUEST') thongTinMatKhauAdmin.guestPIN = newVal; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ thongTinMatKhauAdmin: thongTinMatKhauAdmin }).then(() => showToast("Đã lưu PIN!", "success")); }
function themKtvCssd() { let code = prompt("Mã NV:"); let ten = prompt("Tên:"); let pin = prompt("PIN:"); if(code && ten && pin) { danhSachKtvCssd.push({ code: code.toUpperCase(), ten: ten, pin: pin }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }
function xaoKtvCssd(code) { if(confirm("Xóa?")) { danhSachKtvCssd = danhSachKtvCssd.filter(x => x.code !== code); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }
function themKhoaThuCong() { let t = prompt("Nhập Tên Khoa/Phòng:"); if(t) { danhSachKhoa.push({ ten: t.toUpperCase(), pin: "123", danhSachBo: [] }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Thêm Khoa thành công!", "success")); } }

function khaiSinhKhayVangLai() {
    if(danhSachKhoa.length === 0) return showToast("Hệ thống chưa có Khoa nào!", "error");
    let tenKhay = prompt("Nhập Tên Mâm/Khay dụng cụ:"); if(!tenKhay) return;
    let maId = prompt("Nhập Mã ID khay:"); if(!maId) return; maId = maId.toUpperCase().trim();
    let dsTenKhoa = danhSachKhoa.map((k, idx) => `${idx + 1}. ${k.ten}`).join("\n");
    let luaChon = prompt(`Chọn số thứ tự Khoa sở hữu:\n${dsTenKhoa}`); if(!luaChon) return;
    let idxKhoa = parseInt(luaChon) - 1; if(idxKhoa < 0 || idxKhoa >= danhSachKhoa.length) return showToast("Lựa chọn không hợp lệ!", "error");
    let khoaChon = danhSachKhoa[idxKhoa]; let chuoiKhayDinhDang = `${tenKhay.toUpperCase().trim()} [ID:${maId}]`;
    if(!khoaChon.danhSachBo) khoaChon.danhSachBo = []; if(khoaChon.danhSachBo.includes(chuoiKhayDinhDang)) return showToast("Khay đã tồn tại!", "error");
    khoaChon.danhSachBo.push(chuoiKhayDinhDang);
    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => { showToast(`Khai sinh khay ${maId} thành công!`, "success"); callRender(); });
}

function updatePINTrựcTiep(idx, tenKhoa) { let p = document.getElementById(`pin-khoa-${idx}`).value.trim(); if(p) { danhSachKhoa.find(x => x.ten === tenKhoa).pin = p; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Đổi PIN thành công", "success")); } }

function initSelects() { 
    let opts = '<option value="">-- Chọn Khoa --</option>' + danhSachKhoa.map(k=>`<option value="${k.ten}">${k.ten}</option>`).join('');
    if(document.getElementById("login_khoa")) document.getElementById("login_khoa").innerHTML = opts; 
    if(document.getElementById("khoa_selKhoa")) document.getElementById("khoa_selKhoa").innerHTML = opts; 
    if(document.getElementById("login_nv_cssd")) document.getElementById("login_nv_cssd").innerHTML = '<option value="">-- Chọn KTV CSSD --</option>' + danhSachKtvCssd.map(k=>`<option value="${k.code}">${k.code} - ${k.ten}</option>`).join(''); 
     
    if (document.getElementById("rua_hoaChat")) {
        document.getElementById("rua_hoaChat").innerHTML = danhMucHoaChatRua.map(hc => `<option value="${hc.ten}">${hc.ten}</option>`).join('');
        document.getElementById("rua_lieuLuong").value = danhMucHoaChatRua[0].lieuLuong;
        document.getElementById("rua_hoaChat").addEventListener("change", (e) => {
            let selectedHc = danhMucHoaChatRua.find(hc => hc.ten === e.target.value);
            if(selectedHc) document.getElementById("rua_lieuLuong").value = selectedHc.lieuLuong;
        });
    }
    if (document.getElementById("rua_loaiRua")) { capNhatDanhSachMaMayRua(); }
    if (document.getElementById("hap_loaiHap")) { capNhatDanhSachMaMay(); }
     
    const selKhoaTraba = document.getElementById("khoa_selKhoa"); if(selKhoaTraba) { selKhoaTraba.removeEventListener("change", loadBoDungCuTheoKhoa); selKhoaTraba.addEventListener("change", loadBoDungCuTheoKhoa); }
}

function showToast(msg, type="error") { const t = document.createElement('div'); t.className = `fixed top-6 right-6 ${type==="error"?"bg-rose-600":"bg-emerald-600"} text-white px-5 py-3.5 rounded-lg shadow-2xl z-[100] font-bold text-sm`; t.innerHTML = msg; document.body.appendChild(t); setTimeout(() => t.remove(), 2500); }
function toggleMobileMenu() { document.getElementById("sidebar_menu").classList.toggle("-translate-x-full"); document.getElementById("mobile-overlay").classList.toggle("hidden"); }

function moCamera(inputId) { targetInputIdForScan = inputId; document.getElementById("popupScanner").classList.remove("hidden"); html5QrCode = new Html5Qrcode("reader"); Html5Qrcode.getCameras().then(devices => { let cid = devices.length > 1 ? devices[devices.length - 1].id : devices[0].id; html5QrCode.start(cid, { fps: 15, qrbox: { width: 260, height: 180 } }, (txt) => { document.getElementById(targetInputIdForScan).value = txt.trim().toUpperCase(); if(targetInputIdForScan==='khoa_inpMaBo') themVaoGio(); if(targetInputIdForScan==='xuat_inpMaBo') xuatKhoXoayVong(); if(targetInputIdForScan==='sd_maKhayInp') scanKhayVaoSuDung(); }).catch(e=>{}) }); }
function dongCamera() { if(html5QrCode) html5QrCode.stop().then(() => html5QrCode.clear()); document.getElementById("popupScanner").classList.add("hidden"); }
function xoaSachDuLieuGiaoDichRealtime() { if(prompt("Nhập PIN ADMIN:") === (thongTinMatKhauAdmin.adminPIN||"admin2026")) { db.collection("phieuGiaoNhan").get().then(snap => { let b = db.batch(); snap.forEach(d => b.delete(d.ref)); b.commit().then(() => location.reload()); }); } }

function docAnhBiUpTaiCho(inputElement) { 
    const file = inputElement.files[0]; if (!file) return; 
    const reader = new FileReader(); reader.onloadend = function() { duLieuAnhBiTamBase64 = reader.result; showToast("Tải ảnh minh chứng BI thành công!", "success"); kiemTraQuyenDuyetMeHap(); }; 
    reader.readAsDataURL(file); 
}

// =========================================================================
// 13. HÀM CORE RENDER DỮ LIỆU ĐA TAB LUỒNG HỆ THỐNG
// =========================================================================
function renderTheoTabHienTai() {
    if(activeTab === 'khoaphong') {
        const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;
        let tatCaDonCuaKhoa = listGiaoDich.filter(x => (x.khoa || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase());
        
        // 1. TÍNH TOÁN CÔNG NỢ LUÂN CHUYỂN THỰC TẾ
        let gopCongNo = {};
        tatCaDonCuaKhoa.forEach(x => {
            if (x.status === "CHO_CAP_PHAT") return;

            let tenLoaiMâm = x.bo ? String(x.bo).split(" [ID:")[0] : "Chưa rõ mâm";
            if (!gopCongNo[tenLoaiMâm]) gopCongNo[tenLoaiMâm] = { daTraBan: 0, nhanSach: 0, nhanVoKhuan: 0, cssdNo: 0 };
            
            if (x.status !== "CHO_THU") gopCongNo[tenLoaiMâm].daTraBan += 1; 
            if (["DANG_RUA", "TRONG_BUONG_RUA", "CHO_DONG_GOI", "CHO_HAP", "DANG_HAP", "CHO_XUAT", "ĐANG_VAN_CHUYEN", "HOAN_TAT", "DA_SU_DUNG"].includes(x.status)) gopCongNo[tenLoaiMâm].nhanSach += 1;
            if (x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG") gopCongNo[tenLoaiMâm].nhanVoKhuan += 1; 
            gopCongNo[tenLoaiMâm].cssdNo = gopCongNo[tenLoaiMâm].daTraBan - gopCongNo[tenLoaiMâm].nhanVoKhuan;
        });
        
        let arrHtml = [];
        for (let key in gopCongNo) {
            let item = gopCongNo[key];
            arrHtml.push(`<tr class="border-b hover:bg-slate-50 transition-colors font-medium text-[11px]"><td class="p-3 font-bold text-slate-800 border-r">${key}</td><td class="p-3 text-center text-rose-600 font-bold bg-rose-50/20 border-r">${item.daTraBan}</td><td class="p-3 text-center text-sky-600 font-bold bg-sky-50/20 border-r">${item.nhanSach}</td><td class="p-3 text-center text-purple-600 font-bold bg-purple-50/20 border-r">${item.nhanVoKhuan}</td><td class="p-3 text-center text-amber-600 font-black bg-amber-50/20">${item.cssdNo > 0 ? 'Nợ ' + item.cssdNo : '-'}</td></tr>`);
        }
        if(document.getElementById("bangDonGiaoNhan")) {
            document.getElementById("bangDonGiaoNhan").innerHTML = arrHtml.length ? arrHtml.join('') : `<tr><td colspan="5" class="p-8 text-center text-slate-400 italic">Khoa chưa phát sinh công nợ luân chuyển đồ.</td></tr>`;
        }
        
        // 2. DANH SÁCH CHỜ NHẬN ĐỒ SẠCH (Từ xe trung chuyển CSSD bàn giao lên)
        const tbodyChoNhan = document.getElementById("bangChoNhanTaiKhoa"); const badgeChoNhan = document.getElementById("badgeChoNhanKhoa"); const txtNguoiXacNhan = document.getElementById("txtNguoiDungNhanHienTai");
        if(txtNguoiXacNhan) txtNguoiXacNhan.innerText = `PM-ĐD: ${loginUserCode || "Chưa Đăng Nhập"}`;
        
        let dsDangVanChuyen = tatCaDonCuaKhoa.filter(x => x.status === "ĐANG_VAN_CHUYEN"); if(badgeChoNhan) badgeChoNhan.innerText = `${dsDangVanChuyen.length} khay`;
        if(tbodyChoNhan) {
            if(dsDangVanChuyen.length === 0) { tbodyChoNhan.innerHTML = `<tr><td colspan="5" class="p-3 text-center text-slate-400 italic">Hiện không có dụng cụ nào đang chuyển về khoa.</td></tr>`; } 
            else { tbodyChoNhan.innerHTML = dsDangVanChuyen.map(khay => `<tr class="hover:bg-slate-50 transition-colors"><td class="p-2 text-center"><input type="checkbox" value="${khay.firestoreId}" class="w-3.5 h-3.5 text-sky-600 rounded border-slate-300 focus:ring-sky-500 cursor-pointer"></td><td class="p-2 font-mono font-bold text-slate-700">${khay.maMacDinh || 'N/A'}</td><td class="p-2 font-semibold text-slate-800">${khay.bo ? String(khay.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-2 text-slate-500 font-medium">${khay.nvXuatKho || '--'}</td><td class="p-2 text-center"><span class="bg-purple-50 text-purple-700 font-mono text-[10px] px-1.5 py-0.5 rounded font-bold">${khay.batchCode || 'N/A'}</span></td></tr>`).join(''); }
        }

        // 3. THEO DÕI YÊU CẦU ĐANG CHỜ CSSD CẤP PHÁT
        let dsChoCapPhat = tatCaDonCuaKhoa.filter(x => x.status === "CHO_CAP_PHAT");
        const tbodyChoCapPhat = document.getElementById("bangChoCapPhatTaiKhoa");
        if (tbodyChoCapPhat) {
            if (dsChoCapPhat.length === 0) {
                tbodyChoCapPhat.innerHTML = `<tr><td colspan="4" class="p-3 text-center text-slate-400 italic">Không có yêu cầu mượn khay mới nào đang chờ duyệt.</td></tr>`;
            } else {
                tbodyChoCapPhat.innerHTML = dsChoCapPhat.map(yc => `
                    <tr class="border-b text-xs hover:bg-amber-50/10 font-medium">
                        <td class="p-2.5 font-bold text-amber-800">${yc.bo}</td>
                        <td class="p-2.5 text-center"><span class="px-2 py-0.5 rounded-full text-[10px] font-bold bg-amber-100 text-amber-800 border border-amber-200 animate-pulse">Chờ CSSD cấp khay</span></td>
                        <td class="p-2.5 text-center font-mono text-slate-400">${yc.time}</td>
                        <td class="p-2.5 text-center text-slate-500">${yc.nvYeuCau || 'ĐD'}</td>
                    </tr>
                `).join('');
            }
        }
    }
    else if(activeTab === 'thugom') {
        let fK = document.getElementById("filterKhoaThuGom")?.value || ""; 
        let lsTG = listGiaoDich.filter(x => x.status === "CHO_THU"); 
        
        if (document.getElementById("filterKhoaThuGom")) {
            let danhSachKhoaCoDon = [...new Set(lsTG.map(x => x.khoa))].filter(Boolean); 
            let htmlOpts = '<option value="">-- Lọc theo Khoa --</option>';
            danhSachKhoaCoDon.forEach(k => { 
                htmlOpts += `<option value="${k}" ${(k === fK) ? "selected" : ""}>${k}</option>`; 
            });
            document.getElementById("filterKhoaThuGom").innerHTML = htmlOpts; 
            fK = document.getElementById("filterKhoaThuGom").value || "";
        }
        
        if (fK) lsTG = lsTG.filter(x => (x.khoa || "").toString().trim().toUpperCase() === fK.toString().trim().toUpperCase());
        if (document.getElementById("badgeSoCho")) document.getElementById("badgeSoCho").innerText = `${lsTG.length} Lệnh`;
        
        let bodyThuGom = document.getElementById("bangChoThuGom");
        if(bodyThuGom) {
            bodyThuGom.innerHTML = lsTG.map(i => {
                let tenBo = i.bo ? String(i.bo).split(" [ID:")[0].trim() : ""; 
                
                let itemsInBo = databaseExcel.filter(x => {
                    let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || '';
                    return String(boName).trim().toUpperCase() === String(tenBo).toUpperCase();
                });
                
                let checklistHtml = "";
                if (itemsInBo.length > 0) {
                    checklistHtml = `<div class="max-h-32 overflow-y-auto pr-1 border border-slate-200/60 rounded bg-slate-50/50 p-1.5 mt-1 flex flex-col gap-1">`;
                    itemsInBo.forEach(item => {
                        let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || "Dụng cụ chi tiết";
                        let sl = item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1;
                        
                        if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {
                            tenDc = "Dụng cụ chuẩn mâm";
                        }

                        checklistHtml += `
                            <div class="flex justify-between items-center border-b border-dashed border-slate-200 pb-0.5 last:border-none text-[11px]">
                                <span class="text-slate-700 font-semibold"><i class="fa-solid fa-circle text-[4px] text-sky-400 mr-1.5 align-middle"></i>${tenDc}</span>
                                <span class="font-bold text-sky-700 bg-sky-50 px-1.5 py-0.5 rounded border border-sky-100 text-[10px]">x${sl}</span>
                            </div>`;
                    });
                    checklistHtml += `</div>`;
                } else {
                    checklistHtml = `<div class="text-[10px] text-emerald-700 bg-emerald-50 border border-emerald-100 p-1.5 rounded font-medium mt-1"><i class="fa-solid fa-circle-check mr-1"></i>Dụng cụ lẻ (Cơ số: 1 chiếc)</div>`;
                }
                
                return `
                    <tr class="border-b border-slate-100 hover:bg-slate-50/40 transition-colors">
                        <td class="p-3 align-top">
                            <div class="font-black text-slate-700 text-[11px] uppercase tracking-wide">${i.khoa}</div>
                            ${i.ghiChu ? `<div class="text-[10px] text-rose-600 font-bold bg-rose-50 border border-rose-100 px-1.5 py-0.5 rounded mt-1.5 inline-block"><i class="fa-solid fa-triangle-exclamation mr-1"></i>${i.ghiChu}</div>` : ''}
                        </td>
                        <td class="p-3 align-top">
                            <div class="font-black text-sky-800 text-[13px] uppercase tracking-tight">${tenBo}</div>
                            <div class="text-[10px] font-mono text-slate-400 font-bold mt-0.5">MÃ SỐ ID: ${i.maMacDinh}</div>
                            ${checklistHtml}
                        </td>
                        <td class="p-3 text-center align-top font-mono font-bold text-slate-500 text-[11px]">${i.time}</td>
                        <td class="p-3 text-center align-top action-col">
                            <button onclick="moPopupKiemDem('${i.firestoreId}')" class="bg-sky-600 hover:bg-sky-700 text-white font-black px-3.5 py-2 rounded-lg shadow-sm text-[11px] uppercase tracking-wider transition-all"><i class="fa-solid fa-clipboard-check mr-1"></i>Kiểm Đếm</button>
                        </td>
                    </tr>`;
            }).join('');
        }
    }
    else if(activeTab === 'mayrua') {
        let lsCR = listGiaoDich.filter(x => x.status === "DANG_RUA");
        if(document.getElementById("badgeChoRua")) document.getElementById("badgeChoRua").innerText = `${lsCR.length} Mục`;
        
        let bodyChoRua = document.getElementById("bangChoRua");
        if(bodyChoRua) {
            bodyChoRua.innerHTML = lsCR.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="rua-checkbox"></td><td class="p-3 font-bold">${i.bo}</td><td class="p-3 text-right font-mono">${i.maMacDinh}</td></tr>`).join('');
        }
        
        let lsTR = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA");
        let bodyChoNghiemThuRua = document.getElementById("bangChoNiemThuRua");
        if(bodyChoNghiemThuRua) {
            bodyChoNghiemThuRua.innerHTML = lsTR.map(i => `<tr class="border-b"><td class="p-2.5 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="nghiemthurua-checkbox"></td><td class="p-2 text-xs font-bold">${i.bo} <span class="text-slate-400 font-mono">(${i.rua_batchCode || 'Mẻ Rửa'})</span></td></tr>`).join('');
        }
        
        let bodyLichSuRua = document.getElementById("bangLichSuRua");
        if(bodyLichSuRua) {
            const homNayMoiStr = getTodayDateStr();
            let tatCaMucCoLoRua = listGiaoDich.filter(x => x.rua_batchCode && (x.rua_ngayRua === homNayMoiStr || x.ngayTao === homNayMoiStr));
            let cacMeRuaGop = {};
            tatCaMucCoLoRua.forEach(x => {
                if(!cacMeRuaGop[x.rua_batchCode]) {
                    cacMeRuaGop[x.rua_batchCode] = {
                        batchCode: x.rua_batchCode,
                        mayRua: x.rua_mayRua || "Belimed WD250",
                        chuKy: x.rua_chuKy || "Chu trình tiêu chuẩn",
                        hoaChat: x.rua_hoaChat || "Smeg",
                        timeBatDau: x.rua_timeBatDau || "--:--",
                        ngay: x.rua_ngayRua || "",
                        soLuong: 0,
                        trangThaiMe: x.status === "TRONG_BUONG_RUA" ? "Đang rửa" : "Hoàn tất sạch"
                    };
                }
                cacMeRuaGop[x.rua_batchCode].soLuong += 1;
            });
            let danhSachMeRuaSapXep = Object.values(cacMeRuaGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));
            if(danhSachMeRuaSapXep.length === 0) {
                bodyLichSuRua.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa chạy mẻ rửa nào</td></tr>`;
            } else {
                bodyLichSuRua.innerHTML = danhSachMeRuaSapXep.map(me => `
                    <tr class="hover:bg-slate-50 transition-colors">
                        <td class="p-3">
                            <div class="flex items-center gap-2">
                                <span class="font-mono font-black text-sky-700 text-sm tracking-wider">${me.batchCode}</span>
                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "Đang rửa" ? "text-amber-600 bg-amber-50 border-amber-200" : "text-emerald-600 bg-emerald-50 border-emerald-200"}">${me.trangThaiMe}</span>
                            </div>
                            <div class="text-[11px] text-slate-500 font-medium mt-1">${me.mayRua} | ${me.chuKy}</div>
                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Hoá chất: ${me.hoaChat} | Bắt đầu: ${me.timeBatDau} (${me.ngay})</div>
                        </td>
                        <td class="p-3 text-right pr-4">
                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuong} khay</span>
                        </td>
                    </tr>`).join('');
            }
        }
    }
    else if(activeTab === 'donggoi') {
        let lsDG = listGiaoDich.filter(x => x.status === "CHO_DONG_GOI"); 
        if(document.getElementById("badgeDongGoi")) document.getElementById("badgeDongGoi").innerText = lsDG.length;
        let gDongGoi = document.getElementById("gridDongGoi");
        if(gDongGoi) {
            gDongGoi.innerHTML = lsDG.map(i => `<div class="bg-white p-3 rounded border border-slate-200 mb-2 flex justify-between items-center"><div class="flex-1"><div class="font-bold text-sky-700 text-[13px]">${i.bo}</div><div class="text-[10px] text-slate-500">Từ khoa: ${i.khoa}</div></div><button onclick="moPopupDongGoi('${i.firestoreId}')" class="bg-sky-50 text-sky-700 border border-sky-300 px-3 py-1.5 rounded text-[11px] font-black">ĐÓNG GÓI</button></div>`).join('');
        }
    }
    else if(activeTab === 'mayhap') {
        let lsCH = listGiaoDich.filter(x => x.status === "CHO_HAP"); 
        if(document.getElementById("badgeChoHap")) document.getElementById("badgeChoHap").innerText = `${lsCH.length} Mục`;
        let bodyChoHap = document.getElementById("bangChoHap");
        if(bodyChoHap) { 
            bodyChoHap.innerHTML = lsCH.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="hap-checkbox"></td><td class="p-3 font-bold text-xs">${i.bo}</td><td class="p-3 text-right font-mono text-xs font-bold text-sky-700">${i.maMacDinh}</td></tr>`).join(''); 
        }

        let lsNT = listGiaoDich.filter(x => x.status === "DANG_HAP"); 
        let bodyChoNghiemThu = document.getElementById("bangChoNghiệmThu") || document.getElementById("bangChoNghiemThu");
        
        if(bodyChoNghiemThu) { 
            if (lsNT.length === 0) {
                bodyChoNghiemThu.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Không có mâm/mẻ nào đang trong lò chờ duyệt.</td></tr>`;
            } else {
                bodyChoNghiemThu.innerHTML = lsNT.map(i => {
                    let tenBoClean = i.bo ? String(i.bo).split(" [ID:")[0] : "Chưa rõ mâm";
                    return `
                        <tr class="border-b hover:bg-amber-50/40 transition-colors">
                            <td class="p-2.5 text-center action-col">
                                <input type="checkbox" value="${i.firestoreId}" class="nghiemthu-checkbox w-4 h-4 text-amber-600 rounded cursor-pointer" data-hasbi="${i.hasBI ? 'true' : 'false'}" onchange="kiemTraQuyenDuyetMeHap()">
                            </td>
                            <td class="p-2 text-xs">
                                <div class="font-black text-slate-800">${tenBoClean} <span class="text-[10px] font-mono text-sky-700">(${i.maMacDinh || 'N/A'})</span></div>
                                <div class="flex items-center gap-2 mt-0.5">
                                    <span class="text-[10px] font-mono font-bold text-rose-700 bg-rose-50 px-1 rounded border border-rose-100">Lô: ${i.batchCode || 'N/A'}</span>
                                    ${i.hasBI ? '<span class="px-1.5 py-0.5 bg-amber-100 text-amber-800 rounded text-[9px] font-black border border-amber-300 flex items-center gap-1"><i class="fa-solid fa-flask"></i> CHỜ BI</span>' : ''}
                                </div>
                            </td>
                        </tr>`;
                }).join(''); 
            }
        }
        kiemTraQuyenDuyetMeHap();

        if(document.getElementById("bangLichSuHap")) {
            const homNayMoiStr = getTodayDateStr(); 
            let tatCaMucCoLo = listGiaoDich.filter(x => x.batchCode && (x.ngayHapRealtime === homNayMoiStr || x.ngayTao === homNayMoiStr)); 
            let cacMeHapGop = {};
            tatCaMucCoLo.forEach(x => {
                if(!cacMeHapGop[x.batchCode]) { 
                    cacMeHapGop[x.batchCode] = { 
                        batchCode: x.batchCode, 
                        loaiHap: x.thongTinLoHap?.loaiHap || "Hấp hơi nước", 
                        chuKyNhiet: x.thongTinLoHap?.chuKyNhiet || "134°C - 4 phút", 
                        apSuat: x.thongTinLoHap?.apSuat || "N/A", 
                        thoiGian: x.thongTinLoHap?.thoiGianBatDau || x.time || "--:--", 
                        ngay: x.ngayHapRealtime || x.ngayTao || "", 
                        soLuongKhay: 0, 
                        trangThaiMe: x.status === "DANG_HAP" ? "ĐANG CHẠY LÒ" : "HOÀN THÀNH" 
                    }; 
                }
                cacMeHapGop[x.batchCode].soLuongKhay += 1;
            });
            let danhSachMeHapSapXep = Object.values(cacMeHapGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));
            if(danhSachMeHapSapXep.length === 0) { 
                document.getElementById("bangLichSuHap").innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa có mẻ hấp nào</td></tr>`; 
            } else { 
                document.getElementById("bangLichSuHap").innerHTML = danhSachMeHapSapXep.map(me => `
                    <tr class="hover:bg-slate-50 transition-colors border-b border-slate-100">
                        <td class="p-3">
                            <div class="flex items-center gap-2">
                                <span class="font-mono font-black text-rose-700 text-sm tracking-wider">${me.batchCode}</span>
                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "ĐANG CHẠY LÒ" ? "text-purple-700 bg-purple-100 border border-purple-200 animate-pulse" : "text-emerald-700 bg-emerald-100 border border-emerald-200"}">${me.trangThaiMe}</span>
                            </div>
                            <div class="text-[11px] text-slate-600 font-medium mt-1">${me.loaiHap} | ${me.chuKyNhiet} | Áp suất: ${me.apSuat} Bar</div>
                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Bắt đầu: ${me.thoiGian} (${me.ngay})</div>
                        </td>
                        <td class="p-3 text-right pr-4">
                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuongKhay} khay</span>
                        </td>
                    </tr>`).join(''); 
            }
        }
    }
    else if(activeTab === 'khovokhuan') {
        let uniqueKhoaSanCo = [...new Set(listGiaoDich.map(x => x.khoa))].filter(Boolean); const selKhoaXuat = document.getElementById("xuat_selKhoa");
        if(selKhoaXuat) {
            let currentSelected = selKhoaXuat.value; let htmlOpts = '<option value="">-- Chọn Khoa Muốn Trả Đồ --</option>';
            uniqueKhoaSanCo.forEach(k => { htmlOpts += `<option value="${k}" ${k === currentSelected ? 'selected' : ''}>${k}</option>`; });
            selKhoaXuat.innerHTML = htmlOpts;
        }

        let lsXK = listGiaoDich.filter(x => x.status === "CHO_XUAT").sort((a, b) => a.id - b.id);
        renderKhoVoKhuan(lsXK);

        let dsYeuCau = listGiaoDich.filter(x => x.status === "CHO_CAP_PHAT");
        let vungYeuCau = document.getElementById("vungYeuCauCapPhat");
        if (!vungYeuCau) {
            let parentContainer = document.getElementById("tab-khovokhuan");
            if (parentContainer) {
                let divYc = document.createElement("div");
                divYc.id = "vungYeuCauCapPhat";
                divYc.className = "mb-6 bg-amber-50/70 border border-amber-200 rounded-xl p-4 shadow-sm";
                parentContainer.insertBefore(divYc, parentContainer.firstChild);
                vungYeuCau = divYc;
            }
        }
        
        if (vungYeuCau) {
            if (dsYeuCau.length === 0) {
                vungYeuCau.innerHTML = `<h3 class="font-black text-amber-950 text-xs flex items-center gap-1.5"><i class="fa-solid fa-bell-slash"></i> HIỆN KHÔNG CÓ YÊU CẤP PHÁT ĐANG CHỜ</h3>`;
            } else {
                vungYeuCau.innerHTML = `
                    <h3 class="font-black text-amber-900 text-xs mb-2.5 flex items-center gap-1.5 animate-pulse">
                        <i class="fa-solid fa-bell text-rose-500 animate-bounce"></i> CÓ ${dsYeuCau.length} YÊU CẦU CẤP PHÁT TỪ CÁC KHOA LÂM SÀNG
                    </h3>
                    <div class="flex flex-col gap-2 max-h-48 overflow-y-auto">
                        ${dsYeuCau.map(yc => `
                            <div class="bg-white p-3 rounded-lg border border-amber-200 shadow-sm flex justify-between items-center text-xs">
                                <div>
                                    <span class="font-black text-amber-800 uppercase">[KHOA: ${yc.khoa}]</span> 
                                    Yêu cầu mâm: <span class="font-bold text-slate-800">${yc.bo}</span>
                                    <span class="text-[10px] text-slate-400 block mt-0.5">Thời gian: ${yc.time} - Người yêu cầu: ${yc.nvYeuCau || 'ĐD'}</span>
                                </div>
                                <div class="flex items-center gap-2">
                                    <button onclick="duyetCapPhatTậpTrung('${yc.firestoreId}', '${yc.bo}', '${yc.khoa}')" class="bg-amber-600 hover:bg-amber-700 text-white font-black px-3 py-2 rounded-lg text-[10px] uppercase shadow-sm flex items-center gap-1.5 transition-all">
                                        <i class="fa-solid fa-truck-ramp-box"></i>Cấp phát khay
                                    </button>
                                </div>
                            </div>
                        `).join('')}
                    </div>`;
            }
        }
        renderGioHangXuat();
    }
    else if(activeTab === 'quanlykho') {
        let fK = document.getElementById("inv_filterKhoa") ? document.getElementById("inv_filterKhoa").value : ""; 
        let uniqueKhoa = [...new Set(listGiaoDich.map(x=>x.khoa))].filter(Boolean);
        if(document.getElementById("inv_filterKhoa")) { document.getElementById("inv_filterKhoa").innerHTML = '<option value="">-- Tất cả --</option>' + uniqueKhoa.map(k=>`<option value="${k}" ${k===fK?'selected':''}>${k}</option>`).join(''); }
        let uniqueIDs = [...new Set(listGiaoDich.map(x=>x.maMacDinh))]; let arrHtml = []; const ngayHomNay = new Date(); ngayHomNay.setHours(0,0,0,0); 
        uniqueIDs.forEach(ma => {
            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a,b) => b.id - a.id)[0]; if(!currentTrans || !ma) return;
            let viTriCode = currentTrans.status; let khoaGiữ = currentTrans.khoa; if (fK && (khoaGiữ || "").toString().trim().toUpperCase() !== fK.toString().trim().toUpperCase()) return;
            let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";
            if(viTriCode === "HOAN_TAT") { viTriText = `Sẵn sàng tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 
            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã sử dụng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }
            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang vận chuyển`; viTriColor = "bg-purple-100 text-purple-800 animate-pulse"; }
            else if(viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }
            let hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-slate-100 text-slate-500">An toàn</span>`; let dongColorClass = ""; 
            if (currentTrans.hsd && (viTriCode === "CHO_XUAT" || viTriCode === "HOAN_TAT")) {
                const ngayHsd = new Date(currentTrans.hsd); ngayHsd.setHours(0,0,0,0); const soNgayConLai = Math.ceil((ngayHsd.getTime() - ngayHomNay.getTime()) / (1000 * 60 * 60 * 24));
                if (soNgayConLai < 0) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-rose-600 text-white">QUÁ HẠN</span>`; dongColorClass = "bg-rose-50/70 border-l-4 border-l-rose-500 font-bold"; } 
                else if (soNgayConLai <= 3) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-amber-500 text-white">HẠN < ${soNgayConLai} NGÀY</span>`; dongColorClass = "bg-amber-50/40 border-l-4 border-l-amber-500"; } 
                else { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-emerald-100 text-emerald-800">Hạn ${soNgayConLai} ngày</span>`; }
            }
            arrHtml.push(`<tr class="border-b border-slate-100 ${dongColorClass}"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-slate-500 text-[11px]">${khoaGiữ}</td><td class="p-3 text-center"><span class="px-2.5 py-1 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-mono font-bold text-slate-400">${currentTrans.batchCode || 'N/A'}</td><td class="p-3 text-center">${hsdBadget}</td></tr>`);
        });
        if(document.getElementById("bangTonKhoTe")) document.getElementById("bangTonKhoTe").innerHTML = arrHtml.join('');
    }
    else if(activeTab === 'danhmuc') {
        const tbody = document.getElementById("bangDanhMucTong"); if (!tbody) return;
        let uniqueIDs = [...new Set(listGiaoDich.map(x => x.maMacDinh))].filter(Boolean); let arrHtml = [];
        uniqueIDs.forEach(ma => {
            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a, b) => b.id - a.id)[0]; if (!currentTrans) return;
            let viTriCode = currentTrans.status; let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";
            if (viTriCode === "HOAN_TAT") { viTriText = `Tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 
            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã dùng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }
            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang đi đường`; viTriColor = "bg-purple-100 text-purple-800"; }
            else if (viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }
            let chuKyLo = listGiaoDich.filter(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG" || x.status === "ĐANG_VAN_CHUYEN")).length;
            arrHtml.push(`<tr class="border-b border-slate-100 font-medium"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-center"><span class="px-2.5 py-0.5 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-black text-amber-700 bg-amber-50/50">${chuKyLo} lần lễ</td></tr>`);
        });
        tbody.innerHTML = arrHtml.join('');
    }
    else if(activeTab === 'lichsuluanchuyen') {
        const tbody = document.getElementById("bangLichSuHanhTrinhGoc");
        if(tbody) {
            tbody.innerHTML = listGiaoDich.map(x => {
                let statusBadge = `<span class="px-2 py-0.5 bg-slate-100 text-slate-700 font-bold rounded text-[10px]">${x.status}</span>`;
                if(x.status === "HOAN_TAT") statusBadge = `<span class="px-2 py-0.5 bg-emerald-100 text-emerald-800 font-black rounded text-[10px]">ĐÃ BÀN GIAO</span>`;
                else if(x.status === "CHO_XUAT") statusBadge = `<span class="px-2 py-0.5 bg-teal-100 text-teal-800 font-bold rounded text-[10px]">KHO VÔ KHUẨN</span>`;
                else if(x.status === "DANG_HAP") statusBadge = `<span class="px-2 py-0.5 bg-purple-100 text-purple-800 font-bold rounded text-[10px]">ĐANG HẤP LÒ</span>`;
                else if(x.status === "DA_SU_DUNG") statusBadge = `<span class="px-2 py-0.5 bg-rose-600 text-white font-black rounded text-[10px]">SỬ DỤNG BN</span>`;
                else if(x.status === "ĐANG_VAN_CHUYEN") statusBadge = `<span class="px-2 py-0.5 bg-purple-500 text-white font-bold rounded text-[10px] animate-pulse">ĐANG VẬN CHUYỂN</span>`;
                else if(x.status === "DANG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-blue-100 text-blue-700 font-bold rounded text-[10px]">HÀNG ĐỢI RỬA</span>`;
                else if(x.status === "TRONG_BUONG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-cyan-600 text-white font-bold rounded text-[10px]">ĐANG RỬA MÁY</span>`;
                else if(x.status === "CHO_CAP_PHAT") statusBadge = `<span class="px-2 py-0.5 bg-amber-100 text-amber-800 font-bold rounded text-[10px] border border-amber-200">CHỜ CẤP PHÁT</span>`;
                
                let fontColor = ""; let loGoc = listGiaoDich.find(m => m.batchCode === x.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 
                let anhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || x.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;
                if (anhBase64) fontColor = `<br><span onclick="handleViewAnhBiMoi('${x.firestoreId}')" class="text-[10px] text-emerald-600 bg-emerald-50 border border-emerald-300 rounded px-1 cursor-pointer font-bold mt-1 inline-block"><i class="fa-solid fa-image mr-1"></i>Xem ảnh BI</span>`;
                
                let kpiHtml = x.ketQuaGiamSatKpi ? `<br><span class="text-[9px] px-1 rounded font-black border mt-1 inline-block ${x.ketQuaGiamSatKpi === "ĐẠT" ? "text-emerald-700 bg-emerald-50 border-emerald-200" : "text-rose-700 bg-rose-50 border-rose-200"}">KPI: ${x.ketQuaGiamSatKpi} (${x.thoiGianTinhKpiPhut}m)</span>` : "";
                let thongTinKemTheo = x.nvXuatKho || '<span class="text-slate-300 font-normal">Chưa xuất</span>';
                if (x.nguoiKyNhanKhoa) thongTinKemTheo += ` ➔ <span class="text-emerald-700 font-semibold">[ĐD: ${x.nguoiKyNhanKhoa}]</span>`;
                if (x.status === "DA_SU_DUNG" && x.thongTinBenhNhan) thongTinKemTheo = `<span class="text-teal-700 font-black">BN: ${x.thongTinBenhNhan.thongTinTimKiemBN}</span>`;

                return `<tr class="border-b text-xs hover:bg-slate-50 transition-colors"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3 text-center">${statusBadge}${kpiHtml}</td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/30">${x.batchCode || 'N/A'}${fontColor}</td><td class="p-3 text-center font-bold text-sky-800 bg-sky-50/40">${thongTinKemTheo}</td><td class="p-3 text-center text-slate-400 font-mono text-[11px]">${x.ngayTao || ''} ${x.time || ''}</td></tr>`;
            }).join('');
        }
    }
    else if(activeTab === 'tracuu') {
        let safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");
        if (!safeTbody) {
            let parentContainer = document.getElementById("tab-tracuu") || document.querySelector('.bg-white.p-6.rounded-xl.shadow-md');
            if(parentContainer) {
                let existingTableZone = document.getElementById("vung-ket-qua-tu-dong");
                if(!existingTableZone) {
                    let tableWrapper = document.createElement("div"); tableWrapper.id = "vung-ket-qua-tu-dong"; tableWrapper.className = "mt-6 bg-white border border-slate-200 rounded-xl p-4 shadow-sm";
                    tableWrapper.innerHTML = `<h3 class="font-black text-slate-800 text-sm mb-3 text-sky-800"><i class="fa-solid fa-list-check mr-2"></i>DANH SÁCH MÂM DỤNG CỤ TRONG MẺ TRUY VẾT</h3><div class="overflow-x-auto"><table class="w-full text-left border-collapse"><thead><tr class="bg-slate-100 text-[11px] font-bold text-slate-600 uppercase border-b"><th class="p-3">Mã ID Khay</th><th class="p-3">Tên Bộ Dụng Cụ</th><th class="p-3">Khoa Sử Dụng</th><th class="p-3">Trạng thái</th><th class="p-3 text-center">Mã Lô Hấp</th><th class="p-3 text-center">Thời Gian Kích Hoạt</th></tr></thead><tbody id="bangLichSuTruyXuatAdmin"></tbody></table></div>`;
                    parentContainer.appendChild(tableWrapper); safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");
                }
            }
        }
        let searchInp = maLoTruyVetToanCuc || ""; if (!searchInp && document.getElementById("inp_searchBatch")) { searchInp = document.getElementById("inp_searchBatch").value.trim(); }
        if(safeTbody) {
            if (!searchInp) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng nhập mã mẻ hấp và nhấn "TRA CỨU KHẨN CẤP"...</td></tr>`; return; }
            let dataFiltered = listGiaoDich.filter(x => x.batchCode && String(x.batchCode).toUpperCase().includes(searchInp.toUpperCase()));
            if(dataFiltered.length === 0) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-rose-500 italic font-bold">Không tìm thấy dữ liệu: ${searchInp}</td></tr>`; } 
            else { safeTbody.innerHTML = dataFiltered.map(x => `<tr class="border-b text-xs hover:bg-slate-50 font-medium"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3"><span class="px-2 py-0.5 rounded text-[10px] ${x.status === "HOAN_TAT" ? "bg-emerald-100 text-emerald-800 font-bold" : "bg-teal-100 text-teal-800"}">${x.status}</span></td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/40">${x.batchCode || 'N/A'}</td><td class="p-3 text-center text-slate-400 font-mono">${x.ngayTao || ''} ${x.time || ''}</td></tr>`).join(''); }
        }
    }
    else if(activeTab === 'performance') { renderKpiPerformanceGoc(); }
    else if(activeTab === 'dashboard_tv') { renderDashboardTiviRealtime(); }
}

function renderKhoVoKhuan(listData) {
    const tbody = document.getElementById("bangKhoVoKhuan");
    if (!tbody) return;

    if (!listData || listData.length === 0) {
        tbody.innerHTML = `<tr><td colspan="6" class="p-6 text-center text-slate-400 italic">Kho vô khuẩn hiện tại trống.</td></tr>`;
        return;
    }

    tbody.innerHTML = listData.map(item => `
        <tr class="hover:bg-slate-50 transition-colors">
            <td class="p-3.5 font-bold text-slate-800 text-xs">${item.bo ? String(item.bo).split(" [ID:")[0] : 'N/A'}</td>
            <td class="p-3.5 font-mono font-black text-sky-700 text-xs">${item.maMacDinh || '--'}</td>
            <td class="p-3.5 text-center font-semibold text-slate-600 text-xs">${item.khoa || 'Tập trung'}</td>
            <td class="p-3.5 text-center"><span class="bg-emerald-100 text-emerald-800 font-bold px-2.5 py-1 rounded-full text-[11px]">${item.viTriKe || 'Kệ A1'}</span></td>
            <td class="p-3.5 text-center font-mono font-bold text-xs text-rose-600">${item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN') : 'An toàn'}</td>
            <td class="p-2 text-center">
                <button onclick="xuatKhoXoayVong('${item.maMacDinh}')" 
                        class="min-h-[44px] px-4 bg-emerald-600 active:bg-emerald-700 text-white font-bold rounded-lg text-xs shadow-sm inline-flex items-center gap-1.5 transition-transform active:scale-95">
                    <i class="fa-solid fa-cart-flatbed"></i>
                    <span>Xuất Khay</span>
                </button>
            </td>
        </tr>
    `).join('');
}

// =========================================================================
// VÁ LỖI CÁC HÀM TRUY VẾT & ĐỒNG BỘ HIỆU SUẤT TV MÀN HÌNH
// =========================================================================
function truyVetTheoMaBatch() {
    const inp = document.getElementById("inp_searchBatch");
    if (inp) {
        maLoTruyVetToanCuc = inp.value.trim();
        playSound('success');
        callRender();
    }
}

function clearTruyVetBatch() {
    const inp = document.getElementById("inp_searchBatch");
    if (inp) inp.value = "";
    maLoTruyVetToanCuc = "";
    callRender();
}

function resetDuLieuKet() {
    if (confirm("Anh Hùng có chắc chắn muốn giải phóng các mâm đang xử lý dở dang (chưa Hoàn tất / Chưa dùng) về trạng thái Chờ thu gom?")) {
        let p = [];
        listGiaoDich.forEach(x => {
            if (x.status !== "HOAN_TAT" && x.status !== "DA_SU_DUNG") {
                p.push(db.collection("phieuGiaoNhan").doc(x.firestoreId).update({ status: "CHO_THU" }));
            }
        });
        Promise.all(p).then(() => {
            playSound('success');
            showToast("Đã giải phóng dữ liệu mâm kẹt thành công!", "success");
            callRender();
        });
    }
}

function renderKpiPerformanceGoc() {
    const tbody = document.getElementById("bangHieuSuatKTV");
    if (!tbody) return;
    let kpiGop = {};
    listGiaoDich.forEach(x => {
        let ktv = x.ktvThuGom || x.nvXuatKho || "CSSD_CHUNG";
        if (!kpiGop[ktv]) kpiGop[ktv] = { code: ktv, ten: ktv, dat: 0, tong: 0 };
        if (x.ketQuaGiamSatKpi === "ĐẠT") kpiGop[ktv].dat += 1;
        if (x.ketQuaGiamSatKpi) kpiGop[ktv].tong += 1;
    });
    let sortedKpi = Object.values(kpiGop).sort((a,b) => b.dat - a.dat);
    if(sortedKpi.length === 0) {
        tbody.innerHTML = `<tr><td colspan="4" class="p-4 text-center italic text-slate-400">Hôm nay chưa có dữ liệu mẻ KPI nào.</td></tr>`;
    } else {
        tbody.innerHTML = sortedKpi.map((item, idx) => `
            <tr class="border-b text-xs hover:bg-slate-50 font-medium">
                <td class="p-3 text-center font-bold">${idx + 1}</td>
                <td class="p-3 font-mono font-bold text-sky-700">${item.code}</td>
                <td class="p-3 font-bold text-slate-800">${item.ten}</td>
                <td class="p-3 text-center"><span class="bg-emerald-100 text-emerald-800 px-2.5 py-1 rounded font-black">${item.dat}/${item.tong} Mẻ Đạt KPI</span></td>
            </tr>
        `).join('');
    }
}

function renderDashboardTiviRealtime() {
    let meRuaInDay = listGiaoDich.filter(x => x.rua_batchCode).length;
    let meHapInDay = listGiaoDich.filter(x => x.batchCode).length;
    let dangRua = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA").length;
    let dangHap = listGiaoDich.filter(x => x.status === "DANG_HAP").length;
    let sanSangXuat = listGiaoDich.filter(x => x.status === "CHO_XUAT").length;
    
    const today = new Date(); today.setHours(0,0,0,0);
    let quaHan = 0;
    listGiaoDich.forEach(x => {
        if (x.hsd && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT")) {
            if (new Date(x.hsd) < today) quaHan++;
        }
    });

    if (document.getElementById('tv_meRua')) document.getElementById('tv_meRua').innerText = meRuaInDay;
    if (document.getElementById('tv_meHap')) document.getElementById('tv_meHap').innerText = meHapInDay;
    if (document.getElementById('tv_meHap2')) document.getElementById('tv_meHap2').innerText = meHapInDay;
    if (document.getElementById('tv_dangRua')) document.getElementById('tv_dangRua').innerText = dangRua;
    if (document.getElementById('tv_dangHap')) document.getElementById('tv_dangHap').innerText = dangHap;
    if (document.getElementById('tv_khoVoKhuan')) document.getElementById('tv_khoVoKhuan').innerText = sanSangXuat;
    if (document.getElementById('tv_canhBaoHsd')) document.getElementById('tv_canhBaoHsd').innerText = quaHan;
}

function renderAdminInterface() {
    if (document.getElementById("cfg_pinAdmin")) document.getElementById("cfg_pinAdmin").value = thongTinMatKhauAdmin.adminPIN || "";
    if (document.getElementById("cfg_pinCSSD")) document.getElementById("cfg_pinCSSD").value = thongTinMatKhauAdmin.cssdPIN || "";
    if (document.getElementById("cfg_pinGuest")) document.getElementById("cfg_pinGuest").value = thongTinMatKhauAdmin.guestPIN || "";
    const MatrixRoles = ["CSSD", "KHOA", "GUEST"];
    const MatrixTabs = [
        { id: "khoaphong", name: "1. Cổng Báo Trả Đồ" }, { id: "thugom", name: "2. Xe Thu Gom" }, { id: "mayrua", name: "3. Quản Lý Mẻ Rửa" }, { id: "donggoi", name: "4. Làm Sạch & Gói" }, { id: "mayhap", name: "5. Quản Lý Mẻ Hấp" }, { id: "khovokhuan", name: "6. Kho Vô Khuẩn" }, { id: "quanlykho", name: "7. Tồn Kho Toàn Viện" }, { id: "danhmuc", name: "8. Giám Sát Tuổi Thọ" }, { id: "lichsuluanchuyen", name: "9. Nhật Ký Luân Chuyển" }, { id: "tracuu", name: "10. Cấu Hình Hệ Thống" }, { id: "performance", name: "11. Hiệu Suất KPI" }, { id: "dashboard_tv", name: "12. Màn Hình Tivi" }
    ];
    let htmlMatrix = "";
    MatrixTabs.forEach(tab => {
        htmlMatrix += `<tr class="border-b font-medium text-xs hover:bg-slate-50"><td class="p-3 font-bold text-slate-700">${tab.name}</td>`;
        MatrixRoles.forEach(role => {
            let isChecked = (cauHinhGiaoDien[role] && cauHinhGiaoDien[role].includes(tab.id)) ? "checked" : "";
            htmlMatrix += `<td class="p-3 text-center"><input type="checkbox" ${isChecked} onchange="updateGiaoDienMatrix('${role}', '${tab.id}', this)" class="w-4 h-4 rounded text-sky-600 focus:ring-sky-500"></td>`;
        });
        htmlMatrix += `</tr>`;
    });
    const tbodyMatrix = document.getElementById("bodyMaTranGiaoDien"); if (tbodyMatrix) tbodyMatrix.innerHTML = htmlMatrix;
    const trKhoa = document.getElementById("bangPhanQuyenKhoa");
    if(trKhoa) {
        if(danhSachKhoa.length === 0) { trKhoa.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-rose-500 font-bold italic">Danh sách Khoa trống.</td></tr>`; } 
        else { trKhoa.innerHTML = danhSachKhoa.map((k, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-slate-700 text-[11px]">${k.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-rose-600 text-sm bg-rose-50/50">${k.pin || '123'}</td><td class="p-3 text-center"><div class="flex items-center justify-center gap-2"><input type="text" id="pin-khoa-${index}" placeholder="PIN" class="w-24 p-1.5 text-center border border-slate-300 rounded text-xs font-bold"><button onclick="updatePINTrựcTiep(${index}, '${k.ten}')" class="bg-sky-600 text-white font-bold py-1.5 px-3 rounded shadow text-[10px]">ĐỔI PIN</button></div></td></tr>`).join(''); }
    }
    const tbKtv = document.getElementById("bangNhanVienCssd");
    if(tbKtv) {
        if(danhSachKtvCssd.length === 0) { tbKtv.innerHTML = `<tr><td colspan="4" class="p-4 text-center text-slate-400 italic">Chưa có Nhân viên.</td></tr>`; }
        else { tbKtv.innerHTML = danhSachKtvCssd.map((nv, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-sky-700 text-xs">${nv.code}</td><td class="p-3 font-bold text-slate-700 text-[11px]">${nv.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-sky-600 text-sm bg-sky-50/50">${nv.pin}</td><td class="p-3 text-center"><button onclick="xaoKtvCssd('${nv.code}')" class="text-rose-600 font-bold text-[10px]"><i class="fa-solid fa-trash-can mr-1"></i>XÓA</button></td></tr>`).join(''); }
    }
}

// =========================================================================
// 14. XỬ LÝ ĐỌC FILE EXCEL ĐA SHEET
// =========================================================================
document.addEventListener("DOMContentLoaded", () => {
    const fileInput = document.getElementById("excelFileInput");
    if (fileInput) {
        fileInput.addEventListener("change", (e) => {
            const file = e.target.files[0];
            if (!file) return;

            const reader = new FileReader();
            reader.onload = function(e) {
                try {
                    const data = new Uint8Array(e.target.result);
                    const workbook = XLSX.read(data, { type: 'array' });
                    
                    let updateData = {};
                    let logs = [];

                    workbook.SheetNames.forEach(sheetName => {
                        const worksheet = workbook.Sheets[sheetName];
                        const jsonData = XLSX.utils.sheet_to_json(worksheet);
                        if (jsonData.length === 0) return;

                        const firstRow = jsonData[0];
                        const keys = Object.keys(firstRow).map(k => k.trim().toUpperCase());

                        const coCotMaDC = keys.includes("MÃ DC");
                        if (coCotMaDC) {
                            let mapKhoa = {};
                            danhSachKhoa.forEach(k => {
                                mapKhoa[k.ten.trim().toUpperCase()] = {
                                    ten: k.ten.trim().toUpperCase(),
                                    pin: k.pin || "123",
                                    danhSachBo: []
                                };
                            });

                            jsonData.forEach(row => {
                                let rawKeyKhoa = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS (I)");
                                let rawKeyBo = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS CHUẨN");
                                let rawKeyMa = Object.keys(row).find(k => k.trim().toUpperCase() === "MÃ DC");

                                let tenKhoa = rawKeyKhoa ? row[rawKeyKhoa] : null;
                                let tenBo = rawKeyBo ? row[rawKeyBo] : null;
                                let maKhay = rawKeyMa ? row[rawKeyMa] : null;

                                if (tenKhoa && tenBo) {
                                    let keyKhoa = String(tenKhoa).trim().toUpperCase();
                                    let cleanBo = String(tenBo).trim().toUpperCase();
                                    let cleanMa = maKhay ? String(maKhay).trim().toUpperCase() : "CHUA_CO_ID";
                                    let chuoiDinhDangKhay = `${cleanBo} [ID:${cleanMa}]`;

                                    if (!mapKhoa[keyKhoa]) {
                                        mapKhoa[keyKhoa] = { ten: keyKhoa, pin: "123", danhSachBo: [] };
                                    }
                                    if (!mapKhoa[keyKhoa].danhSachBo.includes(chuoiDinhDangKhay)) {
                                        mapKhoa[keyKhoa].danhSachBo.push(chuoiDinhDangKhay);
                                    }
                                }
                            });

                            updateData.danhSachKhoa = Object.values(mapKhoa);
                            logs.push(`Cập nhật định biên cơ số cho ${updateData.danhSachKhoa.length} Khoa/Phòng`);
                        }

                        const coCotMaAesculap = keys.includes("AB 120/12");
                        if (coCotMaAesculap) {
                            updateData.databaseExcel = jsonData;
                            logs.push(`Nạp thành công ${jsonData.length} linh kiện chi tiết mâm Aesculap`);
                        }
                    });

                    if (Object.keys(updateData).length === 0) {
                        return showToast("Không tìm thấy cấu trúc dữ liệu phù hợp (Cột 'MÃ DC' hoặc 'AB 120/12') trong file Excel up lên!", "error");
                    }

                    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update(updateData)
                    .then(() => {
                        playSound('success');
                        showToast(`Đồng bộ thành công: ${logs.join(" | ")}!`, "success");
                        fileInput.value = ""; 
                        callRender();
                    }).catch(err => {
                        console.error("Lỗi cập nhật Firestore:", err);
                        showToast("Thất bại khi đẩy dữ liệu lên Cloud Firestore!", "error");
                    });

                } catch (err) {
                    console.error("Lỗi FileReader:", err);
                    showToast("Định dạng file không được hỗ trợ hoặc bị lỗi!", "error");
                }
            };
            reader.readAsArrayBuffer(file);
        });
    }
});
tôi gửi bạn code index.html <!DOCTYPE html>

<html lang="vi">

<head>

    <meta charset="UTF-8">

    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <title>Hệ Thống CSSD - V29.0 (Giao Diện Chuẩn Phương Nam)</title>

    <script src="https://cdn.tailwindcss.com"></script>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>

    <script src="https://cdn.jsdelivr.net/npm/jsbarcode@3.11.5/dist/JsBarcode.all.min.js"></script>

    <script src="https://unpkg.com/html5-qrcode"></script>

    <script src="https://www.gstatic.com/firebasejs/10.12.2/firebase-app-compat.js"></script>

    <script src="https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore-compat.js"></script>



    <link rel="stylesheet" href="style.css">

    <style>

        /* Cấu hình nâng cao cho vùng in tem nhiệt Bixolon SLP-TX403 (80mm x 50mm) */

        @media print {

            body * {

                visibility: hidden !important;

            }

            #print-zone, #print-zone * {

                visibility: visible !important;

            }

            #print-zone {

                position: absolute !important;

                left: 0 !important;

                top: 0 !important;

                width: 80mm !important;

                margin: 0 !important;

                padding: 0 !important;

            }

            @page {

                size: 80mm 50mm;

                margin: 0mm;

            }

            .bixolon-label {

                width: 80mm !important;

                height: 50mm !important;

                padding: 3mm 4mm !important;

                box-sizing: border-box !important;

                page-break-after: always;

                display: flex !important;

                flex-direction: column !important;

                justify-content: space-between !important;

                font-family: Arial, sans-serif !important;

                background: #fff !important;

                color: #000 !important;

            }

        }

    </style>

</head>

<body class="flex h-screen overflow-hidden relative">



    <div id="mobile-overlay" class="fixed inset-0 bg-slate-900/50 z-40 hidden md:hidden" onclick="toggleMobileMenu()"></div>



    <div id="login-screen" class="fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-100 bg-opacity-95 backdrop-blur-sm">

        <div class="glass-panel w-full max-w-sm p-8 text-center shadow-xl border-t-4 border-t-sky-600">

            <div class="mb-6">

                <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-sky-50 border border-sky-100 mb-4"><i class="fa-solid fa-hospital text-3xl text-sky-600"></i></div>

                <h1 class="text-xl font-black text-slate-800 uppercase tracking-widest">PHUONG NAM</h1>

                <p class="text-xs text-slate-500 font-bold tracking-widest mt-1">CSSD PLATFORM</p>

            </div>

            <div class="space-y-4 text-left">

                <div>

                    <label class="text-[10px] font-black text-slate-500 uppercase tracking-wide">Vai trò đăng nhập:</label>

                    <select id="login_role" onchange="toggleLoginFields()" class="w-full px-3 py-2.5 mt-1 font-bold shadow-sm">

                        <option value="KHOA">Điều Dưỡng Lâm Sàng</option>

                        <option value="CSSD">Chuyên Viên CSSD (User Riêng)</option>

                        <option value="ADMIN">Quản Trị Hệ Thống</option>

                        <option value="GUEST" class="text-emerald-600 font-black">Khách Tham Quan (View-Only)</option>

                    </select>

                </div>

                <div id="field_khoa">

                    <label class="text-[10px] font-black text-slate-500 uppercase tracking-wide">Khoa phòng:</label>

                    <select id="login_khoa" class="w-full px-3 py-2.5 mt-1 font-bold shadow-sm"><option value="">-- Đang tải dữ liệu... --</option></select>

                </div>

                <div id="field_nhanvien_cssd" class="hidden">

                    <label class="text-[10px] font-black text-slate-500 uppercase tracking-wide">Chọn Nhân Viên Vận Hành:</label>

                    <select id="login_nv_cssd" class="w-full px-3 py-2.5 mt-1 font-bold shadow-sm text-sky-700 bg-sky-50"><option value="">-- Đang tải danh sách KTV... --</option></select>

                </div>

                <div>

                    <label class="text-[10px] font-black text-slate-500 uppercase tracking-wide">Mã PIN xác thực:</label>

                    <input type="password" id="login_pass" class="w-full px-3 py-2.5 mt-1 text-center tracking-widest shadow-sm" placeholder="••••" onkeypress="if(event.key==='Enter') checkLogin()">

                </div>

                <button onclick="checkLogin()" class="w-full btn-primary font-black py-3 rounded-lg mt-4 shadow-md"><i class="fa-solid fa-shield-halved mr-2"></i> TRUY CẬP</button>

            </div>

        </div>

    </div>



    <div id="main-app" class="hidden w-full h-full flex flex-row">

        <div id="sidebar_menu" class="w-64 bg-white flex flex-col justify-between flex-shrink-0 z-50 shadow-lg border-r border-slate-200 fixed inset-y-0 left-0 transform -translate-x-full md:relative md:translate-x-0 transition-transform duration-300">

            <div class="overflow-y-auto flex-1 scrollbar-none">

                <div class="p-4 flex items-center justify-between border-b border-slate-100 bg-white sticky top-0 z-10"><div class="flex flex-col"><span class="text-sm text-sky-700 font-black tracking-widest">PN HOSPITAL</span><span id="nav_user_info" class="text-[10px] text-slate-500 font-bold mt-0.5">Role: Đang tải...</span></div><button onclick="toggleMobileMenu()" class="md:hidden text-slate-400 hover:text-slate-700 text-xl"><i class="fa-solid fa-bars"></i></button></div>

                

                <div id="header-lamsang" class="px-4 py-3 text-[10px] uppercase text-slate-400 font-bold tracking-wider">Lâm Sàng</div>

                <nav class="flex flex-col px-2 space-y-0.5">

                    <a href="#" onclick="switchTab('khoaphong'); toggleMobileMenu();" id="menu-khoaphong" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition"><i class="fa-solid fa-laptop-medical w-4 text-center"></i> <span>1. Cổng Lâm Sàng</span></a>

                    <a href="#" onclick="moPopupSuDungBoDungCu(); toggleMobileMenu();" id="menu-sudung-bn" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-bold text-teal-700 hover:bg-teal-50 transition"><i class="fa-solid fa-user-injured w-4 text-center text-teal-600"></i> <span>1.1 Ghi Nhận Sử Dụng (BN)</span></a>

                </nav>

                

                <div id="header-vanhanh" class="px-4 py-3 text-[10px] uppercase text-slate-400 font-bold tracking-wider">Vận Hành Tiệt Khuẩn</div>

                <nav class="flex flex-col px-2 space-y-0.5">

                    <a href="#" onclick="switchTab('thugom'); toggleMobileMenu();" id="menu-thugom" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition"><i class="fa-solid fa-truck-medical w-4 text-center"></i> <span>2. Điều Phối Xe Thu Gom</span></a>

                    <a href="#" onclick="switchTab('mayrua'); toggleMobileMenu();" id="menu-mayrua" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition"><i class="fa-solid fa-soap w-4 text-center text-sky-600"></i> <span class="font-bold text-slate-700">3. Quản Lý Mẻ Rửa</span></a>

                    <a href="#" onclick="switchTab('donggoi'); toggleMobileMenu();" id="menu-donggoi" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition"><i class="fa-solid fa-hands-bubbles w-4 text-center"></i> <span>4. Trạm Làm Sạch & Gói</span></a>

                    <a href="#" onclick="switchTab('mayhap'); toggleMobileMenu();" id="menu-mayhap" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition"><i class="fa-solid fa-server w-4 text-center"></i> <span>5. Quản Lý Mẻ Hấp</span></a>

                    <a href="#" onclick="switchTab('khovokhuan'); toggleMobileMenu();" id="menu-khovokhuan" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition"><i class="fa-solid fa-door-closed w-4 text-center"></i> <span>6. Kho Vô Khuẩn (Xuất)</span></a>

                </nav>

                

                <div id="header-dulieu" class="px-4 py-3 text-[10px] uppercase text-slate-400 font-bold tracking-wider border-t border-slate-100 mt-2">Dữ Liệu & Tồn Kho</div>

                <nav class="flex flex-col px-2 space-y-0.5">

                    <a href="#" onclick="switchTab('quanlykho'); toggleMobileMenu();" id="menu-quanlykho" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-bold text-slate-700 transition hover:bg-slate-50 bg-amber-50/50"><i class="fa-solid fa-boxes-stacked w-4 text-center text-amber-600"></i> <span class="text-amber-900">7. Tồn Kho Dụng Cụ</span></a>

                    <a href="#" onclick="switchTab('danhmuc'); toggleMobileMenu();" id="menu-danhmuc" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition hover:bg-slate-50"><i class="fa-solid fa-microchip w-4 text-center"></i> <span>8. Giám Sát Tuổi Thọ</span></a>

                    <a href="#" onclick="switchTab('lichsuluanchuyen'); toggleMobileMenu();" id="menu-lichsuluanchuyen" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-bold text-slate-600 transition hover:bg-slate-50"><i class="fa-solid fa-history w-4 text-center text-sky-600"></i> <span class="text-sky-900">9. Nhật Ký Luân Chuyển</span></a>

                    <a href="#" onclick="switchTab('tracuu'); toggleMobileMenu();" id="menu-tracuu" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition hover:bg-slate-50"><i class="fa-solid fa-gears w-4 text-center"></i> <span>10. Cấu Hình Hệ Thống</span></a>

                    <a href="#" onclick="switchTab('performance'); toggleMobileMenu();" id="menu-performance" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition hover:bg-slate-50"><i class="fa-solid fa-chart-line w-4 text-center text-emerald-600"></i> <span class="font-bold text-slate-700">11. Hiệu Suất KPI</span></a>

                    <a href="#" onclick="switchTab('dashboard_tv'); toggleMobileMenu();" id="menu-dashboard_tv" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-bold text-slate-600 transition hover:bg-slate-50 border-t border-slate-100 hidden"><i class="fa-solid fa-tv w-4 text-center text-rose-600"></i> <span class="text-rose-900">12. MÀN HÌNH TRỰC TIVI</span></a>

                </nav>

            </div>

            <div class="p-4 border-t border-slate-100 bg-slate-50 shrink-0"><button onclick="location.reload()" class="w-full text-xs text-slate-500 hover:text-slate-800 bg-white border border-slate-200 py-2 rounded font-bold hover:shadow-sm"><i class="fa-solid fa-arrow-right-from-bracket mr-2"></i>Đăng xuất</button></div>

        </div>



        <div class="flex-1 flex flex-col overflow-y-auto w-full h-full relative bg-slate-100 pb-10" id="mainContentScroll">

            <header class="bg-white px-6 py-3.5 flex justify-between items-center z-10 sticky top-0 border-b border-slate-200 shadow-sm"><div class="flex items-center"><button onclick="toggleMobileMenu()" class="md:hidden text-slate-500 hover:text-sky-600 text-xl mr-4"><i class="fa-solid fa-bars"></i></button><h2 id="header-title" class="text-sm font-black text-slate-700 uppercase tracking-widest">Hệ Thống Quản Lý CSSD</h2></div><div id="clock" class="text-[11px] font-mono text-slate-500 font-bold hidden sm:block"></div></header>



            <main class="p-4 md:p-6 w-full space-y-6 flex-1 z-10 max-w-7xl mx-auto">

                

                <!-- ========================================== -->

                <!-- TRẠM 1: CỔNG LÂM SÀNG -->

                <!-- ========================================== -->

                <div id="tab-khoaphong" class="hidden flex flex-col gap-6 animate-fade-in">

                    

                    <div class="glass-panel p-4 bg-white shadow-sm border-l-4 border-l-sky-600 flex flex-col md:flex-row md:items-center justify-between gap-4">

                        <div class="w-full md:w-1/3">

                            <label class="block text-[10px] font-black text-slate-500 mb-1 uppercase tracking-wider">Đơn Vị Chỉ Định Hiện Tại:</label>

                            <select id="khoa_selKhoa" onchange="loadBoDungCuTheoKhoa(); clearGioHang(); callRender();" class="w-full px-3 py-2 text-xs font-black shadow-sm bg-slate-50 border border-slate-300 rounded-lg text-sky-800">

                                <option value="">-- Đang tải dữ liệu khoa phòng... --</option>

                            </select>

                        </div>



                        <div class="flex bg-slate-100 p-1 rounded-xl border border-slate-200 w-full md:w-auto shrink-0 shadow-inner">

                            <button onclick="switchLamsangSubtab('lssub-tab-tra-ban')" id="btn-lssub-tra-ban" class="px-4 py-2 text-xs font-black rounded-lg transition-all flex items-center gap-1.5 bg-white text-rose-700 shadow-sm">

                                <i class="fa-solid fa-box-tissue"></i> 1. BÁO TRẢ ĐỒ BẨN

                            </button>

                            <button onclick="switchLamsangSubtab('lssub-tab-nhan-sach')" id="btn-lssub-nhan-sach" class="px-4 py-2 text-xs font-black rounded-lg transition-all text-slate-600 hover:text-slate-900 flex items-center gap-1.5">

                                <i class="fa-solid fa-square-envelope"></i> 2. YÊU CẦU & NHẬN SẠCH

                            </button>

                            <button onclick="switchLamsangSubtab('lssub-tab-doi-luu')" id="btn-lssub-doi-luu" class="px-4 py-2 text-xs font-black rounded-lg transition-all text-slate-600 hover:text-slate-900 flex items-center gap-1.5">

                                <i class="fa-solid fa-file-invoice"></i> 3. SỔ GIAO NHẬN

                            </button>

                        </div>

                    </div>



                    <div id="lssub-tab-tra-ban" class="grid grid-cols-1 md:grid-cols-3 gap-6 transition-all duration-200">

                        <div class="glass-panel p-5 border-t-4 border-t-rose-500 bg-white shadow-sm md:col-span-1 flex flex-col justify-between min-h-[350px]">

                            <div class="border-b border-slate-100 pb-3">

                                <h3 class="text-sm font-black text-rose-800 uppercase flex items-center gap-2">

                                    <i class="fa-solid fa-box-tissue"></i> 1. Khai Báo Mâm Bẩn

                                </h3>

                                <p class="text-[10px] text-slate-400 font-medium mt-0.5">Quét hoặc chọn các khay mâm bẩn vừa dùng xong cần hoàn trả</p>

                            </div>

                            <div class="space-y-4 my-4 flex-1">

                                <div class="bg-rose-50/50 p-4 rounded-xl border border-rose-100 space-y-3 shadow-inner">

                                    <label class="block text-[10px] uppercase font-black text-rose-900"><i class="fa-solid fa-qrcode mr-1"></i> Chọn khay dụng cụ bẩn:</label>

                                    <div class="flex gap-2">

                                        <input type="text" id="khoa_inpMaBo" list="listBoDungCu" placeholder="Gõ tên mâm bẩn hoặc quét mã..." class="w-full px-3 py-2 text-xs font-bold text-rose-950 shadow-sm border border-rose-300 rounded-md bg-white focus:ring-2 focus:ring-rose-500 outline-none" onkeypress="if(event.key==='Enter') themVaoGio()">

                                        <datalist id="listBoDungCu"></datalist>

                                        <button onclick="moCamera('khoa_inpMaBo')" class="bg-rose-600 hover:bg-rose-700 text-white px-3.5 py-2 rounded-md shadow font-bold text-xs shrink-0"><i class="fa-solid fa-camera"></i></button>

                                    </div>

                                    <button onclick="themVaoGio()" class="w-full bg-slate-800 hover:bg-slate-700 text-white font-black py-2.5 rounded-lg text-xs shadow uppercase tracking-wider"><i class="fa-solid fa-cart-plus mr-1"></i> Đưa Vào Danh Sách Trả</button>

                                </div>

                            </div>

                        </div>



                        <div class="glass-panel p-5 border-t-4 border-t-slate-400 bg-white shadow-sm md:col-span-2 flex flex-col min-h-[350px]">

                            <div class="flex justify-between items-center border-b border-slate-100 pb-3 mb-4">

                                <h4 class="text-xs font-black text-slate-700 uppercase"><i class="fa-solid fa-basket-shopping text-rose-500 mr-1.5"></i> Danh sách mâm bẩn đang chờ phát lệnh:</h4>

                                <span id="badgeGioHang" class="bg-rose-100 text-rose-700 text-[10px] px-2.5 py-0.5 rounded-full font-black">0 món</span>

                            </div>

                            <div class="flex-1 overflow-auto rounded-lg border border-slate-200 bg-white min-h-[150px] mb-4">

                                <table class="w-full text-left text-xs">

                                    <tbody id="bangGioHang" class="divide-y divide-slate-100 text-slate-700 font-medium"></tbody>

                                </table>

                            </div>

                            <button id="btn-XacNhanGoi" onclick="khoaGuiPhieuTraBatches()" class="w-full bg-rose-600 hover:bg-rose-700 text-white font-black py-3 rounded-xl text-xs shadow-md transition-all uppercase tracking-wider"><i class="fa-solid fa-bell mr-1.5"></i> PHÁT LỆNH THU GOM ĐỒ BẨN</button>

                        </div>

                    </div>



                    <div id="lssub-tab-nhan-sach" class="grid grid-cols-1 lg:grid-cols-3 gap-6 transition-all duration-200 hidden">

                        <div class="glass-panel p-5 flex flex-col border-t-4 border-t-emerald-500 bg-white shadow-sm h-[380px]">

                            <div class="border-b border-slate-100 pb-3 mb-4">

                                <h3 class="text-sm font-black text-emerald-800 uppercase flex items-center gap-2">

                                    <i class="fa-solid fa-cart-flatbed-suitcases"></i> Đăng Ký Đồ Sạch Mới

                                </h3>

                                <p class="text-[10px] text-slate-400 font-medium mt-0.5">Dành cho đăng ký mượn mới hoặc cấp bù mâm vô khuẩn độc lập</p>

                            </div>

                            <div class="space-y-4 flex-1 flex flex-col justify-between">

                                <div class="bg-emerald-50/50 p-4 rounded-xl border border-emerald-100 space-y-3 shadow-inner">

                                    <label class="block text-[10px] uppercase font-black text-emerald-900">Chọn loại mâm cần cấp mới:</label>

                                    <input type="text" id="khoa_inpMaBo" list="listBoDungCu" placeholder="Chọn mâm từ danh mục chuẩn..." class="w-full px-3 py-2.5 text-xs font-bold text-emerald-900 shadow-sm border border-emerald-300 rounded-md bg-white focus:ring-2 focus:ring-emerald-500 outline-none">

                                    <button onclick="khoaGuiYeuCauCapPhat()" class="w-full bg-emerald-600 hover:bg-emerald-700 text-white font-black py-3 rounded-lg text-xs shadow transition-all uppercase tracking-wider"><i class="fa-solid fa-paper-plane mr-1"></i> GỬI YÊU CẦU CẤP MỚI</button>

                                </div>

                                <div class="p-3 bg-slate-50 border border-slate-200 rounded-lg text-[10.5px] text-slate-500 leading-relaxed font-bold">

                                    <i class="fa-solid fa-info-circle text-emerald-600 mr-1"></i> Yêu cầu cấp mới sau khi gửi sẽ được treo thẳng lên màn hình giám sát tại <strong class="text-emerald-700">Trạm 6 (Kho Vô Khuẩn)</strong> để CSSD đóng bàn giao.

                                </div>

                            </div>

                        </div>



                        <div class="glass-panel p-5 flex flex-col border-t-4 border-t-amber-500 bg-white shadow-sm h-[380px]">

                            <div class="border-b border-slate-100 pb-2.5 mb-3">

                                <h3 class="text-xs font-black text-slate-700 uppercase flex items-center gap-1.5"><i class="fa-solid fa-hourglass-half text-amber-500"></i> Các yêu cầu đang chờ cấp phát</h3>

                            </div>

                            <div class="flex-1 overflow-auto rounded-lg border border-slate-200 bg-white">

                                <table class="w-full text-left text-[11px]">

                                    <thead class="bg-slate-50 sticky top-0 shadow-sm z-10 font-bold border-b border-slate-200 text-slate-600">

                                        <tr>

                                            <th class="p-2.5">Tên mâm</th>

                                            <th class="p-2.5 text-center">Trạng thái</th>

                                            <th class="p-2.5 text-center">Giờ gửi</th>

                                            <th class="p-2.5 text-center">Y tá</th>

                                        </tr>

                                    </thead>

                                    <tbody id="bangChoCapPhatTaiKhoa" class="divide-y divide-slate-100 text-slate-700">

                                        <tr><td colspan="4" class="p-4 text-center text-slate-400 italic">Không có yêu cầu mượn khay mới nào đang chờ duyệt.</td></tr>

                                    </tbody>

                                </table>

                            </div>

                        </div>



                        <div class="glass-panel p-5 flex flex-col border-t-4 border-t-purple-600 bg-white shadow-sm h-[380px]">

                            <div class="flex justify-between items-center border-b border-slate-100 pb-2.5 mb-3">

                                <h3 class="text-xs font-black text-slate-700 uppercase flex items-center gap-1.5"><i class="fa-solid fa-file-signature text-purple-600"></i> Đồ sạch đang về khoa (<span id="badgeChoNhanKhoa" class="text-purple-700 font-bold">0 mâm</span>)</h3>

                                <span id="txtNguoiDungNhanHienTai" class="text-[10px] font-mono font-black text-slate-400 bg-slate-100 px-2 py-0.5 rounded">--</span>

                            </div>

                            <div class="flex-1 overflow-auto rounded-lg border border-slate-200 bg-white mb-3">

                                <table class="w-full text-left text-[11px]">

                                    <thead class="bg-slate-50 sticky top-0 shadow-sm z-10 font-bold text-slate-600 border-b">

                                        <tr>

                                            <th class="p-2 text-center w-10">Tích</th>

                                            <th class="p-2">Mã ID Khay</th>

                                            <th class="p-2">Tên Mâm Dụng Cụ</th>

                                        </tr>

                                    </thead>

                                    <tbody id="bangChoNhanTaiKhoa" class="divide-y divide-slate-100 text-slate-700">

                                        <tr><td colspan="3" class="p-4 text-center text-slate-400 italic">Hiện không có dụng cụ nào đang chuyển về khoa.</td></tr>

                                    </tbody>

                                </table>

                            </div>

                            <button onclick="khoaKyNhanDoSachDienTu()" class="w-full bg-purple-600 hover:bg-purple-700 text-white font-black py-2.5 rounded-xl shadow text-xs uppercase tracking-wider flex items-center justify-center gap-1.5 transition-all"><i class="fa-solid fa-pen-fancy"></i> KÝ NHẬN VÀO TỦ KHOA</button>

                        </div>

                    </div>



                    <div id="lssub-tab-doi-luu" class="glass-panel p-5 flex flex-col border-t-4 border-t-teal-600 bg-white shadow-sm h-[420px] transition-all duration-200 hidden animate-fade-in">

                        <div class="flex justify-between items-start border-b border-slate-100 pb-3 mb-4">

                            <div>

                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-2"><i class="fa-solid fa-file-invoice text-teal-600"></i> Sổ Tay Giao Nhận Của Khoa</h3>

                                <p class="text-[10px] text-slate-400 font-bold mt-0.5">Bảng đối soát nợ nần, bàn giao hai chiều bẩn - sạch thực tế với CSSD</p>

                            </div>

                            <button onclick="inHoaDonGiaoNhan()" class="text-[11px] bg-white border border-slate-300 hover:border-teal-600 hover:text-teal-700 text-slate-700 px-3 py-2 rounded-lg shadow-sm font-black transition-all"><i class="fa-solid fa-print mr-1"></i> IN BIÊN BẢN GIAO NHẬN</button>

                        </div>

                        <div class="flex-1 overflow-auto rounded-xl border border-slate-200 bg-white">

                            <table class="w-full text-left text-xs">

                                <thead class="bg-slate-50 sticky top-0 shadow-sm z-10 font-bold border-b border-slate-200">

                                    <tr>

                                        <th class="p-3 w-[40%] border-r border-slate-200 text-slate-600">Phân Loại Mâm / Loại Dụng Cụ</th>

                                        <th class="p-3 text-center w-[15%] text-rose-600 bg-rose-50/40 border-r border-slate-200">Đã Trả Bẩn</th>

                                        <th class="p-3 text-center w-[15%] text-sky-600 bg-sky-50/40 border-r border-slate-200">Đã Nhận Sạch</th>

                                        <th class="p-3 text-center w-[15%] text-purple-600 bg-purple-50/40 border-r border-slate-200">Nhận Vô Khuẩn</th>

                                        <th class="p-3 text-center w-[15%] text-amber-600 bg-amber-50/40">CSSD Nợ Khoa</th>

                                    </tr>

                                </thead>

                                <tbody id="bangDonGiaoNhan" class="divide-y divide-slate-100 text-slate-700"></tbody>

                            </table>

                        </div>

                    </div>

                </div>



                <div id="tab-thugom" class="hidden h-full">

                    <div class="glass-panel p-5 flex flex-col h-[650px] border-t-4 border-t-sky-500 bg-white">

                        <div class="flex flex-col md:flex-row justify-between items-start md:items-center border-b border-slate-100 pb-4 mb-4 gap-3">

                            <div><h3 class="text-base font-black text-slate-700 uppercase"><i class="fa-solid fa-truck-medical mr-2 text-sky-500"></i> Điều phối Xe Thu Gom</h3></div>

                            <div class="flex flex-wrap items-center gap-2 w-full md:w-auto"><select id="filterKhoaThuGom" onchange="callRender()" class="rounded px-3 py-1.5 text-xs font-bold border border-slate-200"><option value="">-- Lọc theo Khoa --</option></select><span id="badgeSoCho" class="bg-slate-100 text-slate-600 text-xs font-bold px-2 py-1.5 rounded">0 Lệnh</span></div>

                        </div>

                        <div class="flex-1 overflow-auto bg-white rounded border border-slate-100">

                            <table class="w-full text-left text-xs">

                                <thead class="sticky top-0 shadow-sm bg-slate-50">

                                    <tr>

                                        <th class="p-3 w-[20%]">Từ Khoa & Ghi chú</th>

                                        <th class="p-3 w-[45%]">Bảng Checklist Chi Tiết</th>

                                        <th class="p-3 text-center w-[15%]">Thời Gian</th>

                                        <th class="p-3 text-center action-col w-[20%]">Thao tác</th>

                                    </tr>

                                </thead>

                                <tbody id="bangChoThuGom" class="divide-y divide-slate-50 text-slate-700"></tbody>

                            </table>

                        </div>

                    </div>

                </div>



                <div id="tab-mayrua" class="hidden flex flex-col gap-6 w-full">

                    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 min-h-[480px]">

                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-slate-400 bg-white shadow-sm">

                            <div class="flex justify-between items-center border-b border-slate-100 pb-3 mb-4">

                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">

                                    <i class="fa-solid fa-soap text-sky-500"></i> Danh sách mâm dụng cụ chờ rửa

                                </h3>

                                <span id="badgeChoRua" class="text-xs font-bold text-sky-700 bg-sky-50 px-2 py-1 rounded">0 Mục</span>

                            </div>

                            <div class="flex-1 overflow-auto rounded border border-slate-200">

                                <table class="w-full text-left text-xs">

                                    <thead class="sticky top-0 bg-slate-50 shadow-sm z-10 border-b">

                                        <tr>

                                            <th class="p-3 w-12 text-center">

                                                <input type="checkbox" id="selectAllRua" onchange="toggleSelectAllRua()" class="w-4 h-4 text-sky-600 rounded focus:ring-sky-500">

                                            </th>

                                            <th class="p-3 font-bold text-slate-600">Tên Mâm Đồ Bẩn</th>

                                            <th class="p-3 text-right font-bold text-slate-600 pr-4">Mã ID Định Vị</th>

                                        </tr>

                                    </thead>

                                    <tbody id="bangChoRua" class="divide-y divide-slate-100 text-slate-700 bg-white"></tbody>

                                </table>

                            </div>

                        </div>

                        

                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-sky-500 bg-white shadow-sm">

                            <div class="border-b border-slate-100 pb-3 mb-4 flex justify-between items-center">

                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">

                                    <i class="fa-solid fa-gears text-sky-600"></i> Thiết Lập Chu Trình & Hoá Chất Rửa

                                </h3>

                                <span class="text-[10px] text-slate-400 font-bold uppercase tracking-wider">Washer Disinfector</span>

                            </div>

                            

                            <div class="bg-slate-50 p-4 rounded-xl border border-slate-200 flex-1 flex flex-col justify-between gap-4">

                                <div class="space-y-3">

                                    <div class="grid grid-cols-2 gap-3">

                                        <div>

                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Phương pháp làm sạch:</label>

                                            <select id="rua_loaiRua" onchange="capNhatDanhSachMaMayRua()" class="w-full px-3 py-2 font-bold text-xs bg-white border border-slate-300 rounded shadow-sm text-slate-700">

                                                <option value="Máy rửa khử khuẩn tự động">Máy rửa khử khuẩn tự động</option>

                                                <option value="Máy rửa sóng siêu âm">Máy rửa sóng siêu âm</option>

                                                <option value="Bồn rửa thủ công">Bồn rửa thủ công</option>

                                            </select>

                                        </div>

                                        <div>

                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Mã máy rửa:</label>

                                            <select id="rua_maySo" onchange="tuDongTaoMaLoMeRua()" class="w-full px-3 py-2 font-black text-xs text-sky-700 bg-white border border-slate-300 rounded shadow-sm">

                                                <option value="WD1">WD1</option>

                                                <option value="WD2">WD2</option>

                                            </select>

                                        </div>

                                    </div>



                                    <div class="grid grid-cols-2 gap-3">

                                        <div>

                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Số mẻ rửa trong ngày:</label>

                                            <input type="text" id="rua_meSo" readonly class="w-full px-3 py-2 text-xs font-mono font-bold text-center bg-slate-100 border border-slate-200 rounded text-slate-600">

                                        </div>

                                        <div>

                                            <label class="block text-[10px] font-black text-sky-600 uppercase mb-1">Mã Lô Rửa (Batch Code):</label>

                                            <input type="text" id="rua_batchId" readonly class="w-full px-3 py-2 text-xs font-mono font-black text-center bg-sky-50 border border-sky-300 rounded text-sky-700 tracking-wider">

                                        </div>

                                    </div>



                                    <div>

                                        <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Chu kỳ nhiệt độ rửa:</label>

                                        <select id="rua_chuKy" class="w-full px-3 py-2 text-xs font-semibold bg-white border border-slate-300 rounded text-slate-700">

                                            <option value="Chu trình tiêu chuẩn (93°C - 10 phút)">Chu trình tiêu chuẩn (93°C - 10 phút)</option>

                                            <option value="Chu trình dụng cụ tinh xảo (cảm biến)">Chu trình dụng cụ tinh xảo (cảm biến)</option>

                                            <option value="Rửa sóng siêu âm kết hợp khử khuẩn">Rửa sóng siêu âm kết hợp khử khuẩn</option>

                                            <option value="Ngâm hoá chất thủ công + xả ráo">Ngâm hoá chất thủ công + xả ráo</option>

                                        </select>

                                    </div>



                                    <div class="grid grid-cols-3 gap-2">

                                        <div class="col-span-2">

                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Hoá chất tiêu hao mẻ này:</label>

                                            <select id="rua_hoaChat" class="w-full px-3 py-2 text-xs font-bold bg-white border border-slate-300 rounded text-sky-800">

                                            </select>

                                        </div>

                                        <div>

                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Liều lượng (ml/L):</label>

                                            <input type="text" id="rua_lieuLuong" placeholder="2 - 5 ml/L" class="w-full px-2 py-2 text-xs font-bold text-center bg-white border border-slate-300 rounded text-slate-700">

                                        </div>

                                    </div>

                                </div>



                                <div class="pt-3 border-t border-slate-200">

                                    <button onclick="xacNhanMeRua()" class="w-full bg-sky-600 hover:bg-sky-700 text-white font-black py-3.5 rounded-lg text-xs uppercase tracking-wider shadow-md flex items-center justify-center gap-2 transition-all active:scale-95">

                                        <i class="fa-solid fa-play text-xs"></i> KHỞI CHẠY CHU TRÌNH MÁY RỬA KHỬ KHUẨN

                                    </button>

                                </div>

                            </div>

                        </div>

                    </div>

                    

                    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 min-h-[450px]">

                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-amber-500 bg-white shadow-sm">

                            <div class="border-b border-slate-100 pb-3 mb-4">

                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">

                                    <i class="fa-solid fa-magnifying-glass text-amber-500"></i> Giám Sát Nghiệm Thu Độ Sạch Mẻ Rửa

                                </h3>

                            </div>

                            <div class="mb-3 bg-slate-50 border border-slate-200 p-3 rounded-xl shadow-inner">

                                <label class="block text-[11px] font-black text-slate-700 uppercase mb-1.5">

                                    Đánh giá chất lượng rửa (Test chỉ thị Protein/ATP):

                                </label>

                                <select id="rua_testDoSach" class="w-full p-2 text-xs font-black border border-slate-300 rounded bg-white text-emerald-700">

                                    <option value="ĐẠT (CẢM QUAN KHÔNG CÒN VẾT MÁU / RỈ)">🟢 ĐẠT (CẢM QUAN KHÔNG VẾT MÁU / RỈ)</option>

                                    <option value="ĐẠT (TEST PROTEIN ÂM TÍNH)">🟢 ĐẠT (TEST PROTEIN/ATP ÂM TÍNH)</option>

                                    <option value="KHÔNG ĐẠT (CÒN MẢNG BÁM / VẾT DƠ)">🔴 KHÔNG ĐẠT (CÒN MẢNG BÁM / VẾT DƠ)</option>

                                </select>

                            </div>

                            <div class="mb-3 flex gap-2">

                                <button onclick="duyetSachMeRuaHangLoat()" class="w-2/3 bg-emerald-600 hover:bg-emerald-700 text-white font-black py-2.5 rounded text-xs shadow-md uppercase transition-colors"><i class="fa-solid fa-circle-check mr-1"></i> Nghiệm thu ĐẠT & Chuyển đóng gói</button>

                                <button onclick="tuChoiMeRuaHangLoat()" class="w-1/3 bg-rose-600 hover:bg-rose-700 text-white font-black py-2.5 rounded text-xs shadow-md uppercase transition-colors"><i class="fa-solid fa-rotate-left mr-1"></i> RỬA LẠI</button>

                            </div>

                            <div class="flex-1 overflow-auto bg-slate-50 rounded-lg border border-slate-200">

                                <table class="w-full text-left">

                                    <thead class="sticky top-0 bg-amber-100 shadow-sm z-10 border-b border-amber-200">

                                        <tr>

                                            <th class="p-3 w-12 text-center">

                                                <input type="checkbox" id="selectAllNghiemThuRua" onchange="toggleSelectAllNghiemThuRua()" class="w-4 h-4 text-amber-600 rounded focus:ring-amber-500">

                                            </th>

                                            <th class="p-2 text-xs font-black text-slate-700">Mâm dụng cụ đang trong buồng máy</th>

                                        </tr>

                                    </thead>

                                    <tbody id="bangChoNiemThuRua" class="divide-y divide-slate-200 bg-white"></tbody>

                                </table>

                            </div>

                        </div>

                        

                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-emerald-500 bg-white shadow-sm">

                            <div class="border-b border-slate-100 pb-3 mb-4">

                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">

                                    <i class="fa-solid fa-clock-rotate-left text-emerald-500"></i> Nhật ký vận hành máy rửa khử khuẩn

                                </h3>

                            </div>

                            <div class="flex-1 overflow-auto rounded-lg border border-slate-200 bg-white">

                                <table class="w-full text-left">

                                    <thead class="sticky top-0 bg-slate-50 shadow-sm border-b border-slate-200">

                                        <tr>

                                            <th class="p-3 text-xs font-bold text-slate-600">Thông tin mẻ máy</th>

                                            <th class="p-3 text-xs font-bold text-slate-600 text-right pr-4">Cơ số mâm</th>

                                        </tr>

                                    </thead>

                                    <tbody id="bangLichSuRua" class="divide-y divide-slate-100 text-slate-700"></tbody>

                                </table>

                            </div>

                        </div>

                    </div>

                </div>



                <div id="tab-donggoi" class="hidden h-full">

                    <div class="glass-panel p-5 flex flex-col h-[650px] border-t-4 border-t-sky-500 bg-white">

                        <div class="flex justify-between items-center border-b border-slate-100 pb-4 mb-4">

                            <div><h3 class="text-base font-black text-slate-700 uppercase"><i class="fa-solid fa-hands-bubbles mr-2 text-sky-500"></i> Trạm Làm Sạch & Đóng Gói</h3></div>

                            <span id="badgeDongGoi" class="bg-slate-100 text-slate-600 text-xs font-bold px-2 py-1 rounded">0 Mâm</span>

                        </div>

                        <div class="flex-1 overflow-auto p-1" id="gridDongGoi"></div>

                    </div>

                </div>



                <div id="tab-mayhap" class="hidden flex flex-col gap-6 w-full">

                    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 min-h-[480px]">

                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-slate-400 bg-white shadow-sm">

                            <div class="flex justify-between items-center border-b border-slate-100 pb-3 mb-4">

                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">

                                    <i class="fa-solid fa-boxes-stacked text-slate-400"></i> Danh sách chờ hấp

                                </h3>

                                <span id="badgeChoHap" class="text-xs font-bold text-slate-500 bg-slate-100 px-2 py-1 rounded">0 Mục</span>

                            </div>

                            <div class="flex-1 overflow-auto rounded border border-slate-200">

                                <table class="w-full text-left text-xs">

                                    <thead class="sticky top-0 bg-slate-50 shadow-sm z-10 border-b">

                                        <tr>

                                            <th class="p-3 w-12 text-center">

                                                <input type="checkbox" id="selectAllHap" onchange="toggleSelectAllHap()" class="w-4 h-4 text-sky-600 rounded focus:ring-sky-500">

                                            </th>

                                            <th class="p-3 font-bold text-slate-600">Mâm Dụng Cụ</th>

                                            <th class="p-3 text-right font-bold text-slate-600 pr-4">Mã Định Hình Khay</th>

                                        </tr>

                                    </thead>

                                    <tbody id="bangChoHap" class="divide-y divide-slate-100 text-slate-700 bg-white"></tbody>

                                </table>

                            </div>

                        </div>

                        

                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-sky-500 bg-white shadow-sm">

                            <div class="border-b border-slate-100 pb-3 mb-4 flex justify-between items-center">

                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">

                                    <i class="fa-solid fa-square-poll-horizontal text-sky-500"></i> Chi Tiết Mẻ Tiệt Khuẩn

                                </h3>

                                <span class="text-[10px] text-slate-400 font-bold uppercase tracking-wider">Autoclave Details</span>

                            </div>

                            

                            <div class="bg-slate-50 p-4 rounded-xl border border-slate-200 flex-1 flex flex-col justify-between gap-4">

                                <div class="space-y-3">

                                    <div class="grid grid-cols-2 gap-3">

                                        <div>

                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Loại hình tiệt khuẩn:</label>

                                            <select id="hap_loaiHap" onchange="capNhatDanhSachMaMay()" class="w-full px-3 py-2 font-bold text-xs bg-white border border-slate-300 rounded shadow-sm text-slate-700">

                                                <option value="Hấp hơi nước">Hấp hơi nước</option>

                                                <option value="Hấp H2O2 (Plasma)">Hấp H2O2 (Plasma)</option>

                                                <option value="Khử khuẩn EO">Khử khuẩn EO</option>

                                            </select>

                                        </div>

                                        <div>

                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Mã máy đã chọn (Code):</label>

                                            <select id="hap_maySo" onchange="tuDongTaoMaLoMeHap()" class="w-full px-3 py-2 font-black text-xs text-sky-700 bg-white border border-slate-300 rounded shadow-sm">

                                                <option value="A1">A1</option>

                                                <option value="A2">A2</option>

                                                <option value="A3">A3</option>

                                                <option value="A4">A4</option>

                                            </select>

                                        </div>

                                    </div>



                                    <div class="grid grid-cols-2 gap-3">

                                        <div>

                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Số lần đã thực hiện:</label>

                                            <input type="text" id="hap_meSo" readonly class="w-full px-3 py-2 text-xs font-mono font-bold text-center bg-slate-100 border border-slate-200 rounded text-slate-600">

                                        </div>

                                        <div>

                                            <label class="block text-[10px] font-black text-rose-600 uppercase mb-1 flex items-center gap-1">

                                                Mã lô liên kết (Batch ID):

                                            </label>

                                            <input type="text" id="hap_batchId" readonly class="w-full px-3 py-2 text-xs font-mono font-black text-center bg-rose-50 border border-rose-300 rounded text-rose-700 tracking-wider">

                                        </div>

                                    </div>



                                    <div class="grid grid-cols-3 gap-2">

                                        <div class="col-span-2">

                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Nhiệt độ / Chương trình:</label>

                                            <select id="hap_nhietDo" class="w-full px-3 py-2 text-xs font-semibold bg-white border border-slate-300 rounded text-slate-700">

                                                <option value="134°C - 4 phút">134°C - 4 phút</option>

                                                <option value="121°C - 20 phút">121°C - 20 phút</option>

                                            </select>

                                        </div>

                                        <div>

                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Áp suất (Bar):</label>

                                            <input type="text" id="hap_apSuat" placeholder="2.1" class="w-full px-2 py-2 text-xs font-bold text-center bg-white border border-slate-300 rounded text-slate-700">

                                        </div>

                                    </div>



                                    <div class="mb-1 mt-2 flex items-center gap-2 bg-amber-50 border border-amber-200 p-2.5 rounded-lg shadow-sm">

                                        <input type="checkbox" id="hap_hasBI" class="w-4 h-4 rounded text-amber-600 focus:ring-amber-500 cursor-pointer">

                                        <label for="hap_hasBI" class="text-xs font-black text-amber-800 cursor-pointer select-none">

                                            🔥 MẺ CÓ KÈM ỐNG TEST SINH HỌC BI (BẮT BUỘC ĐỐI SOÁT KHI DUYỆT)

                                        </label>

                                    </div>

                                </div>



                                <div class="pt-3 border-t border-slate-200">

                                    <button onclick="xacNhanMeHap()" class="w-full bg-sky-600 hover:bg-sky-700 text-white font-black py-3.5 rounded-lg text-xs uppercase tracking-wider shadow-md flex items-center justify-center gap-2 transition-transform active:scale-[0.98]">

                                        <i class="fa-solid fa-play-circle text-sm"></i> KÍCH HOẠT VÀ KHỞI ĐỘNG LÒ HẤP

                                    </button>

                                </div>

                            </div>

                        </div>

                    </div>

                    

                    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 min-h-[450px]">

                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-amber-500 bg-white shadow-sm">

                            <div class="border-b border-slate-100 pb-3 mb-4">

                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">

                                    <i class="fa-solid fa-vial-circle-check text-amber-500"></i> Phê Duyệt - Chấp Nhận Vô Kho

                                </h3>

                            </div>



                            <div class="mb-3 bg-slate-50 border border-slate-200 p-3 rounded-xl shadow-inner">

                                <label class="block text-[11px] font-black text-slate-700 uppercase mb-1.5 flex items-center gap-1.5">

                                    <i class="fa-solid fa-camera text-sky-600"></i> Tải ảnh minh chứng kết quả BI (Nếu duyệt mẻ có BI):

                                </label>

                                <input type="file" id="input_anhBI" accept="image/*" onchange="docAnhBiUpTaiCho(this)" 

                                       class="block w-full text-xs text-slate-500 file:mr-3 file:py-1.5 file:px-3 file:rounded-lg file:border-0 file:text-xs file:font-black file:bg-sky-50 file:text-sky-700 hover:file:bg-sky-100 cursor-pointer border border-slate-300 rounded-lg p-1 bg-white">

                            </div>



                            <div class="mb-3 flex flex-col gap-2">

                                <button onclick="nhapKhoHangLoat()" class="w-full bg-emerald-600 hover:bg-emerald-700 text-white font-black py-2.5 rounded text-xs shadow-md"><i class="fa-solid fa-check-double mr-1"></i> DUYỆT ĐẠT & CHO VÀO KHO LƯU TRỮ CHUNG</button>

                                <div class="flex gap-2">

                                    <button onclick="inTemNghiemThuHangLoat()" class="w-1/2 bg-sky-600 hover:bg-sky-700 text-white font-black py-2 rounded text-xs shadow-md"><i class="fa-solid fa-print mr-1"></i> IN TEM TỔNG</button>

                                    <button onclick="tuChoiHapHangLoat()" class="w-1/2 bg-rose-600 hover:bg-rose-700 text-white font-black py-2 rounded text-xs shadow-md"><i class="fa-solid fa-ban mr-1"></i> KHÔNG ĐẠT (TRẢ LẠI)</button>

                                </div>

                            </div>

                            <div class="flex-1 overflow-auto bg-amber-50/20 rounded-lg border border-amber-200/50">

                                <table class="w-full text-left">

                                    <thead class="sticky top-0 bg-amber-100 shadow-sm z-10 border-b border-amber-200">

                                        <tr>

                                            <th class="p-2.5 w-12 text-center">

                                                <input type="checkbox" id="selectAllNghiemThu" onchange="toggleSelectAllNghiemThu()" class="w-4 h-4 text-amber-600 rounded focus:ring-amber-500">

                                            </th>

                                            <th class="p-2 text-xs font-black text-slate-700">Mâm dụng cụ / Mã mẻ</th>

                                        </tr>

                                    </thead>

                                    <tbody id="bangChoNghiệmThu" class="divide-y divide-slate-200 bg-white"></tbody>

                                </table>

                            </div>

                        </div>

                        

                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-emerald-500 bg-white shadow-sm">

                            <div class="border-b border-slate-100 pb-3 mb-4">

                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">

                                    <i class="fa-solid fa-clock-rotate-left text-emerald-500"></i> Nhật ký vận hành lò

                                </h3>

                            </div>

                            <div class="flex-1 overflow-auto rounded-lg border border-slate-200 bg-white">

                                <table class="w-full text-left">

                                    <thead class="sticky top-0 bg-slate-50 shadow-sm border-b border-slate-200">

                                        <tr>

                                            <th class="p-3 text-xs font-bold text-slate-600">Thông quy trình</th>

                                            <th class="p-3 text-xs font-bold text-slate-600 text-right pr-4">Tổng khay</th>

                                        </tr>

                                    </thead>

                                    <tbody id="bangLichSuHap" class="divide-y divide-slate-100 text-slate-700"></tbody>

                                </table>

                            </div>

                        </div>

                    </div>

                </div>



                <!-- ========================================== -->

                <!-- TRẠM 6: KHO VÔ KHUẨN (TỐI ƯU CẢM ỨNG TABLET) -->

                <!-- ========================================== -->

                <div id="tab-khovokhuan" class="hidden flex flex-col gap-6 h-full">

                    <div id="vungYeuCauCapPhat" class="mb-6 bg-amber-50/70 border border-amber-200 rounded-xl p-4 shadow-sm">

                        <h3 class="font-black text-amber-950 text-xs flex items-center gap-1.5"><i class="fa-solid fa-bell-slash"></i> Đang đồng bộ hộp yêu cầu cấp phát...</h3>

                    </div>



                    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 h-auto">

                        <div class="glass-panel p-5 flex flex-col border-t-4 border-t-sky-500 bg-slate-50 h-auto min-h-[320px]">

                            <div class="border-b border-slate-200 pb-4 mb-4"><h3 class="text-base font-black text-slate-700 uppercase"><i class="fa-solid fa-dolly mr-2 text-sky-500"></i> Bàn Giao Xuất Kho</h3></div>

                            <div class="space-y-4">

                                <div><label class="block text-[10px] font-bold text-slate-500 mb-1.5 uppercase">Chọn Khoa Nhận Đồ:</label><select id="xuat_selKhoa" class="w-full px-3 py-3 shadow-sm font-bold text-slate-700 border border-slate-300 rounded-lg"><option value="">-- Chọn Khoa --</option></select></div>

                                <div class="bg-white p-4 rounded-lg border border-slate-200 shadow-sm relative space-y-3">

                                    <label class="block text-[10px] uppercase font-bold text-slate-500">Quét Mã ID Khay Xuất:</label>

                                    <div class="flex gap-2">

                                        <input type="text" id="xuat_inpMaBo" placeholder="Mã ID mâm..." class="w-full px-3 py-3 font-bold text-sky-700 shadow-sm border border-sky-300 rounded-lg bg-sky-50 text-base" onkeypress="if(event.key==='Enter') xuatKhoXoayVong()">

                                        <button onclick="moCamera('xuat_inpMaBo')" class="bg-slate-800 text-white px-4 py-3 rounded-lg shadow action-btn whitespace-nowrap min-h-[48px]"><i class="fa-solid fa-camera text-lg"></i></button>

                                    </div>

                                    <button onclick="xuatKhoXoayVong()" class="w-full bg-emerald-600 hover:bg-emerald-700 active:bg-emerald-800 text-white font-black py-3.5 rounded-xl text-sm shadow-md min-h-[48px] uppercase tracking-wide transition-all"><i class="fa-solid fa-check mr-2"></i> XÁC NHẬN XUẤT KHO</button>

                                </div>

                            </div>

                        </div>

                        

                        <div class="glass-panel p-5 flex flex-col border-t-4 border-t-emerald-500 lg:col-span-2 min-h-[380px]">

                            <div class="border-b border-slate-100 pb-3 mb-4 flex justify-between items-center">

                                <h3 class="text-sm font-black text-slate-700 uppercase"><i class="fa-solid fa-warehouse mr-2 text-emerald-500"></i> Danh sách mâm đang lưu tại Kho Vô Khuẩn</h3>

                                <span class="text-[10px] font-bold bg-emerald-100 text-emerald-800 px-2.5 py-1 rounded-full uppercase">Chế độ Tablet</span>

                            </div>

                            <div class="flex-1 overflow-auto bg-white rounded-lg border border-slate-200">

                                <table class="w-full text-left text-xs">

                                    <thead class="sticky top-0 shadow-sm bg-slate-50 font-bold border-b border-slate-200 text-slate-600">

                                        <tr>

                                            <th class="p-3.5">Loại Mâm / Tên Dụng Cụ</th>

                                            <th class="p-3.5">Mã ID Khay</th>

                                            <th class="p-3.5 text-center">Định Biên Sở Hữu</th>

                                            <th class="p-3.5 text-center">Vị Trí Kệ</th>

                                            <th class="p-3.5 text-center">HSD</th>

                                            <th class="p-3.5 text-center">Thao tác nhanh</th>

                                        </tr>

                                    </thead>

                                    <tbody id="bangKhoVoKhuan" class="divide-y divide-slate-100 text-slate-700 font-medium"></tbody>

                                </table>

                            </div>

                        </div>

                    </div>

                </div>



                <div id="tab-quanlykho" class="hidden h-full">

                    <div class="glass-panel p-5 flex flex-col h-[650px] border-t-4 border-t-amber-500 bg-white">

                        <div class="border-b border-slate-100 pb-4 mb-4">

                            <h3 class="text-base font-black text-slate-800 uppercase flex items-center gap-2"><i class="fa-solid fa-boxes-stacked text-amber-500"></i> Bản Đồ Theo Dõi Tồn Kho Thực Tế Toàn Viện</h3>

                        </div>

                        <div class="mb-4 bg-slate-50 p-3 rounded border border-slate-200 w-1/3">

                            <label class="block text-[10px] font-bold text-slate-500 uppercase mb-1">Lọc Theo Khoa Sở Hữu:</label>

                            <select id="inv_filterKhoa" onchange="callRender()" class="w-full p-2 text-xs font-bold"><option value="">-- Tất cả Khoa Phòng --</option></select>

                        </div>

                        <div class="flex-1 overflow-auto border border-slate-200 rounded-lg">

                            <table class="w-full text-left text-xs">

                                <thead class="bg-slate-50 sticky top-0 shadow-sm z-10">

                                    <tr>

                                        <th class="p-3">Mã ID Khay</th>

                                        <th class="p-3">Loại Mâm Dụng Cụ</th>

                                        <th class="p-3">Khoa Đang Giao/Giữ</th>

                                        <th class="p-3 text-center">Vị Trí Vật Lý Hiện Tại</th>

                                        <th class="p-3 text-center">Mẻ Lò Gần Nhất</th>

                                        <th class="p-3 text-center">Giám Sát HSD</th> </tr>

                                </thead>

                                <tbody id="bangTonKhoTe" class="divide-y divide-slate-100 bg-white text-slate-700"></tbody>

                            </table>

                        </div>

                    </div>

                </div>



                <div id="tab-danhmuc" class="hidden grid grid-cols-1 lg:grid-cols-2 gap-6 h-full">

                    <div class="glass-panel p-5 flex flex-col h-[650px] border-t-4 border-t-sky-500 bg-white">

                        <div class="flex justify-between items-center border-b border-slate-100 pb-3 mb-4">

                            <h3 class="text-sm font-black text-slate-700 uppercase"><i class="fa-solid fa-list-check mr-2 text-sky-500"></i> 7.1 Danh Mục Loại Bộ Linh Kiện Chuẩn</h3>

                            <span id="badgeLinhKienCount" class="text-xs font-bold text-sky-700 bg-sky-50 px-2 py-1 rounded">Cấu hình chi tiết</span>

                        </div>

                        <div class="flex-1 overflow-auto border border-slate-100 rounded-lg">

                            <table class="w-full text-left text-xs">

                                <thead class="sticky top-0 bg-slate-50 shadow-sm z-10">

                                    <tr>

                                        <th class="p-3 w-1/3">Tên Loại Bộ</th>

                                        <th class="p-3 w-1/2">Chi Tiết Linh Kiện Chuẩn (Tên | SL)</th>

                                        <th class="p-3 text-center">Tổng SL</th>

                                    </tr>

                                </thead>

                                <tbody id="bangDanhMucLinhKien" class="divide-y divide-slate-100 text-slate-700 bg-white">

                                    <tr><td colspan="3" class="p-4 text-center text-slate-400 italic">Đang tải dữ liệu bộ linh kiện...</td></tr>

                                </tbody>

                            </table>

                        </div>

                    </div>



                    <div class="glass-panel p-5 flex flex-col h-[650px] border-t-4 border-t-teal-500 bg-white">

                        <div class="flex justify-between items-center border-b border-slate-100 pb-3 mb-4">

                            <h3 class="text-sm font-black text-teal-700 uppercase"><i class="fa-solid fa-chart-line mr-2 text-teal-500"></i> 7.2 Giám Sát Tuổi Thọ & Chu Kỳ Khay</h3>

                            <span id="badgeTuoiThoKhay" class="text-xs font-bold text-teal-700 bg-teal-50 px-2 py-1 rounded">0 Khay</span>

                        </div>

                        <div class="flex-1 overflow-auto border border-slate-100 rounded-lg">

                            <table class="w-full text-left text-xs">

                                <thead class="sticky top-0 bg-slate-50 shadow-sm z-10">

                                    <tr>

                                        <th class="p-3">Mã Lưu Kho</th>

                                        <th class="p-3">Tên Khay (Phân loại)</th>

                                        <th class="p-3 text-center">Trạng Thái</th>

                                        <th class="p-3 text-center bg-amber-50 text-amber-900 font-bold">Chu Kỳ Lò</th>

                                    </tr>

                                </thead>

                                <tbody id="bangDanhMucTong" class="divide-y divide-slate-100 text-slate-700 bg-white">

                                    <tr><td colspan="4" class="p-4 text-center text-slate-400 italic">Đang tải danh sách khay vận hành...</td></tr>

                                </tbody>

                            </table>

                        </div>

                    </div>

                </div>



                <div id="tab-lichsuluanchuyen" class="hidden flex flex-col gap-6 w-full">

                    <div class="glass-panel p-5 flex flex-col h-[650px] border-t-4 border-t-sky-600 bg-white">

                        <div class="border-b border-slate-100 pb-3 mb-3 flex justify-between items-center">

                            <h4 class="text-base font-black text-slate-800 uppercase flex items-center gap-2"><i class="fa-solid fa-history text-sky-600"></i> Nhật Ký Luân Chuyển & Hành Trình Khay Dụng Cụ</h4>

                            <span class="text-[11px] bg-slate-100 px-2 py-1 rounded font-bold text-slate-500 uppercase">Hành trình khay thực tế</span>

                        </div>

                        <div class="flex-1 overflow-auto border border-slate-200 rounded-lg">

                            <table class="w-full text-left text-xs">

                                <thead class="bg-slate-50 sticky top-0 shadow-sm z-10">

                                    <tr>

                                        <th class="p-3">Mã ID Khay</th>

                                        <th class="p-3">Tên Mâm Dụng Cụ</th>

                                        <th class="p-3">Khoa Lâm Sàng</th>

                                        <th class="p-3 text-center">Vị Trí/Trạng Thái</th>

                                        <th class="p-3 text-center bg-rose-50 text-rose-900 font-bold">Mã Lô Hấp</th>

                                        <th class="p-3 text-center bg-sky-50 text-sky-900 font-bold">KTV Xuất Kho</th>

                                        <th class="p-3 text-center">Thời Gian Ký Gửi</th>

                                    </tr>

                                </thead>

                                <tbody id="bangLichSuHanhTrinhGoc" class="divide-y divide-slate-100 text-slate-600">

                                    <tr><td colspan="7" class="p-4 text-center italic text-slate-400">Đang đồng bộ dòng dữ liệu...</td></tr>

                                </tbody>

                            </table>

                        </div>

                    </div>

                </div>



                <div id="tab-tracuu" class="hidden flex flex-col gap-6 w-full">

                    <div class="flex space-x-2 bg-white p-2 rounded-lg border border-slate-200 shadow-sm">

                        <button onclick="switchAdminSubtab('database')" id="subbtn-database" class="admin-subtab-active px-4 py-2 text-xs font-black rounded">1. TRUY VẾT & NẠP DATABASE</button>

                        <button onclick="switchAdminSubtab('security')" id="subbtn-security" class="px-4 py-2 text-xs font-black rounded text-slate-600 bg-slate-100">2. TÀI KHOẢN & MÃ PIN</button>

                    </div>

                    

                    <div id="subtab-database" class="space-y-6">

                        <div class="glass-panel p-5 border-t-4 border-t-rose-600 bg-white shadow-sm">

                            <h4 class="text-xs font-black text-rose-700 uppercase mb-2 flex items-center gap-1">

                                <i class="fa-solid fa-slate-virus text-sm"></i> TRẠM TRUY VẾT NHIỄM TRÙNG KHẨN CẤP (KHOANH VÙNG THEO MẺ)

                            </h4>

                            <div class="flex gap-2">

                                <input type="text" id="inp_searchBatch" placeholder="Nhập chính xác mã lô cần truy vết..." class="w-full px-3 py-2.5 font-mono font-bold text-rose-700 bg-rose-50/30 border border-rose-300 rounded-md text-sm">

                                <button onclick="truyVetTheoMaBatch()" class="bg-rose-600 hover:bg-rose-700 text-white font-black px-6 py-2.5 rounded shadow text-xs whitespace-nowrap"><i class="fa-solid fa-magnifying-glass-danger mr-1"></i> TRA CỨU KHẨN CẤP</button>

                                <button onclick="clearTruyVetBatch()" class="bg-slate-100 text-slate-600 font-bold px-4 py-2.5 rounded border border-slate-200 text-xs">RESET</button>

                            </div>

                        </div>



                        <div class="bg-amber-50 p-5 rounded border border-amber-200 shadow-sm">

                            <h4 class="text-xs font-black text-amber-800 uppercase mb-1.5 flex items-center gap-1.5"><i class="fa-solid fa-file-excel text-sm text-amber-600"></i> Nạp Danh Sách Bộ Dụng Cụ Từ Cấu Hình Excel</h4>

                            <p class="text-[11px] text-slate-500 mb-3">Hệ thống tự động biên dịch và phân tích cấu hình chi tiết linh kiện bên trong mâm ngay sau khi anh chọn file.</p>

                            <div class="flex items-center gap-4 bg-white p-3 rounded-lg border border-slate-200">

                                <input type="file" id="excelFileInput" accept=".xlsx, .xls" class="block w-full text-xs text-slate-500 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-xs file:font-black file:bg-amber-50 file:text-amber-800 hover:file:bg-amber-100 cursor-pointer">

                            </div>

                        </div>



                        <div class="bg-sky-50 p-5 rounded border border-sky-200 shadow-sm">

                            <h4 class="text-xs font-black text-sky-800 uppercase mb-1.5 flex items-center gap-1.5"><i class="fa-solid fa-circle-plus text-sm text-sky-600"></i> Khai Báo & Định Danh Khay Mới Vãng Lai</h4>

                            <p class="text-[11px] text-slate-500 mb-3">Sử dụng tính năng này để cấp phát khay mới mua hoặc khay độc bản vào hệ thống luân chuyển động toàn viện.</p>

                            <button onclick="khaiSinhKhayVangLai()" class="bg-sky-600 hover:bg-sky-700 text-white font-black py-2.5 px-4 rounded text-xs shadow flex items-center gap-1.5"><i class="fa-solid fa-plus-circle"></i> KHAI SINH KHAY MỚI</button>

                        </div>



                        <div class="border-t border-slate-200 pt-4 space-y-4">

                            <button onclick="resetDuLieuKet()" class="w-full bg-amber-500 hover:bg-amber-600 text-white font-black py-3.5 rounded shadow-md text-xs uppercase tracking-wider"><i class="fa-solid fa-broom mr-2"></i> DỌN DẸP DỮ LIỆU KẸT (REFRESH MÂM)</button>

                            <div class="p-4 bg-rose-50 border border-rose-200 rounded-lg shadow-sm">

                                <button onclick="xoaSachDuLieuGiaoDichRealtime()" class="w-full bg-rose-600 hover:bg-rose-700 text-white font-black py-3 px-4 rounded shadow text-xs uppercase tracking-wider"><i class="fa-solid fa-dumpster-fire mr-1.5"></i> Xóa sạch toàn bộ lịch sử dữ liệu Demo</button>

                            </div>

                        </div>

                    </div>



                    <div id="subtab-security" class="glass-panel p-6 space-y-6 hidden">

                        <div class="bg-slate-50 p-4 rounded-xl border border-slate-200 mb-6">

                            <h4 class="text-xs font-black text-slate-700 uppercase mb-2 flex items-center gap-1.5"><i class="fa-solid fa-toggle-on text-sky-600 text-sm"></i> BẢNG PHÂN QUYỀN & KHÓA TAB CHỨC NĂNG GIAO DIỆN</h4>

                            <div class="overflow-x-auto border border-slate-200 rounded-lg bg-white">

                                <table class="w-full text-left">

                                    <thead class="bg-slate-100 text-[10px] font-black uppercase text-slate-600 border-b">

                                        <tr>

                                            <th class="p-3 w-2/5">Tên Tab / Chức năng hệ thống</th>

                                            <th class="p-3 text-center w-1/5 bg-sky-50/50">KTV CSSD</th>

                                            <th class="p-3 text-center w-1/5">Điều Dưỡng Lâm Sàng</th>

                                            <th class="p-3 text-center w-1/5">Khách Tham Quan</th>

                                        </tr>

                                    </thead>

                                    <tbody id="bodyMaTranGiaoDien"></tbody>

                                </table>

                            </div>

                        </div>



                        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">

                            <div class="bg-slate-50 p-4 rounded border border-slate-200">

                                <h4 class="text-xs font-black text-slate-600 uppercase">PIN ADMIN HỆ THỐNG</h4>

                                <input type="password" id="cfg_pinAdmin" class="w-full p-2 mt-1 font-mono text-center font-bold">

                                <button onclick="saveAdminPIN('ADMIN')" class="bg-slate-800 text-white font-bold py-2 mt-2 rounded text-xs w-full">LƯU PIN ADMIN</button>

                            </div>

                            <div class="bg-slate-50 p-4 rounded border border-slate-200">

                                <h4 class="text-xs font-black text-slate-600 uppercase">PIN CHUNG CSSD (BACKUP)</h4>

                                <input type="password" id="cfg_pinCSSD" class="w-full p-2 mt-1 font-mono text-center font-bold">

                                <button onclick="saveAdminPIN('CSSD')" class="bg-sky-600 text-white font-bold py-2 mt-2 rounded text-xs w-full">LƯU PIN CSSD</button>

                            </div>

                            <div class="bg-slate-50 p-4 rounded border border-slate-200">

                                <h4 class="text-xs font-black text-slate-600 uppercase">PIN KHÁCH (GUEST)</h4>

                                <input type="password" id="cfg_pinGuest" class="w-full p-2 mt-1 font-mono text-center font-bold">

                                <button onclick="saveAdminPIN('GUEST')" class="bg-emerald-600 text-white font-bold py-2 mt-2 rounded text-xs w-full">LƯU PIN GUEST</button>

                            </div>

                        </div>



                        <div class="pt-4 border-t border-slate-200 mt-4">

                            <div class="flex justify-between items-center mb-3">

                                <h4 class="text-xs font-black text-slate-700 uppercase"><i class="fa-solid fa-user-nurse mr-1"></i> Quản Lý Nhân Viên CSSD (KTV)</h4>

                                <button onclick="themKtvCssd()" class="bg-sky-600 hover:bg-sky-700 text-white font-black py-1.5 px-3 rounded text-xs shadow"><i class="fa-solid fa-plus mr-1"></i> THÊM NHÂN VIÊN</button>

                            </div>

                            <div class="overflow-x-auto border border-slate-200 rounded max-h-64 mb-6">

                                <table class="w-full text-left text-xs">

                                    <thead class="bg-slate-50 sticky top-0"><tr><th class="p-3 w-1/6">Mã NV</th><th class="p-3 w-1/3">Tên Nhân Viên</th><th class="p-3 text-center border-x border-slate-200 w-1/4">MÃ PIN ĐĂNG NHẬP</th><th class="p-3 text-center w-1/4">Thao tác</th></tr></thead>

                                    <tbody id="bangNhanVienCssd" class="bg-white divide-y divide-slate-100"></tbody>

                                </table>

                            </div>

                        </div>



                        <div class="pt-4 border-t border-slate-200">

                            <div class="flex justify-between items-center mb-3">

                                <h4 class="text-xs font-black text-slate-700 uppercase"><i class="fa-solid fa-hospital-user mr-1"></i> Quản Lý & Reset PIN Khoa Lâm Sàng</h4>

                                <button onclick="themKhoaThuCong()" class="bg-emerald-600 hover:bg-emerald-700 text-white font-black py-1.5 px-3 rounded text-xs shadow flex items-center gap-1">

                                    <i class="fa-solid fa-plus-circle"></i> + THÊM KHOA MỚI

                                </button>

                            </div>

                            <div class="overflow-x-auto border border-slate-200 rounded max-h-96">

                                <table class="w-full text-left text-xs">

                                    <thead class="bg-slate-50 sticky top-0"><tr><th class="p-3">Tên Khoa</th><th class="p-3 text-center border-x border-slate-200">MÃ PIN HIỆN TẠI</th><th class="p-3 text-center">Thao tác Đổi/Reset</th></tr></thead>

                                    <tbody id="bangPhanQuyenKhoa" class="bg-white divide-y divide-slate-100"></tbody>

                                </table>

                            </div>

                        </div>

                    </div>

                </div>



                <div id="tab-performance" class="hidden flex flex-col gap-6 w-full">

                    <div class="glass-panel p-5 border-t-4 border-t-emerald-600"><h3 class="text-base font-black text-slate-700 uppercase mb-4">Bảng Điểm KPI KTV Ca Trực</h3><div class="overflow-x-auto border border-slate-200 rounded"><table class="w-full text-left text-xs"><thead class="bg-slate-50"><tr><th class="p-3 text-center">Hạng</th><th class="p-3">Mã KTV</th><th class="p-3">Họ và Tên Nhân Viên</th><th class="p-3 text-center bg-slate-100">TỔNG KPI</th></tr></thead><tbody id="bangHieuSuatKTV" class="bg-white"></tbody></table></div></div>

                </div>



                <div id="tab-dashboard_tv" class="hidden p-6 bg-slate-900 text-white min-h-[650px] rounded-2xl shadow-inner">

                    <div class="flex justify-between items-center border-b border-slate-700 pb-4 mb-6">

                        <div class="flex items-center gap-3">

                            <span class="flex h-3 w-3 relative">

                                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"></span>

                                <span class="relative inline-flex rounded-full h-3 w-3 bg-emerald-500"></span>

                            </span>

                            <h1 class="text-lg font-black tracking-wider text-emerald-400 uppercase"><i class="fa-solid fa-tv mr-2"></i> TRẠM GIÁM SÁT TRUNG TÂM CSSD - PHƯƠNG NAM HOSPITAL</h1>

                        </div>

                        <div class="text-right">

                            <p id="tv_clock" class="text-xs font-mono font-bold text-slate-400"></p>

                        </div>

                    </div>



                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">

                        <div class="bg-gradient-to-br from-blue-600 to-sky-700 p-6 rounded-2xl shadow-xl border border-blue-400/30">

                            <div class="text-xs font-black opacity-90 uppercase tracking-wide text-blue-100">Mẻ Rửa Hôm Nay</div>

                            <div id="tv_meRua" class="text-5xl font-black font-mono mt-2 text-white">0</div>

                            <p class="text-[10px] opacity-70 mt-2 font-medium">Tổng chu kỳ làm sạch trong ngày</p>

                        </div>

                        <div class="bg-gradient-to-br from-indigo-600 to-purple-700 p-6 rounded-2xl shadow-xl border border-indigo-400/30">

                            <div class="text-xs font-black opacity-90 uppercase tracking-wide text-indigo-100">Mẻ Hấp Hôm Nay</div>

                            <div id="tv_meHap" class="text-5xl font-black font-mono mt-2 text-white">0</div>

                            <p class="text-[10px] opacity-70 mt-2 font-medium">Lô tiệt khuẩn đạt chuẩn đã chạy</p>

                        </div>

                        <div class="bg-gradient-to-br from-indigo-600 to-purple-700 p-6 rounded-2xl shadow-xl border border-indigo-400/30">

                            <div class="text-xs font-black opacity-90 uppercase tracking-wide text-indigo-100">Mẻ Hấp Hôm Nay</div>

                            <div id="tv_meHap2" class="text-5xl font-black font-mono mt-2 text-white">0</div>

                            <p class="text-[10px] opacity-70 mt-2 font-medium">Lô tiệt khuẩn đạt chuẩn đã chạy</p>

                        </div>

                        <div class="bg-slate-800 p-6 rounded-2xl shadow-xl border border-slate-700 flex justify-between items-center">

                            <div>

                                <div class="text-xs font-black text-slate-400 uppercase tracking-wide">Đang xử lý tại CSSD</div>

                                <div class="flex gap-6 mt-3">

                                    <div>

                                        <span class="text-[10px] text-amber-400 block font-bold uppercase">Trạm rửa</span>

                                        <span id="tv_dangRua" class="text-3xl font-black font-mono text-amber-400">0</span>

                                    </div>

                                    <div class="border-l border-slate-700 pl-6">

                                        <span class="text-[10px] text-indigo-400 block font-bold uppercase">Trong lò</span>

                                        <span id="tv_dangHap" class="text-3xl font-black font-mono text-indigo-400">0</span>

                                    </div>

                                </div>

                            </div>

                            <i class="fa-solid fa-arrows-spin text-4xl text-slate-700 animate-spin" style="animation-duration: 8s;"></i>

                        </div>

                        <div class="bg-slate-800 p-6 rounded-2xl shadow-xl border border-slate-700 flex justify-between items-center">

                            <div>

                                <div class="text-xs font-black text-slate-400 uppercase tracking-wide">Kho Sạch & Vô Khuẩn</div>

                                <div class="flex gap-4 mt-3">

                                    <div>

                                        <span class="text-[10px] text-teal-400 block font-bold uppercase">Sẵn sàng xuất</span>

                                        <span id="tv_khoVoKhuan" class="text-3xl font-black font-mono text-teal-400">0</span>

                                    </div>

                                </div>

                            </div>

                            <i class="fa-solid fa-warehouse text-3xl text-slate-700"></i>

                        </div>

                    </div>



                    <div class="bg-slate-800/60 border border-slate-700 rounded-2xl p-6">

                        <div class="flex justify-between items-center mb-4">

                            <h3 class="text-xs font-black text-slate-300 uppercase tracking-wider flex items-center gap-2"><i class="fa-solid fa-triangle-exclamation text-rose-500 animate-bounce"></i> Giám sát cảnh báo hạn sử dụng (Realtime)</h3>

                        </div>

                        <div class="flex items-center gap-6 p-4 bg-slate-900/80 rounded-xl border border-slate-700/50">

                            <div class="p-4 rounded-xl bg-slate-800 border border-slate-700 text-center min-w-[120px]">

                                <span class="text-[10px] text-slate-400 block font-black uppercase">Quá hạn HSD</span>

                                <span id="tv_canhBaoHsd" class="text-4xl font-black font-mono text-rose-500">0</span>

                            </div>

                            <p class="text-xs text-slate-400 leading-relaxed font-bold">Hệ thống tự động rà quét và cảnh báo những khay/mâm dụng cụ đã lưu kho sạch nhưng vượt quá niên hạn sử dụng của vật liệu bao bì. <br><span class="text-rose-400 font-black mt-1 block">* Đề nghị KTV thu hồi các khay báo đỏ trên hệ thống tủ để tiến hành xử lý tiệt khuẩn lại.</span></p>

                        </div>

                    </div>

                </div>

            </main>

        </div>

    </div>



    <!-- VÙNG IN TEM BIXOLON SLP-TX403 -->

    <div id="print-zone" class="hidden bg-white p-0 font-sans z-[9999]"></div>



    <div id="popupScanner" class="fixed inset-0 popup-overlay z-[99999] hidden flex items-center justify-center p-4">

        <div class="bg-white w-full max-w-sm p-4 rounded-xl shadow-2xl relative">

            <h3 class="text-sm font-black text-slate-700 uppercase border-b border-slate-100 pb-3 mb-2 flex justify-between items-center">

                <span><i class="fa-solid fa-camera text-sky-500 mr-2"></i> Quét Mã Vạch</span>

                <button onclick="dongCamera()" class="text-rose-500 hover:text-rose-700"><i class="fa-solid fa-xmark text-lg"></i></button>

            </h3>

            <div id="reader"></div>

            <p id="camStatus" class="text-[10px] text-center text-slate-500 mt-3 italic font-bold">Đang khởi động Camera Sau...</p>

        </div>

    </div>



    <div id="popupKiemDem" class="fixed inset-0 bg-slate-900/60 backdrop-blur-sm flex justify-center items-center z-[9999] hidden p-4 animate-fade-in">

        <div class="bg-white rounded-2xl shadow-2xl border border-slate-200 w-full max-w-2xl flex flex-col max-h-[90vh] overflow-hidden transform transition-all scale-100">

            <div class="bg-gradient-to-r from-sky-700 to-sky-600 px-6 py-4 flex items-center justify-between shadow-md">

                <div class="flex items-center gap-3">

                    <div class="bg-white/10 p-2 rounded-lg text-white">

                        <i class="fa-solid fa-clipboard-check text-lg"></i>

                    </div>

                    <div>

                        <h3 class="text-white font-black text-sm uppercase tracking-wide">Kiểm toán & Đếm cơ số đầu vào</h3>

                        <p class="text-sky-100 text-[11px] font-medium mt-0.5">Trạm 2: Xe thu gom nhận bàn giao dụng cụ bẩn</p>

                    </div>

                </div>

                <button type="button" onclick="closePopupKiemDem()" class="text-white/80 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-lg transition-colors">

                    <i class="fa-solid fa-xmark text-sm"></i>

                </button>

            </div>

            <div class="bg-slate-50 border-b border-slate-200 px-6 py-3 flex flex-wrap justify-between items-center gap-2">

                <div class="flex items-center gap-2">

                    <span class="text-[11px] font-bold text-slate-400 uppercase">Bộ dụng cụ:</span>

                    <span id="popBo" class="text-xs font-black text-sky-800 uppercase bg-sky-50 border border-sky-200 px-2.5 py-1 rounded">--</span>

                </div>

                <div class="flex items-center gap-2">

                    <span class="text-[11px] font-bold text-slate-400 uppercase">Khoa hoàn trả:</span>

                    <span id="popKhoa" class="text-xs font-black text-rose-700 uppercase bg-rose-50 border border-rose-100 px-2.5 py-1 rounded">--</span>

                </div>

            </div>

            <div class="flex-1 overflow-y-auto p-6 bg-slate-50/50">

                <div class="mb-3 flex justify-between items-center px-1">

                    <span class="text-[11px] font-black text-slate-500 uppercase tracking-wider"><i class="fa-solid fa-list-ol mr-1"></i>Danh mục linh kiện trong mâm</span>

                    <span class="text-[10px] bg-slate-200 text-slate-600 font-bold px-2 py-0.5 rounded-full">Đối soát Realtime</span>

                </div>

                <div id="popKiemDemChecklist" class="bg-white border border-slate-200 rounded-xl p-2 shadow-sm space-y-1 divide-y divide-slate-100"></div>

            </div>

            <div class="p-6 bg-white border-t border-slate-100 shadow-inner">

                <label class="block text-xs font-black text-slate-700 uppercase tracking-wide mb-2 flex items-center justify-between">

                    <span><i class="fa-solid fa-comment-medical text-slate-400 mr-1"></i> Nhật ký đối soát / Lý do chênh lệch</span>

                    <span class="text-[10px] text-rose-500 font-bold lowercase italic select-none">*bắt buộc nhập nếu thiếu hoặc hỏng đồ</span>

                </label>

                <textarea id="popGhiChu" rows="2" placeholder="Ví dụ: Khoa Ngoại chấn thương xác nhận làm mất 1 kẹp phẫu tích trong phòng mổ..." class="w-full text-xs font-medium p-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-sky-500 focus:border-sky-500 placeholder-slate-400 shadow-sm transition-all"></textarea>

            </div>

            <div class="bg-slate-50 px-6 py-4 border-t border-slate-200 flex justify-end items-center gap-3">

                <button type="button" onclick="closePopupKiemDem()" class="bg-white border border-slate-300 text-slate-700 hover:bg-slate-100 font-bold py-2.5 px-4 rounded-xl shadow-sm text-xs transition-colors uppercase">Hủy bỏ</button>

                <button type="button" onclick="saveKiemDem()" class="bg-gradient-to-r from-emerald-600 to-emerald-500 text-white hover:from-emerald-700 hover:to-emerald-600 font-black py-2.5 px-6 rounded-xl shadow-md text-xs transition-all uppercase tracking-wider flex items-center gap-2"><i class="fa-solid fa-share-from-square"></i> Chốt và Chuyển sang rửa</button>

            </div>

        </div>

    </div>



    <div id="popupDongGoi" class="fixed inset-0 popup-overlay z-[9999] hidden flex items-center justify-center p-4">

        <div class="glass-panel w-full max-w-lg p-6 border-t-4 border-t-sky-500">

            <h3 class="text-sm font-black text-slate-700 uppercase border-b border-slate-100 pb-3 mb-2">Đóng gói & Kiểm tra</h3>

            <div class="bg-slate-50 p-2 rounded border border-slate-100 mb-2"><span class="text-[10px] text-slate-500 uppercase font-bold">Mâm:</span> <span id="popDG_Bo" class="font-bold text-sky-700 text-sm ml-1">--</span></div>

            <div>

                <label class="block text-[11px] font-bold text-slate-500 mb-1.5">Chất liệu đóng gói & HSD tương ứng:</label>

                <select id="popDG_Loai" onchange="tinhHanSuDung()" class="w-full p-2.5 text-xs font-bold border border-slate-300 rounded bg-white text-slate-880">

                    <option value="Vải|7">Vải (HSD 7 ngày)</option>

                    <option value="Túi ép nhiệt độ cao|30" selected>Túi ép nhiệt độ cao (HSD 30 ngày)</option>

                    <option value="Hộp chuyên dụng|90">Hộp chuyên dụng (HSD 90 ngày)</option>

                    <option value="Túi ép nhiệt độ thấp (Tyvek)|90">Túi ép nhiệt độ thấp (Tyvek) (HSD 90 ngày)</option>

                    <option value="Giấy gói chuyên dụng (vải không dệt)|30">Giấy gói chuyên dụng (vải không dệt) (HSD 30 ngày)</option>

                </select>

            </div>

            <div class="text-center mt-3"><p class="text-[10px] font-bold text-slate-500 uppercase">HSD Dự kiến:</p><p id="popDG_Han" class="text-xl font-black text-sky-700 mt-1">--/--/----</p></div>

            <div class="flex space-x-2 mt-5"><button onclick="closePopupDongGoi()" class="w-1/3 bg-slate-100 text-slate-600 font-bold py-3 rounded text-xs">HỦY</button><button onclick="chotDongGoi()" class="w-2/3 btn-primary font-bold py-3 rounded text-xs shadow-md">CHỐT GÓI MÂM</button></div>

        </div>

    </div>



    <div id="popupSuDungBoDungCu" class="fixed inset-0 bg-slate-900/60 backdrop-blur-sm flex justify-center items-center z-[110] hidden">

        <div class="bg-white w-full max-w-5xl rounded-xl shadow-2xl overflow-hidden animate-in fade-in zoom-in-95 duration-200">

            <div class="bg-teal-600 text-white px-6 py-4 flex justify-between items-center">

                <h2 class="font-black text-sm flex items-center gap-2 uppercase tracking-wider"><i class="fa-solid fa-user-injured text-base"></i> Ghi Nhận Sử Dụng Bộ Dụng Cụ Trên Bệnh Nhân</h2>

                <button onclick="closePopupSuDung()" type="button" class="text-white/80 hover:text-white font-bold text-2xl outline-none">&times;</button>

            </div>

            <div class="p-6 space-y-4 max-h-[80vh] overflow-y-auto">

                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">

                    <div>

                        <label class="text-[11px] font-black text-slate-500 uppercase">Khoa phòng bệnh nhân <span class="text-rose-500 font-bold">*</span></label>

                        <input type="text" id="sd_khoaBenhNhan" class="w-full border-b-2 border-slate-200 focus:border-teal-500 outline-none py-1.5 text-xs font-bold text-slate-800 transition-colors">

                    </div>

                    <div>

                        <label class="text-[11px] font-black text-slate-500 uppercase">Điều dưỡng vòng trong (Phòng mổ) <span class="text-rose-500 font-bold">*</span></label>

                        <input type="text" id="sd_yTaPhongMo" class="w-full border-b-2 border-slate-200 focus:border-teal-500 outline-none py-1.5 text-xs font-bold text-slate-800 transition-colors">

                    </div>

                    <div>

                        <label class="text-[11px] font-black text-slate-500 uppercase">Điều dưỡng vòng ngoài </label>

                        <input type="text" id="sd_yTaVongNgoai" class="w-full border-b-2 border-slate-200 focus:border-teal-500 outline-none py-1.5 text-xs font-medium text-slate-800 transition-colors">

                    </div>

                </div>

                <div class="flex flex-col md:flex-row gap-4 items-end bg-slate-50 p-3 rounded-lg border border-slate-200">

                    <div class="flex-1 w-full">

                        <label class="text-[11px] font-black text-slate-600 uppercase">Thông Tin Xác Thực Bệnh Nhân (Mã MRN / Họ Tên) <span class="text-rose-500 font-bold">*</span></label>

                        <div class="relative mt-1">

                            <input type="text" id="sd_searchBN" placeholder="Nhập mã bệnh án MRN hoặc tên bệnh nhân..." class="w-full border border-slate-300 rounded px-3 py-2 pl-9 text-xs font-bold text-teal-900 outline-none focus:border-teal-500 bg-white">

                            <i class="fa-solid fa-search absolute left-3 top-2.5 text-slate-400 text-xs"></i>

                        </div>

                    </div>

                    <label class="flex items-center gap-2 pb-2.5 cursor-pointer text-xs font-bold text-slate-600 select-none shrink-0">

                        <input type="checkbox" id="sd_chkXuatVien" class="w-4 h-4 rounded text-teal-600 focus:ring-teal-500"> Bao gồm hồ sơ đã xuất viện

                    </label>

                </div>

                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">

                    <div>

                        <label class="text-[11px] font-black text-slate-500 uppercase">Ngày phẫu thuật / sử dụng</label>

                        <input type="date" id="sd_ngaySuDung" class="w-full border-b-2 border-slate-200 outline-none py-1 text-xs font-bold text-slate-700">

                    </div>

                    <div>

                        <label class="text-[11px] font-black text-slate-500 uppercase">Nhân chứng thực hiện <span class="text-rose-500 font-bold">*</span></label>

                        <input type="text" id="sd_nhanChung" readonly class="w-full border-b-2 border-slate-100 bg-slate-50/50 outline-none py-1 text-xs font-black text-slate-600">

                    </div>

                    <div>

                        <label class="text-[11px] font-black text-slate-500 uppercase">Ghi chú lâm sàng bổ sung</label>

                        <input type="text" id="sd_ghiChu" class="w-full border-b-2 border-slate-200 focus:border-teal-500 outline-none py-1 text-xs text-slate-700 transition-colors">

                    </div>

                </div>

                <div class="pt-2 border-t border-dashed border-slate-200">

                    <label class="text-[11px] font-black text-teal-800 uppercase flex items-center gap-1"><i class="fa-solid fa-qrcode"></i> Quét Hoặc Nhập Mã Vạch Khay Dụng Cụ Phục Vụ Ca Mổ:</label>

                    <div class="flex gap-2 mt-1">

                        <input type="text" id="sd_maKhayInp" onkeypress="if(event.key==='Enter') scanKhayVaoSuDung()" placeholder="Quét barcode định hình dán trên tem khay vô khuẩn..." class="w-full border border-slate-300 rounded px-3 py-2.5 font-mono font-black text-sm bg-amber-50/60 text-amber-900 uppercase focus:border-teal-500 outline-none tracking-wider">

                        <button onclick="moCamera('sd_maKhayInp')" type="button" class="bg-slate-800 hover:bg-slate-900 text-white px-5 rounded shadow"><i class="fa-solid fa-camera"></i></button>

                    </div>

                </div>

                <div class="border border-slate-200 rounded-lg overflow-hidden mt-3 shadow-sm bg-white">

                    <table class="w-full text-left text-xs border-collapse">

                        <thead>

                            <tr class="bg-slate-100 border-b font-black text-slate-600 text-[10px] uppercase tracking-wide">

                                <th class="p-3 text-center w-12">Chọn</th>

                                <th class="p-3 w-[25%]">Mã định hình khay</th>

                                <th class="p-3 w-[40%]">Tên Bộ Dụng Cụ Lâm Sàng</th>

                                <th class="p-3 text-center w-[15%]">Số mẻ hấp</th>

                                <th class="p-3 text-center w-[12%]">Trạng thái</th>

                                <th class="p-3 text-center w-10">Xóa</th>

                            </tr>

                        </thead>

                        <tbody id="sd_bangKhayChon" class="divide-y divide-slate-100 text-slate-700">

                            <tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng quét mã khay dụng cụ để liên kết vào hồ sơ...</td></tr>

                        </tbody>

                    </table>

                </div>

            </div>

            <div class="bg-slate-50 px-6 py-4 flex justify-end gap-3 border-t border-slate-200 shadow-inner">

                <button onclick="savePopupSuDung()" type="button" class="bg-teal-600 hover:bg-teal-700 text-white font-black px-6 py-2.5 rounded shadow text-xs uppercase flex items-center gap-1.5 transition-all"><i class="fa-solid fa-floppy-disk text-sm"></i> Lưu Hồ Sơ Bệnh Án</button>

                <button onclick="closePopupSuDung()" type="button" class="bg-white hover:bg-slate-100 border text-slate-700 font-bold px-6 py-2.5 rounded shadow text-xs uppercase flex items-center gap-1.5 transition-all"><i class="fa-solid fa-rotate-left"></i> Hủy</button>

            </div>

        </div>

    </div>



    <!-- HÀM JAVASCRIPT HỖ TRỢ CHUYỂN TABS -->

    <script>

        function switchLamsangSubtab(activeTabId) {

            const tabs = ['lssub-tab-tra-ban', 'lssub-tab-nhan-sach', 'lssub-tab-doi-luu'];

            

            tabs.forEach(tabId => {

                const el = document.getElementById(tabId);

                if (el) el.classList.add('hidden');

            });

            

            const activeEl = document.getElementById(activeTabId);

            if (activeEl) activeEl.classList.remove('hidden');

            

            const btnTraBan = document.getElementById('btn-lssub-tra-ban');

            const btnNhanSach = document.getElementById('btn-lssub-nhan-sach');

            const btnDoiLuu = document.getElementById('btn-lssub-doi-luu');

            

            [btnTraBan, btnNhanSach, btnDoiLuu].forEach(btn => {

                if(btn) {

                    btn.className = "px-4 py-2 text-xs font-black rounded-lg transition-all text-slate-600 hover:text-slate-900 flex items-center gap-1.5";

                }

            });

            

            const activeBtn = document.getElementById('btn-' + activeTabId);

            if (activeBtn) {

                if (activeTabId === 'lssub-tab-tra-ban') {

                    activeBtn.className = "px-4 py-2 text-xs font-black rounded-lg transition-all flex items-center gap-1.5 bg-white text-rose-700 shadow-sm border border-slate-200";

                } else if (activeTabId === 'lssub-tab-nhan-sach') {

                    activeBtn.className = "px-4 py-2 text-xs font-black rounded-lg transition-all flex items-center gap-1.5 bg-white text-emerald-700 shadow-sm border border-slate-200";

                } else if (activeTabId === 'lssub-tab-doi-luu') {

                    activeBtn.className = "px-4 py-2 text-xs font-black rounded-lg transition-all flex items-center gap-1.5 bg-white text-teal-700 shadow-sm border border-slate-200";

                }

            }

        }

        

        setInterval(() => {

            const timeString = new Date().toLocaleString('vi-VN');

            const tvClock = document.getElementById('tv_clock');

            if(tvClock) tvClock.innerText = "THỜI GIAN THỰC: " + timeString;



            const mainClock = document.getElementById('clock');

            if(mainClock) mainClock.innerText = "THỜI GIAN: " + timeString;

        }, 1000);

    </script>

    <script src="app.js"></script>

</body>

</html>

Chào anh Hùng,

Sau khi kiểm tra cấu trúc file index.html (giao diện Phương Nam Hospital V29.0) mà anh vừa gửi, em phát hiện ra hai điểm lệch quan trọng gây gián đoạn quy trình vận hành và ký nhận điện tử:

Thiếu Popup Ký Chữ Ký Điện Tử Canvas (#popupKyDienTu): Trong file app.js chúng ta đã xây dựng toàn bộ logic vẽ nét ký trực tiếp trên màn hình cảm ứng/chuột và lưu ảnh Base64 lên Firestore. Tuy nhiên, trong index.html chưa có khung Popup HTML này, dẫn đến khi Điều dưỡng bấm "KÝ NHẬN VÀO TỦ KHOA" thì hệ thống phải chạy cơ chế fallback confirm() thô sơ chứ không mở được bảng vẽ chữ ký.

Trùng lặp ID Khai báo mâm (#khoa_inpMaBo): Ở Trạm 1 (Cổng lâm sàng), cả sub-tab "1. Báo trả đồ bẩn" và sub-tab "2. Yêu cầu & Nhận sạch" đều đang sử dụng chung ID id="khoa_inpMaBo". Việc trùng ID HTML này khiến JavaScript lấy sai giá trị khi Điều dưỡng thao tác giữa trả mâm bẩn và đăng ký cấp mâm mới.

File index.html đã được chỉnh sửa & tối ưu hoàn chỉnh
Dưới đây là toàn bộ mã nguồn index.html đã được bổ sung Popup Canvas ký tên chuẩn y tế, sửa triệt để lỗi trùng ID và sẵn sàng kết nối hoàn hảo với file app.js:

HTML
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Hệ Thống CSSD - V29.0 (Giao Diện Chuẩn Phương Nam)</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/jsbarcode@3.11.5/dist/JsBarcode.all.min.js"></script>
    <script src="https://unpkg.com/html5-qrcode"></script>
    <script src="https://www.gstatic.com/firebasejs/10.12.2/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore-compat.js"></script>

    <link rel="stylesheet" href="style.css">
    <style>
        /* Cấu hình nâng cao cho vùng in tem nhiệt Bixolon SLP-TX403 (80mm x 50mm) & In Biên Bản A4 */
        @media print {
            body * {
                visibility: hidden !important;
            }
            #print-zone, #print-zone * {
                visibility: visible !important;
            }
            #print-zone {
                position: absolute !important;
                left: 0 !important;
                top: 0 !important;
                margin: 0 !important;
                padding: 0 !important;
            }

            /* Chế độ in tem Bixolon */
            body.print-mode-bixolon #print-zone {
                width: 80mm !important;
            }
            body.print-mode-bixolon @page {
                size: 80mm 50mm;
                margin: 0mm;
            }

            /* Chế độ in văn bản A4 */
            body.print-mode-doc #print-zone {
                width: 100% !important;
            }
            body.print-mode-doc @page {
                size: A4 portrait;
                margin: 15mm;
            }

            .bixolon-label {
                width: 80mm !important;
                height: 50mm !important;
                padding: 3mm 4mm !important;
                box-sizing: border-box !important;
                page-break-after: always;
                display: flex !important;
                flex-direction: column !important;
                justify-content: space-between !important;
                font-family: Arial, sans-serif !important;
                background: #fff !important;
                color: #000 !important;
            }
        }
    </style>
</head>
<body class="flex h-screen overflow-hidden relative">

    <div id="mobile-overlay" class="fixed inset-0 bg-slate-900/50 z-40 hidden md:hidden" onclick="toggleMobileMenu()"></div>

    <!-- MH ĐĂNG NHẬP -->
    <div id="login-screen" class="fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-100 bg-opacity-95 backdrop-blur-sm">
        <div class="glass-panel w-full max-w-sm p-8 text-center shadow-xl border-t-4 border-t-sky-600">
            <div class="mb-6">
                <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-sky-50 border border-sky-100 mb-4"><i class="fa-solid fa-hospital text-3xl text-sky-600"></i></div>
                <h1 class="text-xl font-black text-slate-800 uppercase tracking-widest">PHUONG NAM</h1>
                <p class="text-xs text-slate-500 font-bold tracking-widest mt-1">CSSD PLATFORM</p>
            </div>
            <div class="space-y-4 text-left">
                <div>
                    <label class="text-[10px] font-black text-slate-500 uppercase tracking-wide">Vai trò đăng nhập:</label>
                    <select id="login_role" onchange="toggleLoginFields()" class="w-full px-3 py-2.5 mt-1 font-bold shadow-sm">
                        <option value="KHOA">Điều Dưỡng Lâm Sàng</option>
                        <option value="CSSD">Chuyên Viên CSSD (User Riêng)</option>
                        <option value="ADMIN">Quản Trị Hệ Thống</option>
                        <option value="GUEST" class="text-emerald-600 font-black">Khách Tham Quan (View-Only)</option>
                    </select>
                </div>
                <div id="field_khoa">
                    <label class="text-[10px] font-black text-slate-500 uppercase tracking-wide">Khoa phòng:</label>
                    <select id="login_khoa" class="w-full px-3 py-2.5 mt-1 font-bold shadow-sm"><option value="">-- Đang tải dữ liệu... --</option></select>
                </div>
                <div id="field_nhanvien_cssd" class="hidden">
                    <label class="text-[10px] font-black text-slate-500 uppercase tracking-wide">Chọn Nhân Viên Vận Hành:</label>
                    <select id="login_nv_cssd" class="w-full px-3 py-2.5 mt-1 font-bold shadow-sm text-sky-700 bg-sky-50"><option value="">-- Đang tải danh sách KTV... --</option></select>
                </div>
                <div>
                    <label class="text-[10px] font-black text-slate-500 uppercase tracking-wide">Mã PIN xác thực:</label>
                    <input type="password" id="login_pass" class="w-full px-3 py-2.5 mt-1 text-center tracking-widest shadow-sm" placeholder="••••" onkeypress="if(event.key==='Enter') checkLogin()">
                </div>
                <button onclick="checkLogin()" class="w-full btn-primary font-black py-3 rounded-lg mt-4 shadow-md"><i class="fa-solid fa-shield-halved mr-2"></i> TRUY CẬP</button>
            </div>
        </div>
    </div>

    <!-- GIAO DIỆN CHÍNH -->
    <div id="main-app" class="hidden w-full h-full flex flex-row">
        <!-- SIDEBAR -->
        <div id="sidebar_menu" class="w-64 bg-white flex flex-col justify-between flex-shrink-0 z-50 shadow-lg border-r border-slate-200 fixed inset-y-0 left-0 transform -translate-x-full md:relative md:translate-x-0 transition-transform duration-300">
            <div class="overflow-y-auto flex-1 scrollbar-none">
                <div class="p-4 flex items-center justify-between border-b border-slate-100 bg-white sticky top-0 z-10"><div class="flex flex-col"><span class="text-sm text-sky-700 font-black tracking-widest">PN HOSPITAL</span><span id="nav_user_info" class="text-[10px] text-slate-500 font-bold mt-0.5">Role: Đang tải...</span></div><button onclick="toggleMobileMenu()" class="md:hidden text-slate-400 hover:text-slate-700 text-xl"><i class="fa-solid fa-bars"></i></button></div>
                
                <div id="header-lamsang" class="px-4 py-3 text-[10px] uppercase text-slate-400 font-bold tracking-wider">Lâm Sàng</div>
                <nav class="flex flex-col px-2 space-y-0.5">
                    <a href="#" onclick="switchTab('khoaphong'); toggleMobileMenu();" id="menu-khoaphong" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition"><i class="fa-solid fa-laptop-medical w-4 text-center"></i> <span>1. Cổng Lâm Sàng</span></a>
                    <a href="#" onclick="moPopupSuDungBoDungCu(); toggleMobileMenu();" id="menu-sudung-bn" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-bold text-teal-700 hover:bg-teal-50 transition"><i class="fa-solid fa-user-injured w-4 text-center text-teal-600"></i> <span>1.1 Ghi Nhận Sử Dụng (BN)</span></a>
                </nav>
                
                <div id="header-vanhanh" class="px-4 py-3 text-[10px] uppercase text-slate-400 font-bold tracking-wider">Vận Hành Tiệt Khuẩn</div>
                <nav class="flex flex-col px-2 space-y-0.5">
                    <a href="#" onclick="switchTab('thugom'); toggleMobileMenu();" id="menu-thugom" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition"><i class="fa-solid fa-truck-medical w-4 text-center"></i> <span>2. Điều Phối Xe Thu Gom</span></a>
                    <a href="#" onclick="switchTab('mayrua'); toggleMobileMenu();" id="menu-mayrua" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition"><i class="fa-solid fa-soap w-4 text-center text-sky-600"></i> <span class="font-bold text-slate-700">3. Quản Lý Mẻ Rửa</span></a>
                    <a href="#" onclick="switchTab('donggoi'); toggleMobileMenu();" id="menu-donggoi" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition"><i class="fa-solid fa-hands-bubbles w-4 text-center"></i> <span>4. Trạm Làm Sạch & Gói</span></a>
                    <a href="#" onclick="switchTab('mayhap'); toggleMobileMenu();" id="menu-mayhap" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition"><i class="fa-solid fa-server w-4 text-center"></i> <span>5. Quản Lý Mẻ Hấp</span></a>
                    <a href="#" onclick="switchTab('khovokhuan'); toggleMobileMenu();" id="menu-khovokhuan" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition"><i class="fa-solid fa-door-closed w-4 text-center"></i> <span>6. Kho Vô Khuẩn (Xuất)</span></a>
                </nav>
                
                <div id="header-dulieu" class="px-4 py-3 text-[10px] uppercase text-slate-400 font-bold tracking-wider border-t border-slate-100 mt-2">Dữ Liệu & Tồn Kho</div>
                <nav class="flex flex-col px-2 space-y-0.5">
                    <a href="#" onclick="switchTab('quanlykho'); toggleMobileMenu();" id="menu-quanlykho" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-bold text-slate-700 transition hover:bg-slate-50 bg-amber-50/50"><i class="fa-solid fa-boxes-stacked w-4 text-center text-amber-600"></i> <span class="text-amber-900">7. Tồn Kho Dụng Cụ</span></a>
                    <a href="#" onclick="switchTab('danhmuc'); toggleMobileMenu();" id="menu-danhmuc" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition hover:bg-slate-50"><i class="fa-solid fa-microchip w-4 text-center"></i> <span>8. Giám Sát Tuổi Thọ</span></a>
                    <a href="#" onclick="switchTab('lichsuluanchuyen'); toggleMobileMenu();" id="menu-lichsuluanchuyen" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-bold text-slate-600 transition hover:bg-slate-50"><i class="fa-solid fa-history w-4 text-center text-sky-600"></i> <span class="text-sky-900">9. Nhật Ký Luân Chuyển</span></a>
                    <a href="#" onclick="switchTab('tracuu'); toggleMobileMenu();" id="menu-tracuu" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition hover:bg-slate-50"><i class="fa-solid fa-gears w-4 text-center"></i> <span>10. Cấu Hình Hệ Thống</span></a>
                    <a href="#" onclick="switchTab('performance'); toggleMobileMenu();" id="menu-performance" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-semibold text-slate-600 transition hover:bg-slate-50"><i class="fa-solid fa-chart-line w-4 text-center text-emerald-600"></i> <span class="font-bold text-slate-700">11. Hiệu Suất KPI</span></a>
                    <a href="#" onclick="switchTab('dashboard_tv'); toggleMobileMenu();" id="menu-dashboard_tv" class="flex items-center space-x-3 px-3 py-2.5 rounded-md text-[13px] font-bold text-slate-600 transition hover:bg-slate-50 border-t border-slate-100 hidden"><i class="fa-solid fa-tv w-4 text-center text-rose-600"></i> <span class="text-rose-900">12. MÀN HÌNH TRỰC TIVI</span></a>
                </nav>
            </div>
            <div class="p-4 border-t border-slate-100 bg-slate-50 shrink-0"><button onclick="location.reload()" class="w-full text-xs text-slate-500 hover:text-slate-800 bg-white border border-slate-200 py-2 rounded font-bold hover:shadow-sm"><i class="fa-solid fa-arrow-right-from-bracket mr-2"></i>Đăng xuất</button></div>
        </div>

        <!-- MAIN CONTENT AREA -->
        <div class="flex-1 flex flex-col overflow-y-auto w-full h-full relative bg-slate-100 pb-10" id="mainContentScroll">
            <header class="bg-white px-6 py-3.5 flex justify-between items-center z-10 sticky top-0 border-b border-slate-200 shadow-sm"><div class="flex items-center"><button onclick="toggleMobileMenu()" class="md:hidden text-slate-500 hover:text-sky-600 text-xl mr-4"><i class="fa-solid fa-bars"></i></button><h2 id="header-title" class="text-sm font-black text-slate-700 uppercase tracking-widest">Hệ Thống Quản Lý CSSD</h2></div><div id="clock" class="text-[11px] font-mono text-slate-500 font-bold hidden sm:block"></div></header>

            <main class="p-4 md:p-6 w-full space-y-6 flex-1 z-10 max-w-7xl mx-auto">
                
                <!-- ========================================== -->
                <!-- TRẠM 1: CỔNG LÂM SÀNG -->
                <!-- ========================================== -->
                <div id="tab-khoaphong" class="hidden flex flex-col gap-6 animate-fade-in">
                    
                    <div class="glass-panel p-4 bg-white shadow-sm border-l-4 border-l-sky-600 flex flex-col md:flex-row md:items-center justify-between gap-4">
                        <div class="w-full md:w-1/3">
                            <label class="block text-[10px] font-black text-slate-500 mb-1 uppercase tracking-wider">Đơn Vị Chỉ Định Hiện Tại:</label>
                            <select id="khoa_selKhoa" onchange="loadBoDungCuTheoKhoa(); clearGioHang(); callRender();" class="w-full px-3 py-2 text-xs font-black shadow-sm bg-slate-50 border border-slate-300 rounded-lg text-sky-800">
                                <option value="">-- Đang tải dữ liệu khoa phòng... --</option>
                            </select>
                        </div>

                        <div class="flex bg-slate-100 p-1 rounded-xl border border-slate-200 w-full md:w-auto shrink-0 shadow-inner">
                            <button onclick="switchLamsangSubtab('lssub-tab-tra-ban')" id="btn-lssub-tra-ban" class="px-4 py-2 text-xs font-black rounded-lg transition-all flex items-center gap-1.5 bg-white text-rose-700 shadow-sm border border-slate-200">
                                <i class="fa-solid fa-box-tissue"></i> 1. BÁO TRẢ ĐỒ BẨN
                            </button>
                            <button onclick="switchLamsangSubtab('lssub-tab-nhan-sach')" id="btn-lssub-nhan-sach" class="px-4 py-2 text-xs font-black rounded-lg transition-all text-slate-600 hover:text-slate-900 flex items-center gap-1.5">
                                <i class="fa-solid fa-square-envelope"></i> 2. YÊU CẦU & NHẬN SẠCH
                            </button>
                            <button onclick="switchLamsangSubtab('lssub-tab-doi-luu')" id="btn-lssub-doi-luu" class="px-4 py-2 text-xs font-black rounded-lg transition-all text-slate-600 hover:text-slate-900 flex items-center gap-1.5">
                                <i class="fa-solid fa-file-invoice"></i> 3. SỔ GIAO NHẬN
                            </button>
                        </div>
                    </div>

                    <!-- SUBTAB 1.1: BÁO TRẢ ĐỒ BẨN -->
                    <div id="lssub-tab-tra-ban" class="grid grid-cols-1 md:grid-cols-3 gap-6 transition-all duration-200">
                        <div class="glass-panel p-5 border-t-4 border-t-rose-500 bg-white shadow-sm md:col-span-1 flex flex-col justify-between min-h-[350px]">
                            <div class="border-b border-slate-100 pb-3">
                                <h3 class="text-sm font-black text-rose-800 uppercase flex items-center gap-2">
                                    <i class="fa-solid fa-box-tissue"></i> 1. Khai Báo Mâm Bẩn
                                </h3>
                                <p class="text-[10px] text-slate-400 font-medium mt-0.5">Quét hoặc chọn các khay mâm bẩn vừa dùng xong cần hoàn trả</p>
                            </div>
                            <div class="space-y-4 my-4 flex-1">
                                <div class="bg-rose-50/50 p-4 rounded-xl border border-rose-100 space-y-3 shadow-inner">
                                    <label class="block text-[10px] uppercase font-black text-rose-900"><i class="fa-solid fa-qrcode mr-1"></i> Chọn khay dụng cụ bẩn:</label>
                                    <div class="flex gap-2">
                                        <input type="text" id="khoa_inpMaBo" list="listBoDungCu" placeholder="Gõ tên mâm bẩn hoặc quét mã..." class="w-full px-3 py-2 text-xs font-bold text-rose-950 shadow-sm border border-rose-300 rounded-md bg-white focus:ring-2 focus:ring-rose-500 outline-none" onkeypress="if(event.key==='Enter') themVaoGio()">
                                        <datalist id="listBoDungCu"></datalist>
                                        <button onclick="moCamera('khoa_inpMaBo')" class="bg-rose-600 hover:bg-rose-700 text-white px-3.5 py-2 rounded-md shadow font-bold text-xs shrink-0"><i class="fa-solid fa-camera"></i></button>
                                    </div>
                                    <button onclick="themVaoGio()" class="w-full bg-slate-800 hover:bg-slate-700 text-white font-black py-2.5 rounded-lg text-xs shadow uppercase tracking-wider"><i class="fa-solid fa-cart-plus mr-1"></i> Đưa Vào Danh Sách Trả</button>
                                </div>
                            </div>
                        </div>

                        <div class="glass-panel p-5 border-t-4 border-t-slate-400 bg-white shadow-sm md:col-span-2 flex flex-col min-h-[350px]">
                            <div class="flex justify-between items-center border-b border-slate-100 pb-3 mb-4">
                                <h4 class="text-xs font-black text-slate-700 uppercase"><i class="fa-solid fa-basket-shopping text-rose-500 mr-1.5"></i> Danh sách mâm bẩn đang chờ phát lệnh:</h4>
                                <span id="badgeGioHang" class="bg-rose-100 text-rose-700 text-[10px] px-2.5 py-0.5 rounded-full font-black">0 món</span>
                            </div>
                            <div class="flex-1 overflow-auto rounded-lg border border-slate-200 bg-white min-h-[150px] mb-4">
                                <table class="w-full text-left text-xs">
                                    <tbody id="bangGioHang" class="divide-y divide-slate-100 text-slate-700 font-medium"></tbody>
                                </table>
                            </div>
                            <button id="btn-XacNhanGoi" onclick="khoaGuiPhieuTraBatches()" class="w-full bg-rose-600 hover:bg-rose-700 text-white font-black py-3 rounded-xl text-xs shadow-md transition-all uppercase tracking-wider"><i class="fa-solid fa-bell mr-1.5"></i> PHÁT LỆNH THU GOM ĐỒ BẨN</button>
                        </div>
                    </div>

                    <!-- SUBTAB 1.2: YÊU CẦU CẤP MỚI & NHẬN SẠCH -->
                    <div id="lssub-tab-nhan-sach" class="grid grid-cols-1 lg:grid-cols-3 gap-6 transition-all duration-200 hidden">
                        <div class="glass-panel p-5 flex flex-col border-t-4 border-t-emerald-500 bg-white shadow-sm h-[380px]">
                            <div class="border-b border-slate-100 pb-3 mb-4">
                                <h3 class="text-sm font-black text-emerald-800 uppercase flex items-center gap-2">
                                    <i class="fa-solid fa-cart-flatbed-suitcases"></i> Đăng Ký Đồ Sạch Mới
                                </h3>
                                <p class="text-[10px] text-slate-400 font-medium mt-0.5">Dành cho đăng ký mượn mới hoặc cấp bù mâm vô khuẩn độc lập</p>
                            </div>
                            <div class="space-y-4 flex-1 flex flex-col justify-between">
                                <div class="bg-emerald-50/50 p-4 rounded-xl border border-emerald-100 space-y-3 shadow-inner">
                                    <label class="block text-[10px] uppercase font-black text-emerald-900">Chọn loại mâm cần cấp mới:</label>
                                    <input type="text" id="khoa_inpMaBoCapMoi" list="listBoDungCu" placeholder="Chọn mâm từ danh mục chuẩn..." class="w-full px-3 py-2.5 text-xs font-bold text-emerald-900 shadow-sm border border-emerald-300 rounded-md bg-white focus:ring-2 focus:ring-emerald-500 outline-none">
                                    <button onclick="khoaGuiYeuCauCapPhat()" class="w-full bg-emerald-600 hover:bg-emerald-700 text-white font-black py-3 rounded-lg text-xs shadow transition-all uppercase tracking-wider"><i class="fa-solid fa-paper-plane mr-1"></i> GỬI YÊU CẦU CẤP MỚI</button>
                                </div>
                                <div class="p-3 bg-slate-50 border border-slate-200 rounded-lg text-[10.5px] text-slate-500 leading-relaxed font-bold">
                                    <i class="fa-solid fa-info-circle text-emerald-600 mr-1"></i> Yêu cầu cấp mới sau khi gửi sẽ được treo thẳng lên màn hình giám sát tại <strong class="text-emerald-700">Trạm 6 (Kho Vô Khuẩn)</strong> để CSSD đóng bàn giao.
                                </div>
                            </div>
                        </div>

                        <div class="glass-panel p-5 flex flex-col border-t-4 border-t-amber-500 bg-white shadow-sm h-[380px]">
                            <div class="border-b border-slate-100 pb-2.5 mb-3">
                                <h3 class="text-xs font-black text-slate-700 uppercase flex items-center gap-1.5"><i class="fa-solid fa-hourglass-half text-amber-500"></i> Các yêu cầu đang chờ cấp phát</h3>
                            </div>
                            <div class="flex-1 overflow-auto rounded-lg border border-slate-200 bg-white">
                                <table class="w-full text-left text-[11px]">
                                    <thead class="bg-slate-50 sticky top-0 shadow-sm z-10 font-bold border-b border-slate-200 text-slate-600">
                                        <tr>
                                            <th class="p-2.5">Tên mâm</th>
                                            <th class="p-2.5 text-center">Trạng thái</th>
                                            <th class="p-2.5 text-center">Giờ gửi</th>
                                            <th class="p-2.5 text-center">Y tá</th>
                                        </tr>
                                    </thead>
                                    <tbody id="bangChoCapPhatTaiKhoa" class="divide-y divide-slate-100 text-slate-700">
                                        <tr><td colspan="4" class="p-4 text-center text-slate-400 italic">Không có yêu cầu mượn khay mới nào đang chờ duyệt.</td></tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>

                        <div class="glass-panel p-5 flex flex-col border-t-4 border-t-purple-600 bg-white shadow-sm h-[380px]">
                            <div class="flex justify-between items-center border-b border-slate-100 pb-2.5 mb-3">
                                <h3 class="text-xs font-black text-slate-700 uppercase flex items-center gap-1.5"><i class="fa-solid fa-file-signature text-purple-600"></i> Đồ sạch đang về khoa (<span id="badgeChoNhanKhoa" class="text-purple-700 font-bold">0 mâm</span>)</h3>
                                <span id="txtNguoiDungNhanHienTai" class="text-[10px] font-mono font-black text-slate-400 bg-slate-100 px-2 py-0.5 rounded">--</span>
                            </div>
                            <div class="flex-1 overflow-auto rounded-lg border border-slate-200 bg-white mb-3">
                                <table class="w-full text-left text-[11px]">
                                    <thead class="bg-slate-50 sticky top-0 shadow-sm z-10 font-bold text-slate-600 border-b">
                                        <tr>
                                            <th class="p-2 text-center w-10">Tích</th>
                                            <th class="p-2">Mã ID Khay</th>
                                            <th class="p-2">Tên Mâm Dụng Cụ</th>
                                        </tr>
                                    </thead>
                                    <tbody id="bangChoNhanTaiKhoa" class="divide-y divide-slate-100 text-slate-700">
                                        <tr><td colspan="3" class="p-4 text-center text-slate-400 italic">Hiện không có dụng cụ nào đang chuyển về khoa.</td></tr>
                                    </tbody>
                                </table>
                            </div>
                            <button onclick="khoaKyNhanDoSachDienTu()" class="w-full bg-purple-600 hover:bg-purple-700 text-white font-black py-2.5 rounded-xl shadow text-xs uppercase tracking-wider flex items-center justify-center gap-1.5 transition-all"><i class="fa-solid fa-pen-fancy"></i> KÝ NHẬN VÀO TỦ KHOA</button>
                        </div>
                    </div>

                    <!-- SUBTAB 1.3: SỔ GIAO NHẬN -->
                    <div id="lssub-tab-doi-luu" class="glass-panel p-5 flex flex-col border-t-4 border-t-teal-600 bg-white shadow-sm h-[420px] transition-all duration-200 hidden animate-fade-in">
                        <div class="flex justify-between items-start border-b border-slate-100 pb-3 mb-4">
                            <div>
                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-2"><i class="fa-solid fa-file-invoice text-teal-600"></i> Sổ Tay Giao Nhận Của Khoa</h3>
                                <p class="text-[10px] text-slate-400 font-bold mt-0.5">Bảng đối soát nợ nần, bàn giao hai chiều bẩn - sạch thực tế với CSSD</p>
                            </div>
                            <button onclick="inHoaDonGiaoNhan()" class="text-[11px] bg-white border border-slate-300 hover:border-teal-600 hover:text-teal-700 text-slate-700 px-3 py-2 rounded-lg shadow-sm font-black transition-all"><i class="fa-solid fa-print mr-1"></i> IN BIÊN BẢN GIAO NHẬN</button>
                        </div>
                        <div class="flex-1 overflow-auto rounded-xl border border-slate-200 bg-white">
                            <table class="w-full text-left text-xs">
                                <thead class="bg-slate-50 sticky top-0 shadow-sm z-10 font-bold border-b border-slate-200">
                                    <tr>
                                        <th class="p-3 w-[40%] border-r border-slate-200 text-slate-600">Phân Loại Mâm / Loại Dụng Cụ</th>
                                        <th class="p-3 text-center w-[15%] text-rose-600 bg-rose-50/40 border-r border-slate-200">Đã Trả Bẩn</th>
                                        <th class="p-3 text-center w-[15%] text-sky-600 bg-sky-50/40 border-r border-slate-200">Đã Nhận Sạch</th>
                                        <th class="p-3 text-center w-[15%] text-purple-600 bg-purple-50/40 border-r border-slate-200">Nhận Vô Khuẩn</th>
                                        <th class="p-3 text-center w-[15%] text-amber-600 bg-amber-50/40">CSSD Nợ Khoa</th>
                                    </tr>
                                </thead>
                                <tbody id="bangDonGiaoNhan" class="divide-y divide-slate-100 text-slate-700"></tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- TRẠM 2: XE THU GOM -->
                <div id="tab-thugom" class="hidden h-full">
                    <div class="glass-panel p-5 flex flex-col h-[650px] border-t-4 border-t-sky-500 bg-white">
                        <div class="flex flex-col md:flex-row justify-between items-start md:items-center border-b border-slate-100 pb-4 mb-4 gap-3">
                            <div><h3 class="text-base font-black text-slate-700 uppercase"><i class="fa-solid fa-truck-medical mr-2 text-sky-500"></i> Điều phối Xe Thu Gom</h3></div>
                            <div class="flex flex-wrap items-center gap-2 w-full md:w-auto"><select id="filterKhoaThuGom" onchange="callRender()" class="rounded px-3 py-1.5 text-xs font-bold border border-slate-200"><option value="">-- Lọc theo Khoa --</option></select><span id="badgeSoCho" class="bg-slate-100 text-slate-600 text-xs font-bold px-2 py-1.5 rounded">0 Lệnh</span></div>
                        </div>
                        <div class="flex-1 overflow-auto bg-white rounded border border-slate-100">
                            <table class="w-full text-left text-xs">
                                <thead class="sticky top-0 shadow-sm bg-slate-50">
                                    <tr>
                                        <th class="p-3 w-[20%]">Từ Khoa & Ghi chú</th>
                                        <th class="p-3 w-[45%]">Bảng Checklist Chi Tiết</th>
                                        <th class="p-3 text-center w-[15%]">Thời Gian</th>
                                        <th class="p-3 text-center action-col w-[20%]">Thao tác</th>
                                    </tr>
                                </thead>
                                <tbody id="bangChoThuGom" class="divide-y divide-slate-50 text-slate-700"></tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- TRẠM 3: MÁY RỬA -->
                <div id="tab-mayrua" class="hidden flex flex-col gap-6 w-full">
                    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 min-h-[480px]">
                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-slate-400 bg-white shadow-sm">
                            <div class="flex justify-between items-center border-b border-slate-100 pb-3 mb-4">
                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">
                                    <i class="fa-solid fa-soap text-sky-500"></i> Danh sách mâm dụng cụ chờ rửa
                                </h3>
                                <span id="badgeChoRua" class="text-xs font-bold text-sky-700 bg-sky-50 px-2 py-1 rounded">0 Mục</span>
                            </div>
                            <div class="flex-1 overflow-auto rounded border border-slate-200">
                                <table class="w-full text-left text-xs">
                                    <thead class="sticky top-0 bg-slate-50 shadow-sm z-10 border-b">
                                        <tr>
                                            <th class="p-3 w-12 text-center">
                                                <input type="checkbox" id="selectAllRua" onchange="toggleSelectAllRua()" class="w-4 h-4 text-sky-600 rounded focus:ring-sky-500">
                                            </th>
                                            <th class="p-3 font-bold text-slate-600">Tên Mâm Đồ Bẩn</th>
                                            <th class="p-3 text-right font-bold text-slate-600 pr-4">Mã ID Định Vị</th>
                                        </tr>
                                    </thead>
                                    <tbody id="bangChoRua" class="divide-y divide-slate-100 text-slate-700 bg-white"></tbody>
                                </table>
                            </div>
                        </div>
                        
                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-sky-500 bg-white shadow-sm">
                            <div class="border-b border-slate-100 pb-3 mb-4 flex justify-between items-center">
                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">
                                    <i class="fa-solid fa-gears text-sky-600"></i> Thiết Lập Chu Trình & Hoá Chất Rửa
                                </h3>
                                <span class="text-[10px] text-slate-400 font-bold uppercase tracking-wider">Washer Disinfector</span>
                            </div>
                            
                            <div class="bg-slate-50 p-4 rounded-xl border border-slate-200 flex-1 flex flex-col justify-between gap-4">
                                <div class="space-y-3">
                                    <div class="grid grid-cols-2 gap-3">
                                        <div>
                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Phương pháp làm sạch:</label>
                                            <select id="rua_loaiRua" onchange="capNhatDanhSachMaMayRua()" class="w-full px-3 py-2 font-bold text-xs bg-white border border-slate-300 rounded shadow-sm text-slate-700">
                                                <option value="Máy rửa khử khuẩn tự động">Máy rửa khử khuẩn tự động</option>
                                                <option value="Máy rửa sóng siêu âm">Máy rửa sóng siêu âm</option>
                                                <option value="Bồn rửa thủ công">Bồn rửa thủ công</option>
                                            </select>
                                        </div>
                                        <div>
                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Mã máy rửa:</label>
                                            <select id="rua_maySo" onchange="tuDongTaoMaLoMeRua()" class="w-full px-3 py-2 font-black text-xs text-sky-700 bg-white border border-slate-300 rounded shadow-sm">
                                                <option value="Belimed WD250">Belimed WD250</option>
                                                <option value="US1">US1</option>
                                                <option value="MAN1">MAN1</option>
                                            </select>
                                        </div>
                                    </div>

                                    <div class="grid grid-cols-2 gap-3">
                                        <div>
                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Số mẻ rửa trong ngày:</label>
                                            <input type="text" id="rua_meSo" readonly class="w-full px-3 py-2 text-xs font-mono font-bold text-center bg-slate-100 border border-slate-200 rounded text-slate-600">
                                        </div>
                                        <div>
                                            <label class="block text-[10px] font-black text-sky-600 uppercase mb-1">Mã Lô Rửa (Batch Code):</label>
                                            <input type="text" id="rua_batchId" readonly class="w-full px-3 py-2 text-xs font-mono font-black text-center bg-sky-50 border border-sky-300 rounded text-sky-700 tracking-wider">
                                        </div>
                                    </div>

                                    <div>
                                        <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Chu kỳ nhiệt độ rửa:</label>
                                        <select id="rua_chuKy" class="w-full px-3 py-2 text-xs font-semibold bg-white border border-slate-300 rounded text-slate-700">
                                            <option value="Chu trình tiêu chuẩn (93°C - 10 phút)">Chu trình tiêu chuẩn (93°C - 10 phút)</option>
                                            <option value="Chu trình dụng cụ tinh xảo (cảm biến)">Chu trình dụng cụ tinh xảo (cảm biến)</option>
                                            <option value="Rửa sóng siêu âm kết hợp khử khuẩn">Rửa sóng siêu âm kết hợp khử khuẩn</option>
                                            <option value="Ngâm hoá chất thủ công + xả ráo">Ngâm hoá chất thủ công + xả ráo</option>
                                        </select>
                                    </div>

                                    <div class="grid grid-cols-3 gap-2">
                                        <div class="col-span-2">
                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Hoá chất tiêu hao mẻ này:</label>
                                            <select id="rua_hoaChat" class="w-full px-3 py-2 text-xs font-bold bg-white border border-slate-300 rounded text-sky-800">
                                            </select>
                                        </div>
                                        <div>
                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Liều lượng (ml/L):</label>
                                            <input type="text" id="rua_lieuLuong" placeholder="5 ml/L" class="w-full px-2 py-2 text-xs font-bold text-center bg-white border border-slate-300 rounded text-slate-700">
                                        </div>
                                    </div>
                                </div>

                                <div class="pt-3 border-t border-slate-200">
                                    <button onclick="xacNhanMeRua()" class="w-full bg-sky-600 hover:bg-sky-700 text-white font-black py-3.5 rounded-lg text-xs uppercase tracking-wider shadow-md flex items-center justify-center gap-2 transition-all active:scale-95">
                                        <i class="fa-solid fa-play text-xs"></i> KHỞI CHẠY CHU TRÌNH MÁY RỬA KHỬ KHUẨN
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 min-h-[450px]">
                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-amber-500 bg-white shadow-sm">
                            <div class="border-b border-slate-100 pb-3 mb-4">
                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">
                                    <i class="fa-solid fa-magnifying-glass text-amber-500"></i> Giám Sát Nghiệm Thu Độ Sạch Mẻ Rửa
                                </h3>
                            </div>
                            <div class="mb-3 bg-slate-50 border border-slate-200 p-3 rounded-xl shadow-inner">
                                <label class="block text-[11px] font-black text-slate-700 uppercase mb-1.5">
                                    Đánh giá chất lượng rửa (Test chỉ thị Protein/ATP):
                                </label>
                                <select id="rua_testDoSach" class="w-full p-2 text-xs font-black border border-slate-300 rounded bg-white text-emerald-700">
                                    <option value="ĐẠT (CẢM QUAN KHÔNG CÒN VẾT MÁU / RỈ)">🟢 ĐẠT (CẢM QUAN KHÔNG VẾT MÁU / RỈ)</option>
                                    <option value="ĐẠT (TEST PROTEIN ÂM TÍNH)">🟢 ĐẠT (TEST PROTEIN/ATP ÂM TÍNH)</option>
                                    <option value="KHÔNG ĐẠT (CÒN MẢNG BÁM / VẾT DƠ)">🔴 KHÔNG ĐẠT (CÒN MẢNG BÁM / VẾT DƠ)</option>
                                </select>
                            </div>
                            <div class="mb-3 flex gap-2">
                                <button onclick="duyetSachMeRuaHangLoat()" class="w-2/3 bg-emerald-600 hover:bg-emerald-700 text-white font-black py-2.5 rounded text-xs shadow-md uppercase transition-colors"><i class="fa-solid fa-circle-check mr-1"></i> Nghiệm thu ĐẠT & Chuyển đóng gói</button>
                                <button onclick="tuChoiMeRuaHangLoat()" class="w-1/3 bg-rose-600 hover:bg-rose-700 text-white font-black py-2.5 rounded text-xs shadow-md uppercase transition-colors"><i class="fa-solid fa-rotate-left mr-1"></i> RỬA LẠI</button>
                            </div>
                            <div class="flex-1 overflow-auto bg-slate-50 rounded-lg border border-slate-200">
                                <table class="w-full text-left">
                                    <thead class="sticky top-0 bg-amber-100 shadow-sm z-10 border-b border-amber-200">
                                        <tr>
                                            <th class="p-3 w-12 text-center">
                                                <input type="checkbox" id="selectAllNghiemThuRua" onchange="toggleSelectAllNghiemThuRua()" class="w-4 h-4 text-amber-600 rounded focus:ring-amber-500">
                                            </th>
                                            <th class="p-2 text-xs font-black text-slate-700">Mâm dụng cụ đang trong buồng máy</th>
                                        </tr>
                                    </thead>
                                    <tbody id="bangChoNiemThuRua" class="divide-y divide-slate-200 bg-white"></tbody>
                                </table>
                            </div>
                        </div>
                        
                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-emerald-500 bg-white shadow-sm">
                            <div class="border-b border-slate-100 pb-3 mb-4">
                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">
                                    <i class="fa-solid fa-clock-rotate-left text-emerald-500"></i> Nhật ký vận hành máy rửa khử khuẩn
                                </h3>
                            </div>
                            <div class="flex-1 overflow-auto rounded-lg border border-slate-200 bg-white">
                                <table class="w-full text-left">
                                    <thead class="sticky top-0 bg-slate-50 shadow-sm border-b border-slate-200">
                                        <tr>
                                            <th class="p-3 text-xs font-bold text-slate-600">Thông tin mẻ máy</th>
                                            <th class="p-3 text-xs font-bold text-slate-600 text-right pr-4">Cơ số mâm</th>
                                        </tr>
                                    </thead>
                                    <tbody id="bangLichSuRua" class="divide-y divide-slate-100 text-slate-700"></tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- TRẠM 4: ĐÓNG GÓI -->
                <div id="tab-donggoi" class="hidden h-full">
                    <div class="glass-panel p-5 flex flex-col h-[650px] border-t-4 border-t-sky-500 bg-white">
                        <div class="flex justify-between items-center border-b border-slate-100 pb-4 mb-4">
                            <div><h3 class="text-base font-black text-slate-700 uppercase"><i class="fa-solid fa-hands-bubbles mr-2 text-sky-500"></i> Trạm Làm Sạch & Đóng Gói</h3></div>
                            <span id="badgeDongGoi" class="bg-slate-100 text-slate-600 text-xs font-bold px-2 py-1 rounded">0 Mâm</span>
                        </div>
                        <div class="flex-1 overflow-auto p-1" id="gridDongGoi"></div>
                    </div>
                </div>

                <!-- TRẠM 5: MÁY HẤP -->
                <div id="tab-mayhap" class="hidden flex flex-col gap-6 w-full">
                    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 min-h-[480px]">
                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-slate-400 bg-white shadow-sm">
                            <div class="flex justify-between items-center border-b border-slate-100 pb-3 mb-4">
                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">
                                    <i class="fa-solid fa-boxes-stacked text-slate-400"></i> Danh sách chờ hấp
                                </h3>
                                <span id="badgeChoHap" class="text-xs font-bold text-slate-500 bg-slate-100 px-2 py-1 rounded">0 Mục</span>
                            </div>
                            <div class="flex-1 overflow-auto rounded border border-slate-200">
                                <table class="w-full text-left text-xs">
                                    <thead class="sticky top-0 bg-slate-50 shadow-sm z-10 border-b">
                                        <tr>
                                            <th class="p-3 w-12 text-center">
                                                <input type="checkbox" id="selectAllHap" onchange="toggleSelectAllHap()" class="w-4 h-4 text-sky-600 rounded focus:ring-sky-500">
                                            </th>
                                            <th class="p-3 font-bold text-slate-600">Mâm Dụng Cụ</th>
                                            <th class="p-3 text-right font-bold text-slate-600 pr-4">Mã Định Hình Khay</th>
                                        </tr>
                                    </thead>
                                    <tbody id="bangChoHap" class="divide-y divide-slate-100 text-slate-700 bg-white"></tbody>
                                </table>
                            </div>
                        </div>
                        
                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-sky-500 bg-white shadow-sm">
                            <div class="border-b border-slate-100 pb-3 mb-4 flex justify-between items-center">
                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">
                                    <i class="fa-solid fa-square-poll-horizontal text-sky-500"></i> Chi Tiết Mẻ Tiệt Khuẩn
                                </h3>
                                <span class="text-[10px] text-slate-400 font-bold uppercase tracking-wider">Autoclave Details</span>
                            </div>
                            
                            <div class="bg-slate-50 p-4 rounded-xl border border-slate-200 flex-1 flex flex-col justify-between gap-4">
                                <div class="space-y-3">
                                    <div class="grid grid-cols-2 gap-3">
                                        <div>
                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Loại hình tiệt khuẩn:</label>
                                            <select id="hap_loaiHap" onchange="capNhatDanhSachMaMay()" class="w-full px-3 py-2 font-bold text-xs bg-white border border-slate-300 rounded shadow-sm text-slate-700">
                                                <option value="Hấp hơi nước">Hấp hơi nước</option>
                                                <option value="Hấp H2O2 (Plasma)">Hấp H2O2 (Plasma)</option>
                                                <option value="Khử khuẩn EO">Khử khuẩn EO</option>
                                            </select>
                                        </div>
                                        <div>
                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Mã máy đã chọn (Code):</label>
                                            <select id="hap_maySo" onchange="tuDongTaoMaLoMeHap()" class="w-full px-3 py-2 font-black text-xs text-sky-700 bg-white border border-slate-300 rounded shadow-sm">
                                                <option value="A1">A1</option>
                                                <option value="A2">A2</option>
                                                <option value="A3">A3</option>
                                                <option value="A4">A4</option>
                                            </select>
                                        </div>
                                    </div>

                                    <div class="grid grid-cols-2 gap-3">
                                        <div>
                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Số lần đã thực hiện:</label>
                                            <input type="text" id="hap_meSo" readonly class="w-full px-3 py-2 text-xs font-mono font-bold text-center bg-slate-100 border border-slate-200 rounded text-slate-600">
                                        </div>
                                        <div>
                                            <label class="block text-[10px] font-black text-rose-600 uppercase mb-1 flex items-center gap-1">
                                                Mã lô liên kết (Batch ID):
                                            </label>
                                            <input type="text" id="hap_batchId" readonly class="w-full px-3 py-2 text-xs font-mono font-black text-center bg-rose-50 border border-rose-300 rounded text-rose-700 tracking-wider">
                                        </div>
                                    </div>

                                    <div class="grid grid-cols-3 gap-2">
                                        <div class="col-span-2">
                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Nhiệt độ / Chương trình:</label>
                                            <select id="hap_nhietDo" class="w-full px-3 py-2 text-xs font-semibold bg-white border border-slate-300 rounded text-slate-700">
                                                <option value="134°C - 4 phút">134°C - 4 phút</option>
                                                <option value="121°C - 20 phút">121°C - 20 phút</option>
                                            </select>
                                        </div>
                                        <div>
                                            <label class="block text-[10px] font-black text-slate-500 uppercase mb-1">Áp suất (Bar):</label>
                                            <input type="text" id="hap_apSuat" placeholder="2.1" class="w-full px-2 py-2 text-xs font-bold text-center bg-white border border-slate-300 rounded text-slate-700">
                                        </div>
                                    </div>

                                    <div class="mb-1 mt-2 flex items-center gap-2 bg-amber-50 border border-amber-200 p-2.5 rounded-lg shadow-sm">
                                        <input type="checkbox" id="hap_hasBI" class="w-4 h-4 rounded text-amber-600 focus:ring-amber-500 cursor-pointer">
                                        <label for="hap_hasBI" class="text-xs font-black text-amber-800 cursor-pointer select-none">
                                            🔥 MẺ CÓ KÈM ỐNG TEST SINH HỌC BI (BẮT BUỘC ĐỐI SOÁT KHI DUYỆT)
                                        </label>
                                    </div>
                                </div>

                                <div class="pt-3 border-t border-slate-200">
                                    <button onclick="xacNhanMeHap()" class="w-full bg-sky-600 hover:bg-sky-700 text-white font-black py-3.5 rounded-lg text-xs uppercase tracking-wider shadow-md flex items-center justify-center gap-2 transition-transform active:scale-[0.98]">
                                        <i class="fa-solid fa-play-circle text-sm"></i> KÍCH HOẠT VÀ KHỞI ĐỘNG LÒ HẤP
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 min-h-[450px]">
                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-amber-500 bg-white shadow-sm">
                            <div class="border-b border-slate-100 pb-3 mb-4">
                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">
                                    <i class="fa-solid fa-vial-circle-check text-amber-500"></i> Phê Duyệt - Chấp Nhận Vô Kho
                                </h3>
                            </div>

                            <div class="mb-3 bg-slate-50 border border-slate-200 p-3 rounded-xl shadow-inner">
                                <label class="block text-[11px] font-black text-slate-700 uppercase mb-1.5 flex items-center gap-1.5">
                                    <i class="fa-solid fa-camera text-sky-600"></i> Tải ảnh minh chứng kết quả BI (Nếu duyệt mẻ có BI):
                                </label>
                                <input type="file" id="input_anhBI" accept="image/*" onchange="docAnhBiUpTaiCho(this)" 
                                       class="block w-full text-xs text-slate-500 file:mr-3 file:py-1.5 file:px-3 file:rounded-lg file:border-0 file:text-xs file:font-black file:bg-sky-50 file:text-sky-700 hover:file:bg-sky-100 cursor-pointer border border-slate-300 rounded-lg p-1 bg-white">
                            </div>

                            <div class="mb-3 flex flex-col gap-2">
                                <button onclick="nhapKhoHangLoat()" class="w-full bg-emerald-600 hover:bg-emerald-700 text-white font-black py-2.5 rounded text-xs shadow-md"><i class="fa-solid fa-check-double mr-1"></i> DUYỆT ĐẠT & CHO VÀO KHO LƯU TRỮ CHUNG</button>
                                <div class="flex gap-2">
                                    <button onclick="inTemNghiemThuHangLoat()" class="w-1/2 bg-sky-600 hover:bg-sky-700 text-white font-black py-2 rounded text-xs shadow-md"><i class="fa-solid fa-print mr-1"></i> IN TEM TỔNG</button>
                                    <button onclick="tuChoiHapHangLoat()" class="w-1/2 bg-rose-600 hover:bg-rose-700 text-white font-black py-2 rounded text-xs shadow-md"><i class="fa-solid fa-ban mr-1"></i> KHÔNG ĐẠT (TRẢ LẠI)</button>
                                </div>
                            </div>
                            <div class="flex-1 overflow-auto bg-amber-50/20 rounded-lg border border-amber-200/50">
                                <table class="w-full text-left">
                                    <thead class="sticky top-0 bg-amber-100 shadow-sm z-10 border-b border-amber-200">
                                        <tr>
                                            <th class="p-2.5 w-12 text-center">
                                                <input type="checkbox" id="selectAllNghiemThu" onchange="toggleSelectAllNghiemThu()" class="w-4 h-4 text-amber-600 rounded focus:ring-amber-500">
                                            </th>
                                            <th class="p-2 text-xs font-black text-slate-700">Mâm dụng cụ / Mã mẻ</th>
                                        </tr>
                                    </thead>
                                    <tbody id="bangChoNghiệmThu" class="divide-y divide-slate-200 bg-white"></tbody>
                                </table>
                            </div>
                        </div>
                        
                        <div class="glass-panel p-5 flex flex-col h-full border-t-4 border-t-emerald-500 bg-white shadow-sm">
                            <div class="border-b border-slate-100 pb-3 mb-4">
                                <h3 class="text-sm font-black text-slate-700 uppercase flex items-center gap-1.5">
                                    <i class="fa-solid fa-clock-rotate-left text-emerald-500"></i> Nhật ký vận hành lò
                                </h3>
                            </div>
                            <div class="flex-1 overflow-auto rounded-lg border border-slate-200 bg-white">
                                <table class="w-full text-left">
                                    <thead class="sticky top-0 bg-slate-50 shadow-sm border-b border-slate-200">
                                        <tr>
                                            <th class="p-3 text-xs font-bold text-slate-600">Thông quy trình</th>
                                            <th class="p-3 text-xs font-bold text-slate-600 text-right pr-4">Tổng khay</th>
                                        </tr>
                                    </thead>
                                    <tbody id="bangLichSuHap" class="divide-y divide-slate-100 text-slate-700"></tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- TRẠM 6: KHO VÔ KHUẨN -->
                <div id="tab-khovokhuan" class="hidden flex flex-col gap-6 h-full">
                    <div id="vungYeuCauCapPhat" class="mb-6 bg-amber-50/70 border border-amber-200 rounded-xl p-4 shadow-sm">
                        <h3 class="font-black text-amber-950 text-xs flex items-center gap-1.5"><i class="fa-solid fa-bell-slash"></i> Đang đồng bộ hộp yêu cầu cấp phát...</h3>
                    </div>

                    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 h-auto">
                        <div class="glass-panel p-5 flex flex-col border-t-4 border-t-sky-500 bg-slate-50 h-auto min-h-[320px]">
                            <div class="border-b border-slate-200 pb-4 mb-4"><h3 class="text-base font-black text-slate-700 uppercase"><i class="fa-solid fa-dolly mr-2 text-sky-500"></i> Bàn Giao Xuất Kho</h3></div>
                            <div class="space-y-4">
                                <div><label class="block text-[10px] font-bold text-slate-500 mb-1.5 uppercase">Chọn Khoa Nhận Đồ:</label><select id="xuat_selKhoa" class="w-full px-3 py-3 shadow-sm font-bold text-slate-700 border border-slate-300 rounded-lg"><option value="">-- Chọn Khoa --</option></select></div>
                                <div class="bg-white p-4 rounded-lg border border-slate-200 shadow-sm relative space-y-3">
                                    <label class="block text-[10px] uppercase font-bold text-slate-500">Quét Mã ID Khay Xuất:</label>
                                    <div class="flex gap-2">
                                        <input type="text" id="xuat_inpMaBo" placeholder="Mã ID mâm..." class="w-full px-3 py-3 font-bold text-sky-700 shadow-sm border border-sky-300 rounded-lg bg-sky-50 text-base" onkeypress="if(event.key==='Enter') xuatKhoXoayVong()">
                                        <button onclick="moCamera('xuat_inpMaBo')" class="bg-slate-800 text-white px-4 py-3 rounded-lg shadow action-btn whitespace-nowrap min-h-[48px]"><i class="fa-solid fa-camera text-lg"></i></button>
                                    </div>
                                    <button onclick="xuatKhoXoayVong()" class="w-full bg-emerald-600 hover:bg-emerald-700 active:bg-emerald-800 text-white font-black py-3.5 rounded-xl text-sm shadow-md min-h-[48px] uppercase tracking-wide transition-all"><i class="fa-solid fa-check mr-2"></i> XÁC NHẬN XUẤT KHO</button>
                                </div>
                            </div>
                        </div>

                        <div class="glass-panel p-5 flex flex-col border-t-4 border-t-emerald-500 lg:col-span-2 min-h-[380px]">
                            <div class="border-b border-slate-100 pb-3 mb-4 flex justify-between items-center">
                                <h3 class="text-sm font-black text-slate-700 uppercase"><i class="fa-solid fa-warehouse mr-2 text-emerald-500"></i> Danh sách mâm đang lưu tại Kho Vô Khuẩn</h3>
                                <span class="text-[10px] font-bold bg-emerald-100 text-emerald-800 px-2.5 py-1 rounded-full uppercase">Chế độ Tablet</span>
                            </div>
                            <div class="flex-1 overflow-auto bg-white rounded-lg border border-slate-200">
                                <table class="w-full text-left text-xs">
                                    <thead class="sticky top-0 shadow-sm bg-slate-50 font-bold border-b border-slate-200 text-slate-600">
                                        <tr>
                                            <th class="p-3.5">Loại Mâm / Tên Dụng Cụ</th>
                                            <th class="p-3.5">Mã ID Khay</th>
                                            <th class="p-3.5 text-center">Định Biên Sở Hữu</th>
                                            <th class="p-3.5 text-center">Vị Trí Kệ</th>
                                            <th class="p-3.5 text-center">HSD</th>
                                            <th class="p-3.5 text-center">Thao tác nhanh</th>
                                        </tr>
                                    </thead>
                                    <tbody id="bangKhoVoKhuan" class="divide-y divide-slate-100 text-slate-700 font-medium"></tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- TRẠM 7: TỒN KHO -->
                <div id="tab-quanlykho" class="hidden h-full">
                    <div class="glass-panel p-5 flex flex-col h-[650px] border-t-4 border-t-amber-500 bg-white">
                        <div class="border-b border-slate-100 pb-4 mb-4">
                            <h3 class="text-base font-black text-slate-800 uppercase flex items-center gap-2"><i class="fa-solid fa-boxes-stacked text-amber-500"></i> Bản Đồ Theo Dõi Tồn Kho Thực Tế Toàn Viện</h3>
                        </div>
                        <div class="mb-4 bg-slate-50 p-3 rounded border border-slate-200 w-1/3">
                            <label class="block text-[10px] font-bold text-slate-500 uppercase mb-1">Lọc Theo Khoa Sở Hữu:</label>
                            <select id="inv_filterKhoa" onchange="callRender()" class="w-full p-2 text-xs font-bold"><option value="">-- Tất cả Khoa Phòng --</option></select>
                        </div>
                        <div class="flex-1 overflow-auto border border-slate-200 rounded-lg">
                            <table class="w-full text-left text-xs">
                                <thead class="bg-slate-50 sticky top-0 shadow-sm z-10">
                                    <tr>
                                        <th class="p-3">Mã ID Khay</th>
                                        <th class="p-3">Loại Mâm Dụng Cụ</th>
                                        <th class="p-3">Khoa Đang Giao/Giữ</th>
                                        <th class="p-3 text-center">Vị Trí Vật Lý Hiện Tại</th>
                                        <th class="p-3 text-center">Mẻ Lò Gần Nhất</th>
                                        <th class="p-3 text-center">Giám Sát HSD</th>
                                    </tr>
                                </thead>
                                <tbody id="bangTonKhoTe" class="divide-y divide-slate-100 bg-white text-slate-700"></tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- TRẠM 8: DANH MỤC & TUỔI THỌ -->
                <div id="tab-danhmuc" class="hidden grid grid-cols-1 lg:grid-cols-2 gap-6 h-full">
                    <div class="glass-panel p-5 flex flex-col h-[650px] border-t-4 border-t-sky-500 bg-white">
                        <div class="flex justify-between items-center border-b border-slate-100 pb-3 mb-4">
                            <h3 class="text-sm font-black text-slate-700 uppercase"><i class="fa-solid fa-list-check mr-2 text-sky-500"></i> 7.1 Danh Mục Loại Bộ Linh Kiện Chuẩn</h3>
                            <span id="badgeLinhKienCount" class="text-xs font-bold text-sky-700 bg-sky-50 px-2 py-1 rounded">Cấu hình chi tiết</span>
                        </div>
                        <div class="flex-1 overflow-auto border border-slate-100 rounded-lg">
                            <table class="w-full text-left text-xs">
                                <thead class="sticky top-0 bg-slate-50 shadow-sm z-10">
                                    <tr>
                                        <th class="p-3 w-1/3">Tên Loại Bộ</th>
                                        <th class="p-3 w-1/2">Chi Tiết Linh Kiện Chuẩn (Tên | SL)</th>
                                        <th class="p-3 text-center">Tổng SL</th>
                                    </tr>
                                </thead>
                                <tbody id="bangDanhMucLinhKien" class="divide-y divide-slate-100 text-slate-700 bg-white">
                                    <tr><td colspan="3" class="p-4 text-center text-slate-400 italic">Đang tải dữ liệu bộ linh kiện...</td></tr>
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <div class="glass-panel p-5 flex flex-col h-[650px] border-t-4 border-t-teal-500 bg-white">
                        <div class="flex justify-between items-center border-b border-slate-100 pb-3 mb-4">
                            <h3 class="text-sm font-black text-teal-700 uppercase"><i class="fa-solid fa-chart-line mr-2 text-teal-500"></i> 7.2 Giám Sát Tuổi Thọ & Chu Kỳ Khay</h3>
                            <span id="badgeTuoiThoKhay" class="text-xs font-bold text-teal-700 bg-teal-50 px-2 py-1 rounded">0 Khay</span>
                        </div>
                        <div class="flex-1 overflow-auto border border-slate-100 rounded-lg">
                            <table class="w-full text-left text-xs">
                                <thead class="sticky top-0 bg-slate-50 shadow-sm z-10">
                                    <tr>
                                        <th class="p-3">Mã Lưu Kho</th>
                                        <th class="p-3">Tên Khay (Phân loại)</th>
                                        <th class="p-3 text-center">Trạng Thái</th>
                                        <th class="p-3 text-center bg-amber-50 text-amber-900 font-bold">Chu Kỳ Lò</th>
                                    </tr>
                                </thead>
                                <tbody id="bangDanhMucTong" class="divide-y divide-slate-100 text-slate-700 bg-white">
                                    <tr><td colspan="4" class="p-4 text-center text-slate-400 italic">Đang tải danh sách khay vận hành...</td></tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- TRẠM 9: NHẬT KÝ LUÂN CHUYỂN -->
                <div id="tab-lichsuluanchuyen" class="hidden flex flex-col gap-6 w-full">
                    <div class="glass-panel p-5 flex flex-col h-[650px] border-t-4 border-t-sky-600 bg-white">
                        <div class="border-b border-slate-100 pb-3 mb-3 flex justify-between items-center">
                            <h4 class="text-base font-black text-slate-800 uppercase flex items-center gap-2"><i class="fa-solid fa-history text-sky-600"></i> Nhật Ký Luân Chuyển & Hành Trình Khay Dụng Cụ</h4>
                            <span class="text-[11px] bg-slate-100 px-2 py-1 rounded font-bold text-slate-500 uppercase">Hành trình khay thực tế</span>
                        </div>
                        <div class="flex-1 overflow-auto border border-slate-200 rounded-lg">
                            <table class="w-full text-left text-xs">
                                <thead class="bg-slate-50 sticky top-0 shadow-sm z-10">
                                    <tr>
                                        <th class="p-3">Mã ID Khay</th>
                                        <th class="p-3">Tên Mâm Dụng Cụ</th>
                                        <th class="p-3">Khoa Lâm Sàng</th>
                                        <th class="p-3 text-center">Vị Trí/Trạng Thái</th>
                                        <th class="p-3 text-center bg-rose-50 text-rose-900 font-bold">Mã Lô Hấp</th>
                                        <th class="p-3 text-center bg-sky-50 text-sky-900 font-bold">KTV Xuất Kho</th>
                                        <th class="p-3 text-center">Thời Gian Ký Gửi</th>
                                    </tr>
                                </thead>
                                <tbody id="bangLichSuHanhTrinhGoc" class="divide-y divide-slate-100 text-slate-600">
                                    <tr><td colspan="7" class="p-4 text-center italic text-slate-400">Đang đồng bộ dòng dữ liệu...</td></tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

                <!-- TRẠM 10: CẤU HÌNH & TRUY VẾT -->
                <div id="tab-tracuu" class="hidden flex flex-col gap-6 w-full">
                    <div class="flex space-x-2 bg-white p-2 rounded-lg border border-slate-200 shadow-sm">
                        <button onclick="switchAdminSubtab('database')" id="subbtn-database" class="admin-subtab-active px-4 py-2 text-xs font-black rounded">1. TRUY VẾT & NẠP DATABASE</button>
                        <button onclick="switchAdminSubtab('security')" id="subbtn-security" class="px-4 py-2 text-xs font-black rounded text-slate-600 bg-slate-100">2. TÀI KHOẢN & MÃ PIN</button>
                    </div>
                    
                    <div id="subtab-database" class="space-y-6">
                        <div class="glass-panel p-5 border-t-4 border-t-rose-600 bg-white shadow-sm">
                            <h4 class="text-xs font-black text-rose-700 uppercase mb-2 flex items-center gap-1">
                                <i class="fa-solid fa-slate-virus text-sm"></i> TRẠM TRUY VẾT NHIỄM TRÙNG KHẨN CẤP (KHOANH VÙNG THEO MẺ)
                            </h4>
                            <div class="flex gap-2">
                                <input type="text" id="inp_searchBatch" placeholder="Nhập chính xác mã lô cần truy vết..." class="w-full px-3 py-2.5 font-mono font-bold text-rose-700 bg-rose-50/30 border border-rose-300 rounded-md text-sm">
                                <button onclick="truyVetTheoMaBatch()" class="bg-rose-600 hover:bg-rose-700 text-white font-black px-6 py-2.5 rounded shadow text-xs whitespace-nowrap"><i class="fa-solid fa-magnifying-glass-danger mr-1"></i> TRA CỨU KHẨN CẤP</button>
                                <button onclick="clearTruyVetBatch()" class="bg-slate-100 text-slate-600 font-bold px-4 py-2.5 rounded border border-slate-200 text-xs">RESET</button>
                            </div>
                        </div>

                        <div class="bg-amber-50 p-5 rounded border border-amber-200 shadow-sm">
                            <h4 class="text-xs font-black text-amber-800 uppercase mb-1.5 flex items-center gap-1.5"><i class="fa-solid fa-file-excel text-sm text-amber-600"></i> Nạp Danh Sách Bộ Dụng Cụ Từ Cấu Hình Excel</h4>
                            <p class="text-[11px] text-slate-500 mb-3">Hệ thống tự động biên dịch và phân tích cấu hình chi tiết linh kiện bên trong mâm ngay sau khi anh chọn file.</p>
                            <div class="flex items-center gap-4 bg-white p-3 rounded-lg border border-slate-200">
                                <input type="file" id="excelFileInput" accept=".xlsx, .xls" class="block w-full text-xs text-slate-500 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-xs file:font-black file:bg-amber-50 file:text-amber-800 hover:file:bg-amber-100 cursor-pointer">
                            </div>
                        </div>

                        <div class="bg-sky-50 p-5 rounded border border-sky-200 shadow-sm">
                            <h4 class="text-xs font-black text-sky-800 uppercase mb-1.5 flex items-center gap-1.5"><i class="fa-solid fa-circle-plus text-sm text-sky-600"></i> Khai Báo & Định Danh Khay Mới Vãng Lai</h4>
                            <p class="text-[11px] text-slate-500 mb-3">Sử dụng tính năng này để cấp phát khay mới mua hoặc khay độc bản vào hệ thống luân chuyển động toàn viện.</p>
                            <button onclick="khaiSinhKhayVangLai()" class="bg-sky-600 hover:bg-sky-700 text-white font-black py-2.5 px-4 rounded text-xs shadow flex items-center gap-1.5"><i class="fa-solid fa-plus-circle"></i> KHAI SINH KHAY MỚI</button>
                        </div>

                        <div class="border-t border-slate-200 pt-4 space-y-4">
                            <button onclick="resetDuLieuKet()" class="w-full bg-amber-500 hover:bg-amber-600 text-white font-black py-3.5 rounded shadow-md text-xs uppercase tracking-wider"><i class="fa-solid fa-broom mr-2"></i> DỌN DẸP DỮ LIỆU KẸT (REFRESH MÂM)</button>
                            <div class="p-4 bg-rose-50 border border-rose-200 rounded-lg shadow-sm">
                                <button onclick="xoaSachDuLieuGiaoDichRealtime()" class="w-full bg-rose-600 hover:bg-rose-700 text-white font-black py-3 px-4 rounded shadow text-xs uppercase tracking-wider"><i class="fa-solid fa-dumpster-fire mr-1.5"></i> Xóa sạch toàn bộ lịch sử dữ liệu Demo</button>
                            </div>
                        </div>
                    </div>

                    <div id="subtab-security" class="glass-panel p-6 space-y-6 hidden">
                        <div class="bg-slate-50 p-4 rounded-xl border border-slate-200 mb-6">
                            <h4 class="text-xs font-black text-slate-700 uppercase mb-2 flex items-center gap-1.5"><i class="fa-solid fa-toggle-on text-sky-600 text-sm"></i> BẢNG PHÂN QUYỀN & KHÓA TAB CHỨC NĂNG GIAO DIỆN</h4>
                            <div class="overflow-x-auto border border-slate-200 rounded-lg bg-white">
                                <table class="w-full text-left">
                                    <thead class="bg-slate-100 text-[10px] font-black uppercase text-slate-600 border-b">
                                        <tr>
                                            <th class="p-3 w-2/5">Tên Tab / Chức năng hệ thống</th>
                                            <th class="p-3 text-center w-1/5 bg-sky-50/50">KTV CSSD</th>
                                            <th class="p-3 text-center w-1/5">Điều Dưỡng Lâm Sàng</th>
                                            <th class="p-3 text-center w-1/5">Khách Tham Quan</th>
                                        </tr>
                                    </thead>
                                    <tbody id="bodyMaTranGiaoDien"></tbody>
                                </table>
                            </div>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                            <div class="bg-slate-50 p-4 rounded border border-slate-200">
                                <h4 class="text-xs font-black text-slate-600 uppercase">PIN ADMIN HỆ THỐNG</h4>
                                <input type="password" id="cfg_pinAdmin" class="w-full p-2 mt-1 font-mono text-center font-bold">
                                <button onclick="saveAdminPIN('ADMIN')" class="bg-slate-800 text-white font-bold py-2 mt-2 rounded text-xs w-full">LƯU PIN ADMIN</button>
                            </div>
                            <div class="bg-slate-50 p-4 rounded border border-slate-200">
                                <h4 class="text-xs font-black text-slate-600 uppercase">PIN CHUNG CSSD (BACKUP)</h4>
                                <input type="password" id="cfg_pinCSSD" class="w-full p-2 mt-1 font-mono text-center font-bold">
                                <button onclick="saveAdminPIN('CSSD')" class="bg-sky-600 text-white font-bold py-2 mt-2 rounded text-xs w-full">LƯU PIN CSSD</button>
                            </div>
                            <div class="bg-slate-50 p-4 rounded border border-slate-200">
                                <h4 class="text-xs font-black text-slate-600 uppercase">PIN KHÁCH (GUEST)</h4>
                                <input type="password" id="cfg_pinGuest" class="w-full p-2 mt-1 font-mono text-center font-bold">
                                <button onclick="saveAdminPIN('GUEST')" class="bg-emerald-600 text-white font-bold py-2 mt-2 rounded text-xs w-full">LƯU PIN GUEST</button>
                            </div>
                        </div>

                        <div class="pt-4 border-t border-slate-200 mt-4">
                            <div class="flex justify-between items-center mb-3">
                                <h4 class="text-xs font-black text-slate-700 uppercase"><i class="fa-solid fa-user-nurse mr-1"></i> Quản Lý Nhân Viên CSSD (KTV)</h4>
                                <button onclick="themKtvCssd()" class="bg-sky-600 hover:bg-sky-700 text-white font-black py-1.5 px-3 rounded text-xs shadow"><i class="fa-solid fa-plus mr-1"></i> THÊM NHÂN VIÊN</button>
                            </div>
                            <div class="overflow-x-auto border border-slate-200 rounded max-h-64 mb-6">
                                <table class="w-full text-left text-xs">
                                    <thead class="bg-slate-50 sticky top-0"><tr><th class="p-3 w-1/6">Mã NV</th><th class="p-3 w-1/3">Tên Nhân Viên</th><th class="p-3 text-center border-x border-slate-200 w-1/4">MÃ PIN ĐĂNG NHẬP</th><th class="p-3 text-center w-1/4">Thao tác</th></tr></thead>
                                    <tbody id="bangNhanVienCssd" class="bg-white divide-y divide-slate-100"></tbody>
                                </table>
                            </div>
                        </div>

                        <div class="pt-4 border-t border-slate-200">
                            <div class="flex justify-between items-center mb-3">
                                <h4 class="text-xs font-black text-slate-700 uppercase"><i class="fa-solid fa-hospital-user mr-1"></i> Quản Lý & Reset PIN Khoa Lâm Sàng</h4>
                                <button onclick="themKhoaThuCong()" class="bg-emerald-600 hover:bg-emerald-700 text-white font-black py-1.5 px-3 rounded text-xs shadow flex items-center gap-1">
                                    <i class="fa-solid fa-plus-circle"></i> + THÊM KHOA MỚI
                                </button>
                            </div>
                            <div class="overflow-x-auto border border-slate-200 rounded max-h-96">
                                <table class="w-full text-left text-xs">
                                    <thead class="bg-slate-50 sticky top-0"><tr><th class="p-3">Tên Khoa</th><th class="p-3 text-center border-x border-slate-200">MÃ PIN HIỆN TẠI</th><th class="p-3 text-center">Thao tác Đổi/Reset</th></tr></thead>
                                    <tbody id="bangPhanQuyenKhoa" class="bg-white divide-y divide-slate-100"></tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- TRẠM 11: PERFORMANCE KPI -->
                <div id="tab-performance" class="hidden flex flex-col gap-6 w-full">
                    <div class="glass-panel p-5 border-t-4 border-t-emerald-600"><h3 class="text-base font-black text-slate-700 uppercase mb-4">Bảng Điểm KPI KTV Ca Trực</h3><div class="overflow-x-auto border border-slate-200 rounded"><table class="w-full text-left text-xs"><thead class="bg-slate-50"><tr><th class="p-3 text-center">Hạng</th><th class="p-3">Mã KTV</th><th class="p-3">Họ và Tên Nhân Viên</th><th class="p-3 text-center bg-slate-100">TỔNG KPI</th></tr></thead><tbody id="bangHieuSuatKTV" class="bg-white"></tbody></table></div></div>
                </div>

                <!-- TRẠM 12: DASHBOARD TIVI -->
                <div id="tab-dashboard_tv" class="hidden p-6 bg-slate-900 text-white min-h-[650px] rounded-2xl shadow-inner">
                    <div class="flex justify-between items-center border-b border-slate-700 pb-4 mb-6">
                        <div class="flex items-center gap-3">
                            <span class="flex h-3 w-3 relative">
                                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"></span>
                                <span class="relative inline-flex rounded-full h-3 w-3 bg-emerald-500"></span>
                            </span>
                            <h1 class="text-lg font-black tracking-wider text-emerald-400 uppercase"><i class="fa-solid fa-tv mr-2"></i> TRẠM GIÁM SÁT TRUNG TÂM CSSD - PHƯƠNG NAM HOSPITAL</h1>
                        </div>
                        <div class="text-right">
                            <p id="tv_clock" class="text-xs font-mono font-bold text-slate-400"></p>
                        </div>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
                        <div class="bg-gradient-to-br from-blue-600 to-sky-700 p-6 rounded-2xl shadow-xl border border-blue-400/30">
                            <div class="text-xs font-black opacity-90 uppercase tracking-wide text-blue-100">Mẻ Rửa Hôm Nay</div>
                            <div id="tv_meRua" class="text-5xl font-black font-mono mt-2 text-white">0</div>
                            <p class="text-[10px] opacity-70 mt-2 font-medium">Tổng chu kỳ làm sạch trong ngày</p>
                        </div>
                        <div class="bg-gradient-to-br from-indigo-600 to-purple-700 p-6 rounded-2xl shadow-xl border border-indigo-400/30">
                            <div class="text-xs font-black opacity-90 uppercase tracking-wide text-indigo-100">Mẻ Hấp Hôm Nay</div>
                            <div id="tv_meHap" class="text-5xl font-black font-mono mt-2 text-white">0</div>
                            <p class="text-[10px] opacity-70 mt-2 font-medium">Lô tiệt khuẩn đạt chuẩn đã chạy</p>
                        </div>
                        <div class="bg-gradient-to-br from-indigo-600 to-purple-700 p-6 rounded-2xl shadow-xl border border-indigo-400/30">
                            <div class="text-xs font-black opacity-90 uppercase tracking-wide text-indigo-100">Mẻ Hấp Hôm Nay</div>
                            <div id="tv_meHap2" class="text-5xl font-black font-mono mt-2 text-white">0</div>
                            <p class="text-[10px] opacity-70 mt-2 font-medium">Lô tiệt khuẩn đạt chuẩn đã chạy</p>
                        </div>
                        <div class="bg-slate-800 p-6 rounded-2xl shadow-xl border border-slate-700 flex justify-between items-center">
                            <div>
                                <div class="text-xs font-black text-slate-400 uppercase tracking-wide">Đang xử lý tại CSSD</div>
                                <div class="flex gap-6 mt-3">
                                    <div>
                                        <span class="text-[10px] text-amber-400 block font-bold uppercase">Trạm rửa</span>
                                        <span id="tv_dangRua" class="text-3xl font-black font-mono text-amber-400">0</span>
                                    </div>
                                    <div class="border-l border-slate-700 pl-6">
                                        <span class="text-[10px] text-indigo-400 block font-bold uppercase">Trong lò</span>
                                        <span id="tv_dangHap" class="text-3xl font-black font-mono text-indigo-400">0</span>
                                    </div>
                                </div>
                            </div>
                            <i class="fa-solid fa-arrows-spin text-4xl text-slate-700 animate-spin" style="animation-duration: 8s;"></i>
                        </div>
                        <div class="bg-slate-800 p-6 rounded-2xl shadow-xl border border-slate-700 flex justify-between items-center">
                            <div>
                                <div class="text-xs font-black text-slate-400 uppercase tracking-wide">Kho Sạch & Vô Khuẩn</div>
                                <div class="flex gap-4 mt-3">
                                    <div>
                                        <span class="text-[10px] text-teal-400 block font-bold uppercase">Sẵn sàng xuất</span>
                                        <span id="tv_khoVoKhuan" class="text-3xl font-black font-mono text-teal-400">0</span>
                                    </div>
                                </div>
                            </div>
                            <i class="fa-solid fa-warehouse text-3xl text-slate-700"></i>
                        </div>
                    </div>

                    <div class="bg-slate-800/60 border border-slate-700 rounded-2xl p-6">
                        <div class="flex justify-between items-center mb-4">
                            <h3 class="text-xs font-black text-slate-300 uppercase tracking-wider flex items-center gap-2"><i class="fa-solid fa-triangle-exclamation text-rose-500 animate-bounce"></i> Giám sát cảnh báo hạn sử dụng (Realtime)</h3>
                        </div>
                        <div class="flex items-center gap-6 p-4 bg-slate-900/80 rounded-xl border border-slate-700/50">
                            <div class="p-4 rounded-xl bg-slate-800 border border-slate-700 text-center min-w-[120px]">
                                <span class="text-[10px] text-slate-400 block font-black uppercase">Quá hạn HSD</span>
                                <span id="tv_canhBaoHsd" class="text-4xl font-black font-mono text-rose-500">0</span>
                            </div>
                            <p class="text-xs text-slate-400 leading-relaxed font-bold">Hệ thống tự động rà quét và cảnh báo những khay/mâm dụng cụ đã lưu kho sạch nhưng vượt quá niên hạn sử dụng của vật liệu bao bì. <br><span class="text-rose-400 font-black mt-1 block">* Đề nghị KTV thu hồi các khay báo đỏ trên hệ thống tủ để tiến hành xử lý tiệt khuẩn lại.</span></p>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <!-- VÙNG IN TEM BIXOLON & IN A4 -->
    <div id="print-zone" class="hidden bg-white p-0 font-sans z-[9999]"></div>

    <!-- POPUP CAMERA QUÉT QR/BARCODE -->
    <div id="popupScanner" class="fixed inset-0 popup-overlay z-[99999] hidden flex items-center justify-center p-4">
        <div class="bg-white w-full max-w-sm p-4 rounded-xl shadow-2xl relative">
            <h3 class="text-sm font-black text-slate-700 uppercase border-b border-slate-100 pb-3 mb-2 flex justify-between items-center">
                <span><i class="fa-solid fa-camera text-sky-500 mr-2"></i> Quét Mã Vạch</span>
                <button onclick="dongCamera()" class="text-rose-500 hover:text-rose-700"><i class="fa-solid fa-xmark text-lg"></i></button>
            </h3>
            <div id="reader"></div>
            <p id="camStatus" class="text-[10px] text-center text-slate-500 mt-3 italic font-bold">Đang khởi động Camera Sau...</p>
        </div>
    </div>

    <!-- POPUP KIỂM ĐẾM CHI TIẾT LINH KIỆN -->
    <div id="popupKiemDem" class="fixed inset-0 bg-slate-900/60 backdrop-blur-sm flex justify-center items-center z-[9999] hidden p-4 animate-fade-in">
        <div class="bg-white rounded-2xl shadow-2xl border border-slate-200 w-full max-w-2xl flex flex-col max-h-[90vh] overflow-hidden transform transition-all scale-100">
            <div class="bg-gradient-to-r from-sky-700 to-sky-600 px-6 py-4 flex items-center justify-between shadow-md">
                <div class="flex items-center gap-3">
                    <div class="bg-white/10 p-2 rounded-lg text-white">
                        <i class="fa-solid fa-clipboard-check text-lg"></i>
                    </div>
                    <div>
                        <h3 class="text-white font-black text-sm uppercase tracking-wide">Kiểm toán & Đếm cơ số đầu vào</h3>
                        <p class="text-sky-100 text-[11px] font-medium mt-0.5">Trạm 2: Xe thu gom nhận bàn giao dụng cụ bẩn</p>
                    </div>
                </div>
                <button type="button" onclick="closePopupKiemDem()" class="text-white/80 hover:text-white bg-white/10 hover:bg-white/20 p-1.5 rounded-lg transition-colors">
                    <i class="fa-solid fa-xmark text-sm"></i>
                </button>
            </div>
            <div class="bg-slate-50 border-b border-slate-200 px-6 py-3 flex flex-wrap justify-between items-center gap-2">
                <div class="flex items-center gap-2">
                    <span class="text-[11px] font-bold text-slate-400 uppercase">Bộ dụng cụ:</span>
                    <span id="popBo" class="text-xs font-black text-sky-800 uppercase bg-sky-50 border border-sky-200 px-2.5 py-1 rounded">--</span>
                </div>
                <div class="flex items-center gap-2">
                    <span class="text-[11px] font-bold text-slate-400 uppercase">Khoa hoàn trả:</span>
                    <span id="popKhoa" class="text-xs font-black text-rose-700 uppercase bg-rose-50 border border-rose-100 px-2.5 py-1 rounded">--</span>
                </div>
            </div>
            <div class="flex-1 overflow-y-auto p-6 bg-slate-50/50">
                <div class="mb-3 flex justify-between items-center px-1">
                    <span class="text-[11px] font-black text-slate-500 uppercase tracking-wider"><i class="fa-solid fa-list-ol mr-1"></i>Danh mục linh kiện trong mâm</span>
                    <span class="text-[10px] bg-slate-200 text-slate-600 font-bold px-2 py-0.5 rounded-full">Đối soát Realtime</span>
                </div>
                <div id="popKiemDemChecklist" class="bg-white border border-slate-200 rounded-xl p-2 shadow-sm space-y-1 divide-y divide-slate-100"></div>
            </div>
            <div class="p-6 bg-white border-t border-slate-100 shadow-inner">
                <label class="block text-xs font-black text-slate-700 uppercase tracking-wide mb-2 flex items-center justify-between">
                    <span><i class="fa-solid fa-comment-medical text-slate-400 mr-1"></i> Nhật ký đối soát / Lý do chênh lệch</span>
                    <span class="text-[10px] text-rose-500 font-bold lowercase italic select-none">*bắt buộc nhập nếu thiếu hoặc hỏng đồ</span>
                </label>
                <textarea id="popGhiChu" rows="2" placeholder="Ví dụ: Khoa Ngoại chấn thương xác nhận làm mất 1 kẹp phẫu tích trong phòng mổ..." class="w-full text-xs font-medium p-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-sky-500 focus:border-sky-500 placeholder-slate-400 shadow-sm transition-all"></textarea>
            </div>
            <div class="bg-slate-50 px-6 py-4 border-t border-slate-200 flex justify-end items-center gap-3">
                <button type="button" onclick="closePopupKiemDem()" class="bg-white border border-slate-300 text-slate-700 hover:bg-slate-100 font-bold py-2.5 px-4 rounded-xl shadow-sm text-xs transition-colors uppercase">Hủy bỏ</button>
                <button type="button" onclick="saveKiemDem()" class="bg-gradient-to-r from-emerald-600 to-emerald-500 text-white hover:from-emerald-700 hover:to-emerald-600 font-black py-2.5 px-6 rounded-xl shadow-md text-xs transition-all uppercase tracking-wider flex items-center gap-2"><i class="fa-solid fa-share-from-square"></i> Chốt và Chuyển sang rửa</button>
            </div>
        </div>
    </div>

    <!-- POPUP ĐÓNG GÓI -->
    <div id="popupDongGoi" class="fixed inset-0 popup-overlay z-[9999] hidden flex items-center justify-center p-4">
        <div class="glass-panel w-full max-w-lg p-6 border-t-4 border-t-sky-500">
            <h3 class="text-sm font-black text-slate-700 uppercase border-b border-slate-100 pb-3 mb-2">Đóng gói & Kiểm tra</h3>
            <div class="bg-slate-50 p-2 rounded border border-slate-100 mb-2"><span class="text-[10px] text-slate-500 uppercase font-bold">Mâm:</span> <span id="popDG_Bo" class="font-bold text-sky-700 text-sm ml-1">--</span></div>
            <div>
                <label class="block text-[11px] font-bold text-slate-500 mb-1.5">Chất liệu đóng gói & HSD tương ứng:</label>
                <select id="popDG_Loai" onchange="tinhHanSuDung()" class="w-full p-2.5 text-xs font-bold border border-slate-300 rounded bg-white text-slate-800">
                    <option value="Vải|7">Vải (HSD 7 ngày)</option>
                    <option value="Túi ép nhiệt độ cao|30" selected>Túi ép nhiệt độ cao (HSD 30 ngày)</option>
                    <option value="Hộp chuyên dụng|90">Hộp chuyên dụng (HSD 90 ngày)</option>
                    <option value="Túi ép nhiệt độ thấp (Tyvek)|90">Túi ép nhiệt độ thấp (Tyvek) (HSD 90 ngày)</option>
                    <option value="Giấy gói chuyên dụng (vải không dệt)|30">Giấy gói chuyên dụng (vải không dệt) (HSD 30 ngày)</option>
                </select>
            </div>
            <div class="text-center mt-3"><p class="text-[10px] font-bold text-slate-500 uppercase">HSD Dự kiến:</p><p id="popDG_Han" class="text-xl font-black text-sky-700 mt-1">--/--/----</p></div>
            <div class="flex space-x-2 mt-5"><button onclick="closePopupDongGoi()" class="w-1/3 bg-slate-100 text-slate-600 font-bold py-3 rounded text-xs">HỦY</button><button onclick="chotDongGoi()" class="w-2/3 btn-primary font-bold py-3 rounded text-xs shadow-md">CHỐT GÓI MÂM</button></div>
        </div>
    </div>

    <!-- POPUP KÝ CHỮ KÝ ĐIỆN TỬ TRÊN CANVAS (BỔ SUNG MỚI CHUẨN Y TẾ) -->
    <div id="popupKyDienTu" class="fixed inset-0 bg-slate-900/60 backdrop-blur-sm flex justify-center items-center z-[99999] hidden p-4 animate-fade-in">
        <div class="bg-white rounded-2xl shadow-2xl border border-slate-200 w-full max-w-md overflow-hidden">
            <div class="bg-purple-700 px-5 py-3.5 flex justify-between items-center text-white">
                <h3 class="font-black text-xs uppercase tracking-wider flex items-center gap-2">
                    <i class="fa-solid fa-pen-nib"></i> Ký Xác Nhận Tiếp Nhận Đồ Sạch
                </h3>
                <button type="button" onclick="dongPopupKyDienTu()" class="text-white/80 hover:text-white font-bold text-lg">&times;</button>
            </div>
            <div class="p-5 space-y-4">
                <div>
                    <label class="block text-[11px] font-black text-slate-600 uppercase mb-1">Họ tên điều dưỡng nhận:</label>
                    <input type="text" id="ky_tenNguoiNhan" placeholder="Nhập tên người ký..." class="w-full px-3 py-2 text-xs font-bold border border-slate-300 rounded-lg focus:ring-2 focus:ring-purple-500 outline-none">
                </div>
                <div>
                    <div class="flex justify-between items-center mb-1">
                        <label class="block text-[11px] font-black text-slate-600 uppercase">Vẽ chữ ký trực tiếp:</label>
                        <button type="button" onclick="xoaChuKyCanvas()" class="text-[10px] text-rose-600 font-bold hover:underline"><i class="fa-solid fa-rotate-right mr-1"></i>Vẽ lại</button>
                    </div>
                    <div class="border-2 border-dashed border-slate-300 rounded-xl bg-slate-50 flex justify-center items-center overflow-hidden">
                        <canvas id="canvasKyDienTu" class="w-full h-40 cursor-crosshair bg-white"></canvas>
                    </div>
                </div>
            </div>
            <div class="bg-slate-50 px-5 py-3 border-t border-slate-200 flex justify-end gap-2">
                <button type="button" onclick="dongPopupKyDienTu()" class="px-4 py-2 bg-white border border-slate-300 rounded-lg text-xs font-bold text-slate-700 uppercase">Hủy</button>
                <button type="button" onclick="luuXacNhanKyNhan()" class="px-5 py-2 bg-purple-600 hover:bg-purple-700 text-white rounded-lg text-xs font-black uppercase shadow-md"><i class="fa-solid fa-check mr-1"></i>Xác nhận & Nhập tủ</button>
            </div>
        </div>
    </div>

    <!-- POPUP GHI NHẬN SỬ DỤNG BỆNH NHÂN -->
    <div id="popupSuDungBoDungCu" class="fixed inset-0 bg-slate-900/60 backdrop-blur-sm flex justify-center items-center z-[110] hidden">
        <div class="bg-white w-full max-w-5xl rounded-xl shadow-2xl overflow-hidden animate-in fade-in zoom-in-95 duration-200">
            <div class="bg-teal-600 text-white px-6 py-4 flex justify-between items-center">
                <h2 class="font-black text-sm flex items-center gap-2 uppercase tracking-wider"><i class="fa-solid fa-user-injured text-base"></i> Ghi Nhận Sử Dụng Bộ Dụng Cụ Trên Bệnh Nhân</h2>
                <button onclick="closePopupSuDung()" type="button" class="text-white/80 hover:text-white font-bold text-2xl outline-none">&times;</button>
            </div>
            <div class="p-6 space-y-4 max-h-[80vh] overflow-y-auto">
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                    <div>
                        <label class="text-[11px] font-black text-slate-500 uppercase">Khoa phòng bệnh nhân <span class="text-rose-500 font-bold">*</span></label>
                        <input type="text" id="sd_khoaBenhNhan" class="w-full border-b-2 border-slate-200 focus:border-teal-500 outline-none py-1.5 text-xs font-bold text-slate-800 transition-colors">
                    </div>
                    <div>
                        <label class="text-[11px] font-black text-slate-500 uppercase">Điều dưỡng vòng trong (Phòng mổ) <span class="text-rose-500 font-bold">*</span></label>
                        <input type="text" id="sd_yTaPhongMo" class="w-full border-b-2 border-slate-200 focus:border-teal-500 outline-none py-1.5 text-xs font-bold text-slate-800 transition-colors">
                    </div>
                    <div>
                        <label class="text-[11px] font-black text-slate-500 uppercase">Điều dưỡng vòng ngoài </label>
                        <input type="text" id="sd_yTaVongNgoai" class="w-full border-b-2 border-slate-200 focus:border-teal-500 outline-none py-1.5 text-xs font-medium text-slate-800 transition-colors">
                    </div>
                </div>
                <div class="flex flex-col md:flex-row gap-4 items-end bg-slate-50 p-3 rounded-lg border border-slate-200">
                    <div class="flex-1 w-full">
                        <label class="text-[11px] font-black text-slate-600 uppercase">Thông Tin Xác Thực Bệnh Nhân (Mã MRN / Họ Tên) <span class="text-rose-500 font-bold">*</span></label>
                        <div class="relative mt-1">
                            <input type="text" id="sd_searchBN" placeholder="Nhập mã bệnh án MRN hoặc tên bệnh nhân..." class="w-full border border-slate-300 rounded px-3 py-2 pl-9 text-xs font-bold text-teal-900 outline-none focus:border-teal-500 bg-white">
                            <i class="fa-solid fa-search absolute left-3 top-2.5 text-slate-400 text-xs"></i>
                        </div>
                    </div>
                    <label class="flex items-center gap-2 pb-2.5 cursor-pointer text-xs font-bold text-slate-600 select-none shrink-0">
                        <input type="checkbox" id="sd_chkXuatVien" class="w-4 h-4 rounded text-teal-600 focus:ring-teal-500"> Bao gồm hồ sơ đã xuất viện
                    </label>
                </div>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                    <div>
                        <label class="text-[11px] font-black text-slate-500 uppercase">Ngày phẫu thuật / sử dụng</label>
                        <input type="date" id="sd_ngaySuDung" class="w-full border-b-2 border-slate-200 outline-none py-1 text-xs font-bold text-slate-700">
                    </div>
                    <div>
                        <label class="text-[11px] font-black text-slate-500 uppercase">Nhân chứng thực hiện <span class="text-rose-500 font-bold">*</span></label>
                        <input type="text" id="sd_nhanChung" readonly class="w-full border-b-2 border-slate-100 bg-slate-50/50 outline-none py-1 text-xs font-black text-slate-600">
                    </div>
                    <div>
                        <label class="text-[11px] font-black text-slate-500 uppercase">Ghi chú lâm sàng bổ sung</label>
                        <input type="text" id="sd_ghiChu" class="w-full border-b-2 border-slate-200 focus:border-teal-500 outline-none py-1 text-xs text-slate-700 transition-colors">
                    </div>
                </div>
                <div class="pt-2 border-t border-dashed border-slate-200">
                    <label class="text-[11px] font-black text-teal-800 uppercase flex items-center gap-1"><i class="fa-solid fa-qrcode"></i> Quét Hoặc Nhập Mã Vạch Khay Dụng Cụ Phục Vụ Ca Mổ:</label>
                    <div class="flex gap-2 mt-1">
                        <input type="text" id="sd_maKhayInp" onkeypress="if(event.key==='Enter') scanKhayVaoSuDung()" placeholder="Quét barcode định hình dán trên tem khay vô khuẩn..." class="w-full border border-slate-300 rounded px-3 py-2.5 font-mono font-black text-sm bg-amber-50/60 text-amber-900 uppercase focus:border-teal-500 outline-none tracking-wider">
                        <button onclick="moCamera('sd_maKhayInp')" type="button" class="bg-slate-800 hover:bg-slate-900 text-white px-5 rounded shadow"><i class="fa-solid fa-camera"></i></button>
                    </div>
                </div>
                <div class="border border-slate-200 rounded-lg overflow-hidden mt-3 shadow-sm bg-white">
                    <table class="w-full text-left text-xs border-collapse">
                        <thead>
                            <tr class="bg-slate-100 border-b font-black text-slate-600 text-[10px] uppercase tracking-wide">
                                <th class="p-3 text-center w-12">Chọn</th>
                                <th class="p-3 w-[25%]">Mã định hình khay</th>
                                <th class="p-3 w-[40%]">Tên Bộ Dụng Cụ Lâm Sàng</th>
                                <th class="p-3 text-center w-[15%]">Số mẻ hấp</th>
                                <th class="p-3 text-center w-[12%]">Trạng thái</th>
                                <th class="p-3 text-center w-10">Xóa</th>
                            </tr>
                        </thead>
                        <tbody id="sd_bangKhayChon" class="divide-y divide-slate-100 text-slate-700">
                            <tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng quét mã khay dụng cụ để liên kết vào hồ sơ...</td></tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="bg-slate-50 px-6 py-4 flex justify-end gap-3 border-t border-slate-200 shadow-inner">
                <button onclick="savePopupSuDung()" type="button" class="bg-teal-600 hover:bg-teal-700 text-white font-black px-6 py-2.5 rounded shadow text-xs uppercase flex items-center gap-1.5 transition-all"><i class="fa-solid fa-floppy-disk text-sm"></i> Lưu Hồ Sơ Bệnh Án</button>
                <button onclick="closePopupSuDung()" type="button" class="bg-white hover:bg-slate-100 border text-slate-700 font-bold px-6 py-2.5 rounded shadow text-xs uppercase flex items-center gap-1.5 transition-all"><i class="fa-solid fa-rotate-left"></i> Hủy</button>
            </div>
        </div>
    </div>

    <!-- SCRIPT CHUYỂN TAB TẠI CHỖ & ĐỒNG BỘ ĐỒNG HỒ -->
    <script>
        function switchLamsangSubtab(activeTabId) {
            const tabs = ['lssub-tab-tra-ban', 'lssub-tab-nhan-sach', 'lssub-tab-doi-luu'];
            
            tabs.forEach(tabId => {
                const el = document.getElementById(tabId);
                if (el) el.classList.add('hidden');
            });
            
            const activeEl = document.getElementById(activeTabId);
            if (activeEl) activeEl.classList.remove('hidden');
            
            const btnTraBan = document.getElementById('btn-lssub-tra-ban');
            const btnNhanSach = document.getElementById('btn-lssub-nhan-sach');
            const btnDoiLuu = document.getElementById('btn-lssub-doi-luu');
            
            [btnTraBan, btnNhanSach, btnDoiLuu].forEach(btn => {
                if(btn) {
                    btn.className = "px-4 py-2 text-xs font-black rounded-lg transition-all text-slate-600 hover:text-slate-900 flex items-center gap-1.5";
                }
            });
            
            const activeBtn = document.getElementById('btn-' + activeTabId);
            if (activeBtn) {
                if (activeTabId === 'lssub-tab-tra-ban') {
                    activeBtn.className = "px-4 py-2 text-xs font-black rounded-lg transition-all flex items-center gap-1.5 bg-white text-rose-700 shadow-sm border border-slate-200";
                } else if (activeTabId === 'lssub-tab-nhan-sach') {
                    activeBtn.className = "px-4 py-2 text-xs font-black rounded-lg transition-all flex items-center gap-1.5 bg-white text-emerald-700 shadow-sm border border-slate-200";
                } else if (activeTabId === 'lssub-tab-doi-luu') {
                    activeBtn.className = "px-4 py-2 text-xs font-black rounded-lg transition-all flex items-center gap-1.5 bg-white text-teal-700 shadow-sm border border-slate-200";
                }
            }
        }
        
        setInterval(() => {
            const timeString = new Date().toLocaleString('vi-VN');
            const tvClock = document.getElementById('tv_clock');
            if(tvClock) tvClock.innerText = "THỜI GIAN THỰC: " + timeString;

            const mainClock = document.getElementById('clock');
            if(mainClock) mainClock.innerText = "THỜI GIAN: " + timeString;
        }, 1000);
    </script>
    <script src="app.js"></script>
</body>
</html>
Anh chỉ cần lưu đoạn mã này đè vào file index.html hiện tại là hai file index.html và app.js sẽ hoàn toàn khớp với nhau 100%! Anh test thử lại giao diện và tính năng ký tên nhé!

/* =========================================================================

   1. CẤU HÌNH CƠ BẢN & GIAO DIỆN CHUNG (CSSD PHUONG NAM HOSPITAL)

   ========================================================================= */

html, body { 

  max-width: 100%; 

  overflow-x: hidden; 

  height: 100%; 

  background-color: #f1f5f9; 

  color: #1e293b; 

  font-family: system-ui, -apple-system, sans-serif; 

}



/* Thanh cuộn ẩn */

.scrollbar-none::-webkit-scrollbar { 

  display: none; 

}



/* Sidebar & Menu Active */

.sidebar-item-active { 

  background-color: #e0f2fe !important; 

  color: #0284c7 !important; 

  border-left: 4px solid #0284c7; 

  font-weight: 800; 

}



/* Form Controls & Focus */

input, select, textarea { 

  background-color: #ffffff; 

  border: 1px solid #cbd5e1; 

  color: #0f172a; 

  border-radius: 0.375rem; 

  font-size: 13px; 

}



input:focus, select:focus, textarea:focus { 

  outline: none; 

  border-color: #0284c7; 

  box-shadow: 0 0 0 3px rgba(2, 132, 199, 0.15); 

}



/* Glass Panel Style */

.glass-panel { 

  background: #ffffff; 

  border: 1px solid #e2e8f0; 

  border-radius: 0.75rem; 

  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05); 

  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);

}



/* Primary Button */

.btn-primary { 

  background: linear-gradient(to right, #0284c7, #0369a1); 

  color: white; 

}



.btn-primary:hover { 

  background: linear-gradient(to right, #0369a1, #075985); 

}



/* Checkbox Trạm Hấp & Nghiệm Thu */

.hap-checkbox, .nghiemthu-checkbox { 

  width: 1.25rem; 

  height: 1.25rem; 

  accent-color: #0284c7; 

  cursor: pointer; 

}



/* Popups & Overlays */

.popup-overlay { 

  background: rgba(15, 23, 42, 0.7); 

  backdrop-filter: blur(2px); 

}



/* Table Headers */

th { 

  font-size: 11px; 

  text-transform: uppercase; 

  letter-spacing: 0.05em; 

  color: #64748b; 

  background-color: #f8fafc; 

  font-weight: 700; 

}



/* Subtabs Admin */

.admin-subtab-active { 

  background-color: #0284c7; 

  color: white !important; 

}



/* Phân quyền Chế độ Guest */

body.guest-mode .action-btn, 

body.guest-mode .action-col { 

  display: none !important; 

  pointer-events: none; 

  opacity: 0.5; 

}



/* Khung Camera Quét Mã QR / Barcode */

#reader { 

  width: 100%; 

  border-radius: 8px; 

  overflow: hidden; 

  border: 2px solid #0284c7; 

  background: #000; 

}



#reader video { 

  object-fit: cover; 

  width: 100%; 

  height: 100%; 

}



/* Utility Classes */

.flex-wrap { flex-wrap: wrap; }

.gap-1 { gap: 0.25rem; }

.transition-colors { 

  transition-property: color, background-color, border-color, text-decoration-color, fill, stroke; 

  transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1); 

  transition-duration: 150ms; 

}



/* =========================================================================

   2. TỐI ƯU CẢM ỨNG TABLET & THAO TÁC NGÓN TAY (TOUCH-TARGET)

   ========================================================================= */



/* Chuẩn vùng chạm ngón tay trên Tablet (min 44px - 48px) */

.touch-btn {

  min-height: 48px;

  display: inline-flex;

  align-items: center;

  justify-content: center;

}



/* Hiệu ứng phản hồi vật lý khi chạm/bấm nút trên màn hình cảm ứng */

button:active, 

.action-btn:active {

  transform: scale(0.97);

  opacity: 0.9;

}



/* Tùy chỉnh thanh cuộn nhỏ gọn, mượt mà cho danh sách khay kệ vô khuẩn */

.overflow-auto::-webkit-scrollbar,

.overflow-y-auto::-webkit-scrollbar {

  width: 5px;

  height: 5px;

}



.overflow-auto::-webkit-scrollbar-track,

.overflow-y-auto::-webkit-scrollbar-track {

  background: #f1f5f9;

}



.overflow-auto::-webkit-scrollbar-thumb,

.overflow-y-auto::-webkit-scrollbar-thumb {

  background-color: #cbd5e1;

  border-radius: 4px;

}



.overflow-auto::-webkit-scrollbar-thumb:hover,

.overflow-y-auto::-webkit-scrollbar-thumb:hover {

  background-color: #94a3b8;

}



/* =========================================================================

   3. CẤU HÌNH IN TEM BIXOLON SLP-TX403 (KHỔ 80mm x 50mm)

   ========================================================================= */

@media print {

  /* Ẩn toàn bộ giao diện web, chỉ chừa vùng in */

  body * {

    visibility: hidden !important;

  }

  

  #print-zone, #print-zone * {

    visibility: visible !important;

  }

  

  #print-zone {

    position: absolute !important;

    left: 0 !important;

    top: 0 !important;

    width: 80mm !important;

    margin: 0 !important;

    padding: 0 !important;

  }



  /* Khai báo kích thước trang in Bixolon 80mm x 50mm */

  @page {

    size: 80mm 50mm;

    margin: 0mm; /* Xóa lề mặc định của trình duyệt */

  }



  /* Khung tem đơn lẻ */

  .bixolon-label {

    width: 80mm !important;

    height: 50mm !important;

    padding: 3mm 4mm !important;

    box-sizing: border-box !important;

    page-break-after: always;

    display: flex !important;

    flex-direction: column !important;

    justify-content: space-between !important;

    font-family: Arial, sans-serif !important;

    background: #fff !important;

    color: #000 !important;

  }

}

Bạn thêm vô code style.css dùm tôi

Đã cập nhật toàn bộ file style.css (CSSD Phương Nam Hospital V29.0).

Bộ mã này đã gom trọn cấu hình nền tảng, giao diện responsive tablet/mobile, các lớp tương tác nét ký/canvas, cùng bộ quy tắc in hai chế độ độc lập (In Tem Nhiệt Bixolon 80x50mm & In Biên Bản Giao Nhận A4).

File style.css đã hợp nhất & tối ưu hoàn chỉnh
CSS
/* =========================================================================
   1. CẤU HÌNH CƠ BẢN & GIAO DIỆN CHUNG (CSSD PHUONG NAM HOSPITAL)
   ========================================================================= */
html, body { 
  max-width: 100%; 
  overflow-x: hidden; 
  height: 100%; 
  background-color: #f1f5f9; 
  color: #1e293b; 
  font-family: system-ui, -apple-system, sans-serif; 
}

/* Thanh cuộn ẩn */
.scrollbar-none::-webkit-scrollbar { 
  display: none; 
}

/* Sidebar & Menu Active */
.sidebar-item-active { 
  background-color: #e0f2fe !important; 
  color: #0284c7 !important; 
  border-left: 4px solid #0284c7; 
  font-weight: 800; 
}

/* Form Controls & Focus */
input, select, textarea { 
  background-color: #ffffff; 
  border: 1px solid #cbd5e1; 
  color: #0f172a; 
  border-radius: 0.375rem; 
  font-size: 13px; 
}

input:focus, select:focus, textarea:focus { 
  outline: none; 
  border-color: #0284c7; 
  box-shadow: 0 0 0 3px rgba(2, 132, 199, 0.15); 
}

/* Glass Panel Style */
.glass-panel { 
  background: #ffffff; 
  border: 1px solid #e2e8f0; 
  border-radius: 0.75rem; 
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05); 
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

/* Primary Button */
.btn-primary { 
  background: linear-gradient(to right, #0284c7, #0369a1); 
  color: white; 
}

.btn-primary:hover { 
  background: linear-gradient(to right, #0369a1, #075985); 
}

/* Checkbox Trạm Hấp & Nghiệm Thu */
.hap-checkbox, .nghiemthu-checkbox { 
  width: 1.25rem; 
  height: 1.25rem; 
  accent-color: #0284c7; 
  cursor: pointer; 
}

/* Popups & Overlays */
.popup-overlay { 
  background: rgba(15, 23, 42, 0.7); 
  backdrop-filter: blur(2px); 
}

/* Table Headers */
th { 
  font-size: 11px; 
  text-transform: uppercase; 
  letter-spacing: 0.05em; 
  color: #64748b; 
  background-color: #f8fafc; 
  font-weight: 700; 
}

/* Subtabs Admin */
.admin-subtab-active { 
  background-color: #0284c7; 
  color: white !important; 
}

/* Phân quyền Chế độ Guest */
body.guest-mode .action-btn, 
body.guest-mode .action-col { 
  display: none !important; 
  pointer-events: none; 
  opacity: 0.5; 
}

/* Khung Camera Quét Mã QR / Barcode */
#reader { 
  width: 100%; 
  border-radius: 8px; 
  overflow: hidden; 
  border: 2px solid #0284c7; 
  background: #000; 
}

#reader video { 
  object-fit: cover; 
  width: 100%; 
  height: 100%; 
}

/* Utility Classes */
.flex-wrap { flex-wrap: wrap; }
.gap-1 { gap: 0.25rem; }
.transition-colors { 
  transition-property: color, background-color, border-color, text-decoration-color, fill, stroke; 
  transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1); 
  transition-duration: 150ms; 
}

/* =========================================================================
   2. TỐI ƯU CẢM ỨNG TABLET & THAO TÁC NGÓN TAY (TOUCH-TARGET)
   ========================================================================= */

/* Chuẩn vùng chạm ngón tay trên Tablet (min 44px - 48px) */
.touch-btn {
  min-height: 48px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

/* Hiệu ứng phản hồi vật lý khi chạm/bấm nút trên màn hình cảm ứng */
button:active, 
.action-btn:active {
  transform: scale(0.97);
  opacity: 0.9;
}

/* Tùy chỉnh thanh cuộn nhỏ gọn, mượt mà cho danh sách khay kệ vô khuẩn */
.overflow-auto::-webkit-scrollbar,
.overflow-y-auto::-webkit-scrollbar {
  width: 5px;
  height: 5px;
}

.overflow-auto::-webkit-scrollbar-track,
.overflow-y-auto::-webkit-scrollbar-track {
  background: #f1f5f9;
}

.overflow-auto::-webkit-scrollbar-thumb,
.overflow-y-auto::-webkit-scrollbar-thumb {
  background-color: #cbd5e1;
  border-radius: 4px;
}

.overflow-auto::-webkit-scrollbar-thumb:hover,
.overflow-y-auto::-webkit-scrollbar-thumb:hover {
  background-color: #94a3b8;
}

/* Canvas ký chữ ký điện tử */
#canvasKyDienTu {
  touch-action: none; /* Ngăn cuộn trang khi vẽ chữ ký trên màn hình cảm ứng */
}

/* =========================================================================
   3. CẤU HÌNH IN DÉP ĐÔI: IN TEM BIXOLON (80x50mm) & IN VĂN BẢN (A4)
   ========================================================================= */
@media print {
  /* Ẩn toàn bộ ứng dụng web, chỉ hiển thị vùng in chuyên dụng */
  body * {
    visibility: hidden !important;
  }
  
  #print-zone, #print-zone * {
    visibility: visible !important;
  }
  
  #print-zone {
    position: absolute !important;
    left: 0 !important;
    top: 0 !important;
    margin: 0 !important;
    padding: 0 !important;
  }

  /* ---------------------------------------------------------------------
     CHẾ ĐỘ 1: IN TEM NHIỆT BIXOLON SLP-TX403 (80mm x 50mm)
     --------------------------------------------------------------------- */
  body.print-mode-bixolon #print-zone {
    width: 80mm !important;
  }

  body.print-mode-bixolon @page {
    size: 80mm 50mm;
    margin: 0mm; /* Bỏ hoàn toàn lề lãng phí */
  }

  .bixolon-label {
    width: 80mm !important;
    height: 50mm !important;
    padding: 3mm 4mm !important;
    box-sizing: border-box !important;
    page-break-after: always;
    display: flex !important;
    flex-direction: column !important;
    justify-content: space-between !important;
    font-family: Arial, sans-serif !important;
    background: #fff !important;
    color: #000 !important;
  }

  /* ---------------------------------------------------------------------
     CHẾ ĐỘ 2: IN BÁO CÁO / BIÊN BẢN GIAO NHẬN (KHỔ A4)
     --------------------------------------------------------------------- */
  body.print-mode-doc #print-zone {
    width: 100% !important;
  }

  body.print-mode-doc @page {
    size: A4 portrait;
    margin: 15mm;
  }

  .doc-page {
    width: 100% !important;
    font-family: "Times New Roman", Times, serif !important;
    color: #000 !important;
    background: #fff !important;
  }
}
💡 Các điểm bổ sung trong file CSS này:
touch-action: none; cho #canvasKyDienTu: Giúp Điều dưỡng vẽ chữ ký trên iPad/máy tính bảng mượt mà, không bị hiện tượng cuộn trang (page drag) theo tay.

Cấu hình song song hai chế độ in: Định nghĩa sẵn body.print-mode-bixolon (cho tem dán mâm 80x50mm) và body.print-mode-doc (cho Biên bản giao nhận A4) để khi xuất lệnh in không bị lệch khổ giấy hay vỡ khung.

Sau khi tôi "Ký xác nhận vào tủ kho" bằng chữ ký điện tử xong thì qa "In biên bản" (hình ảnh) vẫn chưa thấy cập nhận hay vô đâu để xem chữ ký nè bạn

Chào anh Hùng, nhìn vào hình preview in biên bản A4 anh gửi, em thấy rõ nguyên nhân rồi: Hàm inHoaDonGiaoNhan() trong app.js đang render mẫu HTML tĩnh, đoạn chân trang mới chỉ viết chữ cố định (Ký và ghi rõ họ tên) chứ chưa chèn thẻ <img> chứa dữ liệu ảnh chữ ký Base64 mà Điều dưỡng vừa vẽ vào.

Ngoài ra, dữ liệu chữ ký sau khi bấm "Xác nhận & Nhập tủ" cần được lưu trữ vào localStorage (hoặc biến lưu trữ giao dịch của Khoa) để khi bấm "IN BIÊN BẢN GIAO NHẬN", hàm in có thể lấy ra và hiển thị lên giấy.

Cách khắc phục triệt để trong app.js
Anh mở file app.js và thực hiện 2 cập nhật nhỏ sau:

1. Cập nhật hàm luuXacNhanKyNhan()
Khi người dùng vẽ xong và bấm nút xác nhận, ta cần lấy dữ liệu ảnh Canvas (toDataURL()) và tên người ký lưu vào localStorage theo từng Khoa:

JavaScript
function luuXacNhanKyNhan() {
    const canvas = document.getElementById('canvasKyDienTu');
    const tenNguoiNhan = document.getElementById('ky_tenNguoiNhan').value.trim();
    const currentKhoa = document.getElementById('khoa_selKhoa').value;

    if (!tenNguoiNhan) {
        alert("Vui lòng nhập họ tên điều dưỡng tiếp nhận!");
        return;
    }

    // Lấy chuỗi mã hóa ảnh chữ ký Base64 từ Canvas
    const signatureImageBase64 = canvas.toDataURL("image/png");

    // Lưu trữ thông tin chữ ký theo Khoa phòng hiện tại vào localStorage
    const keyKyNhan = `signature_${currentKhoa}`;
    const dataSignature = {
        tenNguoiNhan: tenNguoiNhan,
        chuKyImg: signatureImageBase64,
        timeKy: new Date().toLocaleString('vi-VN')
    };
    localStorage.setItem(keyKyNhan, JSON.stringify(dataSignature));

    // Thực hiện logic chuyển mâm vào tủ khoa hiện tại của anh...
    // ... (Code chuyển trạng thái mâm hiện tại) ...

    alert("Đã ký nhận và cập nhật vào tủ kho thành công!");
    dongPopupKyDienTu();
    callRender(); // Render lại giao diện
}
2. Cập nhật hàm inHoaDonGiaoNhan()
Trong phần render giao diện in A4 của inHoaDonGiaoNhan(), anh thêm đoạn mã kiểm tra xem Khoa này đã có chữ ký lưu trong localStorage chưa. Nếu có thì hiển thị ảnh chữ ký + Họ tên trực tiếp dưới cột ĐIỀU DƯỠNG LÂM SÀNG:

JavaScript
function inHoaDonGiaoNhan() {
    const currentKhoa = document.getElementById('khoa_selKhoa').value || "KHOA LÂM SÀNG";
    const keyKyNhan = `signature_${currentKhoa}`;
    const savedSignature = JSON.parse(localStorage.getItem(keyKyNhan) || "null");

    // Lớp hiển thị chữ ký
    let htmlChuKyDieuDuong = `<p style="font-style: italic; color: #888;">(Ký và ghi rõ họ tên)</p>`;
    
    if (savedSignature && savedSignature.chuKyImg) {
        htmlChuKyDieuDuong = `
            <div style="text-align: center; margin-top: 5px;">
                <img src="${savedSignature.chuKyImg}" style="max-height: 70px; margin: 0 auto; display: block;" alt="Chữ ký điện tử"/>
                <p style="font-weight: bold; margin-top: 5px; text-transform: uppercase;">${savedSignature.tenNguoiNhan}</p>
                <p style="font-size: 10px; color: #555;">Ký lúc: ${savedSignature.timeKy}</p>
            </div>
        `;
    }

    // Gán vào khung HTML In A4 (print-zone)
    const printZone = document.getElementById('print-zone');
    document.body.className = "print-mode-doc"; // Chuyển chế độ in A4
    
    printZone.innerHTML = `
        <div class="doc-page">
            <!-- Phần bảng dữ liệu dụng cụ ở trên giữ nguyên -->
            ...
            
            <!-- CHÂN TRANG KÝ TÊN -->
            <table style="width: 100%; margin-top: 30px; text-align: center; border: none;">
                <tr>
                    <td style="width: 50%; border: none; vertical-align: top;">
                        <p style="font-weight: bold; text-uppercase: uppercase;">ĐIỀU DƯỠNG LÂM SÀNG</p>
                        ${htmlChuKyDieuDuong}
                    </td>
                    <td style="width: 50%; border: none; vertical-align: top;">
                        <p style="font-weight: bold; text-uppercase: uppercase;">CHUYÊN VIÊN CSSD</p>
                        <p style="font-style: italic; color: #888; margin-top: 40px;">(Ký và ghi rõ họ tên)</p>
                    </td>
                </tr>
            </table>
        </div>
    `;

    window.print();
}
Xem lại chữ ký điện tử ở đâu trên giao diện?
Nếu anh muốn điều dưỡng xem lại chữ ký trực tiếp trên màn hình web (không cần bấm In):

Tại Sub-tab 3: SỔ GIAO NHẬN, anh có thể đặt 1 ô nhỏ ở góc dưới ghi: Chữ ký nhận gần nhất: [Xem chữ ký] hoặc hiển thị trực tiếp ảnh thumb chữ ký bên cạnh nút IN BIÊN BẢN GIAO NHẬN.

Khi bấm xem, Popup Canvas hoặc 1 thẻ <img> sẽ load dữ liệu savedSignature.chuKyImg lên cho anh kiểm tra ngay lập tức.

// =========================================================================

// 1. KHỞI TẠO HỆ THỐNG & CẤU HÌNH BIẾN TOÀN CỤC (PN HOSPITAL)

// =========================================================================

const firebaseConfig = { 

    apiKey: "AIzaSyCxjdCTKHQlpm7SYbWCEws1HhcOaFp0LBA", 

    authDomain: "cssd-system-2878c.firebaseapp.com", 

    projectId: "cssd-system-2878c", 

    storageBucket: "cssd-system-2878c.firebasestorage.app", 

    messagingSenderId: "662377321937", 

    appId: "1:662377321937:web:001c092e10319547623cf0" 

};

firebase.initializeApp(firebaseConfig); 

const db = firebase.firestore();



let thongTinMatKhauAdmin = { adminPIN: "admin2026", cssdPIN: "cssd2026", guestPIN: "guest2026" };

let cauHinhGiaoDien = {

    "ADMIN": ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'],

    "CSSD": ['thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','dashboard_tv'], 

    "KHOA": ['khoaphong','quanlykho','lichsuluanchuyen'], 

    "GUEST": ['quanlykho','danhmuc','lichsuluanchuyen','performance','dashboard_tv']

};



let currentRole = "", loginUserCode = "";

let danhSachKhoa = [], listGiaoDich = [], gioHangTam = [], danhSachKtvCssd = [], databaseExcel = [];

let html5QrCode = null; let targetInputIdForScan = ""; let idDangDongGoi = null;

let activeTab = 'thugom'; let renderTimeout = null;

let duLieuAnhBiTamBase64 = ""; let maLoTruyVetToanCuc = ""; 

let gioHangXuatKho = []; let gioKhaySuDungTam = []; 

let currentKiemDemData = null;



// Biến hỗ trợ vẽ chữ ký điện tử

let canvasSignature = null;

let ctxSignature = null;

let isDrawingSignature = false;

let dsIdKhayChoKyNhan = [];



// Cấu hình máy hấp & máy rửa (Belimed WD250)

const cauHinhMayHap = { "Hấp hơi nước": ["A1", "A2", "A3", "A4"], "Hấp H2O2 (Plasma)": ["P1", "P2"], "Khử khuẩn EO": ["EO1", "EO2"] };

const cauHinhMayRua = {

    "Máy rửa khử khuẩn tự động": ["Belimed WD250"],

    "Máy rửa sóng siêu âm": ["US1"],

    "Bồn rửa thủ công": ["MAN1"]

};

const danhMucHoaChatRua = [

    { ten: "Cidezyme (Enzym phân hủy protein)", lieuLuong: "8 ml/L" },

    { ten: "Sekusept Active (Khử khuẩn mức độ cao)", lieuLuong: "10 ml/L" },

    { ten: "Smeg (Nước rửa trung tính chuyên dụng)", lieuLuong: "5 ml/L" },

    { ten: "Aniosyme DD1 (Làm sạch & khử trùng sơ bộ)", lieuLuong: "5 ml/L" }

];



function getTodayDateStr() { 

    const d = new Date(); 

    return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`; 

}



function playSound(type) {

    try {

        const AudioContext = window.AudioContext || window.webkitAudioContext;

        if (!AudioContext) return;

        const ctx = new AudioContext();

        const osc = ctx.createOscillator();

        const gain = ctx.createGain();

        osc.connect(gain); gain.connect(ctx.destination);

        if (type === 'success') {

            osc.type = 'sine'; osc.frequency.setValueAtTime(800, ctx.currentTime);

            gain.gain.setValueAtTime(0.3, ctx.currentTime);

            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.15);

            osc.start(); osc.stop(ctx.currentTime + 0.15);

        } else {

            osc.type = 'sawtooth'; osc.frequency.setValueAtTime(250, ctx.currentTime);

            gain.gain.setValueAtTime(0.3, ctx.currentTime);

            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.4);

            osc.start(); osc.stop(ctx.currentTime + 0.4);

        }

    } catch(e) {}

}



// =========================================================================

// 2. REALTIME LISTENER (FIRESTORE SYNC)

// =========================================================================

db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").onSnapshot(doc => { 

    try {

        if (doc.exists) { 

            let res = doc.data(); 

            danhSachKhoa = res.danhSachKhoa || []; 

            danhSachKtvCssd = res.danhSachKtvCssd || []; 

            databaseExcel = res.databaseExcel || []; 

            if(res.thongTinMatKhauAdmin) thongTinMatKhauAdmin = Object.assign(thongTinMatKhauAdmin, res.thongTinMatKhauAdmin); 

            if(res.cauHinhGiaoDien) cauHinhGiaoDien = Object.assign(cauHinhGiaoDien, res.cauHinhGiaoDien);

        } 

        initSelects();

        if(activeTab === 'khoaphong') loadBoDungCuTheoKhoa(); 

        renderAdminInterface(); 

        if(currentRole) apDungPhanQuyenGiaoDien(currentRole);

        taiDanhMucLinhKienChuan();

        callRender();

    } catch(err) { console.error("Lỗi đồng bộ danh mục:", err); }

});



db.collection("phieuGiaoNhan").orderBy("id", "desc").limit(1000).onSnapshot(snap => { 

    listGiaoDich = []; 

    snap.forEach(doc => { let d = doc.data(); d.firestoreId = doc.id; listGiaoDich.push(d); }); 

    callRender();

});



function callRender() { clearTimeout(renderTimeout); renderTimeout = setTimeout(() => { renderTheoTabHienTai(); }, 100); }



// =========================================================================

// 3. XỬ LÝ ĐĂNG NHẬP & PHÂN QUYỀN HỆ THỐNG

// =========================================================================

function toggleLoginFields() {

    const r = document.getElementById("login_role").value;

    document.getElementById("field_khoa").classList.toggle("hidden", r !== "KHOA");

    document.getElementById("field_nhanvien_cssd").classList.toggle("hidden", r !== "CSSD");

}



function checkLogin() {

    const role = document.getElementById("login_role").value; 

    const pass = document.getElementById("login_pass").value; 

    let pA = thongTinMatKhauAdmin.adminPIN || "admin2026"; 

    let pC = thongTinMatKhauAdmin.cssdPIN || "cssd2026"; 

    let pG = thongTinMatKhauAdmin.guestPIN || "guest2026"; 

     

    if (role === "ADMIN" && pass === pA) { 

        currentRole = "ADMIN"; loginUserCode = "ADMIN"; document.getElementById("nav_user_info").innerText = "ADMINISTRATOR";

        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');

        apDungPhanQuyenGiaoDien("ADMIN");

        switchTab('lichsuluanchuyen');

    } else if (role === "KHOA") { 

        const khoaSelect = document.getElementById("login_khoa").value; 

        let found = danhSachKhoa.find(x => x.ten === khoaSelect); 

        if (pass === (found ? found.pin : "123")) { 

            currentRole = "KHOA"; loginUserCode = khoaSelect; document.getElementById("nav_user_info").innerText = khoaSelect; 

            if (document.getElementById("khoa_selKhoa")) {

                document.getElementById("khoa_selKhoa").value = khoaSelect;

                document.getElementById("khoa_selKhoa").disabled = true;

            }

            document.body.classList.remove('guest-mode');

            apDungPhanQuyenGiaoDien("KHOA");

            switchTab('khoaphong');

        } else { return showToast("Sai mã PIN Khoa!", "error"); } 

    } else if (role === "CSSD") {

        const nvCode = document.getElementById("login_nv_cssd").value;

        if(!nvCode && pass === pC) { currentRole = "CSSD"; loginUserCode = "CSSD_CHUNG"; document.getElementById("nav_user_info").innerText = "CSSD GENERAL"; } 

        else if (nvCode) {

            let nv = danhSachKtvCssd.find(x => x.code === nvCode);

            if (nv && pass === nv.pin) { currentRole = "CSSD"; loginUserCode = nv.ten; document.getElementById("nav_user_info").innerText = `${nvCode} - ${nv.ten}`; }

            else { return showToast("Sai mã PIN của nhân viên này!", "error"); }

        } else { return showToast("Vui lòng chọn Nhân viên hoặc nhập PIN Backup!", "error"); }

        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');

        apDungPhanQuyenGiaoDien("CSSD");

        let tabsDuocPhep = cauHinhGiaoDien["CSSD"] || [];

        if(tabsDuocPhep.includes('thugom')) switchTab('thugom');

        else if (tabsDuocPhep.length > 0) switchTab(tabsDuocPhep[0]);

    } else if (role === "GUEST") {

        if(pass === pG) {

            currentRole = "GUEST"; loginUserCode = "GUEST"; document.getElementById("nav_user_info").innerText = "KHÁCH THAM QUAN";

            document.body.classList.add('guest-mode'); 

            apDungPhanQuyenGiaoDien("GUEST");

            switchTab('quanlykho');

        } else { return showToast("Sai mã PIN Khách tham quan!", "error"); }

    } else { return showToast("Sai thông tin đăng nhập!", "error"); }

    document.getElementById("login-screen").classList.add("hidden"); document.getElementById("main-app").classList.remove("hidden");

}



function anTatCaHeadersVaMenus() {

    document.getElementById('header-lamsang')?.classList.add('hidden');

    document.getElementById('header-vanhanh')?.classList.add('hidden');

    document.getElementById('header-dulieu')?.classList.add('hidden');

    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => {

        document.getElementById('menu-' + x)?.classList.add('hidden');

    });

}



function apDungPhanQuyenGiaoDien(role) {

    anTatCaHeadersVaMenus();

    if (role === "ADMIN") {

        ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(tab => {

            document.getElementById('menu-' + tab)?.classList.remove('hidden');

        });

        document.getElementById('header-lamsang')?.classList.remove('hidden');

        document.getElementById('header-vanhanh')?.classList.remove('hidden');

        document.getElementById('header-dulieu')?.classList.remove('hidden');

        return;

    }

    let tabsDuocPhep = cauHinhGiaoDien[role] || [];

    tabsDuocPhep.forEach(tab => { document.getElementById('menu-' + tab)?.classList.remove('hidden'); });

    if (tabsDuocPhep.includes('khoaphong')) document.getElementById('header-lamsang')?.classList.remove('hidden');

    if (['thugom','mayrua','donggoi','mayhap','khovokhuan'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-vanhanh')?.classList.remove('hidden');

    if (['quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-dulieu')?.classList.remove('hidden');

}



function switchTab(t) { 

    if (currentRole !== "ADMIN") {

        let tabsDuocPhep = cauHinhGiaoDien[currentRole] || [];

        if(!tabsDuocPhep.includes(t)) { return showToast("Tài khoản không có quyền truy cập!", "error"); }

    }

    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => { 

        document.getElementById('tab-'+x)?.classList.add('hidden'); 

        document.getElementById('menu-'+x)?.classList.remove('sidebar-item-active'); 

    }); 

    document.getElementById('tab-'+t)?.classList.remove('hidden'); 

    document.getElementById('menu-'+t)?.classList.add('sidebar-item-active'); 

    activeTab = t; 

    if (t === 'mayrua') { setTimeout(() => { capNhatDanhSachMaMayRua(); }, 50); }

    if (t === 'mayhap') { setTimeout(() => { tuDongTaoMaLoMeHap(); }, 50); }

    callRender(); 

}



// =========================================================================

// 4. TRẠM 1: CỔNG LÂM SÀNG & HÀNH ĐỘNG ĐỘC LẬP (TRẢ ĐỒ BẨN / YÊU CẦU ĐỒ SẠCH)

// =========================================================================

function loadBoDungCuTheoKhoa() { 

    let k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 

    let list = document.getElementById("listBoDungCu"); 

    if(!list) return; list.innerHTML = ""; 

    let f = danhSachKhoa.find(x => (x.ten || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase()); 

    if (f && f.danhSachBo) {

        let htmlOptions = "";

        f.danhSachBo.forEach(x => {

            if (x) {

                let chuoiX = String(x);

                let tenBoThuanTuy = chuoiX.includes(" [ID:") ? chuoiX.split(" [ID:")[0] : chuoiX;

                htmlOptions += `<option value="${chuoiX}"><option value="${tenBoThuanTuy}">`;

            }

        });

        list.innerHTML = htmlOptions; 

    }

}



function themVaoGio() { 

    let val = document.getElementById("khoa_inpMaBo").value.trim().toUpperCase(); if(!val) return; 

    if (gioHangTam.some(x => x.maMacDinh === val)) { document.getElementById("khoa_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách chờ!", "error"); } 

    let tenGoc = val.includes("[ID:") ? val.split(" [ID:")[0] : val; 

    gioHangTam.push({bo: tenGoc, maMacDinh: val, slYeuCau: 1}); 

    document.getElementById("khoa_inpMaBo").value = ""; 

    renderGioHang(); 

}



function renderGioHang() { 

    let khuVuc = document.getElementById("khuVucGioHang"); if(khuVuc) khuVuc.classList.toggle("hidden", gioHangTam.length===0); 

    let bHang = document.getElementById("bangGioHang");

    if(bHang) {

        bHang.innerHTML = gioHangTam.map(i => `<tr><td class="p-2.5 font-bold text-sky-700 text-[11px]">${i.bo}</td></tr>`).join(''); 

    }

    let badge = document.getElementById("badgeGioHang");

    if(badge) badge.innerText = `${gioHangTam.length} món`;

}



function khoaGuiPhieuTraBatches() { 

    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 

    if(!k) return showToast("Vui lòng chọn Khoa trước!"); 

    if(gioHangTam.length === 0) return showToast("Vui lòng quét hoặc chọn mâm bẩn muốn trả trước!", "error"); 

     

    let p=[]; 

    gioHangTam.forEach((i, idx) => {

        p.push(

            db.collection("phieuGiaoNhan").add({ 

                id: Date.now() + idx, 

                ngayTao: getTodayDateStr(), 

                time: new Date().toLocaleTimeString('vi-VN'), 

                khoa: k, 

                bo: i.bo, 

                maMacDinh: i.maMacDinh, 

                slYeuCau: 1, 

                slThucTe: 1, 

                status: "CHO_THU" 

            })

        );

    }); 

     

    Promise.all(p).then(() => { 

        clearGioHang(); 

        playSound('success');

        showToast("Đã gửi lệnh báo trả đồ bẩn về CSSD!", "success"); 

        callRender(); 

    }); 

}



function khoaGuiYeuCauCapPhat() {

    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;

    if(!k) return showToast("Vui lòng chọn Khoa/Phòng gửi yêu cầu!", "error");

    

    const tenBoChon = document.getElementById("khoa_inpMaBo")?.value.trim().toUpperCase();

    if(!tenBoChon) return showToast("Vui lòng chọn hoặc nhập tên mâm cần cấp mới!", "error");

    

    let tenMâmSạch = tenBoChon.split(" [ID:")[0];

    

    db.collection("phieuGiaoNhan").add({

        id: Date.now(),

        ngayTao: getTodayDateStr(),

        time: new Date().toLocaleTimeString('vi-VN'),

        khoa: k,

        bo: tenMâmSạch,

        maMacDinh: "CHO_CAP_PHAT", 

        slYeuCau: 1,

        status: "CHO_CAP_PHAT", 

        nvYeuCau: loginUserCode || "Điều dưỡng khoa"

    }).then(() => {

        playSound('success');

        showToast(`Đã gửi yêu cầu cấp phát mâm ${tenMâmSạch} thành công!`, "success");

        if(document.getElementById("khoa_inpMaBo")) document.getElementById("khoa_inpMaBo").value = "";

        callRender();

    });

}



// =========================================================================

// 5. TRẠM 2: XE THU GOM & KIỂM ĐẾM CHI TIẾT LINH KIỆN

// =========================================================================

function moPopupKiemDem(id) { 

    let item = listGiaoDich.find(x => x.firestoreId === id); 

    if(!item) return showToast("Không tìm thấy thông tin dòng chỉ định!", "error");



    currentKiemDemData = {

        id: id,

        tenBoDungCu: item.bo ? String(item.bo).split(" [ID:")[0] : "Chưa rõ mâm",

        khoaYeuCau: item.khoa || "Chưa rõ khoa",

        ghiChuLamSang: item.ghiChu || ""

    }; 



    document.getElementById('popBo').innerText = currentKiemDemData.tenBoDungCu;

    document.getElementById('popKhoa').innerText = currentKiemDemData.khoaYeuCau;

    document.getElementById('popGhiChu').value = currentKiemDemData.ghiChuLamSang;

    document.getElementById('popGhiChu').classList.remove('border-rose-500', 'ring-2', 'ring-rose-200');



    let itemsInBo = databaseExcel.filter(x => { 

        let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; 

        return String(boName).trim().toUpperCase() === String(currentKiemDemData.tenBoDungCu).trim().toUpperCase(); 

    });



    let checklistSoBo = [];

    if(itemsInBo.length > 0) {

        checklistSoBo = itemsInBo.map(ct => {

            let tenDc = ct['Tên TS chuẩn'] || ct['Tên Dụng Cụ Chi Tiết'] || ct['Tên dụng cụ chi tiết'] || ct['Tên dụng cụ'] || ct['Chi tiết'] || ct['Dụng cụ'] || ct['Tên Chi Tiết'] || ct['NAME'] || "Dụng cụ";

             

            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {

                tenDc = "Dụng cụ chuẩn mâm";

            }

             

            let sl = parseInt(ct['Số lượng'] || ct['SL'] || ct['Số Lượng'] || 1) || 0;

            return { ten: tenDc, slChuan: sl, slThuc: sl, tinhTrang: "ĐỦ" };

        }).filter(line => line.slChuan > 0); 

    } else {

        checklistSoBo = [{ ten: "Dụng cụ nguyên bộ (Chưa phân rã cấu hình)", slChuan: 1, slThuc: 1, tinhTrang: "ĐỦ" }];

    }



    currentKiemDemData.linhKienKiemDem = checklistSoBo;

    renderChecklistLinhKien();

    document.getElementById('popupKiemDem').classList.remove('hidden'); 

}



function renderChecklistLinhKien() {

    const container = document.getElementById('popKiemDemChecklist');

    if(!container) return; container.innerHTML = ""; 



    currentKiemDemData.linhKienKiemDem.forEach((item, index) => {

        let bgBadge = "bg-emerald-50 text-emerald-700 border-emerald-200";

        if (item.tinhTrang === "THIẾU") bgBadge = "bg-rose-50 text-rose-700 border-rose-200 animate-pulse";

        if (item.tinhTrang === "HỎNG") bgBadge = "bg-amber-50 text-amber-700 border-amber-200";



        const row = document.createElement('div');

        row.className = "flex items-center justify-between py-3 px-2 transition-all hover:bg-slate-50";

        row.innerHTML = `

            <div class="flex-1 pr-2">

                <p class="text-xs font-bold text-slate-700">${item.ten}</p>

                <p class="text-[10px] text-slate-400 font-medium mt-0.5">Cơ số chuẩn: <span class="font-bold text-slate-600">${item.slChuan}</span></p>

            </div>

            <div class="flex items-center gap-3 mr-4">

                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, -1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">-</button>

                <span class="w-6 text-center text-xs font-black text-slate-800">${item.slThuc}</span>

                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, 1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">+</button>

            </div>

            <div>

                <select onchange="thayDoiTinhTrangLinhKien(${index}, this.value)" class="text-[11px] font-black border rounded-lg px-2 py-1.5 shadow-sm transition-all ${bgBadge}">

                    <option value="ĐỦ" ${item.tinhTrang === 'ĐỦ' ? 'selected' : ''}>🟢 Đủ đồ</option>

                    <option value="THIẾU" ${item.tinhTrang === 'THIẾU' ? 'selected' : ''}>🔴 Thiếu đồ</option>

                    <option value="HỎNG" ${item.tinhTrang === 'HỎNG' ? 'selected' : ''}>🟡 Hỏng đồ</option>

                </select>

            </div>

        `;

        container.appendChild(row);

    });

}



function thayDoiSoLuongLinhKien(index, thayDoi) {

    const item = currentKiemDemData.linhKienKiemDem[index];

    const targetSl = item.slThuc + thayDoi;

    if (targetSl >= 0 && targetSl <= item.slChuan * 2) {

        item.slThuc = targetSl;

        if (item.slThuc < item.slChuan) { item.tinhTrang = "THIẾU"; } 

        else if (item.slThuc === item.slChuan) { item.tinhTrang = "ĐỦ"; }

        renderChecklistLinhKien();

    }

}



function thayDoiTinhTrangLinhKien(index, value) {

    currentKiemDemData.linhKienKiemDem[index].tinhTrang = value;

    if (value === "THIẾU" && currentKiemDemData.linhKienKiemDem[index].slThuc === currentKiemDemData.linhKienKiemDem[index].slChuan) {

        currentKiemDemData.linhKienKiemDem[index].slThuc = Math.max(0, currentKiemDemData.linhKienKiemDem[index].slChuan - 1);

    } else if (value === "ĐỦ") {

        currentKiemDemData.linhKienKiemDem[index].slThuc = currentKiemDemData.linhKienKiemDem[index].slChuan;

    }

    renderChecklistLinhKien();

}



function closePopupKiemDem() { document.getElementById("popupKiemDem").classList.add("hidden"); currentKiemDemData = null; }



async function saveKiemDem() { 

    if(!currentKiemDemData) return; 

    const ghiChuValue = document.getElementById("popGhiChu").value.trim();

    let coSuCoChenhLech = false;

    currentKiemDemData.linhKienKiemDem.forEach(item => {

        let thuc = Number(item.slThuc) || 0;

        let chuan = Number(item.slChuan) || 0;

        if (item.tinhTrang === "THIẾU" || item.tinhTrang === "HỎNG" || thuc < chuan) { coSuCoChenhLech = true; }

    });



    if (coSuCoChenhLech && ghiChuValue === "") {

        playSound('error');

        alert("⚠️ PHÁT HIỆN SỰ CỐ CHÊNH LỆCH LINH KIỆN!\nAnh/Chị bắt buộc phải nhập lý do chênh lệch hoặc số biên bản sự cố trước khi 'Chốt và Chuyển Rửa'.");

        document.getElementById('popGhiChu').focus();

        document.getElementById('popGhiChu').classList.add('border-rose-500', 'ring-2', 'ring-rose-200');

        return;

    }



    try {

        await db.collection("phieuGiaoNhan").doc(currentKiemDemData.id).update({ 

            status: "DANG_RUA", ghiChu: ghiChuValue,

            ktvThuGom: loginUserCode || "CSSD_CHUNG",

            thoiGianDoiSoat: new Date().toLocaleTimeString('vi-VN'),

            coSuCoChenhLech: coSuCoChenhLech,

            chiTietChecklistLinhKien: currentKiemDemData.linhKienKiemDem

        });

        showToast("Đã đối soát xong! Mâm đồ đã chuyển sang Trạm Rửa.", "success"); 

        closePopupKiemDem(); callRender(); 

    } catch(err) { showToast("Lỗi kết nối Firebase nội bộ!", "error"); }

}



function clearGioHang() { gioHangTam = []; renderGioHang(); }



// =========================================================================

// 6. MODULE: QUẢN LÝ MẺ RỬA KHỬ KHUẨN (BELIMED WD250)

// =========================================================================

function toggleSelectAllRua() { let checked = document.getElementById('selectAllRua').checked; document.querySelectorAll('.rua-checkbox').forEach(cb => cb.checked = checked); }

function toggleSelectAllNghiemThuRua() { let checked = document.getElementById('selectAllNghiemThuRua').checked; document.querySelectorAll('.nghiemthurua-checkbox').forEach(cb => cb.checked = checked); }



function capNhatDanhSachMaMayRua() {

    const loaiRua = document.getElementById("rua_loaiRua")?.value;

    const selectMay = document.getElementById("rua_maySo");

    if (!selectMay || !loaiRua) return;

    selectMay.innerHTML = "";

    if (cauHinhMayRua[loaiRua]) {

        cauHinhMayRua[loaiRua].forEach(may => {

            selectMay.innerHTML += `<option value="${may}">${may}</option>`;

        });

    }

    tuDongTaoMaLoMeRua();

}



function tuDongTaoMaLoMeRua() {

    const maMay = document.getElementById("rua_maySo")?.value || "Belimed WD250";

    const maMayVietTat = (maMay === "Belimed WD250") ? "BL" : maMay;

    const now = new Date();

    const yy = String(now.getFullYear()).slice(-2);

    const mm = String(now.getMonth() + 1).padStart(2, '0');

    const dd = String(now.getDate()).padStart(2, '0');

    const ngayChuoi = `${yy}${mm}${dd}`;



    let cacMeTrongNgay = listGiaoDich.filter(x => x.rua_batchCode && x.rua_batchCode.startsWith(maMayVietTat + ngayChuoi));

    let soMeMax = 0;

    cacMeTrongNgay.forEach(x => {

        let phanDuoi = x.rua_batchCode.split("_")[1];

        if (phanDuoi) {

            let num = parseInt(phanDuoi);

            if (num > soMeMax) soMeMax = num;

        }

    });

    let meTiepTheo = soMeMax + 1;

    const chuoiMe = String(meTiepTheo).padStart(2, '0');

    const batchIdHoanChinh = `${maMayVietTat}${ngayChuoi}_${chuoiMe}`;



    if (document.getElementById("rua_meSo")) document.getElementById("rua_meSo").value = chuoiMe;

    if (document.getElementById("rua_batchId")) document.getElementById("rua_batchId").value = batchIdHoanChinh;

}



function xacNhanMeRua() {

    let checkboxes = document.querySelectorAll('.rua-checkbox:checked');

    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm dụng cụ bẩn để xếp vào máy!", "error");

    

    let loaiRua = document.getElementById("rua_loaiRua").value;

    let maMay = document.getElementById("rua_maySo").value;

    let batchCode = document.getElementById("rua_batchId").value;

    let chuKy = document.getElementById("rua_chuKy").value;

    let hoaChat = document.getElementById("rua_hoaChat").value;

    let lieuLuong = document.getElementById("rua_lieuLuong").value || "Theo chuẩn";

    const ngayHomNay = getTodayDateStr();



    let p = [];

    checkboxes.forEach(cb => {

        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({

                status: "TRONG_BUONG_RUA",

                rua_batchCode: batchCode,

                rua_ngayRua: ngayHomNay,

                rua_mayRua: maMay,

                rua_chuKy: chuKy,

                rua_hoaChat: hoaChat,

                rua_lieuLuong: lieuLuong,

                rua_timeBatDau: new Date().toLocaleTimeString('vi-VN')

            })

        );

    });

    Promise.all(p).then(() => {

        playSound('success');

        showToast(`Bắt đầu chạy mẻ máy rửa: ${batchCode}`, "success");

        tuDongTaoMaLoMeRua();

        callRender();

    });

}



function duyetSachMeRuaHangLoat() {

    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');

    if(checkboxes.length === 0) return showToast("Chọn mâm đã rửa sạch để nghiệm thu!", "error");

    

    let ketQuaTest = document.getElementById("rua_testDoSach").value;

    let p = [];

    checkboxes.forEach(cb => {

        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({

                status: "CHO_DONG_GOI",

                rua_ketQuaTestSach: ketQuaTest,

                rua_timeKetThuc: new Date().toLocaleTimeString('vi-VN')

            })

        );

    });

    Promise.all(p).then(() => {

        playSound('success');

        showToast("Xác nhận nghiệm thu đạt chất lượng làm sạch!", "success");

        callRender();

    });

}



function tuChoiMeRuaHangLoat() {

    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');

    if(checkboxes.length === 0) return showToast("Chọn mâm không đạt!", "error");

    

    let p = [];

    checkboxes.forEach(cb => {

        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({

                status: "DANG_RUA",

                rua_batchCode: firebase.firestore.FieldValue.delete(),

                ghiChu: "Làm sạch chưa đạt, yêu cầu rửa lại."

            })

        );

    });

    Promise.all(p).then(() => {

        playSound('error');

        showToast("Đã trả các mâm không đạt về hàng đợi chờ rửa lại!", "success");

        callRender();

    });

}



// =========================================================================

// 7. TRẠM 4: LÀM SẠCH VÀ ĐÓNG GÓI

// =========================================================================

function moPopupDongGoi(id) { idDangDongGoi = id; let item = listGiaoDich.find(x => x.firestoreId === id); if(item) { document.getElementById("popDG_Bo").innerText = item.bo; tinhHanSuDung(); document.getElementById("popupDongGoi").classList.remove("hidden"); } }

function closePopupDongGoi() { document.getElementById("popupDongGoi").classList.add("hidden"); }

function tinhHanSuDung() { let val = document.getElementById("popDG_Loai").value.split("|"); let days = parseInt(val[1]); let dateHSD = new Date(); dateHSD.setDate(dateHSD.getDate() + days); let p = document.getElementById("popDG_Han"); p.innerText = dateHSD.toLocaleDateString('vi-VN'); p.dataset.dateDB = dateHSD.toISOString().split('T')[0]; }

function chotDongGoi() { if(!idDangDongGoi) return; let chatLieuTen = document.getElementById("popDG_Loai").value.split("|")[0]; db.collection("phieuGiaoNhan").doc(idDangDongGoi).update({ status: "CHO_HAP", chatLieu: chatLieuTen, hsd: document.getElementById("popDG_Han").dataset.dateDB }).then(() => { showToast("Đã đóng gói, chuyển chờ hấp!", "success"); closePopupDongGoi(); callRender(); }); }



// =========================================================================

// 8. TRẠM 5: VẬN HÀNH MẺ HẤP & DUYỆT NHẬP KHO VÔ KHUẨN

// =========================================================================

function toggleSelectAllHap() { let checked = document.getElementById('selectAllHap').checked; document.querySelectorAll('.hap-checkbox').forEach(cb => cb.checked = checked); }

function capNhatDanhSachMaMay() { const loaiHap = document.getElementById("hap_loaiHap")?.value; const selectMay = document.getElementById("hap_maySo"); if (!selectMay || !loaiHap) return; selectMay.innerHTML = ""; if (cauHinhMayHap[loaiHap]) { cauHinhMayHap[loaiHap].forEach(may => { selectMay.innerHTML += `<option value="${may}">${may}</option>`; }); } tuDongTaoMaLoMeHap(); }



function tuDongTaoMaLoMeHap() {

    const maMay = document.getElementById("hap_maySo")?.value || "A1"; const now = new Date(); const yy = String(now.getFullYear()).slice(-2); const mm = String(now.getMonth() + 1).padStart(2, '0'); const dd = String(now.getDate()).padStart(2, '0'); const ngayChuoi = `${yy}${mm}${dd}`;

    let cacMeTrongNgay = listGiaoDich.filter(x => x.batchCode && x.batchCode.startsWith(maMay + ngayChuoi)); let soMeMax = 0;

    cacMeTrongNgay.forEach(x => { let phanDuoi = x.batchCode.split("_")[1]; if (phanDuoi) { let num = parseInt(phanDuoi); if (num > soMeMax) soMeMax = num; } });

    let meTiepTheo = soMeMax + 1; if (meTiepTheo > 99) meTiepTheo = 1; const chuoiMe = String(meTiepTheo).padStart(2, '0'); const soLanDone = `${ngayChuoi}_${chuoiMe}`; const batchIdHoanChinh = `${maMay}${soLanDone}`;

    if (document.getElementById("hap_meSo")) document.getElementById("hap_meSo").value = chuoiMe;

    if (document.getElementById("hap_batchId")) document.getElementById("hap_batchId").value = batchIdHoanChinh;

}



function xacNhanMeHap() { 

    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); 

    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 

    

    let loaiHap = document.getElementById("hap_loaiHap").value; 

    let maMay = document.getElementById("hap_maySo").value; 

    let batchCode = document.getElementById("hap_batchId").value; 

    let chuKyNhiet = document.getElementById("hap_nhietDo").value; 

    let apSuat = document.getElementById("hap_apSuat").value || "N/A"; 

    let coKemBI = document.getElementById("hap_hasBI") ? document.getElementById("hap_hasBI").checked : false;

    const ngayHomNay = getTodayDateStr(); 

    let bayGio = new Date(); 

    let thoiGianDuKienXong = new Date(bayGio.getTime() + 45 * 60 * 1000);



    let p = []; 

    checkboxes.forEach(cb => { 

        if(cb.id === 'selectAllHap') return; 

        

        let thongTinLo = { 

            loaiHap: loaiHap, 

            maMay: maMay, 

            chuKyNhiet: chuKyNhiet, 

            apSuat: apSuat, 

            thoiGianBatDau: bayGio.toLocaleTimeString('vi-VN'), 

            giamSatChatLuong: { 

                chiThiHoaHoc: "ĐẠT", 

                laMeTestSinhHocGoc: coKemBI, 

                ketQuaSinhHoc: coKemBI ? "ĐANG CHỜ MÁY Ủ ĐỌC BI (20 PHÚT)" : "KẾ THỪA ĐẦU NGÀY" 

            } 

        };

        

        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({ 

                status: "DANG_HAP", 

                batchCode: batchCode, 

                ngayHapRealtime: ngayHomNay, 

                hasBI: coKemBI, 

                thoiGianKetThucChuTrinh: thoiGianDuKienXong.toISOString(), 

                thongTinLoHap: thongTinLo 

            })

        ); 

    }); 



    Promise.all(p).then(() => { 

        playSound('success');

        showToast(`Kích hoạt lò thành công! Lô: ${batchCode}`, "success"); 

        if(document.getElementById("hap_hasBI")) document.getElementById("hap_hasBI").checked = false; 

        duLieuAnhBiTamBase64 = ""; 

        const fileInp = document.getElementById("input_anhBI"); 

        if(fileInp) fileInp.value = ""; 

        tuDongTaoMaLoMeHap(); 

        callRender(); 

    }).catch(err => {

        showToast("Lỗi khi cập nhật mẻ hấp: " + err.message, "error");

    }); 

}



function toggleSelectAllNghiemThu() { let checked = document.getElementById('selectAllNghiemThu').checked; document.querySelectorAll('.nghiemthu-checkbox').forEach(cb => cb.checked = checked); }



function kiemTraQuyenDuyetMeHap() {

    const nutDuyetDat = document.querySelector("button[onclick='nhapKhoHangLoat()']"); if (!nutDuyetDat) return;

    let checkboxesNghiemThu = document.querySelectorAll('.nghiemthu-checkbox:checked'); let phaiChoKetQuaBI = false;

    checkboxesNghiemThu.forEach(cb => { if (cb.getAttribute('data-hasbi') === 'true') phaiChoKetQuaBI = true; });



    if (phaiChoKetQuaBI && !duLieuAnhBiTamBase64) {

        nutDuyetDat.disabled = true;

        nutDuyetDat.className = "w-full bg-slate-300 text-slate-500 font-black py-2.5 rounded text-xs shadow-md cursor-not-allowed flex items-center justify-center gap-2";

        nutDuyetDat.innerHTML = `<i class="fa-solid fa-lock text-slate-400"></i> CHỜ MINH CHỨNG BI (MÁY Ủ ĐỌC 20 PHÚT)`;

    } else {

        nutDuyetDat.disabled = false;

        nutDuyetDat.className = "w-full bg-emerald-600 hover:bg-emerald-700 text-white font-black py-2.5 rounded text-xs shadow-md flex items-center justify-center gap-2 transition-all cursor-pointer";

        nutDuyetDat.innerHTML = `<i class="fa-solid fa-check-double mr-1"></i> DUYỆT ĐẠT & CHO VÀO KHO LƯU TRỮ CHUNG`;

    }

}



function nhapKhoHangLoat() { 

    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!"); 

    const bayGio = new Date(); let p = []; 

     

    checkboxes.forEach(cb => { 

        if(cb.id === 'selectAllNghiemThu') return; 

        let itemData = listGiaoDich.find(x => x.firestoreId === cb.value); if(!itemData) return;

        let coKemBI = itemData.hasBI === true;

        let thoiGianHapXong = itemData.thoiGianKetThucChuTrinh ? new Date(itemData.thoiGianKetThucChuTrinh) : new Date(bayGio.getTime() - 45 * 60 * 1000);

        let soPhutThucTe = Math.floor((bayGio - thoiGianHapXong) / (1000 * 60)); 

        let soPhutTinhKPI = coKemBI ? Math.max(0, soPhutThucTe - 20) : soPhutThucTe;

        let trangThaiKPI = (soPhutTinhKPI > 30) ? "KHÔNG ĐẠT" : "ĐẠT";



        let bgGiamSat = itemData.thongTinLoHap?.giamSatChatLuong || {};

        let updateGiamSat = {

            ...bgGiamSat,

            ketQuaSinhHoc: coKemBI ? "ÂM TÍNH (ĐẠT)" : "KẾ THỪA ĐẦU NGÀY",

            minhChungAnhBase64: coKemBI ? duLieuAnhBiTamBase64 : (bgGiamSat.minhChungAnhBase64 || "")

        };



        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({

                status: "CHO_XUAT", thoiGianKetLuan: bayGio.toISOString(), thoiGianDocTestBiPhut: soPhutThucTe, 

                thoiGianTinhKpiPhut: soPhutTinhKPI, ketQuaGiamSatKpi: trangThaiKPI, "thongTinLoHap.giamSatChatLuong": updateGiamSat

            })

        ); 

    }); 

     

    Promise.all(p).then(() => { 

        playSound('success');

        showToast("Đã duyệt mâm đạt chuẩn Vô Khuẩn!", "success"); 

        duLieuAnhBiTamBase64 = ""; const fileInp = document.getElementById("input_anhBI"); if(fileInp) fileInp.value = ""; 

        callRender(); 

    }); 

}



function tuChoiHapHangLoat() { 

    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 

    let p = []; 

    checkboxes.forEach(cb => { if(cb.id !== 'selectAllNghiemThu') p.push(db.collection("phieuGiaoNhan").doc(cb.value).update({status: "DANG_RUA", ghiChu: "Không đạt hấp, trả về rửa lại"})); }); 

    Promise.all(p).then(() => { playSound('error'); showToast("Đã trả các mâm không đạt về Trạm làm sạch!", "success"); callRender(); }); 

}



// =========================================================================

// 9. TRẠM 6: KHO VÔ KHUẨN & PHỐI HỢP CẤP PHÁT ĐIỀU PHỐI XOAY VÒNG (FIFO)

// =========================================================================

function xuatKhoXoayVong(maTrucTiep) { 

    const k = document.getElementById("xuat_selKhoa") ? document.getElementById("xuat_selKhoa").value : ""; 

    let ma = maTrucTiep || (document.getElementById("xuat_inpMaBo") ? document.getElementById("xuat_inpMaBo").value.trim().toUpperCase() : ""); 

    

    if(!k) { playSound('error'); return showToast("Vui lòng Chọn Khoa nhận trước khi xuất!", "error"); } 

    if(!ma) return;

    if(gioHangXuatKho.some(x => x.maMacDinh === ma)) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách xuất!", "error"); }

    

    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && x.status === "CHO_XUAT"); 

    if(!khayThucTe) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast(`Mã ID ${ma} không sẵn sàng ở Kho Vô Khuẩn.`, "error"); } 

    

    gioHangXuatKho.push(khayThucTe); playSound('success'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; renderGioHangXuat();

}



function renderGioHangXuat() {

    let listContainer = document.getElementById("danhSachXuatTam");

    if (!listContainer) {

        let bangKho = document.getElementById("bangKhoVoKhuan");

        if (bangKho && bangKho.closest('table')) {

            let wrapper = document.createElement("div"); wrapper.id = "danhSachXuatTam";

            wrapper.className = "mb-4 bg-sky-50 border border-sky-200 rounded p-3 hidden shadow-inner";

            bangKho.closest('table').parentNode.insertBefore(wrapper, bangKho.closest('table')); listContainer = wrapper;

        }

    }

    if(listContainer) {

        if(gioHangXuatKho.length === 0) { listContainer.classList.add("hidden"); } 

        else {

            listContainer.classList.remove("hidden");

            let html = `<div class="flex justify-between items-center mb-2 pb-2 border-b border-sky-200"><h3 class="font-bold text-sky-800 text-[13px]"><i class="fa-solid fa-cart-flatbed mr-2"></i>Đang quét xuất kho: <span class="bg-rose-500 text-white px-2 py-0.5 rounded ml-1">${gioHangXuatKho.length} mâm</span></h3><button onclick="xacNhanXuatKhoHangLoat()" class="bg-emerald-600 text-white px-4 py-1.5 rounded shadow font-black text-xs hover:bg-emerald-700 transition-colors uppercase"><i class="fa-solid fa-check-double mr-1"></i>Chốt Bàn Giao</button></div><div class="flex flex-wrap gap-2">`;

            gioHangXuatKho.forEach((item, index) => { 

                let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "Dụng cụ";

                html += `<div class="bg-white border border-sky-300 px-2 py-1 rounded flex items-center gap-2 shadow-sm animate-pulse"><span class="text-[11px] font-bold text-slate-700">${tenBoGoc}</span><span class="text-[10px] font-mono text-sky-700 bg-sky-100 px-1 rounded font-bold">${item.maMacDinh}</span><i class="fa-solid fa-xmark text-rose-500 cursor-pointer ml-1 text-xs hover:text-rose-700" onclick="xoaKhoiGioXuat(${index})"></i></div>`; 

            });

            html += `</div>`; listContainer.innerHTML = html;

        }

    }

}



function xoaKhoiGioXuat(index) { gioHangXuatKho.splice(index, 1); renderGioHangXuat(); }



function xacNhanXuatKhoHangLoat() {

    const k = document.getElementById("xuat_selKhoa").value; if(!k || gioHangXuatKho.length === 0) return;

    let p = []; 

    gioHangXuatKho.forEach(khay => { 

        p.push(

            db.collection("phieuGiaoNhan").doc(khay.firestoreId).update({ 

                status: "ĐANG_VAN_CHUYEN", khoa: k, ngayHoanTat: getTodayDateStr(), timeHoanTat: new Date().toLocaleTimeString('vi-VN'), nvXuatKho: loginUserCode || "CSSD_CHUNG" 

            })

        ); 

    });

    Promise.all(p).then(() => { 

        playSound('success'); showToast(`Đã xuất kho bàn giao về Khoa ${k}!`, "success"); 

        gioHangXuatKho = []; if (document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; 

        renderGioHangXuat(); callRender(); 

    });

}



function duyetCapPhatTậpTrung(yeuCauId, tenBo, khoaNhan) {

    let khaySẵnCo = listGiaoDich.find(x => x.status === "CHO_XUAT" && x.bo.split(" [ID:")[0].trim().toUpperCase() === tenBo.trim().toUpperCase());

    

    if (!khaySẵnCo) {

        playSound('error');

        return alert(`⚠️ KHÔNG CÓ KHAY SẴN SÀNG TRONG KHO!\nHiện tại trong Kho vô khuẩn CSSD không có khay "${tenBo}" nào sẵn sàng để cấp phát.`);

    }



    if (confirm(`Xác nhận cấp phát khay ${khaySẵnCo.maMacDinh} để đáp ứng yêu cầu của khoa ${khoaNhan}?`)) {

        let bayGio = new Date();

        let batch = db.batch();

        

        let refKhay = db.collection("phieuGiaoNhan").doc(khaySẵnCo.firestoreId);

        batch.update(refKhay, {

            status: "ĐANG_VAN_CHUYEN",

            khoa: khoaNhan,

            ngayHoanTat: getTodayDateStr(),

            timeHoanTat: bayGio.toLocaleTimeString('vi-VN'),

            nvXuatKho: loginUserCode || "CSSD_CHUNG"

        });



        let refYeuCau = db.collection("phieuGiaoNhan").doc(yeuCauId);

        batch.delete(refYeuCau);



        batch.commit().then(() => {

            playSound('success');

            showToast(`Đã cấp phát thành công khay ${khaySẵnCo.maMacDinh} cho khoa ${khoaNhan}!`, "success");

            callRender();

        });

    }

}



// =========================================================================

// XỬ LÝ KÝ CHỮ KÝ ĐIỆN TỬ TRỰC TIẾP TRÊN CANVAS

// =========================================================================

function khoiTaoCanvasKyDienTu() {

    canvasSignature = document.getElementById("canvasKyDienTu");

    if (!canvasSignature) return;

    ctxSignature = canvasSignature.getContext("2d");



    const rect = canvasSignature.getBoundingClientRect();

    canvasSignature.width = rect.width || 380;

    canvasSignature.height = rect.height || 160;



    ctxSignature.lineWidth = 2.5;

    ctxSignature.lineCap = "round";

    ctxSignature.strokeStyle = "#0284c7";



    // Chuột

    canvasSignature.onmousedown = (e) => {

        isDrawingSignature = true;

        ctxSignature.beginPath();

        const pos = getCanvasPos(e);

        ctxSignature.moveTo(pos.x, pos.y);

    };

    canvasSignature.onmousemove = (e) => {

        if (!isDrawingSignature) return;

        const pos = getCanvasPos(e);

        ctxSignature.lineTo(pos.x, pos.y);

        ctxSignature.stroke();

    };

    canvasSignature.onmouseup = () => { isDrawingSignature = false; };



    // Cảm ứng Tablet/Phone

    canvasSignature.ontouchstart = (e) => {

        isDrawingSignature = true;

        ctxSignature.beginPath();

        const pos = getCanvasPos(e.touches[0]);

        ctxSignature.moveTo(pos.x, pos.y);

    };

    canvasSignature.ontouchmove = (e) => {

        if (!isDrawingSignature) return;

        e.preventDefault();

        const pos = getCanvasPos(e.touches[0]);

        ctxSignature.lineTo(pos.x, pos.y);

        ctxSignature.stroke();

    };

    canvasSignature.ontouchend = () => { isDrawingSignature = false; };

}



function getCanvasPos(evt) {

    const rect = canvasSignature.getBoundingClientRect();

    return {

        x: evt.clientX - rect.left,

        y: evt.clientY - rect.top

    };

}



function xoaChuKyCanvas() {

    if (ctxSignature && canvasSignature) {

        ctxSignature.clearRect(0, 0, canvasSignature.width, canvasSignature.height);

    }

}



function khoaKyNhanDoSachDienTu() {

    const checkboxes = document.querySelectorAll("#bangChoNhanTaiKhoa input[type='checkbox']:checked");

    dsIdKhayChoKyNhan = [];

    checkboxes.forEach(chk => dsIdKhayChoKyNhan.push(chk.value));



    if (dsIdKhayChoKyNhan.length === 0) {

        playSound('error');

        return showToast("Vui lòng tích chọn ít nhất 1 khay để ký nhận!", "error");

    }



    const inpName = document.getElementById("ky_tenNguoiNhan");

    if (inpName) inpName.value = loginUserCode || "";



    const pop = document.getElementById("popupKyDienTu");

    if (pop) {

        pop.classList.remove("hidden");

        setTimeout(() => {

            khoiTaoCanvasKyDienTu();

            xoaChuKyCanvas();

        }, 100);

    } else {

        // Fallback trường hợp chưa thêm popup vào HTML

        const khoaDangNhap = currentRole === "KHOA" ? loginUserCode : (document.getElementById("khoa_selKhoa") ? document.getElementById("khoa_selKhoa").value : "");

        if (confirm(`Xác nhận nhận ${dsIdKhayChoKyNhan.length} mâm về khoa ${khoaDangNhap}?`)) {

            let p = [];

            dsIdKhayChoKyNhan.forEach(idDoc => {

                p.push(

                    db.collection("phieuGiaoNhan").doc(idDoc).update({

                        status: "HOAN_TAT", nguoiKyNhanKhoa: loginUserCode || "Điều dưỡng khoa", timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'), ngayKhoaNhanThucTe: getTodayDateStr()

                    })

                );

            });

            Promise.all(p).then(() => { playSound('success'); showToast(`Đã tiếp nhận vào tủ khoa thành công!`, "success"); callRender(); });

        }

    }

}



function dongPopupKyDienTu() {

    document.getElementById("popupKyDienTu")?.classList.add("hidden");

    dsIdKhayChoKyNhan = [];

}



function luuXacNhanKyNhan() {

    const tenNguoiNhan = document.getElementById("ky_tenNguoiNhan")?.value.trim();

    if (!tenNguoiNhan) {

        playSound('error');

        return showToast("Vui lòng nhập tên người nhận!", "error");

    }



    const chuoiAnhChuKy = canvasSignature ? canvasSignature.toDataURL("image/png") : "";



    let p = [];

    dsIdKhayChoKyNhan.forEach(idDoc => {

        p.push(

            db.collection("phieuGiaoNhan").doc(idDoc).update({

                status: "HOAN_TAT",

                nguoiKyNhanKhoa: tenNguoiNhan,

                anhChuKyNhanBase64: chuoiAnhChuKy,

                timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'),

                ngayKhoaNhanThucTe: getTodayDateStr()

            })

        );

    });



    Promise.all(p).then(() => {

        playSound('success');

        showToast(`Đã ký nhận thành công ${dsIdKhayChoKyNhan.length} mâm vào tủ khoa!`, "success");

        dongPopupKyDienTu();

        callRender();

    });

}



// =========================================================================

// 10. GHI NHẬN SỬ DỤNG BỘ DỤNG CỤ TRÊN BỆNH NHÂN

// =========================================================================

function moPopupSuDungBoDungCu() {

    gioKhaySuDungTam = []; document.getElementById("sd_bangKhayChon").innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng quét hoặc nhập mã khay...</td></tr>`;

    document.getElementById("sd_ngaySuDung").value = getTodayDateStr(); document.getElementById("sd_nhanChung").value = loginUserCode || "ADMIN";

    if(document.getElementById("sd_khoaBenhNhan")) document.getElementById("sd_khoaBenhNhan").value = (currentRole === "KHOA") ? loginUserCode : "";

    if(document.getElementById("sd_yTaPhongMo")) document.getElementById("sd_yTaPhongMo").value = "";

    if(document.getElementById("sd_yTaVongNgoai")) document.getElementById("sd_yTaVongNgoai").value = "";

    if(document.getElementById("sd_searchBN")) document.getElementById("sd_searchBN").value = "";

    if(document.getElementById("sd_ghiChu")) document.getElementById("sd_ghiChu").value = "";

    if(document.getElementById("sd_maKhayInp")) document.getElementById("sd_maKhayInp").value = "";

    document.getElementById("popupSuDungBoDungCu").classList.remove("hidden");

}



function closePopupSuDung() { document.getElementById("popupSuDungBoDungCu").classList.add("hidden"); }



function scanKhayVaoSuDung() {

    let ma = document.getElementById("sd_maKhayInp").value.trim().toUpperCase(); if(!ma) return;

    document.getElementById("sd_maKhayInp").value = "";

    if(gioKhaySuDungTam.some(x => x.maMacDinh === ma)) { playSound('error'); return showToast("Mã khay này đã được quét!", "error"); }



    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT"));

    if(!khayThucTe) { playSound('error'); return showToast(`Mã ID ${ma} không hợp lệ hoặc chưa vô khuẩn!`, "error"); }



    gioKhaySuDungTam.push(khayThucTe); playSound('success'); renderBangKhaySuDung();

}



function renderBangKhaySuDung() {

    const tbody = document.getElementById("sd_bangKhayChon"); if(!tbody) return;

    if(gioKhaySuDungTam.length === 0) { tbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Chưa chọn khay...</td></tr>`; return; }

    tbody.innerHTML = gioKhaySuDungTam.map((item, index) => {

        let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A";

        return `<tr class="border-b font-medium hover:bg-slate-50 text-xs">

            <td class="p-2 text-center"><i class="fa-solid fa-square-check text-teal-600 text-sm"></i></td>

            <td class="p-2 font-mono font-bold text-sky-700">${item.maMacDinh}</td>

            <td class="p-2 font-bold text-slate-800">${tenBoGoc}</td>

            <td class="p-2 text-center font-mono font-bold text-rose-600 bg-rose-50/30">${item.batchCode || 'N/A'}</td>

            <td class="p-2 text-center"><span class="px-2 py-0.5 rounded text-[10px] font-bold bg-teal-100 text-teal-800">${item.status}</span></td>

            <td class="p-2 text-center"><i class="fa-solid fa-trash-can text-rose-500 cursor-pointer hover:text-rose-700" onclick="xoaKhayKhoiListSuDung(${index})"></i></td>

        </tr>`;

    }).join('');

}



function xoaKhayKhoiListSuDung(index) {

    gioKhaySuDungTam.splice(index, 1);

    renderBangKhaySuDung();

}



// =========================================================================

// 11. THIẾT LẬP MÁY IN BIXOLON SLP-TX403 (80mm x 50mm) & BIÊN BẢN HÀNG LOẠT

// =========================================================================



// In tem đơn Bixolon 80mm x 50mm

function inTemBixolonChuan(itemData) {

    const pZone = document.getElementById("print-zone");

    if (!pZone) return;



    const tenBo = itemData.bo ? itemData.bo.split(" [ID:")[0] : "DỤNG CỤ VÔ KHUẨN";

    const maID = itemData.maMacDinh || itemData.idKhay || "000000";

    const ngayNSX = itemData.ngayHapRealtime || itemData.ngayTao || getTodayDateStr();

    const ngayHSD = itemData.hsd ? new Date(itemData.hsd).toLocaleDateString('vi-VN') : "Theo quy định";

    const maBatch = itemData.batchCode || itemData.batchId || "N/A";

    const tenKhoa = itemData.khoa || "TẬP TRUNG";



    pZone.innerHTML = `

        <div class="bixolon-label" style="font-family: Arial, sans-serif;">

            <div style="text-align: center; border-bottom: 1.5px solid #000; padding-bottom: 2px;">

                <div style="font-size: 10px; font-weight: bold; text-transform: uppercase;">PHUONG NAM HOSPITAL - CSSD</div>

                <div style="font-size: 12px; font-weight: 900; margin-top: 1px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${tenBo}</div>

            </div>



            <div style="text-align: center; margin: 3px 0; display: flex; justify-content: center;">

                <svg id="bixolon-single-barcode"></svg>

            </div>



            <div style="font-size: 9px; border-top: 1px dashed #000; padding-top: 2px;">

                <div style="display: flex; justify-content: space-between; font-weight: bold;">

                    <span>LÔ: ${maBatch}</span>

                    <span>KHOA: ${tenKhoa}</span>

                </div>

                <div style="display: flex; justify-content: space-between; margin-top: 1px;">

                    <span>NSX: ${ngayNSX}</span>

                    <span style="font-size: 10px; font-weight: 900;">HSD: ${ngayHSD}</span>

                </div>

            </div>

        </div>

    `;



    JsBarcode("#bixolon-single-barcode", maID, {

        format: "CODE128",

        width: 1.6,

        height: 36,

        displayValue: true,

        fontSize: 10,

        fontOptions: "bold",

        margin: 0

    });



    document.body.classList.remove("print-mode-doc");

    document.body.classList.add("print-mode-bixolon");

    pZone.classList.remove("hidden");



    setTimeout(() => {

        window.print();

        pZone.classList.add("hidden");

        document.body.classList.remove("print-mode-bixolon");

    }, 250);

}



function inTemTongHangLoat() { 

    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 

    let batchCode = document.getElementById("hap_batchId")?.value || "A1000000_01"; 

    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 



    checkboxes.forEach((cb) => { 

        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 

        if(item) { 

            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 

            container.innerHTML += `<div class="bixolon-label" style="font-family: Arial; font-size: 11px; color: #000; background: #fff;"><div style="text-align: center; font-weight: bold; font-size: 12px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-lo-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span></div><div style="display: flex; justify-content: space-between; border-top: 1px dashed #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 9px; font-family: monospace; font-weight: bold; margin-top: 1px;">Lô: ${batchCode}</div></div>`; 

        } 

    }); 



    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); 

    document.body.classList.remove("print-mode-doc");

    document.body.classList.add("print-mode-bixolon");

    pZone.classList.remove("hidden"); 



    checkboxes.forEach(cb => { 

        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 

        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-lo-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 

    }); 



    setTimeout(() => { 

        window.print(); 

        pZone.classList.add("hidden"); 

        document.body.classList.remove("print-mode-bixolon");

    }, 300); 

}



function inTemNghiemThuHangLoat() { 

    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 

    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 



    checkboxes.forEach((cb) => { 

        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 

        if(item) { 

            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 

            container.innerHTML += `<div class="bixolon-label" style="font-family: Arial; font-size: 11px; color: #000; background: #fff;"><div style="text-align: center; font-size: 9px; font-weight: bold; letter-spacing: 0.5px;">PN HOSPITAL - CSSD</div><div style="text-align: center; font-weight: bold; font-size: 12px; margin: 1px 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-nt-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span><span style="color: green;">ĐẠT VÔ KHUẨN</span></div><div style="display: flex; justify-content: space-between; border-top: 1px solid #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 8px; font-weight: bold; font-family: monospace; margin-top: 1px;">BATCH: ${item.batchCode || 'N/A'}</div></div>`; 

        } 

    }); 



    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); 

    document.body.classList.remove("print-mode-doc");

    document.body.classList.add("print-mode-bixolon");

    pZone.classList.remove("hidden"); 



    checkboxes.forEach(cb => { 

        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 

        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-nt-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 

    }); 



    setTimeout(() => { 

        window.print(); 

        pZone.classList.add("hidden"); 

        document.body.classList.remove("print-mode-bixolon");

    }, 300); 

}



// In Biên bản giao nhận khổ A4 máy in văn phòng

function inHoaDonGiaoNhan() {

    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; if (!k) return showToast("Chọn Khoa trước!", "error");

    

    let htmlBang = `

        <table style="width:100%; border-collapse:collapse; margin-top:20px; font-size:13px; text-align:left; font-family:Arial, sans-serif;">

            <thead>

                <tr style="background-color:#f1f5f9;">

                    <th style="padding:10px; border:1px solid #334155; width:35%; text-align:left; color:#0f172a;">PHÂN LOẠI MÂM / LOẠI DỤNG CỤ</th>

                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ TRẢ BẨN</th>

                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ NHẬN SẠCH</th>

                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">NHẬN VÔ KHUẨN</th>

                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:20%; color:#0f172a;">CSSD NỢ KHOA</th>

                </tr>

            </thead>

            <tbody>`;



    const rows = document.querySelectorAll('#bangDonGiaoNhan tr');

    if (rows.length === 0 || (rows.length === 1 && rows[0].cells.length === 1)) { 

        htmlBang += `<tr><td colspan="5" style="padding:15px; border:1px solid #334155; text-align:center; font-style:italic;">Không có dữ liệu công nợ.</td></tr>`; 

    } else {

        rows.forEach(row => {

            const cells = row.cells;

            if (cells.length >= 5) { 

                htmlBang += `

                    <tr>

                        <td style="padding:10px; border:1px solid #334155; font-weight:bold; color:#0f172a;">${cells[0].innerText}</td>

                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#e11d48;">${cells[1].innerText}</td>

                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#0284c7;">${cells[2].innerText}</td>

                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#7c3aed;">${cells[3].innerText}</td>

                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#d97706;">${cells[4].innerText}</td>

                    </tr>`; 

            }

        });

    }

    htmlBang += `</tbody></table>`;



    const pZone = document.getElementById("print-zone");

    pZone.innerHTML = `

        <div style="width:100%; font-family:Arial, sans-serif; padding:10px; color:#000;">

            <div style="text-align:center; margin-bottom:25px;">

                <h2 style="margin:0; font-size:18px; font-weight:900; text-transform:uppercase;">BIÊN BẢN GIAO NHẬN DỤNG CỤ CSSD</h2>

                <p style="margin:8px 0 0 0; font-size:13px; font-weight:bold;">Khoa/Phòng: <span style="text-transform:uppercase; color:#0284c7;">${k}</span> - Ngày xuất: ${new Date().toLocaleDateString('vi-VN')}</p>

            </div>

            ${htmlBang}

            <div style="margin-top:50px; display:flex; justify-content:space-between; font-size:12px; font-weight:bold; padding:0 30px;">

                <div style="text-align:center; width:40%;">

                    <p style="margin-bottom:70px; text-transform:uppercase;">ĐIỀU DƯỠNG LÂM SÀNG</p>

                    <p style="font-weight:normal; font-style:italic; color:#64748b;">(Ký và ghi rõ họ tên)</p>

                </div>

                <div style="text-align:center; width:40%;">

                    <p style="margin-bottom:70px; text-transform:uppercase;">CHUYÊN VIÊN CSSD</p>

                    <p style="font-weight:normal; font-style:italic; color:#64748b;">(Ký và ghi rõ họ tên)</p>

                </div>

            </div>

        </div>`;



    document.body.classList.remove("print-mode-bixolon");

    document.body.classList.add("print-mode-doc");

    pZone.classList.remove("hidden");



    setTimeout(() => { 

        window.print(); 

        pZone.classList.add("hidden"); 

        document.body.classList.remove("print-mode-doc");

    }, 200);

}



// =========================================================================

// 12. HÀM PHỤ TRỢ: CHỨC NĂNG QUẢN TRỊ ADMIN & TIỆN ÍCH

// =========================================================================

function taiDanhMucLinhKienChuan() {

    const tbody = document.getElementById("bangDanhMucLinhKien"); if (!tbody) return; tbody.innerHTML = "";

    if (!databaseExcel || databaseExcel.length === 0) { tbody.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-slate-400 italic">Chưa có dữ liệu. Vui lòng nạp file Excel.</td></tr>`; return; }

    let gopBoExcel = {};

    databaseExcel.forEach(x => {

        let tenBo = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; if (!tenBo) return;

        tenBo = String(tenBo).trim().toUpperCase(); if (!gopBoExcel[tenBo]) gopBoExcel[tenBo] = []; gopBoExcel[tenBo].push(x);

    });

    for (let tenBo in gopBoExcel) {

        let linhKienHtml = `<div class="flex flex-wrap gap-1">`; let tongSl = 0;

        gopBoExcel[tenBo].forEach(item => {

            let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || item['Dụng cụ'] || item['Tên Chi Tiết'] || "Dụng cụ Chi Tiết";

            

            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {

                tenDc = "Dụng cụ chuẩn mâm";

            }

            

            let sl = parseInt(item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1) || 0; let hanHap = item['Tuổi thọ mẻ hấp'] || item['Tuổi thọ'] || 100;

            tongSl += sl; linhKienHtml += `<span class="bg-slate-100 text-slate-700 px-1.5 py-0.5 rounded text-[10px] font-medium border border-slate-200">${tenDc} (${sl}) <span class="text-amber-600 font-bold">[Max: ${hanHap}]</span></span>`;

        });

        linhKienHtml += `</div>`; const tr = document.createElement("tr"); tr.className = "hover:bg-slate-50 transition-colors border-b";

        tr.innerHTML = `<td class="p-3 font-bold text-slate-800 text-xs">${tenBo}</td><td class="p-3">${linhKienHtml}</td><td class="p-3 text-center font-black text-sky-700 bg-sky-50/30">${tongSl}</td>`;

        tbody.appendChild(tr);

    }

}



function updateGiaoDienMatrix(role, tabId, checkboxElement) {

    if(!cauHinhGiaoDien[role]) cauHinhGiaoDien[role] = [];

    if(checkboxElement.checked) { if(!cauHinhGiaoDien[role].includes(tabId)) cauHinhGiaoDien[role].push(tabId); } 

    else { cauHinhGiaoDien[role] = cauHinhGiaoDien[role].filter(x => x !== tabId); }

    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ cauHinhGiaoDien: cauHinhGiaoDien }).then(() => showToast("Đã cập nhật quyền truy cập!", "success"));

}



function handleViewAnhBiMoi(firestoreId) { hanhDongXemAnhBiMoi(firestoreId); }

function hanhDongXemAnhBiMoi(firestoreId) {

    let item = listGiaoDich.find(x => x.firestoreId === firestoreId); if (!item) return showToast("Không tìm thấy thông tin!", "error");

    let loGoc = listGiaoDich.find(m => m.batchCode === item.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 

    let chuoiAnhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || item.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;

    if (!chuoiAnhBase64) return showToast("Mẻ này chưa tải ảnh minh chứng hoặc kế thừa.", "error");

    let w = window.open(); w.document.write(`<html><head><title>MINH CHỨNG BI - LÔ ${item.batchCode}</title></head><body style='margin:0; background:#000; display:flex; justify-content:center; align-items:center;'><img src='${chuoiAnhBase64}' style='max-width:100%; max-height:100vh; object-fit:contain;'/></body></html>`);

}



function switchAdminSubtab(sub) { 

    document.getElementById('subtab-database')?.classList.add('hidden'); document.getElementById('subtab-security')?.classList.add('hidden'); 

    document.getElementById('subbtn-database')?.classList.replace('admin-subtab-active', 'text-slate-600'); document.getElementById('subbtn-security')?.classList.replace('admin-subtab-active', 'text-slate-600'); 

    document.getElementById('subtab-' + sub)?.classList.remove('hidden'); document.getElementById('subbtn-' + sub)?.classList.add('admin-subtab-active'); 

}



function saveAdminPIN(type) { let newVal = document.getElementById(`cfg_pin${type.charAt(0).toUpperCase() + type.slice(1).toLowerCase()}`).value.trim(); if(type === 'ADMIN') thongTinMatKhauAdmin.adminPIN = newVal; if(type === 'CSSD') thongTinMatKhauAdmin.cssdPIN = newVal; if(type === 'GUEST') thongTinMatKhauAdmin.guestPIN = newVal; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ thongTinMatKhauAdmin: thongTinMatKhauAdmin }).then(() => showToast("Đã lưu PIN!", "success")); }

function themKtvCssd() { let code = prompt("Mã NV:"); let ten = prompt("Tên:"); let pin = prompt("PIN:"); if(code && ten && pin) { danhSachKtvCssd.push({ code: code.toUpperCase(), ten: ten, pin: pin }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }

function xaoKtvCssd(code) { if(confirm("Xóa?")) { danhSachKtvCssd = danhSachKtvCssd.filter(x => x.code !== code); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }

function themKhoaThuCong() { let t = prompt("Nhập Tên Khoa/Phòng:"); if(t) { danhSachKhoa.push({ ten: t.toUpperCase(), pin: "123", danhSachBo: [] }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Thêm Khoa thành công!", "success")); } }



function khaiSinhKhayVangLai() {

    if(danhSachKhoa.length === 0) return showToast("Hệ thống chưa có Khoa nào!", "error");

    let tenKhay = prompt("Nhập Tên Mâm/Khay dụng cụ:"); if(!tenKhay) return;

    let maId = prompt("Nhập Mã ID khay:"); if(!maId) return; maId = maId.toUpperCase().trim();

    let dsTenKhoa = danhSachKhoa.map((k, idx) => `${idx + 1}. ${k.ten}`).join("\n");

    let luaChon = prompt(`Chọn số thứ tự Khoa sở hữu:\n${dsTenKhoa}`); if(!luaChon) return;

    let idxKhoa = parseInt(luaChon) - 1; if(idxKhoa < 0 || idxKhoa >= danhSachKhoa.length) return showToast("Lựa chọn không hợp lệ!", "error");

    let khoaChon = danhSachKhoa[idxKhoa]; let chuoiKhayDinhDang = `${tenKhay.toUpperCase().trim()} [ID:${maId}]`;

    if(!khoaChon.danhSachBo) khoaChon.danhSachBo = []; if(khoaChon.danhSachBo.includes(chuoiKhayDinhDang)) return showToast("Khay đã tồn tại!", "error");

    khoaChon.danhSachBo.push(chuoiKhayDinhDang);

    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => { showToast(`Khai sinh khay ${maId} thành công!`, "success"); callRender(); });

}



function updatePINTrựcTiep(idx, tenKhoa) { let p = document.getElementById(`pin-khoa-${idx}`).value.trim(); if(p) { danhSachKhoa.find(x => x.ten === tenKhoa).pin = p; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Đổi PIN thành công", "success")); } }



function initSelects() { 

    let opts = '<option value="">-- Chọn Khoa --</option>' + danhSachKhoa.map(k=>`<option value="${k.ten}">${k.ten}</option>`).join('');

    if(document.getElementById("login_khoa")) document.getElementById("login_khoa").innerHTML = opts; 

    if(document.getElementById("khoa_selKhoa")) document.getElementById("khoa_selKhoa").innerHTML = opts; 

    if(document.getElementById("login_nv_cssd")) document.getElementById("login_nv_cssd").innerHTML = '<option value="">-- Chọn KTV CSSD --</option>' + danhSachKtvCssd.map(k=>`<option value="${k.code}">${k.code} - ${k.ten}</option>`).join(''); 

     

    if (document.getElementById("rua_hoaChat")) {

        document.getElementById("rua_hoaChat").innerHTML = danhMucHoaChatRua.map(hc => `<option value="${hc.ten}">${hc.ten}</option>`).join('');

        document.getElementById("rua_lieuLuong").value = danhMucHoaChatRua[0].lieuLuong;

        document.getElementById("rua_hoaChat").addEventListener("change", (e) => {

            let selectedHc = danhMucHoaChatRua.find(hc => hc.ten === e.target.value);

            if(selectedHc) document.getElementById("rua_lieuLuong").value = selectedHc.lieuLuong;

        });

    }

    if (document.getElementById("rua_loaiRua")) { capNhatDanhSachMaMayRua(); }

    if (document.getElementById("hap_loaiHap")) { capNhatDanhSachMaMay(); }

     

    const selKhoaTraba = document.getElementById("khoa_selKhoa"); if(selKhoaTraba) { selKhoaTraba.removeEventListener("change", loadBoDungCuTheoKhoa); selKhoaTraba.addEventListener("change", loadBoDungCuTheoKhoa); }

}



function showToast(msg, type="error") { const t = document.createElement('div'); t.className = `fixed top-6 right-6 ${type==="error"?"bg-rose-600":"bg-emerald-600"} text-white px-5 py-3.5 rounded-lg shadow-2xl z-[100] font-bold text-sm`; t.innerHTML = msg; document.body.appendChild(t); setTimeout(() => t.remove(), 2500); }

function toggleMobileMenu() { document.getElementById("sidebar_menu").classList.toggle("-translate-x-full"); document.getElementById("mobile-overlay").classList.toggle("hidden"); }



function moCamera(inputId) { targetInputIdForScan = inputId; document.getElementById("popupScanner").classList.remove("hidden"); html5QrCode = new Html5Qrcode("reader"); Html5Qrcode.getCameras().then(devices => { let cid = devices.length > 1 ? devices[devices.length - 1].id : devices[0].id; html5QrCode.start(cid, { fps: 15, qrbox: { width: 260, height: 180 } }, (txt) => { document.getElementById(targetInputIdForScan).value = txt.trim().toUpperCase(); if(targetInputIdForScan==='khoa_inpMaBo') themVaoGio(); if(targetInputIdForScan==='xuat_inpMaBo') xuatKhoXoayVong(); if(targetInputIdForScan==='sd_maKhayInp') scanKhayVaoSuDung(); }).catch(e=>{}) }); }

function dongCamera() { if(html5QrCode) html5QrCode.stop().then(() => html5QrCode.clear()); document.getElementById("popupScanner").classList.add("hidden"); }

function xoaSachDuLieuGiaoDichRealtime() { if(prompt("Nhập PIN ADMIN:") === (thongTinMatKhauAdmin.adminPIN||"admin2026")) { db.collection("phieuGiaoNhan").get().then(snap => { let b = db.batch(); snap.forEach(d => b.delete(d.ref)); b.commit().then(() => location.reload()); }); } }



function docAnhBiUpTaiCho(inputElement) { 

    const file = inputElement.files[0]; if (!file) return; 

    const reader = new FileReader(); reader.onloadend = function() { duLieuAnhBiTamBase64 = reader.result; showToast("Tải ảnh minh chứng BI thành công!", "success"); kiemTraQuyenDuyetMeHap(); }; 

    reader.readAsDataURL(file); 

}



// =========================================================================

// 13. HÀM CORE RENDER DỮ LIỆU ĐA TAB LUỒNG HỆ THỐNG

// =========================================================================

function renderTheoTabHienTai() {

    if(activeTab === 'khoaphong') {

        const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;

        let tatCaDonCuaKhoa = listGiaoDich.filter(x => (x.khoa || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase());

        

        // 1. TÍNH TOÁN CÔNG NỢ LUÂN CHUYỂN THỰC TẾ

        let gopCongNo = {};

        tatCaDonCuaKhoa.forEach(x => {

            if (x.status === "CHO_CAP_PHAT") return;



            let tenLoaiMâm = x.bo ? String(x.bo).split(" [ID:")[0] : "Chưa rõ mâm";

            if (!gopCongNo[tenLoaiMâm]) gopCongNo[tenLoaiMâm] = { daTraBan: 0, nhanSach: 0, nhanVoKhuan: 0, cssdNo: 0 };

            

            if (x.status !== "CHO_THU") gopCongNo[tenLoaiMâm].daTraBan += 1; 

            if (["DANG_RUA", "TRONG_BUONG_RUA", "CHO_DONG_GOI", "CHO_HAP", "DANG_HAP", "CHO_XUAT", "ĐANG_VAN_CHUYEN", "HOAN_TAT", "DA_SU_DUNG"].includes(x.status)) gopCongNo[tenLoaiMâm].nhanSach += 1;

            if (x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG") gopCongNo[tenLoaiMâm].nhanVoKhuan += 1; 

            gopCongNo[tenLoaiMâm].cssdNo = gopCongNo[tenLoaiMâm].daTraBan - gopCongNo[tenLoaiMâm].nhanVoKhuan;

        });

        

        let arrHtml = [];

        for (let key in gopCongNo) {

            let item = gopCongNo[key];

            arrHtml.push(`<tr class="border-b hover:bg-slate-50 transition-colors font-medium text-[11px]"><td class="p-3 font-bold text-slate-800 border-r">${key}</td><td class="p-3 text-center text-rose-600 font-bold bg-rose-50/20 border-r">${item.daTraBan}</td><td class="p-3 text-center text-sky-600 font-bold bg-sky-50/20 border-r">${item.nhanSach}</td><td class="p-3 text-center text-purple-600 font-bold bg-purple-50/20 border-r">${item.nhanVoKhuan}</td><td class="p-3 text-center text-amber-600 font-black bg-amber-50/20">${item.cssdNo > 0 ? 'Nợ ' + item.cssdNo : '-'}</td></tr>`);

        }

        if(document.getElementById("bangDonGiaoNhan")) {

            document.getElementById("bangDonGiaoNhan").innerHTML = arrHtml.length ? arrHtml.join('') : `<tr><td colspan="5" class="p-8 text-center text-slate-400 italic">Khoa chưa phát sinh công nợ luân chuyển đồ.</td></tr>`;

        }

        

        // 2. DANH SÁCH CHỜ NHẬN ĐỒ SẠCH (Từ xe trung chuyển CSSD bàn giao lên)

        const tbodyChoNhan = document.getElementById("bangChoNhanTaiKhoa"); const badgeChoNhan = document.getElementById("badgeChoNhanKhoa"); const txtNguoiXacNhan = document.getElementById("txtNguoiDungNhanHienTai");

        if(txtNguoiXacNhan) txtNguoiXacNhan.innerText = `PM-ĐD: ${loginUserCode || "Chưa Đăng Nhập"}`;

        

        let dsDangVanChuyen = tatCaDonCuaKhoa.filter(x => x.status === "ĐANG_VAN_CHUYEN"); if(badgeChoNhan) badgeChoNhan.innerText = `${dsDangVanChuyen.length} khay`;

        if(tbodyChoNhan) {

            if(dsDangVanChuyen.length === 0) { tbodyChoNhan.innerHTML = `<tr><td colspan="5" class="p-3 text-center text-slate-400 italic">Hiện không có dụng cụ nào đang chuyển về khoa.</td></tr>`; } 

            else { tbodyChoNhan.innerHTML = dsDangVanChuyen.map(khay => `<tr class="hover:bg-slate-50 transition-colors"><td class="p-2 text-center"><input type="checkbox" value="${khay.firestoreId}" class="w-3.5 h-3.5 text-sky-600 rounded border-slate-300 focus:ring-sky-500 cursor-pointer"></td><td class="p-2 font-mono font-bold text-slate-700">${khay.maMacDinh || 'N/A'}</td><td class="p-2 font-semibold text-slate-800">${khay.bo ? String(khay.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-2 text-slate-500 font-medium">${khay.nvXuatKho || '--'}</td><td class="p-2 text-center"><span class="bg-purple-50 text-purple-700 font-mono text-[10px] px-1.5 py-0.5 rounded font-bold">${khay.batchCode || 'N/A'}</span></td></tr>`).join(''); }

        }



        // 3. THEO DÕI YÊU CẦU ĐANG CHỜ CSSD CẤP PHÁT

        let dsChoCapPhat = tatCaDonCuaKhoa.filter(x => x.status === "CHO_CAP_PHAT");

        const tbodyChoCapPhat = document.getElementById("bangChoCapPhatTaiKhoa");

        if (tbodyChoCapPhat) {

            if (dsChoCapPhat.length === 0) {

                tbodyChoCapPhat.innerHTML = `<tr><td colspan="4" class="p-3 text-center text-slate-400 italic">Không có yêu cầu mượn khay mới nào đang chờ duyệt.</td></tr>`;

            } else {

                tbodyChoCapPhat.innerHTML = dsChoCapPhat.map(yc => `

                    <tr class="border-b text-xs hover:bg-amber-50/10 font-medium">

                        <td class="p-2.5 font-bold text-amber-800">${yc.bo}</td>

                        <td class="p-2.5 text-center"><span class="px-2 py-0.5 rounded-full text-[10px] font-bold bg-amber-100 text-amber-800 border border-amber-200 animate-pulse">Chờ CSSD cấp khay</span></td>

                        <td class="p-2.5 text-center font-mono text-slate-400">${yc.time}</td>

                        <td class="p-2.5 text-center text-slate-500">${yc.nvYeuCau || 'ĐD'}</td>

                    </tr>

                `).join('');

            }

        }

    }

    else if(activeTab === 'thugom') {

        let fK = document.getElementById("filterKhoaThuGom")?.value || ""; 

        let lsTG = listGiaoDich.filter(x => x.status === "CHO_THU"); 

        

        if (document.getElementById("filterKhoaThuGom")) {

            let danhSachKhoaCoDon = [...new Set(lsTG.map(x => x.khoa))].filter(Boolean); 

            let htmlOpts = '<option value="">-- Lọc theo Khoa --</option>';

            danhSachKhoaCoDon.forEach(k => { 

                htmlOpts += `<option value="${k}" ${(k === fK) ? "selected" : ""}>${k}</option>`; 

            });

            document.getElementById("filterKhoaThuGom").innerHTML = htmlOpts; 

            fK = document.getElementById("filterKhoaThuGom").value || "";

        }

        

        if (fK) lsTG = lsTG.filter(x => (x.khoa || "").toString().trim().toUpperCase() === fK.toString().trim().toUpperCase());

        if (document.getElementById("badgeSoCho")) document.getElementById("badgeSoCho").innerText = `${lsTG.length} Lệnh`;

        

        let bodyThuGom = document.getElementById("bangChoThuGom");

        if(bodyThuGom) {

            bodyThuGom.innerHTML = lsTG.map(i => {

                let tenBo = i.bo ? String(i.bo).split(" [ID:")[0].trim() : ""; 

                

                let itemsInBo = databaseExcel.filter(x => {

                    let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || '';

                    return String(boName).trim().toUpperCase() === String(tenBo).toUpperCase();

                });

                

                let checklistHtml = "";

                if (itemsInBo.length > 0) {

                    checklistHtml = `<div class="max-h-32 overflow-y-auto pr-1 border border-slate-200/60 rounded bg-slate-50/50 p-1.5 mt-1 flex flex-col gap-1">`;

                    itemsInBo.forEach(item => {

                        let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || "Dụng cụ chi tiết";

                        let sl = item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1;

                        

                        if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {

                            tenDc = "Dụng cụ chuẩn mâm";

                        }



                        checklistHtml += `

                            <div class="flex justify-between items-center border-b border-dashed border-slate-200 pb-0.5 last:border-none text-[11px]">

                                <span class="text-slate-700 font-semibold"><i class="fa-solid fa-circle text-[4px] text-sky-400 mr-1.5 align-middle"></i>${tenDc}</span>

                                <span class="font-bold text-sky-700 bg-sky-50 px-1.5 py-0.5 rounded border border-sky-100 text-[10px]">x${sl}</span>

                            </div>`;

                    });

                    checklistHtml += `</div>`;

                } else {

                    checklistHtml = `<div class="text-[10px] text-emerald-700 bg-emerald-50 border border-emerald-100 p-1.5 rounded font-medium mt-1"><i class="fa-solid fa-circle-check mr-1"></i>Dụng cụ lẻ (Cơ số: 1 chiếc)</div>`;

                }

                

                return `

                    <tr class="border-b border-slate-100 hover:bg-slate-50/40 transition-colors">

                        <td class="p-3 align-top">

                            <div class="font-black text-slate-700 text-[11px] uppercase tracking-wide">${i.khoa}</div>

                            ${i.ghiChu ? `<div class="text-[10px] text-rose-600 font-bold bg-rose-50 border border-rose-100 px-1.5 py-0.5 rounded mt-1.5 inline-block"><i class="fa-solid fa-triangle-exclamation mr-1"></i>${i.ghiChu}</div>` : ''}

                        </td>

                        <td class="p-3 align-top">

                            <div class="font-black text-sky-800 text-[13px] uppercase tracking-tight">${tenBo}</div>

                            <div class="text-[10px] font-mono text-slate-400 font-bold mt-0.5">MÃ SỐ ID: ${i.maMacDinh}</div>

                            ${checklistHtml}

                        </td>

                        <td class="p-3 text-center align-top font-mono font-bold text-slate-500 text-[11px]">${i.time}</td>

                        <td class="p-3 text-center align-top action-col">

                            <button onclick="moPopupKiemDem('${i.firestoreId}')" class="bg-sky-600 hover:bg-sky-700 text-white font-black px-3.5 py-2 rounded-lg shadow-sm text-[11px] uppercase tracking-wider transition-all"><i class="fa-solid fa-clipboard-check mr-1"></i>Kiểm Đếm</button>

                        </td>

                    </tr>`;

            }).join('');

        }

    }

    else if(activeTab === 'mayrua') {

        let lsCR = listGiaoDich.filter(x => x.status === "DANG_RUA");

        if(document.getElementById("badgeChoRua")) document.getElementById("badgeChoRua").innerText = `${lsCR.length} Mục`;

        

        let bodyChoRua = document.getElementById("bangChoRua");

        if(bodyChoRua) {

            bodyChoRua.innerHTML = lsCR.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="rua-checkbox"></td><td class="p-3 font-bold">${i.bo}</td><td class="p-3 text-right font-mono">${i.maMacDinh}</td></tr>`).join('');

        }

        

        let lsTR = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA");

        let bodyChoNghiemThuRua = document.getElementById("bangChoNiemThuRua");

        if(bodyChoNghiemThuRua) {

            bodyChoNghiemThuRua.innerHTML = lsTR.map(i => `<tr class="border-b"><td class="p-2.5 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="nghiemthurua-checkbox"></td><td class="p-2 text-xs font-bold">${i.bo} <span class="text-slate-400 font-mono">(${i.rua_batchCode || 'Mẻ Rửa'})</span></td></tr>`).join('');

        }

        

        let bodyLichSuRua = document.getElementById("bangLichSuRua");

        if(bodyLichSuRua) {

            const homNayMoiStr = getTodayDateStr();

            let tatCaMucCoLoRua = listGiaoDich.filter(x => x.rua_batchCode && (x.rua_ngayRua === homNayMoiStr || x.ngayTao === homNayMoiStr));

            let cacMeRuaGop = {};

            tatCaMucCoLoRua.forEach(x => {

                if(!cacMeRuaGop[x.rua_batchCode]) {

                    cacMeRuaGop[x.rua_batchCode] = {

                        batchCode: x.rua_batchCode,

                        mayRua: x.rua_mayRua || "Belimed WD250",

                        chuKy: x.rua_chuKy || "Chu trình tiêu chuẩn",

                        hoaChat: x.rua_hoaChat || "Smeg",

                        timeBatDau: x.rua_timeBatDau || "--:--",

                        ngay: x.rua_ngayRua || "",

                        soLuong: 0,

                        trangThaiMe: x.status === "TRONG_BUONG_RUA" ? "Đang rửa" : "Hoàn tất sạch"

                    };

                }

                cacMeRuaGop[x.rua_batchCode].soLuong += 1;

            });

            let danhSachMeRuaSapXep = Object.values(cacMeRuaGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));

            if(danhSachMeRuaSapXep.length === 0) {

                bodyLichSuRua.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa chạy mẻ rửa nào</td></tr>`;

            } else {

                bodyLichSuRua.innerHTML = danhSachMeRuaSapXep.map(me => `

                    <tr class="hover:bg-slate-50 transition-colors">

                        <td class="p-3">

                            <div class="flex items-center gap-2">

                                <span class="font-mono font-black text-sky-700 text-sm tracking-wider">${me.batchCode}</span>

                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "Đang rửa" ? "text-amber-600 bg-amber-50 border-amber-200" : "text-emerald-600 bg-emerald-50 border-emerald-200"}">${me.trangThaiMe}</span>

                            </div>

                            <div class="text-[11px] text-slate-500 font-medium mt-1">${me.mayRua} | ${me.chuKy}</div>

                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Hoá chất: ${me.hoaChat} | Bắt đầu: ${me.timeBatDau} (${me.ngay})</div>

                        </td>

                        <td class="p-3 text-right pr-4">

                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuong} khay</span>

                        </td>

                    </tr>`).join('');

            }

        }

    }

    else if(activeTab === 'donggoi') {

        let lsDG = listGiaoDich.filter(x => x.status === "CHO_DONG_GOI"); 

        if(document.getElementById("badgeDongGoi")) document.getElementById("badgeDongGoi").innerText = lsDG.length;

        let gDongGoi = document.getElementById("gridDongGoi");

        if(gDongGoi) {

            gDongGoi.innerHTML = lsDG.map(i => `<div class="bg-white p-3 rounded border border-slate-200 mb-2 flex justify-between items-center"><div class="flex-1"><div class="font-bold text-sky-700 text-[13px]">${i.bo}</div><div class="text-[10px] text-slate-500">Từ khoa: ${i.khoa}</div></div><button onclick="moPopupDongGoi('${i.firestoreId}')" class="bg-sky-50 text-sky-700 border border-sky-300 px-3 py-1.5 rounded text-[11px] font-black">ĐÓNG GÓI</button></div>`).join('');

        }

    }

    else if(activeTab === 'mayhap') {

        let lsCH = listGiaoDich.filter(x => x.status === "CHO_HAP"); 

        if(document.getElementById("badgeChoHap")) document.getElementById("badgeChoHap").innerText = `${lsCH.length} Mục`;

        let bodyChoHap = document.getElementById("bangChoHap");

        if(bodyChoHap) { 

            bodyChoHap.innerHTML = lsCH.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="hap-checkbox"></td><td class="p-3 font-bold text-xs">${i.bo}</td><td class="p-3 text-right font-mono text-xs font-bold text-sky-700">${i.maMacDinh}</td></tr>`).join(''); 

        }



        let lsNT = listGiaoDich.filter(x => x.status === "DANG_HAP"); 

        let bodyChoNghiemThu = document.getElementById("bangChoNghiệmThu") || document.getElementById("bangChoNghiemThu");

        

        if(bodyChoNghiemThu) { 

            if (lsNT.length === 0) {

                bodyChoNghiemThu.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Không có mâm/mẻ nào đang trong lò chờ duyệt.</td></tr>`;

            } else {

                bodyChoNghiemThu.innerHTML = lsNT.map(i => {

                    let tenBoClean = i.bo ? String(i.bo).split(" [ID:")[0] : "Chưa rõ mâm";

                    return `

                        <tr class="border-b hover:bg-amber-50/40 transition-colors">

                            <td class="p-2.5 text-center action-col">

                                <input type="checkbox" value="${i.firestoreId}" class="nghiemthu-checkbox w-4 h-4 text-amber-600 rounded cursor-pointer" data-hasbi="${i.hasBI ? 'true' : 'false'}" onchange="kiemTraQuyenDuyetMeHap()">

                            </td>

                            <td class="p-2 text-xs">

                                <div class="font-black text-slate-800">${tenBoClean} <span class="text-[10px] font-mono text-sky-700">(${i.maMacDinh || 'N/A'})</span></div>

                                <div class="flex items-center gap-2 mt-0.5">

                                    <span class="text-[10px] font-mono font-bold text-rose-700 bg-rose-50 px-1 rounded border border-rose-100">Lô: ${i.batchCode || 'N/A'}</span>

                                    ${i.hasBI ? '<span class="px-1.5 py-0.5 bg-amber-100 text-amber-800 rounded text-[9px] font-black border border-amber-300 flex items-center gap-1"><i class="fa-solid fa-flask"></i> CHỜ BI</span>' : ''}

                                </div>

                            </td>

                        </tr>`;

                }).join(''); 

            }

        }

        kiemTraQuyenDuyetMeHap();



        if(document.getElementById("bangLichSuHap")) {

            const homNayMoiStr = getTodayDateStr(); 

            let tatCaMucCoLo = listGiaoDich.filter(x => x.batchCode && (x.ngayHapRealtime === homNayMoiStr || x.ngayTao === homNayMoiStr)); 

            let cacMeHapGop = {};

            tatCaMucCoLo.forEach(x => {

                if(!cacMeHapGop[x.batchCode]) { 

                    cacMeHapGop[x.batchCode] = { 

                        batchCode: x.batchCode, 

                        loaiHap: x.thongTinLoHap?.loaiHap || "Hấp hơi nước", 

                        chuKyNhiet: x.thongTinLoHap?.chuKyNhiet || "134°C - 4 phút", 

                        apSuat: x.thongTinLoHap?.apSuat || "N/A", 

                        thoiGian: x.thongTinLoHap?.thoiGianBatDau || x.time || "--:--", 

                        ngay: x.ngayHapRealtime || x.ngayTao || "", 

                        soLuongKhay: 0, 

                        trangThaiMe: x.status === "DANG_HAP" ? "ĐANG CHẠY LÒ" : "HOÀN THÀNH" 

                    }; 

                }

                cacMeHapGop[x.batchCode].soLuongKhay += 1;

            });

            let danhSachMeHapSapXep = Object.values(cacMeHapGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));

            if(danhSachMeHapSapXep.length === 0) { 

                document.getElementById("bangLichSuHap").innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa có mẻ hấp nào</td></tr>`; 

            } else { 

                document.getElementById("bangLichSuHap").innerHTML = danhSachMeHapSapXep.map(me => `

                    <tr class="hover:bg-slate-50 transition-colors border-b border-slate-100">

                        <td class="p-3">

                            <div class="flex items-center gap-2">

                                <span class="font-mono font-black text-rose-700 text-sm tracking-wider">${me.batchCode}</span>

                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "ĐANG CHẠY LÒ" ? "text-purple-700 bg-purple-100 border border-purple-200 animate-pulse" : "text-emerald-700 bg-emerald-100 border border-emerald-200"}">${me.trangThaiMe}</span>

                            </div>

                            <div class="text-[11px] text-slate-600 font-medium mt-1">${me.loaiHap} | ${me.chuKyNhiet} | Áp suất: ${me.apSuat} Bar</div>

                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Bắt đầu: ${me.thoiGian} (${me.ngay})</div>

                        </td>

                        <td class="p-3 text-right pr-4">

                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuongKhay} khay</span>

                        </td>

                    </tr>`).join(''); 

            }

        }

    }

    else if(activeTab === 'khovokhuan') {

        let uniqueKhoaSanCo = [...new Set(listGiaoDich.map(x => x.khoa))].filter(Boolean); const selKhoaXuat = document.getElementById("xuat_selKhoa");

        if(selKhoaXuat) {

            let currentSelected = selKhoaXuat.value; let htmlOpts = '<option value="">-- Chọn Khoa Muốn Trả Đồ --</option>';

            uniqueKhoaSanCo.forEach(k => { htmlOpts += `<option value="${k}" ${k === currentSelected ? 'selected' : ''}>${k}</option>`; });

            selKhoaXuat.innerHTML = htmlOpts;

        }



        let lsXK = listGiaoDich.filter(x => x.status === "CHO_XUAT").sort((a, b) => a.id - b.id);

        renderKhoVoKhuan(lsXK);



        let dsYeuCau = listGiaoDich.filter(x => x.status === "CHO_CAP_PHAT");

        let vungYeuCau = document.getElementById("vungYeuCauCapPhat");

        if (!vungYeuCau) {

            let parentContainer = document.getElementById("tab-khovokhuan");

            if (parentContainer) {

                let divYc = document.createElement("div");

                divYc.id = "vungYeuCauCapPhat";

                divYc.className = "mb-6 bg-amber-50/70 border border-amber-200 rounded-xl p-4 shadow-sm";

                parentContainer.insertBefore(divYc, parentContainer.firstChild);

                vungYeuCau = divYc;

            }

        }

        

        if (vungYeuCau) {

            if (dsYeuCau.length === 0) {

                vungYeuCau.innerHTML = `<h3 class="font-black text-amber-950 text-xs flex items-center gap-1.5"><i class="fa-solid fa-bell-slash"></i> HIỆN KHÔNG CÓ YÊU CẤP PHÁT ĐANG CHỜ</h3>`;

            } else {

                vungYeuCau.innerHTML = `

                    <h3 class="font-black text-amber-900 text-xs mb-2.5 flex items-center gap-1.5 animate-pulse">

                        <i class="fa-solid fa-bell text-rose-500 animate-bounce"></i> CÓ ${dsYeuCau.length} YÊU CẦU CẤP PHÁT TỪ CÁC KHOA LÂM SÀNG

                    </h3>

                    <div class="flex flex-col gap-2 max-h-48 overflow-y-auto">

                        ${dsYeuCau.map(yc => `

                            <div class="bg-white p-3 rounded-lg border border-amber-200 shadow-sm flex justify-between items-center text-xs">

                                <div>

                                    <span class="font-black text-amber-800 uppercase">[KHOA: ${yc.khoa}]</span> 

                                    Yêu cầu mâm: <span class="font-bold text-slate-800">${yc.bo}</span>

                                    <span class="text-[10px] text-slate-400 block mt-0.5">Thời gian: ${yc.time} - Người yêu cầu: ${yc.nvYeuCau || 'ĐD'}</span>

                                </div>

                                <div class="flex items-center gap-2">

                                    <button onclick="duyetCapPhatTậpTrung('${yc.firestoreId}', '${yc.bo}', '${yc.khoa}')" class="bg-amber-600 hover:bg-amber-700 text-white font-black px-3 py-2 rounded-lg text-[10px] uppercase shadow-sm flex items-center gap-1.5 transition-all">

                                        <i class="fa-solid fa-truck-ramp-box"></i>Cấp phát khay

                                    </button>

                                </div>

                            </div>

                        `).join('')}

                    </div>`;

            }

        }

        renderGioHangXuat();

    }

    else if(activeTab === 'quanlykho') {

        let fK = document.getElementById("inv_filterKhoa") ? document.getElementById("inv_filterKhoa").value : ""; 

        let uniqueKhoa = [...new Set(listGiaoDich.map(x=>x.khoa))].filter(Boolean);

        if(document.getElementById("inv_filterKhoa")) { document.getElementById("inv_filterKhoa").innerHTML = '<option value="">-- Tất cả --</option>' + uniqueKhoa.map(k=>`<option value="${k}" ${k===fK?'selected':''}>${k}</option>`).join(''); }

        let uniqueIDs = [...new Set(listGiaoDich.map(x=>x.maMacDinh))]; let arrHtml = []; const ngayHomNay = new Date(); ngayHomNay.setHours(0,0,0,0); 

        uniqueIDs.forEach(ma => {

            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a,b) => b.id - a.id)[0]; if(!currentTrans || !ma) return;

            let viTriCode = currentTrans.status; let khoaGiữ = currentTrans.khoa; if (fK && (khoaGiữ || "").toString().trim().toUpperCase() !== fK.toString().trim().toUpperCase()) return;

            let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";

            if(viTriCode === "HOAN_TAT") { viTriText = `Sẵn sàng tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 

            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã sử dụng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }

            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang vận chuyển`; viTriColor = "bg-purple-100 text-purple-800 animate-pulse"; }

            else if(viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }

            let hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-slate-100 text-slate-500">An toàn</span>`; let dongColorClass = ""; 

            if (currentTrans.hsd && (viTriCode === "CHO_XUAT" || viTriCode === "HOAN_TAT")) {

                const ngayHsd = new Date(currentTrans.hsd); ngayHsd.setHours(0,0,0,0); const soNgayConLai = Math.ceil((ngayHsd.getTime() - ngayHomNay.getTime()) / (1000 * 60 * 60 * 24));

                if (soNgayConLai < 0) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-rose-600 text-white">QUÁ HẠN</span>`; dongColorClass = "bg-rose-50/70 border-l-4 border-l-rose-500 font-bold"; } 

                else if (soNgayConLai <= 3) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-amber-500 text-white">HẠN < ${soNgayConLai} NGÀY</span>`; dongColorClass = "bg-amber-50/40 border-l-4 border-l-amber-500"; } 

                else { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-emerald-100 text-emerald-800">Hạn ${soNgayConLai} ngày</span>`; }

            }

            arrHtml.push(`<tr class="border-b border-slate-100 ${dongColorClass}"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-slate-500 text-[11px]">${khoaGiữ}</td><td class="p-3 text-center"><span class="px-2.5 py-1 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-mono font-bold text-slate-400">${currentTrans.batchCode || 'N/A'}</td><td class="p-3 text-center">${hsdBadget}</td></tr>`);

        });

        if(document.getElementById("bangTonKhoTe")) document.getElementById("bangTonKhoTe").innerHTML = arrHtml.join('');

    }

    else if(activeTab === 'danhmuc') {

        const tbody = document.getElementById("bangDanhMucTong"); if (!tbody) return;

        let uniqueIDs = [...new Set(listGiaoDich.map(x => x.maMacDinh))].filter(Boolean); let arrHtml = [];

        uniqueIDs.forEach(ma => {

            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a, b) => b.id - a.id)[0]; if (!currentTrans) return;

            let viTriCode = currentTrans.status; let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";

            if (viTriCode === "HOAN_TAT") { viTriText = `Tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 

            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã dùng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }

            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang đi đường`; viTriColor = "bg-purple-100 text-purple-800"; }

            else if (viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }

            let chuKyLo = listGiaoDich.filter(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG" || x.status === "ĐANG_VAN_CHUYEN")).length;

            arrHtml.push(`<tr class="border-b border-slate-100 font-medium"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-center"><span class="px-2.5 py-0.5 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-black text-amber-700 bg-amber-50/50">${chuKyLo} lần lễ</td></tr>`);

        });

        tbody.innerHTML = arrHtml.join('');

    }

    else if(activeTab === 'lichsuluanchuyen') {

        const tbody = document.getElementById("bangLichSuHanhTrinhGoc");

        if(tbody) {

            tbody.innerHTML = listGiaoDich.map(x => {

                let statusBadge = `<span class="px-2 py-0.5 bg-slate-100 text-slate-700 font-bold rounded text-[10px]">${x.status}</span>`;

                if(x.status === "HOAN_TAT") statusBadge = `<span class="px-2 py-0.5 bg-emerald-100 text-emerald-800 font-black rounded text-[10px]">ĐÃ BÀN GIAO</span>`;

                else if(x.status === "CHO_XUAT") statusBadge = `<span class="px-2 py-0.5 bg-teal-100 text-teal-800 font-bold rounded text-[10px]">KHO VÔ KHUẨN</span>`;

                else if(x.status === "DANG_HAP") statusBadge = `<span class="px-2 py-0.5 bg-purple-100 text-purple-800 font-bold rounded text-[10px]">ĐANG HẤP LÒ</span>`;

                else if(x.status === "DA_SU_DUNG") statusBadge = `<span class="px-2 py-0.5 bg-rose-600 text-white font-black rounded text-[10px]">SỬ DỤNG BN</span>`;

                else if(x.status === "ĐANG_VAN_CHUYEN") statusBadge = `<span class="px-2 py-0.5 bg-purple-500 text-white font-bold rounded text-[10px] animate-pulse">ĐANG VẬN CHUYỂN</span>`;

                else if(x.status === "DANG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-blue-100 text-blue-700 font-bold rounded text-[10px]">HÀNG ĐỢI RỬA</span>`;

                else if(x.status === "TRONG_BUONG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-cyan-600 text-white font-bold rounded text-[10px]">ĐANG RỬA MÁY</span>`;

                else if(x.status === "CHO_CAP_PHAT") statusBadge = `<span class="px-2 py-0.5 bg-amber-100 text-amber-800 font-bold rounded text-[10px] border border-amber-200">CHỜ CẤP PHÁT</span>`;

                

                let fontColor = ""; let loGoc = listGiaoDich.find(m => m.batchCode === x.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 

                let anhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || x.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;

                if (anhBase64) fontColor = `<br><span onclick="handleViewAnhBiMoi('${x.firestoreId}')" class="text-[10px] text-emerald-600 bg-emerald-50 border border-emerald-300 rounded px-1 cursor-pointer font-bold mt-1 inline-block"><i class="fa-solid fa-image mr-1"></i>Xem ảnh BI</span>`;

                

                let kpiHtml = x.ketQuaGiamSatKpi ? `<br><span class="text-[9px] px-1 rounded font-black border mt-1 inline-block ${x.ketQuaGiamSatKpi === "ĐẠT" ? "text-emerald-700 bg-emerald-50 border-emerald-200" : "text-rose-700 bg-rose-50 border-rose-200"}">KPI: ${x.ketQuaGiamSatKpi} (${x.thoiGianTinhKpiPhut}m)</span>` : "";

                let thongTinKemTheo = x.nvXuatKho || '<span class="text-slate-300 font-normal">Chưa xuất</span>';

                if (x.nguoiKyNhanKhoa) thongTinKemTheo += ` ➔ <span class="text-emerald-700 font-semibold">[ĐD: ${x.nguoiKyNhanKhoa}]</span>`;

                if (x.status === "DA_SU_DUNG" && x.thongTinBenhNhan) thongTinKemTheo = `<span class="text-teal-700 font-black">BN: ${x.thongTinBenhNhan.thongTinTimKiemBN}</span>`;



                return `<tr class="border-b text-xs hover:bg-slate-50 transition-colors"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3 text-center">${statusBadge}${kpiHtml}</td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/30">${x.batchCode || 'N/A'}${fontColor}</td><td class="p-3 text-center font-bold text-sky-800 bg-sky-50/40">${thongTinKemTheo}</td><td class="p-3 text-center text-slate-400 font-mono text-[11px]">${x.ngayTao || ''} ${x.time || ''}</td></tr>`;

            }).join('');

        }

    }

    else if(activeTab === 'tracuu') {

        let safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");

        if (!safeTbody) {

            let parentContainer = document.getElementById("tab-tracuu") || document.querySelector('.bg-white.p-6.rounded-xl.shadow-md');

            if(parentContainer) {

                let existingTableZone = document.getElementById("vung-ket-qua-tu-dong");

                if(!existingTableZone) {

                    let tableWrapper = document.createElement("div"); tableWrapper.id = "vung-ket-qua-tu-dong"; tableWrapper.className = "mt-6 bg-white border border-slate-200 rounded-xl p-4 shadow-sm";

                    tableWrapper.innerHTML = `<h3 class="font-black text-slate-800 text-sm mb-3 text-sky-800"><i class="fa-solid fa-list-check mr-2"></i>DANH SÁCH MÂM DỤNG CỤ TRONG MẺ TRUY VẾT</h3><div class="overflow-x-auto"><table class="w-full text-left border-collapse"><thead><tr class="bg-slate-100 text-[11px] font-bold text-slate-600 uppercase border-b"><th class="p-3">Mã ID Khay</th><th class="p-3">Tên Bộ Dụng Cụ</th><th class="p-3">Khoa Sử Dụng</th><th class="p-3">Trạng thái</th><th class="p-3 text-center">Mã Lô Hấp</th><th class="p-3 text-center">Thời Gian Kích Hoạt</th></tr></thead><tbody id="bangLichSuTruyXuatAdmin"></tbody></table></div>`;

                    parentContainer.appendChild(tableWrapper); safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");

                }

            }

        }

        let searchInp = maLoTruyVetToanCuc || ""; if (!searchInp && document.getElementById("inp_searchBatch")) { searchInp = document.getElementById("inp_searchBatch").value.trim(); }

        if(safeTbody) {

            if (!searchInp) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng nhập mã mẻ hấp và nhấn "TRA CỨU KHẨN CẤP"...</td></tr>`; return; }

            let dataFiltered = listGiaoDich.filter(x => x.batchCode && String(x.batchCode).toUpperCase().includes(searchInp.toUpperCase()));

            if(dataFiltered.length === 0) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-rose-500 italic font-bold">Không tìm thấy dữ liệu: ${searchInp}</td></tr>`; } 

            else { safeTbody.innerHTML = dataFiltered.map(x => `<tr class="border-b text-xs hover:bg-slate-50 font-medium"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3"><span class="px-2 py-0.5 rounded text-[10px] ${x.status === "HOAN_TAT" ? "bg-emerald-100 text-emerald-800 font-bold" : "bg-teal-100 text-teal-800"}">${x.status}</span></td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/40">${x.batchCode || 'N/A'}</td><td class="p-3 text-center text-slate-400 font-mono">${x.ngayTao || ''} ${x.time || ''}</td></tr>`).join(''); }

        }

    }

    else if(activeTab === 'performance') { renderKpiPerformanceGoc(); }

    else if(activeTab === 'dashboard_tv') { renderDashboardTiviRealtime(); }

}



function renderKhoVoKhuan(listData) {

    const tbody = document.getElementById("bangKhoVoKhuan");

    if (!tbody) return;



    if (!listData || listData.length === 0) {

        tbody.innerHTML = `<tr><td colspan="6" class="p-6 text-center text-slate-400 italic">Kho vô khuẩn hiện tại trống.</td></tr>`;

        return;

    }



    tbody.innerHTML = listData.map(item => `

        <tr class="hover:bg-slate-50 transition-colors">

            <td class="p-3.5 font-bold text-slate-800 text-xs">${item.bo ? String(item.bo).split(" [ID:")[0] : 'N/A'}</td>

            <td class="p-3.5 font-mono font-black text-sky-700 text-xs">${item.maMacDinh || '--'}</td>

            <td class="p-3.5 text-center font-semibold text-slate-600 text-xs">${item.khoa || 'Tập trung'}</td>

            <td class="p-3.5 text-center"><span class="bg-emerald-100 text-emerald-800 font-bold px-2.5 py-1 rounded-full text-[11px]">${item.viTriKe || 'Kệ A1'}</span></td>

            <td class="p-3.5 text-center font-mono font-bold text-xs text-rose-600">${item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN') : 'An toàn'}</td>

            <td class="p-2 text-center">

                <button onclick="xuatKhoXoayVong('${item.maMacDinh}')" 

                        class="min-h-[44px] px-4 bg-emerald-600 active:bg-emerald-700 text-white font-bold rounded-lg text-xs shadow-sm inline-flex items-center gap-1.5 transition-transform active:scale-95">

                    <i class="fa-solid fa-cart-flatbed"></i>

                    <span>Xuất Khay</span>

                </button>

            </td>

        </tr>

    `).join('');

}



// =========================================================================

// VÁ LỖI CÁC HÀM TRUY VẾT & ĐỒNG BỘ HIỆU SUẤT TV MÀN HÌNH

// =========================================================================

function truyVetTheoMaBatch() {

    const inp = document.getElementById("inp_searchBatch");

    if (inp) {

        maLoTruyVetToanCuc = inp.value.trim();

        playSound('success');

        callRender();

    }

}



function clearTruyVetBatch() {

    const inp = document.getElementById("inp_searchBatch");

    if (inp) inp.value = "";

    maLoTruyVetToanCuc = "";

    callRender();

}



function resetDuLieuKet() {

    if (confirm("Anh Hùng có chắc chắn muốn giải phóng các mâm đang xử lý dở dang (chưa Hoàn tất / Chưa dùng) về trạng thái Chờ thu gom?")) {

        let p = [];

        listGiaoDich.forEach(x => {

            if (x.status !== "HOAN_TAT" && x.status !== "DA_SU_DUNG") {

                p.push(db.collection("phieuGiaoNhan").doc(x.firestoreId).update({ status: "CHO_THU" }));

            }

        });

        Promise.all(p).then(() => {

            playSound('success');

            showToast("Đã giải phóng dữ liệu mâm kẹt thành công!", "success");

            callRender();

        });

    }

}



function renderKpiPerformanceGoc() {

    const tbody = document.getElementById("bangHieuSuatKTV");

    if (!tbody) return;

    let kpiGop = {};

    listGiaoDich.forEach(x => {

        let ktv = x.ktvThuGom || x.nvXuatKho || "CSSD_CHUNG";

        if (!kpiGop[ktv]) kpiGop[ktv] = { code: ktv, ten: ktv, dat: 0, tong: 0 };

        if (x.ketQuaGiamSatKpi === "ĐẠT") kpiGop[ktv].dat += 1;

        if (x.ketQuaGiamSatKpi) kpiGop[ktv].tong += 1;

    });

    let sortedKpi = Object.values(kpiGop).sort((a,b) => b.dat - a.dat);

    if(sortedKpi.length === 0) {

        tbody.innerHTML = `<tr><td colspan="4" class="p-4 text-center italic text-slate-400">Hôm nay chưa có dữ liệu mẻ KPI nào.</td></tr>`;

    } else {

        tbody.innerHTML = sortedKpi.map((item, idx) => `

            <tr class="border-b text-xs hover:bg-slate-50 font-medium">

                <td class="p-3 text-center font-bold">${idx + 1}</td>

                <td class="p-3 font-mono font-bold text-sky-700">${item.code}</td>

                <td class="p-3 font-bold text-slate-800">${item.ten}</td>

                <td class="p-3 text-center"><span class="bg-emerald-100 text-emerald-800 px-2.5 py-1 rounded font-black">${item.dat}/${item.tong} Mẻ Đạt KPI</span></td>

            </tr>

        `).join('');

    }

}



function renderDashboardTiviRealtime() {

    let meRuaInDay = listGiaoDich.filter(x => x.rua_batchCode).length;

    let meHapInDay = listGiaoDich.filter(x => x.batchCode).length;

    let dangRua = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA").length;

    let dangHap = listGiaoDich.filter(x => x.status === "DANG_HAP").length;

    let sanSangXuat = listGiaoDich.filter(x => x.status === "CHO_XUAT").length;

    

    const today = new Date(); today.setHours(0,0,0,0);

    let quaHan = 0;

    listGiaoDich.forEach(x => {

        if (x.hsd && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT")) {

            if (new Date(x.hsd) < today) quaHan++;

        }

    });



    if (document.getElementById('tv_meRua')) document.getElementById('tv_meRua').innerText = meRuaInDay;

    if (document.getElementById('tv_meHap')) document.getElementById('tv_meHap').innerText = meHapInDay;

    if (document.getElementById('tv_meHap2')) document.getElementById('tv_meHap2').innerText = meHapInDay;

    if (document.getElementById('tv_dangRua')) document.getElementById('tv_dangRua').innerText = dangRua;

    if (document.getElementById('tv_dangHap')) document.getElementById('tv_dangHap').innerText = dangHap;

    if (document.getElementById('tv_khoVoKhuan')) document.getElementById('tv_khoVoKhuan').innerText = sanSangXuat;

    if (document.getElementById('tv_canhBaoHsd')) document.getElementById('tv_canhBaoHsd').innerText = quaHan;

}



function renderAdminInterface() {

    if (document.getElementById("cfg_pinAdmin")) document.getElementById("cfg_pinAdmin").value = thongTinMatKhauAdmin.adminPIN || "";

    if (document.getElementById("cfg_pinCSSD")) document.getElementById("cfg_pinCSSD").value = thongTinMatKhauAdmin.cssdPIN || "";

    if (document.getElementById("cfg_pinGuest")) document.getElementById("cfg_pinGuest").value = thongTinMatKhauAdmin.guestPIN || "";

    const MatrixRoles = ["CSSD", "KHOA", "GUEST"];

    const MatrixTabs = [

        { id: "khoaphong", name: "1. Cổng Báo Trả Đồ" }, { id: "thugom", name: "2. Xe Thu Gom" }, { id: "mayrua", name: "3. Quản Lý Mẻ Rửa" }, { id: "donggoi", name: "4. Làm Sạch & Gói" }, { id: "mayhap", name: "5. Quản Lý Mẻ Hấp" }, { id: "khovokhuan", name: "6. Kho Vô Khuẩn" }, { id: "quanlykho", name: "7. Tồn Kho Toàn Viện" }, { id: "danhmuc", name: "8. Giám Sát Tuổi Thọ" }, { id: "lichsuluanchuyen", name: "9. Nhật Ký Luân Chuyển" }, { id: "tracuu", name: "10. Cấu Hình Hệ Thống" }, { id: "performance", name: "11. Hiệu Suất KPI" }, { id: "dashboard_tv", name: "12. Màn Hình Tivi" }

    ];

    let htmlMatrix = "";

    MatrixTabs.forEach(tab => {

        htmlMatrix += `<tr class="border-b font-medium text-xs hover:bg-slate-50"><td class="p-3 font-bold text-slate-700">${tab.name}</td>`;

        MatrixRoles.forEach(role => {

            let isChecked = (cauHinhGiaoDien[role] && cauHinhGiaoDien[role].includes(tab.id)) ? "checked" : "";

            htmlMatrix += `<td class="p-3 text-center"><input type="checkbox" ${isChecked} onchange="updateGiaoDienMatrix('${role}', '${tab.id}', this)" class="w-4 h-4 rounded text-sky-600 focus:ring-sky-500"></td>`;

        });

        htmlMatrix += `</tr>`;

    });

    const tbodyMatrix = document.getElementById("bodyMaTranGiaoDien"); if (tbodyMatrix) tbodyMatrix.innerHTML = htmlMatrix;

    const trKhoa = document.getElementById("bangPhanQuyenKhoa");

    if(trKhoa) {

        if(danhSachKhoa.length === 0) { trKhoa.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-rose-500 font-bold italic">Danh sách Khoa trống.</td></tr>`; } 

        else { trKhoa.innerHTML = danhSachKhoa.map((k, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-slate-700 text-[11px]">${k.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-rose-600 text-sm bg-rose-50/50">${k.pin || '123'}</td><td class="p-3 text-center"><div class="flex items-center justify-center gap-2"><input type="text" id="pin-khoa-${index}" placeholder="PIN" class="w-24 p-1.5 text-center border border-slate-300 rounded text-xs font-bold"><button onclick="updatePINTrựcTiep(${index}, '${k.ten}')" class="bg-sky-600 text-white font-bold py-1.5 px-3 rounded shadow text-[10px]">ĐỔI PIN</button></div></td></tr>`).join(''); }

    }

    const tbKtv = document.getElementById("bangNhanVienCssd");

    if(tbKtv) {

        if(danhSachKtvCssd.length === 0) { tbKtv.innerHTML = `<tr><td colspan="4" class="p-4 text-center text-slate-400 italic">Chưa có Nhân viên.</td></tr>`; }

        else { tbKtv.innerHTML = danhSachKtvCssd.map((nv, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-sky-700 text-xs">${nv.code}</td><td class="p-3 font-bold text-slate-700 text-[11px]">${nv.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-sky-600 text-sm bg-sky-50/50">${nv.pin}</td><td class="p-3 text-center"><button onclick="xaoKtvCssd('${nv.code}')" class="text-rose-600 font-bold text-[10px]"><i class="fa-solid fa-trash-can mr-1"></i>XÓA</button></td></tr>`).join(''); }

    }

}



// =========================================================================

// 14. XỬ LÝ ĐỌC FILE EXCEL ĐA SHEET

// =========================================================================

document.addEventListener("DOMContentLoaded", () => {

    const fileInput = document.getElementById("excelFileInput");

    if (fileInput) {

        fileInput.addEventListener("change", (e) => {

            const file = e.target.files[0];

            if (!file) return;



            const reader = new FileReader();

            reader.onload = function(e) {

                try {

                    const data = new Uint8Array(e.target.result);

                    const workbook = XLSX.read(data, { type: 'array' });

                    

                    let updateData = {};

                    let logs = [];



                    workbook.SheetNames.forEach(sheetName => {

                        const worksheet = workbook.Sheets[sheetName];

                        const jsonData = XLSX.utils.sheet_to_json(worksheet);

                        if (jsonData.length === 0) return;



                        const firstRow = jsonData[0];

                        const keys = Object.keys(firstRow).map(k => k.trim().toUpperCase());



                        const coCotMaDC = keys.includes("MÃ DC");

                        if (coCotMaDC) {

                            let mapKhoa = {};

                            danhSachKhoa.forEach(k => {

                                mapKhoa[k.ten.trim().toUpperCase()] = {

                                    ten: k.ten.trim().toUpperCase(),

                                    pin: k.pin || "123",

                                    danhSachBo: []

                                };

                            });



                            jsonData.forEach(row => {

                                let rawKeyKhoa = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS (I)");

                                let rawKeyBo = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS CHUẨN");

                                let rawKeyMa = Object.keys(row).find(k => k.trim().toUpperCase() === "MÃ DC");



                                let tenKhoa = rawKeyKhoa ? row[rawKeyKhoa] : null;

                                let tenBo = rawKeyBo ? row[rawKeyBo] : null;

                                let maKhay = rawKeyMa ? row[rawKeyMa] : null;



                                if (tenKhoa && tenBo) {

                                    let keyKhoa = String(tenKhoa).trim().toUpperCase();

                                    let cleanBo = String(tenBo).trim().toUpperCase();

                                    let cleanMa = maKhay ? String(maKhay).trim().toUpperCase() : "CHUA_CO_ID";

                                    let chuoiDinhDangKhay = `${cleanBo} [ID:${cleanMa}]`;



                                    if (!mapKhoa[keyKhoa]) {

                                        mapKhoa[keyKhoa] = { ten: keyKhoa, pin: "123", danhSachBo: [] };

                                    }

                                    if (!mapKhoa[keyKhoa].danhSachBo.includes(chuoiDinhDangKhay)) {

                                        mapKhoa[keyKhoa].danhSachBo.push(chuoiDinhDangKhay);

                                    }

                                }

                            });



                            updateData.danhSachKhoa = Object.values(mapKhoa);

                            logs.push(`Cập nhật định biên cơ số cho ${updateData.danhSachKhoa.length} Khoa/Phòng`);

                        }



                        const coCotMaAesculap = keys.includes("AB 120/12");

                        if (coCotMaAesculap) {

                            updateData.databaseExcel = jsonData;

                            logs.push(`Nạp thành công ${jsonData.length} linh kiện chi tiết mâm Aesculap`);

                        }

                    });



                    if (Object.keys(updateData).length === 0) {

                        return showToast("Không tìm thấy cấu trúc dữ liệu phù hợp (Cột 'MÃ DC' hoặc 'AB 120/12') trong file Excel up lên!", "error");

                    }



                    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update(updateData)

                    .then(() => {

                        playSound('success');

                        showToast(`Đồng bộ thành công: ${logs.join(" | ")}!`, "success");

                        fileInput.value = ""; 

                        callRender();

                    }).catch(err => {

                        console.error("Lỗi cập nhật Firestore:", err);

                        showToast("Thất bại khi đẩy dữ liệu lên Cloud Firestore!", "error");

                    });



                } catch (err) {

                    console.error("Lỗi FileReader:", err);

                    showToast("Định dạng file không được hỗ trợ hoặc bị lỗi!", "error");

                }

            };

            reader.readAsArrayBuffer(file);

        });

    }

});

Bạn thêm dùm tôi vô app.js

Toàn bộ mã nguồn file app.js đã được tổng hợp, khắc phục và nâng cấp hoàn chỉnh.

Các điểm đã cập nhật trong file app.js:
Lưu trữ nét vẽ chữ ký điện tử (luuXacNhanKyNhan): Khi người dùng vẽ chữ ký trên canvas và bấm chốt tiếp nhận, ứng dụng sẽ tự động mã hóa chữ ký thành chuỗi Base64 (anhChuKyNhanBase64) kèm tên Điều dưỡng và mốc thời gian ký, đồng thời lưu trữ đồng bộ lên Cloud Firestore và localStorage của trình duyệt theo mã Khoa phòng.

Hiển thị chữ ký lên Biên bản giao nhận A4 (inHoaDonGiaoNhan):

Hàm in sẽ kiểm tra dữ liệu chữ ký gần nhất từ Firestore/localStorage của Khoa hiện tại.

Nếu đã ký nhận, Biên bản A4 sẽ tự động chèn thẻ <img> chứa chữ ký thực tế, hiển thị rõ tên Điều dưỡng lâm sàng, ngày giờ ký nhận trực tiếp bên dưới cột ĐIỀU DƯỠNG LÂM SÀNG.

Nếu chưa ký nhận, biên bản sẽ giữ nguyên dòng chữ nhắc (Ký và ghi rõ họ tên) như bình thường.

Mã nguồn hoàn chỉnh app.js
JavaScript
// =========================================================================
// 1. KHỞI TẠO HỆ THỐNG & CẤU HÌNH BIẾN TOÀN CỤC (PN HOSPITAL)
// =========================================================================
const firebaseConfig = { 
    apiKey: "AIzaSyCxjdCTKHQlpm7SYbWCEws1HhcOaFp0LBA", 
    authDomain: "cssd-system-2878c.firebaseapp.com", 
    projectId: "cssd-system-2878c", 
    storageBucket: "cssd-system-2878c.firebasestorage.app", 
    messagingSenderId: "662377321937", 
    appId: "1:662377321937:web:001c092e10319547623cf0" 
};
firebase.initializeApp(firebaseConfig); 
const db = firebase.firestore();

let thongTinMatKhauAdmin = { adminPIN: "admin2026", cssdPIN: "cssd2026", guestPIN: "guest2026" };
let cauHinhGiaoDien = {
    "ADMIN": ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'],
    "CSSD": ['thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','dashboard_tv'], 
    "KHOA": ['khoaphong','quanlykho','lichsuluanchuyen'], 
    "GUEST": ['quanlykho','danhmuc','lichsuluanchuyen','performance','dashboard_tv']
};

let currentRole = "", loginUserCode = "";
let danhSachKhoa = [], listGiaoDich = [], gioHangTam = [], danhSachKtvCssd = [], databaseExcel = [];
let html5QrCode = null; let targetInputIdForScan = ""; let idDangDongGoi = null;
let activeTab = 'thugom'; let renderTimeout = null;
let duLieuAnhBiTamBase64 = ""; let maLoTruyVetToanCuc = ""; 
let gioHangXuatKho = []; let gioKhaySuDungTam = []; 
let currentKiemDemData = null;

// Biến hỗ trợ vẽ chữ ký điện tử
let canvasSignature = null;
let ctxSignature = null;
let isDrawingSignature = false;
let dsIdKhayChoKyNhan = [];

// Cấu hình máy hấp & máy rửa (Belimed WD250)
const cauHinhMayHap = { "Hấp hơi nước": ["A1", "A2", "A3", "A4"], "Hấp H2O2 (Plasma)": ["P1", "P2"], "Khử khuẩn EO": ["EO1", "EO2"] };
const cauHinhMayRua = {
    "Máy rửa khử khuẩn tự động": ["Belimed WD250"],
    "Máy rửa sóng siêu âm": ["US1"],
    "Bồn rửa thủ công": ["MAN1"]
};
const danhMucHoaChatRua = [
    { ten: "Cidezyme (Enzym phân hủy protein)", lieuLuong: "8 ml/L" },
    { ten: "Sekusept Active (Khử khuẩn mức độ cao)", lieuLuong: "10 ml/L" },
    { ten: "Smeg (Nước rửa trung tính chuyên dụng)", lieuLuong: "5 ml/L" },
    { ten: "Aniosyme DD1 (Làm sạch & khử trùng sơ bộ)", lieuLuong: "5 ml/L" }
];

function getTodayDateStr() { 
    const d = new Date(); 
    return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`; 
}

function playSound(type) {
    try {
        const AudioContext = window.AudioContext || window.webkitAudioContext;
        if (!AudioContext) return;
        const ctx = new AudioContext();
        const osc = ctx.createOscillator();
        const gain = ctx.createGain();
        osc.connect(gain); gain.connect(ctx.destination);
        if (type === 'success') {
            osc.type = 'sine'; osc.frequency.setValueAtTime(800, ctx.currentTime);
            gain.gain.setValueAtTime(0.3, ctx.currentTime);
            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.15);
            osc.start(); osc.stop(ctx.currentTime + 0.15);
        } else {
            osc.type = 'sawtooth'; osc.frequency.setValueAtTime(250, ctx.currentTime);
            gain.gain.setValueAtTime(0.3, ctx.currentTime);
            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.4);
            osc.start(); osc.stop(ctx.currentTime + 0.4);
        }
    } catch(e) {}
}

// =========================================================================
// 2. REALTIME LISTENER (FIRESTORE SYNC)
// =========================================================================
db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").onSnapshot(doc => { 
    try {
        if (doc.exists) { 
            let res = doc.data(); 
            danhSachKhoa = res.danhSachKhoa || []; 
            danhSachKtvCssd = res.danhSachKtvCssd || []; 
            databaseExcel = res.databaseExcel || []; 
            if(res.thongTinMatKhauAdmin) thongTinMatKhauAdmin = Object.assign(thongTinMatKhauAdmin, res.thongTinMatKhauAdmin); 
            if(res.cauHinhGiaoDien) cauHinhGiaoDien = Object.assign(cauHinhGiaoDien, res.cauHinhGiaoDien);
        } 
        initSelects();
        if(activeTab === 'khoaphong') loadBoDungCuTheoKhoa(); 
        renderAdminInterface(); 
        if(currentRole) apDungPhanQuyenGiaoDien(currentRole);
        taiDanhMucLinhKienChuan();
        callRender();
    } catch(err) { console.error("Lỗi đồng bộ danh mục:", err); }
});

db.collection("phieuGiaoNhan").orderBy("id", "desc").limit(1000).onSnapshot(snap => { 
    listGiaoDich = []; 
    snap.forEach(doc => { let d = doc.data(); d.firestoreId = doc.id; listGiaoDich.push(d); }); 
    callRender();
});

function callRender() { clearTimeout(renderTimeout); renderTimeout = setTimeout(() => { renderTheoTabHienTai(); }, 100); }

// =========================================================================
// 3. XỬ LÝ ĐĂNG NHẬP & PHÂN QUYỀN HỆ THỐNG
// =========================================================================
function toggleLoginFields() {
    const r = document.getElementById("login_role").value;
    document.getElementById("field_khoa").classList.toggle("hidden", r !== "KHOA");
    document.getElementById("field_nhanvien_cssd").classList.toggle("hidden", r !== "CSSD");
}

function checkLogin() {
    const role = document.getElementById("login_role").value; 
    const pass = document.getElementById("login_pass").value; 
    let pA = thongTinMatKhauAdmin.adminPIN || "admin2026"; 
    let pC = thongTinMatKhauAdmin.cssdPIN || "cssd2026"; 
    let pG = thongTinMatKhauAdmin.guestPIN || "guest2026"; 
     
    if (role === "ADMIN" && pass === pA) { 
        currentRole = "ADMIN"; loginUserCode = "ADMIN"; document.getElementById("nav_user_info").innerText = "ADMINISTRATOR";
        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');
        apDungPhanQuyenGiaoDien("ADMIN");
        switchTab('lichsuluanchuyen');
    } else if (role === "KHOA") { 
        const khoaSelect = document.getElementById("login_khoa").value; 
        let found = danhSachKhoa.find(x => x.ten === khoaSelect); 
        if (pass === (found ? found.pin : "123")) { 
            currentRole = "KHOA"; loginUserCode = khoaSelect; document.getElementById("nav_user_info").innerText = khoaSelect; 
            if (document.getElementById("khoa_selKhoa")) {
                document.getElementById("khoa_selKhoa").value = khoaSelect;
                document.getElementById("khoa_selKhoa").disabled = true;
            }
            document.body.classList.remove('guest-mode');
            apDungPhanQuyenGiaoDien("KHOA");
            switchTab('khoaphong');
        } else { return showToast("Sai mã PIN Khoa!", "error"); } 
    } else if (role === "CSSD") {
        const nvCode = document.getElementById("login_nv_cssd").value;
        if(!nvCode && pass === pC) { currentRole = "CSSD"; loginUserCode = "CSSD_CHUNG"; document.getElementById("nav_user_info").innerText = "CSSD GENERAL"; } 
        else if (nvCode) {
            let nv = danhSachKtvCssd.find(x => x.code === nvCode);
            if (nv && pass === nv.pin) { currentRole = "CSSD"; loginUserCode = nv.ten; document.getElementById("nav_user_info").innerText = `${nvCode} - ${nv.ten}`; }
            else { return showToast("Sai mã PIN của nhân viên này!", "error"); }
        } else { return showToast("Vui lòng chọn Nhân viên hoặc nhập PIN Backup!", "error"); }
        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');
        apDungPhanQuyenGiaoDien("CSSD");
        let tabsDuocPhep = cauHinhGiaoDien["CSSD"] || [];
        if(tabsDuocPhep.includes('thugom')) switchTab('thugom');
        else if (tabsDuocPhep.length > 0) switchTab(tabsDuocPhep[0]);
    } else if (role === "GUEST") {
        if(pass === pG) {
            currentRole = "GUEST"; loginUserCode = "GUEST"; document.getElementById("nav_user_info").innerText = "KHÁCH THAM QUAN";
            document.body.classList.add('guest-mode'); 
            apDungPhanQuyenGiaoDien("GUEST");
            switchTab('quanlykho');
        } else { return showToast("Sai mã PIN Khách tham quan!", "error"); }
    } else { return showToast("Sai thông tin đăng nhập!", "error"); }
    document.getElementById("login-screen").classList.add("hidden"); document.getElementById("main-app").classList.remove("hidden");
}

function anTatCaHeadersVaMenus() {
    document.getElementById('header-lamsang')?.classList.add('hidden');
    document.getElementById('header-vanhanh')?.classList.add('hidden');
    document.getElementById('header-dulieu')?.classList.add('hidden');
    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => {
        document.getElementById('menu-' + x)?.classList.add('hidden');
    });
}

function apDungPhanQuyenGiaoDien(role) {
    anTatCaHeadersVaMenus();
    if (role === "ADMIN") {
        ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(tab => {
            document.getElementById('menu-' + tab)?.classList.remove('hidden');
        });
        document.getElementById('header-lamsang')?.classList.remove('hidden');
        document.getElementById('header-vanhanh')?.classList.remove('hidden');
        document.getElementById('header-dulieu')?.classList.remove('hidden');
        return;
    }
    let tabsDuocPhep = cauHinhGiaoDien[role] || [];
    tabsDuocPhep.forEach(tab => { document.getElementById('menu-' + tab)?.classList.remove('hidden'); });
    if (tabsDuocPhep.includes('khoaphong')) document.getElementById('header-lamsang')?.classList.remove('hidden');
    if (['thugom','mayrua','donggoi','mayhap','khovokhuan'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-vanhanh')?.classList.remove('hidden');
    if (['quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-dulieu')?.classList.remove('hidden');
}

function switchTab(t) { 
    if (currentRole !== "ADMIN") {
        let tabsDuocPhep = cauHinhGiaoDien[currentRole] || [];
        if(!tabsDuocPhep.includes(t)) { return showToast("Tài khoản không có quyền truy cập!", "error"); }
    }
    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => { 
        document.getElementById('tab-'+x)?.classList.add('hidden'); 
        document.getElementById('menu-'+x)?.classList.remove('sidebar-item-active'); 
    }); 
    document.getElementById('tab-'+t)?.classList.remove('hidden'); 
    document.getElementById('menu-'+t)?.classList.add('sidebar-item-active'); 
    activeTab = t; 
    if (t === 'mayrua') { setTimeout(() => { capNhatDanhSachMaMayRua(); }, 50); }
    if (t === 'mayhap') { setTimeout(() => { tuDongTaoMaLoMeHap(); }, 50); }
    callRender(); 
}

// =========================================================================
// 4. TRẠM 1: CỔNG LÂM SÀNG & HÀNH ĐỘNG ĐỘC LẬP (TRẢ ĐỒ BẨN / YÊU CẦU ĐỒ SẠCH)
// =========================================================================
function loadBoDungCuTheoKhoa() { 
    let k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 
    let list = document.getElementById("listBoDungCu"); 
    if(!list) return; list.innerHTML = ""; 
    let f = danhSachKhoa.find(x => (x.ten || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase()); 
    if (f && f.danhSachBo) {
        let htmlOptions = "";
        f.danhSachBo.forEach(x => {
            if (x) {
                let chuoiX = String(x);
                let tenBoThuanTuy = chuoiX.includes(" [ID:") ? chuoiX.split(" [ID:")[0] : chuoiX;
                htmlOptions += `<option value="${chuoiX}"><option value="${tenBoThuanTuy}">`;
            }
        });
        list.innerHTML = htmlOptions; 
    }
}

function themVaoGio() { 
    let val = document.getElementById("khoa_inpMaBo").value.trim().toUpperCase(); if(!val) return; 
    if (gioHangTam.some(x => x.maMacDinh === val)) { document.getElementById("khoa_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách chờ!", "error"); } 
    let tenGoc = val.includes("[ID:") ? val.split(" [ID:")[0] : val; 
    gioHangTam.push({bo: tenGoc, maMacDinh: val, slYeuCau: 1}); 
    document.getElementById("khoa_inpMaBo").value = ""; 
    renderGioHang(); 
}

function renderGioHang() { 
    let khuVuc = document.getElementById("khuVucGioHang"); if(khuVuc) khuVuc.classList.toggle("hidden", gioHangTam.length===0); 
    let bHang = document.getElementById("bangGioHang");
    if(bHang) {
        bHang.innerHTML = gioHangTam.map(i => `<tr><td class="p-2.5 font-bold text-sky-700 text-[11px]">${i.bo}</td></tr>`).join(''); 
    }
    let badge = document.getElementById("badgeGioHang");
    if(badge) badge.innerText = `${gioHangTam.length} món`;
}

function khoaGuiPhieuTraBatches() { 
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 
    if(!k) return showToast("Vui lòng chọn Khoa trước!"); 
    if(gioHangTam.length === 0) return showToast("Vui lòng quét hoặc chọn mâm bẩn muốn trả trước!", "error"); 
     
    let p=[]; 
    gioHangTam.forEach((i, idx) => {
        p.push(
            db.collection("phieuGiaoNhan").add({ 
                id: Date.now() + idx, 
                ngayTao: getTodayDateStr(), 
                time: new Date().toLocaleTimeString('vi-VN'), 
                khoa: k, 
                bo: i.bo, 
                maMacDinh: i.maMacDinh, 
                slYeuCau: 1, 
                slThucTe: 1, 
                status: "CHO_THU" 
            })
        );
    }); 
     
    Promise.all(p).then(() => { 
        clearGioHang(); 
        playSound('success');
        showToast("Đã gửi lệnh báo trả đồ bẩn về CSSD!", "success"); 
        callRender(); 
    }); 
}

function khoaGuiYeuCauCapPhat() {
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;
    if(!k) return showToast("Vui lòng chọn Khoa/Phòng gửi yêu cầu!", "error");
    
    const tenBoChon = document.getElementById("khoa_inpMaBo")?.value.trim().toUpperCase();
    if(!tenBoChon) return showToast("Vui lòng chọn hoặc nhập tên mâm cần cấp mới!", "error");
    
    let tenMâmSạch = tenBoChon.split(" [ID:")[0];
    
    db.collection("phieuGiaoNhan").add({
        id: Date.now(),
        ngayTao: getTodayDateStr(),
        time: new Date().toLocaleTimeString('vi-VN'),
        khoa: k,
        bo: tenMâmSạch,
        maMacDinh: "CHO_CAP_PHAT", 
        slYeuCau: 1,
        status: "CHO_CAP_PHAT", 
        nvYeuCau: loginUserCode || "Điều dưỡng khoa"
    }).then(() => {
        playSound('success');
        showToast(`Đã gửi yêu cầu cấp phát mâm ${tenMâmSạch} thành công!`, "success");
        if(document.getElementById("khoa_inpMaBo")) document.getElementById("khoa_inpMaBo").value = "";
        callRender();
    });
}

// =========================================================================
// 5. TRẠM 2: XE THU GOM & KIỂM ĐẾM CHI TIẾT LINH KIỆN
// =========================================================================
function moPopupKiemDem(id) { 
    let item = listGiaoDich.find(x => x.firestoreId === id); 
    if(!item) return showToast("Không tìm thấy thông tin dòng chỉ định!", "error");

    currentKiemDemData = {
        id: id,
        tenBoDungCu: item.bo ? String(item.bo).split(" [ID:")[0] : "Chưa rõ mâm",
        khoaYeuCau: item.khoa || "Chưa rõ khoa",
        ghiChuLamSang: item.ghiChu || ""
    }; 

    document.getElementById('popBo').innerText = currentKiemDemData.tenBoDungCu;
    document.getElementById('popKhoa').innerText = currentKiemDemData.khoaYeuCau;
    document.getElementById('popGhiChu').value = currentKiemDemData.ghiChuLamSang;
    document.getElementById('popGhiChu').classList.remove('border-rose-500', 'ring-2', 'ring-rose-200');

    let itemsInBo = databaseExcel.filter(x => { 
        let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; 
        return String(boName).trim().toUpperCase() === String(currentKiemDemData.tenBoDungCu).trim().toUpperCase(); 
    });

    let checklistSoBo = [];
    if(itemsInBo.length > 0) {
        checklistSoBo = itemsInBo.map(ct => {
            let tenDc = ct['Tên TS chuẩn'] || ct['Tên Dụng Cụ Chi Tiết'] || ct['Tên dụng cụ chi tiết'] || ct['Tên dụng cụ'] || ct['Chi tiết'] || ct['Dụng cụ'] || ct['Tên Chi Tiết'] || ct['NAME'] || "Dụng cụ";
             
            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {
                tenDc = "Dụng cụ chuẩn mâm";
            }
             
            let sl = parseInt(ct['Số lượng'] || ct['SL'] || ct['Số Lượng'] || 1) || 0;
            return { ten: tenDc, slChuan: sl, slThuc: sl, tinhTrang: "ĐỦ" };
        }).filter(line => line.slChuan > 0); 
    } else {
        checklistSoBo = [{ ten: "Dụng cụ nguyên bộ (Chưa phân rã cấu hình)", slChuan: 1, slThuc: 1, tinhTrang: "ĐỦ" }];
    }

    currentKiemDemData.linhKienKiemDem = checklistSoBo;
    renderChecklistLinhKien();
    document.getElementById('popupKiemDem').classList.remove('hidden'); 
}

function renderChecklistLinhKien() {
    const container = document.getElementById('popKiemDemChecklist');
    if(!container) return; container.innerHTML = ""; 

    currentKiemDemData.linhKienKiemDem.forEach((item, index) => {
        let bgBadge = "bg-emerald-50 text-emerald-700 border-emerald-200";
        if (item.tinhTrang === "THIẾU") bgBadge = "bg-rose-50 text-rose-700 border-rose-200 animate-pulse";
        if (item.tinhTrang === "HỎNG") bgBadge = "bg-amber-50 text-amber-700 border-amber-200";

        const row = document.createElement('div');
        row.className = "flex items-center justify-between py-3 px-2 transition-all hover:bg-slate-50";
        row.innerHTML = `
            <div class="flex-1 pr-2">
                <p class="text-xs font-bold text-slate-700">${item.ten}</p>
                <p class="text-[10px] text-slate-400 font-medium mt-0.5">Cơ số chuẩn: <span class="font-bold text-slate-600">${item.slChuan}</span></p>
            </div>
            <div class="flex items-center gap-3 mr-4">
                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, -1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">-</button>
                <span class="w-6 text-center text-xs font-black text-slate-800">${item.slThuc}</span>
                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, 1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">+</button>
            </div>
            <div>
                <select onchange="thayDoiTinhTrangLinhKien(${index}, this.value)" class="text-[11px] font-black border rounded-lg px-2 py-1.5 shadow-sm transition-all ${bgBadge}">
                    <option value="ĐỦ" ${item.tinhTrang === 'ĐỦ' ? 'selected' : ''}>🟢 Đủ đồ</option>
                    <option value="THIẾU" ${item.tinhTrang === 'THIẾU' ? 'selected' : ''}>🔴 Thiếu đồ</option>
                    <option value="HỎNG" ${item.tinhTrang === 'HỎNG' ? 'selected' : ''}>🟡 Hỏng đồ</option>
                </select>
            </div>
        `;
        container.appendChild(row);
    });
}

function thayDoiSoLuongLinhKien(index, thayDoi) {
    const item = currentKiemDemData.linhKienKiemDem[index];
    const targetSl = item.slThuc + thayDoi;
    if (targetSl >= 0 && targetSl <= item.slChuan * 2) {
        item.slThuc = targetSl;
        if (item.slThuc < item.slChuan) { item.tinhTrang = "THIẾU"; } 
        else if (item.slThuc === item.slChuan) { item.tinhTrang = "ĐỦ"; }
        renderChecklistLinhKien();
    }
}

function thayDoiTinhTrangLinhKien(index, value) {
    currentKiemDemData.linhKienKiemDem[index].tinhTrang = value;
    if (value === "THIẾU" && currentKiemDemData.linhKienKiemDem[index].slThuc === currentKiemDemData.linhKienKiemDem[index].slChuan) {
        currentKiemDemData.linhKienKiemDem[index].slThuc = Math.max(0, currentKiemDemData.linhKienKiemDem[index].slChuan - 1);
    } else if (value === "ĐỦ") {
        currentKiemDemData.linhKienKiemDem[index].slThuc = currentKiemDemData.linhKienKiemDem[index].slChuan;
    }
    renderChecklistLinhKien();
}

function closePopupKiemDem() { document.getElementById("popupKiemDem").classList.add("hidden"); currentKiemDemData = null; }

async function saveKiemDem() { 
    if(!currentKiemDemData) return; 
    const ghiChuValue = document.getElementById("popGhiChu").value.trim();
    let coSuCoChenhLech = false;
    currentKiemDemData.linhKienKiemDem.forEach(item => {
        let thuc = Number(item.slThuc) || 0;
        let chuan = Number(item.slChuan) || 0;
        if (item.tinhTrang === "THIẾU" || item.tinhTrang === "HỎNG" || thuc < chuan) { coSuCoChenhLech = true; }
    });

    if (coSuCoChenhLech && ghiChuValue === "") {
        playSound('error');
        alert("⚠️ PHÁT HIỆN SỰ CỐ CHÊNH LỆCH LINH KIỆN!\nAnh/Chị bắt buộc phải nhập lý do chênh lệch hoặc số biên bản sự cố trước khi 'Chốt và Chuyển Rửa'.");
        document.getElementById('popGhiChu').focus();
        document.getElementById('popGhiChu').classList.add('border-rose-500', 'ring-2', 'ring-rose-200');
        return;
    }

    try {
        await db.collection("phieuGiaoNhan").doc(currentKiemDemData.id).update({ 
            status: "DANG_RUA", ghiChu: ghiChuValue,
            ktvThuGom: loginUserCode || "CSSD_CHUNG",
            thoiGianDoiSoat: new Date().toLocaleTimeString('vi-VN'),
            coSuCoChenhLech: coSuCoChenhLech,
            chiTietChecklistLinhKien: currentKiemDemData.linhKienKiemDem
        });
        showToast("Đã đối soát xong! Mâm đồ đã chuyển sang Trạm Rửa.", "success"); 
        closePopupKiemDem(); callRender(); 
    } catch(err) { showToast("Lỗi kết nối Firebase nội bộ!", "error"); }
}

function clearGioHang() { gioHangTam = []; renderGioHang(); }

// =========================================================================
// 6. MODULE: QUẢN LÝ MẺ RỬA KHỬ KHUẨN (BELIMED WD250)
// =========================================================================
function toggleSelectAllRua() { let checked = document.getElementById('selectAllRua').checked; document.querySelectorAll('.rua-checkbox').forEach(cb => cb.checked = checked); }
function toggleSelectAllNghiemThuRua() { let checked = document.getElementById('selectAllNghiemThuRua').checked; document.querySelectorAll('.nghiemthurua-checkbox').forEach(cb => cb.checked = checked); }

function capNhatDanhSachMaMayRua() {
    const loaiRua = document.getElementById("rua_loaiRua")?.value;
    const selectMay = document.getElementById("rua_maySo");
    if (!selectMay || !loaiRua) return;
    selectMay.innerHTML = "";
    if (cauHinhMayRua[loaiRua]) {
        cauHinhMayRua[loaiRua].forEach(may => {
            selectMay.innerHTML += `<option value="${may}">${may}</option>`;
        });
    }
    tuDongTaoMaLoMeRua();
}

function tuDongTaoMaLoMeRua() {
    const maMay = document.getElementById("rua_maySo")?.value || "Belimed WD250";
    const maMayVietTat = (maMay === "Belimed WD250") ? "BL" : maMay;
    const now = new Date();
    const yy = String(now.getFullYear()).slice(-2);
    const mm = String(now.getMonth() + 1).padStart(2, '0');
    const dd = String(now.getDate()).padStart(2, '0');
    const ngayChuoi = `${yy}${mm}${dd}`;

    let cacMeTrongNgay = listGiaoDich.filter(x => x.rua_batchCode && x.rua_batchCode.startsWith(maMayVietTat + ngayChuoi));
    let soMeMax = 0;
    cacMeTrongNgay.forEach(x => {
        let phanDuoi = x.rua_batchCode.split("_")[1];
        if (phanDuoi) {
            let num = parseInt(phanDuoi);
            if (num > soMeMax) soMeMax = num;
        }
    });
    let meTiepTheo = soMeMax + 1;
    const chuoiMe = String(meTiepTheo).padStart(2, '0');
    const batchIdHoanChinh = `${maMayVietTat}${ngayChuoi}_${chuoiMe}`;

    if (document.getElementById("rua_meSo")) document.getElementById("rua_meSo").value = chuoiMe;
    if (document.getElementById("rua_batchId")) document.getElementById("rua_batchId").value = batchIdHoanChinh;
}

function xacNhanMeRua() {
    let checkboxes = document.querySelectorAll('.rua-checkbox:checked');
    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm dụng cụ bẩn để xếp vào máy!", "error");
    
    let loaiRua = document.getElementById("rua_loaiRua").value;
    let maMay = document.getElementById("rua_maySo").value;
    let batchCode = document.getElementById("rua_batchId").value;
    let chuKy = document.getElementById("rua_chuKy").value;
    let hoaChat = document.getElementById("rua_hoaChat").value;
    let lieuLuong = document.getElementById("rua_lieuLuong").value || "Theo chuẩn";
    const ngayHomNay = getTodayDateStr();

    let p = [];
    checkboxes.forEach(cb => {
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "TRONG_BUONG_RUA",
                rua_batchCode: batchCode,
                rua_ngayRua: ngayHomNay,
                rua_mayRua: maMay,
                rua_chuKy: chuKy,
                rua_hoaChat: hoaChat,
                rua_lieuLuong: lieuLuong,
                rua_timeBatDau: new Date().toLocaleTimeString('vi-VN')
            })
        );
    });
    Promise.all(p).then(() => {
        playSound('success');
        showToast(`Bắt đầu chạy mẻ máy rửa: ${batchCode}`, "success");
        tuDongTaoMaLoMeRua();
        callRender();
    });
}

function duyetSachMeRuaHangLoat() {
    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');
    if(checkboxes.length === 0) return showToast("Chọn mâm đã rửa sạch để nghiệm thu!", "error");
    
    let ketQuaTest = document.getElementById("rua_testDoSach").value;
    let p = [];
    checkboxes.forEach(cb => {
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "CHO_DONG_GOI",
                rua_ketQuaTestSach: ketQuaTest,
                rua_timeKetThuc: new Date().toLocaleTimeString('vi-VN')
            })
        );
    });
    Promise.all(p).then(() => {
        playSound('success');
        showToast("Xác nhận nghiệm thu đạt chất lượng làm sạch!", "success");
        callRender();
    });
}

function tuChoiMeRuaHangLoat() {
    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');
    if(checkboxes.length === 0) return showToast("Chọn mâm không đạt!", "error");
    
    let p = [];
    checkboxes.forEach(cb => {
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "DANG_RUA",
                rua_batchCode: firebase.firestore.FieldValue.delete(),
                ghiChu: "Làm sạch chưa đạt, yêu cầu rửa lại."
            })
        );
    });
    Promise.all(p).then(() => {
        playSound('error');
        showToast("Đã trả các mâm không đạt về hàng đợi chờ rửa lại!", "success");
        callRender();
    });
}

// =========================================================================
// 7. TRẠM 4: LÀM SẠCH VÀ ĐÓNG GÓI
// =========================================================================
function moPopupDongGoi(id) { idDangDongGoi = id; let item = listGiaoDich.find(x => x.firestoreId === id); if(item) { document.getElementById("popDG_Bo").innerText = item.bo; tinhHanSuDung(); document.getElementById("popupDongGoi").classList.remove("hidden"); } }
function closePopupDongGoi() { document.getElementById("popupDongGoi").classList.add("hidden"); }
function tinhHanSuDung() { let val = document.getElementById("popDG_Loai").value.split("|"); let days = parseInt(val[1]); let dateHSD = new Date(); dateHSD.setDate(dateHSD.getDate() + days); let p = document.getElementById("popDG_Han"); p.innerText = dateHSD.toLocaleDateString('vi-VN'); p.dataset.dateDB = dateHSD.toISOString().split('T')[0]; }
function chotDongGoi() { if(!idDangDongGoi) return; let chatLieuTen = document.getElementById("popDG_Loai").value.split("|")[0]; db.collection("phieuGiaoNhan").doc(idDangDongGoi).update({ status: "CHO_HAP", chatLieu: chatLieuTen, hsd: document.getElementById("popDG_Han").dataset.dateDB }).then(() => { showToast("Đã đóng gói, chuyển chờ hấp!", "success"); closePopupDongGoi(); callRender(); }); }

// =========================================================================
// 8. TRẠM 5: VẬN HÀNH MẺ HẤP & DUYỆT NHẬP KHO VÔ KHUẨN
// =========================================================================
function toggleSelectAllHap() { let checked = document.getElementById('selectAllHap').checked; document.querySelectorAll('.hap-checkbox').forEach(cb => cb.checked = checked); }
function capNhatDanhSachMaMay() { const loaiHap = document.getElementById("hap_loaiHap")?.value; const selectMay = document.getElementById("hap_maySo"); if (!selectMay || !loaiHap) return; selectMay.innerHTML = ""; if (cauHinhMayHap[loaiHap]) { cauHinhMayHap[loaiHap].forEach(may => { selectMay.innerHTML += `<option value="${may}">${may}</option>`; }); } tuDongTaoMaLoMeHap(); }

function tuDongTaoMaLoMeHap() {
    const maMay = document.getElementById("hap_maySo")?.value || "A1"; const now = new Date(); const yy = String(now.getFullYear()).slice(-2); const mm = String(now.getMonth() + 1).padStart(2, '0'); const dd = String(now.getDate()).padStart(2, '0'); const ngayChuoi = `${yy}${mm}${dd}`;
    let cacMeTrongNgay = listGiaoDich.filter(x => x.batchCode && x.batchCode.startsWith(maMay + ngayChuoi)); let soMeMax = 0;
    cacMeTrongNgay.forEach(x => { let phanDuoi = x.batchCode.split("_")[1]; if (phanDuoi) { let num = parseInt(phanDuoi); if (num > soMeMax) soMeMax = num; } });
    let meTiepTheo = soMeMax + 1; if (meTiepTheo > 99) meTiepTheo = 1; const chuoiMe = String(meTiepTheo).padStart(2, '0'); const soLanDone = `${ngayChuoi}_${chuoiMe}`; const batchIdHoanChinh = `${maMay}${soLanDone}`;
    if (document.getElementById("hap_meSo")) document.getElementById("hap_meSo").value = chuoiMe;
    if (document.getElementById("hap_batchId")) document.getElementById("hap_batchId").value = batchIdHoanChinh;
}

function xacNhanMeHap() { 
    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); 
    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 
    
    let loaiHap = document.getElementById("hap_loaiHap").value; 
    let maMay = document.getElementById("hap_maySo").value; 
    let batchCode = document.getElementById("hap_batchId").value; 
    let chuKyNhiet = document.getElementById("hap_nhietDo").value; 
    let apSuat = document.getElementById("hap_apSuat").value || "N/A"; 
    let coKemBI = document.getElementById("hap_hasBI") ? document.getElementById("hap_hasBI").checked : false;
    const ngayHomNay = getTodayDateStr(); 
    let bayGio = new Date(); 
    let thoiGianDuKienXong = new Date(bayGio.getTime() + 45 * 60 * 1000);

    let p = []; 
    checkboxes.forEach(cb => { 
        if(cb.id === 'selectAllHap') return; 
        
        let thongTinLo = { 
            loaiHap: loaiHap, 
            maMay: maMay, 
            chuKyNhiet: chuKyNhiet, 
            apSuat: apSuat, 
            thoiGianBatDau: bayGio.toLocaleTimeString('vi-VN'), 
            giamSatChatLuong: { 
                chiThiHoaHoc: "ĐẠT", 
                laMeTestSinhHocGoc: coKemBI, 
                ketQuaSinhHoc: coKemBI ? "ĐANG CHỜ MÁY Ủ ĐỌC BI (20 PHÚT)" : "KẾ THỪA ĐẦU NGÀY" 
            } 
        };
        
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({ 
                status: "DANG_HAP", 
                batchCode: batchCode, 
                ngayHapRealtime: ngayHomNay, 
                hasBI: coKemBI, 
                thoiGianKetThucChuTrinh: thoiGianDuKienXong.toISOString(), 
                thongTinLoHap: thongTinLo 
            })
        ); 
    }); 

    Promise.all(p).then(() => { 
        playSound('success');
        showToast(`Kích hoạt lò thành công! Lô: ${batchCode}`, "success"); 
        if(document.getElementById("hap_hasBI")) document.getElementById("hap_hasBI").checked = false; 
        duLieuAnhBiTamBase64 = ""; 
        const fileInp = document.getElementById("input_anhBI"); 
        if(fileInp) fileInp.value = ""; 
        tuDongTaoMaLoMeHap(); 
        callRender(); 
    }).catch(err => {
        showToast("Lỗi khi cập nhật mẻ hấp: " + err.message, "error");
    }); 
}

function toggleSelectAllNghiemThu() { let checked = document.getElementById('selectAllNghiemThu').checked; document.querySelectorAll('.nghiemthu-checkbox').forEach(cb => cb.checked = checked); }

function kiemTraQuyenDuyetMeHap() {
    const nutDuyetDat = document.querySelector("button[onclick='nhapKhoHangLoat()']"); if (!nutDuyetDat) return;
    let checkboxesNghiemThu = document.querySelectorAll('.nghiemthu-checkbox:checked'); let phaiChoKetQuaBI = false;
    checkboxesNghiemThu.forEach(cb => { if (cb.getAttribute('data-hasbi') === 'true') phaiChoKetQuaBI = true; });

    if (phaiChoKetQuaBI && !duLieuAnhBiTamBase64) {
        nutDuyetDat.disabled = true;
        nutDuyetDat.className = "w-full bg-slate-300 text-slate-500 font-black py-2.5 rounded text-xs shadow-md cursor-not-allowed flex items-center justify-center gap-2";
        nutDuyetDat.innerHTML = `<i class="fa-solid fa-lock text-slate-400"></i> CHỜ MINH CHỨNG BI (MÁY Ủ ĐỌC 20 PHÚT)`;
    } else {
        nutDuyetDat.disabled = false;
        nutDuyetDat.className = "w-full bg-emerald-600 hover:bg-emerald-700 text-white font-black py-2.5 rounded text-xs shadow-md flex items-center justify-center gap-2 transition-all cursor-pointer";
        nutDuyetDat.innerHTML = `<i class="fa-solid fa-check-double mr-1"></i> DUYỆT ĐẠT & CHO VÀO KHO LƯU TRỮ CHUNG`;
    }
}

function nhapKhoHangLoat() { 
    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!"); 
    const bayGio = new Date(); let p = []; 
     
    checkboxes.forEach(cb => { 
        if(cb.id === 'selectAllNghiemThu') return; 
        let itemData = listGiaoDich.find(x => x.firestoreId === cb.value); if(!itemData) return;
        let coKemBI = itemData.hasBI === true;
        let thoiGianHapXong = itemData.thoiGianKetThucChuTrinh ? new Date(itemData.thoiGianKetThucChuTrinh) : new Date(bayGio.getTime() - 45 * 60 * 1000);
        let soPhutThucTe = Math.floor((bayGio - thoiGianHapXong) / (1000 * 60)); 
        let soPhutTinhKPI = coKemBI ? Math.max(0, soPhutThucTe - 20) : soPhutThucTe;
        let trangThaiKPI = (soPhutTinhKPI > 30) ? "KHÔNG ĐẠT" : "ĐẠT";

        let bgGiamSat = itemData.thongTinLoHap?.giamSatChatLuong || {};
        let updateGiamSat = {
            ...bgGiamSat,
            ketQuaSinhHoc: coKemBI ? "ÂM TÍNH (ĐẠT)" : "KẾ THỪA ĐẦU NGÀY",
            minhChungAnhBase64: coKemBI ? duLieuAnhBiTamBase64 : (bgGiamSat.minhChungAnhBase64 || "")
        };

        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "CHO_XUAT", thoiGianKetLuan: bayGio.toISOString(), thoiGianDocTestBiPhut: soPhutThucTe, 
                thoiGianTinhKpiPhut: soPhutTinhKPI, ketQuaGiamSatKpi: trangThaiKPI, "thongTinLoHap.giamSatChatLuong": updateGiamSat
            })
        ); 
    }); 
     
    Promise.all(p).then(() => { 
        playSound('success');
        showToast("Đã duyệt mâm đạt chuẩn Vô Khuẩn!", "success"); 
        duLieuAnhBiTamBase64 = ""; const fileInp = document.getElementById("input_anhBI"); if(fileInp) fileInp.value = ""; 
        callRender(); 
    }); 
}

function tuChoiHapHangLoat() { 
    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 
    let p = []; 
    checkboxes.forEach(cb => { if(cb.id !== 'selectAllNghiemThu') p.push(db.collection("phieuGiaoNhan").doc(cb.value).update({status: "DANG_RUA", ghiChu: "Không đạt hấp, trả về rửa lại"})); }); 
    Promise.all(p).then(() => { playSound('error'); showToast("Đã trả các mâm không đạt về Trạm làm sạch!", "success"); callRender(); }); 
}

// =========================================================================
// 9. TRẠM 6: KHO VÔ KHUẨN & PHỐI HỢP CẤP PHÁT ĐIỀU PHỐI XOAY VÒNG (FIFO)
// =========================================================================
function xuatKhoXoayVong(maTrucTiep) { 
    const k = document.getElementById("xuat_selKhoa") ? document.getElementById("xuat_selKhoa").value : ""; 
    let ma = maTrucTiep || (document.getElementById("xuat_inpMaBo") ? document.getElementById("xuat_inpMaBo").value.trim().toUpperCase() : ""); 
    
    if(!k) { playSound('error'); return showToast("Vui lòng Chọn Khoa nhận trước khi xuất!", "error"); } 
    if(!ma) return;
    if(gioHangXuatKho.some(x => x.maMacDinh === ma)) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách xuất!", "error"); }
    
    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && x.status === "CHO_XUAT"); 
    if(!khayThucTe) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast(`Mã ID ${ma} không sẵn sàng ở Kho Vô Khuẩn.`, "error"); } 
    
    gioHangXuatKho.push(khayThucTe); playSound('success'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; renderGioHangXuat();
}

function renderGioHangXuat() {
    let listContainer = document.getElementById("danhSachXuatTam");
    if (!listContainer) {
        let bangKho = document.getElementById("bangKhoVoKhuan");
        if (bangKho && bangKho.closest('table')) {
            let wrapper = document.createElement("div"); wrapper.id = "danhSachXuatTam";
            wrapper.className = "mb-4 bg-sky-50 border border-sky-200 rounded p-3 hidden shadow-inner";
            bangKho.closest('table').parentNode.insertBefore(wrapper, bangKho.closest('table')); listContainer = wrapper;
        }
    }
    if(listContainer) {
        if(gioHangXuatKho.length === 0) { listContainer.classList.add("hidden"); } 
        else {
            listContainer.classList.remove("hidden");
            let html = `<div class="flex justify-between items-center mb-2 pb-2 border-b border-sky-200"><h3 class="font-bold text-sky-800 text-[13px]"><i class="fa-solid fa-cart-flatbed mr-2"></i>Đang quét xuất kho: <span class="bg-rose-500 text-white px-2 py-0.5 rounded ml-1">${gioHangXuatKho.length} mâm</span></h3><button onclick="xacNhanXuatKhoHangLoat()" class="bg-emerald-600 text-white px-4 py-1.5 rounded shadow font-black text-xs hover:bg-emerald-700 transition-colors uppercase"><i class="fa-solid fa-check-double mr-1"></i>Chốt Bàn Giao</button></div><div class="flex flex-wrap gap-2">`;
            gioHangXuatKho.forEach((item, index) => { 
                let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "Dụng cụ";
                html += `<div class="bg-white border border-sky-300 px-2 py-1 rounded flex items-center gap-2 shadow-sm animate-pulse"><span class="text-[11px] font-bold text-slate-700">${tenBoGoc}</span><span class="text-[10px] font-mono text-sky-700 bg-sky-100 px-1 rounded font-bold">${item.maMacDinh}</span><i class="fa-solid fa-xmark text-rose-500 cursor-pointer ml-1 text-xs hover:text-rose-700" onclick="xoaKhoiGioXuat(${index})"></i></div>`; 
            });
            html += `</div>`; listContainer.innerHTML = html;
        }
    }
}

function xoaKhoiGioXuat(index) { gioHangXuatKho.splice(index, 1); renderGioHangXuat(); }

function xacNhanXuatKhoHangLoat() {
    const k = document.getElementById("xuat_selKhoa").value; if(!k || gioHangXuatKho.length === 0) return;
    let p = []; 
    gioHangXuatKho.forEach(khay => { 
        p.push(
            db.collection("phieuGiaoNhan").doc(khay.firestoreId).update({ 
                status: "ĐANG_VAN_CHUYEN", khoa: k, ngayHoanTat: getTodayDateStr(), timeHoanTat: new Date().toLocaleTimeString('vi-VN'), nvXuatKho: loginUserCode || "CSSD_CHUNG" 
            })
        ); 
    });
    Promise.all(p).then(() => { 
        playSound('success'); showToast(`Đã xuất kho bàn giao về Khoa ${k}!`, "success"); 
        gioHangXuatKho = []; if (document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; 
        renderGioHangXuat(); callRender(); 
    });
}

function duyetCapPhatTậpTrung(yeuCauId, tenBo, khoaNhan) {
    let khaySẵnCo = listGiaoDich.find(x => x.status === "CHO_XUAT" && x.bo.split(" [ID:")[0].trim().toUpperCase() === tenBo.trim().toUpperCase());
    
    if (!khaySẵnCo) {
        playSound('error');
        return alert(`⚠️ KHÔNG CÓ KHAY SẴN SÀNG TRONG KHO!\nHiện tại trong Kho vô khuẩn CSSD không có khay "${tenBo}" nào sẵn sàng để cấp phát.`);
    }

    if (confirm(`Xác nhận cấp phát khay ${khaySẵnCo.maMacDinh} để đáp ứng yêu cầu của khoa ${khoaNhan}?`)) {
        let bayGio = new Date();
        let batch = db.batch();
        
        let refKhay = db.collection("phieuGiaoNhan").doc(khaySẵnCo.firestoreId);
        batch.update(refKhay, {
            status: "ĐANG_VAN_CHUYEN",
            khoa: khoaNhan,
            ngayHoanTat: getTodayDateStr(),
            timeHoanTat: bayGio.toLocaleTimeString('vi-VN'),
            nvXuatKho: loginUserCode || "CSSD_CHUNG"
        });

        let refYeuCau = db.collection("phieuGiaoNhan").doc(yeuCauId);
        batch.delete(refYeuCau);

        batch.commit().then(() => {
            playSound('success');
            showToast(`Đã cấp phát thành công khay ${khaySẵnCo.maMacDinh} cho khoa ${khoaNhan}!`, "success");
            callRender();
        });
    }
}

// =========================================================================
// XỬ LÝ KÝ CHỮ KÝ ĐIỆN TỬ TRỰC TIẾP TRÊN CANVAS & CẬP NHẬT KHOA
// =========================================================================
function khoiTaoCanvasKyDienTu() {
    canvasSignature = document.getElementById("canvasKyDienTu");
    if (!canvasSignature) return;
    ctxSignature = canvasSignature.getContext("2d");

    const rect = canvasSignature.getBoundingClientRect();
    canvasSignature.width = rect.width || 380;
    canvasSignature.height = rect.height || 160;

    ctxSignature.lineWidth = 2.5;
    ctxSignature.lineCap = "round";
    ctxSignature.strokeStyle = "#0284c7";

    // Chuột
    canvasSignature.onmousedown = (e) => {
        isDrawingSignature = true;
        ctxSignature.beginPath();
        const pos = getCanvasPos(e);
        ctxSignature.moveTo(pos.x, pos.y);
    };
    canvasSignature.onmousemove = (e) => {
        if (!isDrawingSignature) return;
        const pos = getCanvasPos(e);
        ctxSignature.lineTo(pos.x, pos.y);
        ctxSignature.stroke();
    };
    canvasSignature.onmouseup = () => { isDrawingSignature = false; };

    // Cảm ứng Tablet/Phone
    canvasSignature.ontouchstart = (e) => {
        isDrawingSignature = true;
        ctxSignature.beginPath();
        const pos = getCanvasPos(e.touches[0]);
        ctxSignature.moveTo(pos.x, pos.y);
    };
    canvasSignature.ontouchmove = (e) => {
        if (!isDrawingSignature) return;
        e.preventDefault();
        const pos = getCanvasPos(e.touches[0]);
        ctxSignature.lineTo(pos.x, pos.y);
        ctxSignature.stroke();
    };
    canvasSignature.ontouchend = () => { isDrawingSignature = false; };
}

function getCanvasPos(evt) {
    const rect = canvasSignature.getBoundingClientRect();
    return {
        x: evt.clientX - rect.left,
        y: evt.clientY - rect.top
    };
}

function xoaChuKyCanvas() {
    if (ctxSignature && canvasSignature) {
        ctxSignature.clearRect(0, 0, canvasSignature.width, canvasSignature.height);
    }
}

function khoaKyNhanDoSachDienTu() {
    const checkboxes = document.querySelectorAll("#bangChoNhanTaiKhoa input[type='checkbox']:checked");
    dsIdKhayChoKyNhan = [];
    checkboxes.forEach(chk => dsIdKhayChoKyNhan.push(chk.value));

    if (dsIdKhayChoKyNhan.length === 0) {
        playSound('error');
        return showToast("Vui lòng tích chọn ít nhất 1 khay để ký nhận!", "error");
    }

    const inpName = document.getElementById("ky_tenNguoiNhan");
    if (inpName) inpName.value = loginUserCode || "";

    const pop = document.getElementById("popupKyDienTu");
    if (pop) {
        pop.classList.remove("hidden");
        setTimeout(() => {
            khoiTaoCanvasKyDienTu();
            xoaChuKyCanvas();
        }, 100);
    } else {
        // Fallback trường hợp chưa mở popup
        const khoaDangNhap = currentRole === "KHOA" ? loginUserCode : (document.getElementById("khoa_selKhoa") ? document.getElementById("khoa_selKhoa").value : "");
        if (confirm(`Xác nhận nhận ${dsIdKhayChoKyNhan.length} mâm về khoa ${khoaDangNhap}?`)) {
            let p = [];
            dsIdKhayChoKyNhan.forEach(idDoc => {
                p.push(
                    db.collection("phieuGiaoNhan").doc(idDoc).update({
                        status: "HOAN_TAT", nguoiKyNhanKhoa: loginUserCode || "Điều dưỡng khoa", timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'), ngayKhoaNhanThucTe: getTodayDateStr()
                    })
                );
            });
            Promise.all(p).then(() => { playSound('success'); showToast(`Đã tiếp nhận vào tủ khoa thành công!`, "success"); callRender(); });
        }
    }
}

function dongPopupKyDienTu() {
    document.getElementById("popupKyDienTu")?.classList.add("hidden");
    dsIdKhayChoKyNhan = [];
}

function luuXacNhanKyNhan() {
    const tenNguoiNhan = document.getElementById("ky_tenNguoiNhan")?.value.trim();
    if (!tenNguoiNhan) {
        playSound('error');
        return showToast("Vui lòng nhập tên người nhận!", "error");
    }

    const chuoiAnhChuKy = canvasSignature ? canvasSignature.toDataURL("image/png") : "";
    const currentKhoa = currentRole === "KHOA" ? loginUserCode : (document.getElementById("khoa_selKhoa")?.value || "KHOA_LAM_SANG");
    const thoiGianKy = new Date().toLocaleString('vi-VN');

    // 1. Lưu chữ ký gần nhất vào localStorage theo Khoa
    const dataSignature = {
        tenNguoiNhan: tenNguoiNhan,
        chuKyImg: chuoiAnhChuKy,
        timeKy: thoiGianKy
    };
    localStorage.setItem(`signature_${currentKhoa}`, JSON.stringify(dataSignature));

    // 2. Cập nhật trạng thái từng mâm vào Cloud Firestore
    let p = [];
    dsIdKhayChoKyNhan.forEach(idDoc => {
        p.push(
            db.collection("phieuGiaoNhan").doc(idDoc).update({
                status: "HOAN_TAT",
                nguoiKyNhanKhoa: tenNguoiNhan,
                anhChuKyNhanBase64: chuoiAnhChuKy,
                timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'),
                ngayKhoaNhanThucTe: getTodayDateStr()
            })
        );
    });

    Promise.all(p).then(() => {
        playSound('success');
        showToast(`Đã ký nhận thành công ${dsIdKhayChoKyNhan.length} mâm vào tủ khoa!`, "success");
        dongPopupKyDienTu();
        callRender();
    });
}

// =========================================================================
// 10. GHI NHẬN SỬ DỤNG BỘ DỤNG CỤ TRÊN BỆNH NHÂN
// =========================================================================
function moPopupSuDungBoDungCu() {
    gioKhaySuDungTam = []; document.getElementById("sd_bangKhayChon").innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng quét hoặc nhập mã khay...</td></tr>`;
    document.getElementById("sd_ngaySuDung").value = getTodayDateStr(); document.getElementById("sd_nhanChung").value = loginUserCode || "ADMIN";
    if(document.getElementById("sd_khoaBenhNhan")) document.getElementById("sd_khoaBenhNhan").value = (currentRole === "KHOA") ? loginUserCode : "";
    if(document.getElementById("sd_yTaPhongMo")) document.getElementById("sd_yTaPhongMo").value = "";
    if(document.getElementById("sd_yTaVongNgoai")) document.getElementById("sd_yTaVongNgoai").value = "";
    if(document.getElementById("sd_searchBN")) document.getElementById("sd_searchBN").value = "";
    if(document.getElementById("sd_ghiChu")) document.getElementById("sd_ghiChu").value = "";
    if(document.getElementById("sd_maKhayInp")) document.getElementById("sd_maKhayInp").value = "";
    document.getElementById("popupSuDungBoDungCu").classList.remove("hidden");
}

function closePopupSuDung() { document.getElementById("popupSuDungBoDungCu").classList.add("hidden"); }

function scanKhayVaoSuDung() {
    let ma = document.getElementById("sd_maKhayInp").value.trim().toUpperCase(); if(!ma) return;
    document.getElementById("sd_maKhayInp").value = "";
    if(gioKhaySuDungTam.some(x => x.maMacDinh === ma)) { playSound('error'); return showToast("Mã khay này đã được quét!", "error"); }

    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT"));
    if(!khayThucTe) { playSound('error'); return showToast(`Mã ID ${ma} không hợp lệ hoặc chưa vô khuẩn!`, "error"); }

    gioKhaySuDungTam.push(khayThucTe); playSound('success'); renderBangKhaySuDung();
}

function renderBangKhaySuDung() {
    const tbody = document.getElementById("sd_bangKhayChon"); if(!tbody) return;
    if(gioKhaySuDungTam.length === 0) { tbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Chưa chọn khay...</td></tr>`; return; }
    tbody.innerHTML = gioKhaySuDungTam.map((item, index) => {
        let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A";
        return `<tr class="border-b font-medium hover:bg-slate-50 text-xs">
            <td class="p-2 text-center"><i class="fa-solid fa-square-check text-teal-600 text-sm"></i></td>
            <td class="p-2 font-mono font-bold text-sky-700">${item.maMacDinh}</td>
            <td class="p-2 font-bold text-slate-800">${tenBoGoc}</td>
            <td class="p-2 text-center font-mono font-bold text-rose-600 bg-rose-50/30">${item.batchCode || 'N/A'}</td>
            <td class="p-2 text-center"><span class="px-2 py-0.5 rounded text-[10px] font-bold bg-teal-100 text-teal-800">${item.status}</span></td>
            <td class="p-2 text-center"><i class="fa-solid fa-trash-can text-rose-500 cursor-pointer hover:text-rose-700" onclick="xoaKhayKhoiListSuDung(${index})"></i></td>
        </tr>`;
    }).join('');
}

function xoaKhayKhoiListSuDung(index) {
    gioKhaySuDungTam.splice(index, 1);
    renderBangKhaySuDung();
}

// =========================================================================
// 11. THIẾT LẬP MÁY IN BIXOLON SLP-TX403 (80mm x 50mm) & BIÊN BẢN HÀNG LOẠT
// =========================================================================

// In tem đơn Bixolon 80mm x 50mm
function inTemBixolonChuan(itemData) {
    const pZone = document.getElementById("print-zone");
    if (!pZone) return;

    const tenBo = itemData.bo ? itemData.bo.split(" [ID:")[0] : "DỤNG CỤ VÔ KHUẨN";
    const maID = itemData.maMacDinh || itemData.idKhay || "000000";
    const ngayNSX = itemData.ngayHapRealtime || itemData.ngayTao || getTodayDateStr();
    const ngayHSD = itemData.hsd ? new Date(itemData.hsd).toLocaleDateString('vi-VN') : "Theo quy định";
    const maBatch = itemData.batchCode || itemData.batchId || "N/A";
    const tenKhoa = itemData.khoa || "TẬP TRUNG";

    pZone.innerHTML = `
        <div class="bixolon-label" style="font-family: Arial, sans-serif;">
            <div style="text-align: center; border-bottom: 1.5px solid #000; padding-bottom: 2px;">
                <div style="font-size: 10px; font-weight: bold; text-transform: uppercase;">PHUONG NAM HOSPITAL - CSSD</div>
                <div style="font-size: 12px; font-weight: 900; margin-top: 1px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${tenBo}</div>
            </div>

            <div style="text-align: center; margin: 3px 0; display: flex; justify-content: center;">
                <svg id="bixolon-single-barcode"></svg>
            </div>

            <div style="font-size: 9px; border-top: 1px dashed #000; padding-top: 2px;">
                <div style="display: flex; justify-content: space-between; font-weight: bold;">
                    <span>LÔ: ${maBatch}</span>
                    <span>KHOA: ${tenKhoa}</span>
                </div>
                <div style="display: flex; justify-content: space-between; margin-top: 1px;">
                    <span>NSX: ${ngayNSX}</span>
                    <span style="font-size: 10px; font-weight: 900;">HSD: ${ngayHSD}</span>
                </div>
            </div>
        </div>
    `;

    JsBarcode("#bixolon-single-barcode", maID, {
        format: "CODE128",
        width: 1.6,
        height: 36,
        displayValue: true,
        fontSize: 10,
        fontOptions: "bold",
        margin: 0
    });

    document.body.classList.remove("print-mode-doc");
    document.body.classList.add("print-mode-bixolon");
    pZone.classList.remove("hidden");

    setTimeout(() => {
        window.print();
        pZone.classList.add("hidden");
        document.body.classList.remove("print-mode-bixolon");
    }, 250);
}

function inTemTongHangLoat() { 
    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 
    let batchCode = document.getElementById("hap_batchId")?.value || "A1000000_01"; 
    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 

    checkboxes.forEach((cb) => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { 
            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 
            container.innerHTML += `<div class="bixolon-label" style="font-family: Arial; font-size: 11px; color: #000; background: #fff;"><div style="text-align: center; font-weight: bold; font-size: 12px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-lo-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span></div><div style="display: flex; justify-content: space-between; border-top: 1px dashed #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 9px; font-family: monospace; font-weight: bold; margin-top: 1px;">Lô: ${batchCode}</div></div>`; 
        } 
    }); 

    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); 
    document.body.classList.remove("print-mode-doc");
    document.body.classList.add("print-mode-bixolon");
    pZone.classList.remove("hidden"); 

    checkboxes.forEach(cb => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-lo-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 
    }); 

    setTimeout(() => { 
        window.print(); 
        pZone.classList.add("hidden"); 
        document.body.classList.remove("print-mode-bixolon");
    }, 300); 
}

function inTemNghiemThuHangLoat() { 
    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 
    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 

    checkboxes.forEach((cb) => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { 
            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 
            container.innerHTML += `<div class="bixolon-label" style="font-family: Arial; font-size: 11px; color: #000; background: #fff;"><div style="text-align: center; font-size: 9px; font-weight: bold; letter-spacing: 0.5px;">PN HOSPITAL - CSSD</div><div style="text-align: center; font-weight: bold; font-size: 12px; margin: 1px 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-nt-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span><span style="color: green;">ĐẠT VÔ KHUẨN</span></div><div style="display: flex; justify-content: space-between; border-top: 1px solid #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 8px; font-weight: bold; font-family: monospace; margin-top: 1px;">BATCH: ${item.batchCode || 'N/A'}</div></div>`; 
        } 
    }); 

    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); 
    document.body.classList.remove("print-mode-doc");
    document.body.classList.add("print-mode-bixolon");
    pZone.classList.remove("hidden"); 

    checkboxes.forEach(cb => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-nt-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 
    }); 

    setTimeout(() => { 
        window.print(); 
        pZone.classList.add("hidden"); 
        document.body.classList.remove("print-mode-bixolon");
    }, 300); 
}

// In Biên bản giao nhận khổ A4 máy in văn phòng (Có hỗ trợ chữ ký điện tử)
function inHoaDonGiaoNhan() {
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 
    if (!k) return showToast("Chọn Khoa trước!", "error");
    
    // Tìm chữ ký điện tử mới nhất từ Firestore hoặc localStorage
    const itemKyGầnNhat = listGiaoDich.find(x => x.khoa === k && x.anhChuKyNhanBase64);
    const savedSignature = JSON.parse(localStorage.getItem(`signature_${k}`) || "null");

    let htmlChuKyDieuDuong = `<p style="font-weight:normal; font-style:italic; color:#64748b; margin-top:60px;">(Ký và ghi rõ họ tên)</p>`;
    
    if (itemKyGầnNhat && itemKyGầnNhat.anhChuKyNhanBase64) {
        htmlChuKyDieuDuong = `
            <div style="text-align:center; margin-top:10px;">
                <img src="${itemKyGầnNhat.anhChuKyNhanBase64}" style="max-height:75px; margin:0 auto; display:block;" alt="Chữ ký"/>
                <p style="font-weight:bold; margin-top:5px; text-transform:uppercase; color:#0f172a;">${itemKyGầnNhat.nguoiKyNhanKhoa}</p>
                <p style="font-size:10px; color:#64748b; font-weight:normal;">Xác nhận: ${itemKyGầnNhat.ngayKhoaNhanThucTe || ''} ${itemKyGầnNhat.timeKhoaNhanThucTe || ''}</p>
            </div>
        `;
    } else if (savedSignature && savedSignature.chuKyImg) {
        htmlChuKyDieuDuong = `
            <div style="text-align:center; margin-top:10px;">
                <img src="${savedSignature.chuKyImg}" style="max-height:75px; margin:0 auto; display:block;" alt="Chữ ký"/>
                <p style="font-weight:bold; margin-top:5px; text-transform:uppercase; color:#0f172a;">${savedSignature.tenNguoiNhan}</p>
                <p style="font-size:10px; color:#64748b; font-weight:normal;">Xác nhận: ${savedSignature.timeKy}</p>
            </div>
        `;
    }

    let htmlBang = `
        <table style="width:100%; border-collapse:collapse; margin-top:20px; font-size:13px; text-align:left; font-family:Arial, sans-serif;">
            <thead>
                <tr style="background-color:#f1f5f9;">
                    <th style="padding:10px; border:1px solid #334155; width:35%; text-align:left; color:#0f172a;">PHÂN LOẠI MÂM / LOẠI DỤNG CỤ</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ TRẢ BẨN</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ NHẬN SẠCH</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">NHẬN VÔ KHUẨN</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:20%; color:#0f172a;">CSSD NỢ KHOA</th>
                </tr>
            </thead>
            <tbody>`;

    const rows = document.querySelectorAll('#bangDonGiaoNhan tr');
    if (rows.length === 0 || (rows.length === 1 && rows[0].cells.length === 1)) { 
        htmlBang += `<tr><td colspan="5" style="padding:15px; border:1px solid #334155; text-align:center; font-style:italic;">Không có dữ liệu công nợ.</td></tr>`; 
    } else {
        rows.forEach(row => {
            const cells = row.cells;
            if (cells.length >= 5) { 
                htmlBang += `
                    <tr>
                        <td style="padding:10px; border:1px solid #334155; font-weight:bold; color:#0f172a;">${cells[0].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#e11d48;">${cells[1].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#0284c7;">${cells[2].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#7c3aed;">${cells[3].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#d97706;">${cells[4].innerText}</td>
                    </tr>`; 
            }
        });
    }
    htmlBang += `</tbody></table>`;

    const pZone = document.getElementById("print-zone");
    pZone.innerHTML = `
        <div style="width:100%; font-family:Arial, sans-serif; padding:10px; color:#000;">
            <div style="text-align:center; margin-bottom:25px;">
                <h2 style="margin:0; font-size:18px; font-weight:900; text-transform:uppercase;">BIÊN BẢN GIAO NHẬN DỤNG CỤ CSSD</h2>
                <p style="margin:8px 0 0 0; font-size:13px; font-weight:bold;">Khoa/Phòng: <span style="text-transform:uppercase; color:#0284c7;">${k}</span> - Ngày xuất: ${new Date().toLocaleDateString('vi-VN')}</p>
            </div>
            ${htmlBang}
            <div style="margin-top:40px; display:flex; justify-content:space-between; font-size:12px; font-weight:bold; padding:0 30px;">
                <div style="text-align:center; width:45%;">
                    <p style="margin-bottom:10px; text-transform:uppercase;">ĐIỀU DƯỠNG LÂM SÀNG</p>
                    ${htmlChuKyDieuDuong}
                </div>
                <div style="text-align:center; width:45%;">
                    <p style="margin-bottom:10px; text-transform:uppercase;">CHUYÊN VIÊN CSSD</p>
                    <p style="font-weight:normal; font-style:italic; color:#64748b; margin-top:60px;">(Ký và ghi rõ họ tên)</p>
                </div>
            </div>
        </div>`;

    document.body.classList.remove("print-mode-bixolon");
    document.body.classList.add("print-mode-doc");
    pZone.classList.remove("hidden");

    setTimeout(() => { 
        window.print(); 
        pZone.classList.add("hidden"); 
        document.body.classList.remove("print-mode-doc");
    }, 200);
}

// =========================================================================
// 12. HÀM PHỤ TRỢ: CHỨC NĂNG QUẢN TRỊ ADMIN & TIỆN ÍCH
// =========================================================================
function taiDanhMucLinhKienChuan() {
    const tbody = document.getElementById("bangDanhMucLinhKien"); if (!tbody) return; tbody.innerHTML = "";
    if (!databaseExcel || databaseExcel.length === 0) { tbody.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-slate-400 italic">Chưa có dữ liệu. Vui lòng nạp file Excel.</td></tr>`; return; }
    let gopBoExcel = {};
    databaseExcel.forEach(x => {
        let tenBo = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; if (!tenBo) return;
        tenBo = String(tenBo).trim().toUpperCase(); if (!gopBoExcel[tenBo]) gopBoExcel[tenBo] = []; gopBoExcel[tenBo].push(x);
    });
    for (let tenBo in gopBoExcel) {
        let linhKienHtml = `<div class="flex flex-wrap gap-1">`; let tongSl = 0;
        gopBoExcel[tenBo].forEach(item => {
            let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || item['Dụng cụ'] || item['Tên Chi Tiết'] || "Dụng cụ Chi Tiết";
            
            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {
                tenDc = "Dụng cụ chuẩn mâm";
            }
            
            let sl = parseInt(item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1) || 0; let hanHap = item['Tuổi thọ mẻ hấp'] || item['Tuổi thọ'] || 100;
            tongSl += sl; linhKienHtml += `<span class="bg-slate-100 text-slate-700 px-1.5 py-0.5 rounded text-[10px] font-medium border border-slate-200">${tenDc} (${sl}) <span class="text-amber-600 font-bold">[Max: ${hanHap}]</span></span>`;
        });
        linhKienHtml += `</div>`; const tr = document.createElement("tr"); tr.className = "hover:bg-slate-50 transition-colors border-b";
        tr.innerHTML = `<td class="p-3 font-bold text-slate-800 text-xs">${tenBo}</td><td class="p-3">${linhKienHtml}</td><td class="p-3 text-center font-black text-sky-700 bg-sky-50/30">${tongSl}</td>`;
        tbody.appendChild(tr);
    }
}

function updateGiaoDienMatrix(role, tabId, checkboxElement) {
    if(!cauHinhGiaoDien[role]) cauHinhGiaoDien[role] = [];
    if(checkboxElement.checked) { if(!cauHinhGiaoDien[role].includes(tabId)) cauHinhGiaoDien[role].push(tabId); } 
    else { cauHinhGiaoDien[role] = cauHinhGiaoDien[role].filter(x => x !== tabId); }
    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ cauHinhGiaoDien: cauHinhGiaoDien }).then(() => showToast("Đã cập nhật quyền truy cập!", "success"));
}

function handleViewAnhBiMoi(firestoreId) { hanhDongXemAnhBiMoi(firestoreId); }
function hanhDongXemAnhBiMoi(firestoreId) {
    let item = listGiaoDich.find(x => x.firestoreId === firestoreId); if (!item) return showToast("Không tìm thấy thông tin!", "error");
    let loGoc = listGiaoDich.find(m => m.batchCode === item.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 
    let chuoiAnhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || item.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;
    if (!chuoiAnhBase64) return showToast("Mẻ này chưa tải ảnh minh chứng hoặc kế thừa.", "error");
    let w = window.open(); w.document.write(`<html><head><title>MINH CHỨNG BI - LÔ ${item.batchCode}</title></head><body style='margin:0; background:#000; display:flex; justify-content:center; align-items:center;'><img src='${chuoiAnhBase64}' style='max-width:100%; max-height:100vh; object-fit:contain;'/></body></html>`);
}

function switchAdminSubtab(sub) { 
    document.getElementById('subtab-database')?.classList.add('hidden'); document.getElementById('subtab-security')?.classList.add('hidden'); 
    document.getElementById('subbtn-database')?.classList.replace('admin-subtab-active', 'text-slate-600'); document.getElementById('subbtn-security')?.classList.replace('admin-subtab-active', 'text-slate-600'); 
    document.getElementById('subtab-' + sub)?.classList.remove('hidden'); document.getElementById('subbtn-' + sub)?.classList.add('admin-subtab-active'); 
}

function saveAdminPIN(type) { let newVal = document.getElementById(`cfg_pin${type.charAt(0).toUpperCase() + type.slice(1).toLowerCase()}`).value.trim(); if(type === 'ADMIN') thongTinMatKhauAdmin.adminPIN = newVal; if(type === 'CSSD') thongTinMatKhauAdmin.cssdPIN = newVal; if(type === 'GUEST') thongTinMatKhauAdmin.guestPIN = newVal; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ thongTinMatKhauAdmin: thongTinMatKhauAdmin }).then(() => showToast("Đã lưu PIN!", "success")); }
function themKtvCssd() { let code = prompt("Mã NV:"); let ten = prompt("Tên:"); let pin = prompt("PIN:"); if(code && ten && pin) { danhSachKtvCssd.push({ code: code.toUpperCase(), ten: ten, pin: pin }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }
function xaoKtvCssd(code) { if(confirm("Xóa?")) { danhSachKtvCssd = danhSachKtvCssd.filter(x => x.code !== code); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }
function themKhoaThuCong() { let t = prompt("Nhập Tên Khoa/Phòng:"); if(t) { danhSachKhoa.push({ ten: t.toUpperCase(), pin: "123", danhSachBo: [] }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Thêm Khoa thành công!", "success")); } }

function khaiSinhKhayVangLai() {
    if(danhSachKhoa.length === 0) return showToast("Hệ thống chưa có Khoa nào!", "error");
    let tenKhay = prompt("Nhập Tên Mâm/Khay dụng cụ:"); if(!tenKhay) return;
    let maId = prompt("Nhập Mã ID khay:"); if(!maId) return; maId = maId.toUpperCase().trim();
    let dsTenKhoa = danhSachKhoa.map((k, idx) => `${idx + 1}. ${k.ten}`).join("\n");
    let luaChon = prompt(`Chọn số thứ tự Khoa sở hữu:\n${dsTenKhoa}`); if(!luaChon) return;
    let idxKhoa = parseInt(luaChon) - 1; if(idxKhoa < 0 || idxKhoa >= danhSachKhoa.length) return showToast("Lựa chọn không hợp lệ!", "error");
    let khoaChon = danhSachKhoa[idxKhoa]; let chuoiKhayDinhDang = `${tenKhay.toUpperCase().trim()} [ID:${maId}]`;
    if(!khoaChon.danhSachBo) khoaChon.danhSachBo = []; if(khoaChon.danhSachBo.includes(chuoiKhayDinhDang)) return showToast("Khay đã tồn tại!", "error");
    khoaChon.danhSachBo.push(chuoiKhayDinhDang);
    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => { showToast(`Khai sinh khay ${maId} thành công!`, "success"); callRender(); });
}

function updatePINTrựcTiep(idx, tenKhoa) { let p = document.getElementById(`pin-khoa-${idx}`).value.trim(); if(p) { danhSachKhoa.find(x => x.ten === tenKhoa).pin = p; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Đổi PIN thành công", "success")); } }

function initSelects() { 
    let opts = '<option value="">-- Chọn Khoa --</option>' + danhSachKhoa.map(k=>`<option value="${k.ten}">${k.ten}</option>`).join('');
    if(document.getElementById("login_khoa")) document.getElementById("login_khoa").innerHTML = opts; 
    if(document.getElementById("khoa_selKhoa")) document.getElementById("khoa_selKhoa").innerHTML = opts; 
    if(document.getElementById("login_nv_cssd")) document.getElementById("login_nv_cssd").innerHTML = '<option value="">-- Chọn KTV CSSD --</option>' + danhSachKtvCssd.map(k=>`<option value="${k.code}">${k.code} - ${k.ten}</option>`).join(''); 
     
    if (document.getElementById("rua_hoaChat")) {
        document.getElementById("rua_hoaChat").innerHTML = danhMucHoaChatRua.map(hc => `<option value="${hc.ten}">${hc.ten}</option>`).join('');
        document.getElementById("rua_lieuLuong").value = danhMucHoaChatRua[0].lieuLuong;
        document.getElementById("rua_hoaChat").addEventListener("change", (e) => {
            let selectedHc = danhMucHoaChatRua.find(hc => hc.ten === e.target.value);
            if(selectedHc) document.getElementById("rua_lieuLuong").value = selectedHc.lieuLuong;
        });
    }
    if (document.getElementById("rua_loaiRua")) { capNhatDanhSachMaMayRua(); }
    if (document.getElementById("hap_loaiHap")) { capNhatDanhSachMaMay(); }
     
    const selKhoaTraba = document.getElementById("khoa_selKhoa"); if(selKhoaTraba) { selKhoaTraba.removeEventListener("change", loadBoDungCuTheoKhoa); selKhoaTraba.addEventListener("change", loadBoDungCuTheoKhoa); }
}

function showToast(msg, type="error") { const t = document.createElement('div'); t.className = `fixed top-6 right-6 ${type==="error"?"bg-rose-600":"bg-emerald-600"} text-white px-5 py-3.5 rounded-lg shadow-2xl z-[100] font-bold text-sm`; t.innerHTML = msg; document.body.appendChild(t); setTimeout(() => t.remove(), 2500); }
function toggleMobileMenu() { document.getElementById("sidebar_menu").classList.toggle("-translate-x-full"); document.getElementById("mobile-overlay").classList.toggle("hidden"); }

function moCamera(inputId) { targetInputIdForScan = inputId; document.getElementById("popupScanner").classList.remove("hidden"); html5QrCode = new Html5Qrcode("reader"); Html5Qrcode.getCameras().then(devices => { let cid = devices.length > 1 ? devices[devices.length - 1].id : devices[0].id; html5QrCode.start(cid, { fps: 15, qrbox: { width: 260, height: 180 } }, (txt) => { document.getElementById(targetInputIdForScan).value = txt.trim().toUpperCase(); if(targetInputIdForScan==='khoa_inpMaBo') themVaoGio(); if(targetInputIdForScan==='xuat_inpMaBo') xuatKhoXoayVong(); if(targetInputIdForScan==='sd_maKhayInp') scanKhayVaoSuDung(); }).catch(e=>{}) }); }
function dongCamera() { if(html5QrCode) html5QrCode.stop().then(() => html5QrCode.clear()); document.getElementById("popupScanner").classList.add("hidden"); }
function xoaSachDuLieuGiaoDichRealtime() { if(prompt("Nhập PIN ADMIN:") === (thongTinMatKhauAdmin.adminPIN||"admin2026")) { db.collection("phieuGiaoNhan").get().then(snap => { let b = db.batch(); snap.forEach(d => b.delete(d.ref)); b.commit().then(() => location.reload()); }); } }

function docAnhBiUpTaiCho(inputElement) { 
    const file = inputElement.files[0]; if (!file) return; 
    const reader = new FileReader(); reader.onloadend = function() { duLieuAnhBiTamBase64 = reader.result; showToast("Tải ảnh minh chứng BI thành công!", "success"); kiemTraQuyenDuyetMeHap(); }; 
    reader.readAsDataURL(file); 
}

// =========================================================================
// 13. HÀM CORE RENDER DỮ LIỆU ĐA TAB LUỒNG HỆ THỐNG
// =========================================================================
function renderTheoTabHienTai() {
    if(activeTab === 'khoaphong') {
        const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;
        let tatCaDonCuaKhoa = listGiaoDich.filter(x => (x.khoa || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase());
        
        // 1. TÍNH TOÁN CÔNG NỢ LUÂN CHUYỂN THỰC TẾ
        let gopCongNo = {};
        tatCaDonCuaKhoa.forEach(x => {
            if (x.status === "CHO_CAP_PHAT") return;

            let tenLoaiMâm = x.bo ? String(x.bo).split(" [ID:")[0] : "Chưa rõ mâm";
            if (!gopCongNo[tenLoaiMâm]) gopCongNo[tenLoaiMâm] = { daTraBan: 0, nhanSach: 0, nhanVoKhuan: 0, cssdNo: 0 };
            
            if (x.status !== "CHO_THU") gopCongNo[tenLoaiMâm].daTraBan += 1; 
            if (["DANG_RUA", "TRONG_BUONG_RUA", "CHO_DONG_GOI", "CHO_HAP", "DANG_HAP", "CHO_XUAT", "ĐANG_VAN_CHUYEN", "HOAN_TAT", "DA_SU_DUNG"].includes(x.status)) gopCongNo[tenLoaiMâm].nhanSach += 1;
            if (x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG") gopCongNo[tenLoaiMâm].nhanVoKhuan += 1; 
            gopCongNo[tenLoaiMâm].cssdNo = gopCongNo[tenLoaiMâm].daTraBan - gopCongNo[tenLoaiMâm].nhanVoKhuan;
        });
        
        let arrHtml = [];
        for (let key in gopCongNo) {
            let item = gopCongNo[key];
            arrHtml.push(`<tr class="border-b hover:bg-slate-50 transition-colors font-medium text-[11px]"><td class="p-3 font-bold text-slate-800 border-r">${key}</td><td class="p-3 text-center text-rose-600 font-bold bg-rose-50/20 border-r">${item.daTraBan}</td><td class="p-3 text-center text-sky-600 font-bold bg-sky-50/20 border-r">${item.nhanSach}</td><td class="p-3 text-center text-purple-600 font-bold bg-purple-50/20 border-r">${item.nhanVoKhuan}</td><td class="p-3 text-center text-amber-600 font-black bg-amber-50/20">${item.cssdNo > 0 ? 'Nợ ' + item.cssdNo : '-'}</td></tr>`);
        }
        if(document.getElementById("bangDonGiaoNhan")) {
            document.getElementById("bangDonGiaoNhan").innerHTML = arrHtml.length ? arrHtml.join('') : `<tr><td colspan="5" class="p-8 text-center text-slate-400 italic">Khoa chưa phát sinh công nợ luân chuyển đồ.</td></tr>`;
        }
        
        // 2. DANH SÁCH CHỜ NHẬN ĐỒ SẠCH (Từ xe trung chuyển CSSD bàn giao lên)
        const tbodyChoNhan = document.getElementById("bangChoNhanTaiKhoa"); const badgeChoNhan = document.getElementById("badgeChoNhanKhoa"); const txtNguoiXacNhan = document.getElementById("txtNguoiDungNhanHienTai");
        if(txtNguoiXacNhan) txtNguoiXacNhan.innerText = `PM-ĐD: ${loginUserCode || "Chưa Đăng Nhập"}`;
        
        let dsDangVanChuyen = tatCaDonCuaKhoa.filter(x => x.status === "ĐANG_VAN_CHUYEN"); if(badgeChoNhan) badgeChoNhan.innerText = `${dsDangVanChuyen.length} khay`;
        if(tbodyChoNhan) {
            if(dsDangVanChuyen.length === 0) { tbodyChoNhan.innerHTML = `<tr><td colspan="5" class="p-3 text-center text-slate-400 italic">Hiện không có dụng cụ nào đang chuyển về khoa.</td></tr>`; } 
            else { tbodyChoNhan.innerHTML = dsDangVanChuyen.map(khay => `<tr class="hover:bg-slate-50 transition-colors"><td class="p-2 text-center"><input type="checkbox" value="${khay.firestoreId}" class="w-3.5 h-3.5 text-sky-600 rounded border-slate-300 focus:ring-sky-500 cursor-pointer"></td><td class="p-2 font-mono font-bold text-slate-700">${khay.maMacDinh || 'N/A'}</td><td class="p-2 font-semibold text-slate-800">${khay.bo ? String(khay.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-2 text-slate-500 font-medium">${khay.nvXuatKho || '--'}</td><td class="p-2 text-center"><span class="bg-purple-50 text-purple-700 font-mono text-[10px] px-1.5 py-0.5 rounded font-bold">${khay.batchCode || 'N/A'}</span></td></tr>`).join(''); }
        }

        // 3. THEO DÕI YÊU CẦU ĐANG CHỜ CSSD CẤP PHÁT
        let dsChoCapPhat = tatCaDonCuaKhoa.filter(x => x.status === "CHO_CAP_PHAT");
        const tbodyChoCapPhat = document.getElementById("bangChoCapPhatTaiKhoa");
        if (tbodyChoCapPhat) {
            if (dsChoCapPhat.length === 0) {
                tbodyChoCapPhat.innerHTML = `<tr><td colspan="4" class="p-3 text-center text-slate-400 italic">Không có yêu cầu mượn khay mới nào đang chờ duyệt.</td></tr>`;
            } else {
                tbodyChoCapPhat.innerHTML = dsChoCapPhat.map(yc => `
                    <tr class="border-b text-xs hover:bg-amber-50/10 font-medium">
                        <td class="p-2.5 font-bold text-amber-800">${yc.bo}</td>
                        <td class="p-2.5 text-center"><span class="px-2 py-0.5 rounded-full text-[10px] font-bold bg-amber-100 text-amber-800 border border-amber-200 animate-pulse">Chờ CSSD cấp khay</span></td>
                        <td class="p-2.5 text-center font-mono text-slate-400">${yc.time}</td>
                        <td class="p-2.5 text-center text-slate-500">${yc.nvYeuCau || 'ĐD'}</td>
                    </tr>
                `).join('');
            }
        }
    }
    else if(activeTab === 'thugom') {
        let fK = document.getElementById("filterKhoaThuGom")?.value || ""; 
        let lsTG = listGiaoDich.filter(x => x.status === "CHO_THU"); 
        
        if (document.getElementById("filterKhoaThuGom")) {
            let danhSachKhoaCoDon = [...new Set(lsTG.map(x => x.khoa))].filter(Boolean); 
            let htmlOpts = '<option value="">-- Lọc theo Khoa --</option>';
            danhSachKhoaCoDon.forEach(k => { 
                htmlOpts += `<option value="${k}" ${(k === fK) ? "selected" : ""}>${k}</option>`; 
            });
            document.getElementById("filterKhoaThuGom").innerHTML = htmlOpts; 
            fK = document.getElementById("filterKhoaThuGom").value || "";
        }
        
        if (fK) lsTG = lsTG.filter(x => (x.khoa || "").toString().trim().toUpperCase() === fK.toString().trim().toUpperCase());
        if (document.getElementById("badgeSoCho")) document.getElementById("badgeSoCho").innerText = `${lsTG.length} Lệnh`;
        
        let bodyThuGom = document.getElementById("bangChoThuGom");
        if(bodyThuGom) {
            bodyThuGom.innerHTML = lsTG.map(i => {
                let tenBo = i.bo ? String(i.bo).split(" [ID:")[0].trim() : ""; 
                
                let itemsInBo = databaseExcel.filter(x => {
                    let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || '';
                    return String(boName).trim().toUpperCase() === String(tenBo).toUpperCase();
                });
                
                let checklistHtml = "";
                if (itemsInBo.length > 0) {
                    checklistHtml = `<div class="max-h-32 overflow-y-auto pr-1 border border-slate-200/60 rounded bg-slate-50/50 p-1.5 mt-1 flex flex-col gap-1">`;
                    itemsInBo.forEach(item => {
                        let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || "Dụng cụ chi tiết";
                        let sl = item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1;
                        
                        if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {
                            tenDc = "Dụng cụ chuẩn mâm";
                        }

                        checklistHtml += `
                            <div class="flex justify-between items-center border-b border-dashed border-slate-200 pb-0.5 last:border-none text-[11px]">
                                <span class="text-slate-700 font-semibold"><i class="fa-solid fa-circle text-[4px] text-sky-400 mr-1.5 align-middle"></i>${tenDc}</span>
                                <span class="font-bold text-sky-700 bg-sky-50 px-1.5 py-0.5 rounded border border-sky-100 text-[10px]">x${sl}</span>
                            </div>`;
                    });
                    checklistHtml += `</div>`;
                } else {
                    checklistHtml = `<div class="text-[10px] text-emerald-700 bg-emerald-50 border border-emerald-100 p-1.5 rounded font-medium mt-1"><i class="fa-solid fa-circle-check mr-1"></i>Dụng cụ lẻ (Cơ số: 1 chiếc)</div>`;
                }
                
                return `
                    <tr class="border-b border-slate-100 hover:bg-slate-50/40 transition-colors">
                        <td class="p-3 align-top">
                            <div class="font-black text-slate-700 text-[11px] uppercase tracking-wide">${i.khoa}</div>
                            ${i.ghiChu ? `<div class="text-[10px] text-rose-600 font-bold bg-rose-50 border border-rose-100 px-1.5 py-0.5 rounded mt-1.5 inline-block"><i class="fa-solid fa-triangle-exclamation mr-1"></i>${i.ghiChu}</div>` : ''}
                        </td>
                        <td class="p-3 align-top">
                            <div class="font-black text-sky-800 text-[13px] uppercase tracking-tight">${tenBo}</div>
                            <div class="text-[10px] font-mono text-slate-400 font-bold mt-0.5">MÃ SỐ ID: ${i.maMacDinh}</div>
                            ${checklistHtml}
                        </td>
                        <td class="p-3 text-center align-top font-mono font-bold text-slate-500 text-[11px]">${i.time}</td>
                        <td class="p-3 text-center align-top action-col">
                            <button onclick="moPopupKiemDem('${i.firestoreId}')" class="bg-sky-600 hover:bg-sky-700 text-white font-black px-3.5 py-2 rounded-lg shadow-sm text-[11px] uppercase tracking-wider transition-all"><i class="fa-solid fa-clipboard-check mr-1"></i>Kiểm Đếm</button>
                        </td>
                    </tr>`;
            }).join('');
        }
    }
    else if(activeTab === 'mayrua') {
        let lsCR = listGiaoDich.filter(x => x.status === "DANG_RUA");
        if(document.getElementById("badgeChoRua")) document.getElementById("badgeChoRua").innerText = `${lsCR.length} Mục`;
        
        let bodyChoRua = document.getElementById("bangChoRua");
        if(bodyChoRua) {
            bodyChoRua.innerHTML = lsCR.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="rua-checkbox"></td><td class="p-3 font-bold">${i.bo}</td><td class="p-3 text-right font-mono">${i.maMacDinh}</td></tr>`).join('');
        }
        
        let lsTR = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA");
        let bodyChoNghiemThuRua = document.getElementById("bangChoNiemThuRua");
        if(bodyChoNghiemThuRua) {
            bodyChoNghiemThuRua.innerHTML = lsTR.map(i => `<tr class="border-b"><td class="p-2.5 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="nghiemthurua-checkbox"></td><td class="p-2 text-xs font-bold">${i.bo} <span class="text-slate-400 font-mono">(${i.rua_batchCode || 'Mẻ Rửa'})</span></td></tr>`).join('');
        }
        
        let bodyLichSuRua = document.getElementById("bangLichSuRua");
        if(bodyLichSuRua) {
            const homNayMoiStr = getTodayDateStr();
            let tatCaMucCoLoRua = listGiaoDich.filter(x => x.rua_batchCode && (x.rua_ngayRua === homNayMoiStr || x.ngayTao === homNayMoiStr));
            let cacMeRuaGop = {};
            tatCaMucCoLoRua.forEach(x => {
                if(!cacMeRuaGop[x.rua_batchCode]) {
                    cacMeRuaGop[x.rua_batchCode] = {
                        batchCode: x.rua_batchCode,
                        mayRua: x.rua_mayRua || "Belimed WD250",
                        chuKy: x.rua_chuKy || "Chu trình tiêu chuẩn",
                        hoaChat: x.rua_hoaChat || "Smeg",
                        timeBatDau: x.rua_timeBatDau || "--:--",
                        ngay: x.rua_ngayRua || "",
                        soLuong: 0,
                        trangThaiMe: x.status === "TRONG_BUONG_RUA" ? "Đang rửa" : "Hoàn tất sạch"
                    };
                }
                cacMeRuaGop[x.rua_batchCode].soLuong += 1;
            });
            let danhSachMeRuaSapXep = Object.values(cacMeRuaGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));
            if(danhSachMeRuaSapXep.length === 0) {
                bodyLichSuRua.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa chạy mẻ rửa nào</td></tr>`;
            } else {
                bodyLichSuRua.innerHTML = danhSachMeRuaSapXep.map(me => `
                    <tr class="hover:bg-slate-50 transition-colors">
                        <td class="p-3">
                            <div class="flex items-center gap-2">
                                <span class="font-mono font-black text-sky-700 text-sm tracking-wider">${me.batchCode}</span>
                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "Đang rửa" ? "text-amber-600 bg-amber-50 border-amber-200" : "text-emerald-600 bg-emerald-50 border-emerald-200"}">${me.trangThaiMe}</span>
                            </div>
                            <div class="text-[11px] text-slate-500 font-medium mt-1">${me.mayRua} | ${me.chuKy}</div>
                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Hoá chất: ${me.hoaChat} | Bắt đầu: ${me.timeBatDau} (${me.ngay})</div>
                        </td>
                        <td class="p-3 text-right pr-4">
                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuong} khay</span>
                        </td>
                    </tr>`).join('');
            }
        }
    }
    else if(activeTab === 'donggoi') {
        let lsDG = listGiaoDich.filter(x => x.status === "CHO_DONG_GOI"); 
        if(document.getElementById("badgeDongGoi")) document.getElementById("badgeDongGoi").innerText = lsDG.length;
        let gDongGoi = document.getElementById("gridDongGoi");
        if(gDongGoi) {
            gDongGoi.innerHTML = lsDG.map(i => `<div class="bg-white p-3 rounded border border-slate-200 mb-2 flex justify-between items-center"><div class="flex-1"><div class="font-bold text-sky-700 text-[13px]">${i.bo}</div><div class="text-[10px] text-slate-500">Từ khoa: ${i.khoa}</div></div><button onclick="moPopupDongGoi('${i.firestoreId}')" class="bg-sky-50 text-sky-700 border border-sky-300 px-3 py-1.5 rounded text-[11px] font-black">ĐÓNG GÓI</button></div>`).join('');
        }
    }
    else if(activeTab === 'mayhap') {
        let lsCH = listGiaoDich.filter(x => x.status === "CHO_HAP"); 
        if(document.getElementById("badgeChoHap")) document.getElementById("badgeChoHap").innerText = `${lsCH.length} Mục`;
        let bodyChoHap = document.getElementById("bangChoHap");
        if(bodyChoHap) { 
            bodyChoHap.innerHTML = lsCH.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="hap-checkbox"></td><td class="p-3 font-bold text-xs">${i.bo}</td><td class="p-3 text-right font-mono text-xs font-bold text-sky-700">${i.maMacDinh}</td></tr>`).join(''); 
        }

        let lsNT = listGiaoDich.filter(x => x.status === "DANG_HAP"); 
        let bodyChoNghiemThu = document.getElementById("bangChoNghiệmThu") || document.getElementById("bangChoNghiemThu");
        
        if(bodyChoNghiemThu) { 
            if (lsNT.length === 0) {
                bodyChoNghiemThu.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Không có mâm/mẻ nào đang trong lò chờ duyệt.</td></tr>`;
            } else {
                bodyChoNghiemThu.innerHTML = lsNT.map(i => {
                    let tenBoClean = i.bo ? String(i.bo).split(" [ID:")[0] : "Chưa rõ mâm";
                    return `
                        <tr class="border-b hover:bg-amber-50/40 transition-colors">
                            <td class="p-2.5 text-center action-col">
                                <input type="checkbox" value="${i.firestoreId}" class="nghiemthu-checkbox w-4 h-4 text-amber-600 rounded cursor-pointer" data-hasbi="${i.hasBI ? 'true' : 'false'}" onchange="kiemTraQuyenDuyetMeHap()">
                            </td>
                            <td class="p-2 text-xs">
                                <div class="font-black text-slate-800">${tenBoClean} <span class="text-[10px] font-mono text-sky-700">(${i.maMacDinh || 'N/A'})</span></div>
                                <div class="flex items-center gap-2 mt-0.5">
                                    <span class="text-[10px] font-mono font-bold text-rose-700 bg-rose-50 px-1 rounded border border-rose-100">Lô: ${i.batchCode || 'N/A'}</span>
                                    ${i.hasBI ? '<span class="px-1.5 py-0.5 bg-amber-100 text-amber-800 rounded text-[9px] font-black border border-amber-300 flex items-center gap-1"><i class="fa-solid fa-flask"></i> CHỜ BI</span>' : ''}
                                </div>
                            </td>
                        </tr>`;
                }).join(''); 
            }
        }
        kiemTraQuyenDuyetMeHap();

        if(document.getElementById("bangLichSuHap")) {
            const homNayMoiStr = getTodayDateStr(); 
            let tatCaMucCoLo = listGiaoDich.filter(x => x.batchCode && (x.ngayHapRealtime === homNayMoiStr || x.ngayTao === homNayMoiStr)); 
            let cacMeHapGop = {};
            tatCaMucCoLo.forEach(x => {
                if(!cacMeHapGop[x.batchCode]) { 
                    cacMeHapGop[x.batchCode] = { 
                        batchCode: x.batchCode, 
                        loaiHap: x.thongTinLoHap?.loaiHap || "Hấp hơi nước", 
                        chuKyNhiet: x.thongTinLoHap?.chuKyNhiet || "134°C - 4 phút", 
                        apSuat: x.thongTinLoHap?.apSuat || "N/A", 
                        thoiGian: x.thongTinLoHap?.thoiGianBatDau || x.time || "--:--", 
                        ngay: x.ngayHapRealtime || x.ngayTao || "", 
                        soLuongKhay: 0, 
                        trangThaiMe: x.status === "DANG_HAP" ? "ĐANG CHẠY LÒ" : "HOÀN THÀNH" 
                    }; 
                }
                cacMeHapGop[x.batchCode].soLuongKhay += 1;
            });
            let danhSachMeHapSapXep = Object.values(cacMeHapGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));
            if(danhSachMeHapSapXep.length === 0) { 
                document.getElementById("bangLichSuHap").innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa có mẻ hấp nào</td></tr>`; 
            } else { 
                document.getElementById("bangLichSuHap").innerHTML = danhSachMeHapSapXep.map(me => `
                    <tr class="hover:bg-slate-50 transition-colors border-b border-slate-100">
                        <td class="p-3">
                            <div class="flex items-center gap-2">
                                <span class="font-mono font-black text-rose-700 text-sm tracking-wider">${me.batchCode}</span>
                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "ĐANG CHẠY LÒ" ? "text-purple-700 bg-purple-100 border border-purple-200 animate-pulse" : "text-emerald-700 bg-emerald-100 border border-emerald-200"}">${me.trangThaiMe}</span>
                            </div>
                            <div class="text-[11px] text-slate-600 font-medium mt-1">${me.loaiHap} | ${me.chuKyNhiet} | Áp suất: ${me.apSuat} Bar</div>
                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Bắt đầu: ${me.thoiGian} (${me.ngay})</div>
                        </td>
                        <td class="p-3 text-right pr-4">
                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuongKhay} khay</span>
                        </td>
                    </tr>`).join(''); 
            }
        }
    }
    else if(activeTab === 'khovokhuan') {
        let uniqueKhoaSanCo = [...new Set(listGiaoDich.map(x => x.khoa))].filter(Boolean); const selKhoaXuat = document.getElementById("xuat_selKhoa");
        if(selKhoaXuat) {
            let currentSelected = selKhoaXuat.value; let htmlOpts = '<option value="">-- Chọn Khoa Muốn Trả Đồ --</option>';
            uniqueKhoaSanCo.forEach(k => { htmlOpts += `<option value="${k}" ${k === currentSelected ? 'selected' : ''}>${k}</option>`; });
            selKhoaXuat.innerHTML = htmlOpts;
        }

        let lsXK = listGiaoDich.filter(x => x.status === "CHO_XUAT").sort((a, b) => a.id - b.id);
        renderKhoVoKhuan(lsXK);

        let dsYeuCau = listGiaoDich.filter(x => x.status === "CHO_CAP_PHAT");
        let vungYeuCau = document.getElementById("vungYeuCauCapPhat");
        if (!vungYeuCau) {
            let parentContainer = document.getElementById("tab-khovokhuan");
            if (parentContainer) {
                let divYc = document.createElement("div");
                divYc.id = "vungYeuCauCapPhat";
                divYc.className = "mb-6 bg-amber-50/70 border border-amber-200 rounded-xl p-4 shadow-sm";
                parentContainer.insertBefore(divYc, parentContainer.firstChild);
                vungYeuCau = divYc;
            }
        }
        
        if (vungYeuCau) {
            if (dsYeuCau.length === 0) {
                vungYeuCau.innerHTML = `<h3 class="font-black text-amber-950 text-xs flex items-center gap-1.5"><i class="fa-solid fa-bell-slash"></i> HIỆN KHÔNG CÓ YÊU CẤP PHÁT ĐANG CHỜ</h3>`;
            } else {
                vungYeuCau.innerHTML = `
                    <h3 class="font-black text-amber-900 text-xs mb-2.5 flex items-center gap-1.5 animate-pulse">
                        <i class="fa-solid fa-bell text-rose-500 animate-bounce"></i> CÓ ${dsYeuCau.length} YÊU CẦU CẤP PHÁT TỪ CÁC KHOA LÂM SÀNG
                    </h3>
                    <div class="flex flex-col gap-2 max-h-48 overflow-y-auto">
                        ${dsYeuCau.map(yc => `
                            <div class="bg-white p-3 rounded-lg border border-amber-200 shadow-sm flex justify-between items-center text-xs">
                                <div>
                                    <span class="font-black text-amber-800 uppercase">[KHOA: ${yc.khoa}]</span> 
                                    Yêu cầu mâm: <span class="font-bold text-slate-800">${yc.bo}</span>
                                    <span class="text-[10px] text-slate-400 block mt-0.5">Thời gian: ${yc.time} - Người yêu cầu: ${yc.nvYeuCau || 'ĐD'}</span>
                                </div>
                                <div class="flex items-center gap-2">
                                    <button onclick="duyetCapPhatTậpTrung('${yc.firestoreId}', '${yc.bo}', '${yc.khoa}')" class="bg-amber-600 hover:bg-amber-700 text-white font-black px-3 py-2 rounded-lg text-[10px] uppercase shadow-sm flex items-center gap-1.5 transition-all">
                                        <i class="fa-solid fa-truck-ramp-box"></i>Cấp phát khay
                                    </button>
                                </div>
                            </div>
                        `).join('')}
                    </div>`;
            }
        }
        renderGioHangXuat();
    }
    else if(activeTab === 'quanlykho') {
        let fK = document.getElementById("inv_filterKhoa") ? document.getElementById("inv_filterKhoa").value : ""; 
        let uniqueKhoa = [...new Set(listGiaoDich.map(x=>x.khoa))].filter(Boolean);
        if(document.getElementById("inv_filterKhoa")) { document.getElementById("inv_filterKhoa").innerHTML = '<option value="">-- Tất cả --</option>' + uniqueKhoa.map(k=>`<option value="${k}" ${k===fK?'selected':''}>${k}</option>`).join(''); }
        let uniqueIDs = [...new Set(listGiaoDich.map(x=>x.maMacDinh))]; let arrHtml = []; const ngayHomNay = new Date(); ngayHomNay.setHours(0,0,0,0); 
        uniqueIDs.forEach(ma => {
            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a,b) => b.id - a.id)[0]; if(!currentTrans || !ma) return;
            let viTriCode = currentTrans.status; let khoaGiữ = currentTrans.khoa; if (fK && (khoaGiữ || "").toString().trim().toUpperCase() !== fK.toString().trim().toUpperCase()) return;
            let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";
            if(viTriCode === "HOAN_TAT") { viTriText = `Sẵn sàng tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 
            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã sử dụng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }
            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang vận chuyển`; viTriColor = "bg-purple-100 text-purple-800 animate-pulse"; }
            else if(viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }
            let hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-slate-100 text-slate-500">An toàn</span>`; let dongColorClass = ""; 
            if (currentTrans.hsd && (viTriCode === "CHO_XUAT" || viTriCode === "HOAN_TAT")) {
                const ngayHsd = new Date(currentTrans.hsd); ngayHsd.setHours(0,0,0,0); const soNgayConLai = Math.ceil((ngayHsd.getTime() - ngayHomNay.getTime()) / (1000 * 60 * 60 * 24));
                if (soNgayConLai < 0) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-rose-600 text-white">QUÁ HẠN</span>`; dongColorClass = "bg-rose-50/70 border-l-4 border-l-rose-500 font-bold"; } 
                else if (soNgayConLai <= 3) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-amber-500 text-white">HẠN < ${soNgayConLai} NGÀY</span>`; dongColorClass = "bg-amber-50/40 border-l-4 border-l-amber-500"; } 
                else { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-emerald-100 text-emerald-800">Hạn ${soNgayConLai} ngày</span>`; }
            }
            arrHtml.push(`<tr class="border-b border-slate-100 ${dongColorClass}"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-slate-500 text-[11px]">${khoaGiữ}</td><td class="p-3 text-center"><span class="px-2.5 py-1 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-mono font-bold text-slate-400">${currentTrans.batchCode || 'N/A'}</td><td class="p-3 text-center">${hsdBadget}</td></tr>`);
        });
        if(document.getElementById("bangTonKhoTe")) document.getElementById("bangTonKhoTe").innerHTML = arrHtml.join('');
    }
    else if(activeTab === 'danhmuc') {
        const tbody = document.getElementById("bangDanhMucTong"); if (!tbody) return;
        let uniqueIDs = [...new Set(listGiaoDich.map(x => x.maMacDinh))].filter(Boolean); let arrHtml = [];
        uniqueIDs.forEach(ma => {
            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a, b) => b.id - a.id)[0]; if (!currentTrans) return;
            let viTriCode = currentTrans.status; let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";
            if (viTriCode === "HOAN_TAT") { viTriText = `Tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 
            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã dùng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }
            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang đi đường`; viTriColor = "bg-purple-100 text-purple-800"; }
            else if (viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }
            let chuKyLo = listGiaoDich.filter(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG" || x.status === "ĐANG_VAN_CHUYEN")).length;
            arrHtml.push(`<tr class="border-b border-slate-100 font-medium"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-center"><span class="px-2.5 py-0.5 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-black text-amber-700 bg-amber-50/50">${chuKyLo} lần lễ</td></tr>`);
        });
        tbody.innerHTML = arrHtml.join('');
    }
    else if(activeTab === 'lichsuluanchuyen') {
        const tbody = document.getElementById("bangLichSuHanhTrinhGoc");
        if(tbody) {
            tbody.innerHTML = listGiaoDich.map(x => {
                let statusBadge = `<span class="px-2 py-0.5 bg-slate-100 text-slate-700 font-bold rounded text-[10px]">${x.status}</span>`;
                if(x.status === "HOAN_TAT") statusBadge = `<span class="px-2 py-0.5 bg-emerald-100 text-emerald-800 font-black rounded text-[10px]">ĐÃ BÀN GIAO</span>`;
                else if(x.status === "CHO_XUAT") statusBadge = `<span class="px-2 py-0.5 bg-teal-100 text-teal-800 font-bold rounded text-[10px]">KHO VÔ KHUẨN</span>`;
                else if(x.status === "DANG_HAP") statusBadge = `<span class="px-2 py-0.5 bg-purple-100 text-purple-800 font-bold rounded text-[10px]">ĐANG HẤP LÒ</span>`;
                else if(x.status === "DA_SU_DUNG") statusBadge = `<span class="px-2 py-0.5 bg-rose-600 text-white font-black rounded text-[10px]">SỬ DỤNG BN</span>`;
                else if(x.status === "ĐANG_VAN_CHUYEN") statusBadge = `<span class="px-2 py-0.5 bg-purple-500 text-white font-bold rounded text-[10px] animate-pulse">ĐANG VẬN CHUYỂN</span>`;
                else if(x.status === "DANG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-blue-100 text-blue-700 font-bold rounded text-[10px]">HÀNG ĐỢI RỬA</span>`;
                else if(x.status === "TRONG_BUONG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-cyan-600 text-white font-bold rounded text-[10px]">ĐANG RỬA MÁY</span>`;
                else if(x.status === "CHO_CAP_PHAT") statusBadge = `<span class="px-2 py-0.5 bg-amber-100 text-amber-800 font-bold rounded text-[10px] border border-amber-200">CHỜ CẤP PHÁT</span>`;
                
                let fontColor = ""; let loGoc = listGiaoDich.find(m => m.batchCode === x.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 
                let anhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || x.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;
                if (anhBase64) fontColor = `<br><span onclick="handleViewAnhBiMoi('${x.firestoreId}')" class="text-[10px] text-emerald-600 bg-emerald-50 border border-emerald-300 rounded px-1 cursor-pointer font-bold mt-1 inline-block"><i class="fa-solid fa-image mr-1"></i>Xem ảnh BI</span>`;
                
                let kpiHtml = x.ketQuaGiamSatKpi ? `<br><span class="text-[9px] px-1 rounded font-black border mt-1 inline-block ${x.ketQuaGiamSatKpi === "ĐẠT" ? "text-emerald-700 bg-emerald-50 border-emerald-200" : "text-rose-700 bg-rose-50 border-rose-200"}">KPI: ${x.ketQuaGiamSatKpi} (${x.thoiGianTinhKpiPhut}m)</span>` : "";
                let thongTinKemTheo = x.nvXuatKho || '<span class="text-slate-300 font-normal">Chưa xuất</span>';
                if (x.nguoiKyNhanKhoa) thongTinKemTheo += ` ➔ <span class="text-emerald-700 font-semibold">[ĐD: ${x.nguoiKyNhanKhoa}]</span>`;
                if (x.status === "DA_SU_DUNG" && x.thongTinBenhNhan) thongTinKemTheo = `<span class="text-teal-700 font-black">BN: ${x.thongTinBenhNhan.thongTinTimKiemBN}</span>`;

                return `<tr class="border-b text-xs hover:bg-slate-50 transition-colors"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3 text-center">${statusBadge}${kpiHtml}</td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/30">${x.batchCode || 'N/A'}${fontColor}</td><td class="p-3 text-center font-bold text-sky-800 bg-sky-50/40">${thongTinKemTheo}</td><td class="p-3 text-center text-slate-400 font-mono text-[11px]">${x.ngayTao || ''} ${x.time || ''}</td></tr>`;
            }).join('');
        }
    }
    else if(activeTab === 'tracuu') {
        let safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");
        if (!safeTbody) {
            let parentContainer = document.getElementById("tab-tracuu") || document.querySelector('.bg-white.p-6.rounded-xl.shadow-md');
            if(parentContainer) {
                let existingTableZone = document.getElementById("vung-ket-qua-tu-dong");
                if(!existingTableZone) {
                    let tableWrapper = document.createElement("div"); tableWrapper.id = "vung-ket-qua-tu-dong"; tableWrapper.className = "mt-6 bg-white border border-slate-200 rounded-xl p-4 shadow-sm";
                    tableWrapper.innerHTML = `<h3 class="font-black text-slate-800 text-sm mb-3 text-sky-800"><i class="fa-solid fa-list-check mr-2"></i>DANH SÁCH MÂM DỤNG CỤ TRONG MẺ TRUY VẾT</h3><div class="overflow-x-auto"><table class="w-full text-left border-collapse"><thead><tr class="bg-slate-100 text-[11px] font-bold text-slate-600 uppercase border-b"><th class="p-3">Mã ID Khay</th><th class="p-3">Tên Bộ Dụng Cụ</th><th class="p-3">Khoa Sử Dụng</th><th class="p-3">Trạng thái</th><th class="p-3 text-center">Mã Lô Hấp</th><th class="p-3 text-center">Thời Gian Kích Hoạt</th></tr></thead><tbody id="bangLichSuTruyXuatAdmin"></tbody></table></div>`;
                    parentContainer.appendChild(tableWrapper); safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");
                }
            }
        }
        let searchInp = maLoTruyVetToanCuc || ""; if (!searchInp && document.getElementById("inp_searchBatch")) { searchInp = document.getElementById("inp_searchBatch").value.trim(); }
        if(safeTbody) {
            if (!searchInp) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng nhập mã mẻ hấp và nhấn "TRA CỨU KHẨN CẤP"...</td></tr>`; return; }
            let dataFiltered = listGiaoDich.filter(x => x.batchCode && String(x.batchCode).toUpperCase().includes(searchInp.toUpperCase()));
            if(dataFiltered.length === 0) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-rose-500 italic font-bold">Không tìm thấy dữ liệu: ${searchInp}</td></tr>`; } 
            else { safeTbody.innerHTML = dataFiltered.map(x => `<tr class="border-b text-xs hover:bg-slate-50 font-medium"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3"><span class="px-2 py-0.5 rounded text-[10px] ${x.status === "HOAN_TAT" ? "bg-emerald-100 text-emerald-800 font-bold" : "bg-teal-100 text-teal-800"}">${x.status}</span></td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/40">${x.batchCode || 'N/A'}</td><td class="p-3 text-center text-slate-400 font-mono">${x.ngayTao || ''} ${x.time || ''}</td></tr>`).join(''); }
        }
    }
    else if(activeTab === 'performance') { renderKpiPerformanceGoc(); }
    else if(activeTab === 'dashboard_tv') { renderDashboardTiviRealtime(); }
}

function renderKhoVoKhuan(listData) {
    const tbody = document.getElementById("bangKhoVoKhuan");
    if (!tbody) return;

    if (!listData || listData.length === 0) {
        tbody.innerHTML = `<tr><td colspan="6" class="p-6 text-center text-slate-400 italic">Kho vô khuẩn hiện tại trống.</td></tr>`;
        return;
    }

    tbody.innerHTML = listData.map(item => `
        <tr class="hover:bg-slate-50 transition-colors">
            <td class="p-3.5 font-bold text-slate-800 text-xs">${item.bo ? String(item.bo).split(" [ID:")[0] : 'N/A'}</td>
            <td class="p-3.5 font-mono font-black text-sky-700 text-xs">${item.maMacDinh || '--'}</td>
            <td class="p-3.5 text-center font-semibold text-slate-600 text-xs">${item.khoa || 'Tập trung'}</td>
            <td class="p-3.5 text-center"><span class="bg-emerald-100 text-emerald-800 font-bold px-2.5 py-1 rounded-full text-[11px]">${item.viTriKe || 'Kệ A1'}</span></td>
            <td class="p-3.5 text-center font-mono font-bold text-xs text-rose-600">${item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN') : 'An toàn'}</td>
            <td class="p-2 text-center">
                <button onclick="xuatKhoXoayVong('${item.maMacDinh}')" 
                        class="min-h-[44px] px-4 bg-emerald-600 active:bg-emerald-700 text-white font-bold rounded-lg text-xs shadow-sm inline-flex items-center gap-1.5 transition-transform active:scale-95">
                    <i class="fa-solid fa-cart-flatbed"></i>
                    <span>Xuất Khay</span>
                </button>
            </td>
        </tr>
    `).join('');
}

// =========================================================================
// VÁ LỖI CÁC HÀM TRUY VẾT & ĐỒNG BỘ HIỆU SUẤT TV MÀN HÌNH
// =========================================================================
function truyVetTheoMaBatch() {
    const inp = document.getElementById("inp_searchBatch");
    if (inp) {
        maLoTruyVetToanCuc = inp.value.trim();
        playSound('success');
        callRender();
    }
}

function clearTruyVetBatch() {
    const inp = document.getElementById("inp_searchBatch");
    if (inp) inp.value = "";
    maLoTruyVetToanCuc = "";
    callRender();
}

function resetDuLieuKet() {
    if (confirm("Anh Hùng có chắc chắn muốn giải phóng các mâm đang xử lý dở dang (chưa Hoàn tất / Chưa dùng) về trạng thái Chờ thu gom?")) {
        let p = [];
        listGiaoDich.forEach(x => {
            if (x.status !== "HOAN_TAT" && x.status !== "DA_SU_DUNG") {
                p.push(db.collection("phieuGiaoNhan").doc(x.firestoreId).update({ status: "CHO_THU" }));
            }
        });
        Promise.all(p).then(() => {
            playSound('success');
            showToast("Đã giải phóng dữ liệu mâm kẹt thành công!", "success");
            callRender();
        });
    }
}

function renderKpiPerformanceGoc() {
    const tbody = document.getElementById("bangHieuSuatKTV");
    if (!tbody) return;
    let kpiGop = {};
    listGiaoDich.forEach(x => {
        let ktv = x.ktvThuGom || x.nvXuatKho || "CSSD_CHUNG";
        if (!kpiGop[ktv]) kpiGop[ktv] = { code: ktv, ten: ktv, dat: 0, tong: 0 };
        if (x.ketQuaGiamSatKpi === "ĐẠT") kpiGop[ktv].dat += 1;
        if (x.ketQuaGiamSatKpi) kpiGop[ktv].tong += 1;
    });
    let sortedKpi = Object.values(kpiGop).sort((a,b) => b.dat - a.dat);
    if(sortedKpi.length === 0) {
        tbody.innerHTML = `<tr><td colspan="4" class="p-4 text-center italic text-slate-400">Hôm nay chưa có dữ liệu mẻ KPI nào.</td></tr>`;
    } else {
        tbody.innerHTML = sortedKpi.map((item, idx) => `
            <tr class="border-b text-xs hover:bg-slate-50 font-medium">
                <td class="p-3 text-center font-bold">${idx + 1}</td>
                <td class="p-3 font-mono font-bold text-sky-700">${item.code}</td>
                <td class="p-3 font-bold text-slate-800">${item.ten}</td>
                <td class="p-3 text-center"><span class="bg-emerald-100 text-emerald-800 px-2.5 py-1 rounded font-black">${item.dat}/${item.tong} Mẻ Đạt KPI</span></td>
            </tr>
        `).join('');
    }
}

function renderDashboardTiviRealtime() {
    let meRuaInDay = listGiaoDich.filter(x => x.rua_batchCode).length;
    let meHapInDay = listGiaoDich.filter(x => x.batchCode).length;
    let dangRua = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA").length;
    let dangHap = listGiaoDich.filter(x => x.status === "DANG_HAP").length;
    let sanSangXuat = listGiaoDich.filter(x => x.status === "CHO_XUAT").length;
    
    const today = new Date(); today.setHours(0,0,0,0);
    let quaHan = 0;
    listGiaoDich.forEach(x => {
        if (x.hsd && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT")) {
            if (new Date(x.hsd) < today) quaHan++;
        }
    });

    if (document.getElementById('tv_meRua')) document.getElementById('tv_meRua').innerText = meRuaInDay;
    if (document.getElementById('tv_meHap')) document.getElementById('tv_meHap').innerText = meHapInDay;
    if (document.getElementById('tv_meHap2')) document.getElementById('tv_meHap2').innerText = meHapInDay;
    if (document.getElementById('tv_dangRua')) document.getElementById('tv_dangRua').innerText = dangRua;
    if (document.getElementById('tv_dangHap')) document.getElementById('tv_dangHap').innerText = dangHap;
    if (document.getElementById('tv_khoVoKhuan')) document.getElementById('tv_khoVoKhuan').innerText = sanSangXuat;
    if (document.getElementById('tv_canhBaoHsd')) document.getElementById('tv_canhBaoHsd').innerText = quaHan;
}

function renderAdminInterface() {
    if (document.getElementById("cfg_pinAdmin")) document.getElementById("cfg_pinAdmin").value = thongTinMatKhauAdmin.adminPIN || "";
    if (document.getElementById("cfg_pinCSSD")) document.getElementById("cfg_pinCSSD").value = thongTinMatKhauAdmin.cssdPIN || "";
    if (document.getElementById("cfg_pinGuest")) document.getElementById("cfg_pinGuest").value = thongTinMatKhauAdmin.guestPIN || "";
    const MatrixRoles = ["CSSD", "KHOA", "GUEST"];
    const MatrixTabs = [
        { id: "khoaphong", name: "1. Cổng Báo Trả Đồ" }, { id: "thugom", name: "2. Xe Thu Gom" }, { id: "mayrua", name: "3. Quản Lý Mẻ Rửa" }, { id: "donggoi", name: "4. Làm Sạch & Gói" }, { id: "mayhap", name: "5. Quản Lý Mẻ Hấp" }, { id: "khovokhuan", name: "6. Kho Vô Khuẩn" }, { id: "quanlykho", name: "7. Tồn Kho Toàn Viện" }, { id: "danhmuc", name: "8. Giám Sát Tuổi Thọ" }, { id: "lichsuluanchuyen", name: "9. Nhật Ký Luân Chuyển" }, { id: "tracuu", name: "10. Cấu Hình Hệ Thống" }, { id: "performance", name: "11. Hiệu Suất KPI" }, { id: "dashboard_tv", name: "12. Màn Hình Tivi" }
    ];
    let htmlMatrix = "";
    MatrixTabs.forEach(tab => {
        htmlMatrix += `<tr class="border-b font-medium text-xs hover:bg-slate-50"><td class="p-3 font-bold text-slate-700">${tab.name}</td>`;
        MatrixRoles.forEach(role => {
            let isChecked = (cauHinhGiaoDien[role] && cauHinhGiaoDien[role].includes(tab.id)) ? "checked" : "";
            htmlMatrix += `<td class="p-3 text-center"><input type="checkbox" ${isChecked} onchange="updateGiaoDienMatrix('${role}', '${tab.id}', this)" class="w-4 h-4 rounded text-sky-600 focus:ring-sky-500"></td>`;
        });
        htmlMatrix += `</tr>`;
    });
    const tbodyMatrix = document.getElementById("bodyMaTranGiaoDien"); if (tbodyMatrix) tbodyMatrix.innerHTML = htmlMatrix;
    const trKhoa = document.getElementById("bangPhanQuyenKhoa");
    if(trKhoa) {
        if(danhSachKhoa.length === 0) { trKhoa.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-rose-500 font-bold italic">Danh sách Khoa trống.</td></tr>`; } 
        else { trKhoa.innerHTML = danhSachKhoa.map((k, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-slate-700 text-[11px]">${k.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-rose-600 text-sm bg-rose-50/50">${k.pin || '123'}</td><td class="p-3 text-center"><div class="flex items-center justify-center gap-2"><input type="text" id="pin-khoa-${index}" placeholder="PIN" class="w-24 p-1.5 text-center border border-slate-300 rounded text-xs font-bold"><button onclick="updatePINTrựcTiep(${index}, '${k.ten}')" class="bg-sky-600 text-white font-bold py-1.5 px-3 rounded shadow text-[10px]">ĐỔI PIN</button></div></td></tr>`).join(''); }
    }
    const tbKtv = document.getElementById("bangNhanVienCssd");
    if(tbKtv) {
        if(danhSachKtvCssd.length === 0) { tbKtv.innerHTML = `<tr><td colspan="4" class="p-4 text-center text-slate-400 italic">Chưa có Nhân viên.</td></tr>`; }
        else { tbKtv.innerHTML = danhSachKtvCssd.map((nv, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-sky-700 text-xs">${nv.code}</td><td class="p-3 font-bold text-slate-700 text-[11px]">${nv.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-sky-600 text-sm bg-sky-50/50">${nv.pin}</td><td class="p-3 text-center"><button onclick="xaoKtvCssd('${nv.code}')" class="text-rose-600 font-bold text-[10px]"><i class="fa-solid fa-trash-can mr-1"></i>XÓA</button></td></tr>`).join(''); }
    }
}

// =========================================================================
// 14. XỬ LÝ ĐỌC FILE EXCEL ĐA SHEET
// =========================================================================
document.addEventListener("DOMContentLoaded", () => {
    const fileInput = document.getElementById("excelFileInput");
    if (fileInput) {
        fileInput.addEventListener("change", (e) => {
            const file = e.target.files[0];
            if (!file) return;

            const reader = new FileReader();
            reader.onload = function(e) {
                try {
                    const data = new Uint8Array(e.target.result);
                    const workbook = XLSX.read(data, { type: 'array' });
                    
                    let updateData = {};
                    let logs = [];

                    workbook.SheetNames.forEach(sheetName => {
                        const worksheet = workbook.Sheets[sheetName];
                        const jsonData = XLSX.utils.sheet_to_json(worksheet);
                        if (jsonData.length === 0) return;

                        const firstRow = jsonData[0];
                        const keys = Object.keys(firstRow).map(k => k.trim().toUpperCase());

                        const coCotMaDC = keys.includes("MÃ DC");
                        if (coCotMaDC) {
                            let mapKhoa = {};
                            danhSachKhoa.forEach(k => {
                                mapKhoa[k.ten.trim().toUpperCase()] = {
                                    ten: k.ten.trim().toUpperCase(),
                                    pin: k.pin || "123",
                                    danhSachBo: []
                                };
                            });

                            jsonData.forEach(row => {
                                let rawKeyKhoa = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS (I)");
                                let rawKeyBo = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS CHUẨN");
                                let rawKeyMa = Object.keys(row).find(k => k.trim().toUpperCase() === "MÃ DC");

                                let tenKhoa = rawKeyKhoa ? row[rawKeyKhoa] : null;
                                let tenBo = rawKeyBo ? row[rawKeyBo] : null;
                                let maKhay = rawKeyMa ? row[rawKeyMa] : null;

                                if (tenKhoa && tenBo) {
                                    let keyKhoa = String(tenKhoa).trim().toUpperCase();
                                    let cleanBo = String(tenBo).trim().toUpperCase();
                                    let cleanMa = maKhay ? String(maKhay).trim().toUpperCase() : "CHUA_CO_ID";
                                    let chuoiDinhDangKhay = `${cleanBo} [ID:${cleanMa}]`;

                                    if (!mapKhoa[keyKhoa]) {
                                        mapKhoa[keyKhoa] = { ten: keyKhoa, pin: "123", danhSachBo: [] };
                                    }
                                    if (!mapKhoa[keyKhoa].danhSachBo.includes(chuoiDinhDangKhay)) {
                                        mapKhoa[keyKhoa].danhSachBo.push(chuoiDinhDangKhay);
                                    }
                                }
                            });

                            updateData.danhSachKhoa = Object.values(mapKhoa);
                            logs.push(`Cập nhật định biên cơ số cho ${updateData.danhSachKhoa.length} Khoa/Phòng`);
                        }

                        const coCotMaAesculap = keys.includes("AB 120/12");
                        if (coCotMaAesculap) {
                            updateData.databaseExcel = jsonData;
                            logs.push(`Nạp thành công ${jsonData.length} linh kiện chi tiết mâm Aesculap`);
                        }
                    });

                    if (Object.keys(updateData).length === 0) {
                        return showToast("Không tìm thấy cấu trúc dữ liệu phù hợp (Cột 'MÃ DC' hoặc 'AB 120/12') trong file Excel up lên!", "error");
                    }

                    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update(updateData)
                    .then(() => {
                        playSound('success');
                        showToast(`Đồng bộ thành công: ${logs.join(" | ")}!`, "success");
                        fileInput.value = ""; 
                        callRender();
                    }).catch(err => {
                        console.error("Lỗi cập nhật Firestore:", err);
                        showToast("Thất bại khi đẩy dữ liệu lên Cloud Firestore!", "error");
                    });

                } catch (err) {
                    console.error("Lỗi FileReader:", err);
                    showToast("Định dạng file không được hỗ trợ hoặc bị lỗi!", "error");
                }
            };
            reader.readAsArrayBuffer(file);
        });
    }
});
oki đã được rồi

Tuyệt vời quá anh Hùng! 🎉

Rất vui vì file app.js cập nhật mới đã chạy mượt mà và hoạt động đúng theo ý anh.

Hiện tại tính năng ký chữ ký điện tử trực tiếp trên Canvas và tự động hiển thị chữ ký/ngày giờ tiếp nhận lên Biên bản A4 đã được đồng bộ hoàn toàn giữa Cloud Firestore và bộ nhớ máy.

Nếu trong quá trình vận hành thực tế tại phòng CSSD hoặc kiểm thử trên máy in Bixolon / máy in văn phòng có thêm bất kỳ yêu cầu điều chỉnh nhỏ nào (như giao diện, định dạng tem, luồng duyệt mẻ hấp...), anh cứ nhắn em hỗ trợ ngay nhé!

Chúc anh một ngày làm việc hiệu quả và triển khai dự án CSSD số hóa thành công rực rỡ! 💪🏼✨

Tôi muốn tách phần này ra thành 1 tab riêng, đặt tên là "Truy xuất mẻ hấp". Tất cả các tab tôi thấy không cần đánh số thứ tự như bây giờ

Chào anh Hùng, em đã cập nhật toàn bộ file app.js để thực hiện đúng 2 yêu cầu của anh:

Bỏ toàn bộ số thứ tự ở tên các tab:

Loại bỏ các số 1., 2., 3.... trong ma trận phân quyền Admin và cấu hình giao diện. Tên các tab giờ đây hiển thị thuần túy, gọn gàng (ví dụ: Cổng Báo Trả Đồ, Xe Thu Gom, Quản Lý Mẻ Hấp, Kho Vô Khuẩn, Truy xuất mẻ hấp...).

Tách riêng tính năng "Truy xuất mẻ hấp":

Tách phần "Trạm truy vết nhiễm trùng khẩn cấp" và "Danh sách mâm dụng cụ trong mẻ truy vết" ra khỏi tab Danh mục/Cấu hình, chuyển thành một tab chức năng độc lập mang tên "Truy xuất mẻ hấp" (tracuu).

Cập nhật hàm renderAdminInterface() và renderTheoTabHienTai() để điều hướng độc lập, hiển thị chuẩn mực và không bị số thứ tự làm rườm rà.

Mã nguồn hoàn chỉnh app.js đã cập nhật:
JavaScript
// =========================================================================
// 1. KHỞI TẠO HỆ THỐNG & CẤU HÌNH BIẾN TOÀN CỤC (PN HOSPITAL)
// =========================================================================
const firebaseConfig = { 
    apiKey: "AIzaSyCxjdCTKHQlpm7SYbWCEws1HhcOaFp0LBA", 
    authDomain: "cssd-system-2878c.firebaseapp.com", 
    projectId: "cssd-system-2878c", 
    storageBucket: "cssd-system-2878c.firebasestorage.app", 
    messagingSenderId: "662377321937", 
    appId: "1:662377321937:web:001c092e10319547623cf0" 
};
firebase.initializeApp(firebaseConfig); 
const db = firebase.firestore();

let thongTinMatKhauAdmin = { adminPIN: "admin2026", cssdPIN: "cssd2026", guestPIN: "guest2026" };
let cauHinhGiaoDien = {
    "ADMIN": ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'],
    "CSSD": ['thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','dashboard_tv'], 
    "KHOA": ['khoaphong','quanlykho','lichsuluanchuyen','tracuu'], 
    "GUEST": ['quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv']
};

let currentRole = "", loginUserCode = "";
let danhSachKhoa = [], listGiaoDich = [], gioHangTam = [], danhSachKtvCssd = [], databaseExcel = [];
let html5QrCode = null; let targetInputIdForScan = ""; let idDangDongGoi = null;
let activeTab = 'thugom'; let renderTimeout = null;
let duLieuAnhBiTamBase64 = ""; let maLoTruyVetToanCuc = ""; 
let gioHangXuatKho = []; let gioKhaySuDungTam = []; 
let currentKiemDemData = null;

// Biến hỗ trợ vẽ chữ ký điện tử
let canvasSignature = null;
let ctxSignature = null;
let isDrawingSignature = false;
let dsIdKhayChoKyNhan = [];

// Cấu hình máy hấp & máy rửa (Belimed WD250)
const cauHinhMayHap = { "Hấp hơi nước": ["A1", "A2", "A3", "A4"], "Hấp H2O2 (Plasma)": ["P1", "P2"], "Khử khuẩn EO": ["EO1", "EO2"] };
const cauHinhMayRua = {
    "Máy rửa khử khuẩn tự động": ["Belimed WD250"],
    "Máy rửa sóng siêu âm": ["US1"],
    "Bồn rửa thủ công": ["MAN1"]
};
const danhMucHoaChatRua = [
    { ten: "Cidezyme (Enzym phân hủy protein)", lieuLuong: "8 ml/L" },
    { ten: "Sekusept Active (Khử khuẩn mức độ cao)", lieuLuong: "10 ml/L" },
    { ten: "Smeg (Nước rửa trung tính chuyên dụng)", lieuLuong: "5 ml/L" },
    { ten: "Aniosyme DD1 (Làm sạch & khử trùng sơ bộ)", lieuLuong: "5 ml/L" }
];

function getTodayDateStr() { 
    const d = new Date(); 
    return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`; 
}

function playSound(type) {
    try {
        const AudioContext = window.AudioContext || window.webkitAudioContext;
        if (!AudioContext) return;
        const ctx = new AudioContext();
        const osc = ctx.createOscillator();
        const gain = ctx.createGain();
        osc.connect(gain); gain.connect(ctx.destination);
        if (type === 'success') {
            osc.type = 'sine'; osc.frequency.setValueAtTime(800, ctx.currentTime);
            gain.gain.setValueAtTime(0.3, ctx.currentTime);
            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.15);
            osc.start(); osc.stop(ctx.currentTime + 0.15);
        } else {
            osc.type = 'sawtooth'; osc.frequency.setValueAtTime(250, ctx.currentTime);
            gain.gain.setValueAtTime(0.3, ctx.currentTime);
            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.4);
            osc.start(); osc.stop(ctx.currentTime + 0.4);
        }
    } catch(e) {}
}

// =========================================================================
// 2. REALTIME LISTENER (FIRESTORE SYNC)
// =========================================================================
db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").onSnapshot(doc => { 
    try {
        if (doc.exists) { 
            let res = doc.data(); 
            danhSachKhoa = res.danhSachKhoa || []; 
            danhSachKtvCssd = res.danhSachKtvCssd || []; 
            databaseExcel = res.databaseExcel || []; 
            if(res.thongTinMatKhauAdmin) thongTinMatKhauAdmin = Object.assign(thongTinMatKhauAdmin, res.thongTinMatKhauAdmin); 
            if(res.cauHinhGiaoDien) cauHinhGiaoDien = Object.assign(cauHinhGiaoDien, res.cauHinhGiaoDien);
        } 
        initSelects();
        if(activeTab === 'khoaphong') loadBoDungCuTheoKhoa(); 
        renderAdminInterface(); 
        if(currentRole) apDungPhanQuyenGiaoDien(currentRole);
        taiDanhMucLinhKienChuan();
        callRender();
    } catch(err) { console.error("Lỗi đồng bộ danh mục:", err); }
});

db.collection("phieuGiaoNhan").orderBy("id", "desc").limit(1000).onSnapshot(snap => { 
    listGiaoDich = []; 
    snap.forEach(doc => { let d = doc.data(); d.firestoreId = doc.id; listGiaoDich.push(d); }); 
    callRender();
});

function callRender() { clearTimeout(renderTimeout); renderTimeout = setTimeout(() => { renderTheoTabHienTai(); }, 100); }

// =========================================================================
// 3. XỬ LÝ ĐĂNG NHẬP & PHÂN QUYỀN HỆ THỐNG
// =========================================================================
function toggleLoginFields() {
    const r = document.getElementById("login_role").value;
    document.getElementById("field_khoa").classList.toggle("hidden", r !== "KHOA");
    document.getElementById("field_nhanvien_cssd").classList.toggle("hidden", r !== "CSSD");
}

function checkLogin() {
    const role = document.getElementById("login_role").value; 
    const pass = document.getElementById("login_pass").value; 
    let pA = thongTinMatKhauAdmin.adminPIN || "admin2026"; 
    let pC = thongTinMatKhauAdmin.cssdPIN || "cssd2026"; 
    let pG = thongTinMatKhauAdmin.guestPIN || "guest2026"; 
     
    if (role === "ADMIN" && pass === pA) { 
        currentRole = "ADMIN"; loginUserCode = "ADMIN"; document.getElementById("nav_user_info").innerText = "ADMINISTRATOR";
        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');
        apDungPhanQuyenGiaoDien("ADMIN");
        switchTab('lichsuluanchuyen');
    } else if (role === "KHOA") { 
        const khoaSelect = document.getElementById("login_khoa").value; 
        let found = danhSachKhoa.find(x => x.ten === khoaSelect); 
        if (pass === (found ? found.pin : "123")) { 
            currentRole = "KHOA"; loginUserCode = khoaSelect; document.getElementById("nav_user_info").innerText = khoaSelect; 
            if (document.getElementById("khoa_selKhoa")) {
                document.getElementById("khoa_selKhoa").value = khoaSelect;
                document.getElementById("khoa_selKhoa").disabled = true;
            }
            document.body.classList.remove('guest-mode');
            apDungPhanQuyenGiaoDien("KHOA");
            switchTab('khoaphong');
        } else { return showToast("Sai mã PIN Khoa!", "error"); } 
    } else if (role === "CSSD") {
        const nvCode = document.getElementById("login_nv_cssd").value;
        if(!nvCode && pass === pC) { currentRole = "CSSD"; loginUserCode = "CSSD_CHUNG"; document.getElementById("nav_user_info").innerText = "CSSD GENERAL"; } 
        else if (nvCode) {
            let nv = danhSachKtvCssd.find(x => x.code === nvCode);
            if (nv && pass === nv.pin) { currentRole = "CSSD"; loginUserCode = nv.ten; document.getElementById("nav_user_info").innerText = `${nvCode} - ${nv.ten}`; }
            else { return showToast("Sai mã PIN của nhân viên này!", "error"); }
        } else { return showToast("Vui lòng chọn Nhân viên hoặc nhập PIN Backup!", "error"); }
        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');
        apDungPhanQuyenGiaoDien("CSSD");
        let tabsDuocPhep = cauHinhGiaoDien["CSSD"] || [];
        if(tabsDuocPhep.includes('thugom')) switchTab('thugom');
        else if (tabsDuocPhep.length > 0) switchTab(tabsDuocPhep[0]);
    } else if (role === "GUEST") {
        if(pass === pG) {
            currentRole = "GUEST"; loginUserCode = "GUEST"; document.getElementById("nav_user_info").innerText = "KHÁCH THAM QUAN";
            document.body.classList.add('guest-mode'); 
            apDungPhanQuyenGiaoDien("GUEST");
            switchTab('quanlykho');
        } else { return showToast("Sai mã PIN Khách tham quan!", "error"); }
    } else { return showToast("Sai thông tin đăng nhập!", "error"); }
    document.getElementById("login-screen").classList.add("hidden"); document.getElementById("main-app").classList.remove("hidden");
}

function anTatCaHeadersVaMenus() {
    document.getElementById('header-lamsang')?.classList.add('hidden');
    document.getElementById('header-vanhanh')?.classList.add('hidden');
    document.getElementById('header-dulieu')?.classList.add('hidden');
    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => {
        document.getElementById('menu-' + x)?.classList.add('hidden');
    });
}

function apDungPhanQuyenGiaoDien(role) {
    anTatCaHeadersVaMenus();
    if (role === "ADMIN") {
        ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(tab => {
            document.getElementById('menu-' + tab)?.classList.remove('hidden');
        });
        document.getElementById('header-lamsang')?.classList.remove('hidden');
        document.getElementById('header-vanhanh')?.classList.remove('hidden');
        document.getElementById('header-dulieu')?.classList.remove('hidden');
        return;
    }
    let tabsDuocPhep = cauHinhGiaoDien[role] || [];
    tabsDuocPhep.forEach(tab => { document.getElementById('menu-' + tab)?.classList.remove('hidden'); });
    if (tabsDuocPhep.includes('khoaphong')) document.getElementById('header-lamsang')?.classList.remove('hidden');
    if (['thugom','mayrua','donggoi','mayhap','khovokhuan'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-vanhanh')?.classList.remove('hidden');
    if (['quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-dulieu')?.classList.remove('hidden');
}

function switchTab(t) { 
    if (currentRole !== "ADMIN") {
        let tabsDuocPhep = cauHinhGiaoDien[currentRole] || [];
        if(!tabsDuocPhep.includes(t)) { return showToast("Tài khoản không có quyền truy cập!", "error"); }
    }
    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => { 
        document.getElementById('tab-'+x)?.classList.add('hidden'); 
        document.getElementById('menu-'+x)?.classList.remove('sidebar-item-active'); 
    }); 
    document.getElementById('tab-'+t)?.classList.remove('hidden'); 
    document.getElementById('menu-'+t)?.classList.add('sidebar-item-active'); 
    activeTab = t; 
    if (t === 'mayrua') { setTimeout(() => { capNhatDanhSachMaMayRua(); }, 50); }
    if (t === 'mayhap') { setTimeout(() => { tuDongTaoMaLoMeHap(); }, 50); }
    callRender(); 
}

// =========================================================================
// 4. TRẠM 1: CỔNG LÂM SÀNG & HÀNH ĐỘNG ĐỘC LẬP (TRẢ ĐỒ BẨN / YÊU CẦU ĐỒ SẠCH)
// =========================================================================
function loadBoDungCuTheoKhoa() { 
    let k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 
    let list = document.getElementById("listBoDungCu"); 
    if(!list) return; list.innerHTML = ""; 
    let f = danhSachKhoa.find(x => (x.ten || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase()); 
    if (f && f.danhSachBo) {
        let htmlOptions = "";
        f.danhSachBo.forEach(x => {
            if (x) {
                let chuoiX = String(x);
                let tenBoThuanTuy = chuoiX.includes(" [ID:") ? chuoiX.split(" [ID:")[0] : chuoiX;
                htmlOptions += `<option value="${chuoiX}"><option value="${tenBoThuanTuy}">`;
            }
        });
        list.innerHTML = htmlOptions; 
    }
}

function themVaoGio() { 
    let val = document.getElementById("khoa_inpMaBo").value.trim().toUpperCase(); if(!val) return; 
    if (gioHangTam.some(x => x.maMacDinh === val)) { document.getElementById("khoa_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách chờ!", "error"); } 
    let tenGoc = val.includes("[ID:") ? val.split(" [ID:")[0] : val; 
    gioHangTam.push({bo: tenGoc, maMacDinh: val, slYeuCau: 1}); 
    document.getElementById("khoa_inpMaBo").value = ""; 
    renderGioHang(); 
}

function renderGioHang() { 
    let khuVuc = document.getElementById("khuVucGioHang"); if(khuVuc) khuVuc.classList.toggle("hidden", gioHangTam.length===0); 
    let bHang = document.getElementById("bangGioHang");
    if(bHang) {
        bHang.innerHTML = gioHangTam.map(i => `<tr><td class="p-2.5 font-bold text-sky-700 text-[11px]">${i.bo}</td></tr>`).join(''); 
    }
    let badge = document.getElementById("badgeGioHang");
    if(badge) badge.innerText = `${gioHangTam.length} món`;
}

function khoaGuiPhieuTraBatches() { 
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 
    if(!k) return showToast("Vui lòng chọn Khoa trước!"); 
    if(gioHangTam.length === 0) return showToast("Vui lòng quét hoặc chọn mâm bẩn muốn trả trước!", "error"); 
     
    let p=[]; 
    gioHangTam.forEach((i, idx) => {
        p.push(
            db.collection("phieuGiaoNhan").add({ 
                id: Date.now() + idx, 
                ngayTao: getTodayDateStr(), 
                time: new Date().toLocaleTimeString('vi-VN'), 
                khoa: k, 
                bo: i.bo, 
                maMacDinh: i.maMacDinh, 
                slYeuCau: 1, 
                slThucTe: 1, 
                status: "CHO_THU" 
            })
        );
    }); 
     
    Promise.all(p).then(() => { 
        clearGioHang(); 
        playSound('success');
        showToast("Đã gửi lệnh báo trả đồ bẩn về CSSD!", "success"); 
        callRender(); 
    }); 
}

function khoaGuiYeuCauCapPhat() {
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;
    if(!k) return showToast("Vui lòng chọn Khoa/Phòng gửi yêu cầu!", "error");
    
    const tenBoChon = document.getElementById("khoa_inpMaBo")?.value.trim().toUpperCase();
    if(!tenBoChon) return showToast("Vui lòng chọn hoặc nhập tên mâm cần cấp mới!", "error");
    
    let tenMâmSạch = tenBoChon.split(" [ID:")[0];
    
    db.collection("phieuGiaoNhan").add({
        id: Date.now(),
        ngayTao: getTodayDateStr(),
        time: new Date().toLocaleTimeString('vi-VN'),
        khoa: k,
        bo: tenMâmSạch,
        maMacDinh: "CHO_CAP_PHAT", 
        slYeuCau: 1,
        status: "CHO_CAP_PHAT", 
        nvYeuCau: loginUserCode || "Điều dưỡng khoa"
    }).then(() => {
        playSound('success');
        showToast(`Đã gửi yêu cầu cấp phát mâm ${tenMâmSạch} thành công!`, "success");
        if(document.getElementById("khoa_inpMaBo")) document.getElementById("khoa_inpMaBo").value = "";
        callRender();
    });
}

// =========================================================================
// 5. TRẠM 2: XE THU GOM & KIỂM ĐẾM CHI TIẾT LINH KIỆN
// =========================================================================
function moPopupKiemDem(id) { 
    let item = listGiaoDich.find(x => x.firestoreId === id); 
    if(!item) return showToast("Không tìm thấy thông tin dòng chỉ định!", "error");

    currentKiemDemData = {
        id: id,
        tenBoDungCu: item.bo ? String(item.bo).split(" [ID:")[0] : "Chưa rõ mâm",
        khoaYeuCau: item.khoa || "Chưa rõ khoa",
        ghiChuLamSang: item.ghiChu || ""
    }; 

    document.getElementById('popBo').innerText = currentKiemDemData.tenBoDungCu;
    document.getElementById('popKhoa').innerText = currentKiemDemData.khoaYeuCau;
    document.getElementById('popGhiChu').value = currentKiemDemData.ghiChuLamSang;
    document.getElementById('popGhiChu').classList.remove('border-rose-500', 'ring-2', 'ring-rose-200');

    let itemsInBo = databaseExcel.filter(x => { 
        let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; 
        return String(boName).trim().toUpperCase() === String(currentKiemDemData.tenBoDungCu).trim().toUpperCase(); 
    });

    let checklistSoBo = [];
    if(itemsInBo.length > 0) {
        checklistSoBo = itemsInBo.map(ct => {
            let tenDc = ct['Tên TS chuẩn'] || ct['Tên Dụng Cụ Chi Tiết'] || ct['Tên dụng cụ chi tiết'] || ct['Tên dụng cụ'] || ct['Chi tiết'] || ct['Dụng cụ'] || ct['Tên Chi Tiết'] || ct['NAME'] || "Dụng cụ";
             
            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {
                tenDc = "Dụng cụ chuẩn mâm";
            }
             
            let sl = parseInt(ct['Số lượng'] || ct['SL'] || ct['Số Lượng'] || 1) || 0;
            return { ten: tenDc, slChuan: sl, slThuc: sl, tinhTrang: "ĐỦ" };
        }).filter(line => line.slChuan > 0); 
    } else {
        checklistSoBo = [{ ten: "Dụng cụ nguyên bộ (Chưa phân rã cấu hình)", slChuan: 1, slThuc: 1, tinhTrang: "ĐỦ" }];
    }

    currentKiemDemData.linhKienKiemDem = checklistSoBo;
    renderChecklistLinhKien();
    document.getElementById('popupKiemDem').classList.remove('hidden'); 
}

function renderChecklistLinhKien() {
    const container = document.getElementById('popKiemDemChecklist');
    if(!container) return; container.innerHTML = ""; 

    currentKiemDemData.linhKienKiemDem.forEach((item, index) => {
        let bgBadge = "bg-emerald-50 text-emerald-700 border-emerald-200";
        if (item.tinhTrang === "THIẾU") bgBadge = "bg-rose-50 text-rose-700 border-rose-200 animate-pulse";
        if (item.tinhTrang === "HỎNG") bgBadge = "bg-amber-50 text-amber-700 border-amber-200";

        const row = document.createElement('div');
        row.className = "flex items-center justify-between py-3 px-2 transition-all hover:bg-slate-50";
        row.innerHTML = `
            <div class="flex-1 pr-2">
                <p class="text-xs font-bold text-slate-700">${item.ten}</p>
                <p class="text-[10px] text-slate-400 font-medium mt-0.5">Cơ số chuẩn: <span class="font-bold text-slate-600">${item.slChuan}</span></p>
            </div>
            <div class="flex items-center gap-3 mr-4">
                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, -1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">-</button>
                <span class="w-6 text-center text-xs font-black text-slate-800">${item.slThuc}</span>
                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, 1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">+</button>
            </div>
            <div>
                <select onchange="thayDoiTinhTrangLinhKien(${index}, this.value)" class="text-[11px] font-black border rounded-lg px-2 py-1.5 shadow-sm transition-all ${bgBadge}">
                    <option value="ĐỦ" ${item.tinhTrang === 'ĐỦ' ? 'selected' : ''}>🟢 Đủ đồ</option>
                    <option value="THIẾU" ${item.tinhTrang === 'THIẾU' ? 'selected' : ''}>🔴 Thiếu đồ</option>
                    <option value="HỎNG" ${item.tinhTrang === 'HỎNG' ? 'selected' : ''}>🟡 Hỏng đồ</option>
                </select>
            </div>
        `;
        container.appendChild(row);
    });
}

function thayDoiSoLuongLinhKien(index, thayDoi) {
    const item = currentKiemDemData.linhKienKiemDem[index];
    const targetSl = item.slThuc + thayDoi;
    if (targetSl >= 0 && targetSl <= item.slChuan * 2) {
        item.slThuc = targetSl;
        if (item.slThuc < item.slChuan) { item.tinhTrang = "THIẾU"; } 
        else if (item.slThuc === item.slChuan) { item.tinhTrang = "ĐỦ"; }
        renderChecklistLinhKien();
    }
}

function thayDoiTinhTrangLinhKien(index, value) {
    currentKiemDemData.linhKienKiemDem[index].tinhTrang = value;
    if (value === "THIẾU" && currentKiemDemData.linhKienKiemDem[index].slThuc === currentKiemDemData.linhKienKiemDem[index].slChuan) {
        currentKiemDemData.linhKienKiemDem[index].slThuc = Math.max(0, currentKiemDemData.linhKienKiemDem[index].slChuan - 1);
    } else if (value === "ĐỦ") {
        currentKiemDemData.linhKienKiemDem[index].slThuc = currentKiemDemData.linhKienKiemDem[index].slChuan;
    }
    renderChecklistLinhKien();
}

function closePopupKiemDem() { document.getElementById("popupKiemDem").classList.add("hidden"); currentKiemDemData = null; }

async function saveKiemDem() { 
    if(!currentKiemDemData) return; 
    const ghiChuValue = document.getElementById("popGhiChu").value.trim();
    let coSuCoChenhLech = false;
    currentKiemDemData.linhKienKiemDem.forEach(item => {
        let thuc = Number(item.slThuc) || 0;
        let chuan = Number(item.slChuan) || 0;
        if (item.tinhTrang === "THIẾU" || item.tinhTrang === "HỎNG" || thuc < chuan) { coSuCoChenhLech = true; }
    });

    if (coSuCoChenhLech && ghiChuValue === "") {
        playSound('error');
        alert("⚠️ PHÁT HIỆN SỰ CỐ CHÊNH LỆCH LINH KIỆN!\nAnh/Chị bắt buộc phải nhập lý do chênh lệch hoặc số biên bản sự cố trước khi 'Chốt và Chuyển Rửa'.");
        document.getElementById('popGhiChu').focus();
        document.getElementById('popGhiChu').classList.add('border-rose-500', 'ring-2', 'ring-rose-200');
        return;
    }

    try {
        await db.collection("phieuGiaoNhan").doc(currentKiemDemData.id).update({ 
            status: "DANG_RUA", ghiChu: ghiChuValue,
            ktvThuGom: loginUserCode || "CSSD_CHUNG",
            thoiGianDoiSoat: new Date().toLocaleTimeString('vi-VN'),
            coSuCoChenhLech: coSuCoChenhLech,
            chiTietChecklistLinhKien: currentKiemDemData.linhKienKiemDem
        });
        showToast("Đã đối soát xong! Mâm đồ đã chuyển sang Trạm Rửa.", "success"); 
        closePopupKiemDem(); callRender(); 
    } catch(err) { showToast("Lỗi kết nối Firebase nội bộ!", "error"); }
}

function clearGioHang() { gioHangTam = []; renderGioHang(); }

// =========================================================================
// 6. MODULE: QUẢN LÝ MẺ RỬA KHỬ KHUẨN (BELIMED WD250)
// =========================================================================
function toggleSelectAllRua() { let checked = document.getElementById('selectAllRua').checked; document.querySelectorAll('.rua-checkbox').forEach(cb => cb.checked = checked); }
function toggleSelectAllNghiemThuRua() { let checked = document.getElementById('selectAllNghiemThuRua').checked; document.querySelectorAll('.nghiemthurua-checkbox').forEach(cb => cb.checked = checked); }

function capNhatDanhSachMaMayRua() {
    const loaiRua = document.getElementById("rua_loaiRua")?.value;
    const selectMay = document.getElementById("rua_maySo");
    if (!selectMay || !loaiRua) return;
    selectMay.innerHTML = "";
    if (cauHinhMayRua[loaiRua]) {
        cauHinhMayRua[loaiRua].forEach(may => {
            selectMay.innerHTML += `<option value="${may}">${may}</option>`;
        });
    }
    tuDongTaoMaLoMeRua();
}

function tuDongTaoMaLoMeRua() {
    const maMay = document.getElementById("rua_maySo")?.value || "Belimed WD250";
    const maMayVietTat = (maMay === "Belimed WD250") ? "BL" : maMay;
    const now = new Date();
    const yy = String(now.getFullYear()).slice(-2);
    const mm = String(now.getMonth() + 1).padStart(2, '0');
    const dd = String(now.getDate()).padStart(2, '0');
    const ngayChuoi = `${yy}${mm}${dd}`;

    let cacMeTrongNgay = listGiaoDich.filter(x => x.rua_batchCode && x.rua_batchCode.startsWith(maMayVietTat + ngayChuoi));
    let soMeMax = 0;
    cacMeTrongNgay.forEach(x => {
        let phanDuoi = x.rua_batchCode.split("_")[1];
        if (phanDuoi) {
            let num = parseInt(phanDuoi);
            if (num > soMeMax) soMeMax = num;
        }
    });
    let meTiepTheo = soMeMax + 1;
    const chuoiMe = String(meTiepTheo).padStart(2, '0');
    const batchIdHoanChinh = `${maMayVietTat}${ngayChuoi}_${chuoiMe}`;

    if (document.getElementById("rua_meSo")) document.getElementById("rua_meSo").value = chuoiMe;
    if (document.getElementById("rua_batchId")) document.getElementById("rua_batchId").value = batchIdHoanChinh;
}

function xacNhanMeRua() {
    let checkboxes = document.querySelectorAll('.rua-checkbox:checked');
    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm dụng cụ bẩn để xếp vào máy!", "error");
    
    let loaiRua = document.getElementById("rua_loaiRua").value;
    let maMay = document.getElementById("rua_maySo").value;
    let batchCode = document.getElementById("rua_batchId").value;
    let chuKy = document.getElementById("rua_chuKy").value;
    let hoaChat = document.getElementById("rua_hoaChat").value;
    let lieuLuong = document.getElementById("rua_lieuLuong").value || "Theo chuẩn";
    const ngayHomNay = getTodayDateStr();

    let p = [];
    checkboxes.forEach(cb => {
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "TRONG_BUONG_RUA",
                rua_batchCode: batchCode,
                rua_ngayRua: ngayHomNay,
                rua_mayRua: maMay,
                rua_chuKy: chuKy,
                rua_hoaChat: hoaChat,
                rua_lieuLuong: lieuLuong,
                rua_timeBatDau: new Date().toLocaleTimeString('vi-VN')
            })
        );
    });
    Promise.all(p).then(() => {
        playSound('success');
        showToast(`Bắt đầu chạy mẻ máy rửa: ${batchCode}`, "success");
        tuDongTaoMaLoMeRua();
        callRender();
    });
}

function duyetSachMeRuaHangLoat() {
    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');
    if(checkboxes.length === 0) return showToast("Chọn mâm đã rửa sạch để nghiệm thu!", "error");
    
    let ketQuaTest = document.getElementById("rua_testDoSach").value;
    let p = [];
    checkboxes.forEach(cb => {
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "CHO_DONG_GOI",
                rua_ketQuaTestSach: ketQuaTest,
                rua_timeKetThuc: new Date().toLocaleTimeString('vi-VN')
            })
        );
    });
    Promise.all(p).then(() => {
        playSound('success');
        showToast("Xác nhận nghiệm thu đạt chất lượng làm sạch!", "success");
        callRender();
    });
}

function tuChoiMeRuaHangLoat() {
    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');
    if(checkboxes.length === 0) return showToast("Chọn mâm không đạt!", "error");
    
    let p = [];
    checkboxes.forEach(cb => {
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "DANG_RUA",
                rua_batchCode: firebase.firestore.FieldValue.delete(),
                ghiChu: "Làm sạch chưa đạt, yêu cầu rửa lại."
            })
        );
    });
    Promise.all(p).then(() => {
        playSound('error');
        showToast("Đã trả các mâm không đạt về hàng đợi chờ rửa lại!", "success");
        callRender();
    });
}

// =========================================================================
// 7. TRẠM 4: LÀM SẠCH VÀ ĐÓNG GÓI
// =========================================================================
function moPopupDongGoi(id) { idDangDongGoi = id; let item = listGiaoDich.find(x => x.firestoreId === id); if(item) { document.getElementById("popDG_Bo").innerText = item.bo; tinhHanSuDung(); document.getElementById("popupDongGoi").classList.remove("hidden"); } }
function closePopupDongGoi() { document.getElementById("popupDongGoi").classList.add("hidden"); }
function tinhHanSuDung() { let val = document.getElementById("popDG_Loai").value.split("|"); let days = parseInt(val[1]); let dateHSD = new Date(); dateHSD.setDate(dateHSD.getDate() + days); let p = document.getElementById("popDG_Han"); p.innerText = dateHSD.toLocaleDateString('vi-VN'); p.dataset.dateDB = dateHSD.toISOString().split('T')[0]; }
function chotDongGoi() { if(!idDangDongGoi) return; let chatLieuTen = document.getElementById("popDG_Loai").value.split("|")[0]; db.collection("phieuGiaoNhan").doc(idDangDongGoi).update({ status: "CHO_HAP", chatLieu: chatLieuTen, hsd: document.getElementById("popDG_Han").dataset.dateDB }).then(() => { showToast("Đã đóng gói, chuyển chờ hấp!", "success"); closePopupDongGoi(); callRender(); }); }

// =========================================================================
// 8. TRẠM 5: VẬN HÀNH MẺ HẤP & DUYỆT NHẬP KHO VÔ KHUẨN
// =========================================================================
function toggleSelectAllHap() { let checked = document.getElementById('selectAllHap').checked; document.querySelectorAll('.hap-checkbox').forEach(cb => cb.checked = checked); }
function capNhatDanhSachMaMay() { const loaiHap = document.getElementById("hap_loaiHap")?.value; const selectMay = document.getElementById("hap_maySo"); if (!selectMay || !loaiHap) return; selectMay.innerHTML = ""; if (cauHinhMayHap[loaiHap]) { cauHinhMayHap[loaiHap].forEach(may => { selectMay.innerHTML += `<option value="${may}">${may}</option>`; }); } tuDongTaoMaLoMeHap(); }

function tuDongTaoMaLoMeHap() {
    const maMay = document.getElementById("hap_maySo")?.value || "A1"; const now = new Date(); const yy = String(now.getFullYear()).slice(-2); const mm = String(now.getMonth() + 1).padStart(2, '0'); const dd = String(now.getDate()).padStart(2, '0'); const ngayChuoi = `${yy}${mm}${dd}`;
    let cacMeTrongNgay = listGiaoDich.filter(x => x.batchCode && x.batchCode.startsWith(maMay + ngayChuoi)); let soMeMax = 0;
    cacMeTrongNgay.forEach(x => { let phanDuoi = x.batchCode.split("_")[1]; if (phanDuoi) { let num = parseInt(phanDuoi); if (num > soMeMax) soMeMax = num; } });
    let meTiepTheo = soMeMax + 1; if (meTiepTheo > 99) meTiepTheo = 1; const chuoiMe = String(meTiepTheo).padStart(2, '0'); const soLanDone = `${ngayChuoi}_${chuoiMe}`; const batchIdHoanChinh = `${maMay}${soLanDone}`;
    if (document.getElementById("hap_meSo")) document.getElementById("hap_meSo").value = chuoiMe;
    if (document.getElementById("hap_batchId")) document.getElementById("hap_batchId").value = batchIdHoanChinh;
}

function xacNhanMeHap() { 
    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); 
    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 
    
    let loaiHap = document.getElementById("hap_loaiHap").value; 
    let maMay = document.getElementById("hap_maySo").value; 
    let batchCode = document.getElementById("hap_batchId").value; 
    let chuKyNhiet = document.getElementById("hap_nhietDo").value; 
    let apSuat = document.getElementById("hap_apSuat").value || "N/A"; 
    let coKemBI = document.getElementById("hap_hasBI") ? document.getElementById("hap_hasBI").checked : false;
    const ngayHomNay = getTodayDateStr(); 
    let bayGio = new Date(); 
    let thoiGianDuKienXong = new Date(bayGio.getTime() + 45 * 60 * 1000);

    let p = []; 
    checkboxes.forEach(cb => { 
        if(cb.id === 'selectAllHap') return; 
        
        let thongTinLo = { 
            loaiHap: loaiHap, 
            maMay: maMay, 
            chuKyNhiet: chuKyNhiet, 
            apSuat: apSuat, 
            thoiGianBatDau: bayGio.toLocaleTimeString('vi-VN'), 
            giamSatChatLuong: { 
                chiThiHoaHoc: "ĐẠT", 
                laMeTestSinhHocGoc: coKemBI, 
                ketQuaSinhHoc: coKemBI ? "ĐANG CHỜ MÁY Ủ ĐỌC BI (20 PHÚT)" : "KẾ THỪA ĐẦU NGÀY" 
            } 
        };
        
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({ 
                status: "DANG_HAP", 
                batchCode: batchCode, 
                ngayHapRealtime: ngayHomNay, 
                hasBI: coKemBI, 
                thoiGianKetThucChuTrinh: thoiGianDuKienXong.toISOString(), 
                thongTinLoHap: thongTinLo 
            })
        ); 
    }); 

    Promise.all(p).then(() => { 
        playSound('success');
        showToast(`Kích hoạt lò thành công! Lô: ${batchCode}`, "success"); 
        if(document.getElementById("hap_hasBI")) document.getElementById("hap_hasBI").checked = false; 
        duLieuAnhBiTamBase64 = ""; 
        const fileInp = document.getElementById("input_anhBI"); 
        if(fileInp) fileInp.value = ""; 
        tuDongTaoMaLoMeHap(); 
        callRender(); 
    }).catch(err => {
        showToast("Lỗi khi cập nhật mẻ hấp: " + err.message, "error");
    }); 
}

function toggleSelectAllNghiemThu() { let checked = document.getElementById('selectAllNghiemThu').checked; document.querySelectorAll('.nghiemthu-checkbox').forEach(cb => cb.checked = checked); }

function kiemTraQuyenDuyetMeHap() {
    const nutDuyetDat = document.querySelector("button[onclick='nhapKhoHangLoat()']"); if (!nutDuyetDat) return;
    let checkboxesNghiemThu = document.querySelectorAll('.nghiemthu-checkbox:checked'); let phaiChoKetQuaBI = false;
    checkboxesNghiemThu.forEach(cb => { if (cb.getAttribute('data-hasbi') === 'true') phaiChoKetQuaBI = true; });

    if (phaiChoKetQuaBI && !duLieuAnhBiTamBase64) {
        nutDuyetDat.disabled = true;
        nutDuyetDat.className = "w-full bg-slate-300 text-slate-500 font-black py-2.5 rounded text-xs shadow-md cursor-not-allowed flex items-center justify-center gap-2";
        nutDuyetDat.innerHTML = `<i class="fa-solid fa-lock text-slate-400"></i> CHỜ MINH CHỨNG BI (MÁY Ủ ĐỌC 20 PHÚT)`;
    } else {
        nutDuyetDat.disabled = false;
        nutDuyetDat.className = "w-full bg-emerald-600 hover:bg-emerald-700 text-white font-black py-2.5 rounded text-xs shadow-md flex items-center justify-center gap-2 transition-all cursor-pointer";
        nutDuyetDat.innerHTML = `<i class="fa-solid fa-check-double mr-1"></i> DUYỆT ĐẠT & CHO VÀO KHO LƯU TRỮ CHUNG`;
    }
}

function nhapKhoHangLoat() { 
    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!"); 
    const bayGio = new Date(); let p = []; 
     
    checkboxes.forEach(cb => { 
        if(cb.id === 'selectAllNghiemThu') return; 
        let itemData = listGiaoDich.find(x => x.firestoreId === cb.value); if(!itemData) return;
        let coKemBI = itemData.hasBI === true;
        let thoiGianHapXong = itemData.thoiGianKetThucChuTrinh ? new Date(itemData.thoiGianKetThucChuTrinh) : new Date(bayGio.getTime() - 45 * 60 * 1000);
        let soPhutThucTe = Math.floor((bayGio - thoiGianHapXong) / (1000 * 60)); 
        let soPhutTinhKPI = coKemBI ? Math.max(0, soPhutThucTe - 20) : soPhutThucTe;
        let trangThaiKPI = (soPhutTinhKPI > 30) ? "KHÔNG ĐẠT" : "ĐẠT";

        let bgGiamSat = itemData.thongTinLoHap?.giamSatChatLuong || {};
        let updateGiamSat = {
            ...bgGiamSat,
            ketQuaSinhHoc: coKemBI ? "ÂM TÍNH (ĐẠT)" : "KẾ THỪA ĐẦU NGÀY",
            minhChungAnhBase64: coKemBI ? duLieuAnhBiTamBase64 : (bgGiamSat.minhChungAnhBase64 || "")
        };

        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "CHO_XUAT", thoiGianKetLuan: bayGio.toISOString(), thoiGianDocTestBiPhut: soPhutThucTe, 
                thoiGianTinhKpiPhut: soPhutTinhKPI, ketQuaGiamSatKpi: trangThaiKPI, "thongTinLoHap.giamSatChatLuong": updateGiamSat
            })
        ); 
    }); 
     
    Promise.all(p).then(() => { 
        playSound('success');
        showToast("Đã duyệt mâm đạt chuẩn Vô Khuẩn!", "success"); 
        duLieuAnhBiTamBase64 = ""; const fileInp = document.getElementById("input_anhBI"); if(fileInp) fileInp.value = ""; 
        callRender(); 
    }); 
}

function tuChoiHapHangLoat() { 
    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 
    let p = []; 
    checkboxes.forEach(cb => { if(cb.id !== 'selectAllNghiemThu') p.push(db.collection("phieuGiaoNhan").doc(cb.value).update({status: "DANG_RUA", ghiChu: "Không đạt hấp, trả về rửa lại"})); }); 
    Promise.all(p).then(() => { playSound('error'); showToast("Đã trả các mâm không đạt về Trạm làm sạch!", "success"); callRender(); }); 
}

// =========================================================================
// 9. TRẠM 6: KHO VÔ KHUẨN & PHỐI HỢP CẤP PHÁT ĐIỀU PHỐI XOAY VÒNG (FIFO)
// =========================================================================
function xuatKhoXoayVong(maTrucTiep) { 
    const k = document.getElementById("xuat_selKhoa") ? document.getElementById("xuat_selKhoa").value : ""; 
    let ma = maTrucTiep || (document.getElementById("xuat_inpMaBo") ? document.getElementById("xuat_inpMaBo").value.trim().toUpperCase() : ""); 
    
    if(!k) { playSound('error'); return showToast("Vui lòng Chọn Khoa nhận trước khi xuất!", "error"); } 
    if(!ma) return;
    if(gioHangXuatKho.some(x => x.maMacDinh === ma)) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách xuất!", "error"); }
    
    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && x.status === "CHO_XUAT"); 
    if(!khayThucTe) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast(`Mã ID ${ma} không sẵn sàng ở Kho Vô Khuẩn.`, "error"); } 
    
    gioHangXuatKho.push(khayThucTe); playSound('success'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; renderGioHangXuat();
}

function renderGioHangXuat() {
    let listContainer = document.getElementById("danhSachXuatTam");
    if (!listContainer) {
        let bangKho = document.getElementById("bangKhoVoKhuan");
        if (bangKho && bangKho.closest('table')) {
            let wrapper = document.createElement("div"); wrapper.id = "danhSachXuatTam";
            wrapper.className = "mb-4 bg-sky-50 border border-sky-200 rounded p-3 hidden shadow-inner";
            bangKho.closest('table').parentNode.insertBefore(wrapper, bangKho.closest('table')); listContainer = wrapper;
        }
    }
    if(listContainer) {
        if(gioHangXuatKho.length === 0) { listContainer.classList.add("hidden"); } 
        else {
            listContainer.classList.remove("hidden");
            let html = `<div class="flex justify-between items-center mb-2 pb-2 border-b border-sky-200"><h3 class="font-bold text-sky-800 text-[13px]"><i class="fa-solid fa-cart-flatbed mr-2"></i>Đang quét xuất kho: <span class="bg-rose-500 text-white px-2 py-0.5 rounded ml-1">${gioHangXuatKho.length} mâm</span></h3><button onclick="xacNhanXuatKhoHangLoat()" class="bg-emerald-600 text-white px-4 py-1.5 rounded shadow font-black text-xs hover:bg-emerald-700 transition-colors uppercase"><i class="fa-solid fa-check-double mr-1"></i>Chốt Bàn Giao</button></div><div class="flex flex-wrap gap-2">`;
            gioHangXuatKho.forEach((item, index) => { 
                let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "Dụng cụ";
                html += `<div class="bg-white border border-sky-300 px-2 py-1 rounded flex items-center gap-2 shadow-sm animate-pulse"><span class="text-[11px] font-bold text-slate-700">${tenBoGoc}</span><span class="text-[10px] font-mono text-sky-700 bg-sky-100 px-1 rounded font-bold">${item.maMacDinh}</span><i class="fa-solid fa-xmark text-rose-500 cursor-pointer ml-1 text-xs hover:text-rose-700" onclick="xoaKhoiGioXuat(${index})"></i></div>`; 
            });
            html += `</div>`; listContainer.innerHTML = html;
        }
    }
}

function xoaKhoiGioXuat(index) { gioHangXuatKho.splice(index, 1); renderGioHangXuat(); }

function xacNhanXuatKhoHangLoat() {
    const k = document.getElementById("xuat_selKhoa").value; if(!k || gioHangXuatKho.length === 0) return;
    let p = []; 
    gioHangXuatKho.forEach(khay => { 
        p.push(
            db.collection("phieuGiaoNhan").doc(khay.firestoreId).update({ 
                status: "ĐANG_VAN_CHUYEN", khoa: k, ngayHoanTat: getTodayDateStr(), timeHoanTat: new Date().toLocaleTimeString('vi-VN'), nvXuatKho: loginUserCode || "CSSD_CHUNG" 
            })
        ); 
    });
    Promise.all(p).then(() => { 
        playSound('success'); showToast(`Đã xuất kho bàn giao về Khoa ${k}!`, "success"); 
        gioHangXuatKho = []; if (document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; 
        renderGioHangXuat(); callRender(); 
    });
}

function duyetCapPhatTậpTrung(yeuCauId, tenBo, khoaNhan) {
    let khaySẵnCo = listGiaoDich.find(x => x.status === "CHO_XUAT" && x.bo.split(" [ID:")[0].trim().toUpperCase() === tenBo.trim().toUpperCase());
    
    if (!khaySẵnCo) {
        playSound('error');
        return alert(`⚠️ KHÔNG CÓ KHAY SẴN SÀNG TRONG KHO!\nHiện tại trong Kho vô khuẩn CSSD không có khay "${tenBo}" nào sẵn sàng để cấp phát.`);
    }

    if (confirm(`Xác nhận cấp phát khay ${khaySẵnCo.maMacDinh} để đáp ứng yêu cầu của khoa ${khoaNhan}?`)) {
        let bayGio = new Date();
        let batch = db.batch();
        
        let refKhay = db.collection("phieuGiaoNhan").doc(khaySẵnCo.firestoreId);
        batch.update(refKhay, {
            status: "ĐANG_VAN_CHUYEN",
            khoa: khoaNhan,
            ngayHoanTat: getTodayDateStr(),
            timeHoanTat: bayGio.toLocaleTimeString('vi-VN'),
            nvXuatKho: loginUserCode || "CSSD_CHUNG"
        });

        let refYeuCau = db.collection("phieuGiaoNhan").doc(yeuCauId);
        batch.delete(refYeuCau);

        batch.commit().then(() => {
            playSound('success');
            showToast(`Đã cấp phát thành công khay ${khaySẵnCo.maMacDinh} cho khoa ${khoaNhan}!`, "success");
            callRender();
        });
    }
}

// =========================================================================
// XỬ LÝ KÝ CHỮ KÝ ĐIỆN TỬ TRỰC TIẾP TRÊN CANVAS & CẬP NHẬT KHOA
// =========================================================================
function khoiTaoCanvasKyDienTu() {
    canvasSignature = document.getElementById("canvasKyDienTu");
    if (!canvasSignature) return;
    ctxSignature = canvasSignature.getContext("2d");

    const rect = canvasSignature.getBoundingClientRect();
    canvasSignature.width = rect.width || 380;
    canvasSignature.height = rect.height || 160;

    ctxSignature.lineWidth = 2.5;
    ctxSignature.lineCap = "round";
    ctxSignature.strokeStyle = "#0284c7";

    // Chuột
    canvasSignature.onmousedown = (e) => {
        isDrawingSignature = true;
        ctxSignature.beginPath();
        const pos = getCanvasPos(e);
        ctxSignature.moveTo(pos.x, pos.y);
    };
    canvasSignature.onmousemove = (e) => {
        if (!isDrawingSignature) return;
        const pos = getCanvasPos(e);
        ctxSignature.lineTo(pos.x, pos.y);
        ctxSignature.stroke();
    };
    canvasSignature.onmouseup = () => { isDrawingSignature = false; };

    // Cảm ứng Tablet/Phone
    canvasSignature.ontouchstart = (e) => {
        isDrawingSignature = true;
        ctxSignature.beginPath();
        const pos = getCanvasPos(e.touches[0]);
        ctxSignature.moveTo(pos.x, pos.y);
    };
    canvasSignature.ontouchmove = (e) => {
        if (!isDrawingSignature) return;
        e.preventDefault();
        const pos = getCanvasPos(e.touches[0]);
        ctxSignature.lineTo(pos.x, pos.y);
        ctxSignature.stroke();
    };
    canvasSignature.ontouchend = () => { isDrawingSignature = false; };
}

function getCanvasPos(evt) {
    const rect = canvasSignature.getBoundingClientRect();
    return {
        x: evt.clientX - rect.left,
        y: evt.clientY - rect.top
    };
}

function xoaChuKyCanvas() {
    if (ctxSignature && canvasSignature) {
        ctxSignature.clearRect(0, 0, canvasSignature.width, canvasSignature.height);
    }
}

function khoaKyNhanDoSachDienTu() {
    const checkboxes = document.querySelectorAll("#bangChoNhanTaiKhoa input[type='checkbox']:checked");
    dsIdKhayChoKyNhan = [];
    checkboxes.forEach(chk => dsIdKhayChoKyNhan.push(chk.value));

    if (dsIdKhayChoKyNhan.length === 0) {
        playSound('error');
        return showToast("Vui lòng tích chọn ít nhất 1 khay để ký nhận!", "error");
    }

    const inpName = document.getElementById("ky_tenNguoiNhan");
    if (inpName) inpName.value = loginUserCode || "";

    const pop = document.getElementById("popupKyDienTu");
    if (pop) {
        pop.classList.remove("hidden");
        setTimeout(() => {
            khoiTaoCanvasKyDienTu();
            xoaChuKyCanvas();
        }, 100);
    } else {
        const khoaDangNhap = currentRole === "KHOA" ? loginUserCode : (document.getElementById("khoa_selKhoa") ? document.getElementById("khoa_selKhoa").value : "");
        if (confirm(`Xác nhận nhận ${dsIdKhayChoKyNhan.length} mâm về khoa ${khoaDangNhap}?`)) {
            let p = [];
            dsIdKhayChoKyNhan.forEach(idDoc => {
                p.push(
                    db.collection("phieuGiaoNhan").doc(idDoc).update({
                        status: "HOAN_TAT", nguoiKyNhanKhoa: loginUserCode || "Điều dưỡng khoa", timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'), ngayKhoaNhanThucTe: getTodayDateStr()
                    })
                );
            });
            Promise.all(p).then(() => { playSound('success'); showToast(`Đã tiếp nhận vào tủ khoa thành công!`, "success"); callRender(); });
        }
    }
}

function dongPopupKyDienTu() {
    document.getElementById("popupKyDienTu")?.classList.add("hidden");
    dsIdKhayChoKyNhan = [];
}

function luuXacNhanKyNhan() {
    const tenNguoiNhan = document.getElementById("ky_tenNguoiNhan")?.value.trim();
    if (!tenNguoiNhan) {
        playSound('error');
        return showToast("Vui lòng nhập tên người nhận!", "error");
    }

    const chuoiAnhChuKy = canvasSignature ? canvasSignature.toDataURL("image/png") : "";
    const currentKhoa = currentRole === "KHOA" ? loginUserCode : (document.getElementById("khoa_selKhoa")?.value || "KHOA_LAM_SANG");
    const thoiGianKy = new Date().toLocaleString('vi-VN');

    // 1. Lưu chữ ký gần nhất vào localStorage theo Khoa
    const dataSignature = {
        tenNguoiNhan: tenNguoiNhan,
        chuKyImg: chuoiAnhChuKy,
        timeKy: thoiGianKy
    };
    localStorage.setItem(`signature_${currentKhoa}`, JSON.stringify(dataSignature));

    // 2. Cập nhật trạng thái từng mâm vào Cloud Firestore
    let p = [];
    dsIdKhayChoKyNhan.forEach(idDoc => {
        p.push(
            db.collection("phieuGiaoNhan").doc(idDoc).update({
                status: "HOAN_TAT",
                nguoiKyNhanKhoa: tenNguoiNhan,
                anhChuKyNhanBase64: chuoiAnhChuKy,
                timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'),
                ngayKhoaNhanThucTe: getTodayDateStr()
            })
        );
    });

    Promise.all(p).then(() => {
        playSound('success');
        showToast(`Đã ký nhận thành công ${dsIdKhayChoKyNhan.length} mâm vào tủ khoa!`, "success");
        dongPopupKyDienTu();
        callRender();
    });
}

// =========================================================================
// 10. GHI NHẬN SỬ DỤNG BỘ DỤNG CỤ TRÊN BỆNH NHÂN
// =========================================================================
function moPopupSuDungBoDungCu() {
    gioKhaySuDungTam = []; document.getElementById("sd_bangKhayChon").innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng quét hoặc nhập mã khay...</td></tr>`;
    document.getElementById("sd_ngaySuDung").value = getTodayDateStr(); document.getElementById("sd_nhanChung").value = loginUserCode || "ADMIN";
    if(document.getElementById("sd_khoaBenhNhan")) document.getElementById("sd_khoaBenhNhan").value = (currentRole === "KHOA") ? loginUserCode : "";
    if(document.getElementById("sd_yTaPhongMo")) document.getElementById("sd_yTaPhongMo").value = "";
    if(document.getElementById("sd_yTaVongNgoai")) document.getElementById("sd_yTaVongNgoai").value = "";
    if(document.getElementById("sd_searchBN")) document.getElementById("sd_searchBN").value = "";
    if(document.getElementById("sd_ghiChu")) document.getElementById("sd_ghiChu").value = "";
    if(document.getElementById("sd_maKhayInp")) document.getElementById("sd_maKhayInp").value = "";
    document.getElementById("popupSuDungBoDungCu").classList.remove("hidden");
}

function closePopupSuDung() { document.getElementById("popupSuDungBoDungCu").classList.add("hidden"); }

function scanKhayVaoSuDung() {
    let ma = document.getElementById("sd_maKhayInp").value.trim().toUpperCase(); if(!ma) return;
    document.getElementById("sd_maKhayInp").value = "";
    if(gioKhaySuDungTam.some(x => x.maMacDinh === ma)) { playSound('error'); return showToast("Mã khay này đã được quét!", "error"); }

    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT"));
    if(!khayThucTe) { playSound('error'); return showToast(`Mã ID ${ma} không hợp lệ hoặc chưa vô khuẩn!`, "error"); }

    gioKhaySuDungTam.push(khayThucTe); playSound('success'); renderBangKhaySuDung();
}

function renderBangKhaySuDung() {
    const tbody = document.getElementById("sd_bangKhayChon"); if(!tbody) return;
    if(gioKhaySuDungTam.length === 0) { tbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Chưa chọn khay...</td></tr>`; return; }
    tbody.innerHTML = gioKhaySuDungTam.map((item, index) => {
        let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A";
        return `<tr class="border-b font-medium hover:bg-slate-50 text-xs">
            <td class="p-2 text-center"><i class="fa-solid fa-square-check text-teal-600 text-sm"></i></td>
            <td class="p-2 font-mono font-bold text-sky-700">${item.maMacDinh}</td>
            <td class="p-2 font-bold text-slate-800">${tenBoGoc}</td>
            <td class="p-2 text-center font-mono font-bold text-rose-600 bg-rose-50/30">${item.batchCode || 'N/A'}</td>
            <td class="p-2 text-center"><span class="px-2 py-0.5 rounded text-[10px] font-bold bg-teal-100 text-teal-800">${item.status}</span></td>
            <td class="p-2 text-center"><i class="fa-solid fa-trash-can text-rose-500 cursor-pointer hover:text-rose-700" onclick="xoaKhayKhoiListSuDung(${index})"></i></td>
        </tr>`;
    }).join('');
}

function xoaKhayKhoiListSuDung(index) {
    gioKhaySuDungTam.splice(index, 1);
    renderBangKhaySuDung();
}

// =========================================================================
// 11. THIẾT LẬP MÁY IN BIXOLON SLP-TX403 (80mm x 50mm) & BIÊN BẢN HÀNG LOẠT
// =========================================================================

// In tem đơn Bixolon 80mm x 50mm
function inTemBixolonChuan(itemData) {
    const pZone = document.getElementById("print-zone");
    if (!pZone) return;

    const tenBo = itemData.bo ? itemData.bo.split(" [ID:")[0] : "DỤNG CỤ VÔ KHUẨN";
    const maID = itemData.maMacDinh || itemData.idKhay || "000000";
    const ngayNSX = itemData.ngayHapRealtime || itemData.ngayTao || getTodayDateStr();
    const ngayHSD = itemData.hsd ? new Date(itemData.hsd).toLocaleDateString('vi-VN') : "Theo quy định";
    const maBatch = itemData.batchCode || itemData.batchId || "N/A";
    const tenKhoa = itemData.khoa || "TẬP TRUNG";

    pZone.innerHTML = `
        <div class="bixolon-label" style="font-family: Arial, sans-serif;">
            <div style="text-align: center; border-bottom: 1.5px solid #000; padding-bottom: 2px;">
                <div style="font-size: 10px; font-weight: bold; text-transform: uppercase;">PHUONG NAM HOSPITAL - CSSD</div>
                <div style="font-size: 12px; font-weight: 900; margin-top: 1px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${tenBo}</div>
            </div>

            <div style="text-align: center; margin: 3px 0; display: flex; justify-content: center;">
                <svg id="bixolon-single-barcode"></svg>
            </div>

            <div style="font-size: 9px; border-top: 1px dashed #000; padding-top: 2px;">
                <div style="display: flex; justify-content: space-between; font-weight: bold;">
                    <span>LÔ: ${maBatch}</span>
                    <span>KHOA: ${tenKhoa}</span>
                </div>
                <div style="display: flex; justify-content: space-between; margin-top: 1px;">
                    <span>NSX: ${ngayNSX}</span>
                    <span style="font-size: 10px; font-weight: 900;">HSD: ${ngayHSD}</span>
                </div>
            </div>
        </div>
    `;

    JsBarcode("#bixolon-single-barcode", maID, {
        format: "CODE128",
        width: 1.6,
        height: 36,
        displayValue: true,
        fontSize: 10,
        fontOptions: "bold",
        margin: 0
    });

    document.body.classList.remove("print-mode-doc");
    document.body.classList.add("print-mode-bixolon");
    pZone.classList.remove("hidden");

    setTimeout(() => {
        window.print();
        pZone.classList.add("hidden");
        document.body.classList.remove("print-mode-bixolon");
    }, 250);
}

function inTemTongHangLoat() { 
    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 
    let batchCode = document.getElementById("hap_batchId")?.value || "A1000000_01"; 
    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 

    checkboxes.forEach((cb) => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { 
            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 
            container.innerHTML += `<div class="bixolon-label" style="font-family: Arial; font-size: 11px; color: #000; background: #fff;"><div style="text-align: center; font-weight: bold; font-size: 12px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-lo-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span></div><div style="display: flex; justify-content: space-between; border-top: 1px dashed #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 9px; font-family: monospace; font-weight: bold; margin-top: 1px;">Lô: ${batchCode}</div></div>`; 
        } 
    }); 

    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); 
    document.body.classList.remove("print-mode-doc");
    document.body.classList.add("print-mode-bixolon");
    pZone.classList.remove("hidden"); 

    checkboxes.forEach(cb => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-lo-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 
    }); 

    setTimeout(() => { 
        window.print(); 
        pZone.classList.add("hidden"); 
        document.body.classList.remove("print-mode-bixolon");
    }, 300); 
}

function inTemNghiemThuHangLoat() { 
    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 
    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 

    checkboxes.forEach((cb) => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { 
            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 
            container.innerHTML += `<div class="bixolon-label" style="font-family: Arial; font-size: 11px; color: #000; background: #fff;"><div style="text-align: center; font-size: 9px; font-weight: bold; letter-spacing: 0.5px;">PN HOSPITAL - CSSD</div><div style="text-align: center; font-weight: bold; font-size: 12px; margin: 1px 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-nt-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span><span style="color: green;">ĐẠT VÔ KHUẨN</span></div><div style="display: flex; justify-content: space-between; border-top: 1px solid #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 8px; font-weight: bold; font-family: monospace; margin-top: 1px;">BATCH: ${item.batchCode || 'N/A'}</div></div>`; 
        } 
    }); 

    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); 
    document.body.classList.remove("print-mode-doc");
    document.body.classList.add("print-mode-bixolon");
    pZone.classList.remove("hidden"); 

    checkboxes.forEach(cb => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-nt-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 
    }); 

    setTimeout(() => { 
        window.print(); 
        pZone.classList.add("hidden"); 
        document.body.classList.remove("print-mode-bixolon");
    }, 300); 
}

// In Biên bản giao nhận khổ A4 máy in văn phòng (Có hỗ trợ chữ ký điện tử)
function inHoaDonGiaoNhan() {
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 
    if (!k) return showToast("Chọn Khoa trước!", "error");
    
    // Tìm chữ ký điện tử mới nhất từ Firestore hoặc localStorage
    const itemKyGầnNhat = listGiaoDich.find(x => x.khoa === k && x.anhChuKyNhanBase64);
    const savedSignature = JSON.parse(localStorage.getItem(`signature_${k}`) || "null");

    let htmlChuKyDieuDuong = `<p style="font-weight:normal; font-style:italic; color:#64748b; margin-top:60px;">(Ký và ghi rõ họ tên)</p>`;
    
    if (itemKyGầnNhat && itemKyGầnNhat.anhChuKyNhanBase64) {
        htmlChuKyDieuDuong = `
            <div style="text-align:center; margin-top:10px;">
                <img src="${itemKyGầnNhat.anhChuKyNhanBase64}" style="max-height:75px; margin:0 auto; display:block;" alt="Chữ ký"/>
                <p style="font-weight:bold; margin-top:5px; text-transform:uppercase; color:#0f172a;">${itemKyGầnNhat.nguoiKyNhanKhoa}</p>
                <p style="font-size:10px; color:#64748b; font-weight:normal;">Xác nhận: ${itemKyGầnNhat.ngayKhoaNhanThucTe || ''} ${itemKyGầnNhat.timeKhoaNhanThucTe || ''}</p>
            </div>
        `;
    } else if (savedSignature && savedSignature.chuKyImg) {
        htmlChuKyDieuDuong = `
            <div style="text-align:center; margin-top:10px;">
                <img src="${savedSignature.chuKyImg}" style="max-height:75px; margin:0 auto; display:block;" alt="Chữ ký"/>
                <p style="font-weight:bold; margin-top:5px; text-transform:uppercase; color:#0f172a;">${savedSignature.tenNguoiNhan}</p>
                <p style="font-size:10px; color:#64748b; font-weight:normal;">Xác nhận: ${savedSignature.timeKy}</p>
            </div>
        `;
    }

    let htmlBang = `
        <table style="width:100%; border-collapse:collapse; margin-top:20px; font-size:13px; text-align:left; font-family:Arial, sans-serif;">
            <thead>
                <tr style="background-color:#f1f5f9;">
                    <th style="padding:10px; border:1px solid #334155; width:35%; text-align:left; color:#0f172a;">PHÂN LOẠI MÂM / LOẠI DỤNG CỤ</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ TRẢ BẨN</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ NHẬN SẠCH</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">NHẬN VÔ KHUẨN</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:20%; color:#0f172a;">CSSD NỢ KHOA</th>
                </tr>
            </thead>
            <tbody>`;

    const rows = document.querySelectorAll('#bangDonGiaoNhan tr');
    if (rows.length === 0 || (rows.length === 1 && rows[0].cells.length === 1)) { 
        htmlBang += `<tr><td colspan="5" style="padding:15px; border:1px solid #334155; text-align:center; font-style:italic;">Không có dữ liệu công nợ.</td></tr>`; 
    } else {
        rows.forEach(row => {
            const cells = row.cells;
            if (cells.length >= 5) { 
                htmlBang += `
                    <tr>
                        <td style="padding:10px; border:1px solid #334155; font-weight:bold; color:#0f172a;">${cells[0].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#e11d48;">${cells[1].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#0284c7;">${cells[2].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#7c3aed;">${cells[3].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#d97706;">${cells[4].innerText}</td>
                    </tr>`; 
            }
        });
    }
    htmlBang += `</tbody></table>`;

    const pZone = document.getElementById("print-zone");
    pZone.innerHTML = `
        <div style="width:100%; font-family:Arial, sans-serif; padding:10px; color:#000;">
            <div style="text-align:center; margin-bottom:25px;">
                <h2 style="margin:0; font-size:18px; font-weight:900; text-transform:uppercase;">BIÊN BẢN GIAO NHẬN DỤNG CỤ CSSD</h2>
                <p style="margin:8px 0 0 0; font-size:13px; font-weight:bold;">Khoa/Phòng: <span style="text-transform:uppercase; color:#0284c7;">${k}</span> - Ngày xuất: ${new Date().toLocaleDateString('vi-VN')}</p>
            </div>
            ${htmlBang}
            <div style="margin-top:40px; display:flex; justify-content:space-between; font-size:12px; font-weight:bold; padding:0 30px;">
                <div style="text-align:center; width:45%;">
                    <p style="margin-bottom:10px; text-transform:uppercase;">ĐIỀU DƯỠNG LÂM SÀNG</p>
                    ${htmlChuKyDieuDuong}
                </div>
                <div style="text-align:center; width:45%;">
                    <p style="margin-bottom:10px; text-transform:uppercase;">CHUYÊN VIÊN CSSD</p>
                    <p style="font-weight:normal; font-style:italic; color:#64748b; margin-top:60px;">(Ký và ghi rõ họ tên)</p>
                </div>
            </div>
        </div>`;

    document.body.classList.remove("print-mode-bixolon");
    document.body.classList.add("print-mode-doc");
    pZone.classList.remove("hidden");

    setTimeout(() => { 
        window.print(); 
        pZone.classList.add("hidden"); 
        document.body.classList.remove("print-mode-doc");
    }, 200);
}

// =========================================================================
// 12. HÀM PHỤ TRỢ: CHỨC NĂNG QUẢN TRỊ ADMIN & TIỆN ÍCH
// =========================================================================
function taiDanhMucLinhKienChuan() {
    const tbody = document.getElementById("bangDanhMucLinhKien"); if (!tbody) return; tbody.innerHTML = "";
    if (!databaseExcel || databaseExcel.length === 0) { tbody.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-slate-400 italic">Chưa có dữ liệu. Vui lòng nạp file Excel.</td></tr>`; return; }
    let gopBoExcel = {};
    databaseExcel.forEach(x => {
        let tenBo = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; if (!tenBo) return;
        tenBo = String(tenBo).trim().toUpperCase(); if (!gopBoExcel[tenBo]) gopBoExcel[tenBo] = []; gopBoExcel[tenBo].push(x);
    });
    for (let tenBo in gopBoExcel) {
        let linhKienHtml = `<div class="flex flex-wrap gap-1">`; let tongSl = 0;
        gopBoExcel[tenBo].forEach(item => {
            let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || item['Dụng cụ'] || item['Tên Chi Tiết'] || "Dụng cụ Chi Tiết";
            
            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {
                tenDc = "Dụng cụ chuẩn mâm";
            }
            
            let sl = parseInt(item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1) || 0; let hanHap = item['Tuổi thọ mẻ hấp'] || item['Tuổi thọ'] || 100;
            tongSl += sl; linhKienHtml += `<span class="bg-slate-100 text-slate-700 px-1.5 py-0.5 rounded text-[10px] font-medium border border-slate-200">${tenDc} (${sl}) <span class="text-amber-600 font-bold">[Max: ${hanHap}]</span></span>`;
        });
        linhKienHtml += `</div>`; const tr = document.createElement("tr"); tr.className = "hover:bg-slate-50 transition-colors border-b";
        tr.innerHTML = `<td class="p-3 font-bold text-slate-800 text-xs">${tenBo}</td><td class="p-3">${linhKienHtml}</td><td class="p-3 text-center font-black text-sky-700 bg-sky-50/30">${tongSl}</td>`;
        tbody.appendChild(tr);
    }
}

function updateGiaoDienMatrix(role, tabId, checkboxElement) {
    if(!cauHinhGiaoDien[role]) cauHinhGiaoDien[role] = [];
    if(checkboxElement.checked) { if(!cauHinhGiaoDien[role].includes(tabId)) cauHinhGiaoDien[role].push(tabId); } 
    else { cauHinhGiaoDien[role] = cauHinhGiaoDien[role].filter(x => x !== tabId); }
    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ cauHinhGiaoDien: cauHinhGiaoDien }).then(() => showToast("Đã cập nhật quyền truy cập!", "success"));
}

function handleViewAnhBiMoi(firestoreId) { hanhDongXemAnhBiMoi(firestoreId); }
function hanhDongXemAnhBiMoi(firestoreId) {
    let item = listGiaoDich.find(x => x.firestoreId === firestoreId); if (!item) return showToast("Không tìm thấy thông tin!", "error");
    let loGoc = listGiaoDich.find(m => m.batchCode === item.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 
    let chuoiAnhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || item.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;
    if (!chuoiAnhBase64) return showToast("Mẻ này chưa tải ảnh minh chứng hoặc kế thừa.", "error");
    let w = window.open(); w.document.write(`<html><head><title>MINH CHỨNG BI - LÔ ${item.batchCode}</title></head><body style='margin:0; background:#000; display:flex; justify-content:center; align-items:center;'><img src='${chuoiAnhBase64}' style='max-width:100%; max-height:100vh; object-fit:contain;'/></body></html>`);
}

function switchAdminSubtab(sub) { 
    document.getElementById('subtab-database')?.classList.add('hidden'); document.getElementById('subtab-security')?.classList.add('hidden'); 
    document.getElementById('subbtn-database')?.classList.replace('admin-subtab-active', 'text-slate-600'); document.getElementById('subbtn-security')?.classList.replace('admin-subtab-active', 'text-slate-600'); 
    document.getElementById('subtab-' + sub)?.classList.remove('hidden'); document.getElementById('subbtn-' + sub)?.classList.add('admin-subtab-active'); 
}

function saveAdminPIN(type) { let newVal = document.getElementById(`cfg_pin${type.charAt(0).toUpperCase() + type.slice(1).toLowerCase()}`).value.trim(); if(type === 'ADMIN') thongTinMatKhauAdmin.adminPIN = newVal; if(type === 'CSSD') thongTinMatKhauAdmin.cssdPIN = newVal; if(type === 'GUEST') thongTinMatKhauAdmin.guestPIN = newVal; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ thongTinMatKhauAdmin: thongTinMatKhauAdmin }).then(() => showToast("Đã lưu PIN!", "success")); }
function themKtvCssd() { let code = prompt("Mã NV:"); let ten = prompt("Tên:"); let pin = prompt("PIN:"); if(code && ten && pin) { danhSachKtvCssd.push({ code: code.toUpperCase(), ten: ten, pin: pin }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }
function xaoKtvCssd(code) { if(confirm("Xóa?")) { danhSachKtvCssd = danhSachKtvCssd.filter(x => x.code !== code); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }
function themKhoaThuCong() { let t = prompt("Nhập Tên Khoa/Phòng:"); if(t) { danhSachKhoa.push({ ten: t.toUpperCase(), pin: "123", danhSachBo: [] }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Thêm Khoa thành công!", "success")); } }

function khaiSinhKhayVangLai() {
    if(danhSachKhoa.length === 0) return showToast("Hệ thống chưa có Khoa nào!", "error");
    let tenKhay = prompt("Nhập Tên Mâm/Khay dụng cụ:"); if(!tenKhay) return;
    let maId = prompt("Nhập Mã ID khay:"); if(!maId) return; maId = maId.toUpperCase().trim();
    let dsTenKhoa = danhSachKhoa.map((k, idx) => `${idx + 1}. ${k.ten}`).join("\n");
    let luaChon = prompt(`Chọn số thứ tự Khoa sở hữu:\n${dsTenKhoa}`); if(!luaChon) return;
    let idxKhoa = parseInt(luaChon) - 1; if(idxKhoa < 0 || idxKhoa >= danhSachKhoa.length) return showToast("Lựa chọn không hợp lệ!", "error");
    let khoaChon = danhSachKhoa[idxKhoa]; let chuoiKhayDinhDang = `${tenKhay.toUpperCase().trim()} [ID:${maId}]`;
    if(!khoaChon.danhSachBo) khoaChon.danhSachBo = []; if(khoaChon.danhSachBo.includes(chuoiKhayDinhDang)) return showToast("Khay đã tồn tại!", "error");
    khoaChon.danhSachBo.push(chuoiKhayDinhDang);
    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => { showToast(`Khai sinh khay ${maId} thành công!`, "success"); callRender(); });
}

function updatePINTrựcTiep(idx, tenKhoa) { let p = document.getElementById(`pin-khoa-${idx}`).value.trim(); if(p) { danhSachKhoa.find(x => x.ten === tenKhoa).pin = p; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Đổi PIN thành công", "success")); } }

function initSelects() { 
    let opts = '<option value="">-- Chọn Khoa --</option>' + danhSachKhoa.map(k=>`<option value="${k.ten}">${k.ten}</option>`).join('');
    if(document.getElementById("login_khoa")) document.getElementById("login_khoa").innerHTML = opts; 
    if(document.getElementById("khoa_selKhoa")) document.getElementById("khoa_selKhoa").innerHTML = opts; 
    if(document.getElementById("login_nv_cssd")) document.getElementById("login_nv_cssd").innerHTML = '<option value="">-- Chọn KTV CSSD --</option>' + danhSachKtvCssd.map(k=>`<option value="${k.code}">${k.code} - ${k.ten}</option>`).join(''); 
     
    if (document.getElementById("rua_hoaChat")) {
        document.getElementById("rua_hoaChat").innerHTML = danhMucHoaChatRua.map(hc => `<option value="${hc.ten}">${hc.ten}</option>`).join('');
        document.getElementById("rua_lieuLuong").value = danhMucHoaChatRua[0].lieuLuong;
        document.getElementById("rua_hoaChat").addEventListener("change", (e) => {
            let selectedHc = danhMucHoaChatRua.find(hc => hc.ten === e.target.value);
            if(selectedHc) document.getElementById("rua_lieuLuong").value = selectedHc.lieuLuong;
        });
    }
    if (document.getElementById("rua_loaiRua")) { capNhatDanhSachMaMayRua(); }
    if (document.getElementById("hap_loaiHap")) { capNhatDanhSachMaMay(); }
     
    const selKhoaTraba = document.getElementById("khoa_selKhoa"); if(selKhoaTraba) { selKhoaTraba.removeEventListener("change", loadBoDungCuTheoKhoa); selKhoaTraba.addEventListener("change", loadBoDungCuTheoKhoa); }
}

function showToast(msg, type="error") { const t = document.createElement('div'); t.className = `fixed top-6 right-6 ${type==="error"?"bg-rose-600":"bg-emerald-600"} text-white px-5 py-3.5 rounded-lg shadow-2xl z-[100] font-bold text-sm`; t.innerHTML = msg; document.body.appendChild(t); setTimeout(() => t.remove(), 2500); }
function toggleMobileMenu() { document.getElementById("sidebar_menu").classList.toggle("-translate-x-full"); document.getElementById("mobile-overlay").classList.toggle("hidden"); }

function moCamera(inputId) { targetInputIdForScan = inputId; document.getElementById("popupScanner").classList.remove("hidden"); html5QrCode = new Html5Qrcode("reader"); Html5Qrcode.getCameras().then(devices => { let cid = devices.length > 1 ? devices[devices.length - 1].id : devices[0].id; html5QrCode.start(cid, { fps: 15, qrbox: { width: 260, height: 180 } }, (txt) => { document.getElementById(targetInputIdForScan).value = txt.trim().toUpperCase(); if(targetInputIdForScan==='khoa_inpMaBo') themVaoGio(); if(targetInputIdForScan==='xuat_inpMaBo') xuatKhoXoayVong(); if(targetInputIdForScan==='sd_maKhayInp') scanKhayVaoSuDung(); }).catch(e=>{}) }); }
function dongCamera() { if(html5QrCode) html5QrCode.stop().then(() => html5QrCode.clear()); document.getElementById("popupScanner").classList.add("hidden"); }
function xoaSachDuLieuGiaoDichRealtime() { if(prompt("Nhập PIN ADMIN:") === (thongTinMatKhauAdmin.adminPIN||"admin2026")) { db.collection("phieuGiaoNhan").get().then(snap => { let b = db.batch(); snap.forEach(d => b.delete(d.ref)); b.commit().then(() => location.reload()); }); } }

function docAnhBiUpTaiCho(inputElement) { 
    const file = inputElement.files[0]; if (!file) return; 
    const reader = new FileReader(); reader.onloadend = function() { duLieuAnhBiTamBase64 = reader.result; showToast("Tải ảnh minh chứng BI thành công!", "success"); kiemTraQuyenDuyetMeHap(); }; 
    reader.readAsDataURL(file); 
}

// =========================================================================
// 13. HÀM CORE RENDER DỮ LIỆU ĐA TAB LUỒNG HỆ THỐNG
// =========================================================================
function renderTheoTabHienTai() {
    if(activeTab === 'khoaphong') {
        const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;
        let tatCaDonCuaKhoa = listGiaoDich.filter(x => (x.khoa || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase());
        
        // 1. TÍNH TOÁN CÔNG NỢ LUÂN CHUYỂN THỰC TẾ
        let gopCongNo = {};
        tatCaDonCuaKhoa.forEach(x => {
            if (x.status === "CHO_CAP_PHAT") return;

            let tenLoaiMâm = x.bo ? String(x.bo).split(" [ID:")[0] : "Chưa rõ mâm";
            if (!gopCongNo[tenLoaiMâm]) gopCongNo[tenLoaiMâm] = { daTraBan: 0, nhanSach: 0, nhanVoKhuan: 0, cssdNo: 0 };
            
            if (x.status !== "CHO_THU") gopCongNo[tenLoaiMâm].daTraBan += 1; 
            if (["DANG_RUA", "TRONG_BUONG_RUA", "CHO_DONG_GOI", "CHO_HAP", "DANG_HAP", "CHO_XUAT", "ĐANG_VAN_CHUYEN", "HOAN_TAT", "DA_SU_DUNG"].includes(x.status)) gopCongNo[tenLoaiMâm].nhanSach += 1;
            if (x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG") gopCongNo[tenLoaiMâm].nhanVoKhuan += 1; 
            gopCongNo[tenLoaiMâm].cssdNo = gopCongNo[tenLoaiMâm].daTraBan - gopCongNo[tenLoaiMâm].nhanVoKhuan;
        });
        
        let arrHtml = [];
        for (let key in gopCongNo) {
            let item = gopCongNo[key];
            arrHtml.push(`<tr class="border-b hover:bg-slate-50 transition-colors font-medium text-[11px]"><td class="p-3 font-bold text-slate-800 border-r">${key}</td><td class="p-3 text-center text-rose-600 font-bold bg-rose-50/20 border-r">${item.daTraBan}</td><td class="p-3 text-center text-sky-600 font-bold bg-sky-50/20 border-r">${item.nhanSach}</td><td class="p-3 text-center text-purple-600 font-bold bg-purple-50/20 border-r">${item.nhanVoKhuan}</td><td class="p-3 text-center text-amber-600 font-black bg-amber-50/20">${item.cssdNo > 0 ? 'Nợ ' + item.cssdNo : '-'}</td></tr>`);
        }
        if(document.getElementById("bangDonGiaoNhan")) {
            document.getElementById("bangDonGiaoNhan").innerHTML = arrHtml.length ? arrHtml.join('') : `<tr><td colspan="5" class="p-8 text-center text-slate-400 italic">Khoa chưa phát sinh công nợ luân chuyển đồ.</td></tr>`;
        }
        
        // 2. DANH SÁCH CHỜ NHẬN ĐỒ SẠCH (Từ xe trung chuyển CSSD bàn giao lên)
        const tbodyChoNhan = document.getElementById("bangChoNhanTaiKhoa"); const badgeChoNhan = document.getElementById("badgeChoNhanKhoa"); const txtNguoiXacNhan = document.getElementById("txtNguoiDungNhanHienTai");
        if(txtNguoiXacNhan) txtNguoiXacNhan.innerText = `PM-ĐD: ${loginUserCode || "Chưa Đăng Nhập"}`;
        
        let dsDangVanChuyen = tatCaDonCuaKhoa.filter(x => x.status === "ĐANG_VAN_CHUYEN"); if(badgeChoNhan) badgeChoNhan.innerText = `${dsDangVanChuyen.length} khay`;
        if(tbodyChoNhan) {
            if(dsDangVanChuyen.length === 0) { tbodyChoNhan.innerHTML = `<tr><td colspan="5" class="p-3 text-center text-slate-400 italic">Hiện không có dụng cụ nào đang chuyển về khoa.</td></tr>`; } 
            else { tbodyChoNhan.innerHTML = dsDangVanChuyen.map(khay => `<tr class="hover:bg-slate-50 transition-colors"><td class="p-2 text-center"><input type="checkbox" value="${khay.firestoreId}" class="w-3.5 h-3.5 text-sky-600 rounded border-slate-300 focus:ring-sky-500 cursor-pointer"></td><td class="p-2 font-mono font-bold text-slate-700">${khay.maMacDinh || 'N/A'}</td><td class="p-2 font-semibold text-slate-800">${khay.bo ? String(khay.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-2 text-slate-500 font-medium">${khay.nvXuatKho || '--'}</td><td class="p-2 text-center"><span class="bg-purple-50 text-purple-700 font-mono text-[10px] px-1.5 py-0.5 rounded font-bold">${khay.batchCode || 'N/A'}</span></td></tr>`).join(''); }
        }

        // 3. THEO DÕI YÊU CẦU ĐANG CHỜ CSSD CẤP PHÁT
        let dsChoCapPhat = tatCaDonCuaKhoa.filter(x => x.status === "CHO_CAP_PHAT");
        const tbodyChoCapPhat = document.getElementById("bangChoCapPhatTaiKhoa");
        if (tbodyChoCapPhat) {
            if (dsChoCapPhat.length === 0) {
                tbodyChoCapPhat.innerHTML = `<tr><td colspan="4" class="p-3 text-center text-slate-400 italic">Không có yêu cầu mượn khay mới nào đang chờ duyệt.</td></tr>`;
            } else {
                tbodyChoCapPhat.innerHTML = dsChoCapPhat.map(yc => `
                    <tr class="border-b text-xs hover:bg-amber-50/10 font-medium">
                        <td class="p-2.5 font-bold text-amber-800">${yc.bo}</td>
                        <td class="p-2.5 text-center"><span class="px-2 py-0.5 rounded-full text-[10px] font-bold bg-amber-100 text-amber-800 border border-amber-200 animate-pulse">Chờ CSSD cấp khay</span></td>
                        <td class="p-2.5 text-center font-mono text-slate-400">${yc.time}</td>
                        <td class="p-2.5 text-center text-slate-500">${yc.nvYeuCau || 'ĐD'}</td>
                    </tr>
                `).join('');
            }
        }
    }
    else if(activeTab === 'thugom') {
        let fK = document.getElementById("filterKhoaThuGom")?.value || ""; 
        let lsTG = listGiaoDich.filter(x => x.status === "CHO_THU"); 
        
        if (document.getElementById("filterKhoaThuGom")) {
            let danhSachKhoaCoDon = [...new Set(lsTG.map(x => x.khoa))].filter(Boolean); 
            let htmlOpts = '<option value="">-- Lọc theo Khoa --</option>';
            danhSachKhoaCoDon.forEach(k => { 
                htmlOpts += `<option value="${k}" ${(k === fK) ? "selected" : ""}>${k}</option>`; 
            });
            document.getElementById("filterKhoaThuGom").innerHTML = htmlOpts; 
            fK = document.getElementById("filterKhoaThuGom").value || "";
        }
        
        if (fK) lsTG = lsTG.filter(x => (x.khoa || "").toString().trim().toUpperCase() === fK.toString().trim().toUpperCase());
        if (document.getElementById("badgeSoCho")) document.getElementById("badgeSoCho").innerText = `${lsTG.length} Lệnh`;
        
        let bodyThuGom = document.getElementById("bangChoThuGom");
        if(bodyThuGom) {
            bodyThuGom.innerHTML = lsTG.map(i => {
                let tenBo = i.bo ? String(i.bo).split(" [ID:")[0].trim() : ""; 
                
                let itemsInBo = databaseExcel.filter(x => {
                    let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || '';
                    return String(boName).trim().toUpperCase() === String(tenBo).toUpperCase();
                });
                
                let checklistHtml = "";
                if (itemsInBo.length > 0) {
                    checklistHtml = `<div class="max-h-32 overflow-y-auto pr-1 border border-slate-200/60 rounded bg-slate-50/50 p-1.5 mt-1 flex flex-col gap-1">`;
                    itemsInBo.forEach(item => {
                        let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || "Dụng cụ chi tiết";
                        let sl = item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1;
                        
                        if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {
                            tenDc = "Dụng cụ chuẩn mâm";
                        }

                        checklistHtml += `
                            <div class="flex justify-between items-center border-b border-dashed border-slate-200 pb-0.5 last:border-none text-[11px]">
                                <span class="text-slate-700 font-semibold"><i class="fa-solid fa-circle text-[4px] text-sky-400 mr-1.5 align-middle"></i>${tenDc}</span>
                                <span class="font-bold text-sky-700 bg-sky-50 px-1.5 py-0.5 rounded border border-sky-100 text-[10px]">x${sl}</span>
                            </div>`;
                    });
                    checklistHtml += `</div>`;
                } else {
                    checklistHtml = `<div class="text-[10px] text-emerald-700 bg-emerald-50 border border-emerald-100 p-1.5 rounded font-medium mt-1"><i class="fa-solid fa-circle-check mr-1"></i>Dụng cụ lẻ (Cơ số: 1 chiếc)</div>`;
                }
                
                return `
                    <tr class="border-b border-slate-100 hover:bg-slate-50/40 transition-colors">
                        <td class="p-3 align-top">
                            <div class="font-black text-slate-700 text-[11px] uppercase tracking-wide">${i.khoa}</div>
                            ${i.ghiChu ? `<div class="text-[10px] text-rose-600 font-bold bg-rose-50 border border-rose-100 px-1.5 py-0.5 rounded mt-1.5 inline-block"><i class="fa-solid fa-triangle-exclamation mr-1"></i>${i.ghiChu}</div>` : ''}
                        </td>
                        <td class="p-3 align-top">
                            <div class="font-black text-sky-800 text-[13px] uppercase tracking-tight">${tenBo}</div>
                            <div class="text-[10px] font-mono text-slate-400 font-bold mt-0.5">MÃ SỐ ID: ${i.maMacDinh}</div>
                            ${checklistHtml}
                        </td>
                        <td class="p-3 text-center align-top font-mono font-bold text-slate-500 text-[11px]">${i.time}</td>
                        <td class="p-3 text-center align-top action-col">
                            <button onclick="moPopupKiemDem('${i.firestoreId}')" class="bg-sky-600 hover:bg-sky-700 text-white font-black px-3.5 py-2 rounded-lg shadow-sm text-[11px] uppercase tracking-wider transition-all"><i class="fa-solid fa-clipboard-check mr-1"></i>Kiểm Đếm</button>
                        </td>
                    </tr>`;
            }).join('');
        }
    }
    else if(activeTab === 'mayrua') {
        let lsCR = listGiaoDich.filter(x => x.status === "DANG_RUA");
        if(document.getElementById("badgeChoRua")) document.getElementById("badgeChoRua").innerText = `${lsCR.length} Mục`;
        
        let bodyChoRua = document.getElementById("bangChoRua");
        if(bodyChoRua) {
            bodyChoRua.innerHTML = lsCR.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="rua-checkbox"></td><td class="p-3 font-bold">${i.bo}</td><td class="p-3 text-right font-mono">${i.maMacDinh}</td></tr>`).join('');
        }
        
        let lsTR = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA");
        let bodyChoNghiemThuRua = document.getElementById("bangChoNiemThuRua");
        if(bodyChoNghiemThuRua) {
            bodyChoNghiemThuRua.innerHTML = lsTR.map(i => `<tr class="border-b"><td class="p-2.5 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="nghiemthurua-checkbox"></td><td class="p-2 text-xs font-bold">${i.bo} <span class="text-slate-400 font-mono">(${i.rua_batchCode || 'Mẻ Rửa'})</span></td></tr>`).join('');
        }
        
        let bodyLichSuRua = document.getElementById("bangLichSuRua");
        if(bodyLichSuRua) {
            const homNayMoiStr = getTodayDateStr();
            let tatCaMucCoLoRua = listGiaoDich.filter(x => x.rua_batchCode && (x.rua_ngayRua === homNayMoiStr || x.ngayTao === homNayMoiStr));
            let cacMeRuaGop = {};
            tatCaMucCoLoRua.forEach(x => {
                if(!cacMeRuaGop[x.rua_batchCode]) {
                    cacMeRuaGop[x.rua_batchCode] = {
                        batchCode: x.rua_batchCode,
                        mayRua: x.rua_mayRua || "Belimed WD250",
                        chuKy: x.rua_chuKy || "Chu trình tiêu chuẩn",
                        hoaChat: x.rua_hoaChat || "Smeg",
                        timeBatDau: x.rua_timeBatDau || "--:--",
                        ngay: x.rua_ngayRua || "",
                        soLuong: 0,
                        trangThaiMe: x.status === "TRONG_BUONG_RUA" ? "Đang rửa" : "Hoàn tất sạch"
                    };
                }
                cacMeRuaGop[x.rua_batchCode].soLuong += 1;
            });
            let danhSachMeRuaSapXep = Object.values(cacMeRuaGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));
            if(danhSachMeRuaSapXep.length === 0) {
                bodyLichSuRua.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa chạy mẻ rửa nào</td></tr>`;
            } else {
                bodyLichSuRua.innerHTML = danhSachMeRuaSapXep.map(me => `
                    <tr class="hover:bg-slate-50 transition-colors">
                        <td class="p-3">
                            <div class="flex items-center gap-2">
                                <span class="font-mono font-black text-sky-700 text-sm tracking-wider">${me.batchCode}</span>
                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "Đang rửa" ? "text-amber-600 bg-amber-50 border-amber-200" : "text-emerald-600 bg-emerald-50 border-emerald-200"}">${me.trangThaiMe}</span>
                            </div>
                            <div class="text-[11px] text-slate-500 font-medium mt-1">${me.mayRua} | ${me.chuKy}</div>
                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Hoá chất: ${me.hoaChat} | Bắt đầu: ${me.timeBatDau} (${me.ngay})</div>
                        </td>
                        <td class="p-3 text-right pr-4">
                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuong} khay</span>
                        </td>
                    </tr>`).join('');
            }
        }
    }
    else if(activeTab === 'donggoi') {
        let lsDG = listGiaoDich.filter(x => x.status === "CHO_DONG_GOI"); 
        if(document.getElementById("badgeDongGoi")) document.getElementById("badgeDongGoi").innerText = lsDG.length;
        let gDongGoi = document.getElementById("gridDongGoi");
        if(gDongGoi) {
            gDongGoi.innerHTML = lsDG.map(i => `<div class="bg-white p-3 rounded border border-slate-200 mb-2 flex justify-between items-center"><div class="flex-1"><div class="font-bold text-sky-700 text-[13px]">${i.bo}</div><div class="text-[10px] text-slate-500">Từ khoa: ${i.khoa}</div></div><button onclick="moPopupDongGoi('${i.firestoreId}')" class="bg-sky-50 text-sky-700 border border-sky-300 px-3 py-1.5 rounded text-[11px] font-black">ĐÓNG GÓI</button></div>`).join('');
        }
    }
    else if(activeTab === 'mayhap') {
        let lsCH = listGiaoDich.filter(x => x.status === "CHO_HAP"); 
        if(document.getElementById("badgeChoHap")) document.getElementById("badgeChoHap").innerText = `${lsCH.length} Mục`;
        let bodyChoHap = document.getElementById("bangChoHap");
        if(bodyChoHap) { 
            bodyChoHap.innerHTML = lsCH.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="hap-checkbox"></td><td class="p-3 font-bold text-xs">${i.bo}</td><td class="p-3 text-right font-mono text-xs font-bold text-sky-700">${i.maMacDinh}</td></tr>`).join(''); 
        }

        let lsNT = listGiaoDich.filter(x => x.status === "DANG_HAP"); 
        let bodyChoNghiemThu = document.getElementById("bangChoNghiệmThu") || document.getElementById("bangChoNghiemThu");
        
        if(bodyChoNghiemThu) { 
            if (lsNT.length === 0) {
                bodyChoNghiemThu.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Không có mâm/mẻ nào đang trong lò chờ duyệt.</td></tr>`;
            } else {
                bodyChoNghiemThu.innerHTML = lsNT.map(i => {
                    let tenBoClean = i.bo ? String(i.bo).split(" [ID:")[0] : "Chưa rõ mâm";
                    return `
                        <tr class="border-b hover:bg-amber-50/40 transition-colors">
                            <td class="p-2.5 text-center action-col">
                                <input type="checkbox" value="${i.firestoreId}" class="nghiemthu-checkbox w-4 h-4 text-amber-600 rounded cursor-pointer" data-hasbi="${i.hasBI ? 'true' : 'false'}" onchange="kiemTraQuyenDuyetMeHap()">
                            </td>
                            <td class="p-2 text-xs">
                                <div class="font-black text-slate-800">${tenBoClean} <span class="text-[10px] font-mono text-sky-700">(${i.maMacDinh || 'N/A'})</span></div>
                                <div class="flex items-center gap-2 mt-0.5">
                                    <span class="text-[10px] font-mono font-bold text-rose-700 bg-rose-50 px-1 rounded border border-rose-100">Lô: ${i.batchCode || 'N/A'}</span>
                                    ${i.hasBI ? '<span class="px-1.5 py-0.5 bg-amber-100 text-amber-800 rounded text-[9px] font-black border border-amber-300 flex items-center gap-1"><i class="fa-solid fa-flask"></i> CHỜ BI</span>' : ''}
                                </div>
                            </td>
                        </tr>`;
                }).join(''); 
            }
        }
        kiemTraQuyenDuyetMeHap();

        if(document.getElementById("bangLichSuHap")) {
            const homNayMoiStr = getTodayDateStr(); 
            let tatCaMucCoLo = listGiaoDich.filter(x => x.batchCode && (x.ngayHapRealtime === homNayMoiStr || x.ngayTao === homNayMoiStr)); 
            let cacMeHapGop = {};
            tatCaMucCoLo.forEach(x => {
                if(!cacMeHapGop[x.batchCode]) { 
                    cacMeHapGop[x.batchCode] = { 
                        batchCode: x.batchCode, 
                        loaiHap: x.thongTinLoHap?.loaiHap || "Hấp hơi nước", 
                        chuKyNhiet: x.thongTinLoHap?.chuKyNhiet || "134°C - 4 phút", 
                        apSuat: x.thongTinLoHap?.apSuat || "N/A", 
                        thoiGian: x.thongTinLoHap?.thoiGianBatDau || x.time || "--:--", 
                        ngay: x.ngayHapRealtime || x.ngayTao || "", 
                        soLuongKhay: 0, 
                        trangThaiMe: x.status === "DANG_HAP" ? "ĐANG CHẠY LÒ" : "HOÀN THÀNH" 
                    }; 
                }
                cacMeHapGop[x.batchCode].soLuongKhay += 1;
            });
            let danhSachMeHapSapXep = Object.values(cacMeHapGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));
            if(danhSachMeHapSapXep.length === 0) { 
                document.getElementById("bangLichSuHap").innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa có mẻ hấp nào</td></tr>`; 
            } else { 
                document.getElementById("bangLichSuHap").innerHTML = danhSachMeHapSapXep.map(me => `
                    <tr class="hover:bg-slate-50 transition-colors border-b border-slate-100">
                        <td class="p-3">
                            <div class="flex items-center gap-2">
                                <span class="font-mono font-black text-rose-700 text-sm tracking-wider">${me.batchCode}</span>
                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "ĐANG CHẠY LÒ" ? "text-purple-700 bg-purple-100 border border-purple-200 animate-pulse" : "text-emerald-700 bg-emerald-100 border border-emerald-200"}">${me.trangThaiMe}</span>
                            </div>
                            <div class="text-[11px] text-slate-600 font-medium mt-1">${me.loaiHap} | ${me.chuKyNhiet} | Áp suất: ${me.apSuat} Bar</div>
                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Bắt đầu: ${me.thoiGian} (${me.ngay})</div>
                        </td>
                        <td class="p-3 text-right pr-4">
                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuongKhay} khay</span>
                        </td>
                    </tr>`).join(''); 
            }
        }
    }
    else if(activeTab === 'khovokhuan') {
        let uniqueKhoaSanCo = [...new Set(listGiaoDich.map(x => x.khoa))].filter(Boolean); const selKhoaXuat = document.getElementById("xuat_selKhoa");
        if(selKhoaXuat) {
            let currentSelected = selKhoaXuat.value; let htmlOpts = '<option value="">-- Chọn Khoa Muốn Trả Đồ --</option>';
            uniqueKhoaSanCo.forEach(k => { htmlOpts += `<option value="${k}" ${k === currentSelected ? 'selected' : ''}>${k}</option>`; });
            selKhoaXuat.innerHTML = htmlOpts;
        }

        let lsXK = listGiaoDich.filter(x => x.status === "CHO_XUAT").sort((a, b) => a.id - b.id);
        renderKhoVoKhuan(lsXK);

        let dsYeuCau = listGiaoDich.filter(x => x.status === "CHO_CAP_PHAT");
        let vungYeuCau = document.getElementById("vungYeuCauCapPhat");
        if (!vungYeuCau) {
            let parentContainer = document.getElementById("tab-khovokhuan");
            if (parentContainer) {
                let divYc = document.createElement("div");
                divYc.id = "vungYeuCauCapPhat";
                divYc.className = "mb-6 bg-amber-50/70 border border-amber-200 rounded-xl p-4 shadow-sm";
                parentContainer.insertBefore(divYc, parentContainer.firstChild);
                vungYeuCau = divYc;
            }
        }
        
        if (vungYeuCau) {
            if (dsYeuCau.length === 0) {
                vungYeuCau.innerHTML = `<h3 class="font-black text-amber-950 text-xs flex items-center gap-1.5"><i class="fa-solid fa-bell-slash"></i> HIỆN KHÔNG CÓ YÊU CẤP PHÁT ĐANG CHỜ</h3>`;
            } else {
                vungYeuCau.innerHTML = `
                    <h3 class="font-black text-amber-900 text-xs mb-2.5 flex items-center gap-1.5 animate-pulse">
                        <i class="fa-solid fa-bell text-rose-500 animate-bounce"></i> CÓ ${dsYeuCau.length} YÊU CẦU CẤP PHÁT TỪ CÁC KHOA LÂM SÀNG
                    </h3>
                    <div class="flex flex-col gap-2 max-h-48 overflow-y-auto">
                        ${dsYeuCau.map(yc => `
                            <div class="bg-white p-3 rounded-lg border border-amber-200 shadow-sm flex justify-between items-center text-xs">
                                <div>
                                    <span class="font-black text-amber-800 uppercase">[KHOA: ${yc.khoa}]</span> 
                                    Yêu cầu mâm: <span class="font-bold text-slate-800">${yc.bo}</span>
                                    <span class="text-[10px] text-slate-400 block mt-0.5">Thời gian: ${yc.time} - Người yêu cầu: ${yc.nvYeuCau || 'ĐD'}</span>
                                </div>
                                <div class="flex items-center gap-2">
                                    <button onclick="duyetCapPhatTậpTrung('${yc.firestoreId}', '${yc.bo}', '${yc.khoa}')" class="bg-amber-600 hover:bg-amber-700 text-white font-black px-3 py-2 rounded-lg text-[10px] uppercase shadow-sm flex items-center gap-1.5 transition-all">
                                        <i class="fa-solid fa-truck-ramp-box"></i>Cấp phát khay
                                    </button>
                                </div>
                            </div>
                        `).join('')}
                    </div>`;
            }
        }
        renderGioHangXuat();
    }
    else if(activeTab === 'quanlykho') {
        let fK = document.getElementById("inv_filterKhoa") ? document.getElementById("inv_filterKhoa").value : ""; 
        let uniqueKhoa = [...new Set(listGiaoDich.map(x=>x.khoa))].filter(Boolean);
        if(document.getElementById("inv_filterKhoa")) { document.getElementById("inv_filterKhoa").innerHTML = '<option value="">-- Tất cả --</option>' + uniqueKhoa.map(k=>`<option value="${k}" ${k===fK?'selected':''}>${k}</option>`).join(''); }
        let uniqueIDs = [...new Set(listGiaoDich.map(x=>x.maMacDinh))]; let arrHtml = []; const ngayHomNay = new Date(); ngayHomNay.setHours(0,0,0,0); 
        uniqueIDs.forEach(ma => {
            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a,b) => b.id - a.id)[0]; if(!currentTrans || !ma) return;
            let viTriCode = currentTrans.status; let khoaGiữ = currentTrans.khoa; if (fK && (khoaGiữ || "").toString().trim().toUpperCase() !== fK.toString().trim().toUpperCase()) return;
            let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";
            if(viTriCode === "HOAN_TAT") { viTriText = `Sẵn sàng tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 
            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã sử dụng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }
            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang vận chuyển`; viTriColor = "bg-purple-100 text-purple-800 animate-pulse"; }
            else if(viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }
            let hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-slate-100 text-slate-500">An toàn</span>`; let dongColorClass = ""; 
            if (currentTrans.hsd && (viTriCode === "CHO_XUAT" || viTriCode === "HOAN_TAT")) {
                const ngayHsd = new Date(currentTrans.hsd); ngayHsd.setHours(0,0,0,0); const soNgayConLai = Math.ceil((ngayHsd.getTime() - ngayHomNay.getTime()) / (1000 * 60 * 60 * 24));
                if (soNgayConLai < 0) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-rose-600 text-white">QUÁ HẠN</span>`; dongColorClass = "bg-rose-50/70 border-l-4 border-l-rose-500 font-bold"; } 
                else if (soNgayConLai <= 3) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-amber-500 text-white">HẠN < ${soNgayConLai} NGÀY</span>`; dongColorClass = "bg-amber-50/40 border-l-4 border-l-amber-500"; } 
                else { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-emerald-100 text-emerald-800">Hạn ${soNgayConLai} ngày</span>`; }
            }
            arrHtml.push(`<tr class="border-b border-slate-100 ${dongColorClass}"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-slate-500 text-[11px]">${khoaGiữ}</td><td class="p-3 text-center"><span class="px-2.5 py-1 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-mono font-bold text-slate-400">${currentTrans.batchCode || 'N/A'}</td><td class="p-3 text-center">${hsdBadget}</td></tr>`);
        });
        if(document.getElementById("bangTonKhoTe")) document.getElementById("bangTonKhoTe").innerHTML = arrHtml.join('');
    }
    else if(activeTab === 'danhmuc') {
        const tbody = document.getElementById("bangDanhMucTong"); if (!tbody) return;
        let uniqueIDs = [...new Set(listGiaoDich.map(x => x.maMacDinh))].filter(Boolean); let arrHtml = [];
        uniqueIDs.forEach(ma => {
            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a, b) => b.id - a.id)[0]; if (!currentTrans) return;
            let viTriCode = currentTrans.status; let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";
            if (viTriCode === "HOAN_TAT") { viTriText = `Tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 
            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã dùng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }
            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang đi đường`; viTriColor = "bg-purple-100 text-purple-800"; }
            else if (viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }
            let chuKyLo = listGiaoDich.filter(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG" || x.status === "ĐANG_VAN_CHUYEN")).length;
            arrHtml.push(`<tr class="border-b border-slate-100 font-medium"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-center"><span class="px-2.5 py-0.5 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-black text-amber-700 bg-amber-50/50">${chuKyLo} lần lễ</td></tr>`);
        });
        tbody.innerHTML = arrHtml.join('');
    }
    else if(activeTab === 'lichsuluanchuyen') {
        const tbody = document.getElementById("bangLichSuHanhTrinhGoc");
        if(tbody) {
            tbody.innerHTML = listGiaoDich.map(x => {
                let statusBadge = `<span class="px-2 py-0.5 bg-slate-100 text-slate-700 font-bold rounded text-[10px]">${x.status}</span>`;
                if(x.status === "HOAN_TAT") statusBadge = `<span class="px-2 py-0.5 bg-emerald-100 text-emerald-800 font-black rounded text-[10px]">ĐÃ BÀN GIAO</span>`;
                else if(x.status === "CHO_XUAT") statusBadge = `<span class="px-2 py-0.5 bg-teal-100 text-teal-800 font-bold rounded text-[10px]">KHO VÔ KHUẨN</span>`;
                else if(x.status === "DANG_HAP") statusBadge = `<span class="px-2 py-0.5 bg-purple-100 text-purple-800 font-bold rounded text-[10px]">ĐANG HẤP LÒ</span>`;
                else if(x.status === "DA_SU_DUNG") statusBadge = `<span class="px-2 py-0.5 bg-rose-600 text-white font-black rounded text-[10px]">SỬ DỤNG BN</span>`;
                else if(x.status === "ĐANG_VAN_CHUYEN") statusBadge = `<span class="px-2 py-0.5 bg-purple-500 text-white font-bold rounded text-[10px] animate-pulse">ĐANG VẬN CHUYỂN</span>`;
                else if(x.status === "DANG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-blue-100 text-blue-700 font-bold rounded text-[10px]">HÀNG ĐỢI RỬA</span>`;
                else if(x.status === "TRONG_BUONG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-cyan-600 text-white font-bold rounded text-[10px]">ĐANG RỬA MÁY</span>`;
                else if(x.status === "CHO_CAP_PHAT") statusBadge = `<span class="px-2 py-0.5 bg-amber-100 text-amber-800 font-bold rounded text-[10px] border border-amber-200">CHỜ CẤP PHÁT</span>`;
                
                let fontColor = ""; let loGoc = listGiaoDich.find(m => m.batchCode === x.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 
                let anhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || x.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;
                if (anhBase64) fontColor = `<br><span onclick="handleViewAnhBiMoi('${x.firestoreId}')" class="text-[10px] text-emerald-600 bg-emerald-50 border border-emerald-300 rounded px-1 cursor-pointer font-bold mt-1 inline-block"><i class="fa-solid fa-image mr-1"></i>Xem ảnh BI</span>`;
                
                let kpiHtml = x.ketQuaGiamSatKpi ? `<br><span class="text-[9px] px-1 rounded font-black border mt-1 inline-block ${x.ketQuaGiamSatKpi === "ĐẠT" ? "text-emerald-700 bg-emerald-50 border-emerald-200" : "text-rose-700 bg-rose-50 border-rose-200"}">KPI: ${x.ketQuaGiamSatKpi} (${x.thoiGianTinhKpiPhut}m)</span>` : "";
                let thongTinKemTheo = x.nvXuatKho || '<span class="text-slate-300 font-normal">Chưa xuất</span>';
                if (x.nguoiKyNhanKhoa) thongTinKemTheo += ` ➔ <span class="text-emerald-700 font-semibold">[ĐD: ${x.nguoiKyNhanKhoa}]</span>`;
                if (x.status === "DA_SU_DUNG" && x.thongTinBenhNhan) thongTinKemTheo = `<span class="text-teal-700 font-black">BN: ${x.thongTinBenhNhan.thongTinTimKiemBN}</span>`;

                return `<tr class="border-b text-xs hover:bg-slate-50 transition-colors"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3 text-center">${statusBadge}${kpiHtml}</td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/30">${x.batchCode || 'N/A'}${fontColor}</td><td class="p-3 text-center font-bold text-sky-800 bg-sky-50/40">${thongTinKemTheo}</td><td class="p-3 text-center text-slate-400 font-mono text-[11px]">${x.ngayTao || ''} ${x.time || ''}</td></tr>`;
            }).join('');
        }
    }
    else if(activeTab === 'tracuu') {
        let safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");
        if (!safeTbody) {
            let parentContainer = document.getElementById("tab-tracuu") || document.querySelector('.bg-white.p-6.rounded-xl.shadow-md');
            if(parentContainer) {
                let existingTableZone = document.getElementById("vung-ket-qua-tu-dong");
                if(!existingTableZone) {
                    let tableWrapper = document.createElement("div"); tableWrapper.id = "vung-ket-qua-tu-dong"; tableWrapper.className = "mt-6 bg-white border border-slate-200 rounded-xl p-4 shadow-sm";
                    tableWrapper.innerHTML = `<h3 class="font-black text-slate-800 text-sm mb-3 text-sky-800"><i class="fa-solid fa-list-check mr-2"></i>DANH SÁCH MÂM DỤNG CỤ TRONG MẺ TRUY VẾT</h3><div class="overflow-x-auto"><table class="w-full text-left border-collapse"><thead><tr class="bg-slate-100 text-[11px] font-bold text-slate-600 uppercase border-b"><th class="p-3">Mã ID Khay</th><th class="p-3">Tên Bộ Dụng Cụ</th><th class="p-3">Khoa Sử Dụng</th><th class="p-3">Trạng thái</th><th class="p-3 text-center">Mã Lô Hấp</th><th class="p-3 text-center">Thời Gian Kích Hoạt</th></tr></thead><tbody id="bangLichSuTruyXuatAdmin"></tbody></table></div>`;
                    parentContainer.appendChild(tableWrapper); safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");
                }
            }
        }
        let searchInp = maLoTruyVetToanCuc || ""; if (!searchInp && document.getElementById("inp_searchBatch")) { searchInp = document.getElementById("inp_searchBatch").value.trim(); }
        if(safeTbody) {
            if (!searchInp) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng nhập mã mẻ hấp và nhấn "TRA CỨU KHẨN CẤP"...</td></tr>`; return; }
            let dataFiltered = listGiaoDich.filter(x => x.batchCode && String(x.batchCode).toUpperCase().includes(searchInp.toUpperCase()));
            if(dataFiltered.length === 0) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-rose-500 italic font-bold">Không tìm thấy dữ liệu: ${searchInp}</td></tr>`; } 
            else { safeTbody.innerHTML = dataFiltered.map(x => `<tr class="border-b text-xs hover:bg-slate-50 font-medium"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3"><span class="px-2 py-0.5 rounded text-[10px] ${x.status === "HOAN_TAT" ? "bg-emerald-100 text-emerald-800 font-bold" : "bg-teal-100 text-teal-800"}">${x.status}</span></td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/40">${x.batchCode || 'N/A'}</td><td class="p-3 text-center text-slate-400 font-mono">${x.ngayTao || ''} ${x.time || ''}</td></tr>`).join(''); }
        }
    }
    else if(activeTab === 'performance') { renderKpiPerformanceGoc(); }
    else if(activeTab === 'dashboard_tv') { renderDashboardTiviRealtime(); }
}

function renderKhoVoKhuan(listData) {
    const tbody = document.getElementById("bangKhoVoKhuan");
    if (!tbody) return;

    if (!listData || listData.length === 0) {
        tbody.innerHTML = `<tr><td colspan="6" class="p-6 text-center text-slate-400 italic">Kho vô khuẩn hiện tại trống.</td></tr>`;
        return;
    }

    tbody.innerHTML = listData.map(item => `
        <tr class="hover:bg-slate-50 transition-colors">
            <td class="p-3.5 font-bold text-slate-800 text-xs">${item.bo ? String(item.bo).split(" [ID:")[0] : 'N/A'}</td>
            <td class="p-3.5 font-mono font-black text-sky-700 text-xs">${item.maMacDinh || '--'}</td>
            <td class="p-3.5 text-center font-semibold text-slate-600 text-xs">${item.khoa || 'Tập trung'}</td>
            <td class="p-3.5 text-center"><span class="bg-emerald-100 text-emerald-800 font-bold px-2.5 py-1 rounded-full text-[11px]">${item.viTriKe || 'Kệ A1'}</span></td>
            <td class="p-3.5 text-center font-mono font-bold text-xs text-rose-600">${item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN') : 'An toàn'}</td>
            <td class="p-2 text-center">
                <button onclick="xuatKhoXoayVong('${item.maMacDinh}')" 
                        class="min-h-[44px] px-4 bg-emerald-600 active:bg-emerald-700 text-white font-bold rounded-lg text-xs shadow-sm inline-flex items-center gap-1.5 transition-transform active:scale-95">
                    <i class="fa-solid fa-cart-flatbed"></i>
                    <span>Xuất Khay</span>
                </button>
            </td>
        </tr>
    `).join('');
}

// =========================================================================
// VÁ LỖI CÁC HÀM TRUY VẾT & ĐỒNG BỘ HIỆU SUẤT TV MÀN HÌNH
// =========================================================================
function truyVetTheoMaBatch() {
    const inp = document.getElementById("inp_searchBatch");
    if (inp) {
        maLoTruyVetToanCuc = inp.value.trim();
        playSound('success');
        callRender();
    }
}

function clearTruyVetBatch() {
    const inp = document.getElementById("inp_searchBatch");
    if (inp) inp.value = "";
    maLoTruyVetToanCuc = "";
    callRender();
}

function resetDuLieuKet() {
    if (confirm("Anh Hùng có chắc chắn muốn giải phóng các mâm đang xử lý dở dang (chưa Hoàn tất / Chưa dùng) về trạng thái Chờ thu gom?")) {
        let p = [];
        listGiaoDich.forEach(x => {
            if (x.status !== "HOAN_TAT" && x.status !== "DA_SU_DUNG") {
                p.push(db.collection("phieuGiaoNhan").doc(x.firestoreId).update({ status: "CHO_THU" }));
            }
        });
        Promise.all(p).then(() => {
            playSound('success');
            showToast("Đã giải phóng dữ liệu mâm kẹt thành công!", "success");
            callRender();
        });
    }
}

function renderKpiPerformanceGoc() {
    const tbody = document.getElementById("bangHieuSuatKTV");
    if (!tbody) return;
    let kpiGop = {};
    listGiaoDich.forEach(x => {
        let ktv = x.ktvThuGom || x.nvXuatKho || "CSSD_CHUNG";
        if (!kpiGop[ktv]) kpiGop[ktv] = { code: ktv, ten: ktv, dat: 0, tong: 0 };
        if (x.ketQuaGiamSatKpi === "ĐẠT") kpiGop[ktv].dat += 1;
        if (x.ketQuaGiamSatKpi) kpiGop[ktv].tong += 1;
    });
    let sortedKpi = Object.values(kpiGop).sort((a,b) => b.dat - a.dat);
    if(sortedKpi.length === 0) {
        tbody.innerHTML = `<tr><td colspan="4" class="p-4 text-center italic text-slate-400">Hôm nay chưa có dữ liệu mẻ KPI nào.</td></tr>`;
    } else {
        tbody.innerHTML = sortedKpi.map((item, idx) => `
            <tr class="border-b text-xs hover:bg-slate-50 font-medium">
                <td class="p-3 text-center font-bold">${idx + 1}</td>
                <td class="p-3 font-mono font-bold text-sky-700">${item.code}</td>
                <td class="p-3 font-bold text-slate-800">${item.ten}</td>
                <td class="p-3 text-center"><span class="bg-emerald-100 text-emerald-800 px-2.5 py-1 rounded font-black">${item.dat}/${item.tong} Mẻ Đạt KPI</span></td>
            </tr>
        `).join('');
    }
}

function renderDashboardTiviRealtime() {
    let meRuaInDay = listGiaoDich.filter(x => x.rua_batchCode).length;
    let meHapInDay = listGiaoDich.filter(x => x.batchCode).length;
    let dangRua = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA").length;
    let dangHap = listGiaoDich.filter(x => x.status === "DANG_HAP").length;
    let sanSangXuat = listGiaoDich.filter(x => x.status === "CHO_XUAT").length;
    
    const today = new Date(); today.setHours(0,0,0,0);
    let quaHan = 0;
    listGiaoDich.forEach(x => {
        if (x.hsd && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT")) {
            if (new Date(x.hsd) < today) quaHan++;
        }
    });

    if (document.getElementById('tv_meRua')) document.getElementById('tv_meRua').innerText = meRuaInDay;
    if (document.getElementById('tv_meHap')) document.getElementById('tv_meHap').innerText = meHapInDay;
    if (document.getElementById('tv_meHap2')) document.getElementById('tv_meHap2').innerText = meHapInDay;
    if (document.getElementById('tv_dangRua')) document.getElementById('tv_dangRua').innerText = dangRua;
    if (document.getElementById('tv_dangHap')) document.getElementById('tv_dangHap').innerText = dangHap;
    if (document.getElementById('tv_khoVoKhuan')) document.getElementById('tv_khoVoKhuan').innerText = sanSangXuat;
    if (document.getElementById('tv_canhBaoHsd')) document.getElementById('tv_canhBaoHsd').innerText = quaHan;
}

function renderAdminInterface() {
    if (document.getElementById("cfg_pinAdmin")) document.getElementById("cfg_pinAdmin").value = thongTinMatKhauAdmin.adminPIN || "";
    if (document.getElementById("cfg_pinCSSD")) document.getElementById("cfg_pinCSSD").value = thongTinMatKhauAdmin.cssdPIN || "";
    if (document.getElementById("cfg_pinGuest")) document.getElementById("cfg_pinGuest").value = thongTinMatKhauAdmin.guestPIN || "";
    
    const MatrixRoles = ["CSSD", "KHOA", "GUEST"];
    
    // Đã loại bỏ hoàn toàn các số thứ tự 1., 2., 3.... khỏi tên tab
    const MatrixTabs = [
        { id: "khoaphong", name: "Cổng Báo Trả Đồ" },
        { id: "thugom", name: "Xe Thu Gom" },
        { id: "mayrua", name: "Quản Lý Mẻ Rửa" },
        { id: "donggoi", name: "Làm Sạch & Đóng Gói" },
        { id: "mayhap", name: "Quản Lý Mẻ Hấp" },
        { id: "khovokhuan", name: "Kho Vô Khuẩn" },
        { id: "quanlykho", name: "Tồn Kho Toàn Viện" },
        { id: "danhmuc", name: "Giám Sát Tuổi Thọ Dụng Cụ" },
        { id: "lichsuluanchuyen", name: "Nhật Ký Luân Chuyển" },
        { id: "tracuu", name: "Truy xuất mẻ hấp" },
        { id: "performance", name: "Hiệu Suất KPI" },
        { id: "dashboard_tv", name: "Màn Hình Tivi" }
    ];
    
    let htmlMatrix = "";
    MatrixTabs.forEach(tab => {
        htmlMatrix += `<tr class="border-b font-medium text-xs hover:bg-slate-50"><td class="p-3 font-bold text-slate-700">${tab.name}</td>`;
        MatrixRoles.forEach(role => {
            let isChecked = (cauHinhGiaoDien[role] && cauHinhGiaoDien[role].includes(tab.id)) ? "checked" : "";
            htmlMatrix += `<td class="p-3 text-center"><input type="checkbox" ${isChecked} onchange="updateGiaoDienMatrix('${role}', '${tab.id}', this)" class="w-4 h-4 rounded text-sky-600 focus:ring-sky-500"></td>`;
        });
        htmlMatrix += `</tr>`;
    });
    const tbodyMatrix = document.getElementById("bodyMaTranGiaoDien"); if (tbodyMatrix) tbodyMatrix.innerHTML = htmlMatrix;
    const trKhoa = document.getElementById("bangPhanQuyenKhoa");
    if(trKhoa) {
        if(danhSachKhoa.length === 0) { trKhoa.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-rose-500 font-bold italic">Danh sách Khoa trống.</td></tr>`; } 
        else { trKhoa.innerHTML = danhSachKhoa.map((k, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-slate-700 text-[11px]">${k.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-rose-600 text-sm bg-rose-50/50">${k.pin || '123'}</td><td class="p-3 text-center"><div class="flex items-center justify-center gap-2"><input type="text" id="pin-khoa-${index}" placeholder="PIN" class="w-24 p-1.5 text-center border border-slate-300 rounded text-xs font-bold"><button onclick="updatePINTrựcTiep(${index}, '${k.ten}')" class="bg-sky-600 text-white font-bold py-1.5 px-3 rounded shadow text-[10px]">ĐỔI PIN</button></div></td></tr>`).join(''); }
    }
    const tbKtv = document.getElementById("bangNhanVienCssd");
    if(tbKtv) {
        if(danhSachKtvCssd.length === 0) { tbKtv.innerHTML = `<tr><td colspan="4" class="p-4 text-center text-slate-400 italic">Chưa có Nhân viên.</td></tr>`; }
        else { tbKtv.innerHTML = danhSachKtvCssd.map((nv, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-sky-700 text-xs">${nv.code}</td><td class="p-3 font-bold text-slate-700 text-[11px]">${nv.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-sky-600 text-sm bg-sky-50/50">${nv.pin}</td><td class="p-3 text-center"><button onclick="xaoKtvCssd('${nv.code}')" class="text-rose-600 font-bold text-[10px]"><i class="fa-solid fa-trash-can mr-1"></i>XÓA</button></td></tr>`).join(''); }
    }
}

// =========================================================================
// 14. XỬ LÝ ĐỌC FILE EXCEL ĐA SHEET
// =========================================================================
document.addEventListener("DOMContentLoaded", () => {
    const fileInput = document.getElementById("excelFileInput");
    if (fileInput) {
        fileInput.addEventListener("change", (e) => {
            const file = e.target.files[0];
            if (!file) return;

            const reader = new FileReader();
            reader.onload = function(e) {
                try {
                    const data = new Uint8Array(e.target.result);
                    const workbook = XLSX.read(data, { type: 'array' });
                    
                    let updateData = {};
                    let logs = [];

                    workbook.SheetNames.forEach(sheetName => {
                        const worksheet = workbook.Sheets[sheetName];
                        const jsonData = XLSX.utils.sheet_to_json(worksheet);
                        if (jsonData.length === 0) return;

                        const firstRow = jsonData[0];
                        const keys = Object.keys(firstRow).map(k => k.trim().toUpperCase());

                        const coCotMaDC = keys.includes("MÃ DC");
                        if (coCotMaDC) {
                            let mapKhoa = {};
                            danhSachKhoa.forEach(k => {
                                mapKhoa[k.ten.trim().toUpperCase()] = {
                                    ten: k.ten.trim().toUpperCase(),
                                    pin: k.pin || "123",
                                    danhSachBo: []
                                };
                            });

                            jsonData.forEach(row => {
                                let rawKeyKhoa = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS (I)");
                                let rawKeyBo = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS CHUẨN");
                                let rawKeyMa = Object.keys(row).find(k => k.trim().toUpperCase() === "MÃ DC");

                                let tenKhoa = rawKeyKhoa ? row[rawKeyKhoa] : null;
                                let tenBo = rawKeyBo ? row[rawKeyBo] : null;
                                let maKhay = rawKeyMa ? row[rawKeyMa] : null;

                                if (tenKhoa && tenBo) {
                                    let keyKhoa = String(tenKhoa).trim().toUpperCase();
                                    let cleanBo = String(tenBo).trim().toUpperCase();
                                    let cleanMa = maKhay ? String(maKhay).trim().toUpperCase() : "CHUA_CO_ID";
                                    let chuoiDinhDangKhay = `${cleanBo} [ID:${cleanMa}]`;

                                    if (!mapKhoa[keyKhoa]) {
                                        mapKhoa[keyKhoa] = { ten: keyKhoa, pin: "123", danhSachBo: [] };
                                    }
                                    if (!mapKhoa[keyKhoa].danhSachBo.includes(chuoiDinhDangKhay)) {
                                        mapKhoa[keyKhoa].danhSachBo.push(chuoiDinhDangKhay);
                                    }
                                }
                            });

                            updateData.danhSachKhoa = Object.values(mapKhoa);
                            logs.push(`Cập nhật định biên cơ số cho ${updateData.danhSachKhoa.length} Khoa/Phòng`);
                        }

                        const coCotMaAesculap = keys.includes("AB 120/12");
                        if (coCotMaAesculap) {
                            updateData.databaseExcel = jsonData;
                            logs.push(`Nạp thành công ${jsonData.length} linh kiện chi tiết mâm Aesculap`);
                        }
                    });

                    if (Object.keys(updateData).length === 0) {
                        return showToast("Không tìm thấy cấu trúc dữ liệu phù hợp (Cột 'MÃ DC' hoặc 'AB 120/12') trong file Excel up lên!", "error");
                    }

                    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update(updateData)
                    .then(() => {
                        playSound('success');
                        showToast(`Đồng bộ thành công: ${logs.join(" | ")}!`, "success");
                        fileInput.value = ""; 
                        callRender();
                    }).catch(err => {
                        console.error("Lỗi cập nhật Firestore:", err);
                        showToast("Thất bại khi đẩy dữ liệu lên Cloud Firestore!", "error");
                    });

                } catch (err) {
                    console.error("Lỗi FileReader:", err);
                    showToast("Định dạng file không được hỗ trợ hoặc bị lỗi!", "error");
                }
            };
            reader.readAsArrayBuffer(file);
        });
    }
});
Hiện tôi ko thấy hiện ra 

Hình ảnh màn hình thực tế cho thấy thanh menu điều hướng bên trái (Sidebar HTML) vẫn đang sử dụng danh sách các mục cũ có số thứ tự (ví dụ: 9. Nhật Ký Luân Chuyển, 10. Cấu Hình Hệ Thống...) và chưa có nút menu "Truy xuất mẻ hấp".

Lý do là hàm app.js quản lý logic chuyển đổi tab, nhưng cấu trúc giao diện HTML (file index.html) vẫn chưa được thêm nút menu này và bỏ số thứ tự ở sidebar bên trái.

Hướng dẫn cập nhật HTML & JS đồng bộ:
Anh hãy cập nhật 2 phần sau để menu hiển thị chính xác:

1. Cập nhật Sidebar Menu trong file HTML (thay thế đoạn <div id="sidebar_menu">...</div>)
Loại bỏ toàn bộ các số thứ tự 1., 2., 7., 9., 10.... và thêm nút Truy xuất mẻ hấp (menu-tracuu):

HTML
<!-- CỤM LÂM SÀNG -->
<div id="header-lamsang" class="px-4 pt-3 pb-1 text-[10px] font-black text-slate-400 uppercase tracking-wider">Lâm Sàng</div>
<div id="menu-khoaphong" onclick="switchTab('khoaphong')" class="sidebar-item flex items-center px-4 py-2.5 text-xs font-bold text-slate-700 cursor-pointer hover:bg-slate-100 transition-colors">
    <i class="fa-solid fa-file-invoice mr-3 text-sky-600 w-4 text-center"></i>
    <span>Cổng Lâm Sàng (Báo Trả)</span>
</div>

<!-- CỤM VẬN HÀNH TIỆT KHUẨN -->
<div id="header-vanhanh" class="px-4 pt-3 pb-1 text-[10px] font-black text-slate-400 uppercase tracking-wider">Vận Hành Tiệt Khuẩn</div>
<div id="menu-thugom" onclick="switchTab('thugom')" class="sidebar-item flex items-center px-4 py-2.5 text-xs font-bold text-slate-700 cursor-pointer hover:bg-slate-100 transition-colors">
    <i class="fa-solid fa-truck-ramp-box mr-3 text-sky-600 w-4 text-center"></i>
    <span>Điều Phối Xe Thu Gom</span>
</div>
<div id="menu-mayrua" onclick="switchTab('mayrua')" class="sidebar-item flex items-center px-4 py-2.5 text-xs font-bold text-slate-700 cursor-pointer hover:bg-slate-100 transition-colors">
    <i class="fa-solid fa-soap mr-3 text-sky-600 w-4 text-center"></i>
    <span>Quản Lý Mẻ Rửa</span>
</div>
<div id="menu-donggoi" onclick="switchTab('donggoi')" class="sidebar-item flex items-center px-4 py-2.5 text-xs font-bold text-slate-700 cursor-pointer hover:bg-slate-100 transition-colors">
    <i class="fa-solid fa-box mr-3 text-sky-600 w-4 text-center"></i>
    <span>Trạm Làm Sạch & Gói</span>
</div>
<div id="menu-mayhap" onclick="switchTab('mayhap')" class="sidebar-item flex items-center px-4 py-2.5 text-xs font-bold text-slate-700 cursor-pointer hover:bg-slate-100 transition-colors">
    <i class="fa-solid fa-fire-burner mr-3 text-sky-600 w-4 text-center"></i>
    <span>Quản Lý Mẻ Hấp</span>
</div>
<div id="menu-khovokhuan" onclick="switchTab('khovokhuan')" class="sidebar-item flex items-center px-4 py-2.5 text-xs font-bold text-slate-700 cursor-pointer hover:bg-slate-100 transition-colors">
    <i class="fa-solid fa-warehouse mr-3 text-sky-600 w-4 text-center"></i>
    <span>Kho Vô Khuẩn (Xuất)</span>
</div>

<!-- CỤM DỮ LIỆU & TỒN KHO -->
<div id="header-dulieu" class="px-4 pt-3 pb-1 text-[10px] font-black text-slate-400 uppercase tracking-wider">Dữ Liệu & Tồn Kho</div>
<div id="menu-quanlykho" onclick="switchTab('quanlykho')" class="sidebar-item flex items-center px-4 py-2.5 text-xs font-bold text-slate-700 cursor-pointer hover:bg-slate-100 transition-colors">
    <i class="fa-solid fa-boxes-stacked mr-3 text-sky-600 w-4 text-center"></i>
    <span>Tồn Kho Dụng Cụ</span>
</div>
<div id="menu-danhmuc" onclick="switchTab('danhmuc')" class="sidebar-item flex items-center px-4 py-2.5 text-xs font-bold text-slate-700 cursor-pointer hover:bg-slate-100 transition-colors">
    <i class="fa-solid fa-microchip mr-3 text-sky-600 w-4 text-center"></i>
    <span>Giám Sát Tuổi Thọ</span>
</div>
<div id="menu-lichsuluanchuyen" onclick="switchTab('lichsuluanchuyen')" class="sidebar-item flex items-center px-4 py-2.5 text-xs font-bold text-slate-700 cursor-pointer hover:bg-slate-100 transition-colors">
    <i class="fa-solid fa-clock-rotate-left mr-3 text-sky-600 w-4 text-center"></i>
    <span>Nhật Ký Luân Chuyển</span>
</div>

<!-- MỤC TRUY XUẤT MẺ HẤP TÁCH RIÊNG (MỚI) -->
<div id="menu-tracuu" onclick="switchTab('tracuu')" class="sidebar-item flex items-center px-4 py-2.5 text-xs font-bold text-slate-700 cursor-pointer hover:bg-slate-100 transition-colors">
    <i class="fa-solid fa-magnifying-glass-location mr-3 text-rose-600 w-4 text-center"></i>
    <span class="text-rose-700 font-black">Truy xuất mẻ hấp</span>
</div>

<div id="menu-performance" onclick="switchTab('performance')" class="sidebar-item flex items-center px-4 py-2.5 text-xs font-bold text-slate-700 cursor-pointer hover:bg-slate-100 transition-colors">
    <i class="fa-solid fa-chart-line mr-3 text-sky-600 w-4 text-center"></i>
    <span>Hiệu Suất KPI</span>
</div>
<div id="menu-dashboard_tv" onclick="switchTab('dashboard_tv')" class="sidebar-item flex items-center px-4 py-2.5 text-xs font-bold text-slate-700 cursor-pointer hover:bg-slate-100 transition-colors">
    <i class="fa-solid fa-tv mr-3 text-sky-600 w-4 text-center"></i>
    <span>Màn Hình Trực TIVI</span>
</div>
2. Thêm thẻ Container cho tab "Truy xuất mẻ hấp" trong HTML (tab-tracuu)
Thêm đoạn container dưới đây vào vùng chứa nội dung chính (main-content) của file HTML để khi nhấn vào tab Truy xuất mẻ hấp, giao diện sẽ hiện ra khung tìm kiếm độc lập:

HTML
<!-- TAB: TRUY XUẤT MẺ HẤP (TÁCH ĐỘC LẬP) -->
<div id="tab-tracuu" class="hidden space-y-6">
    <div class="bg-white p-6 rounded-2xl shadow-sm border border-slate-200/80">
        <div class="flex items-center justify-between border-b border-slate-100 pb-4 mb-5">
            <div>
                <h2 class="text-base font-black text-slate-800 uppercase tracking-wider flex items-center gap-2">
                    <i class="fa-solid fa-shield-virus text-rose-600"></i>
                    TRẠM TRUY VẾT NHIỄM TRÙNG KHẨN CẤP (KHOANH VÙNG THEO MẺ)
                </h2>
                <p class="text-xs text-slate-400 font-medium mt-0.5">Nhập chính xác mã lô/mẻ hấp để kiểm tra danh sách tất cả các mâm dụng cụ liên quan</p>
            </div>
        </div>

        <div class="flex flex-col sm:flex-row gap-3">
            <input type="text" id="inp_searchBatch" placeholder="Nhập chính xác mã lô cần truy vết (VD: A1260721_01)..." 
                   class="flex-1 border border-rose-300 focus:border-rose-500 focus:ring-2 focus:ring-rose-200 rounded-xl px-4 py-3 text-sm font-mono font-bold text-slate-800 uppercase outline-none transition-all">
            <div class="flex gap-2">
                <button onclick="truyVetTheoMaBatch()" class="bg-rose-600 hover:bg-rose-700 active:scale-95 text-white font-black px-6 py-3 rounded-xl text-xs uppercase tracking-wider shadow-sm transition-all flex items-center gap-2">
                    <i class="fa-solid fa-magnifying-glass"></i> Tra Cứu Khẩn Cấp
                </button>
                <button onclick="clearTruyVetBatch()" class="bg-slate-100 hover:bg-slate-200 active:scale-95 text-slate-600 font-bold px-4 py-3 rounded-xl text-xs uppercase transition-all">
                    Reset
                </button>
            </div>
        </div>
    </div>

    <!-- VÙNG HIỂN THỊ KẾT QUẢ TRUY VẾT -->
    <div class="bg-white p-6 rounded-2xl shadow-sm border border-slate-200/80">
        <h3 class="font-black text-slate-800 text-sm mb-4 text-sky-900 flex items-center gap-2">
            <i class="fa-solid fa-list-check text-sky-600"></i>
            DANH SÁCH MÂM DỤNG CỤ TRONG MẺ TRUY VẾT
        </h3>
        <div class="overflow-x-auto">
            <table class="w-full text-left border-collapse">
                <thead>
                    <tr class="bg-slate-50 text-[11px] font-black text-slate-500 uppercase border-b border-slate-200/80">
                        <th class="p-3">Mã ID Khay</th>
                        <th class="p-3">Tên Bộ Dụng Cụ</th>
                        <th class="p-3">Khoa Sử Dụng</th>
                        <th class="p-3">Trạng Thái</th>
                        <th class="p-3 text-center">Mã Lô Hấp</th>
                        <th class="p-3 text-center">Thời Gian Kích Hoạt</th>
                    </tr>
                </thead>
                <tbody id="bangLichSuTruyXuatAdmin">
                    <tr>
                        <td colspan="6" class="p-6 text-center text-slate-400 italic">Vui lòng nhập mã mẻ hấp và nhấn "TRA CỨU KHẨN CẤP"...</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>
3. Bổ sung hàm ẩn/hiện menu trong app.js
Để đảm bảo danh sách tab không bị ẩn sót menu-tracuu khi phân quyền, anh cập nhật hàm anTatCaHeadersVaMenus() và apDungPhanQuyenGiaoDien() trong file app.js:

JavaScript
function anTatCaHeadersVaMenus() {
    document.getElementById('header-lamsang')?.classList.add('hidden');
    document.getElementById('header-vanhanh')?.classList.add('hidden');
    document.getElementById('header-dulieu')?.classList.add('hidden');
    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => {
        document.getElementById('menu-' + x)?.classList.add('hidden');
    });
}

function apDungPhanQuyenGiaoDien(role) {
    anTatCaHeadersVaMenus();
    if (role === "ADMIN") {
        ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(tab => {
            document.getElementById('menu-' + tab)?.classList.remove('hidden');
        });
        document.getElementById('header-lamsang')?.classList.remove('hidden');
        document.getElementById('header-vanhanh')?.classList.remove('hidden');
        document.getElementById('header-dulieu')?.classList.remove('hidden');
        return;
    }
    let tabsDuocPhep = cauHinhGiaoDien[role] || [];
    tabsDuocPhep.forEach(tab => { document.getElementById('menu-' + tab)?.classList.remove('hidden'); });
    if (tabsDuocPhep.includes('khoaphong')) document.getElementById('header-lamsang')?.classList.remove('hidden');
    if (['thugom','mayrua','donggoi','mayhap','khovokhuan'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-vanhanh')?.classList.remove('hidden');
    if (['quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-dulieu')?.classList.remove('hidden');
}
Sau khi dán các đoạn mã này, menu bên trái sẽ mất hết số thứ tự và nút "Truy xuất mẻ hấp" sẽ xuất hiện màu đỏ nổi bật ngay dưới mục Nhật Ký Luân Chuyển.

// =========================================================================

// 1. KHỞI TẠO HỆ THỐNG & CẤU HÌNH BIẾN TOÀN CỤC (PN HOSPITAL)

// =========================================================================

const firebaseConfig = { 

    apiKey: "AIzaSyCxjdCTKHQlpm7SYbWCEws1HhcOaFp0LBA", 

    authDomain: "cssd-system-2878c.firebaseapp.com", 

    projectId: "cssd-system-2878c", 

    storageBucket: "cssd-system-2878c.firebasestorage.app", 

    messagingSenderId: "662377321937", 

    appId: "1:662377321937:web:001c092e10319547623cf0" 

};

firebase.initializeApp(firebaseConfig); 

const db = firebase.firestore();



let thongTinMatKhauAdmin = { adminPIN: "admin2026", cssdPIN: "cssd2026", guestPIN: "guest2026" };

let cauHinhGiaoDien = {

    "ADMIN": ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'],

    "CSSD": ['thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','dashboard_tv'], 

    "KHOA": ['khoaphong','quanlykho','lichsuluanchuyen','tracuu'], 

    "GUEST": ['quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv']

};



let currentRole = "", loginUserCode = "";

let danhSachKhoa = [], listGiaoDich = [], gioHangTam = [], danhSachKtvCssd = [], databaseExcel = [];

let html5QrCode = null; let targetInputIdForScan = ""; let idDangDongGoi = null;

let activeTab = 'thugom'; let renderTimeout = null;

let duLieuAnhBiTamBase64 = ""; let maLoTruyVetToanCuc = ""; 

let gioHangXuatKho = []; let gioKhaySuDungTam = []; 

let currentKiemDemData = null;



// Biến hỗ trợ vẽ chữ ký điện tử

let canvasSignature = null;

let ctxSignature = null;

let isDrawingSignature = false;

let dsIdKhayChoKyNhan = [];



// Cấu hình máy hấp & máy rửa (Belimed WD250)

const cauHinhMayHap = { "Hấp hơi nước": ["A1", "A2", "A3", "A4"], "Hấp H2O2 (Plasma)": ["P1", "P2"], "Khử khuẩn EO": ["EO1", "EO2"] };

const cauHinhMayRua = {

    "Máy rửa khử khuẩn tự động": ["Belimed WD250"],

    "Máy rửa sóng siêu âm": ["US1"],

    "Bồn rửa thủ công": ["MAN1"]

};

const danhMucHoaChatRua = [

    { ten: "Cidezyme (Enzym phân hủy protein)", lieuLuong: "8 ml/L" },

    { ten: "Sekusept Active (Khử khuẩn mức độ cao)", lieuLuong: "10 ml/L" },

    { ten: "Smeg (Nước rửa trung tính chuyên dụng)", lieuLuong: "5 ml/L" },

    { ten: "Aniosyme DD1 (Làm sạch & khử trùng sơ bộ)", lieuLuong: "5 ml/L" }

];



function getTodayDateStr() { 

    const d = new Date(); 

    return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`; 

}



function playSound(type) {

    try {

        const AudioContext = window.AudioContext || window.webkitAudioContext;

        if (!AudioContext) return;

        const ctx = new AudioContext();

        const osc = ctx.createOscillator();

        const gain = ctx.createGain();

        osc.connect(gain); gain.connect(ctx.destination);

        if (type === 'success') {

            osc.type = 'sine'; osc.frequency.setValueAtTime(800, ctx.currentTime);

            gain.gain.setValueAtTime(0.3, ctx.currentTime);

            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.15);

            osc.start(); osc.stop(ctx.currentTime + 0.15);

        } else {

            osc.type = 'sawtooth'; osc.frequency.setValueAtTime(250, ctx.currentTime);

            gain.gain.setValueAtTime(0.3, ctx.currentTime);

            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.4);

            osc.start(); osc.stop(ctx.currentTime + 0.4);

        }

    } catch(e) {}

}



// =========================================================================

// 2. REALTIME LISTENER (FIRESTORE SYNC)

// =========================================================================

db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").onSnapshot(doc => { 

    try {

        if (doc.exists) { 

            let res = doc.data(); 

            danhSachKhoa = res.danhSachKhoa || []; 

            danhSachKtvCssd = res.danhSachKtvCssd || []; 

            databaseExcel = res.databaseExcel || []; 

            if(res.thongTinMatKhauAdmin) thongTinMatKhauAdmin = Object.assign(thongTinMatKhauAdmin, res.thongTinMatKhauAdmin); 

            if(res.cauHinhGiaoDien) cauHinhGiaoDien = Object.assign(cauHinhGiaoDien, res.cauHinhGiaoDien);

        } 

        initSelects();

        if(activeTab === 'khoaphong') loadBoDungCuTheoKhoa(); 

        renderAdminInterface(); 

        if(currentRole) apDungPhanQuyenGiaoDien(currentRole);

        taiDanhMucLinhKienChuan();

        callRender();

    } catch(err) { console.error("Lỗi đồng bộ danh mục:", err); }

});



db.collection("phieuGiaoNhan").orderBy("id", "desc").limit(1000).onSnapshot(snap => { 

    listGiaoDich = []; 

    snap.forEach(doc => { let d = doc.data(); d.firestoreId = doc.id; listGiaoDich.push(d); }); 

    callRender();

});



function callRender() { clearTimeout(renderTimeout); renderTimeout = setTimeout(() => { renderTheoTabHienTai(); }, 100); }



// =========================================================================

// 3. XỬ LÝ ĐĂNG NHẬP & PHÂN QUYỀN HỆ THỐNG

// =========================================================================

function toggleLoginFields() {

    const r = document.getElementById("login_role").value;

    document.getElementById("field_khoa").classList.toggle("hidden", r !== "KHOA");

    document.getElementById("field_nhanvien_cssd").classList.toggle("hidden", r !== "CSSD");

}



function checkLogin() {

    const role = document.getElementById("login_role").value; 

    const pass = document.getElementById("login_pass").value; 

    let pA = thongTinMatKhauAdmin.adminPIN || "admin2026"; 

    let pC = thongTinMatKhauAdmin.cssdPIN || "cssd2026"; 

    let pG = thongTinMatKhauAdmin.guestPIN || "guest2026"; 

     

    if (role === "ADMIN" && pass === pA) { 

        currentRole = "ADMIN"; loginUserCode = "ADMIN"; document.getElementById("nav_user_info").innerText = "ADMINISTRATOR";

        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');

        apDungPhanQuyenGiaoDien("ADMIN");

        switchTab('lichsuluanchuyen');

    } else if (role === "KHOA") { 

        const khoaSelect = document.getElementById("login_khoa").value; 

        let found = danhSachKhoa.find(x => x.ten === khoaSelect); 

        if (pass === (found ? found.pin : "123")) { 

            currentRole = "KHOA"; loginUserCode = khoaSelect; document.getElementById("nav_user_info").innerText = khoaSelect; 

            if (document.getElementById("khoa_selKhoa")) {

                document.getElementById("khoa_selKhoa").value = khoaSelect;

                document.getElementById("khoa_selKhoa").disabled = true;

            }

            document.body.classList.remove('guest-mode');

            apDungPhanQuyenGiaoDien("KHOA");

            switchTab('khoaphong');

        } else { return showToast("Sai mã PIN Khoa!", "error"); } 

    } else if (role === "CSSD") {

        const nvCode = document.getElementById("login_nv_cssd").value;

        if(!nvCode && pass === pC) { currentRole = "CSSD"; loginUserCode = "CSSD_CHUNG"; document.getElementById("nav_user_info").innerText = "CSSD GENERAL"; } 

        else if (nvCode) {

            let nv = danhSachKtvCssd.find(x => x.code === nvCode);

            if (nv && pass === nv.pin) { currentRole = "CSSD"; loginUserCode = nv.ten; document.getElementById("nav_user_info").innerText = `${nvCode} - ${nv.ten}`; }

            else { return showToast("Sai mã PIN của nhân viên này!", "error"); }

        } else { return showToast("Vui lòng chọn Nhân viên hoặc nhập PIN Backup!", "error"); }

        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');

        apDungPhanQuyenGiaoDien("CSSD");

        let tabsDuocPhep = cauHinhGiaoDien["CSSD"] || [];

        if(tabsDuocPhep.includes('thugom')) switchTab('thugom');

        else if (tabsDuocPhep.length > 0) switchTab(tabsDuocPhep[0]);

    } else if (role === "GUEST") {

        if(pass === pG) {

            currentRole = "GUEST"; loginUserCode = "GUEST"; document.getElementById("nav_user_info").innerText = "KHÁCH THAM QUAN";

            document.body.classList.add('guest-mode'); 

            apDungPhanQuyenGiaoDien("GUEST");

            switchTab('quanlykho');

        } else { return showToast("Sai mã PIN Khách tham quan!", "error"); }

    } else { return showToast("Sai thông tin đăng nhập!", "error"); }

    document.getElementById("login-screen").classList.add("hidden"); document.getElementById("main-app").classList.remove("hidden");

}



function anTatCaHeadersVaMenus() {

    document.getElementById('header-lamsang')?.classList.add('hidden');

    document.getElementById('header-vanhanh')?.classList.add('hidden');

    document.getElementById('header-dulieu')?.classList.add('hidden');

    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => {

        document.getElementById('menu-' + x)?.classList.add('hidden');

    });

}



function apDungPhanQuyenGiaoDien(role) {

    anTatCaHeadersVaMenus();

    if (role === "ADMIN") {

        ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(tab => {

            document.getElementById('menu-' + tab)?.classList.remove('hidden');

        });

        document.getElementById('header-lamsang')?.classList.remove('hidden');

        document.getElementById('header-vanhanh')?.classList.remove('hidden');

        document.getElementById('header-dulieu')?.classList.remove('hidden');

        return;

    }

    let tabsDuocPhep = cauHinhGiaoDien[role] || [];

    tabsDuocPhep.forEach(tab => { document.getElementById('menu-' + tab)?.classList.remove('hidden'); });

    if (tabsDuocPhep.includes('khoaphong')) document.getElementById('header-lamsang')?.classList.remove('hidden');

    if (['thugom','mayrua','donggoi','mayhap','khovokhuan'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-vanhanh')?.classList.remove('hidden');

    if (['quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-dulieu')?.classList.remove('hidden');

}



function switchTab(t) { 

    if (currentRole !== "ADMIN") {

        let tabsDuocPhep = cauHinhGiaoDien[currentRole] || [];

        if(!tabsDuocPhep.includes(t)) { return showToast("Tài khoản không có quyền truy cập!", "error"); }

    }

    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => { 

        document.getElementById('tab-'+x)?.classList.add('hidden'); 

        document.getElementById('menu-'+x)?.classList.remove('sidebar-item-active'); 

    }); 

    document.getElementById('tab-'+t)?.classList.remove('hidden'); 

    document.getElementById('menu-'+t)?.classList.add('sidebar-item-active'); 

    activeTab = t; 

    if (t === 'mayrua') { setTimeout(() => { capNhatDanhSachMaMayRua(); }, 50); }

    if (t === 'mayhap') { setTimeout(() => { tuDongTaoMaLoMeHap(); }, 50); }

    callRender(); 

}



// =========================================================================

// 4. TRẠM 1: CỔNG LÂM SÀNG & HÀNH ĐỘNG ĐỘC LẬP (TRẢ ĐỒ BẨN / YÊU CẦU ĐỒ SẠCH)

// =========================================================================

function loadBoDungCuTheoKhoa() { 

    let k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 

    let list = document.getElementById("listBoDungCu"); 

    if(!list) return; list.innerHTML = ""; 

    let f = danhSachKhoa.find(x => (x.ten || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase()); 

    if (f && f.danhSachBo) {

        let htmlOptions = "";

        f.danhSachBo.forEach(x => {

            if (x) {

                let chuoiX = String(x);

                let tenBoThuanTuy = chuoiX.includes(" [ID:") ? chuoiX.split(" [ID:")[0] : chuoiX;

                htmlOptions += `<option value="${chuoiX}"><option value="${tenBoThuanTuy}">`;

            }

        });

        list.innerHTML = htmlOptions; 

    }

}



function themVaoGio() { 

    let val = document.getElementById("khoa_inpMaBo").value.trim().toUpperCase(); if(!val) return; 

    if (gioHangTam.some(x => x.maMacDinh === val)) { document.getElementById("khoa_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách chờ!", "error"); } 

    let tenGoc = val.includes("[ID:") ? val.split(" [ID:")[0] : val; 

    gioHangTam.push({bo: tenGoc, maMacDinh: val, slYeuCau: 1}); 

    document.getElementById("khoa_inpMaBo").value = ""; 

    renderGioHang(); 

}



function renderGioHang() { 

    let khuVuc = document.getElementById("khuVucGioHang"); if(khuVuc) khuVuc.classList.toggle("hidden", gioHangTam.length===0); 

    let bHang = document.getElementById("bangGioHang");

    if(bHang) {

        bHang.innerHTML = gioHangTam.map(i => `<tr><td class="p-2.5 font-bold text-sky-700 text-[11px]">${i.bo}</td></tr>`).join(''); 

    }

    let badge = document.getElementById("badgeGioHang");

    if(badge) badge.innerText = `${gioHangTam.length} món`;

}



function khoaGuiPhieuTraBatches() { 

    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 

    if(!k) return showToast("Vui lòng chọn Khoa trước!"); 

    if(gioHangTam.length === 0) return showToast("Vui lòng quét hoặc chọn mâm bẩn muốn trả trước!", "error"); 

     

    let p=[]; 

    gioHangTam.forEach((i, idx) => {

        p.push(

            db.collection("phieuGiaoNhan").add({ 

                id: Date.now() + idx, 

                ngayTao: getTodayDateStr(), 

                time: new Date().toLocaleTimeString('vi-VN'), 

                khoa: k, 

                bo: i.bo, 

                maMacDinh: i.maMacDinh, 

                slYeuCau: 1, 

                slThucTe: 1, 

                status: "CHO_THU" 

            })

        );

    }); 

     

    Promise.all(p).then(() => { 

        clearGioHang(); 

        playSound('success');

        showToast("Đã gửi lệnh báo trả đồ bẩn về CSSD!", "success"); 

        callRender(); 

    }); 

}



function khoaGuiYeuCauCapPhat() {

    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;

    if(!k) return showToast("Vui lòng chọn Khoa/Phòng gửi yêu cầu!", "error");

    

    const tenBoChon = document.getElementById("khoa_inpMaBo")?.value.trim().toUpperCase();

    if(!tenBoChon) return showToast("Vui lòng chọn hoặc nhập tên mâm cần cấp mới!", "error");

    

    let tenMâmSạch = tenBoChon.split(" [ID:")[0];

    

    db.collection("phieuGiaoNhan").add({

        id: Date.now(),

        ngayTao: getTodayDateStr(),

        time: new Date().toLocaleTimeString('vi-VN'),

        khoa: k,

        bo: tenMâmSạch,

        maMacDinh: "CHO_CAP_PHAT", 

        slYeuCau: 1,

        status: "CHO_CAP_PHAT", 

        nvYeuCau: loginUserCode || "Điều dưỡng khoa"

    }).then(() => {

        playSound('success');

        showToast(`Đã gửi yêu cầu cấp phát mâm ${tenMâmSạch} thành công!`, "success");

        if(document.getElementById("khoa_inpMaBo")) document.getElementById("khoa_inpMaBo").value = "";

        callRender();

    });

}



// =========================================================================

// 5. TRẠM 2: XE THU GOM & KIỂM ĐẾM CHI TIẾT LINH KIỆN

// =========================================================================

function moPopupKiemDem(id) { 

    let item = listGiaoDich.find(x => x.firestoreId === id); 

    if(!item) return showToast("Không tìm thấy thông tin dòng chỉ định!", "error");



    currentKiemDemData = {

        id: id,

        tenBoDungCu: item.bo ? String(item.bo).split(" [ID:")[0] : "Chưa rõ mâm",

        khoaYeuCau: item.khoa || "Chưa rõ khoa",

        ghiChuLamSang: item.ghiChu || ""

    }; 



    document.getElementById('popBo').innerText = currentKiemDemData.tenBoDungCu;

    document.getElementById('popKhoa').innerText = currentKiemDemData.khoaYeuCau;

    document.getElementById('popGhiChu').value = currentKiemDemData.ghiChuLamSang;

    document.getElementById('popGhiChu').classList.remove('border-rose-500', 'ring-2', 'ring-rose-200');



    let itemsInBo = databaseExcel.filter(x => { 

        let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; 

        return String(boName).trim().toUpperCase() === String(currentKiemDemData.tenBoDungCu).trim().toUpperCase(); 

    });



    let checklistSoBo = [];

    if(itemsInBo.length > 0) {

        checklistSoBo = itemsInBo.map(ct => {

            let tenDc = ct['Tên TS chuẩn'] || ct['Tên Dụng Cụ Chi Tiết'] || ct['Tên dụng cụ chi tiết'] || ct['Tên dụng cụ'] || ct['Chi tiết'] || ct['Dụng cụ'] || ct['Tên Chi Tiết'] || ct['NAME'] || "Dụng cụ";

             

            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {

                tenDc = "Dụng cụ chuẩn mâm";

            }

             

            let sl = parseInt(ct['Số lượng'] || ct['SL'] || ct['Số Lượng'] || 1) || 0;

            return { ten: tenDc, slChuan: sl, slThuc: sl, tinhTrang: "ĐỦ" };

        }).filter(line => line.slChuan > 0); 

    } else {

        checklistSoBo = [{ ten: "Dụng cụ nguyên bộ (Chưa phân rã cấu hình)", slChuan: 1, slThuc: 1, tinhTrang: "ĐỦ" }];

    }



    currentKiemDemData.linhKienKiemDem = checklistSoBo;

    renderChecklistLinhKien();

    document.getElementById('popupKiemDem').classList.remove('hidden'); 

}



function renderChecklistLinhKien() {

    const container = document.getElementById('popKiemDemChecklist');

    if(!container) return; container.innerHTML = ""; 



    currentKiemDemData.linhKienKiemDem.forEach((item, index) => {

        let bgBadge = "bg-emerald-50 text-emerald-700 border-emerald-200";

        if (item.tinhTrang === "THIẾU") bgBadge = "bg-rose-50 text-rose-700 border-rose-200 animate-pulse";

        if (item.tinhTrang === "HỎNG") bgBadge = "bg-amber-50 text-amber-700 border-amber-200";



        const row = document.createElement('div');

        row.className = "flex items-center justify-between py-3 px-2 transition-all hover:bg-slate-50";

        row.innerHTML = `

            <div class="flex-1 pr-2">

                <p class="text-xs font-bold text-slate-700">${item.ten}</p>

                <p class="text-[10px] text-slate-400 font-medium mt-0.5">Cơ số chuẩn: <span class="font-bold text-slate-600">${item.slChuan}</span></p>

            </div>

            <div class="flex items-center gap-3 mr-4">

                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, -1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">-</button>

                <span class="w-6 text-center text-xs font-black text-slate-800">${item.slThuc}</span>

                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, 1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">+</button>

            </div>

            <div>

                <select onchange="thayDoiTinhTrangLinhKien(${index}, this.value)" class="text-[11px] font-black border rounded-lg px-2 py-1.5 shadow-sm transition-all ${bgBadge}">

                    <option value="ĐỦ" ${item.tinhTrang === 'ĐỦ' ? 'selected' : ''}>🟢 Đủ đồ</option>

                    <option value="THIẾU" ${item.tinhTrang === 'THIẾU' ? 'selected' : ''}>🔴 Thiếu đồ</option>

                    <option value="HỎNG" ${item.tinhTrang === 'HỎNG' ? 'selected' : ''}>🟡 Hỏng đồ</option>

                </select>

            </div>

        `;

        container.appendChild(row);

    });

}



function thayDoiSoLuongLinhKien(index, thayDoi) {

    const item = currentKiemDemData.linhKienKiemDem[index];

    const targetSl = item.slThuc + thayDoi;

    if (targetSl >= 0 && targetSl <= item.slChuan * 2) {

        item.slThuc = targetSl;

        if (item.slThuc < item.slChuan) { item.tinhTrang = "THIẾU"; } 

        else if (item.slThuc === item.slChuan) { item.tinhTrang = "ĐỦ"; }

        renderChecklistLinhKien();

    }

}



function thayDoiTinhTrangLinhKien(index, value) {

    currentKiemDemData.linhKienKiemDem[index].tinhTrang = value;

    if (value === "THIẾU" && currentKiemDemData.linhKienKiemDem[index].slThuc === currentKiemDemData.linhKienKiemDem[index].slChuan) {

        currentKiemDemData.linhKienKiemDem[index].slThuc = Math.max(0, currentKiemDemData.linhKienKiemDem[index].slChuan - 1);

    } else if (value === "ĐỦ") {

        currentKiemDemData.linhKienKiemDem[index].slThuc = currentKiemDemData.linhKienKiemDem[index].slChuan;

    }

    renderChecklistLinhKien();

}



function closePopupKiemDem() { document.getElementById("popupKiemDem").classList.add("hidden"); currentKiemDemData = null; }



async function saveKiemDem() { 

    if(!currentKiemDemData) return; 

    const ghiChuValue = document.getElementById("popGhiChu").value.trim();

    let coSuCoChenhLech = false;

    currentKiemDemData.linhKienKiemDem.forEach(item => {

        let thuc = Number(item.slThuc) || 0;

        let chuan = Number(item.slChuan) || 0;

        if (item.tinhTrang === "THIẾU" || item.tinhTrang === "HỎNG" || thuc < chuan) { coSuCoChenhLech = true; }

    });



    if (coSuCoChenhLech && ghiChuValue === "") {

        playSound('error');

        alert("⚠️ PHÁT HIỆN SỰ CỐ CHÊNH LỆCH LINH KIỆN!\nAnh/Chị bắt buộc phải nhập lý do chênh lệch hoặc số biên bản sự cố trước khi 'Chốt và Chuyển Rửa'.");

        document.getElementById('popGhiChu').focus();

        document.getElementById('popGhiChu').classList.add('border-rose-500', 'ring-2', 'ring-rose-200');

        return;

    }



    try {

        await db.collection("phieuGiaoNhan").doc(currentKiemDemData.id).update({ 

            status: "DANG_RUA", ghiChu: ghiChuValue,

            ktvThuGom: loginUserCode || "CSSD_CHUNG",

            thoiGianDoiSoat: new Date().toLocaleTimeString('vi-VN'),

            coSuCoChenhLech: coSuCoChenhLech,

            chiTietChecklistLinhKien: currentKiemDemData.linhKienKiemDem

        });

        showToast("Đã đối soát xong! Mâm đồ đã chuyển sang Trạm Rửa.", "success"); 

        closePopupKiemDem(); callRender(); 

    } catch(err) { showToast("Lỗi kết nối Firebase nội bộ!", "error"); }

}



function clearGioHang() { gioHangTam = []; renderGioHang(); }



// =========================================================================

// 6. MODULE: QUẢN LÝ MẺ RỬA KHỬ KHUẨN (BELIMED WD250)

// =========================================================================

function toggleSelectAllRua() { let checked = document.getElementById('selectAllRua').checked; document.querySelectorAll('.rua-checkbox').forEach(cb => cb.checked = checked); }

function toggleSelectAllNghiemThuRua() { let checked = document.getElementById('selectAllNghiemThuRua').checked; document.querySelectorAll('.nghiemthurua-checkbox').forEach(cb => cb.checked = checked); }



function capNhatDanhSachMaMayRua() {

    const loaiRua = document.getElementById("rua_loaiRua")?.value;

    const selectMay = document.getElementById("rua_maySo");

    if (!selectMay || !loaiRua) return;

    selectMay.innerHTML = "";

    if (cauHinhMayRua[loaiRua]) {

        cauHinhMayRua[loaiRua].forEach(may => {

            selectMay.innerHTML += `<option value="${may}">${may}</option>`;

        });

    }

    tuDongTaoMaLoMeRua();

}



function tuDongTaoMaLoMeRua() {

    const maMay = document.getElementById("rua_maySo")?.value || "Belimed WD250";

    const maMayVietTat = (maMay === "Belimed WD250") ? "BL" : maMay;

    const now = new Date();

    const yy = String(now.getFullYear()).slice(-2);

    const mm = String(now.getMonth() + 1).padStart(2, '0');

    const dd = String(now.getDate()).padStart(2, '0');

    const ngayChuoi = `${yy}${mm}${dd}`;



    let cacMeTrongNgay = listGiaoDich.filter(x => x.rua_batchCode && x.rua_batchCode.startsWith(maMayVietTat + ngayChuoi));

    let soMeMax = 0;

    cacMeTrongNgay.forEach(x => {

        let phanDuoi = x.rua_batchCode.split("_")[1];

        if (phanDuoi) {

            let num = parseInt(phanDuoi);

            if (num > soMeMax) soMeMax = num;

        }

    });

    let meTiepTheo = soMeMax + 1;

    const chuoiMe = String(meTiepTheo).padStart(2, '0');

    const batchIdHoanChinh = `${maMayVietTat}${ngayChuoi}_${chuoiMe}`;



    if (document.getElementById("rua_meSo")) document.getElementById("rua_meSo").value = chuoiMe;

    if (document.getElementById("rua_batchId")) document.getElementById("rua_batchId").value = batchIdHoanChinh;

}



function xacNhanMeRua() {

    let checkboxes = document.querySelectorAll('.rua-checkbox:checked');

    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm dụng cụ bẩn để xếp vào máy!", "error");

    

    let loaiRua = document.getElementById("rua_loaiRua").value;

    let maMay = document.getElementById("rua_maySo").value;

    let batchCode = document.getElementById("rua_batchId").value;

    let chuKy = document.getElementById("rua_chuKy").value;

    let hoaChat = document.getElementById("rua_hoaChat").value;

    let lieuLuong = document.getElementById("rua_lieuLuong").value || "Theo chuẩn";

    const ngayHomNay = getTodayDateStr();



    let p = [];

    checkboxes.forEach(cb => {

        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({

                status: "TRONG_BUONG_RUA",

                rua_batchCode: batchCode,

                rua_ngayRua: ngayHomNay,

                rua_mayRua: maMay,

                rua_chuKy: chuKy,

                rua_hoaChat: hoaChat,

                rua_lieuLuong: lieuLuong,

                rua_timeBatDau: new Date().toLocaleTimeString('vi-VN')

            })

        );

    });

    Promise.all(p).then(() => {

        playSound('success');

        showToast(`Bắt đầu chạy mẻ máy rửa: ${batchCode}`, "success");

        tuDongTaoMaLoMeRua();

        callRender();

    });

}



function duyetSachMeRuaHangLoat() {

    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');

    if(checkboxes.length === 0) return showToast("Chọn mâm đã rửa sạch để nghiệm thu!", "error");

    

    let ketQuaTest = document.getElementById("rua_testDoSach").value;

    let p = [];

    checkboxes.forEach(cb => {

        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({

                status: "CHO_DONG_GOI",

                rua_ketQuaTestSach: ketQuaTest,

                rua_timeKetThuc: new Date().toLocaleTimeString('vi-VN')

            })

        );

    });

    Promise.all(p).then(() => {

        playSound('success');

        showToast("Xác nhận nghiệm thu đạt chất lượng làm sạch!", "success");

        callRender();

    });

}



function tuChoiMeRuaHangLoat() {

    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');

    if(checkboxes.length === 0) return showToast("Chọn mâm không đạt!", "error");

    

    let p = [];

    checkboxes.forEach(cb => {

        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({

                status: "DANG_RUA",

                rua_batchCode: firebase.firestore.FieldValue.delete(),

                ghiChu: "Làm sạch chưa đạt, yêu cầu rửa lại."

            })

        );

    });

    Promise.all(p).then(() => {

        playSound('error');

        showToast("Đã trả các mâm không đạt về hàng đợi chờ rửa lại!", "success");

        callRender();

    });

}



// =========================================================================

// 7. TRẠM 4: LÀM SẠCH VÀ ĐÓNG GÓI

// =========================================================================

function moPopupDongGoi(id) { idDangDongGoi = id; let item = listGiaoDich.find(x => x.firestoreId === id); if(item) { document.getElementById("popDG_Bo").innerText = item.bo; tinhHanSuDung(); document.getElementById("popupDongGoi").classList.remove("hidden"); } }

function closePopupDongGoi() { document.getElementById("popupDongGoi").classList.add("hidden"); }

function tinhHanSuDung() { let val = document.getElementById("popDG_Loai").value.split("|"); let days = parseInt(val[1]); let dateHSD = new Date(); dateHSD.setDate(dateHSD.getDate() + days); let p = document.getElementById("popDG_Han"); p.innerText = dateHSD.toLocaleDateString('vi-VN'); p.dataset.dateDB = dateHSD.toISOString().split('T')[0]; }

function chotDongGoi() { if(!idDangDongGoi) return; let chatLieuTen = document.getElementById("popDG_Loai").value.split("|")[0]; db.collection("phieuGiaoNhan").doc(idDangDongGoi).update({ status: "CHO_HAP", chatLieu: chatLieuTen, hsd: document.getElementById("popDG_Han").dataset.dateDB }).then(() => { showToast("Đã đóng gói, chuyển chờ hấp!", "success"); closePopupDongGoi(); callRender(); }); }



// =========================================================================

// 8. TRẠM 5: VẬN HÀNH MẺ HẤP & DUYỆT NHẬP KHO VÔ KHUẨN

// =========================================================================

function toggleSelectAllHap() { let checked = document.getElementById('selectAllHap').checked; document.querySelectorAll('.hap-checkbox').forEach(cb => cb.checked = checked); }

function capNhatDanhSachMaMay() { const loaiHap = document.getElementById("hap_loaiHap")?.value; const selectMay = document.getElementById("hap_maySo"); if (!selectMay || !loaiHap) return; selectMay.innerHTML = ""; if (cauHinhMayHap[loaiHap]) { cauHinhMayHap[loaiHap].forEach(may => { selectMay.innerHTML += `<option value="${may}">${may}</option>`; }); } tuDongTaoMaLoMeHap(); }



function tuDongTaoMaLoMeHap() {

    const maMay = document.getElementById("hap_maySo")?.value || "A1"; const now = new Date(); const yy = String(now.getFullYear()).slice(-2); const mm = String(now.getMonth() + 1).padStart(2, '0'); const dd = String(now.getDate()).padStart(2, '0'); const ngayChuoi = `${yy}${mm}${dd}`;

    let cacMeTrongNgay = listGiaoDich.filter(x => x.batchCode && x.batchCode.startsWith(maMay + ngayChuoi)); let soMeMax = 0;

    cacMeTrongNgay.forEach(x => { let phanDuoi = x.batchCode.split("_")[1]; if (phanDuoi) { let num = parseInt(phanDuoi); if (num > soMeMax) soMeMax = num; } });

    let meTiepTheo = soMeMax + 1; if (meTiepTheo > 99) meTiepTheo = 1; const chuoiMe = String(meTiepTheo).padStart(2, '0'); const soLanDone = `${ngayChuoi}_${chuoiMe}`; const batchIdHoanChinh = `${maMay}${soLanDone}`;

    if (document.getElementById("hap_meSo")) document.getElementById("hap_meSo").value = chuoiMe;

    if (document.getElementById("hap_batchId")) document.getElementById("hap_batchId").value = batchIdHoanChinh;

}



function xacNhanMeHap() { 

    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); 

    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 

    

    let loaiHap = document.getElementById("hap_loaiHap").value; 

    let maMay = document.getElementById("hap_maySo").value; 

    let batchCode = document.getElementById("hap_batchId").value; 

    let chuKyNhiet = document.getElementById("hap_nhietDo").value; 

    let apSuat = document.getElementById("hap_apSuat").value || "N/A"; 

    let coKemBI = document.getElementById("hap_hasBI") ? document.getElementById("hap_hasBI").checked : false;

    const ngayHomNay = getTodayDateStr(); 

    let bayGio = new Date(); 

    let thoiGianDuKienXong = new Date(bayGio.getTime() + 45 * 60 * 1000);



    let p = []; 

    checkboxes.forEach(cb => { 

        if(cb.id === 'selectAllHap') return; 

        

        let thongTinLo = { 

            loaiHap: loaiHap, 

            maMay: maMay, 

            chuKyNhiet: chuKyNhiet, 

            apSuat: apSuat, 

            thoiGianBatDau: bayGio.toLocaleTimeString('vi-VN'), 

            giamSatChatLuong: { 

                chiThiHoaHoc: "ĐẠT", 

                laMeTestSinhHocGoc: coKemBI, 

                ketQuaSinhHoc: coKemBI ? "ĐANG CHỜ MÁY Ủ ĐỌC BI (20 PHÚT)" : "KẾ THỪA ĐẦU NGÀY" 

            } 

        };

        

        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({ 

                status: "DANG_HAP", 

                batchCode: batchCode, 

                ngayHapRealtime: ngayHomNay, 

                hasBI: coKemBI, 

                thoiGianKetThucChuTrinh: thoiGianDuKienXong.toISOString(), 

                thongTinLoHap: thongTinLo 

            })

        ); 

    }); 



    Promise.all(p).then(() => { 

        playSound('success');

        showToast(`Kích hoạt lò thành công! Lô: ${batchCode}`, "success"); 

        if(document.getElementById("hap_hasBI")) document.getElementById("hap_hasBI").checked = false; 

        duLieuAnhBiTamBase64 = ""; 

        const fileInp = document.getElementById("input_anhBI"); 

        if(fileInp) fileInp.value = ""; 

        tuDongTaoMaLoMeHap(); 

        callRender(); 

    }).catch(err => {

        showToast("Lỗi khi cập nhật mẻ hấp: " + err.message, "error");

    }); 

}



function toggleSelectAllNghiemThu() { let checked = document.getElementById('selectAllNghiemThu').checked; document.querySelectorAll('.nghiemthu-checkbox').forEach(cb => cb.checked = checked); }



function kiemTraQuyenDuyetMeHap() {

    const nutDuyetDat = document.querySelector("button[onclick='nhapKhoHangLoat()']"); if (!nutDuyetDat) return;

    let checkboxesNghiemThu = document.querySelectorAll('.nghiemthu-checkbox:checked'); let phaiChoKetQuaBI = false;

    checkboxesNghiemThu.forEach(cb => { if (cb.getAttribute('data-hasbi') === 'true') phaiChoKetQuaBI = true; });



    if (phaiChoKetQuaBI && !duLieuAnhBiTamBase64) {

        nutDuyetDat.disabled = true;

        nutDuyetDat.className = "w-full bg-slate-300 text-slate-500 font-black py-2.5 rounded text-xs shadow-md cursor-not-allowed flex items-center justify-center gap-2";

        nutDuyetDat.innerHTML = `<i class="fa-solid fa-lock text-slate-400"></i> CHỜ MINH CHỨNG BI (MÁY Ủ ĐỌC 20 PHÚT)`;

    } else {

        nutDuyetDat.disabled = false;

        nutDuyetDat.className = "w-full bg-emerald-600 hover:bg-emerald-700 text-white font-black py-2.5 rounded text-xs shadow-md flex items-center justify-center gap-2 transition-all cursor-pointer";

        nutDuyetDat.innerHTML = `<i class="fa-solid fa-check-double mr-1"></i> DUYỆT ĐẠT & CHO VÀO KHO LƯU TRỮ CHUNG`;

    }

}



function nhapKhoHangLoat() { 

    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!"); 

    const bayGio = new Date(); let p = []; 

     

    checkboxes.forEach(cb => { 

        if(cb.id === 'selectAllNghiemThu') return; 

        let itemData = listGiaoDich.find(x => x.firestoreId === cb.value); if(!itemData) return;

        let coKemBI = itemData.hasBI === true;

        let thoiGianHapXong = itemData.thoiGianKetThucChuTrinh ? new Date(itemData.thoiGianKetThucChuTrinh) : new Date(bayGio.getTime() - 45 * 60 * 1000);

        let soPhutThucTe = Math.floor((bayGio - thoiGianHapXong) / (1000 * 60)); 

        let soPhutTinhKPI = coKemBI ? Math.max(0, soPhutThucTe - 20) : soPhutThucTe;

        let trangThaiKPI = (soPhutTinhKPI > 30) ? "KHÔNG ĐẠT" : "ĐẠT";



        let bgGiamSat = itemData.thongTinLoHap?.giamSatChatLuong || {};

        let updateGiamSat = {

            ...bgGiamSat,

            ketQuaSinhHoc: coKemBI ? "ÂM TÍNH (ĐẠT)" : "KẾ THỪA ĐẦU NGÀY",

            minhChungAnhBase64: coKemBI ? duLieuAnhBiTamBase64 : (bgGiamSat.minhChungAnhBase64 || "")

        };



        p.push(

            db.collection("phieuGiaoNhan").doc(cb.value).update({

                status: "CHO_XUAT", thoiGianKetLuan: bayGio.toISOString(), thoiGianDocTestBiPhut: soPhutThucTe, 

                thoiGianTinhKpiPhut: soPhutTinhKPI, ketQuaGiamSatKpi: trangThaiKPI, "thongTinLoHap.giamSatChatLuong": updateGiamSat

            })

        ); 

    }); 

     

    Promise.all(p).then(() => { 

        playSound('success');

        showToast("Đã duyệt mâm đạt chuẩn Vô Khuẩn!", "success"); 

        duLieuAnhBiTamBase64 = ""; const fileInp = document.getElementById("input_anhBI"); if(fileInp) fileInp.value = ""; 

        callRender(); 

    }); 

}



function tuChoiHapHangLoat() { 

    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 

    let p = []; 

    checkboxes.forEach(cb => { if(cb.id !== 'selectAllNghiemThu') p.push(db.collection("phieuGiaoNhan").doc(cb.value).update({status: "DANG_RUA", ghiChu: "Không đạt hấp, trả về rửa lại"})); }); 

    Promise.all(p).then(() => { playSound('error'); showToast("Đã trả các mâm không đạt về Trạm làm sạch!", "success"); callRender(); }); 

}



// =========================================================================

// 9. TRẠM 6: KHO VÔ KHUẨN & PHỐI HỢP CẤP PHÁT ĐIỀU PHỐI XOAY VÒNG (FIFO)

// =========================================================================

function xuatKhoXoayVong(maTrucTiep) { 

    const k = document.getElementById("xuat_selKhoa") ? document.getElementById("xuat_selKhoa").value : ""; 

    let ma = maTrucTiep || (document.getElementById("xuat_inpMaBo") ? document.getElementById("xuat_inpMaBo").value.trim().toUpperCase() : ""); 

    

    if(!k) { playSound('error'); return showToast("Vui lòng Chọn Khoa nhận trước khi xuất!", "error"); } 

    if(!ma) return;

    if(gioHangXuatKho.some(x => x.maMacDinh === ma)) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách xuất!", "error"); }

    

    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && x.status === "CHO_XUAT"); 

    if(!khayThucTe) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast(`Mã ID ${ma} không sẵn sàng ở Kho Vô Khuẩn.`, "error"); } 

    

    gioHangXuatKho.push(khayThucTe); playSound('success'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; renderGioHangXuat();

}



function renderGioHangXuat() {

    let listContainer = document.getElementById("danhSachXuatTam");

    if (!listContainer) {

        let bangKho = document.getElementById("bangKhoVoKhuan");

        if (bangKho && bangKho.closest('table')) {

            let wrapper = document.createElement("div"); wrapper.id = "danhSachXuatTam";

            wrapper.className = "mb-4 bg-sky-50 border border-sky-200 rounded p-3 hidden shadow-inner";

            bangKho.closest('table').parentNode.insertBefore(wrapper, bangKho.closest('table')); listContainer = wrapper;

        }

    }

    if(listContainer) {

        if(gioHangXuatKho.length === 0) { listContainer.classList.add("hidden"); } 

        else {

            listContainer.classList.remove("hidden");

            let html = `<div class="flex justify-between items-center mb-2 pb-2 border-b border-sky-200"><h3 class="font-bold text-sky-800 text-[13px]"><i class="fa-solid fa-cart-flatbed mr-2"></i>Đang quét xuất kho: <span class="bg-rose-500 text-white px-2 py-0.5 rounded ml-1">${gioHangXuatKho.length} mâm</span></h3><button onclick="xacNhanXuatKhoHangLoat()" class="bg-emerald-600 text-white px-4 py-1.5 rounded shadow font-black text-xs hover:bg-emerald-700 transition-colors uppercase"><i class="fa-solid fa-check-double mr-1"></i>Chốt Bàn Giao</button></div><div class="flex flex-wrap gap-2">`;

            gioHangXuatKho.forEach((item, index) => { 

                let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "Dụng cụ";

                html += `<div class="bg-white border border-sky-300 px-2 py-1 rounded flex items-center gap-2 shadow-sm animate-pulse"><span class="text-[11px] font-bold text-slate-700">${tenBoGoc}</span><span class="text-[10px] font-mono text-sky-700 bg-sky-100 px-1 rounded font-bold">${item.maMacDinh}</span><i class="fa-solid fa-xmark text-rose-500 cursor-pointer ml-1 text-xs hover:text-rose-700" onclick="xoaKhoiGioXuat(${index})"></i></div>`; 

            });

            html += `</div>`; listContainer.innerHTML = html;

        }

    }

}



function xoaKhoiGioXuat(index) { gioHangXuatKho.splice(index, 1); renderGioHangXuat(); }



function xacNhanXuatKhoHangLoat() {

    const k = document.getElementById("xuat_selKhoa").value; if(!k || gioHangXuatKho.length === 0) return;

    let p = []; 

    gioHangXuatKho.forEach(khay => { 

        p.push(

            db.collection("phieuGiaoNhan").doc(khay.firestoreId).update({ 

                status: "ĐANG_VAN_CHUYEN", khoa: k, ngayHoanTat: getTodayDateStr(), timeHoanTat: new Date().toLocaleTimeString('vi-VN'), nvXuatKho: loginUserCode || "CSSD_CHUNG" 

            })

        ); 

    });

    Promise.all(p).then(() => { 

        playSound('success'); showToast(`Đã xuất kho bàn giao về Khoa ${k}!`, "success"); 

        gioHangXuatKho = []; if (document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; 

        renderGioHangXuat(); callRender(); 

    });

}



function duyetCapPhatTậpTrung(yeuCauId, tenBo, khoaNhan) {

    let khaySẵnCo = listGiaoDich.find(x => x.status === "CHO_XUAT" && x.bo.split(" [ID:")[0].trim().toUpperCase() === tenBo.trim().toUpperCase());

    

    if (!khaySẵnCo) {

        playSound('error');

        return alert(`⚠️ KHÔNG CÓ KHAY SẴN SÀNG TRONG KHO!\nHiện tại trong Kho vô khuẩn CSSD không có khay "${tenBo}" nào sẵn sàng để cấp phát.`);

    }



    if (confirm(`Xác nhận cấp phát khay ${khaySẵnCo.maMacDinh} để đáp ứng yêu cầu của khoa ${khoaNhan}?`)) {

        let bayGio = new Date();

        let batch = db.batch();

        

        let refKhay = db.collection("phieuGiaoNhan").doc(khaySẵnCo.firestoreId);

        batch.update(refKhay, {

            status: "ĐANG_VAN_CHUYEN",

            khoa: khoaNhan,

            ngayHoanTat: getTodayDateStr(),

            timeHoanTat: bayGio.toLocaleTimeString('vi-VN'),

            nvXuatKho: loginUserCode || "CSSD_CHUNG"

        });



        let refYeuCau = db.collection("phieuGiaoNhan").doc(yeuCauId);

        batch.delete(refYeuCau);



        batch.commit().then(() => {

            playSound('success');

            showToast(`Đã cấp phát thành công khay ${khaySẵnCo.maMacDinh} cho khoa ${khoaNhan}!`, "success");

            callRender();

        });

    }

}



// =========================================================================

// XỬ LÝ KÝ CHỮ KÝ ĐIỆN TỬ TRỰC TIẾP TRÊN CANVAS & CẬP NHẬT KHOA

// =========================================================================

function khoiTaoCanvasKyDienTu() {

    canvasSignature = document.getElementById("canvasKyDienTu");

    if (!canvasSignature) return;

    ctxSignature = canvasSignature.getContext("2d");



    const rect = canvasSignature.getBoundingClientRect();

    canvasSignature.width = rect.width || 380;

    canvasSignature.height = rect.height || 160;



    ctxSignature.lineWidth = 2.5;

    ctxSignature.lineCap = "round";

    ctxSignature.strokeStyle = "#0284c7";



    // Chuột

    canvasSignature.onmousedown = (e) => {

        isDrawingSignature = true;

        ctxSignature.beginPath();

        const pos = getCanvasPos(e);

        ctxSignature.moveTo(pos.x, pos.y);

    };

    canvasSignature.onmousemove = (e) => {

        if (!isDrawingSignature) return;

        const pos = getCanvasPos(e);

        ctxSignature.lineTo(pos.x, pos.y);

        ctxSignature.stroke();

    };

    canvasSignature.onmouseup = () => { isDrawingSignature = false; };



    // Cảm ứng Tablet/Phone

    canvasSignature.ontouchstart = (e) => {

        isDrawingSignature = true;

        ctxSignature.beginPath();

        const pos = getCanvasPos(e.touches[0]);

        ctxSignature.moveTo(pos.x, pos.y);

    };

    canvasSignature.ontouchmove = (e) => {

        if (!isDrawingSignature) return;

        e.preventDefault();

        const pos = getCanvasPos(e.touches[0]);

        ctxSignature.lineTo(pos.x, pos.y);

        ctxSignature.stroke();

    };

    canvasSignature.ontouchend = () => { isDrawingSignature = false; };

}



function getCanvasPos(evt) {

    const rect = canvasSignature.getBoundingClientRect();

    return {

        x: evt.clientX - rect.left,

        y: evt.clientY - rect.top

    };

}



function xoaChuKyCanvas() {

    if (ctxSignature && canvasSignature) {

        ctxSignature.clearRect(0, 0, canvasSignature.width, canvasSignature.height);

    }

}



function khoaKyNhanDoSachDienTu() {

    const checkboxes = document.querySelectorAll("#bangChoNhanTaiKhoa input[type='checkbox']:checked");

    dsIdKhayChoKyNhan = [];

    checkboxes.forEach(chk => dsIdKhayChoKyNhan.push(chk.value));



    if (dsIdKhayChoKyNhan.length === 0) {

        playSound('error');

        return showToast("Vui lòng tích chọn ít nhất 1 khay để ký nhận!", "error");

    }



    const inpName = document.getElementById("ky_tenNguoiNhan");

    if (inpName) inpName.value = loginUserCode || "";



    const pop = document.getElementById("popupKyDienTu");

    if (pop) {

        pop.classList.remove("hidden");

        setTimeout(() => {

            khoiTaoCanvasKyDienTu();

            xoaChuKyCanvas();

        }, 100);

    } else {

        const khoaDangNhap = currentRole === "KHOA" ? loginUserCode : (document.getElementById("khoa_selKhoa") ? document.getElementById("khoa_selKhoa").value : "");

        if (confirm(`Xác nhận nhận ${dsIdKhayChoKyNhan.length} mâm về khoa ${khoaDangNhap}?`)) {

            let p = [];

            dsIdKhayChoKyNhan.forEach(idDoc => {

                p.push(

                    db.collection("phieuGiaoNhan").doc(idDoc).update({

                        status: "HOAN_TAT", nguoiKyNhanKhoa: loginUserCode || "Điều dưỡng khoa", timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'), ngayKhoaNhanThucTe: getTodayDateStr()

                    })

                );

            });

            Promise.all(p).then(() => { playSound('success'); showToast(`Đã tiếp nhận vào tủ khoa thành công!`, "success"); callRender(); });

        }

    }

}



function dongPopupKyDienTu() {

    document.getElementById("popupKyDienTu")?.classList.add("hidden");

    dsIdKhayChoKyNhan = [];

}



function luuXacNhanKyNhan() {

    const tenNguoiNhan = document.getElementById("ky_tenNguoiNhan")?.value.trim();

    if (!tenNguoiNhan) {

        playSound('error');

        return showToast("Vui lòng nhập tên người nhận!", "error");

    }



    const chuoiAnhChuKy = canvasSignature ? canvasSignature.toDataURL("image/png") : "";

    const currentKhoa = currentRole === "KHOA" ? loginUserCode : (document.getElementById("khoa_selKhoa")?.value || "KHOA_LAM_SANG");

    const thoiGianKy = new Date().toLocaleString('vi-VN');



    // 1. Lưu chữ ký gần nhất vào localStorage theo Khoa

    const dataSignature = {

        tenNguoiNhan: tenNguoiNhan,

        chuKyImg: chuoiAnhChuKy,

        timeKy: thoiGianKy

    };

    localStorage.setItem(`signature_${currentKhoa}`, JSON.stringify(dataSignature));



    // 2. Cập nhật trạng thái từng mâm vào Cloud Firestore

    let p = [];

    dsIdKhayChoKyNhan.forEach(idDoc => {

        p.push(

            db.collection("phieuGiaoNhan").doc(idDoc).update({

                status: "HOAN_TAT",

                nguoiKyNhanKhoa: tenNguoiNhan,

                anhChuKyNhanBase64: chuoiAnhChuKy,

                timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'),

                ngayKhoaNhanThucTe: getTodayDateStr()

            })

        );

    });



    Promise.all(p).then(() => {

        playSound('success');

        showToast(`Đã ký nhận thành công ${dsIdKhayChoKyNhan.length} mâm vào tủ khoa!`, "success");

        dongPopupKyDienTu();

        callRender();

    });

}



// =========================================================================

// 10. GHI NHẬN SỬ DỤNG BỘ DỤNG CỤ TRÊN BỆNH NHÂN

// =========================================================================

function moPopupSuDungBoDungCu() {

    gioKhaySuDungTam = []; document.getElementById("sd_bangKhayChon").innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng quét hoặc nhập mã khay...</td></tr>`;

    document.getElementById("sd_ngaySuDung").value = getTodayDateStr(); document.getElementById("sd_nhanChung").value = loginUserCode || "ADMIN";

    if(document.getElementById("sd_khoaBenhNhan")) document.getElementById("sd_khoaBenhNhan").value = (currentRole === "KHOA") ? loginUserCode : "";

    if(document.getElementById("sd_yTaPhongMo")) document.getElementById("sd_yTaPhongMo").value = "";

    if(document.getElementById("sd_yTaVongNgoai")) document.getElementById("sd_yTaVongNgoai").value = "";

    if(document.getElementById("sd_searchBN")) document.getElementById("sd_searchBN").value = "";

    if(document.getElementById("sd_ghiChu")) document.getElementById("sd_ghiChu").value = "";

    if(document.getElementById("sd_maKhayInp")) document.getElementById("sd_maKhayInp").value = "";

    document.getElementById("popupSuDungBoDungCu").classList.remove("hidden");

}



function closePopupSuDung() { document.getElementById("popupSuDungBoDungCu").classList.add("hidden"); }



function scanKhayVaoSuDung() {

    let ma = document.getElementById("sd_maKhayInp").value.trim().toUpperCase(); if(!ma) return;

    document.getElementById("sd_maKhayInp").value = "";

    if(gioKhaySuDungTam.some(x => x.maMacDinh === ma)) { playSound('error'); return showToast("Mã khay này đã được quét!", "error"); }



    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT"));

    if(!khayThucTe) { playSound('error'); return showToast(`Mã ID ${ma} không hợp lệ hoặc chưa vô khuẩn!`, "error"); }



    gioKhaySuDungTam.push(khayThucTe); playSound('success'); renderBangKhaySuDung();

}



function renderBangKhaySuDung() {

    const tbody = document.getElementById("sd_bangKhayChon"); if(!tbody) return;

    if(gioKhaySuDungTam.length === 0) { tbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Chưa chọn khay...</td></tr>`; return; }

    tbody.innerHTML = gioKhaySuDungTam.map((item, index) => {

        let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A";

        return `<tr class="border-b font-medium hover:bg-slate-50 text-xs">

            <td class="p-2 text-center"><i class="fa-solid fa-square-check text-teal-600 text-sm"></i></td>

            <td class="p-2 font-mono font-bold text-sky-700">${item.maMacDinh}</td>

            <td class="p-2 font-bold text-slate-800">${tenBoGoc}</td>

            <td class="p-2 text-center font-mono font-bold text-rose-600 bg-rose-50/30">${item.batchCode || 'N/A'}</td>

            <td class="p-2 text-center"><span class="px-2 py-0.5 rounded text-[10px] font-bold bg-teal-100 text-teal-800">${item.status}</span></td>

            <td class="p-2 text-center"><i class="fa-solid fa-trash-can text-rose-500 cursor-pointer hover:text-rose-700" onclick="xoaKhayKhoiListSuDung(${index})"></i></td>

        </tr>`;

    }).join('');

}



function xoaKhayKhoiListSuDung(index) {

    gioKhaySuDungTam.splice(index, 1);

    renderBangKhaySuDung();

}



// =========================================================================

// 11. THIẾT LẬP MÁY IN BIXOLON SLP-TX403 (80mm x 50mm) & BIÊN BẢN HÀNG LOẠT

// =========================================================================



// In tem đơn Bixolon 80mm x 50mm

function inTemBixolonChuan(itemData) {

    const pZone = document.getElementById("print-zone");

    if (!pZone) return;



    const tenBo = itemData.bo ? itemData.bo.split(" [ID:")[0] : "DỤNG CỤ VÔ KHUẨN";

    const maID = itemData.maMacDinh || itemData.idKhay || "000000";

    const ngayNSX = itemData.ngayHapRealtime || itemData.ngayTao || getTodayDateStr();

    const ngayHSD = itemData.hsd ? new Date(itemData.hsd).toLocaleDateString('vi-VN') : "Theo quy định";

    const maBatch = itemData.batchCode || itemData.batchId || "N/A";

    const tenKhoa = itemData.khoa || "TẬP TRUNG";



    pZone.innerHTML = `

        <div class="bixolon-label" style="font-family: Arial, sans-serif;">

            <div style="text-align: center; border-bottom: 1.5px solid #000; padding-bottom: 2px;">

                <div style="font-size: 10px; font-weight: bold; text-transform: uppercase;">PHUONG NAM HOSPITAL - CSSD</div>

                <div style="font-size: 12px; font-weight: 900; margin-top: 1px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${tenBo}</div>

            </div>



            <div style="text-align: center; margin: 3px 0; display: flex; justify-content: center;">

                <svg id="bixolon-single-barcode"></svg>

            </div>



            <div style="font-size: 9px; border-top: 1px dashed #000; padding-top: 2px;">

                <div style="display: flex; justify-content: space-between; font-weight: bold;">

                    <span>LÔ: ${maBatch}</span>

                    <span>KHOA: ${tenKhoa}</span>

                </div>

                <div style="display: flex; justify-content: space-between; margin-top: 1px;">

                    <span>NSX: ${ngayNSX}</span>

                    <span style="font-size: 10px; font-weight: 900;">HSD: ${ngayHSD}</span>

                </div>

            </div>

        </div>

    `;



    JsBarcode("#bixolon-single-barcode", maID, {

        format: "CODE128",

        width: 1.6,

        height: 36,

        displayValue: true,

        fontSize: 10,

        fontOptions: "bold",

        margin: 0

    });



    document.body.classList.remove("print-mode-doc");

    document.body.classList.add("print-mode-bixolon");

    pZone.classList.remove("hidden");



    setTimeout(() => {

        window.print();

        pZone.classList.add("hidden");

        document.body.classList.remove("print-mode-bixolon");

    }, 250);

}



function inTemTongHangLoat() { 

    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 

    let batchCode = document.getElementById("hap_batchId")?.value || "A1000000_01"; 

    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 



    checkboxes.forEach((cb) => { 

        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 

        if(item) { 

            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 

            container.innerHTML += `<div class="bixolon-label" style="font-family: Arial; font-size: 11px; color: #000; background: #fff;"><div style="text-align: center; font-weight: bold; font-size: 12px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-lo-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span></div><div style="display: flex; justify-content: space-between; border-top: 1px dashed #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 9px; font-family: monospace; font-weight: bold; margin-top: 1px;">Lô: ${batchCode}</div></div>`; 

        } 

    }); 



    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); 

    document.body.classList.remove("print-mode-doc");

    document.body.classList.add("print-mode-bixolon");

    pZone.classList.remove("hidden"); 



    checkboxes.forEach(cb => { 

        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 

        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-lo-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 

    }); 



    setTimeout(() => { 

        window.print(); 

        pZone.classList.add("hidden"); 

        document.body.classList.remove("print-mode-bixolon");

    }, 300); 

}



function inTemNghiemThuHangLoat() { 

    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 

    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 



    checkboxes.forEach((cb) => { 

        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 

        if(item) { 

            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 

            container.innerHTML += `<div class="bixolon-label" style="font-family: Arial; font-size: 11px; color: #000; background: #fff;"><div style="text-align: center; font-size: 9px; font-weight: bold; letter-spacing: 0.5px;">PN HOSPITAL - CSSD</div><div style="text-align: center; font-weight: bold; font-size: 12px; margin: 1px 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-nt-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span><span style="color: green;">ĐẠT VÔ KHUẨN</span></div><div style="display: flex; justify-content: space-between; border-top: 1px solid #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 8px; font-weight: bold; font-family: monospace; margin-top: 1px;">BATCH: ${item.batchCode || 'N/A'}</div></div>`; 

        } 

    }); 



    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); 

    document.body.classList.remove("print-mode-doc");

    document.body.classList.add("print-mode-bixolon");

    pZone.classList.remove("hidden"); 



    checkboxes.forEach(cb => { 

        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 

        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-nt-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 

    }); 



    setTimeout(() => { 

        window.print(); 

        pZone.classList.add("hidden"); 

        document.body.classList.remove("print-mode-bixolon");

    }, 300); 

}



// In Biên bản giao nhận khổ A4 máy in văn phòng (Có hỗ trợ chữ ký điện tử)

function inHoaDonGiaoNhan() {

    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 

    if (!k) return showToast("Chọn Khoa trước!", "error");

    

    // Tìm chữ ký điện tử mới nhất từ Firestore hoặc localStorage

    const itemKyGầnNhat = listGiaoDich.find(x => x.khoa === k && x.anhChuKyNhanBase64);

    const savedSignature = JSON.parse(localStorage.getItem(`signature_${k}`) || "null");



    let htmlChuKyDieuDuong = `<p style="font-weight:normal; font-style:italic; color:#64748b; margin-top:60px;">(Ký và ghi rõ họ tên)</p>`;

    

    if (itemKyGầnNhat && itemKyGầnNhat.anhChuKyNhanBase64) {

        htmlChuKyDieuDuong = `

            <div style="text-align:center; margin-top:10px;">

                <img src="${itemKyGầnNhat.anhChuKyNhanBase64}" style="max-height:75px; margin:0 auto; display:block;" alt="Chữ ký"/>

                <p style="font-weight:bold; margin-top:5px; text-transform:uppercase; color:#0f172a;">${itemKyGầnNhat.nguoiKyNhanKhoa}</p>

                <p style="font-size:10px; color:#64748b; font-weight:normal;">Xác nhận: ${itemKyGầnNhat.ngayKhoaNhanThucTe || ''} ${itemKyGầnNhat.timeKhoaNhanThucTe || ''}</p>

            </div>

        `;

    } else if (savedSignature && savedSignature.chuKyImg) {

        htmlChuKyDieuDuong = `

            <div style="text-align:center; margin-top:10px;">

                <img src="${savedSignature.chuKyImg}" style="max-height:75px; margin:0 auto; display:block;" alt="Chữ ký"/>

                <p style="font-weight:bold; margin-top:5px; text-transform:uppercase; color:#0f172a;">${savedSignature.tenNguoiNhan}</p>

                <p style="font-size:10px; color:#64748b; font-weight:normal;">Xác nhận: ${savedSignature.timeKy}</p>

            </div>

        `;

    }



    let htmlBang = `

        <table style="width:100%; border-collapse:collapse; margin-top:20px; font-size:13px; text-align:left; font-family:Arial, sans-serif;">

            <thead>

                <tr style="background-color:#f1f5f9;">

                    <th style="padding:10px; border:1px solid #334155; width:35%; text-align:left; color:#0f172a;">PHÂN LOẠI MÂM / LOẠI DỤNG CỤ</th>

                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ TRẢ BẨN</th>

                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ NHẬN SẠCH</th>

                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">NHẬN VÔ KHUẨN</th>

                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:20%; color:#0f172a;">CSSD NỢ KHOA</th>

                </tr>

            </thead>

            <tbody>`;



    const rows = document.querySelectorAll('#bangDonGiaoNhan tr');

    if (rows.length === 0 || (rows.length === 1 && rows[0].cells.length === 1)) { 

        htmlBang += `<tr><td colspan="5" style="padding:15px; border:1px solid #334155; text-align:center; font-style:italic;">Không có dữ liệu công nợ.</td></tr>`; 

    } else {

        rows.forEach(row => {

            const cells = row.cells;

            if (cells.length >= 5) { 

                htmlBang += `

                    <tr>

                        <td style="padding:10px; border:1px solid #334155; font-weight:bold; color:#0f172a;">${cells[0].innerText}</td>

                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#e11d48;">${cells[1].innerText}</td>

                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#0284c7;">${cells[2].innerText}</td>

                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#7c3aed;">${cells[3].innerText}</td>

                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#d97706;">${cells[4].innerText}</td>

                    </tr>`; 

            }

        });

    }

    htmlBang += `</tbody></table>`;



    const pZone = document.getElementById("print-zone");

    pZone.innerHTML = `

        <div style="width:100%; font-family:Arial, sans-serif; padding:10px; color:#000;">

            <div style="text-align:center; margin-bottom:25px;">

                <h2 style="margin:0; font-size:18px; font-weight:900; text-transform:uppercase;">BIÊN BẢN GIAO NHẬN DỤNG CỤ CSSD</h2>

                <p style="margin:8px 0 0 0; font-size:13px; font-weight:bold;">Khoa/Phòng: <span style="text-transform:uppercase; color:#0284c7;">${k}</span> - Ngày xuất: ${new Date().toLocaleDateString('vi-VN')}</p>

            </div>

            ${htmlBang}

            <div style="margin-top:40px; display:flex; justify-content:space-between; font-size:12px; font-weight:bold; padding:0 30px;">

                <div style="text-align:center; width:45%;">

                    <p style="margin-bottom:10px; text-transform:uppercase;">ĐIỀU DƯỠNG LÂM SÀNG</p>

                    ${htmlChuKyDieuDuong}

                </div>

                <div style="text-align:center; width:45%;">

                    <p style="margin-bottom:10px; text-transform:uppercase;">CHUYÊN VIÊN CSSD</p>

                    <p style="font-weight:normal; font-style:italic; color:#64748b; margin-top:60px;">(Ký và ghi rõ họ tên)</p>

                </div>

            </div>

        </div>`;



    document.body.classList.remove("print-mode-bixolon");

    document.body.classList.add("print-mode-doc");

    pZone.classList.remove("hidden");



    setTimeout(() => { 

        window.print(); 

        pZone.classList.add("hidden"); 

        document.body.classList.remove("print-mode-doc");

    }, 200);

}



// =========================================================================

// 12. HÀM PHỤ TRỢ: CHỨC NĂNG QUẢN TRỊ ADMIN & TIỆN ÍCH

// =========================================================================

function taiDanhMucLinhKienChuan() {

    const tbody = document.getElementById("bangDanhMucLinhKien"); if (!tbody) return; tbody.innerHTML = "";

    if (!databaseExcel || databaseExcel.length === 0) { tbody.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-slate-400 italic">Chưa có dữ liệu. Vui lòng nạp file Excel.</td></tr>`; return; }

    let gopBoExcel = {};

    databaseExcel.forEach(x => {

        let tenBo = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; if (!tenBo) return;

        tenBo = String(tenBo).trim().toUpperCase(); if (!gopBoExcel[tenBo]) gopBoExcel[tenBo] = []; gopBoExcel[tenBo].push(x);

    });

    for (let tenBo in gopBoExcel) {

        let linhKienHtml = `<div class="flex flex-wrap gap-1">`; let tongSl = 0;

        gopBoExcel[tenBo].forEach(item => {

            let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || item['Dụng cụ'] || item['Tên Chi Tiết'] || "Dụng cụ Chi Tiết";

            

            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {

                tenDc = "Dụng cụ chuẩn mâm";

            }

            

            let sl = parseInt(item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1) || 0; let hanHap = item['Tuổi thọ mẻ hấp'] || item['Tuổi thọ'] || 100;

            tongSl += sl; linhKienHtml += `<span class="bg-slate-100 text-slate-700 px-1.5 py-0.5 rounded text-[10px] font-medium border border-slate-200">${tenDc} (${sl}) <span class="text-amber-600 font-bold">[Max: ${hanHap}]</span></span>`;

        });

        linhKienHtml += `</div>`; const tr = document.createElement("tr"); tr.className = "hover:bg-slate-50 transition-colors border-b";

        tr.innerHTML = `<td class="p-3 font-bold text-slate-800 text-xs">${tenBo}</td><td class="p-3">${linhKienHtml}</td><td class="p-3 text-center font-black text-sky-700 bg-sky-50/30">${tongSl}</td>`;

        tbody.appendChild(tr);

    }

}



function updateGiaoDienMatrix(role, tabId, checkboxElement) {

    if(!cauHinhGiaoDien[role]) cauHinhGiaoDien[role] = [];

    if(checkboxElement.checked) { if(!cauHinhGiaoDien[role].includes(tabId)) cauHinhGiaoDien[role].push(tabId); } 

    else { cauHinhGiaoDien[role] = cauHinhGiaoDien[role].filter(x => x !== tabId); }

    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ cauHinhGiaoDien: cauHinhGiaoDien }).then(() => showToast("Đã cập nhật quyền truy cập!", "success"));

}



function handleViewAnhBiMoi(firestoreId) { hanhDongXemAnhBiMoi(firestoreId); }

function hanhDongXemAnhBiMoi(firestoreId) {

    let item = listGiaoDich.find(x => x.firestoreId === firestoreId); if (!item) return showToast("Không tìm thấy thông tin!", "error");

    let loGoc = listGiaoDich.find(m => m.batchCode === item.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 

    let chuoiAnhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || item.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;

    if (!chuoiAnhBase64) return showToast("Mẻ này chưa tải ảnh minh chứng hoặc kế thừa.", "error");

    let w = window.open(); w.document.write(`<html><head><title>MINH CHỨNG BI - LÔ ${item.batchCode}</title></head><body style='margin:0; background:#000; display:flex; justify-content:center; align-items:center;'><img src='${chuoiAnhBase64}' style='max-width:100%; max-height:100vh; object-fit:contain;'/></body></html>`);

}



function switchAdminSubtab(sub) { 

    document.getElementById('subtab-database')?.classList.add('hidden'); document.getElementById('subtab-security')?.classList.add('hidden'); 

    document.getElementById('subbtn-database')?.classList.replace('admin-subtab-active', 'text-slate-600'); document.getElementById('subbtn-security')?.classList.replace('admin-subtab-active', 'text-slate-600'); 

    document.getElementById('subtab-' + sub)?.classList.remove('hidden'); document.getElementById('subbtn-' + sub)?.classList.add('admin-subtab-active'); 

}



function saveAdminPIN(type) { let newVal = document.getElementById(`cfg_pin${type.charAt(0).toUpperCase() + type.slice(1).toLowerCase()}`).value.trim(); if(type === 'ADMIN') thongTinMatKhauAdmin.adminPIN = newVal; if(type === 'CSSD') thongTinMatKhauAdmin.cssdPIN = newVal; if(type === 'GUEST') thongTinMatKhauAdmin.guestPIN = newVal; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ thongTinMatKhauAdmin: thongTinMatKhauAdmin }).then(() => showToast("Đã lưu PIN!", "success")); }

function themKtvCssd() { let code = prompt("Mã NV:"); let ten = prompt("Tên:"); let pin = prompt("PIN:"); if(code && ten && pin) { danhSachKtvCssd.push({ code: code.toUpperCase(), ten: ten, pin: pin }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }

function xaoKtvCssd(code) { if(confirm("Xóa?")) { danhSachKtvCssd = danhSachKtvCssd.filter(x => x.code !== code); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }

function themKhoaThuCong() { let t = prompt("Nhập Tên Khoa/Phòng:"); if(t) { danhSachKhoa.push({ ten: t.toUpperCase(), pin: "123", danhSachBo: [] }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Thêm Khoa thành công!", "success")); } }



function khaiSinhKhayVangLai() {

    if(danhSachKhoa.length === 0) return showToast("Hệ thống chưa có Khoa nào!", "error");

    let tenKhay = prompt("Nhập Tên Mâm/Khay dụng cụ:"); if(!tenKhay) return;

    let maId = prompt("Nhập Mã ID khay:"); if(!maId) return; maId = maId.toUpperCase().trim();

    let dsTenKhoa = danhSachKhoa.map((k, idx) => `${idx + 1}. ${k.ten}`).join("\n");

    let luaChon = prompt(`Chọn số thứ tự Khoa sở hữu:\n${dsTenKhoa}`); if(!luaChon) return;

    let idxKhoa = parseInt(luaChon) - 1; if(idxKhoa < 0 || idxKhoa >= danhSachKhoa.length) return showToast("Lựa chọn không hợp lệ!", "error");

    let khoaChon = danhSachKhoa[idxKhoa]; let chuoiKhayDinhDang = `${tenKhay.toUpperCase().trim()} [ID:${maId}]`;

    if(!khoaChon.danhSachBo) khoaChon.danhSachBo = []; if(khoaChon.danhSachBo.includes(chuoiKhayDinhDang)) return showToast("Khay đã tồn tại!", "error");

    khoaChon.danhSachBo.push(chuoiKhayDinhDang);

    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => { showToast(`Khai sinh khay ${maId} thành công!`, "success"); callRender(); });

}



function updatePINTrựcTiep(idx, tenKhoa) { let p = document.getElementById(`pin-khoa-${idx}`).value.trim(); if(p) { danhSachKhoa.find(x => x.ten === tenKhoa).pin = p; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Đổi PIN thành công", "success")); } }



function initSelects() { 

    let opts = '<option value="">-- Chọn Khoa --</option>' + danhSachKhoa.map(k=>`<option value="${k.ten}">${k.ten}</option>`).join('');

    if(document.getElementById("login_khoa")) document.getElementById("login_khoa").innerHTML = opts; 

    if(document.getElementById("khoa_selKhoa")) document.getElementById("khoa_selKhoa").innerHTML = opts; 

    if(document.getElementById("login_nv_cssd")) document.getElementById("login_nv_cssd").innerHTML = '<option value="">-- Chọn KTV CSSD --</option>' + danhSachKtvCssd.map(k=>`<option value="${k.code}">${k.code} - ${k.ten}</option>`).join(''); 

     

    if (document.getElementById("rua_hoaChat")) {

        document.getElementById("rua_hoaChat").innerHTML = danhMucHoaChatRua.map(hc => `<option value="${hc.ten}">${hc.ten}</option>`).join('');

        document.getElementById("rua_lieuLuong").value = danhMucHoaChatRua[0].lieuLuong;

        document.getElementById("rua_hoaChat").addEventListener("change", (e) => {

            let selectedHc = danhMucHoaChatRua.find(hc => hc.ten === e.target.value);

            if(selectedHc) document.getElementById("rua_lieuLuong").value = selectedHc.lieuLuong;

        });

    }

    if (document.getElementById("rua_loaiRua")) { capNhatDanhSachMaMayRua(); }

    if (document.getElementById("hap_loaiHap")) { capNhatDanhSachMaMay(); }

     

    const selKhoaTraba = document.getElementById("khoa_selKhoa"); if(selKhoaTraba) { selKhoaTraba.removeEventListener("change", loadBoDungCuTheoKhoa); selKhoaTraba.addEventListener("change", loadBoDungCuTheoKhoa); }

}



function showToast(msg, type="error") { const t = document.createElement('div'); t.className = `fixed top-6 right-6 ${type==="error"?"bg-rose-600":"bg-emerald-600"} text-white px-5 py-3.5 rounded-lg shadow-2xl z-[100] font-bold text-sm`; t.innerHTML = msg; document.body.appendChild(t); setTimeout(() => t.remove(), 2500); }

function toggleMobileMenu() { document.getElementById("sidebar_menu").classList.toggle("-translate-x-full"); document.getElementById("mobile-overlay").classList.toggle("hidden"); }



function moCamera(inputId) { targetInputIdForScan = inputId; document.getElementById("popupScanner").classList.remove("hidden"); html5QrCode = new Html5Qrcode("reader"); Html5Qrcode.getCameras().then(devices => { let cid = devices.length > 1 ? devices[devices.length - 1].id : devices[0].id; html5QrCode.start(cid, { fps: 15, qrbox: { width: 260, height: 180 } }, (txt) => { document.getElementById(targetInputIdForScan).value = txt.trim().toUpperCase(); if(targetInputIdForScan==='khoa_inpMaBo') themVaoGio(); if(targetInputIdForScan==='xuat_inpMaBo') xuatKhoXoayVong(); if(targetInputIdForScan==='sd_maKhayInp') scanKhayVaoSuDung(); }).catch(e=>{}) }); }

function dongCamera() { if(html5QrCode) html5QrCode.stop().then(() => html5QrCode.clear()); document.getElementById("popupScanner").classList.add("hidden"); }

function xoaSachDuLieuGiaoDichRealtime() { if(prompt("Nhập PIN ADMIN:") === (thongTinMatKhauAdmin.adminPIN||"admin2026")) { db.collection("phieuGiaoNhan").get().then(snap => { let b = db.batch(); snap.forEach(d => b.delete(d.ref)); b.commit().then(() => location.reload()); }); } }



function docAnhBiUpTaiCho(inputElement) { 

    const file = inputElement.files[0]; if (!file) return; 

    const reader = new FileReader(); reader.onloadend = function() { duLieuAnhBiTamBase64 = reader.result; showToast("Tải ảnh minh chứng BI thành công!", "success"); kiemTraQuyenDuyetMeHap(); }; 

    reader.readAsDataURL(file); 

}



// =========================================================================

// 13. HÀM CORE RENDER DỮ LIỆU ĐA TAB LUỒNG HỆ THỐNG

// =========================================================================

function renderTheoTabHienTai() {

    if(activeTab === 'khoaphong') {

        const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;

        let tatCaDonCuaKhoa = listGiaoDich.filter(x => (x.khoa || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase());

        

        // 1. TÍNH TOÁN CÔNG NỢ LUÂN CHUYỂN THỰC TẾ

        let gopCongNo = {};

        tatCaDonCuaKhoa.forEach(x => {

            if (x.status === "CHO_CAP_PHAT") return;



            let tenLoaiMâm = x.bo ? String(x.bo).split(" [ID:")[0] : "Chưa rõ mâm";

            if (!gopCongNo[tenLoaiMâm]) gopCongNo[tenLoaiMâm] = { daTraBan: 0, nhanSach: 0, nhanVoKhuan: 0, cssdNo: 0 };

            

            if (x.status !== "CHO_THU") gopCongNo[tenLoaiMâm].daTraBan += 1; 

            if (["DANG_RUA", "TRONG_BUONG_RUA", "CHO_DONG_GOI", "CHO_HAP", "DANG_HAP", "CHO_XUAT", "ĐANG_VAN_CHUYEN", "HOAN_TAT", "DA_SU_DUNG"].includes(x.status)) gopCongNo[tenLoaiMâm].nhanSach += 1;

            if (x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG") gopCongNo[tenLoaiMâm].nhanVoKhuan += 1; 

            gopCongNo[tenLoaiMâm].cssdNo = gopCongNo[tenLoaiMâm].daTraBan - gopCongNo[tenLoaiMâm].nhanVoKhuan;

        });

        

        let arrHtml = [];

        for (let key in gopCongNo) {

            let item = gopCongNo[key];

            arrHtml.push(`<tr class="border-b hover:bg-slate-50 transition-colors font-medium text-[11px]"><td class="p-3 font-bold text-slate-800 border-r">${key}</td><td class="p-3 text-center text-rose-600 font-bold bg-rose-50/20 border-r">${item.daTraBan}</td><td class="p-3 text-center text-sky-600 font-bold bg-sky-50/20 border-r">${item.nhanSach}</td><td class="p-3 text-center text-purple-600 font-bold bg-purple-50/20 border-r">${item.nhanVoKhuan}</td><td class="p-3 text-center text-amber-600 font-black bg-amber-50/20">${item.cssdNo > 0 ? 'Nợ ' + item.cssdNo : '-'}</td></tr>`);

        }

        if(document.getElementById("bangDonGiaoNhan")) {

            document.getElementById("bangDonGiaoNhan").innerHTML = arrHtml.length ? arrHtml.join('') : `<tr><td colspan="5" class="p-8 text-center text-slate-400 italic">Khoa chưa phát sinh công nợ luân chuyển đồ.</td></tr>`;

        }

        

        // 2. DANH SÁCH CHỜ NHẬN ĐỒ SẠCH (Từ xe trung chuyển CSSD bàn giao lên)

        const tbodyChoNhan = document.getElementById("bangChoNhanTaiKhoa"); const badgeChoNhan = document.getElementById("badgeChoNhanKhoa"); const txtNguoiXacNhan = document.getElementById("txtNguoiDungNhanHienTai");

        if(txtNguoiXacNhan) txtNguoiXacNhan.innerText = `PM-ĐD: ${loginUserCode || "Chưa Đăng Nhập"}`;

        

        let dsDangVanChuyen = tatCaDonCuaKhoa.filter(x => x.status === "ĐANG_VAN_CHUYEN"); if(badgeChoNhan) badgeChoNhan.innerText = `${dsDangVanChuyen.length} khay`;

        if(tbodyChoNhan) {

            if(dsDangVanChuyen.length === 0) { tbodyChoNhan.innerHTML = `<tr><td colspan="5" class="p-3 text-center text-slate-400 italic">Hiện không có dụng cụ nào đang chuyển về khoa.</td></tr>`; } 

            else { tbodyChoNhan.innerHTML = dsDangVanChuyen.map(khay => `<tr class="hover:bg-slate-50 transition-colors"><td class="p-2 text-center"><input type="checkbox" value="${khay.firestoreId}" class="w-3.5 h-3.5 text-sky-600 rounded border-slate-300 focus:ring-sky-500 cursor-pointer"></td><td class="p-2 font-mono font-bold text-slate-700">${khay.maMacDinh || 'N/A'}</td><td class="p-2 font-semibold text-slate-800">${khay.bo ? String(khay.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-2 text-slate-500 font-medium">${khay.nvXuatKho || '--'}</td><td class="p-2 text-center"><span class="bg-purple-50 text-purple-700 font-mono text-[10px] px-1.5 py-0.5 rounded font-bold">${khay.batchCode || 'N/A'}</span></td></tr>`).join(''); }

        }



        // 3. THEO DÕI YÊU CẦU ĐANG CHỜ CSSD CẤP PHÁT

        let dsChoCapPhat = tatCaDonCuaKhoa.filter(x => x.status === "CHO_CAP_PHAT");

        const tbodyChoCapPhat = document.getElementById("bangChoCapPhatTaiKhoa");

        if (tbodyChoCapPhat) {

            if (dsChoCapPhat.length === 0) {

                tbodyChoCapPhat.innerHTML = `<tr><td colspan="4" class="p-3 text-center text-slate-400 italic">Không có yêu cầu mượn khay mới nào đang chờ duyệt.</td></tr>`;

            } else {

                tbodyChoCapPhat.innerHTML = dsChoCapPhat.map(yc => `

                    <tr class="border-b text-xs hover:bg-amber-50/10 font-medium">

                        <td class="p-2.5 font-bold text-amber-800">${yc.bo}</td>

                        <td class="p-2.5 text-center"><span class="px-2 py-0.5 rounded-full text-[10px] font-bold bg-amber-100 text-amber-800 border border-amber-200 animate-pulse">Chờ CSSD cấp khay</span></td>

                        <td class="p-2.5 text-center font-mono text-slate-400">${yc.time}</td>

                        <td class="p-2.5 text-center text-slate-500">${yc.nvYeuCau || 'ĐD'}</td>

                    </tr>

                `).join('');

            }

        }

    }

    else if(activeTab === 'thugom') {

        let fK = document.getElementById("filterKhoaThuGom")?.value || ""; 

        let lsTG = listGiaoDich.filter(x => x.status === "CHO_THU"); 

        

        if (document.getElementById("filterKhoaThuGom")) {

            let danhSachKhoaCoDon = [...new Set(lsTG.map(x => x.khoa))].filter(Boolean); 

            let htmlOpts = '<option value="">-- Lọc theo Khoa --</option>';

            danhSachKhoaCoDon.forEach(k => { 

                htmlOpts += `<option value="${k}" ${(k === fK) ? "selected" : ""}>${k}</option>`; 

            });

            document.getElementById("filterKhoaThuGom").innerHTML = htmlOpts; 

            fK = document.getElementById("filterKhoaThuGom").value || "";

        }

        

        if (fK) lsTG = lsTG.filter(x => (x.khoa || "").toString().trim().toUpperCase() === fK.toString().trim().toUpperCase());

        if (document.getElementById("badgeSoCho")) document.getElementById("badgeSoCho").innerText = `${lsTG.length} Lệnh`;

        

        let bodyThuGom = document.getElementById("bangChoThuGom");

        if(bodyThuGom) {

            bodyThuGom.innerHTML = lsTG.map(i => {

                let tenBo = i.bo ? String(i.bo).split(" [ID:")[0].trim() : ""; 

                

                let itemsInBo = databaseExcel.filter(x => {

                    let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || '';

                    return String(boName).trim().toUpperCase() === String(tenBo).toUpperCase();

                });

                

                let checklistHtml = "";

                if (itemsInBo.length > 0) {

                    checklistHtml = `<div class="max-h-32 overflow-y-auto pr-1 border border-slate-200/60 rounded bg-slate-50/50 p-1.5 mt-1 flex flex-col gap-1">`;

                    itemsInBo.forEach(item => {

                        let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || "Dụng cụ chi tiết";

                        let sl = item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1;

                        

                        if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {

                            tenDc = "Dụng cụ chuẩn mâm";

                        }



                        checklistHtml += `

                            <div class="flex justify-between items-center border-b border-dashed border-slate-200 pb-0.5 last:border-none text-[11px]">

                                <span class="text-slate-700 font-semibold"><i class="fa-solid fa-circle text-[4px] text-sky-400 mr-1.5 align-middle"></i>${tenDc}</span>

                                <span class="font-bold text-sky-700 bg-sky-50 px-1.5 py-0.5 rounded border border-sky-100 text-[10px]">x${sl}</span>

                            </div>`;

                    });

                    checklistHtml += `</div>`;

                } else {

                    checklistHtml = `<div class="text-[10px] text-emerald-700 bg-emerald-50 border border-emerald-100 p-1.5 rounded font-medium mt-1"><i class="fa-solid fa-circle-check mr-1"></i>Dụng cụ lẻ (Cơ số: 1 chiếc)</div>`;

                }

                

                return `

                    <tr class="border-b border-slate-100 hover:bg-slate-50/40 transition-colors">

                        <td class="p-3 align-top">

                            <div class="font-black text-slate-700 text-[11px] uppercase tracking-wide">${i.khoa}</div>

                            ${i.ghiChu ? `<div class="text-[10px] text-rose-600 font-bold bg-rose-50 border border-rose-100 px-1.5 py-0.5 rounded mt-1.5 inline-block"><i class="fa-solid fa-triangle-exclamation mr-1"></i>${i.ghiChu}</div>` : ''}

                        </td>

                        <td class="p-3 align-top">

                            <div class="font-black text-sky-800 text-[13px] uppercase tracking-tight">${tenBo}</div>

                            <div class="text-[10px] font-mono text-slate-400 font-bold mt-0.5">MÃ SỐ ID: ${i.maMacDinh}</div>

                            ${checklistHtml}

                        </td>

                        <td class="p-3 text-center align-top font-mono font-bold text-slate-500 text-[11px]">${i.time}</td>

                        <td class="p-3 text-center align-top action-col">

                            <button onclick="moPopupKiemDem('${i.firestoreId}')" class="bg-sky-600 hover:bg-sky-700 text-white font-black px-3.5 py-2 rounded-lg shadow-sm text-[11px] uppercase tracking-wider transition-all"><i class="fa-solid fa-clipboard-check mr-1"></i>Kiểm Đếm</button>

                        </td>

                    </tr>`;

            }).join('');

        }

    }

    else if(activeTab === 'mayrua') {

        let lsCR = listGiaoDich.filter(x => x.status === "DANG_RUA");

        if(document.getElementById("badgeChoRua")) document.getElementById("badgeChoRua").innerText = `${lsCR.length} Mục`;

        

        let bodyChoRua = document.getElementById("bangChoRua");

        if(bodyChoRua) {

            bodyChoRua.innerHTML = lsCR.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="rua-checkbox"></td><td class="p-3 font-bold">${i.bo}</td><td class="p-3 text-right font-mono">${i.maMacDinh}</td></tr>`).join('');

        }

        

        let lsTR = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA");

        let bodyChoNghiemThuRua = document.getElementById("bangChoNiemThuRua");

        if(bodyChoNghiemThuRua) {

            bodyChoNghiemThuRua.innerHTML = lsTR.map(i => `<tr class="border-b"><td class="p-2.5 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="nghiemthurua-checkbox"></td><td class="p-2 text-xs font-bold">${i.bo} <span class="text-slate-400 font-mono">(${i.rua_batchCode || 'Mẻ Rửa'})</span></td></tr>`).join('');

        }

        

        let bodyLichSuRua = document.getElementById("bangLichSuRua");

        if(bodyLichSuRua) {

            const homNayMoiStr = getTodayDateStr();

            let tatCaMucCoLoRua = listGiaoDich.filter(x => x.rua_batchCode && (x.rua_ngayRua === homNayMoiStr || x.ngayTao === homNayMoiStr));

            let cacMeRuaGop = {};

            tatCaMucCoLoRua.forEach(x => {

                if(!cacMeRuaGop[x.rua_batchCode]) {

                    cacMeRuaGop[x.rua_batchCode] = {

                        batchCode: x.rua_batchCode,

                        mayRua: x.rua_mayRua || "Belimed WD250",

                        chuKy: x.rua_chuKy || "Chu trình tiêu chuẩn",

                        hoaChat: x.rua_hoaChat || "Smeg",

                        timeBatDau: x.rua_timeBatDau || "--:--",

                        ngay: x.rua_ngayRua || "",

                        soLuong: 0,

                        trangThaiMe: x.status === "TRONG_BUONG_RUA" ? "Đang rửa" : "Hoàn tất sạch"

                    };

                }

                cacMeRuaGop[x.rua_batchCode].soLuong += 1;

            });

            let danhSachMeRuaSapXep = Object.values(cacMeRuaGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));

            if(danhSachMeRuaSapXep.length === 0) {

                bodyLichSuRua.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa chạy mẻ rửa nào</td></tr>`;

            } else {

                bodyLichSuRua.innerHTML = danhSachMeRuaSapXep.map(me => `

                    <tr class="hover:bg-slate-50 transition-colors">

                        <td class="p-3">

                            <div class="flex items-center gap-2">

                                <span class="font-mono font-black text-sky-700 text-sm tracking-wider">${me.batchCode}</span>

                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "Đang rửa" ? "text-amber-600 bg-amber-50 border-amber-200" : "text-emerald-600 bg-emerald-50 border-emerald-200"}">${me.trangThaiMe}</span>

                            </div>

                            <div class="text-[11px] text-slate-500 font-medium mt-1">${me.mayRua} | ${me.chuKy}</div>

                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Hoá chất: ${me.hoaChat} | Bắt đầu: ${me.timeBatDau} (${me.ngay})</div>

                        </td>

                        <td class="p-3 text-right pr-4">

                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuong} khay</span>

                        </td>

                    </tr>`).join('');

            }

        }

    }

    else if(activeTab === 'donggoi') {

        let lsDG = listGiaoDich.filter(x => x.status === "CHO_DONG_GOI"); 

        if(document.getElementById("badgeDongGoi")) document.getElementById("badgeDongGoi").innerText = lsDG.length;

        let gDongGoi = document.getElementById("gridDongGoi");

        if(gDongGoi) {

            gDongGoi.innerHTML = lsDG.map(i => `<div class="bg-white p-3 rounded border border-slate-200 mb-2 flex justify-between items-center"><div class="flex-1"><div class="font-bold text-sky-700 text-[13px]">${i.bo}</div><div class="text-[10px] text-slate-500">Từ khoa: ${i.khoa}</div></div><button onclick="moPopupDongGoi('${i.firestoreId}')" class="bg-sky-50 text-sky-700 border border-sky-300 px-3 py-1.5 rounded text-[11px] font-black">ĐÓNG GÓI</button></div>`).join('');

        }

    }

    else if(activeTab === 'mayhap') {

        let lsCH = listGiaoDich.filter(x => x.status === "CHO_HAP"); 

        if(document.getElementById("badgeChoHap")) document.getElementById("badgeChoHap").innerText = `${lsCH.length} Mục`;

        let bodyChoHap = document.getElementById("bangChoHap");

        if(bodyChoHap) { 

            bodyChoHap.innerHTML = lsCH.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="hap-checkbox"></td><td class="p-3 font-bold text-xs">${i.bo}</td><td class="p-3 text-right font-mono text-xs font-bold text-sky-700">${i.maMacDinh}</td></tr>`).join(''); 

        }



        let lsNT = listGiaoDich.filter(x => x.status === "DANG_HAP"); 

        let bodyChoNghiemThu = document.getElementById("bangChoNghiệmThu") || document.getElementById("bangChoNghiemThu");

        

        if(bodyChoNghiemThu) { 

            if (lsNT.length === 0) {

                bodyChoNghiemThu.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Không có mâm/mẻ nào đang trong lò chờ duyệt.</td></tr>`;

            } else {

                bodyChoNghiemThu.innerHTML = lsNT.map(i => {

                    let tenBoClean = i.bo ? String(i.bo).split(" [ID:")[0] : "Chưa rõ mâm";

                    return `

                        <tr class="border-b hover:bg-amber-50/40 transition-colors">

                            <td class="p-2.5 text-center action-col">

                                <input type="checkbox" value="${i.firestoreId}" class="nghiemthu-checkbox w-4 h-4 text-amber-600 rounded cursor-pointer" data-hasbi="${i.hasBI ? 'true' : 'false'}" onchange="kiemTraQuyenDuyetMeHap()">

                            </td>

                            <td class="p-2 text-xs">

                                <div class="font-black text-slate-800">${tenBoClean} <span class="text-[10px] font-mono text-sky-700">(${i.maMacDinh || 'N/A'})</span></div>

                                <div class="flex items-center gap-2 mt-0.5">

                                    <span class="text-[10px] font-mono font-bold text-rose-700 bg-rose-50 px-1 rounded border border-rose-100">Lô: ${i.batchCode || 'N/A'}</span>

                                    ${i.hasBI ? '<span class="px-1.5 py-0.5 bg-amber-100 text-amber-800 rounded text-[9px] font-black border border-amber-300 flex items-center gap-1"><i class="fa-solid fa-flask"></i> CHỜ BI</span>' : ''}

                                </div>

                            </td>

                        </tr>`;

                }).join(''); 

            }

        }

        kiemTraQuyenDuyetMeHap();



        if(document.getElementById("bangLichSuHap")) {

            const homNayMoiStr = getTodayDateStr(); 

            let tatCaMucCoLo = listGiaoDich.filter(x => x.batchCode && (x.ngayHapRealtime === homNayMoiStr || x.ngayTao === homNayMoiStr)); 

            let cacMeHapGop = {};

            tatCaMucCoLo.forEach(x => {

                if(!cacMeHapGop[x.batchCode]) { 

                    cacMeHapGop[x.batchCode] = { 

                        batchCode: x.batchCode, 

                        loaiHap: x.thongTinLoHap?.loaiHap || "Hấp hơi nước", 

                        chuKyNhiet: x.thongTinLoHap?.chuKyNhiet || "134°C - 4 phút", 

                        apSuat: x.thongTinLoHap?.apSuat || "N/A", 

                        thoiGian: x.thongTinLoHap?.thoiGianBatDau || x.time || "--:--", 

                        ngay: x.ngayHapRealtime || x.ngayTao || "", 

                        soLuongKhay: 0, 

                        trangThaiMe: x.status === "DANG_HAP" ? "ĐANG CHẠY LÒ" : "HOÀN THÀNH" 

                    }; 

                }

                cacMeHapGop[x.batchCode].soLuongKhay += 1;

            });

            let danhSachMeHapSapXep = Object.values(cacMeHapGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));

            if(danhSachMeHapSapXep.length === 0) { 

                document.getElementById("bangLichSuHap").innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa có mẻ hấp nào</td></tr>`; 

            } else { 

                document.getElementById("bangLichSuHap").innerHTML = danhSachMeHapSapXep.map(me => `

                    <tr class="hover:bg-slate-50 transition-colors border-b border-slate-100">

                        <td class="p-3">

                            <div class="flex items-center gap-2">

                                <span class="font-mono font-black text-rose-700 text-sm tracking-wider">${me.batchCode}</span>

                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "ĐANG CHẠY LÒ" ? "text-purple-700 bg-purple-100 border border-purple-200 animate-pulse" : "text-emerald-700 bg-emerald-100 border border-emerald-200"}">${me.trangThaiMe}</span>

                            </div>

                            <div class="text-[11px] text-slate-600 font-medium mt-1">${me.loaiHap} | ${me.chuKyNhiet} | Áp suất: ${me.apSuat} Bar</div>

                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Bắt đầu: ${me.thoiGian} (${me.ngay})</div>

                        </td>

                        <td class="p-3 text-right pr-4">

                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuongKhay} khay</span>

                        </td>

                    </tr>`).join(''); 

            }

        }

    }

    else if(activeTab === 'khovokhuan') {

        let uniqueKhoaSanCo = [...new Set(listGiaoDich.map(x => x.khoa))].filter(Boolean); const selKhoaXuat = document.getElementById("xuat_selKhoa");

        if(selKhoaXuat) {

            let currentSelected = selKhoaXuat.value; let htmlOpts = '<option value="">-- Chọn Khoa Muốn Trả Đồ --</option>';

            uniqueKhoaSanCo.forEach(k => { htmlOpts += `<option value="${k}" ${k === currentSelected ? 'selected' : ''}>${k}</option>`; });

            selKhoaXuat.innerHTML = htmlOpts;

        }



        let lsXK = listGiaoDich.filter(x => x.status === "CHO_XUAT").sort((a, b) => a.id - b.id);

        renderKhoVoKhuan(lsXK);



        let dsYeuCau = listGiaoDich.filter(x => x.status === "CHO_CAP_PHAT");

        let vungYeuCau = document.getElementById("vungYeuCauCapPhat");

        if (!vungYeuCau) {

            let parentContainer = document.getElementById("tab-khovokhuan");

            if (parentContainer) {

                let divYc = document.createElement("div");

                divYc.id = "vungYeuCauCapPhat";

                divYc.className = "mb-6 bg-amber-50/70 border border-amber-200 rounded-xl p-4 shadow-sm";

                parentContainer.insertBefore(divYc, parentContainer.firstChild);

                vungYeuCau = divYc;

            }

        }

        

        if (vungYeuCau) {

            if (dsYeuCau.length === 0) {

                vungYeuCau.innerHTML = `<h3 class="font-black text-amber-950 text-xs flex items-center gap-1.5"><i class="fa-solid fa-bell-slash"></i> HIỆN KHÔNG CÓ YÊU CẤP PHÁT ĐANG CHỜ</h3>`;

            } else {

                vungYeuCau.innerHTML = `

                    <h3 class="font-black text-amber-900 text-xs mb-2.5 flex items-center gap-1.5 animate-pulse">

                        <i class="fa-solid fa-bell text-rose-500 animate-bounce"></i> CÓ ${dsYeuCau.length} YÊU CẦU CẤP PHÁT TỪ CÁC KHOA LÂM SÀNG

                    </h3>

                    <div class="flex flex-col gap-2 max-h-48 overflow-y-auto">

                        ${dsYeuCau.map(yc => `

                            <div class="bg-white p-3 rounded-lg border border-amber-200 shadow-sm flex justify-between items-center text-xs">

                                <div>

                                    <span class="font-black text-amber-800 uppercase">[KHOA: ${yc.khoa}]</span> 

                                    Yêu cầu mâm: <span class="font-bold text-slate-800">${yc.bo}</span>

                                    <span class="text-[10px] text-slate-400 block mt-0.5">Thời gian: ${yc.time} - Người yêu cầu: ${yc.nvYeuCau || 'ĐD'}</span>

                                </div>

                                <div class="flex items-center gap-2">

                                    <button onclick="duyetCapPhatTậpTrung('${yc.firestoreId}', '${yc.bo}', '${yc.khoa}')" class="bg-amber-600 hover:bg-amber-700 text-white font-black px-3 py-2 rounded-lg text-[10px] uppercase shadow-sm flex items-center gap-1.5 transition-all">

                                        <i class="fa-solid fa-truck-ramp-box"></i>Cấp phát khay

                                    </button>

                                </div>

                            </div>

                        `).join('')}

                    </div>`;

            }

        }

        renderGioHangXuat();

    }

    else if(activeTab === 'quanlykho') {

        let fK = document.getElementById("inv_filterKhoa") ? document.getElementById("inv_filterKhoa").value : ""; 

        let uniqueKhoa = [...new Set(listGiaoDich.map(x=>x.khoa))].filter(Boolean);

        if(document.getElementById("inv_filterKhoa")) { document.getElementById("inv_filterKhoa").innerHTML = '<option value="">-- Tất cả --</option>' + uniqueKhoa.map(k=>`<option value="${k}" ${k===fK?'selected':''}>${k}</option>`).join(''); }

        let uniqueIDs = [...new Set(listGiaoDich.map(x=>x.maMacDinh))]; let arrHtml = []; const ngayHomNay = new Date(); ngayHomNay.setHours(0,0,0,0); 

        uniqueIDs.forEach(ma => {

            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a,b) => b.id - a.id)[0]; if(!currentTrans || !ma) return;

            let viTriCode = currentTrans.status; let khoaGiữ = currentTrans.khoa; if (fK && (khoaGiữ || "").toString().trim().toUpperCase() !== fK.toString().trim().toUpperCase()) return;

            let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";

            if(viTriCode === "HOAN_TAT") { viTriText = `Sẵn sàng tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 

            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã sử dụng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }

            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang vận chuyển`; viTriColor = "bg-purple-100 text-purple-800 animate-pulse"; }

            else if(viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }

            let hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-slate-100 text-slate-500">An toàn</span>`; let dongColorClass = ""; 

            if (currentTrans.hsd && (viTriCode === "CHO_XUAT" || viTriCode === "HOAN_TAT")) {

                const ngayHsd = new Date(currentTrans.hsd); ngayHsd.setHours(0,0,0,0); const soNgayConLai = Math.ceil((ngayHsd.getTime() - ngayHomNay.getTime()) / (1000 * 60 * 60 * 24));

                if (soNgayConLai < 0) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-rose-600 text-white">QUÁ HẠN</span>`; dongColorClass = "bg-rose-50/70 border-l-4 border-l-rose-500 font-bold"; } 

                else if (soNgayConLai <= 3) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-amber-500 text-white">HẠN < ${soNgayConLai} NGÀY</span>`; dongColorClass = "bg-amber-50/40 border-l-4 border-l-amber-500"; } 

                else { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-emerald-100 text-emerald-800">Hạn ${soNgayConLai} ngày</span>`; }

            }

            arrHtml.push(`<tr class="border-b border-slate-100 ${dongColorClass}"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-slate-500 text-[11px]">${khoaGiữ}</td><td class="p-3 text-center"><span class="px-2.5 py-1 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-mono font-bold text-slate-400">${currentTrans.batchCode || 'N/A'}</td><td class="p-3 text-center">${hsdBadget}</td></tr>`);

        });

        if(document.getElementById("bangTonKhoTe")) document.getElementById("bangTonKhoTe").innerHTML = arrHtml.join('');

    }

    else if(activeTab === 'danhmuc') {

        const tbody = document.getElementById("bangDanhMucTong"); if (!tbody) return;

        let uniqueIDs = [...new Set(listGiaoDich.map(x => x.maMacDinh))].filter(Boolean); let arrHtml = [];

        uniqueIDs.forEach(ma => {

            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a, b) => b.id - a.id)[0]; if (!currentTrans) return;

            let viTriCode = currentTrans.status; let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";

            if (viTriCode === "HOAN_TAT") { viTriText = `Tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 

            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã dùng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }

            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang đi đường`; viTriColor = "bg-purple-100 text-purple-800"; }

            else if (viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }

            let chuKyLo = listGiaoDich.filter(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG" || x.status === "ĐANG_VAN_CHUYEN")).length;

            arrHtml.push(`<tr class="border-b border-slate-100 font-medium"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-center"><span class="px-2.5 py-0.5 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-black text-amber-700 bg-amber-50/50">${chuKyLo} lần lễ</td></tr>`);

        });

        tbody.innerHTML = arrHtml.join('');

    }

    else if(activeTab === 'lichsuluanchuyen') {

        const tbody = document.getElementById("bangLichSuHanhTrinhGoc");

        if(tbody) {

            tbody.innerHTML = listGiaoDich.map(x => {

                let statusBadge = `<span class="px-2 py-0.5 bg-slate-100 text-slate-700 font-bold rounded text-[10px]">${x.status}</span>`;

                if(x.status === "HOAN_TAT") statusBadge = `<span class="px-2 py-0.5 bg-emerald-100 text-emerald-800 font-black rounded text-[10px]">ĐÃ BÀN GIAO</span>`;

                else if(x.status === "CHO_XUAT") statusBadge = `<span class="px-2 py-0.5 bg-teal-100 text-teal-800 font-bold rounded text-[10px]">KHO VÔ KHUẨN</span>`;

                else if(x.status === "DANG_HAP") statusBadge = `<span class="px-2 py-0.5 bg-purple-100 text-purple-800 font-bold rounded text-[10px]">ĐANG HẤP LÒ</span>`;

                else if(x.status === "DA_SU_DUNG") statusBadge = `<span class="px-2 py-0.5 bg-rose-600 text-white font-black rounded text-[10px]">SỬ DỤNG BN</span>`;

                else if(x.status === "ĐANG_VAN_CHUYEN") statusBadge = `<span class="px-2 py-0.5 bg-purple-500 text-white font-bold rounded text-[10px] animate-pulse">ĐANG VẬN CHUYỂN</span>`;

                else if(x.status === "DANG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-blue-100 text-blue-700 font-bold rounded text-[10px]">HÀNG ĐỢI RỬA</span>`;

                else if(x.status === "TRONG_BUONG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-cyan-600 text-white font-bold rounded text-[10px]">ĐANG RỬA MÁY</span>`;

                else if(x.status === "CHO_CAP_PHAT") statusBadge = `<span class="px-2 py-0.5 bg-amber-100 text-amber-800 font-bold rounded text-[10px] border border-amber-200">CHỜ CẤP PHÁT</span>`;

                

                let fontColor = ""; let loGoc = listGiaoDich.find(m => m.batchCode === x.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 

                let anhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || x.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;

                if (anhBase64) fontColor = `<br><span onclick="handleViewAnhBiMoi('${x.firestoreId}')" class="text-[10px] text-emerald-600 bg-emerald-50 border border-emerald-300 rounded px-1 cursor-pointer font-bold mt-1 inline-block"><i class="fa-solid fa-image mr-1"></i>Xem ảnh BI</span>`;

                

                let kpiHtml = x.ketQuaGiamSatKpi ? `<br><span class="text-[9px] px-1 rounded font-black border mt-1 inline-block ${x.ketQuaGiamSatKpi === "ĐẠT" ? "text-emerald-700 bg-emerald-50 border-emerald-200" : "text-rose-700 bg-rose-50 border-rose-200"}">KPI: ${x.ketQuaGiamSatKpi} (${x.thoiGianTinhKpiPhut}m)</span>` : "";

                let thongTinKemTheo = x.nvXuatKho || '<span class="text-slate-300 font-normal">Chưa xuất</span>';

                if (x.nguoiKyNhanKhoa) thongTinKemTheo += ` ➔ <span class="text-emerald-700 font-semibold">[ĐD: ${x.nguoiKyNhanKhoa}]</span>`;

                if (x.status === "DA_SU_DUNG" && x.thongTinBenhNhan) thongTinKemTheo = `<span class="text-teal-700 font-black">BN: ${x.thongTinBenhNhan.thongTinTimKiemBN}</span>`;



                return `<tr class="border-b text-xs hover:bg-slate-50 transition-colors"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3 text-center">${statusBadge}${kpiHtml}</td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/30">${x.batchCode || 'N/A'}${fontColor}</td><td class="p-3 text-center font-bold text-sky-800 bg-sky-50/40">${thongTinKemTheo}</td><td class="p-3 text-center text-slate-400 font-mono text-[11px]">${x.ngayTao || ''} ${x.time || ''}</td></tr>`;

            }).join('');

        }

    }

    else if(activeTab === 'tracuu') {

        let safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");

        if (!safeTbody) {

            let parentContainer = document.getElementById("tab-tracuu") || document.querySelector('.bg-white.p-6.rounded-xl.shadow-md');

            if(parentContainer) {

                let existingTableZone = document.getElementById("vung-ket-qua-tu-dong");

                if(!existingTableZone) {

                    let tableWrapper = document.createElement("div"); tableWrapper.id = "vung-ket-qua-tu-dong"; tableWrapper.className = "mt-6 bg-white border border-slate-200 rounded-xl p-4 shadow-sm";

                    tableWrapper.innerHTML = `<h3 class="font-black text-slate-800 text-sm mb-3 text-sky-800"><i class="fa-solid fa-list-check mr-2"></i>DANH SÁCH MÂM DỤNG CỤ TRONG MẺ TRUY VẾT</h3><div class="overflow-x-auto"><table class="w-full text-left border-collapse"><thead><tr class="bg-slate-100 text-[11px] font-bold text-slate-600 uppercase border-b"><th class="p-3">Mã ID Khay</th><th class="p-3">Tên Bộ Dụng Cụ</th><th class="p-3">Khoa Sử Dụng</th><th class="p-3">Trạng thái</th><th class="p-3 text-center">Mã Lô Hấp</th><th class="p-3 text-center">Thời Gian Kích Hoạt</th></tr></thead><tbody id="bangLichSuTruyXuatAdmin"></tbody></table></div>`;

                    parentContainer.appendChild(tableWrapper); safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");

                }

            }

        }

        let searchInp = maLoTruyVetToanCuc || ""; if (!searchInp && document.getElementById("inp_searchBatch")) { searchInp = document.getElementById("inp_searchBatch").value.trim(); }

        if(safeTbody) {

            if (!searchInp) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng nhập mã mẻ hấp và nhấn "TRA CỨU KHẨN CẤP"...</td></tr>`; return; }

            let dataFiltered = listGiaoDich.filter(x => x.batchCode && String(x.batchCode).toUpperCase().includes(searchInp.toUpperCase()));

            if(dataFiltered.length === 0) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-rose-500 italic font-bold">Không tìm thấy dữ liệu: ${searchInp}</td></tr>`; } 

            else { safeTbody.innerHTML = dataFiltered.map(x => `<tr class="border-b text-xs hover:bg-slate-50 font-medium"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3"><span class="px-2 py-0.5 rounded text-[10px] ${x.status === "HOAN_TAT" ? "bg-emerald-100 text-emerald-800 font-bold" : "bg-teal-100 text-teal-800"}">${x.status}</span></td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/40">${x.batchCode || 'N/A'}</td><td class="p-3 text-center text-slate-400 font-mono">${x.ngayTao || ''} ${x.time || ''}</td></tr>`).join(''); }

        }

    }

    else if(activeTab === 'performance') { renderKpiPerformanceGoc(); }

    else if(activeTab === 'dashboard_tv') { renderDashboardTiviRealtime(); }

}



function renderKhoVoKhuan(listData) {

    const tbody = document.getElementById("bangKhoVoKhuan");

    if (!tbody) return;



    if (!listData || listData.length === 0) {

        tbody.innerHTML = `<tr><td colspan="6" class="p-6 text-center text-slate-400 italic">Kho vô khuẩn hiện tại trống.</td></tr>`;

        return;

    }



    tbody.innerHTML = listData.map(item => `

        <tr class="hover:bg-slate-50 transition-colors">

            <td class="p-3.5 font-bold text-slate-800 text-xs">${item.bo ? String(item.bo).split(" [ID:")[0] : 'N/A'}</td>

            <td class="p-3.5 font-mono font-black text-sky-700 text-xs">${item.maMacDinh || '--'}</td>

            <td class="p-3.5 text-center font-semibold text-slate-600 text-xs">${item.khoa || 'Tập trung'}</td>

            <td class="p-3.5 text-center"><span class="bg-emerald-100 text-emerald-800 font-bold px-2.5 py-1 rounded-full text-[11px]">${item.viTriKe || 'Kệ A1'}</span></td>

            <td class="p-3.5 text-center font-mono font-bold text-xs text-rose-600">${item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN') : 'An toàn'}</td>

            <td class="p-2 text-center">

                <button onclick="xuatKhoXoayVong('${item.maMacDinh}')" 

                        class="min-h-[44px] px-4 bg-emerald-600 active:bg-emerald-700 text-white font-bold rounded-lg text-xs shadow-sm inline-flex items-center gap-1.5 transition-transform active:scale-95">

                    <i class="fa-solid fa-cart-flatbed"></i>

                    <span>Xuất Khay</span>

                </button>

            </td>

        </tr>

    `).join('');

}



// =========================================================================

// VÁ LỖI CÁC HÀM TRUY VẾT & ĐỒNG BỘ HIỆU SUẤT TV MÀN HÌNH

// =========================================================================

function truyVetTheoMaBatch() {

    const inp = document.getElementById("inp_searchBatch");

    if (inp) {

        maLoTruyVetToanCuc = inp.value.trim();

        playSound('success');

        callRender();

    }

}



function clearTruyVetBatch() {

    const inp = document.getElementById("inp_searchBatch");

    if (inp) inp.value = "";

    maLoTruyVetToanCuc = "";

    callRender();

}



function resetDuLieuKet() {

    if (confirm("Anh Hùng có chắc chắn muốn giải phóng các mâm đang xử lý dở dang (chưa Hoàn tất / Chưa dùng) về trạng thái Chờ thu gom?")) {

        let p = [];

        listGiaoDich.forEach(x => {

            if (x.status !== "HOAN_TAT" && x.status !== "DA_SU_DUNG") {

                p.push(db.collection("phieuGiaoNhan").doc(x.firestoreId).update({ status: "CHO_THU" }));

            }

        });

        Promise.all(p).then(() => {

            playSound('success');

            showToast("Đã giải phóng dữ liệu mâm kẹt thành công!", "success");

            callRender();

        });

    }

}



function renderKpiPerformanceGoc() {

    const tbody = document.getElementById("bangHieuSuatKTV");

    if (!tbody) return;

    let kpiGop = {};

    listGiaoDich.forEach(x => {

        let ktv = x.ktvThuGom || x.nvXuatKho || "CSSD_CHUNG";

        if (!kpiGop[ktv]) kpiGop[ktv] = { code: ktv, ten: ktv, dat: 0, tong: 0 };

        if (x.ketQuaGiamSatKpi === "ĐẠT") kpiGop[ktv].dat += 1;

        if (x.ketQuaGiamSatKpi) kpiGop[ktv].tong += 1;

    });

    let sortedKpi = Object.values(kpiGop).sort((a,b) => b.dat - a.dat);

    if(sortedKpi.length === 0) {

        tbody.innerHTML = `<tr><td colspan="4" class="p-4 text-center italic text-slate-400">Hôm nay chưa có dữ liệu mẻ KPI nào.</td></tr>`;

    } else {

        tbody.innerHTML = sortedKpi.map((item, idx) => `

            <tr class="border-b text-xs hover:bg-slate-50 font-medium">

                <td class="p-3 text-center font-bold">${idx + 1}</td>

                <td class="p-3 font-mono font-bold text-sky-700">${item.code}</td>

                <td class="p-3 font-bold text-slate-800">${item.ten}</td>

                <td class="p-3 text-center"><span class="bg-emerald-100 text-emerald-800 px-2.5 py-1 rounded font-black">${item.dat}/${item.tong} Mẻ Đạt KPI</span></td>

            </tr>

        `).join('');

    }

}



function renderDashboardTiviRealtime() {

    let meRuaInDay = listGiaoDich.filter(x => x.rua_batchCode).length;

    let meHapInDay = listGiaoDich.filter(x => x.batchCode).length;

    let dangRua = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA").length;

    let dangHap = listGiaoDich.filter(x => x.status === "DANG_HAP").length;

    let sanSangXuat = listGiaoDich.filter(x => x.status === "CHO_XUAT").length;

    

    const today = new Date(); today.setHours(0,0,0,0);

    let quaHan = 0;

    listGiaoDich.forEach(x => {

        if (x.hsd && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT")) {

            if (new Date(x.hsd) < today) quaHan++;

        }

    });



    if (document.getElementById('tv_meRua')) document.getElementById('tv_meRua').innerText = meRuaInDay;

    if (document.getElementById('tv_meHap')) document.getElementById('tv_meHap').innerText = meHapInDay;

    if (document.getElementById('tv_meHap2')) document.getElementById('tv_meHap2').innerText = meHapInDay;

    if (document.getElementById('tv_dangRua')) document.getElementById('tv_dangRua').innerText = dangRua;

    if (document.getElementById('tv_dangHap')) document.getElementById('tv_dangHap').innerText = dangHap;

    if (document.getElementById('tv_khoVoKhuan')) document.getElementById('tv_khoVoKhuan').innerText = sanSangXuat;

    if (document.getElementById('tv_canhBaoHsd')) document.getElementById('tv_canhBaoHsd').innerText = quaHan;

}



function renderAdminInterface() {

    if (document.getElementById("cfg_pinAdmin")) document.getElementById("cfg_pinAdmin").value = thongTinMatKhauAdmin.adminPIN || "";

    if (document.getElementById("cfg_pinCSSD")) document.getElementById("cfg_pinCSSD").value = thongTinMatKhauAdmin.cssdPIN || "";

    if (document.getElementById("cfg_pinGuest")) document.getElementById("cfg_pinGuest").value = thongTinMatKhauAdmin.guestPIN || "";

    

    const MatrixRoles = ["CSSD", "KHOA", "GUEST"];

    

    // Đã loại bỏ hoàn toàn các số thứ tự 1., 2., 3.... khỏi tên tab

    const MatrixTabs = [

        { id: "khoaphong", name: "Cổng Báo Trả Đồ" },

        { id: "thugom", name: "Xe Thu Gom" },

        { id: "mayrua", name: "Quản Lý Mẻ Rửa" },

        { id: "donggoi", name: "Làm Sạch & Đóng Gói" },

        { id: "mayhap", name: "Quản Lý Mẻ Hấp" },

        { id: "khovokhuan", name: "Kho Vô Khuẩn" },

        { id: "quanlykho", name: "Tồn Kho Toàn Viện" },

        { id: "danhmuc", name: "Giám Sát Tuổi Thọ Dụng Cụ" },

        { id: "lichsuluanchuyen", name: "Nhật Ký Luân Chuyển" },

        { id: "tracuu", name: "Truy xuất mẻ hấp" },

        { id: "performance", name: "Hiệu Suất KPI" },

        { id: "dashboard_tv", name: "Màn Hình Tivi" }

    ];

    

    let htmlMatrix = "";

    MatrixTabs.forEach(tab => {

        htmlMatrix += `<tr class="border-b font-medium text-xs hover:bg-slate-50"><td class="p-3 font-bold text-slate-700">${tab.name}</td>`;

        MatrixRoles.forEach(role => {

            let isChecked = (cauHinhGiaoDien[role] && cauHinhGiaoDien[role].includes(tab.id)) ? "checked" : "";

            htmlMatrix += `<td class="p-3 text-center"><input type="checkbox" ${isChecked} onchange="updateGiaoDienMatrix('${role}', '${tab.id}', this)" class="w-4 h-4 rounded text-sky-600 focus:ring-sky-500"></td>`;

        });

        htmlMatrix += `</tr>`;

    });

    const tbodyMatrix = document.getElementById("bodyMaTranGiaoDien"); if (tbodyMatrix) tbodyMatrix.innerHTML = htmlMatrix;

    const trKhoa = document.getElementById("bangPhanQuyenKhoa");

    if(trKhoa) {

        if(danhSachKhoa.length === 0) { trKhoa.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-rose-500 font-bold italic">Danh sách Khoa trống.</td></tr>`; } 

        else { trKhoa.innerHTML = danhSachKhoa.map((k, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-slate-700 text-[11px]">${k.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-rose-600 text-sm bg-rose-50/50">${k.pin || '123'}</td><td class="p-3 text-center"><div class="flex items-center justify-center gap-2"><input type="text" id="pin-khoa-${index}" placeholder="PIN" class="w-24 p-1.5 text-center border border-slate-300 rounded text-xs font-bold"><button onclick="updatePINTrựcTiep(${index}, '${k.ten}')" class="bg-sky-600 text-white font-bold py-1.5 px-3 rounded shadow text-[10px]">ĐỔI PIN</button></div></td></tr>`).join(''); }

    }

    const tbKtv = document.getElementById("bangNhanVienCssd");

    if(tbKtv) {

        if(danhSachKtvCssd.length === 0) { tbKtv.innerHTML = `<tr><td colspan="4" class="p-4 text-center text-slate-400 italic">Chưa có Nhân viên.</td></tr>`; }

        else { tbKtv.innerHTML = danhSachKtvCssd.map((nv, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-sky-700 text-xs">${nv.code}</td><td class="p-3 font-bold text-slate-700 text-[11px]">${nv.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-sky-600 text-sm bg-sky-50/50">${nv.pin}</td><td class="p-3 text-center"><button onclick="xaoKtvCssd('${nv.code}')" class="text-rose-600 font-bold text-[10px]"><i class="fa-solid fa-trash-can mr-1"></i>XÓA</button></td></tr>`).join(''); }

    }

}



// =========================================================================

// 14. XỬ LÝ ĐỌC FILE EXCEL ĐA SHEET

// =========================================================================

document.addEventListener("DOMContentLoaded", () => {

    const fileInput = document.getElementById("excelFileInput");

    if (fileInput) {

        fileInput.addEventListener("change", (e) => {

            const file = e.target.files[0];

            if (!file) return;



            const reader = new FileReader();

            reader.onload = function(e) {

                try {

                    const data = new Uint8Array(e.target.result);

                    const workbook = XLSX.read(data, { type: 'array' });

                    

                    let updateData = {};

                    let logs = [];



                    workbook.SheetNames.forEach(sheetName => {

                        const worksheet = workbook.Sheets[sheetName];

                        const jsonData = XLSX.utils.sheet_to_json(worksheet);

                        if (jsonData.length === 0) return;



                        const firstRow = jsonData[0];

                        const keys = Object.keys(firstRow).map(k => k.trim().toUpperCase());



                        const coCotMaDC = keys.includes("MÃ DC");

                        if (coCotMaDC) {

                            let mapKhoa = {};

                            danhSachKhoa.forEach(k => {

                                mapKhoa[k.ten.trim().toUpperCase()] = {

                                    ten: k.ten.trim().toUpperCase(),

                                    pin: k.pin || "123",

                                    danhSachBo: []

                                };

                            });



                            jsonData.forEach(row => {

                                let rawKeyKhoa = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS (I)");

                                let rawKeyBo = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS CHUẨN");

                                let rawKeyMa = Object.keys(row).find(k => k.trim().toUpperCase() === "MÃ DC");



                                let tenKhoa = rawKeyKhoa ? row[rawKeyKhoa] : null;

                                let tenBo = rawKeyBo ? row[rawKeyBo] : null;

                                let maKhay = rawKeyMa ? row[rawKeyMa] : null;



                                if (tenKhoa && tenBo) {

                                    let keyKhoa = String(tenKhoa).trim().toUpperCase();

                                    let cleanBo = String(tenBo).trim().toUpperCase();

                                    let cleanMa = maKhay ? String(maKhay).trim().toUpperCase() : "CHUA_CO_ID";

                                    let chuoiDinhDangKhay = `${cleanBo} [ID:${cleanMa}]`;



                                    if (!mapKhoa[keyKhoa]) {

                                        mapKhoa[keyKhoa] = { ten: keyKhoa, pin: "123", danhSachBo: [] };

                                    }

                                    if (!mapKhoa[keyKhoa].danhSachBo.includes(chuoiDinhDangKhay)) {

                                        mapKhoa[keyKhoa].danhSachBo.push(chuoiDinhDangKhay);

                                    }

                                }

                            });



                            updateData.danhSachKhoa = Object.values(mapKhoa);

                            logs.push(`Cập nhật định biên cơ số cho ${updateData.danhSachKhoa.length} Khoa/Phòng`);

                        }



                        const coCotMaAesculap = keys.includes("AB 120/12");

                        if (coCotMaAesculap) {

                            updateData.databaseExcel = jsonData;

                            logs.push(`Nạp thành công ${jsonData.length} linh kiện chi tiết mâm Aesculap`);

                        }

                    });



                    if (Object.keys(updateData).length === 0) {

                        return showToast("Không tìm thấy cấu trúc dữ liệu phù hợp (Cột 'MÃ DC' hoặc 'AB 120/12') trong file Excel up lên!", "error");

                    }



                    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update(updateData)

                    .then(() => {

                        playSound('success');

                        showToast(`Đồng bộ thành công: ${logs.join(" | ")}!`, "success");

                        fileInput.value = ""; 

                        callRender();

                    }).catch(err => {

                        console.error("Lỗi cập nhật Firestore:", err);

                        showToast("Thất bại khi đẩy dữ liệu lên Cloud Firestore!", "error");

                    });



                } catch (err) {

                    console.error("Lỗi FileReader:", err);

                    showToast("Định dạng file không được hỗ trợ hoặc bị lỗi!", "error");

                }

            };

            reader.readAsArrayBuffer(file);

        });

    }

});

Bạn cập nhật dùm tôi app.js

Your JavaScript file (app.js) is ready
Biểu tượng JS
app
 JS 
Đang mở

File app.js đã được cập nhật hoàn chỉnh với toàn bộ cấu trúc mã nguồn hệ thống CSSD Bệnh viện Phương Nam, bao gồm:

Khởi tạo & Đồng bộ Realtime: Firebase Firestore (heThongDanhMuc & phieuGiaoNhan).

Phân quyền & Đăng nhập: Các vai trò ADMIN, CSSD, KHOA, GUEST.

Các trạm vận hành: Thu gom & kiểm đếm linh kiện chi tiết, Quản lý máy rửa Belimed WD250, Đóng gói, Vận hành máy hấp & kiểm soát BI/KPI, Kho vô khuẩn (FIFO).

Ký nhận điện tử: Khởi tạo canvas vẽ chữ ký điện tử trực tiếp trên tablet/điện thoại và lưu minh chứng.

In ấn: Cấu hình in tem đơn/mẻ Bixolon SLP-TX403 (80mm x 50mm) và Biên bản giao nhận khổ A4.




Gemini là AI và có thể mắc sai sót.

// =========================================================================
// 1. KHỞI TẠO HỆ THỐNG & CẤU HÌNH BIẾN TOÀN CỤC (PN HOSPITAL)
// =========================================================================
const firebaseConfig = { 
    apiKey: "AIzaSyCxjdCTKHQlpm7SYbWCEws1HhcOaFp0LBA", 
    authDomain: "cssd-system-2878c.firebaseapp.com", 
    projectId: "cssd-system-2878c", 
    storageBucket: "cssd-system-2878c.firebasestorage.app", 
    messagingSenderId: "662377321937", 
    appId: "1:662377321937:web:001c092e10319547623cf0" 
};
firebase.initializeApp(firebaseConfig); 
const db = firebase.firestore();

let thongTinMatKhauAdmin = { adminPIN: "admin2026", cssdPIN: "cssd2026", guestPIN: "guest2026" };
let cauHinhGiaoDien = {
    "ADMIN": ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'],
    "CSSD": ['thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','dashboard_tv'], 
    "KHOA": ['khoaphong','quanlykho','lichsuluanchuyen','tracuu'], 
    "GUEST": ['quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv']
};

let currentRole = "", loginUserCode = "";
let danhSachKhoa = [], listGiaoDich = [], gioHangTam = [], danhSachKtvCssd = [], databaseExcel = [];
let html5QrCode = null; let targetInputIdForScan = ""; let idDangDongGoi = null;
let activeTab = 'thugom'; let renderTimeout = null;
let duLieuAnhBiTamBase64 = ""; let maLoTruyVetToanCuc = ""; 
let gioHangXuatKho = []; let gioKhaySuDungTam = []; 
let currentKiemDemData = null;

// Biến hỗ trợ vẽ chữ ký điện tử
let canvasSignature = null;
let ctxSignature = null;
let isDrawingSignature = false;
let dsIdKhayChoKyNhan = [];

// Cấu hình máy hấp & máy rửa (Belimed WD250)
const cauHinhMayHap = { "Hấp hơi nước": ["A1", "A2", "A3", "A4"], "Hấp H2O2 (Plasma)": ["P1", "P2"], "Khử khuẩn EO": ["EO1", "EO2"] };
const cauHinhMayRua = {
    "Máy rửa khử khuẩn tự động": ["Belimed WD250"],
    "Máy rửa sóng siêu âm": ["US1"],
    "Bồn rửa thủ công": ["MAN1"]
};
const danhMucHoaChatRua = [
    { ten: "Cidezyme (Enzym phân hủy protein)", lieuLuong: "8 ml/L" },
    { ten: "Sekusept Active (Khử khuẩn mức độ cao)", lieuLuong: "10 ml/L" },
    { ten: "Smeg (Nước rửa trung tính chuyên dụng)", lieuLuong: "5 ml/L" },
    { ten: "Aniosyme DD1 (Làm sạch & khử trùng sơ bộ)", lieuLuong: "5 ml/L" }
];

function getTodayDateStr() { 
    const d = new Date(); 
    return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`; 
}

function playSound(type) {
    try {
        const AudioContext = window.AudioContext || window.webkitAudioContext;
        if (!AudioContext) return;
        const ctx = new AudioContext();
        const osc = ctx.createOscillator();
        const gain = ctx.createGain();
        osc.connect(gain); gain.connect(ctx.destination);
        if (type === 'success') {
            osc.type = 'sine'; osc.frequency.setValueAtTime(800, ctx.currentTime);
            gain.gain.setValueAtTime(0.3, ctx.currentTime);
            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.15);
            osc.start(); osc.stop(ctx.currentTime + 0.15);
        } else {
            osc.type = 'sawtooth'; osc.frequency.setValueAtTime(250, ctx.currentTime);
            gain.gain.setValueAtTime(0.3, ctx.currentTime);
            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + 0.4);
            osc.start(); osc.stop(ctx.currentTime + 0.4);
        }
    } catch(e) {}
}

// =========================================================================
// 2. REALTIME LISTENER (FIRESTORE SYNC)
// =========================================================================
db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").onSnapshot(doc => { 
    try {
        if (doc.exists) { 
            let res = doc.data(); 
            danhSachKhoa = res.danhSachKhoa || []; 
            danhSachKtvCssd = res.danhSachKtvCssd || []; 
            databaseExcel = res.databaseExcel || []; 
            if(res.thongTinMatKhauAdmin) thongTinMatKhauAdmin = Object.assign(thongTinMatKhauAdmin, res.thongTinMatKhauAdmin); 
            if(res.cauHinhGiaoDien) cauHinhGiaoDien = Object.assign(cauHinhGiaoDien, res.cauHinhGiaoDien);
        } 
        initSelects();
        if(activeTab === 'khoaphong') loadBoDungCuTheoKhoa(); 
        renderAdminInterface(); 
        if(currentRole) apDungPhanQuyenGiaoDien(currentRole);
        taiDanhMucLinhKienChuan();
        callRender();
    } catch(err) { console.error("Lỗi đồng bộ danh mục:", err); }
});

db.collection("phieuGiaoNhan").orderBy("id", "desc").limit(1000).onSnapshot(snap => { 
    listGiaoDich = []; 
    snap.forEach(doc => { let d = doc.data(); d.firestoreId = doc.id; listGiaoDich.push(d); }); 
    callRender();
});

function callRender() { clearTimeout(renderTimeout); renderTimeout = setTimeout(() => { renderTheoTabHienTai(); }, 100); }

// =========================================================================
// 3. XỬ LÝ ĐĂNG NHẬP & PHÂN QUYỀN HỆ THỐNG
// =========================================================================
function toggleLoginFields() {
    const r = document.getElementById("login_role").value;
    document.getElementById("field_khoa").classList.toggle("hidden", r !== "KHOA");
    document.getElementById("field_nhanvien_cssd").classList.toggle("hidden", r !== "CSSD");
}

function checkLogin() {
    const role = document.getElementById("login_role").value; 
    const pass = document.getElementById("login_pass").value; 
    let pA = thongTinMatKhauAdmin.adminPIN || "admin2026"; 
    let pC = thongTinMatKhauAdmin.cssdPIN || "cssd2026"; 
    let pG = thongTinMatKhauAdmin.guestPIN || "guest2026"; 
     
    if (role === "ADMIN" && pass === pA) { 
        currentRole = "ADMIN"; loginUserCode = "ADMIN"; document.getElementById("nav_user_info").innerText = "ADMINISTRATOR";
        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');
        apDungPhanQuyenGiaoDien("ADMIN");
        switchTab('lichsuluanchuyen');
    } else if (role === "KHOA") { 
        const khoaSelect = document.getElementById("login_khoa").value; 
        let found = danhSachKhoa.find(x => x.ten === khoaSelect); 
        if (pass === (found ? found.pin : "123")) { 
            currentRole = "KHOA"; loginUserCode = khoaSelect; document.getElementById("nav_user_info").innerText = khoaSelect; 
            if (document.getElementById("khoa_selKhoa")) {
                document.getElementById("khoa_selKhoa").value = khoaSelect;
                document.getElementById("khoa_selKhoa").disabled = true;
            }
            document.body.classList.remove('guest-mode');
            apDungPhanQuyenGiaoDien("KHOA");
            switchTab('khoaphong');
        } else { return showToast("Sai mã PIN Khoa!", "error"); } 
    } else if (role === "CSSD") {
        const nvCode = document.getElementById("login_nv_cssd").value;
        if(!nvCode && pass === pC) { currentRole = "CSSD"; loginUserCode = "CSSD_CHUNG"; document.getElementById("nav_user_info").innerText = "CSSD GENERAL"; } 
        else if (nvCode) {
            let nv = danhSachKtvCssd.find(x => x.code === nvCode);
            if (nv && pass === nv.pin) { currentRole = "CSSD"; loginUserCode = nv.ten; document.getElementById("nav_user_info").innerText = `${nvCode} - ${nv.ten}`; }
            else { return showToast("Sai mã PIN của nhân viên này!", "error"); }
        } else { return showToast("Vui lòng chọn Nhân viên hoặc nhập PIN Backup!", "error"); }
        document.getElementById("khoa_selKhoa").disabled = false; document.body.classList.remove('guest-mode');
        apDungPhanQuyenGiaoDien("CSSD");
        let tabsDuocPhep = cauHinhGiaoDien["CSSD"] || [];
        if(tabsDuocPhep.includes('thugom')) switchTab('thugom');
        else if (tabsDuocPhep.length > 0) switchTab(tabsDuocPhep[0]);
    } else if (role === "GUEST") {
        if(pass === pG) {
            currentRole = "GUEST"; loginUserCode = "GUEST"; document.getElementById("nav_user_info").innerText = "KHÁCH THAM QUAN";
            document.body.classList.add('guest-mode'); 
            apDungPhanQuyenGiaoDien("GUEST");
            switchTab('quanlykho');
        } else { return showToast("Sai mã PIN Khách tham quan!", "error"); }
    } else { return showToast("Sai thông tin đăng nhập!", "error"); }
    document.getElementById("login-screen").classList.add("hidden"); document.getElementById("main-app").classList.remove("hidden");
}

function anTatCaHeadersVaMenus() {
    document.getElementById('header-lamsang')?.classList.add('hidden');
    document.getElementById('header-vanhanh')?.classList.add('hidden');
    document.getElementById('header-dulieu')?.classList.add('hidden');
    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => {
        document.getElementById('menu-' + x)?.classList.add('hidden');
    });
}

function apDungPhanQuyenGiaoDien(role) {
    anTatCaHeadersVaMenus();
    if (role === "ADMIN") {
        ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(tab => {
            document.getElementById('menu-' + tab)?.classList.remove('hidden');
        });
        document.getElementById('header-lamsang')?.classList.remove('hidden');
        document.getElementById('header-vanhanh')?.classList.remove('hidden');
        document.getElementById('header-dulieu')?.classList.remove('hidden');
        return;
    }
    let tabsDuocPhep = cauHinhGiaoDien[role] || [];
    tabsDuocPhep.forEach(tab => { document.getElementById('menu-' + tab)?.classList.remove('hidden'); });
    if (tabsDuocPhep.includes('khoaphong')) document.getElementById('header-lamsang')?.classList.remove('hidden');
    if (['thugom','mayrua','donggoi','mayhap','khovokhuan'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-vanhanh')?.classList.remove('hidden');
    if (['quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].some(t => tabsDuocPhep.includes(t))) document.getElementById('header-dulieu')?.classList.remove('hidden');
}

function switchTab(t) { 
    if (currentRole !== "ADMIN") {
        let tabsDuocPhep = cauHinhGiaoDien[currentRole] || [];
        if(!tabsDuocPhep.includes(t)) { return showToast("Tài khoản không có quyền truy cập!", "error"); }
    }
    ['khoaphong','thugom','mayrua','donggoi','mayhap','khovokhuan','quanlykho','danhmuc','lichsuluanchuyen','tracuu','performance','dashboard_tv'].forEach(x => { 
        document.getElementById('tab-'+x)?.classList.add('hidden'); 
        document.getElementById('menu-'+x)?.classList.remove('sidebar-item-active'); 
    }); 
    document.getElementById('tab-'+t)?.classList.remove('hidden'); 
    document.getElementById('menu-'+t)?.classList.add('sidebar-item-active'); 
    activeTab = t; 
    if (t === 'mayrua') { setTimeout(() => { capNhatDanhSachMaMayRua(); }, 50); }
    if (t === 'mayhap') { setTimeout(() => { tuDongTaoMaLoMeHap(); }, 50); }
    callRender(); 
}

// =========================================================================
// 4. TRẠM 1: CỔNG LÂM SÀNG & HÀNH ĐỘNG ĐỘC LẬP (TRẢ ĐỒ BẨN / YÊU CẦU ĐỒ SẠCH)
// =========================================================================
function loadBoDungCuTheoKhoa() { 
    let k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 
    let list = document.getElementById("listBoDungCu"); 
    if(!list) return; list.innerHTML = ""; 
    let f = danhSachKhoa.find(x => (x.ten || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase()); 
    if (f && f.danhSachBo) {
        let htmlOptions = "";
        f.danhSachBo.forEach(x => {
            if (x) {
                let chuoiX = String(x);
                let tenBoThuanTuy = chuoiX.includes(" [ID:") ? chuoiX.split(" [ID:")[0] : chuoiX;
                htmlOptions += `<option value="${chuoiX}"><option value="${tenBoThuanTuy}">`;
            }
        });
        list.innerHTML = htmlOptions; 
    }
}

function themVaoGio() { 
    let val = document.getElementById("khoa_inpMaBo").value.trim().toUpperCase(); if(!val) return; 
    if (gioHangTam.some(x => x.maMacDinh === val)) { document.getElementById("khoa_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách chờ!", "error"); } 
    let tenGoc = val.includes("[ID:") ? val.split(" [ID:")[0] : val; 
    gioHangTam.push({bo: tenGoc, maMacDinh: val, slYeuCau: 1}); 
    document.getElementById("khoa_inpMaBo").value = ""; 
    renderGioHang(); 
}

function renderGioHang() { 
    let khuVuc = document.getElementById("khuVucGioHang"); if(khuVuc) khuVuc.classList.toggle("hidden", gioHangTam.length===0); 
    let bHang = document.getElementById("bangGioHang");
    if(bHang) {
        bHang.innerHTML = gioHangTam.map(i => `<tr><td class="p-2.5 font-bold text-sky-700 text-[11px]">${i.bo}</td></tr>`).join(''); 
    }
    let badge = document.getElementById("badgeGioHang");
    if(badge) badge.innerText = `${gioHangTam.length} món`;
}

function khoaGuiPhieuTraBatches() { 
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 
    if(!k) return showToast("Vui lòng chọn Khoa trước!"); 
    if(gioHangTam.length === 0) return showToast("Vui lòng quét hoặc chọn mâm bẩn muốn trả trước!", "error"); 
     
    let p=[]; 
    gioHangTam.forEach((i, idx) => {
        p.push(
            db.collection("phieuGiaoNhan").add({ 
                id: Date.now() + idx, 
                ngayTao: getTodayDateStr(), 
                time: new Date().toLocaleTimeString('vi-VN'), 
                khoa: k, 
                bo: i.bo, 
                maMacDinh: i.maMacDinh, 
                slYeuCau: 1, 
                slThucTe: 1, 
                status: "CHO_THU" 
            })
        );
    }); 
     
    Promise.all(p).then(() => { 
        clearGioHang(); 
        playSound('success');
        showToast("Đã gửi lệnh báo trả đồ bẩn về CSSD!", "success"); 
        callRender(); 
    }); 
}

function khoaGuiYeuCauCapPhat() {
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;
    if(!k) return showToast("Vui lòng chọn Khoa/Phòng gửi yêu cầu!", "error");
    
    const tenBoChon = document.getElementById("khoa_inpMaBo")?.value.trim().toUpperCase();
    if(!tenBoChon) return showToast("Vui lòng chọn hoặc nhập tên mâm cần cấp mới!", "error");
    
    let tenMâmSạch = tenBoChon.split(" [ID:")[0];
    
    db.collection("phieuGiaoNhan").add({
        id: Date.now(),
        ngayTao: getTodayDateStr(),
        time: new Date().toLocaleTimeString('vi-VN'),
        khoa: k,
        bo: tenMâmSạch,
        maMacDinh: "CHO_CAP_PHAT", 
        slYeuCau: 1,
        status: "CHO_CAP_PHAT", 
        nvYeuCau: loginUserCode || "Điều dưỡng khoa"
    }).then(() => {
        playSound('success');
        showToast(`Đã gửi yêu cầu cấp phát mâm ${tenMâmSạch} thành công!`, "success");
        if(document.getElementById("khoa_inpMaBo")) document.getElementById("khoa_inpMaBo").value = "";
        callRender();
    });
}

// =========================================================================
// 5. TRẠM 2: XE THU GOM & KIỂM ĐẾM CHI TIẾT LINH KIỆN
// =========================================================================
function moPopupKiemDem(id) { 
    let item = listGiaoDich.find(x => x.firestoreId === id); 
    if(!item) return showToast("Không tìm thấy thông tin dòng chỉ định!", "error");

    currentKiemDemData = {
        id: id,
        tenBoDungCu: item.bo ? String(item.bo).split(" [ID:")[0] : "Chưa rõ mâm",
        khoaYeuCau: item.khoa || "Chưa rõ khoa",
        ghiChuLamSang: item.ghiChu || ""
    }; 

    document.getElementById('popBo').innerText = currentKiemDemData.tenBoDungCu;
    document.getElementById('popKhoa').innerText = currentKiemDemData.khoaYeuCau;
    document.getElementById('popGhiChu').value = currentKiemDemData.ghiChuLamSang;
    document.getElementById('popGhiChu').classList.remove('border-rose-500', 'ring-2', 'ring-rose-200');

    let itemsInBo = databaseExcel.filter(x => { 
        let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; 
        return String(boName).trim().toUpperCase() === String(currentKiemDemData.tenBoDungCu).trim().toUpperCase(); 
    });

    let checklistSoBo = [];
    if(itemsInBo.length > 0) {
        checklistSoBo = itemsInBo.map(ct => {
            let tenDc = ct['Tên TS chuẩn'] || ct['Tên Dụng Cụ Chi Tiết'] || ct['Tên dụng cụ chi tiết'] || ct['Tên dụng cụ'] || ct['Chi tiết'] || ct['Dụng cụ'] || ct['Tên Chi Tiết'] || ct['NAME'] || "Dụng cụ";
             
            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {
                tenDc = "Dụng cụ chuẩn mâm";
            }
             
            let sl = parseInt(ct['Số lượng'] || ct['SL'] || ct['Số Lượng'] || 1) || 0;
            return { ten: tenDc, slChuan: sl, slThuc: sl, tinhTrang: "ĐỦ" };
        }).filter(line => line.slChuan > 0); 
    } else {
        checklistSoBo = [{ ten: "Dụng cụ nguyên bộ (Chưa phân rã cấu hình)", slChuan: 1, slThuc: 1, tinhTrang: "ĐỦ" }];
    }

    currentKiemDemData.linhKienKiemDem = checklistSoBo;
    renderChecklistLinhKien();
    document.getElementById('popupKiemDem').classList.remove('hidden'); 
}

function renderChecklistLinhKien() {
    const container = document.getElementById('popKiemDemChecklist');
    if(!container) return; container.innerHTML = ""; 

    currentKiemDemData.linhKienKiemDem.forEach((item, index) => {
        let bgBadge = "bg-emerald-50 text-emerald-700 border-emerald-200";
        if (item.tinhTrang === "THIẾU") bgBadge = "bg-rose-50 text-rose-700 border-rose-200 animate-pulse";
        if (item.tinhTrang === "HỎNG") bgBadge = "bg-amber-50 text-amber-700 border-amber-200";

        const row = document.createElement('div');
        row.className = "flex items-center justify-between py-3 px-2 transition-all hover:bg-slate-50";
        row.innerHTML = `
            <div class="flex-1 pr-2">
                <p class="text-xs font-bold text-slate-700">${item.ten}</p>
                <p class="text-[10px] text-slate-400 font-medium mt-0.5">Cơ số chuẩn: <span class="font-bold text-slate-600">${item.slChuan}</span></p>
            </div>
            <div class="flex items-center gap-3 mr-4">
                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, -1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">-</button>
                <span class="w-6 text-center text-xs font-black text-slate-800">${item.slThuc}</span>
                <button type="button" onclick="thayDoiSoLuongLinhKien(${index}, 1)" class="w-6 h-6 rounded-lg bg-slate-100 hover:bg-slate-200 text-slate-600 font-black flex items-center justify-center text-xs transition-colors">+</button>
            </div>
            <div>
                <select onchange="thayDoiTinhTrangLinhKien(${index}, this.value)" class="text-[11px] font-black border rounded-lg px-2 py-1.5 shadow-sm transition-all ${bgBadge}">
                    <option value="ĐỦ" ${item.tinhTrang === 'ĐỦ' ? 'selected' : ''}>🟢 Đủ đồ</option>
                    <option value="THIẾU" ${item.tinhTrang === 'THIẾU' ? 'selected' : ''}>🔴 Thiếu đồ</option>
                    <option value="HỎNG" ${item.tinhTrang === 'HỎNG' ? 'selected' : ''}>🟡 Hỏng đồ</option>
                </select>
            </div>
        `;
        container.appendChild(row);
    });
}

function thayDoiSoLuongLinhKien(index, thayDoi) {
    const item = currentKiemDemData.linhKienKiemDem[index];
    const targetSl = item.slThuc + thayDoi;
    if (targetSl >= 0 && targetSl <= item.slChuan * 2) {
        item.slThuc = targetSl;
        if (item.slThuc < item.slChuan) { item.tinhTrang = "THIẾU"; } 
        else if (item.slThuc === item.slChuan) { item.tinhTrang = "ĐỦ"; }
        renderChecklistLinhKien();
    }
}

function thayDoiTinhTrangLinhKien(index, value) {
    currentKiemDemData.linhKienKiemDem[index].tinhTrang = value;
    if (value === "THIẾU" && currentKiemDemData.linhKienKiemDem[index].slThuc === currentKiemDemData.linhKienKiemDem[index].slChuan) {
        currentKiemDemData.linhKienKiemDem[index].slThuc = Math.max(0, currentKiemDemData.linhKienKiemDem[index].slChuan - 1);
    } else if (value === "ĐỦ") {
        currentKiemDemData.linhKienKiemDem[index].slThuc = currentKiemDemData.linhKienKiemDem[index].slChuan;
    }
    renderChecklistLinhKien();
}

function closePopupKiemDem() { document.getElementById("popupKiemDem").classList.add("hidden"); currentKiemDemData = null; }

async function saveKiemDem() { 
    if(!currentKiemDemData) return; 
    const ghiChuValue = document.getElementById("popGhiChu").value.trim();
    let coSuCoChenhLech = false;
    currentKiemDemData.linhKienKiemDem.forEach(item => {
        let thuc = Number(item.slThuc) || 0;
        let chuan = Number(item.slChuan) || 0;
        if (item.tinhTrang === "THIẾU" || item.tinhTrang === "HỎNG" || thuc < chuan) { coSuCoChenhLech = true; }
    });

    if (coSuCoChenhLech && ghiChuValue === "") {
        playSound('error');
        alert("⚠️ PHÁT HIỆN SỰ CỐ CHÊNH LỆCH LINH KIỆN!\nAnh/Chị bắt buộc phải nhập lý do chênh lệch hoặc số biên bản sự cố trước khi 'Chốt và Chuyển Rửa'.");
        document.getElementById('popGhiChu').focus();
        document.getElementById('popGhiChu').classList.add('border-rose-500', 'ring-2', 'ring-rose-200');
        return;
    }

    try {
        await db.collection("phieuGiaoNhan").doc(currentKiemDemData.id).update({ 
            status: "DANG_RUA", ghiChu: ghiChuValue,
            ktvThuGom: loginUserCode || "CSSD_CHUNG",
            thoiGianDoiSoat: new Date().toLocaleTimeString('vi-VN'),
            coSuCoChenhLech: coSuCoChenhLech,
            chiTietChecklistLinhKien: currentKiemDemData.linhKienKiemDem
        });
        showToast("Đã đối soát xong! Mâm đồ đã chuyển sang Trạm Rửa.", "success"); 
        closePopupKiemDem(); callRender(); 
    } catch(err) { showToast("Lỗi kết nối Firebase nội bộ!", "error"); }
}

function clearGioHang() { gioHangTam = []; renderGioHang(); }

// =========================================================================
// 6. MODULE: QUẢN LÝ MẺ RỬA KHỬ KHUẨN (BELIMED WD250)
// =========================================================================
function toggleSelectAllRua() { let checked = document.getElementById('selectAllRua').checked; document.querySelectorAll('.rua-checkbox').forEach(cb => cb.checked = checked); }
function toggleSelectAllNghiemThuRua() { let checked = document.getElementById('selectAllNghiemThuRua').checked; document.querySelectorAll('.nghiemthurua-checkbox').forEach(cb => cb.checked = checked); }

function capNhatDanhSachMaMayRua() {
    const loaiRua = document.getElementById("rua_loaiRua")?.value;
    const selectMay = document.getElementById("rua_maySo");
    if (!selectMay || !loaiRua) return;
    selectMay.innerHTML = "";
    if (cauHinhMayRua[loaiRua]) {
        cauHinhMayRua[loaiRua].forEach(may => {
            selectMay.innerHTML += `<option value="${may}">${may}</option>`;
        });
    }
    tuDongTaoMaLoMeRua();
}

function tuDongTaoMaLoMeRua() {
    const maMay = document.getElementById("rua_maySo")?.value || "Belimed WD250";
    const maMayVietTat = (maMay === "Belimed WD250") ? "BL" : maMay;
    const now = new Date();
    const yy = String(now.getFullYear()).slice(-2);
    const mm = String(now.getMonth() + 1).padStart(2, '0');
    const dd = String(now.getDate()).padStart(2, '0');
    const ngayChuoi = `${yy}${mm}${dd}`;

    let cacMeTrongNgay = listGiaoDich.filter(x => x.rua_batchCode && x.rua_batchCode.startsWith(maMayVietTat + ngayChuoi));
    let soMeMax = 0;
    cacMeTrongNgay.forEach(x => {
        let phanDuoi = x.rua_batchCode.split("_")[1];
        if (phanDuoi) {
            let num = parseInt(phanDuoi);
            if (num > soMeMax) soMeMax = num;
        }
    });
    let meTiepTheo = soMeMax + 1;
    const chuoiMe = String(meTiepTheo).padStart(2, '0');
    const batchIdHoanChinh = `${maMayVietTat}${ngayChuoi}_${chuoiMe}`;

    if (document.getElementById("rua_meSo")) document.getElementById("rua_meSo").value = chuoiMe;
    if (document.getElementById("rua_batchId")) document.getElementById("rua_batchId").value = batchIdHoanChinh;
}

function xacNhanMeRua() {
    let checkboxes = document.querySelectorAll('.rua-checkbox:checked');
    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm dụng cụ bẩn để xếp vào máy!", "error");
    
    let loaiRua = document.getElementById("rua_loaiRua").value;
    let maMay = document.getElementById("rua_maySo").value;
    let batchCode = document.getElementById("rua_batchId").value;
    let chuKy = document.getElementById("rua_chuKy").value;
    let hoaChat = document.getElementById("rua_hoaChat").value;
    let lieuLuong = document.getElementById("rua_lieuLuong").value || "Theo chuẩn";
    const ngayHomNay = getTodayDateStr();

    let p = [];
    checkboxes.forEach(cb => {
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "TRONG_BUONG_RUA",
                rua_batchCode: batchCode,
                rua_ngayRua: ngayHomNay,
                rua_mayRua: maMay,
                rua_chuKy: chuKy,
                rua_hoaChat: hoaChat,
                rua_lieuLuong: lieuLuong,
                rua_timeBatDau: new Date().toLocaleTimeString('vi-VN')
            })
        );
    });
    Promise.all(p).then(() => {
        playSound('success');
        showToast(`Bắt đầu chạy mẻ máy rửa: ${batchCode}`, "success");
        tuDongTaoMaLoMeRua();
        callRender();
    });
}

function duyetSachMeRuaHangLoat() {
    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');
    if(checkboxes.length === 0) return showToast("Chọn mâm đã rửa sạch để nghiệm thu!", "error");
    
    let ketQuaTest = document.getElementById("rua_testDoSach").value;
    let p = [];
    checkboxes.forEach(cb => {
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "CHO_DONG_GOI",
                rua_ketQuaTestSach: ketQuaTest,
                rua_timeKetThuc: new Date().toLocaleTimeString('vi-VN')
            })
        );
    });
    Promise.all(p).then(() => {
        playSound('success');
        showToast("Xác nhận nghiệm thu đạt chất lượng làm sạch!", "success");
        callRender();
    });
}

function tuChoiMeRuaHangLoat() {
    let checkboxes = document.querySelectorAll('.nghiemthurua-checkbox:checked');
    if(checkboxes.length === 0) return showToast("Chọn mâm không đạt!", "error");
    
    let p = [];
    checkboxes.forEach(cb => {
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "DANG_RUA",
                rua_batchCode: firebase.firestore.FieldValue.delete(),
                ghiChu: "Làm sạch chưa đạt, yêu cầu rửa lại."
            })
        );
    });
    Promise.all(p).then(() => {
        playSound('error');
        showToast("Đã trả các mâm không đạt về hàng đợi chờ rửa lại!", "success");
        callRender();
    });
}

// =========================================================================
// 7. TRẠM 4: LÀM SẠCH VÀ ĐÓNG GÓI
// =========================================================================
function moPopupDongGoi(id) { idDangDongGoi = id; let item = listGiaoDich.find(x => x.firestoreId === id); if(item) { document.getElementById("popDG_Bo").innerText = item.bo; tinhHanSuDung(); document.getElementById("popupDongGoi").classList.remove("hidden"); } }
function closePopupDongGoi() { document.getElementById("popupDongGoi").classList.add("hidden"); }
function tinhHanSuDung() { let val = document.getElementById("popDG_Loai").value.split("|"); let days = parseInt(val[1]); let dateHSD = new Date(); dateHSD.setDate(dateHSD.getDate() + days); let p = document.getElementById("popDG_Han"); p.innerText = dateHSD.toLocaleDateString('vi-VN'); p.dataset.dateDB = dateHSD.toISOString().split('T')[0]; }
function chotDongGoi() { if(!idDangDongGoi) return; let chatLieuTen = document.getElementById("popDG_Loai").value.split("|")[0]; db.collection("phieuGiaoNhan").doc(idDangDongGoi).update({ status: "CHO_HAP", chatLieu: chatLieuTen, hsd: document.getElementById("popDG_Han").dataset.dateDB }).then(() => { showToast("Đã đóng gói, chuyển chờ hấp!", "success"); closePopupDongGoi(); callRender(); }); }

// =========================================================================
// 8. TRẠM 5: VẬN HÀNH MẺ HẤP & DUYỆT NHẬP KHO VÔ KHUẨN
// =========================================================================
function toggleSelectAllHap() { let checked = document.getElementById('selectAllHap').checked; document.querySelectorAll('.hap-checkbox').forEach(cb => cb.checked = checked); }
function capNhatDanhSachMaMay() { const loaiHap = document.getElementById("hap_loaiHap")?.value; const selectMay = document.getElementById("hap_maySo"); if (!selectMay || !loaiHap) return; selectMay.innerHTML = ""; if (cauHinhMayHap[loaiHap]) { cauHinhMayHap[loaiHap].forEach(may => { selectMay.innerHTML += `<option value="${may}">${may}</option>`; }); } tuDongTaoMaLoMeHap(); }

function tuDongTaoMaLoMeHap() {
    const maMay = document.getElementById("hap_maySo")?.value || "A1"; const now = new Date(); const yy = String(now.getFullYear()).slice(-2); const mm = String(now.getMonth() + 1).padStart(2, '0'); const dd = String(now.getDate()).padStart(2, '0'); const ngayChuoi = `${yy}${mm}${dd}`;
    let cacMeTrongNgay = listGiaoDich.filter(x => x.batchCode && x.batchCode.startsWith(maMay + ngayChuoi)); let soMeMax = 0;
    cacMeTrongNgay.forEach(x => { let phanDuoi = x.batchCode.split("_")[1]; if (phanDuoi) { let num = parseInt(phanDuoi); if (num > soMeMax) soMeMax = num; } });
    let meTiepTheo = soMeMax + 1; if (meTiepTheo > 99) meTiepTheo = 1; const chuoiMe = String(meTiepTheo).padStart(2, '0'); const soLanDone = `${ngayChuoi}_${chuoiMe}`; const batchIdHoanChinh = `${maMay}${soLanDone}`;
    if (document.getElementById("hap_meSo")) document.getElementById("hap_meSo").value = chuoiMe;
    if (document.getElementById("hap_batchId")) document.getElementById("hap_batchId").value = batchIdHoanChinh;
}

function xacNhanMeHap() { 
    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); 
    if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 
    
    let loaiHap = document.getElementById("hap_loaiHap").value; 
    let maMay = document.getElementById("hap_maySo").value; 
    let batchCode = document.getElementById("hap_batchId").value; 
    let chuKyNhiet = document.getElementById("hap_nhietDo").value; 
    let apSuat = document.getElementById("hap_apSuat").value || "N/A"; 
    let coKemBI = document.getElementById("hap_hasBI") ? document.getElementById("hap_hasBI").checked : false;
    const ngayHomNay = getTodayDateStr(); 
    let bayGio = new Date(); 
    let thoiGianDuKienXong = new Date(bayGio.getTime() + 45 * 60 * 1000);

    let p = []; 
    checkboxes.forEach(cb => { 
        if(cb.id === 'selectAllHap') return; 
        
        let thongTinLo = { 
            loaiHap: loaiHap, 
            maMay: maMay, 
            chuKyNhiet: chuKyNhiet, 
            apSuat: apSuat, 
            thoiGianBatDau: bayGio.toLocaleTimeString('vi-VN'), 
            giamSatChatLuong: { 
                chiThiHoaHoc: "ĐẠT", 
                laMeTestSinhHocGoc: coKemBI, 
                ketQuaSinhHoc: coKemBI ? "ĐANG CHỜ MÁY Ủ ĐỌC BI (20 PHÚT)" : "KẾ THỪA ĐẦU NGÀY" 
            } 
        };
        
        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({ 
                status: "DANG_HAP", 
                batchCode: batchCode, 
                ngayHapRealtime: ngayHomNay, 
                hasBI: coKemBI, 
                thoiGianKetThucChuTrinh: thoiGianDuKienXong.toISOString(), 
                thongTinLoHap: thongTinLo 
            })
        ); 
    }); 

    Promise.all(p).then(() => { 
        playSound('success');
        showToast(`Kích hoạt lò thành công! Lô: ${batchCode}`, "success"); 
        if(document.getElementById("hap_hasBI")) document.getElementById("hap_hasBI").checked = false; 
        duLieuAnhBiTamBase64 = ""; 
        const fileInp = document.getElementById("input_anhBI"); 
        if(fileInp) fileInp.value = ""; 
        tuDongTaoMaLoMeHap(); 
        callRender(); 
    }).catch(err => {
        showToast("Lỗi khi cập nhật mẻ hấp: " + err.message, "error");
    }); 
}

function toggleSelectAllNghiemThu() { let checked = document.getElementById('selectAllNghiemThu').checked; document.querySelectorAll('.nghiemthu-checkbox').forEach(cb => cb.checked = checked); }

function kiemTraQuyenDuyetMeHap() {
    const nutDuyetDat = document.querySelector("button[onclick='nhapKhoHangLoat()']"); if (!nutDuyetDat) return;
    let checkboxesNghiemThu = document.querySelectorAll('.nghiemthu-checkbox:checked'); let phaiChoKetQuaBI = false;
    checkboxesNghiemThu.forEach(cb => { if (cb.getAttribute('data-hasbi') === 'true') phaiChoKetQuaBI = true; });

    if (phaiChoKetQuaBI && !duLieuAnhBiTamBase64) {
        nutDuyetDat.disabled = true;
        nutDuyetDat.className = "w-full bg-slate-300 text-slate-500 font-black py-2.5 rounded text-xs shadow-md cursor-not-allowed flex items-center justify-center gap-2";
        nutDuyetDat.innerHTML = `<i class="fa-solid fa-lock text-slate-400"></i> CHỜ MINH CHỨNG BI (MÁY Ủ ĐỌC 20 PHÚT)`;
    } else {
        nutDuyetDat.disabled = false;
        nutDuyetDat.className = "w-full bg-emerald-600 hover:bg-emerald-700 text-white font-black py-2.5 rounded text-xs shadow-md flex items-center justify-center gap-2 transition-all cursor-pointer";
        nutDuyetDat.innerHTML = `<i class="fa-solid fa-check-double mr-1"></i> DUYỆT ĐẠT & CHO VÀO KHO LƯU TRỮ CHUNG`;
    }
}

function nhapKhoHangLoat() { 
    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!"); 
    const bayGio = new Date(); let p = []; 
     
    checkboxes.forEach(cb => { 
        if(cb.id === 'selectAllNghiemThu') return; 
        let itemData = listGiaoDich.find(x => x.firestoreId === cb.value); if(!itemData) return;
        let coKemBI = itemData.hasBI === true;
        let thoiGianHapXong = itemData.thoiGianKetThucChuTrinh ? new Date(itemData.thoiGianKetThucChuTrinh) : new Date(bayGio.getTime() - 45 * 60 * 1000);
        let soPhutThucTe = Math.floor((bayGio - thoiGianHapXong) / (1000 * 60)); 
        let soPhutTinhKPI = coKemBI ? Math.max(0, soPhutThucTe - 20) : soPhutThucTe;
        let trangThaiKPI = (soPhutTinhKPI > 30) ? "KHÔNG ĐẠT" : "ĐẠT";

        let bgGiamSat = itemData.thongTinLoHap?.giamSatChatLuong || {};
        let updateGiamSat = {
            ...bgGiamSat,
            ketQuaSinhHoc: coKemBI ? "ÂM TÍNH (ĐẠT)" : "KẾ THỪA ĐẦU NGÀY",
            minhChungAnhBase64: coKemBI ? duLieuAnhBiTamBase64 : (bgGiamSat.minhChungAnhBase64 || "")
        };

        p.push(
            db.collection("phieuGiaoNhan").doc(cb.value).update({
                status: "CHO_XUAT", thoiGianKetLuan: bayGio.toISOString(), thoiGianDocTestBiPhut: soPhutThucTe, 
                thoiGianTinhKpiPhut: soPhutTinhKPI, ketQuaGiamSatKpi: trangThaiKPI, "thongTinLoHap.giamSatChatLuong": updateGiamSat
            })
        ); 
    }); 
     
    Promise.all(p).then(() => { 
        playSound('success');
        showToast("Đã duyệt mâm đạt chuẩn Vô Khuẩn!", "success"); 
        duLieuAnhBiTamBase64 = ""; const fileInp = document.getElementById("input_anhBI"); if(fileInp) fileInp.value = ""; 
        callRender(); 
    }); 
}

function tuChoiHapHangLoat() { 
    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 
    let p = []; 
    checkboxes.forEach(cb => { if(cb.id !== 'selectAllNghiemThu') p.push(db.collection("phieuGiaoNhan").doc(cb.value).update({status: "DANG_RUA", ghiChu: "Không đạt hấp, trả về rửa lại"})); }); 
    Promise.all(p).then(() => { playSound('error'); showToast("Đã trả các mâm không đạt về Trạm làm sạch!", "success"); callRender(); }); 
}

// =========================================================================
// 9. TRẠM 6: KHO VÔ KHUẨN & PHỐI HỢP CẤP PHÁT ĐIỀU PHỐI XOAY VÒNG (FIFO)
// =========================================================================
function xuatKhoXoayVong(maTrucTiep) { 
    const k = document.getElementById("xuat_selKhoa") ? document.getElementById("xuat_selKhoa").value : ""; 
    let ma = maTrucTiep || (document.getElementById("xuat_inpMaBo") ? document.getElementById("xuat_inpMaBo").value.trim().toUpperCase() : ""); 
    
    if(!k) { playSound('error'); return showToast("Vui lòng Chọn Khoa nhận trước khi xuất!", "error"); } 
    if(!ma) return;
    if(gioHangXuatKho.some(x => x.maMacDinh === ma)) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast("Mã này đã có trong danh sách xuất!", "error"); }
    
    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && x.status === "CHO_XUAT"); 
    if(!khayThucTe) { playSound('error'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; return showToast(`Mã ID ${ma} không sẵn sàng ở Kho Vô Khuẩn.`, "error"); } 
    
    gioHangXuatKho.push(khayThucTe); playSound('success'); if(document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; renderGioHangXuat();
}

function renderGioHangXuat() {
    let listContainer = document.getElementById("danhSachXuatTam");
    if (!listContainer) {
        let bangKho = document.getElementById("bangKhoVoKhuan");
        if (bangKho && bangKho.closest('table')) {
            let wrapper = document.createElement("div"); wrapper.id = "danhSachXuatTam";
            wrapper.className = "mb-4 bg-sky-50 border border-sky-200 rounded p-3 hidden shadow-inner";
            bangKho.closest('table').parentNode.insertBefore(wrapper, bangKho.closest('table')); listContainer = wrapper;
        }
    }
    if(listContainer) {
        if(gioHangXuatKho.length === 0) { listContainer.classList.add("hidden"); } 
        else {
            listContainer.classList.remove("hidden");
            let html = `<div class="flex justify-between items-center mb-2 pb-2 border-b border-sky-200"><h3 class="font-bold text-sky-800 text-[13px]"><i class="fa-solid fa-cart-flatbed mr-2"></i>Đang quét xuất kho: <span class="bg-rose-500 text-white px-2 py-0.5 rounded ml-1">${gioHangXuatKho.length} mâm</span></h3><button onclick="xacNhanXuatKhoHangLoat()" class="bg-emerald-600 text-white px-4 py-1.5 rounded shadow font-black text-xs hover:bg-emerald-700 transition-colors uppercase"><i class="fa-solid fa-check-double mr-1"></i>Chốt Bàn Giao</button></div><div class="flex flex-wrap gap-2">`;
            gioHangXuatKho.forEach((item, index) => { 
                let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "Dụng cụ";
                html += `<div class="bg-white border border-sky-300 px-2 py-1 rounded flex items-center gap-2 shadow-sm animate-pulse"><span class="text-[11px] font-bold text-slate-700">${tenBoGoc}</span><span class="text-[10px] font-mono text-sky-700 bg-sky-100 px-1 rounded font-bold">${item.maMacDinh}</span><i class="fa-solid fa-xmark text-rose-500 cursor-pointer ml-1 text-xs hover:text-rose-700" onclick="xoaKhoiGioXuat(${index})"></i></div>`; 
            });
            html += `</div>`; listContainer.innerHTML = html;
        }
    }
}

function xoaKhoiGioXuat(index) { gioHangXuatKho.splice(index, 1); renderGioHangXuat(); }

function xacNhanXuatKhoHangLoat() {
    const k = document.getElementById("xuat_selKhoa").value; if(!k || gioHangXuatKho.length === 0) return;
    let p = []; 
    gioHangXuatKho.forEach(khay => { 
        p.push(
            db.collection("phieuGiaoNhan").doc(khay.firestoreId).update({ 
                status: "ĐANG_VAN_CHUYEN", khoa: k, ngayHoanTat: getTodayDateStr(), timeHoanTat: new Date().toLocaleTimeString('vi-VN'), nvXuatKho: loginUserCode || "CSSD_CHUNG" 
            })
        ); 
    });
    Promise.all(p).then(() => { 
        playSound('success'); showToast(`Đã xuất kho bàn giao về Khoa ${k}!`, "success"); 
        gioHangXuatKho = []; if (document.getElementById("xuat_inpMaBo")) document.getElementById("xuat_inpMaBo").value = ""; 
        renderGioHangXuat(); callRender(); 
    });
}

function duyetCapPhatTậpTrung(yeuCauId, tenBo, khoaNhan) {
    let khaySẵnCo = listGiaoDich.find(x => x.status === "CHO_XUAT" && x.bo.split(" [ID:")[0].trim().toUpperCase() === tenBo.trim().toUpperCase());
    
    if (!khaySẵnCo) {
        playSound('error');
        return alert(`⚠️ KHÔNG CÓ KHAY SẴN SÀNG TRONG KHO!\nHiện tại trong Kho vô khuẩn CSSD không có khay "${tenBo}" nào sẵn sàng để cấp phát.`);
    }

    if (confirm(`Xác nhận cấp phát khay ${khaySẵnCo.maMacDinh} để đáp ứng yêu cầu của khoa ${khoaNhan}?`)) {
        let bayGio = new Date();
        let batch = db.batch();
        
        let refKhay = db.collection("phieuGiaoNhan").doc(khaySẵnCo.firestoreId);
        batch.update(refKhay, {
            status: "ĐANG_VAN_CHUYEN",
            khoa: khoaNhan,
            ngayHoanTat: getTodayDateStr(),
            timeHoanTat: bayGio.toLocaleTimeString('vi-VN'),
            nvXuatKho: loginUserCode || "CSSD_CHUNG"
        });

        let refYeuCau = db.collection("phieuGiaoNhan").doc(yeuCauId);
        batch.delete(refYeuCau);

        batch.commit().then(() => {
            playSound('success');
            showToast(`Đã cấp phát thành công khay ${khaySẵnCo.maMacDinh} cho khoa ${khoaNhan}!`, "success");
            callRender();
        });
    }
}

// =========================================================================
// XỬ LÝ KÝ CHỮ KÝ ĐIỆN TỬ TRỰC TIẾP TRÊN CANVAS & CẬP NHẬT KHOA
// =========================================================================
function khoiTaoCanvasKyDienTu() {
    canvasSignature = document.getElementById("canvasKyDienTu");
    if (!canvasSignature) return;
    ctxSignature = canvasSignature.getContext("2d");

    const rect = canvasSignature.getBoundingClientRect();
    canvasSignature.width = rect.width || 380;
    canvasSignature.height = rect.height || 160;

    ctxSignature.lineWidth = 2.5;
    ctxSignature.lineCap = "round";
    ctxSignature.strokeStyle = "#0284c7";

    // Chuột
    canvasSignature.onmousedown = (e) => {
        isDrawingSignature = true;
        ctxSignature.beginPath();
        const pos = getCanvasPos(e);
        ctxSignature.moveTo(pos.x, pos.y);
    };
    canvasSignature.onmousemove = (e) => {
        if (!isDrawingSignature) return;
        const pos = getCanvasPos(e);
        ctxSignature.lineTo(pos.x, pos.y);
        ctxSignature.stroke();
    };
    canvasSignature.onmouseup = () => { isDrawingSignature = false; };

    // Cảm ứng Tablet/Phone
    canvasSignature.ontouchstart = (e) => {
        isDrawingSignature = true;
        ctxSignature.beginPath();
        const pos = getCanvasPos(e.touches[0]);
        ctxSignature.moveTo(pos.x, pos.y);
    };
    canvasSignature.ontouchmove = (e) => {
        if (!isDrawingSignature) return;
        e.preventDefault();
        const pos = getCanvasPos(e.touches[0]);
        ctxSignature.lineTo(pos.x, pos.y);
        ctxSignature.stroke();
    };
    canvasSignature.ontouchend = () => { isDrawingSignature = false; };
}

function getCanvasPos(evt) {
    const rect = canvasSignature.getBoundingClientRect();
    return {
        x: evt.clientX - rect.left,
        y: evt.clientY - rect.top
    };
}

function xoaChuKyCanvas() {
    if (ctxSignature && canvasSignature) {
        ctxSignature.clearRect(0, 0, canvasSignature.width, canvasSignature.height);
    }
}

function khoaKyNhanDoSachDienTu() {
    const checkboxes = document.querySelectorAll("#bangChoNhanTaiKhoa input[type='checkbox']:checked");
    dsIdKhayChoKyNhan = [];
    checkboxes.forEach(chk => dsIdKhayChoKyNhan.push(chk.value));

    if (dsIdKhayChoKyNhan.length === 0) {
        playSound('error');
        return showToast("Vui lòng tích chọn ít nhất 1 khay để ký nhận!", "error");
    }

    const inpName = document.getElementById("ky_tenNguoiNhan");
    if (inpName) inpName.value = loginUserCode || "";

    const pop = document.getElementById("popupKyDienTu");
    if (pop) {
        pop.classList.remove("hidden");
        setTimeout(() => {
            khoiTaoCanvasKyDienTu();
            xoaChuKyCanvas();
        }, 100);
    } else {
        const khoaDangNhap = currentRole === "KHOA" ? loginUserCode : (document.getElementById("khoa_selKhoa") ? document.getElementById("khoa_selKhoa").value : "");
        if (confirm(`Xác nhận nhận ${dsIdKhayChoKyNhan.length} mâm về khoa ${khoaDangNhap}?`)) {
            let p = [];
            dsIdKhayChoKyNhan.forEach(idDoc => {
                p.push(
                    db.collection("phieuGiaoNhan").doc(idDoc).update({
                        status: "HOAN_TAT", nguoiKyNhanKhoa: loginUserCode || "Điều dưỡng khoa", timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'), ngayKhoaNhanThucTe: getTodayDateStr()
                    })
                );
            });
            Promise.all(p).then(() => { playSound('success'); showToast(`Đã tiếp nhận vào tủ khoa thành công!`, "success"); callRender(); });
        }
    }
}

function dongPopupKyDienTu() {
    document.getElementById("popupKyDienTu")?.classList.add("hidden");
    dsIdKhayChoKyNhan = [];
}

function luuXacNhanKyNhan() {
    const tenNguoiNhan = document.getElementById("ky_tenNguoiNhan")?.value.trim();
    if (!tenNguoiNhan) {
        playSound('error');
        return showToast("Vui lòng nhập tên người nhận!", "error");
    }

    const chuoiAnhChuKy = canvasSignature ? canvasSignature.toDataURL("image/png") : "";
    const currentKhoa = currentRole === "KHOA" ? loginUserCode : (document.getElementById("khoa_selKhoa")?.value || "KHOA_LAM_SANG");
    const thoiGianKy = new Date().toLocaleString('vi-VN');

    // 1. Lưu chữ ký gần nhất vào localStorage theo Khoa
    const dataSignature = {
        tenNguoiNhan: tenNguoiNhan,
        chuKyImg: chuoiAnhChuKy,
        timeKy: thoiGianKy
    };
    localStorage.setItem(`signature_${currentKhoa}`, JSON.stringify(dataSignature));

    // 2. Cập nhật trạng thái từng mâm vào Cloud Firestore
    let p = [];
    dsIdKhayChoKyNhan.forEach(idDoc => {
        p.push(
            db.collection("phieuGiaoNhan").doc(idDoc).update({
                status: "HOAN_TAT",
                nguoiKyNhanKhoa: tenNguoiNhan,
                anhChuKyNhanBase64: chuoiAnhChuKy,
                timeKhoaNhanThucTe: new Date().toLocaleTimeString('vi-VN'),
                ngayKhoaNhanThucTe: getTodayDateStr()
            })
        );
    });

    Promise.all(p).then(() => {
        playSound('success');
        showToast(`Đã ký nhận thành công ${dsIdKhayChoKyNhan.length} mâm vào tủ khoa!`, "success");
        dongPopupKyDienTu();
        callRender();
    });
}

// =========================================================================
// 10. GHI NHẬN SỬ DỤNG BỘ DỤNG CỤ TRÊN BỆNH NHÂN
// =========================================================================
function moPopupSuDungBoDungCu() {
    gioKhaySuDungTam = []; document.getElementById("sd_bangKhayChon").innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng quét hoặc nhập mã khay...</td></tr>`;
    document.getElementById("sd_ngaySuDung").value = getTodayDateStr(); document.getElementById("sd_nhanChung").value = loginUserCode || "ADMIN";
    if(document.getElementById("sd_khoaBenhNhan")) document.getElementById("sd_khoaBenhNhan").value = (currentRole === "KHOA") ? loginUserCode : "";
    if(document.getElementById("sd_yTaPhongMo")) document.getElementById("sd_yTaPhongMo").value = "";
    if(document.getElementById("sd_yTaVongNgoai")) document.getElementById("sd_yTaVongNgoai").value = "";
    if(document.getElementById("sd_searchBN")) document.getElementById("sd_searchBN").value = "";
    if(document.getElementById("sd_ghiChu")) document.getElementById("sd_ghiChu").value = "";
    if(document.getElementById("sd_maKhayInp")) document.getElementById("sd_maKhayInp").value = "";
    document.getElementById("popupSuDungBoDungCu").classList.remove("hidden");
}

function closePopupSuDung() { document.getElementById("popupSuDungBoDungCu").classList.add("hidden"); }

function scanKhayVaoSuDung() {
    let ma = document.getElementById("sd_maKhayInp").value.trim().toUpperCase(); if(!ma) return;
    document.getElementById("sd_maKhayInp").value = "";
    if(gioKhaySuDungTam.some(x => x.maMacDinh === ma)) { playSound('error'); return showToast("Mã khay này đã được quét!", "error"); }

    let khayThucTe = listGiaoDich.find(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT"));
    if(!khayThucTe) { playSound('error'); return showToast(`Mã ID ${ma} không hợp lệ hoặc chưa vô khuẩn!`, "error"); }

    gioKhaySuDungTam.push(khayThucTe); playSound('success'); renderBangKhaySuDung();
}

function renderBangKhaySuDung() {
    const tbody = document.getElementById("sd_bangKhayChon"); if(!tbody) return;
    if(gioKhaySuDungTam.length === 0) { tbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Chưa chọn khay...</td></tr>`; return; }
    tbody.innerHTML = gioKhaySuDungTam.map((item, index) => {
        let tenBoGoc = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A";
        return `<tr class="border-b font-medium hover:bg-slate-50 text-xs">
            <td class="p-2 text-center"><i class="fa-solid fa-square-check text-teal-600 text-sm"></i></td>
            <td class="p-2 font-mono font-bold text-sky-700">${item.maMacDinh}</td>
            <td class="p-2 font-bold text-slate-800">${tenBoGoc}</td>
            <td class="p-2 text-center font-mono font-bold text-rose-600 bg-rose-50/30">${item.batchCode || 'N/A'}</td>
            <td class="p-2 text-center"><span class="px-2 py-0.5 rounded text-[10px] font-bold bg-teal-100 text-teal-800">${item.status}</span></td>
            <td class="p-2 text-center"><i class="fa-solid fa-trash-can text-rose-500 cursor-pointer hover:text-rose-700" onclick="xoaKhayKhoiListSuDung(${index})"></i></td>
        </tr>`;
    }).join('');
}

function xoaKhayKhoiListSuDung(index) {
    gioKhaySuDungTam.splice(index, 1);
    renderBangKhaySuDung();
}

// =========================================================================
// 11. THIẾT LẬP MÁY IN BIXOLON SLP-TX403 (80mm x 50mm) & BIÊN BẢN HÀNG LOẠT
// =========================================================================

// In tem đơn Bixolon 80mm x 50mm
function inTemBixolonChuan(itemData) {
    const pZone = document.getElementById("print-zone");
    if (!pZone) return;

    const tenBo = itemData.bo ? itemData.bo.split(" [ID:")[0] : "DỤNG CỤ VÔ KHUẨN";
    const maID = itemData.maMacDinh || itemData.idKhay || "000000";
    const ngayNSX = itemData.ngayHapRealtime || itemData.ngayTao || getTodayDateStr();
    const ngayHSD = itemData.hsd ? new Date(itemData.hsd).toLocaleDateString('vi-VN') : "Theo quy định";
    const maBatch = itemData.batchCode || itemData.batchId || "N/A";
    const tenKhoa = itemData.khoa || "TẬP TRUNG";

    pZone.innerHTML = `
        <div class="bixolon-label" style="font-family: Arial, sans-serif;">
            <div style="text-align: center; border-bottom: 1.5px solid #000; padding-bottom: 2px;">
                <div style="font-size: 10px; font-weight: bold; text-transform: uppercase;">PHUONG NAM HOSPITAL - CSSD</div>
                <div style="font-size: 12px; font-weight: 900; margin-top: 1px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${tenBo}</div>
            </div>

            <div style="text-align: center; margin: 3px 0; display: flex; justify-content: center;">
                <svg id="bixolon-single-barcode"></svg>
            </div>

            <div style="font-size: 9px; border-top: 1px dashed #000; padding-top: 2px;">
                <div style="display: flex; justify-content: space-between; font-weight: bold;">
                    <span>LÔ: ${maBatch}</span>
                    <span>KHOA: ${tenKhoa}</span>
                </div>
                <div style="display: flex; justify-content: space-between; margin-top: 1px;">
                    <span>NSX: ${ngayNSX}</span>
                    <span style="font-size: 10px; font-weight: 900;">HSD: ${ngayHSD}</span>
                </div>
            </div>
        </div>
    `;

    JsBarcode("#bixolon-single-barcode", maID, {
        format: "CODE128",
        width: 1.6,
        height: 36,
        displayValue: true,
        fontSize: 10,
        fontOptions: "bold",
        margin: 0
    });

    document.body.classList.remove("print-mode-doc");
    document.body.classList.add("print-mode-bixolon");
    pZone.classList.remove("hidden");

    setTimeout(() => {
        window.print();
        pZone.classList.add("hidden");
        document.body.classList.remove("print-mode-bixolon");
    }, 250);
}

function inTemTongHangLoat() { 
    let checkboxes = document.querySelectorAll('.hap-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn ít nhất 1 mâm!", "error"); 
    let batchCode = document.getElementById("hap_batchId")?.value || "A1000000_01"; 
    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 

    checkboxes.forEach((cb) => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { 
            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 
            container.innerHTML += `<div class="bixolon-label" style="font-family: Arial; font-size: 11px; color: #000; background: #fff;"><div style="text-align: center; font-weight: bold; font-size: 12px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-lo-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span></div><div style="display: flex; justify-content: space-between; border-top: 1px dashed #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 9px; font-family: monospace; font-weight: bold; margin-top: 1px;">Lô: ${batchCode}</div></div>`; 
        } 
    }); 

    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); 
    document.body.classList.remove("print-mode-doc");
    document.body.classList.add("print-mode-bixolon");
    pZone.classList.remove("hidden"); 

    checkboxes.forEach(cb => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-lo-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 
    }); 

    setTimeout(() => { 
        window.print(); 
        pZone.classList.add("hidden"); 
        document.body.classList.remove("print-mode-bixolon");
    }, 300); 
}

function inTemNghiemThuHangLoat() { 
    let checkboxes = document.querySelectorAll('.nghiemthu-checkbox:checked'); if(checkboxes.length === 0) return showToast("Chọn mâm dụng cụ!", "error"); 
    let container = document.createElement('div'); container.className = "print-label-container"; container.style.width = "100%"; 

    checkboxes.forEach((cb) => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { 
            let cleanBo = item.bo ? String(item.bo).split(" [ID:")[0] : "N/A"; let dateHapStr = new Date().toLocaleDateString('vi-VN').replace(/\//g, '-'); let dateHsdStr = item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN').replace(/\//g, '-') : dateHapStr; 
            container.innerHTML += `<div class="bixolon-label" style="font-family: Arial; font-size: 11px; color: #000; background: #fff;"><div style="text-align: center; font-size: 9px; font-weight: bold; letter-spacing: 0.5px;">PN HOSPITAL - CSSD</div><div style="text-align: center; font-weight: bold; font-size: 12px; margin: 1px 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">${cleanBo}</div><div style="text-align: center; margin: 2px 0;"><svg id="barcode-nt-${item.firestoreId}"></svg></div><div style="display: flex; justify-content: space-between; font-weight: bold; font-size: 10px;"><span>SL: ${item.slThucTe || 1}</span><span style="color: green;">ĐẠT VÔ KHUẨN</span></div><div style="display: flex; justify-content: space-between; border-top: 1px solid #000; padding-top: 3px; font-size: 10px; margin-top: 2px;"><span>NSX: ${dateHapStr}</span><strong>HSD: ${dateHsdStr}</strong></div><div style="text-align: center; font-size: 8px; font-weight: bold; font-family: monospace; margin-top: 1px;">BATCH: ${item.batchCode || 'N/A'}</div></div>`; 
        } 
    }); 

    const pZone = document.getElementById("print-zone"); pZone.innerHTML = ""; pZone.appendChild(container); 
    document.body.classList.remove("print-mode-doc");
    document.body.classList.add("print-mode-bixolon");
    pZone.classList.remove("hidden"); 

    checkboxes.forEach(cb => { 
        let item = listGiaoDich.find(x => x.firestoreId === cb.value); 
        if(item) { let cleanId = item.maMacDinh ? item.maMacDinh.replace(/[^a-zA-Z0-9]/g, "") : "0000"; JsBarcode(`#barcode-nt-${item.firestoreId}`, cleanId, { format: "CODE128", width: 1.4, height: 32, displayValue: true, fontSize: 10, margin: 2 }); } 
    }); 

    setTimeout(() => { 
        window.print(); 
        pZone.classList.add("hidden"); 
        document.body.classList.remove("print-mode-bixolon");
    }, 300); 
}

// In Biên bản giao nhận khổ A4 máy in văn phòng (Có hỗ trợ chữ ký điện tử)
function inHoaDonGiaoNhan() {
    const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value; 
    if (!k) return showToast("Chọn Khoa trước!", "error");
    
    // Tìm chữ ký điện tử mới nhất từ Firestore hoặc localStorage
    const itemKyGầnNhat = listGiaoDich.find(x => x.khoa === k && x.anhChuKyNhanBase64);
    const savedSignature = JSON.parse(localStorage.getItem(`signature_${k}`) || "null");

    let htmlChuKyDieuDuong = `<p style="font-weight:normal; font-style:italic; color:#64748b; margin-top:60px;">(Ký và ghi rõ họ tên)</p>`;
    
    if (itemKyGầnNhat && itemKyGầnNhat.anhChuKyNhanBase64) {
        htmlChuKyDieuDuong = `
            <div style="text-align:center; margin-top:10px;">
                <img src="${itemKyGầnNhat.anhChuKyNhanBase64}" style="max-height:75px; margin:0 auto; display:block;" alt="Chữ ký"/>
                <p style="font-weight:bold; margin-top:5px; text-transform:uppercase; color:#0f172a;">${itemKyGầnNhat.nguoiKyNhanKhoa}</p>
                <p style="font-size:10px; color:#64748b; font-weight:normal;">Xác nhận: ${itemKyGầnNhat.ngayKhoaNhanThucTe || ''} ${itemKyGầnNhat.timeKhoaNhanThucTe || ''}</p>
            </div>
        `;
    } else if (savedSignature && savedSignature.chuKyImg) {
        htmlChuKyDieuDuong = `
            <div style="text-align:center; margin-top:10px;">
                <img src="${savedSignature.chuKyImg}" style="max-height:75px; margin:0 auto; display:block;" alt="Chữ ký"/>
                <p style="font-weight:bold; margin-top:5px; text-transform:uppercase; color:#0f172a;">${savedSignature.tenNguoiNhan}</p>
                <p style="font-size:10px; color:#64748b; font-weight:normal;">Xác nhận: ${savedSignature.timeKy}</p>
            </div>
        `;
    }

    let htmlBang = `
        <table style="width:100%; border-collapse:collapse; margin-top:20px; font-size:13px; text-align:left; font-family:Arial, sans-serif;">
            <thead>
                <tr style="background-color:#f1f5f9;">
                    <th style="padding:10px; border:1px solid #334155; width:35%; text-align:left; color:#0f172a;">PHÂN LOẠI MÂM / LOẠI DỤNG CỤ</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ TRẢ BẨN</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">ĐÃ NHẬN SẠCH</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:15%; color:#0f172a;">NHẬN VÔ KHUẨN</th>
                    <th style="padding:10px; border:1px solid #334155; text-align:center; width:20%; color:#0f172a;">CSSD NỢ KHOA</th>
                </tr>
            </thead>
            <tbody>`;

    const rows = document.querySelectorAll('#bangDonGiaoNhan tr');
    if (rows.length === 0 || (rows.length === 1 && rows[0].cells.length === 1)) { 
        htmlBang += `<tr><td colspan="5" style="padding:15px; border:1px solid #334155; text-align:center; font-style:italic;">Không có dữ liệu công nợ.</td></tr>`; 
    } else {
        rows.forEach(row => {
            const cells = row.cells;
            if (cells.length >= 5) { 
                htmlBang += `
                    <tr>
                        <td style="padding:10px; border:1px solid #334155; font-weight:bold; color:#0f172a;">${cells[0].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#e11d48;">${cells[1].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#0284c7;">${cells[2].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#7c3aed;">${cells[3].innerText}</td>
                        <td style="padding:10px; border:1px solid #334155; text-align:center; font-weight:bold; color:#d97706;">${cells[4].innerText}</td>
                    </tr>`; 
            }
        });
    }
    htmlBang += `</tbody></table>`;

    const pZone = document.getElementById("print-zone");
    pZone.innerHTML = `
        <div style="width:100%; font-family:Arial, sans-serif; padding:10px; color:#000;">
            <div style="text-align:center; margin-bottom:25px;">
                <h2 style="margin:0; font-size:18px; font-weight:900; text-transform:uppercase;">BIÊN BẢN GIAO NHẬN DỤNG CỤ CSSD</h2>
                <p style="margin:8px 0 0 0; font-size:13px; font-weight:bold;">Khoa/Phòng: <span style="text-transform:uppercase; color:#0284c7;">${k}</span> - Ngày xuất: ${new Date().toLocaleDateString('vi-VN')}</p>
            </div>
            ${htmlBang}
            <div style="margin-top:40px; display:flex; justify-content:space-between; font-size:12px; font-weight:bold; padding:0 30px;">
                <div style="text-align:center; width:45%;">
                    <p style="margin-bottom:10px; text-transform:uppercase;">ĐIỀU DƯỠNG LÂM SÀNG</p>
                    ${htmlChuKyDieuDuong}
                </div>
                <div style="text-align:center; width:45%;">
                    <p style="margin-bottom:10px; text-transform:uppercase;">CHUYÊN VIÊN CSSD</p>
                    <p style="font-weight:normal; font-style:italic; color:#64748b; margin-top:60px;">(Ký và ghi rõ họ tên)</p>
                </div>
            </div>
        </div>`;

    document.body.classList.remove("print-mode-bixolon");
    document.body.classList.add("print-mode-doc");
    pZone.classList.remove("hidden");

    setTimeout(() => { 
        window.print(); 
        pZone.classList.add("hidden"); 
        document.body.classList.remove("print-mode-doc");
    }, 200);
}

// =========================================================================
// 12. HÀM PHỤ TRỢ: CHỨC NĂNG QUẢN TRỊ ADMIN & TIỆN ÍCH
// =========================================================================
function taiDanhMucLinhKienChuan() {
    const tbody = document.getElementById("bangDanhMucLinhKien"); if (!tbody) return; tbody.innerHTML = "";
    if (!databaseExcel || databaseExcel.length === 0) { tbody.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-slate-400 italic">Chưa có dữ liệu. Vui lòng nạp file Excel.</td></tr>`; return; }
    let gopBoExcel = {};
    databaseExcel.forEach(x => {
        let tenBo = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || x['NAME'] || x['name']; if (!tenBo) return;
        tenBo = String(tenBo).trim().toUpperCase(); if (!gopBoExcel[tenBo]) gopBoExcel[tenBo] = []; gopBoExcel[tenBo].push(x);
    });
    for (let tenBo in gopBoExcel) {
        let linhKienHtml = `<div class="flex flex-wrap gap-1">`; let tongSl = 0;
        gopBoExcel[tenBo].forEach(item => {
            let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || item['Dụng cụ'] || item['Tên Chi Tiết'] || "Dụng cụ Chi Tiết";
            
            if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {
                tenDc = "Dụng cụ chuẩn mâm";
            }
            
            let sl = parseInt(item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1) || 0; let hanHap = item['Tuổi thọ mẻ hấp'] || item['Tuổi thọ'] || 100;
            tongSl += sl; linhKienHtml += `<span class="bg-slate-100 text-slate-700 px-1.5 py-0.5 rounded text-[10px] font-medium border border-slate-200">${tenDc} (${sl}) <span class="text-amber-600 font-bold">[Max: ${hanHap}]</span></span>`;
        });
        linhKienHtml += `</div>`; const tr = document.createElement("tr"); tr.className = "hover:bg-slate-50 transition-colors border-b";
        tr.innerHTML = `<td class="p-3 font-bold text-slate-800 text-xs">${tenBo}</td><td class="p-3">${linhKienHtml}</td><td class="p-3 text-center font-black text-sky-700 bg-sky-50/30">${tongSl}</td>`;
        tbody.appendChild(tr);
    }
}

function updateGiaoDienMatrix(role, tabId, checkboxElement) {
    if(!cauHinhGiaoDien[role]) cauHinhGiaoDien[role] = [];
    if(checkboxElement.checked) { if(!cauHinhGiaoDien[role].includes(tabId)) cauHinhGiaoDien[role].push(tabId); } 
    else { cauHinhGiaoDien[role] = cauHinhGiaoDien[role].filter(x => x !== tabId); }
    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ cauHinhGiaoDien: cauHinhGiaoDien }).then(() => showToast("Đã cập nhật quyền truy cập!", "success"));
}

function handleViewAnhBiMoi(firestoreId) { hanhDongXemAnhBiMoi(firestoreId); }
function hanhDongXemAnhBiMoi(firestoreId) {
    let item = listGiaoDich.find(x => x.firestoreId === firestoreId); if (!item) return showToast("Không tìm thấy thông tin!", "error");
    let loGoc = listGiaoDich.find(m => m.batchCode === item.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 
    let chuoiAnhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || item.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;
    if (!chuoiAnhBase64) return showToast("Mẻ này chưa tải ảnh minh chứng hoặc kế thừa.", "error");
    let w = window.open(); w.document.write(`<html><head><title>MINH CHỨNG BI - LÔ ${item.batchCode}</title></head><body style='margin:0; background:#000; display:flex; justify-content:center; align-items:center;'><img src='${chuoiAnhBase64}' style='max-width:100%; max-height:100vh; object-fit:contain;'/></body></html>`);
}

function switchAdminSubtab(sub) { 
    document.getElementById('subtab-database')?.classList.add('hidden'); document.getElementById('subtab-security')?.classList.add('hidden'); 
    document.getElementById('subbtn-database')?.classList.replace('admin-subtab-active', 'text-slate-600'); document.getElementById('subbtn-security')?.classList.replace('admin-subtab-active', 'text-slate-600'); 
    document.getElementById('subtab-' + sub)?.classList.remove('hidden'); document.getElementById('subbtn-' + sub)?.classList.add('admin-subtab-active'); 
}

function saveAdminPIN(type) { let newVal = document.getElementById(`cfg_pin${type.charAt(0).toUpperCase() + type.slice(1).toLowerCase()}`).value.trim(); if(type === 'ADMIN') thongTinMatKhauAdmin.adminPIN = newVal; if(type === 'CSSD') thongTinMatKhauAdmin.cssdPIN = newVal; if(type === 'GUEST') thongTinMatKhauAdmin.guestPIN = newVal; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ thongTinMatKhauAdmin: thongTinMatKhauAdmin }).then(() => showToast("Đã lưu PIN!", "success")); }
function themKtvCssd() { let code = prompt("Mã NV:"); let ten = prompt("Tên:"); let pin = prompt("PIN:"); if(code && ten && pin) { danhSachKtvCssd.push({ code: code.toUpperCase(), ten: ten, pin: pin }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }
function xaoKtvCssd(code) { if(confirm("Xóa?")) { danhSachKtvCssd = danhSachKtvCssd.filter(x => x.code !== code); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKtvCssd: danhSachKtvCssd }); } }
function themKhoaThuCong() { let t = prompt("Nhập Tên Khoa/Phòng:"); if(t) { danhSachKhoa.push({ ten: t.toUpperCase(), pin: "123", danhSachBo: [] }); db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Thêm Khoa thành công!", "success")); } }

function khaiSinhKhayVangLai() {
    if(danhSachKhoa.length === 0) return showToast("Hệ thống chưa có Khoa nào!", "error");
    let tenKhay = prompt("Nhập Tên Mâm/Khay dụng cụ:"); if(!tenKhay) return;
    let maId = prompt("Nhập Mã ID khay:"); if(!maId) return; maId = maId.toUpperCase().trim();
    let dsTenKhoa = danhSachKhoa.map((k, idx) => `${idx + 1}. ${k.ten}`).join("\n");
    let luaChon = prompt(`Chọn số thứ tự Khoa sở hữu:\n${dsTenKhoa}`); if(!luaChon) return;
    let idxKhoa = parseInt(luaChon) - 1; if(idxKhoa < 0 || idxKhoa >= danhSachKhoa.length) return showToast("Lựa chọn không hợp lệ!", "error");
    let khoaChon = danhSachKhoa[idxKhoa]; let chuoiKhayDinhDang = `${tenKhay.toUpperCase().trim()} [ID:${maId}]`;
    if(!khoaChon.danhSachBo) khoaChon.danhSachBo = []; if(khoaChon.danhSachBo.includes(chuoiKhayDinhDang)) return showToast("Khay đã tồn tại!", "error");
    khoaChon.danhSachBo.push(chuoiKhayDinhDang);
    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => { showToast(`Khai sinh khay ${maId} thành công!`, "success"); callRender(); });
}

function updatePINTrựcTiep(idx, tenKhoa) { let p = document.getElementById(`pin-khoa-${idx}`).value.trim(); if(p) { danhSachKhoa.find(x => x.ten === tenKhoa).pin = p; db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update({ danhSachKhoa: danhSachKhoa }).then(() => showToast("Đổi PIN thành công", "success")); } }

function initSelects() { 
    let opts = '<option value="">-- Chọn Khoa --</option>' + danhSachKhoa.map(k=>`<option value="${k.ten}">${k.ten}</option>`).join('');
    if(document.getElementById("login_khoa")) document.getElementById("login_khoa").innerHTML = opts; 
    if(document.getElementById("khoa_selKhoa")) document.getElementById("khoa_selKhoa").innerHTML = opts; 
    if(document.getElementById("login_nv_cssd")) document.getElementById("login_nv_cssd").innerHTML = '<option value="">-- Chọn KTV CSSD --</option>' + danhSachKtvCssd.map(k=>`<option value="${k.code}">${k.code} - ${k.ten}</option>`).join(''); 
     
    if (document.getElementById("rua_hoaChat")) {
        document.getElementById("rua_hoaChat").innerHTML = danhMucHoaChatRua.map(hc => `<option value="${hc.ten}">${hc.ten}</option>`).join('');
        document.getElementById("rua_lieuLuong").value = danhMucHoaChatRua[0].lieuLuong;
        document.getElementById("rua_hoaChat").addEventListener("change", (e) => {
            let selectedHc = danhMucHoaChatRua.find(hc => hc.ten === e.target.value);
            if(selectedHc) document.getElementById("rua_lieuLuong").value = selectedHc.lieuLuong;
        });
    }
    if (document.getElementById("rua_loaiRua")) { capNhatDanhSachMaMayRua(); }
    if (document.getElementById("hap_loaiHap")) { capNhatDanhSachMaMay(); }
     
    const selKhoaTraba = document.getElementById("khoa_selKhoa"); if(selKhoaTraba) { selKhoaTraba.removeEventListener("change", loadBoDungCuTheoKhoa); selKhoaTraba.addEventListener("change", loadBoDungCuTheoKhoa); }
}

function showToast(msg, type="error") { const t = document.createElement('div'); t.className = `fixed top-6 right-6 ${type==="error"?"bg-rose-600":"bg-emerald-600"} text-white px-5 py-3.5 rounded-lg shadow-2xl z-[100] font-bold text-sm`; t.innerHTML = msg; document.body.appendChild(t); setTimeout(() => t.remove(), 2500); }
function toggleMobileMenu() { document.getElementById("sidebar_menu").classList.toggle("-translate-x-full"); document.getElementById("mobile-overlay").classList.toggle("hidden"); }

function moCamera(inputId) { targetInputIdForScan = inputId; document.getElementById("popupScanner").classList.remove("hidden"); html5QrCode = new Html5Qrcode("reader"); Html5Qrcode.getCameras().then(devices => { let cid = devices.length > 1 ? devices[devices.length - 1].id : devices[0].id; html5QrCode.start(cid, { fps: 15, qrbox: { width: 260, height: 180 } }, (txt) => { document.getElementById(targetInputIdForScan).value = txt.trim().toUpperCase(); if(targetInputIdForScan==='khoa_inpMaBo') themVaoGio(); if(targetInputIdForScan==='xuat_inpMaBo') xuatKhoXoayVong(); if(targetInputIdForScan==='sd_maKhayInp') scanKhayVaoSuDung(); }).catch(e=>{}) }); }
function dongCamera() { if(html5QrCode) html5QrCode.stop().then(() => html5QrCode.clear()); document.getElementById("popupScanner").classList.add("hidden"); }
function xoaSachDuLieuGiaoDichRealtime() { if(prompt("Nhập PIN ADMIN:") === (thongTinMatKhauAdmin.adminPIN||"admin2026")) { db.collection("phieuGiaoNhan").get().then(snap => { let b = db.batch(); snap.forEach(d => b.delete(d.ref)); b.commit().then(() => location.reload()); }); } }

function docAnhBiUpTaiCho(inputElement) { 
    const file = inputElement.files[0]; if (!file) return; 
    const reader = new FileReader(); reader.onloadend = function() { duLieuAnhBiTamBase64 = reader.result; showToast("Tải ảnh minh chứng BI thành công!", "success"); kiemTraQuyenDuyetMeHap(); }; 
    reader.readAsDataURL(file); 
}

// =========================================================================
// 13. HÀM CORE RENDER DỮ LIỆU ĐA TAB LUỒNG HỆ THỐNG
// =========================================================================
function renderTheoTabHienTai() {
    if(activeTab === 'khoaphong') {
        const k = currentRole === "KHOA" ? loginUserCode : document.getElementById("khoa_selKhoa").value;
        let tatCaDonCuaKhoa = listGiaoDich.filter(x => (x.khoa || "").toString().trim().toUpperCase() === (k || "").toString().trim().toUpperCase());
        
        // 1. TÍNH TOÁN CÔNG NỢ LUÂN CHUYỂN THỰC TẾ
        let gopCongNo = {};
        tatCaDonCuaKhoa.forEach(x => {
            if (x.status === "CHO_CAP_PHAT") return;

            let tenLoaiMâm = x.bo ? String(x.bo).split(" [ID:")[0] : "Chưa rõ mâm";
            if (!gopCongNo[tenLoaiMâm]) gopCongNo[tenLoaiMâm] = { daTraBan: 0, nhanSach: 0, nhanVoKhuan: 0, cssdNo: 0 };
            
            if (x.status !== "CHO_THU") gopCongNo[tenLoaiMâm].daTraBan += 1; 
            if (["DANG_RUA", "TRONG_BUONG_RUA", "CHO_DONG_GOI", "CHO_HAP", "DANG_HAP", "CHO_XUAT", "ĐANG_VAN_CHUYEN", "HOAN_TAT", "DA_SU_DUNG"].includes(x.status)) gopCongNo[tenLoaiMâm].nhanSach += 1;
            if (x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG") gopCongNo[tenLoaiMâm].nhanVoKhuan += 1; 
            gopCongNo[tenLoaiMâm].cssdNo = gopCongNo[tenLoaiMâm].daTraBan - gopCongNo[tenLoaiMâm].nhanVoKhuan;
        });
        
        let arrHtml = [];
        for (let key in gopCongNo) {
            let item = gopCongNo[key];
            arrHtml.push(`<tr class="border-b hover:bg-slate-50 transition-colors font-medium text-[11px]"><td class="p-3 font-bold text-slate-800 border-r">${key}</td><td class="p-3 text-center text-rose-600 font-bold bg-rose-50/20 border-r">${item.daTraBan}</td><td class="p-3 text-center text-sky-600 font-bold bg-sky-50/20 border-r">${item.nhanSach}</td><td class="p-3 text-center text-purple-600 font-bold bg-purple-50/20 border-r">${item.nhanVoKhuan}</td><td class="p-3 text-center text-amber-600 font-black bg-amber-50/20">${item.cssdNo > 0 ? 'Nợ ' + item.cssdNo : '-'}</td></tr>`);
        }
        if(document.getElementById("bangDonGiaoNhan")) {
            document.getElementById("bangDonGiaoNhan").innerHTML = arrHtml.length ? arrHtml.join('') : `<tr><td colspan="5" class="p-8 text-center text-slate-400 italic">Khoa chưa phát sinh công nợ luân chuyển đồ.</td></tr>`;
        }
        
        // 2. DANH SÁCH CHỜ NHẬN ĐỒ SẠCH (Từ xe trung chuyển CSSD bàn giao lên)
        const tbodyChoNhan = document.getElementById("bangChoNhanTaiKhoa"); const badgeChoNhan = document.getElementById("badgeChoNhanKhoa"); const txtNguoiXacNhan = document.getElementById("txtNguoiDungNhanHienTai");
        if(txtNguoiXacNhan) txtNguoiXacNhan.innerText = `PM-ĐD: ${loginUserCode || "Chưa Đăng Nhập"}`;
        
        let dsDangVanChuyen = tatCaDonCuaKhoa.filter(x => x.status === "ĐANG_VAN_CHUYEN"); if(badgeChoNhan) badgeChoNhan.innerText = `${dsDangVanChuyen.length} khay`;
        if(tbodyChoNhan) {
            if(dsDangVanChuyen.length === 0) { tbodyChoNhan.innerHTML = `<tr><td colspan="5" class="p-3 text-center text-slate-400 italic">Hiện không có dụng cụ nào đang chuyển về khoa.</td></tr>`; } 
            else { tbodyChoNhan.innerHTML = dsDangVanChuyen.map(khay => `<tr class="hover:bg-slate-50 transition-colors"><td class="p-2 text-center"><input type="checkbox" value="${khay.firestoreId}" class="w-3.5 h-3.5 text-sky-600 rounded border-slate-300 focus:ring-sky-500 cursor-pointer"></td><td class="p-2 font-mono font-bold text-slate-700">${khay.maMacDinh || 'N/A'}</td><td class="p-2 font-semibold text-slate-800">${khay.bo ? String(khay.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-2 text-slate-500 font-medium">${khay.nvXuatKho || '--'}</td><td class="p-2 text-center"><span class="bg-purple-50 text-purple-700 font-mono text-[10px] px-1.5 py-0.5 rounded font-bold">${khay.batchCode || 'N/A'}</span></td></tr>`).join(''); }
        }

        // 3. THEO DÕI YÊU CẦU ĐANG CHỜ CSSD CẤP PHÁT
        let dsChoCapPhat = tatCaDonCuaKhoa.filter(x => x.status === "CHO_CAP_PHAT");
        const tbodyChoCapPhat = document.getElementById("bangChoCapPhatTaiKhoa");
        if (tbodyChoCapPhat) {
            if (dsChoCapPhat.length === 0) {
                tbodyChoCapPhat.innerHTML = `<tr><td colspan="4" class="p-3 text-center text-slate-400 italic">Không có yêu cầu mượn khay mới nào đang chờ duyệt.</td></tr>`;
            } else {
                tbodyChoCapPhat.innerHTML = dsChoCapPhat.map(yc => `
                    <tr class="border-b text-xs hover:bg-amber-50/10 font-medium">
                        <td class="p-2.5 font-bold text-amber-800">${yc.bo}</td>
                        <td class="p-2.5 text-center"><span class="px-2 py-0.5 rounded-full text-[10px] font-bold bg-amber-100 text-amber-800 border border-amber-200 animate-pulse">Chờ CSSD cấp khay</span></td>
                        <td class="p-2.5 text-center font-mono text-slate-400">${yc.time}</td>
                        <td class="p-2.5 text-center text-slate-500">${yc.nvYeuCau || 'ĐD'}</td>
                    </tr>
                `).join('');
            }
        }
    }
    else if(activeTab === 'thugom') {
        let fK = document.getElementById("filterKhoaThuGom")?.value || ""; 
        let lsTG = listGiaoDich.filter(x => x.status === "CHO_THU"); 
        
        if (document.getElementById("filterKhoaThuGom")) {
            let danhSachKhoaCoDon = [...new Set(lsTG.map(x => x.khoa))].filter(Boolean); 
            let htmlOpts = '<option value="">-- Lọc theo Khoa --</option>';
            danhSachKhoaCoDon.forEach(k => { 
                htmlOpts += `<option value="${k}" ${(k === fK) ? "selected" : ""}>${k}</option>`; 
            });
            document.getElementById("filterKhoaThuGom").innerHTML = htmlOpts; 
            fK = document.getElementById("filterKhoaThuGom").value || "";
        }
        
        if (fK) lsTG = lsTG.filter(x => (x.khoa || "").toString().trim().toUpperCase() === fK.toString().trim().toUpperCase());
        if (document.getElementById("badgeSoCho")) document.getElementById("badgeSoCho").innerText = `${lsTG.length} Lệnh`;
        
        let bodyThuGom = document.getElementById("bangChoThuGom");
        if(bodyThuGom) {
            bodyThuGom.innerHTML = lsTG.map(i => {
                let tenBo = i.bo ? String(i.bo).split(" [ID:")[0].trim() : ""; 
                
                let itemsInBo = databaseExcel.filter(x => {
                    let boName = x['Tên TS (i)'] || x['Tên Bộ Dụng Cụ'] || x['Bộ dụng cụ'] || x['Dụng cụ'] || x['TÊN BỘ'] || x['Tên Bộ'] || '';
                    return String(boName).trim().toUpperCase() === String(tenBo).toUpperCase();
                });
                
                let checklistHtml = "";
                if (itemsInBo.length > 0) {
                    checklistHtml = `<div class="max-h-32 overflow-y-auto pr-1 border border-slate-200/60 rounded bg-slate-50/50 p-1.5 mt-1 flex flex-col gap-1">`;
                    itemsInBo.forEach(item => {
                        let tenDc = item['Tên TS chuẩn'] || item['Tên Dụng Cụ Chi Tiết'] || item['Tên dụng cụ chi tiết'] || item['Tên dụng cụ'] || item['Chi tiết'] || "Dụng cụ chi tiết";
                        let sl = item['Số lượng'] || item['SL'] || item['Số Lượng'] || 1;
                        
                        if (String(tenDc).trim().toUpperCase() === "NGUYÊN BỘ CẤU HÌNH CƠ SỐ") {
                            tenDc = "Dụng cụ chuẩn mâm";
                        }

                        checklistHtml += `
                            <div class="flex justify-between items-center border-b border-dashed border-slate-200 pb-0.5 last:border-none text-[11px]">
                                <span class="text-slate-700 font-semibold"><i class="fa-solid fa-circle text-[4px] text-sky-400 mr-1.5 align-middle"></i>${tenDc}</span>
                                <span class="font-bold text-sky-700 bg-sky-50 px-1.5 py-0.5 rounded border border-sky-100 text-[10px]">x${sl}</span>
                            </div>`;
                    });
                    checklistHtml += `</div>`;
                } else {
                    checklistHtml = `<div class="text-[10px] text-emerald-700 bg-emerald-50 border border-emerald-100 p-1.5 rounded font-medium mt-1"><i class="fa-solid fa-circle-check mr-1"></i>Dụng cụ lẻ (Cơ số: 1 chiếc)</div>`;
                }
                
                return `
                    <tr class="border-b border-slate-100 hover:bg-slate-50/40 transition-colors">
                        <td class="p-3 align-top">
                            <div class="font-black text-slate-700 text-[11px] uppercase tracking-wide">${i.khoa}</div>
                            ${i.ghiChu ? `<div class="text-[10px] text-rose-600 font-bold bg-rose-50 border border-rose-100 px-1.5 py-0.5 rounded mt-1.5 inline-block"><i class="fa-solid fa-triangle-exclamation mr-1"></i>${i.ghiChu}</div>` : ''}
                        </td>
                        <td class="p-3 align-top">
                            <div class="font-black text-sky-800 text-[13px] uppercase tracking-tight">${tenBo}</div>
                            <div class="text-[10px] font-mono text-slate-400 font-bold mt-0.5">MÃ SỐ ID: ${i.maMacDinh}</div>
                            ${checklistHtml}
                        </td>
                        <td class="p-3 text-center align-top font-mono font-bold text-slate-500 text-[11px]">${i.time}</td>
                        <td class="p-3 text-center align-top action-col">
                            <button onclick="moPopupKiemDem('${i.firestoreId}')" class="bg-sky-600 hover:bg-sky-700 text-white font-black px-3.5 py-2 rounded-lg shadow-sm text-[11px] uppercase tracking-wider transition-all"><i class="fa-solid fa-clipboard-check mr-1"></i>Kiểm Đếm</button>
                        </td>
                    </tr>`;
            }).join('');
        }
    }
    else if(activeTab === 'mayrua') {
        let lsCR = listGiaoDich.filter(x => x.status === "DANG_RUA");
        if(document.getElementById("badgeChoRua")) document.getElementById("badgeChoRua").innerText = `${lsCR.length} Mục`;
        
        let bodyChoRua = document.getElementById("bangChoRua");
        if(bodyChoRua) {
            bodyChoRua.innerHTML = lsCR.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="rua-checkbox"></td><td class="p-3 font-bold">${i.bo}</td><td class="p-3 text-right font-mono">${i.maMacDinh}</td></tr>`).join('');
        }
        
        let lsTR = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA");
        let bodyChoNghiemThuRua = document.getElementById("bangChoNiemThuRua");
        if(bodyChoNghiemThuRua) {
            bodyChoNghiemThuRua.innerHTML = lsTR.map(i => `<tr class="border-b"><td class="p-2.5 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="nghiemthurua-checkbox"></td><td class="p-2 text-xs font-bold">${i.bo} <span class="text-slate-400 font-mono">(${i.rua_batchCode || 'Mẻ Rửa'})</span></td></tr>`).join('');
        }
        
        let bodyLichSuRua = document.getElementById("bangLichSuRua");
        if(bodyLichSuRua) {
            const homNayMoiStr = getTodayDateStr();
            let tatCaMucCoLoRua = listGiaoDich.filter(x => x.rua_batchCode && (x.rua_ngayRua === homNayMoiStr || x.ngayTao === homNayMoiStr));
            let cacMeRuaGop = {};
            tatCaMucCoLoRua.forEach(x => {
                if(!cacMeRuaGop[x.rua_batchCode]) {
                    cacMeRuaGop[x.rua_batchCode] = {
                        batchCode: x.rua_batchCode,
                        mayRua: x.rua_mayRua || "Belimed WD250",
                        chuKy: x.rua_chuKy || "Chu trình tiêu chuẩn",
                        hoaChat: x.rua_hoaChat || "Smeg",
                        timeBatDau: x.rua_timeBatDau || "--:--",
                        ngay: x.rua_ngayRua || "",
                        soLuong: 0,
                        trangThaiMe: x.status === "TRONG_BUONG_RUA" ? "Đang rửa" : "Hoàn tất sạch"
                    };
                }
                cacMeRuaGop[x.rua_batchCode].soLuong += 1;
            });
            let danhSachMeRuaSapXep = Object.values(cacMeRuaGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));
            if(danhSachMeRuaSapXep.length === 0) {
                bodyLichSuRua.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa chạy mẻ rửa nào</td></tr>`;
            } else {
                bodyLichSuRua.innerHTML = danhSachMeRuaSapXep.map(me => `
                    <tr class="hover:bg-slate-50 transition-colors">
                        <td class="p-3">
                            <div class="flex items-center gap-2">
                                <span class="font-mono font-black text-sky-700 text-sm tracking-wider">${me.batchCode}</span>
                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "Đang rửa" ? "text-amber-600 bg-amber-50 border-amber-200" : "text-emerald-600 bg-emerald-50 border-emerald-200"}">${me.trangThaiMe}</span>
                            </div>
                            <div class="text-[11px] text-slate-500 font-medium mt-1">${me.mayRua} | ${me.chuKy}</div>
                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Hoá chất: ${me.hoaChat} | Bắt đầu: ${me.timeBatDau} (${me.ngay})</div>
                        </td>
                        <td class="p-3 text-right pr-4">
                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuong} khay</span>
                        </td>
                    </tr>`).join('');
            }
        }
    }
    else if(activeTab === 'donggoi') {
        let lsDG = listGiaoDich.filter(x => x.status === "CHO_DONG_GOI"); 
        if(document.getElementById("badgeDongGoi")) document.getElementById("badgeDongGoi").innerText = lsDG.length;
        let gDongGoi = document.getElementById("gridDongGoi");
        if(gDongGoi) {
            gDongGoi.innerHTML = lsDG.map(i => `<div class="bg-white p-3 rounded border border-slate-200 mb-2 flex justify-between items-center"><div class="flex-1"><div class="font-bold text-sky-700 text-[13px]">${i.bo}</div><div class="text-[10px] text-slate-500">Từ khoa: ${i.khoa}</div></div><button onclick="moPopupDongGoi('${i.firestoreId}')" class="bg-sky-50 text-sky-700 border border-sky-300 px-3 py-1.5 rounded text-[11px] font-black">ĐÓNG GÓI</button></div>`).join('');
        }
    }
    else if(activeTab === 'mayhap') {
        let lsCH = listGiaoDich.filter(x => x.status === "CHO_HAP"); 
        if(document.getElementById("badgeChoHap")) document.getElementById("badgeChoHap").innerText = `${lsCH.length} Mục`;
        let bodyChoHap = document.getElementById("bangChoHap");
        if(bodyChoHap) { 
            bodyChoHap.innerHTML = lsCH.map(i => `<tr class="border-b"><td class="p-3 text-center action-col"><input type="checkbox" value="${i.firestoreId}" class="hap-checkbox"></td><td class="p-3 font-bold text-xs">${i.bo}</td><td class="p-3 text-right font-mono text-xs font-bold text-sky-700">${i.maMacDinh}</td></tr>`).join(''); 
        }

        let lsNT = listGiaoDich.filter(x => x.status === "DANG_HAP"); 
        let bodyChoNghiemThu = document.getElementById("bangChoNghiệmThu") || document.getElementById("bangChoNghiemThu");
        
        if(bodyChoNghiemThu) { 
            if (lsNT.length === 0) {
                bodyChoNghiemThu.innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Không có mâm/mẻ nào đang trong lò chờ duyệt.</td></tr>`;
            } else {
                bodyChoNghiemThu.innerHTML = lsNT.map(i => {
                    let tenBoClean = i.bo ? String(i.bo).split(" [ID:")[0] : "Chưa rõ mâm";
                    return `
                        <tr class="border-b hover:bg-amber-50/40 transition-colors">
                            <td class="p-2.5 text-center action-col">
                                <input type="checkbox" value="${i.firestoreId}" class="nghiemthu-checkbox w-4 h-4 text-amber-600 rounded cursor-pointer" data-hasbi="${i.hasBI ? 'true' : 'false'}" onchange="kiemTraQuyenDuyetMeHap()">
                            </td>
                            <td class="p-2 text-xs">
                                <div class="font-black text-slate-800">${tenBoClean} <span class="text-[10px] font-mono text-sky-700">(${i.maMacDinh || 'N/A'})</span></div>
                                <div class="flex items-center gap-2 mt-0.5">
                                    <span class="text-[10px] font-mono font-bold text-rose-700 bg-rose-50 px-1 rounded border border-rose-100">Lô: ${i.batchCode || 'N/A'}</span>
                                    ${i.hasBI ? '<span class="px-1.5 py-0.5 bg-amber-100 text-amber-800 rounded text-[9px] font-black border border-amber-300 flex items-center gap-1"><i class="fa-solid fa-flask"></i> CHỜ BI</span>' : ''}
                                </div>
                            </td>
                        </tr>`;
                }).join(''); 
            }
        }
        kiemTraQuyenDuyetMeHap();

        if(document.getElementById("bangLichSuHap")) {
            const homNayMoiStr = getTodayDateStr(); 
            let tatCaMucCoLo = listGiaoDich.filter(x => x.batchCode && (x.ngayHapRealtime === homNayMoiStr || x.ngayTao === homNayMoiStr)); 
            let cacMeHapGop = {};
            tatCaMucCoLo.forEach(x => {
                if(!cacMeHapGop[x.batchCode]) { 
                    cacMeHapGop[x.batchCode] = { 
                        batchCode: x.batchCode, 
                        loaiHap: x.thongTinLoHap?.loaiHap || "Hấp hơi nước", 
                        chuKyNhiet: x.thongTinLoHap?.chuKyNhiet || "134°C - 4 phút", 
                        apSuat: x.thongTinLoHap?.apSuat || "N/A", 
                        thoiGian: x.thongTinLoHap?.thoiGianBatDau || x.time || "--:--", 
                        ngay: x.ngayHapRealtime || x.ngayTao || "", 
                        soLuongKhay: 0, 
                        trangThaiMe: x.status === "DANG_HAP" ? "ĐANG CHẠY LÒ" : "HOÀN THÀNH" 
                    }; 
                }
                cacMeHapGop[x.batchCode].soLuongKhay += 1;
            });
            let danhSachMeHapSapXep = Object.values(cacMeHapGop).sort((a, b) => String(b.batchCode).localeCompare(String(a.batchCode)));
            if(danhSachMeHapSapXep.length === 0) { 
                document.getElementById("bangLichSuHap").innerHTML = `<tr><td colspan="2" class="p-4 text-center text-slate-400 italic">Hôm nay chưa có mẻ hấp nào</td></tr>`; 
            } else { 
                document.getElementById("bangLichSuHap").innerHTML = danhSachMeHapSapXep.map(me => `
                    <tr class="hover:bg-slate-50 transition-colors border-b border-slate-100">
                        <td class="p-3">
                            <div class="flex items-center gap-2">
                                <span class="font-mono font-black text-rose-700 text-sm tracking-wider">${me.batchCode}</span>
                                <span class="px-2 py-0.5 text-[9px] font-black uppercase rounded ${me.trangThaiMe === "ĐANG CHẠY LÒ" ? "text-purple-700 bg-purple-100 border border-purple-200 animate-pulse" : "text-emerald-700 bg-emerald-100 border border-emerald-200"}">${me.trangThaiMe}</span>
                            </div>
                            <div class="text-[11px] text-slate-600 font-medium mt-1">${me.loaiHap} | ${me.chuKyNhiet} | Áp suất: ${me.apSuat} Bar</div>
                            <div class="text-[10px] text-slate-400 font-mono mt-0.5">Bắt đầu: ${me.thoiGian} (${me.ngay})</div>
                        </td>
                        <td class="p-3 text-right pr-4">
                            <span class="font-black text-slate-700 bg-slate-100 border border-slate-200 px-2.5 py-1 rounded-md text-xs">${me.soLuongKhay} khay</span>
                        </td>
                    </tr>`).join(''); 
            }
        }
    }
    else if(activeTab === 'khovokhuan') {
        let uniqueKhoaSanCo = [...new Set(listGiaoDich.map(x => x.khoa))].filter(Boolean); const selKhoaXuat = document.getElementById("xuat_selKhoa");
        if(selKhoaXuat) {
            let currentSelected = selKhoaXuat.value; let htmlOpts = '<option value="">-- Chọn Khoa Muốn Trả Đồ --</option>';
            uniqueKhoaSanCo.forEach(k => { htmlOpts += `<option value="${k}" ${k === currentSelected ? 'selected' : ''}>${k}</option>`; });
            selKhoaXuat.innerHTML = htmlOpts;
        }

        let lsXK = listGiaoDich.filter(x => x.status === "CHO_XUAT").sort((a, b) => a.id - b.id);
        renderKhoVoKhuan(lsXK);

        let dsYeuCau = listGiaoDich.filter(x => x.status === "CHO_CAP_PHAT");
        let vungYeuCau = document.getElementById("vungYeuCauCapPhat");
        if (!vungYeuCau) {
            let parentContainer = document.getElementById("tab-khovokhuan");
            if (parentContainer) {
                let divYc = document.createElement("div");
                divYc.id = "vungYeuCauCapPhat";
                divYc.className = "mb-6 bg-amber-50/70 border border-amber-200 rounded-xl p-4 shadow-sm";
                parentContainer.insertBefore(divYc, parentContainer.firstChild);
                vungYeuCau = divYc;
            }
        }
        
        if (vungYeuCau) {
            if (dsYeuCau.length === 0) {
                vungYeuCau.innerHTML = `<h3 class="font-black text-amber-950 text-xs flex items-center gap-1.5"><i class="fa-solid fa-bell-slash"></i> HIỆN KHÔNG CÓ YÊU CẤP PHÁT ĐANG CHỜ</h3>`;
            } else {
                vungYeuCau.innerHTML = `
                    <h3 class="font-black text-amber-900 text-xs mb-2.5 flex items-center gap-1.5 animate-pulse">
                        <i class="fa-solid fa-bell text-rose-500 animate-bounce"></i> CÓ ${dsYeuCau.length} YÊU CẦU CẤP PHÁT TỪ CÁC KHOA LÂM SÀNG
                    </h3>
                    <div class="flex flex-col gap-2 max-h-48 overflow-y-auto">
                        ${dsYeuCau.map(yc => `
                            <div class="bg-white p-3 rounded-lg border border-amber-200 shadow-sm flex justify-between items-center text-xs">
                                <div>
                                    <span class="font-black text-amber-800 uppercase">[KHOA: ${yc.khoa}]</span> 
                                    Yêu cầu mâm: <span class="font-bold text-slate-800">${yc.bo}</span>
                                    <span class="text-[10px] text-slate-400 block mt-0.5">Thời gian: ${yc.time} - Người yêu cầu: ${yc.nvYeuCau || 'ĐD'}</span>
                                </div>
                                <div class="flex items-center gap-2">
                                    <button onclick="duyetCapPhatTậpTrung('${yc.firestoreId}', '${yc.bo}', '${yc.khoa}')" class="bg-amber-600 hover:bg-amber-700 text-white font-black px-3 py-2 rounded-lg text-[10px] uppercase shadow-sm flex items-center gap-1.5 transition-all">
                                        <i class="fa-solid fa-truck-ramp-box"></i>Cấp phát khay
                                    </button>
                                </div>
                            </div>
                        `).join('')}
                    </div>`;
            }
        }
        renderGioHangXuat();
    }
    else if(activeTab === 'quanlykho') {
        let fK = document.getElementById("inv_filterKhoa") ? document.getElementById("inv_filterKhoa").value : ""; 
        let uniqueKhoa = [...new Set(listGiaoDich.map(x=>x.khoa))].filter(Boolean);
        if(document.getElementById("inv_filterKhoa")) { document.getElementById("inv_filterKhoa").innerHTML = '<option value="">-- Tất cả --</option>' + uniqueKhoa.map(k=>`<option value="${k}" ${k===fK?'selected':''}>${k}</option>`).join(''); }
        let uniqueIDs = [...new Set(listGiaoDich.map(x=>x.maMacDinh))]; let arrHtml = []; const ngayHomNay = new Date(); ngayHomNay.setHours(0,0,0,0); 
        uniqueIDs.forEach(ma => {
            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a,b) => b.id - a.id)[0]; if(!currentTrans || !ma) return;
            let viTriCode = currentTrans.status; let khoaGiữ = currentTrans.khoa; if (fK && (khoaGiữ || "").toString().trim().toUpperCase() !== fK.toString().trim().toUpperCase()) return;
            let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";
            if(viTriCode === "HOAN_TAT") { viTriText = `Sẵn sàng tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 
            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã sử dụng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }
            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang vận chuyển`; viTriColor = "bg-purple-100 text-purple-800 animate-pulse"; }
            else if(viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }
            let hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-slate-100 text-slate-500">An toàn</span>`; let dongColorClass = ""; 
            if (currentTrans.hsd && (viTriCode === "CHO_XUAT" || viTriCode === "HOAN_TAT")) {
                const ngayHsd = new Date(currentTrans.hsd); ngayHsd.setHours(0,0,0,0); const soNgayConLai = Math.ceil((ngayHsd.getTime() - ngayHomNay.getTime()) / (1000 * 60 * 60 * 24));
                if (soNgayConLai < 0) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-rose-600 text-white">QUÁ HẠN</span>`; dongColorClass = "bg-rose-50/70 border-l-4 border-l-rose-500 font-bold"; } 
                else if (soNgayConLai <= 3) { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-black bg-amber-500 text-white">HẠN < ${soNgayConLai} NGÀY</span>`; dongColorClass = "bg-amber-50/40 border-l-4 border-l-amber-500"; } 
                else { hsdBadget = `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-emerald-100 text-emerald-800">Hạn ${soNgayConLai} ngày</span>`; }
            }
            arrHtml.push(`<tr class="border-b border-slate-100 ${dongColorClass}"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-slate-500 text-[11px]">${khoaGiữ}</td><td class="p-3 text-center"><span class="px-2.5 py-1 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-mono font-bold text-slate-400">${currentTrans.batchCode || 'N/A'}</td><td class="p-3 text-center">${hsdBadget}</td></tr>`);
        });
        if(document.getElementById("bangTonKhoTe")) document.getElementById("bangTonKhoTe").innerHTML = arrHtml.join('');
    }
    else if(activeTab === 'danhmuc') {
        const tbody = document.getElementById("bangDanhMucTong"); if (!tbody) return;
        let uniqueIDs = [...new Set(listGiaoDich.map(x => x.maMacDinh))].filter(Boolean); let arrHtml = [];
        uniqueIDs.forEach(ma => {
            let allTrans = listGiaoDich.filter(x => x.maMacDinh === ma); let currentTrans = allTrans.sort((a, b) => b.id - a.id)[0]; if (!currentTrans) return;
            let viTriCode = currentTrans.status; let viTriText = "Kho vô khuẩn"; let viTriColor = "bg-teal-100 text-teal-800";
            if (viTriCode === "HOAN_TAT") { viTriText = `Tại Khoa`; viTriColor = "bg-emerald-100 text-emerald-800"; } 
            else if (viTriCode === "DA_SU_DUNG") { viTriText = `Đã dùng (BN)`; viTriColor = "bg-rose-100 text-rose-800"; }
            else if (viTriCode === "ĐANG_VAN_CHUYEN") { viTriText = `Đang đi đường`; viTriColor = "bg-purple-100 text-purple-800"; }
            else if (viTriCode !== "CHO_XUAT") { viTriText = "Xử lý tại CSSD"; viTriColor = "bg-amber-100 text-amber-800"; }
            let chuKyLo = listGiaoDich.filter(x => x.maMacDinh === ma && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT" || x.status === "DA_SU_DUNG" || x.status === "ĐANG_VAN_CHUYEN")).length;
            arrHtml.push(`<tr class="border-b border-slate-100 font-medium"><td class="p-3 font-mono text-sky-700 font-bold">${ma}</td><td class="p-3 font-bold text-slate-800">${currentTrans.bo ? String(currentTrans.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 text-center"><span class="px-2.5 py-0.5 rounded text-[10px] font-bold ${viTriColor}">${viTriText}</span></td><td class="p-3 text-center font-black text-amber-700 bg-amber-50/50">${chuKyLo} lần lễ</td></tr>`);
        });
        tbody.innerHTML = arrHtml.join('');
    }
    else if(activeTab === 'lichsuluanchuyen') {
        const tbody = document.getElementById("bangLichSuHanhTrinhGoc");
        if(tbody) {
            tbody.innerHTML = listGiaoDich.map(x => {
                let statusBadge = `<span class="px-2 py-0.5 bg-slate-100 text-slate-700 font-bold rounded text-[10px]">${x.status}</span>`;
                if(x.status === "HOAN_TAT") statusBadge = `<span class="px-2 py-0.5 bg-emerald-100 text-emerald-800 font-black rounded text-[10px]">ĐÃ BÀN GIAO</span>`;
                else if(x.status === "CHO_XUAT") statusBadge = `<span class="px-2 py-0.5 bg-teal-100 text-teal-800 font-bold rounded text-[10px]">KHO VÔ KHUẨN</span>`;
                else if(x.status === "DANG_HAP") statusBadge = `<span class="px-2 py-0.5 bg-purple-100 text-purple-800 font-bold rounded text-[10px]">ĐANG HẤP LÒ</span>`;
                else if(x.status === "DA_SU_DUNG") statusBadge = `<span class="px-2 py-0.5 bg-rose-600 text-white font-black rounded text-[10px]">SỬ DỤNG BN</span>`;
                else if(x.status === "ĐANG_VAN_CHUYEN") statusBadge = `<span class="px-2 py-0.5 bg-purple-500 text-white font-bold rounded text-[10px] animate-pulse">ĐANG VẬN CHUYỂN</span>`;
                else if(x.status === "DANG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-blue-100 text-blue-700 font-bold rounded text-[10px]">HÀNG ĐỢI RỬA</span>`;
                else if(x.status === "TRONG_BUONG_RUA") statusBadge = `<span class="px-2 py-0.5 bg-cyan-600 text-white font-bold rounded text-[10px]">ĐANG RỬA MÁY</span>`;
                else if(x.status === "CHO_CAP_PHAT") statusBadge = `<span class="px-2 py-0.5 bg-amber-100 text-amber-800 font-bold rounded text-[10px] border border-amber-200">CHỜ CẤP PHÁT</span>`;
                
                let fontColor = ""; let loGoc = listGiaoDich.find(m => m.batchCode === x.batchCode && m.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64); 
                let anhBase64 = loGoc?.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64 || x.thongTinLoHap?.giamSatChatLuong?.minhChungAnhBase64;
                if (anhBase64) fontColor = `<br><span onclick="handleViewAnhBiMoi('${x.firestoreId}')" class="text-[10px] text-emerald-600 bg-emerald-50 border border-emerald-300 rounded px-1 cursor-pointer font-bold mt-1 inline-block"><i class="fa-solid fa-image mr-1"></i>Xem ảnh BI</span>`;
                
                let kpiHtml = x.ketQuaGiamSatKpi ? `<br><span class="text-[9px] px-1 rounded font-black border mt-1 inline-block ${x.ketQuaGiamSatKpi === "ĐẠT" ? "text-emerald-700 bg-emerald-50 border-emerald-200" : "text-rose-700 bg-rose-50 border-rose-200"}">KPI: ${x.ketQuaGiamSatKpi} (${x.thoiGianTinhKpiPhut}m)</span>` : "";
                let thongTinKemTheo = x.nvXuatKho || '<span class="text-slate-300 font-normal">Chưa xuất</span>';
                if (x.nguoiKyNhanKhoa) thongTinKemTheo += ` ➔ <span class="text-emerald-700 font-semibold">[ĐD: ${x.nguoiKyNhanKhoa}]</span>`;
                if (x.status === "DA_SU_DUNG" && x.thongTinBenhNhan) thongTinKemTheo = `<span class="text-teal-700 font-black">BN: ${x.thongTinBenhNhan.thongTinTimKiemBN}</span>`;

                return `<tr class="border-b text-xs hover:bg-slate-50 transition-colors"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : 'N/A'}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3 text-center">${statusBadge}${kpiHtml}</td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/30">${x.batchCode || 'N/A'}${fontColor}</td><td class="p-3 text-center font-bold text-sky-800 bg-sky-50/40">${thongTinKemTheo}</td><td class="p-3 text-center text-slate-400 font-mono text-[11px]">${x.ngayTao || ''} ${x.time || ''}</td></tr>`;
            }).join('');
        }
    }
    else if(activeTab === 'tracuu') {
        let safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");
        if (!safeTbody) {
            let parentContainer = document.getElementById("tab-tracuu") || document.querySelector('.bg-white.p-6.rounded-xl.shadow-md');
            if(parentContainer) {
                let existingTableZone = document.getElementById("vung-ket-qua-tu-dong");
                if(!existingTableZone) {
                    let tableWrapper = document.createElement("div"); tableWrapper.id = "vung-ket-qua-tu-dong"; tableWrapper.className = "mt-6 bg-white border border-slate-200 rounded-xl p-4 shadow-sm";
                    tableWrapper.innerHTML = `<h3 class="font-black text-slate-800 text-sm mb-3 text-sky-800"><i class="fa-solid fa-list-check mr-2"></i>DANH SÁCH MÂM DỤNG CỤ TRONG MẺ TRUY VẾT</h3><div class="overflow-x-auto"><table class="w-full text-left border-collapse"><thead><tr class="bg-slate-100 text-[11px] font-bold text-slate-600 uppercase border-b"><th class="p-3">Mã ID Khay</th><th class="p-3">Tên Bộ Dụng Cụ</th><th class="p-3">Khoa Sử Dụng</th><th class="p-3">Trạng thái</th><th class="p-3 text-center">Mã Lô Hấp</th><th class="p-3 text-center">Thời Gian Kích Hoạt</th></tr></thead><tbody id="bangLichSuTruyXuatAdmin"></tbody></table></div>`;
                    parentContainer.appendChild(tableWrapper); safeTbody = document.getElementById("bangLichSuTruyXuatAdmin");
                }
            }
        }
        let searchInp = maLoTruyVetToanCuc || ""; if (!searchInp && document.getElementById("inp_searchBatch")) { searchInp = document.getElementById("inp_searchBatch").value.trim(); }
        if(safeTbody) {
            if (!searchInp) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-slate-400 italic">Vui lòng nhập mã mẻ hấp và nhấn "TRA CỨU KHẨN CẤP"...</td></tr>`; return; }
            let dataFiltered = listGiaoDich.filter(x => x.batchCode && String(x.batchCode).toUpperCase().includes(searchInp.toUpperCase()));
            if(dataFiltered.length === 0) { safeTbody.innerHTML = `<tr><td colspan="6" class="p-4 text-center text-rose-500 italic font-bold">Không tìm thấy dữ liệu: ${searchInp}</td></tr>`; } 
            else { safeTbody.innerHTML = dataFiltered.map(x => `<tr class="border-b text-xs hover:bg-slate-50 font-medium"><td class="p-3 font-mono font-bold text-sky-700">${x.maMacDinh || 'N/A'}</td><td class="p-3 font-bold text-slate-800">${x.bo ? String(x.bo).split(" [ID:")[0] : "N/A"}</td><td class="p-3 font-semibold text-slate-500">${x.khoa || 'N/A'}</td><td class="p-3"><span class="px-2 py-0.5 rounded text-[10px] ${x.status === "HOAN_TAT" ? "bg-emerald-100 text-emerald-800 font-bold" : "bg-teal-100 text-teal-800"}">${x.status}</span></td><td class="p-3 text-center font-mono font-black text-rose-700 bg-rose-50/40">${x.batchCode || 'N/A'}</td><td class="p-3 text-center text-slate-400 font-mono">${x.ngayTao || ''} ${x.time || ''}</td></tr>`).join(''); }
        }
    }
    else if(activeTab === 'performance') { renderKpiPerformanceGoc(); }
    else if(activeTab === 'dashboard_tv') { renderDashboardTiviRealtime(); }
}

function renderKhoVoKhuan(listData) {
    const tbody = document.getElementById("bangKhoVoKhuan");
    if (!tbody) return;

    if (!listData || listData.length === 0) {
        tbody.innerHTML = `<tr><td colspan="6" class="p-6 text-center text-slate-400 italic">Kho vô khuẩn hiện tại trống.</td></tr>`;
        return;
    }

    tbody.innerHTML = listData.map(item => `
        <tr class="hover:bg-slate-50 transition-colors">
            <td class="p-3.5 font-bold text-slate-800 text-xs">${item.bo ? String(item.bo).split(" [ID:")[0] : 'N/A'}</td>
            <td class="p-3.5 font-mono font-black text-sky-700 text-xs">${item.maMacDinh || '--'}</td>
            <td class="p-3.5 text-center font-semibold text-slate-600 text-xs">${item.khoa || 'Tập trung'}</td>
            <td class="p-3.5 text-center"><span class="bg-emerald-100 text-emerald-800 font-bold px-2.5 py-1 rounded-full text-[11px]">${item.viTriKe || 'Kệ A1'}</span></td>
            <td class="p-3.5 text-center font-mono font-bold text-xs text-rose-600">${item.hsd ? new Date(item.hsd).toLocaleDateString('vi-VN') : 'An toàn'}</td>
            <td class="p-2 text-center">
                <button onclick="xuatKhoXoayVong('${item.maMacDinh}')" 
                        class="min-h-[44px] px-4 bg-emerald-600 active:bg-emerald-700 text-white font-bold rounded-lg text-xs shadow-sm inline-flex items-center gap-1.5 transition-transform active:scale-95">
                    <i class="fa-solid fa-cart-flatbed"></i>
                    <span>Xuất Khay</span>
                </button>
            </td>
        </tr>
    `).join('');
}

// =========================================================================
// VÁ LỖI CÁC HÀM TRUY VẾT & ĐỒNG BỘ HIỆU SUẤT TV MÀN HÌNH
// =========================================================================
function truyVetTheoMaBatch() {
    const inp = document.getElementById("inp_searchBatch");
    if (inp) {
        maLoTruyVetToanCuc = inp.value.trim();
        playSound('success');
        callRender();
    }
}

function clearTruyVetBatch() {
    const inp = document.getElementById("inp_searchBatch");
    if (inp) inp.value = "";
    maLoTruyVetToanCuc = "";
    callRender();
}

function resetDuLieuKet() {
    if (confirm("Anh Hùng có chắc chắn muốn giải phóng các mâm đang xử lý dở dang (chưa Hoàn tất / Chưa dùng) về trạng thái Chờ thu gom?")) {
        let p = [];
        listGiaoDich.forEach(x => {
            if (x.status !== "HOAN_TAT" && x.status !== "DA_SU_DUNG") {
                p.push(db.collection("phieuGiaoNhan").doc(x.firestoreId).update({ status: "CHO_THU" }));
            }
        });
        Promise.all(p).then(() => {
            playSound('success');
            showToast("Đã giải phóng dữ liệu mâm kẹt thành công!", "success");
            callRender();
        });
    }
}

function renderKpiPerformanceGoc() {
    const tbody = document.getElementById("bangHieuSuatKTV");
    if (!tbody) return;
    let kpiGop = {};
    listGiaoDich.forEach(x => {
        let ktv = x.ktvThuGom || x.nvXuatKho || "CSSD_CHUNG";
        if (!kpiGop[ktv]) kpiGop[ktv] = { code: ktv, ten: ktv, dat: 0, tong: 0 };
        if (x.ketQuaGiamSatKpi === "ĐẠT") kpiGop[ktv].dat += 1;
        if (x.ketQuaGiamSatKpi) kpiGop[ktv].tong += 1;
    });
    let sortedKpi = Object.values(kpiGop).sort((a,b) => b.dat - a.dat);
    if(sortedKpi.length === 0) {
        tbody.innerHTML = `<tr><td colspan="4" class="p-4 text-center italic text-slate-400">Hôm nay chưa có dữ liệu mẻ KPI nào.</td></tr>`;
    } else {
        tbody.innerHTML = sortedKpi.map((item, idx) => `
            <tr class="border-b text-xs hover:bg-slate-50 font-medium">
                <td class="p-3 text-center font-bold">${idx + 1}</td>
                <td class="p-3 font-mono font-bold text-sky-700">${item.code}</td>
                <td class="p-3 font-bold text-slate-800">${item.ten}</td>
                <td class="p-3 text-center"><span class="bg-emerald-100 text-emerald-800 px-2.5 py-1 rounded font-black">${item.dat}/${item.tong} Mẻ Đạt KPI</span></td>
            </tr>
        `).join('');
    }
}

function renderDashboardTiviRealtime() {
    let meRuaInDay = listGiaoDich.filter(x => x.rua_batchCode).length;
    let meHapInDay = listGiaoDich.filter(x => x.batchCode).length;
    let dangRua = listGiaoDich.filter(x => x.status === "TRONG_BUONG_RUA").length;
    let dangHap = listGiaoDich.filter(x => x.status === "DANG_HAP").length;
    let sanSangXuat = listGiaoDich.filter(x => x.status === "CHO_XUAT").length;
    
    const today = new Date(); today.setHours(0,0,0,0);
    let quaHan = 0;
    listGiaoDich.forEach(x => {
        if (x.hsd && (x.status === "CHO_XUAT" || x.status === "HOAN_TAT")) {
            if (new Date(x.hsd) < today) quaHan++;
        }
    });

    if (document.getElementById('tv_meRua')) document.getElementById('tv_meRua').innerText = meRuaInDay;
    if (document.getElementById('tv_meHap')) document.getElementById('tv_meHap').innerText = meHapInDay;
    if (document.getElementById('tv_meHap2')) document.getElementById('tv_meHap2').innerText = meHapInDay;
    if (document.getElementById('tv_dangRua')) document.getElementById('tv_dangRua').innerText = dangRua;
    if (document.getElementById('tv_dangHap')) document.getElementById('tv_dangHap').innerText = dangHap;
    if (document.getElementById('tv_khoVoKhuan')) document.getElementById('tv_khoVoKhuan').innerText = sanSangXuat;
    if (document.getElementById('tv_canhBaoHsd')) document.getElementById('tv_canhBaoHsd').innerText = quaHan;
}

function renderAdminInterface() {
    if (document.getElementById("cfg_pinAdmin")) document.getElementById("cfg_pinAdmin").value = thongTinMatKhauAdmin.adminPIN || "";
    if (document.getElementById("cfg_pinCSSD")) document.getElementById("cfg_pinCSSD").value = thongTinMatKhauAdmin.cssdPIN || "";
    if (document.getElementById("cfg_pinGuest")) document.getElementById("cfg_pinGuest").value = thongTinMatKhauAdmin.guestPIN || "";
    
    const MatrixRoles = ["CSSD", "KHOA", "GUEST"];
    
    // Đã loại bỏ hoàn toàn các số thứ tự 1., 2., 3.... khỏi tên tab
    const MatrixTabs = [
        { id: "khoaphong", name: "Cổng Báo Trả Đồ" },
        { id: "thugom", name: "Xe Thu Gom" },
        { id: "mayrua", name: "Quản Lý Mẻ Rửa" },
        { id: "donggoi", name: "Làm Sạch & Đóng Gói" },
        { id: "mayhap", name: "Quản Lý Mẻ Hấp" },
        { id: "khovokhuan", name: "Kho Vô Khuẩn" },
        { id: "quanlykho", name: "Tồn Kho Toàn Viện" },
        { id: "danhmuc", name: "Giám Sát Tuổi Thọ Dụng Cụ" },
        { id: "lichsuluanchuyen", name: "Nhật Ký Luân Chuyển" },
        { id: "tracuu", name: "Truy xuất mẻ hấp" },
        { id: "performance", name: "Hiệu Suất KPI" },
        { id: "dashboard_tv", name: "Màn Hình Tivi" }
    ];
    
    let htmlMatrix = "";
    MatrixTabs.forEach(tab => {
        htmlMatrix += `<tr class="border-b font-medium text-xs hover:bg-slate-50"><td class="p-3 font-bold text-slate-700">${tab.name}</td>`;
        MatrixRoles.forEach(role => {
            let isChecked = (cauHinhGiaoDien[role] && cauHinhGiaoDien[role].includes(tab.id)) ? "checked" : "";
            htmlMatrix += `<td class="p-3 text-center"><input type="checkbox" ${isChecked} onchange="updateGiaoDienMatrix('${role}', '${tab.id}', this)" class="w-4 h-4 rounded text-sky-600 focus:ring-sky-500"></td>`;
        });
        htmlMatrix += `</tr>`;
    });
    const tbodyMatrix = document.getElementById("bodyMaTranGiaoDien"); if (tbodyMatrix) tbodyMatrix.innerHTML = htmlMatrix;
    const trKhoa = document.getElementById("bangPhanQuyenKhoa");
    if(trKhoa) {
        if(danhSachKhoa.length === 0) { trKhoa.innerHTML = `<tr><td colspan="3" class="p-4 text-center text-rose-500 font-bold italic">Danh sách Khoa trống.</td></tr>`; } 
        else { trKhoa.innerHTML = danhSachKhoa.map((k, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-slate-700 text-[11px]">${k.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-rose-600 text-sm bg-rose-50/50">${k.pin || '123'}</td><td class="p-3 text-center"><div class="flex items-center justify-center gap-2"><input type="text" id="pin-khoa-${index}" placeholder="PIN" class="w-24 p-1.5 text-center border border-slate-300 rounded text-xs font-bold"><button onclick="updatePINTrựcTiep(${index}, '${k.ten}')" class="bg-sky-600 text-white font-bold py-1.5 px-3 rounded shadow text-[10px]">ĐỔI PIN</button></div></td></tr>`).join(''); }
    }
    const tbKtv = document.getElementById("bangNhanVienCssd");
    if(tbKtv) {
        if(danhSachKtvCssd.length === 0) { tbKtv.innerHTML = `<tr><td colspan="4" class="p-4 text-center text-slate-400 italic">Chưa có Nhân viên.</td></tr>`; }
        else { tbKtv.innerHTML = danhSachKtvCssd.map((nv, index) => `<tr class="border-b hover:bg-slate-50"><td class="p-3 font-black text-sky-700 text-xs">${nv.code}</td><td class="p-3 font-bold text-slate-700 text-[11px]">${nv.ten}</td><td class="p-3 text-center border-x border-slate-200 font-mono font-black text-sky-600 text-sm bg-sky-50/50">${nv.pin}</td><td class="p-3 text-center"><button onclick="xaoKtvCssd('${nv.code}')" class="text-rose-600 font-bold text-[10px]"><i class="fa-solid fa-trash-can mr-1"></i>XÓA</button></td></tr>`).join(''); }
    }
}

// =========================================================================
// 14. XỬ LÝ ĐỌC FILE EXCEL ĐA SHEET
// =========================================================================
document.addEventListener("DOMContentLoaded", () => {
    const fileInput = document.getElementById("excelFileInput");
    if (fileInput) {
        fileInput.addEventListener("change", (e) => {
            const file = e.target.files[0];
            if (!file) return;

            const reader = new FileReader();
            reader.onload = function(e) {
                try {
                    const data = new Uint8Array(e.target.result);
                    const workbook = XLSX.read(data, { type: 'array' });
                    
                    let updateData = {};
                    let logs = [];

                    workbook.SheetNames.forEach(sheetName => {
                        const worksheet = workbook.Sheets[sheetName];
                        const jsonData = XLSX.utils.sheet_to_json(worksheet);
                        if (jsonData.length === 0) return;

                        const firstRow = jsonData[0];
                        const keys = Object.keys(firstRow).map(k => k.trim().toUpperCase());

                        const coCotMaDC = keys.includes("MÃ DC");
                        if (coCotMaDC) {
                            let mapKhoa = {};
                            danhSachKhoa.forEach(k => {
                                mapKhoa[k.ten.trim().toUpperCase()] = {
                                    ten: k.ten.trim().toUpperCase(),
                                    pin: k.pin || "123",
                                    danhSachBo: []
                                };
                            });

                            jsonData.forEach(row => {
                                let rawKeyKhoa = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS (I)");
                                let rawKeyBo = Object.keys(row).find(k => k.trim().toUpperCase() === "TÊN TS CHUẨN");
                                let rawKeyMa = Object.keys(row).find(k => k.trim().toUpperCase() === "MÃ DC");

                                let tenKhoa = rawKeyKhoa ? row[rawKeyKhoa] : null;
                                let tenBo = rawKeyBo ? row[rawKeyBo] : null;
                                let maKhay = rawKeyMa ? row[rawKeyMa] : null;

                                if (tenKhoa && tenBo) {
                                    let keyKhoa = String(tenKhoa).trim().toUpperCase();
                                    let cleanBo = String(tenBo).trim().toUpperCase();
                                    let cleanMa = maKhay ? String(maKhay).trim().toUpperCase() : "CHUA_CO_ID";
                                    let chuoiDinhDangKhay = `${cleanBo} [ID:${cleanMa}]`;

                                    if (!mapKhoa[keyKhoa]) {
                                        mapKhoa[keyKhoa] = { ten: keyKhoa, pin: "123", danhSachBo: [] };
                                    }
                                    if (!mapKhoa[keyKhoa].danhSachBo.includes(chuoiDinhDangKhay)) {
                                        mapKhoa[keyKhoa].danhSachBo.push(chuoiDinhDangKhay);
                                    }
                                }
                            });

                            updateData.danhSachKhoa = Object.values(mapKhoa);
                            logs.push(`Cập nhật định biên cơ số cho ${updateData.danhSachKhoa.length} Khoa/Phòng`);
                        }

                        const coCotMaAesculap = keys.includes("AB 120/12");
                        if (coCotMaAesculap) {
                            updateData.databaseExcel = jsonData;
                            logs.push(`Nạp thành công ${jsonData.length} linh kiện chi tiết mâm Aesculap`);
                        }
                    });

                    if (Object.keys(updateData).length === 0) {
                        return showToast("Không tìm thấy cấu trúc dữ liệu phù hợp (Cột 'MÃ DC' hoặc 'AB 120/12') trong file Excel up lên!", "error");
                    }

                    db.collection("heThongDanhMuc").doc("danhMucTongPhuongNam").update(updateData)
                    .then(() => {
                        playSound('success');
                        showToast(`Đồng bộ thành công: ${logs.join(" | ")}!`, "success");
                        fileInput.value = ""; 
                        callRender();
                    }).catch(err => {
                        console.error("Lỗi cập nhật Firestore:", err);
                        showToast("Thất bại khi đẩy dữ liệu lên Cloud Firestore!", "error");
                    });

                } catch (err) {
                    console.error("Lỗi FileReader:", err);
                    showToast("Định dạng file không được hỗ trợ hoặc bị lỗi!", "error");
                }
            };
            reader.readAsArrayBuffer(file);
        });
    }
});
app.js
Đang hiển thị app.js.
